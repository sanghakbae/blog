import type { SeedPost } from './types'

/** 770~779 — 코드를 읽고 고치는 자리 */
export const posts78: SeedPost[] = [
  {
    slug: 'code-review-security-focus',
    title: '코드 리뷰에서 무엇을 봐야 할까',
    body: `모든 코드를 보안 관점으로 꼼꼼히 보기는 어렵다. 어디를 집중해서 볼지 정해 두면 같은 시간에 더 많은 결함을 잡을 수 있고, 리뷰어마다 보는 기준이 달라지는 문제도 줄어든다.

## 어디를 먼저 보나

| 영역 | 이유 |
| --- | --- |
| 인증·인가 코드 | 영향 큼 |
| 입력 처리 | 삽입 경로 |
| 파일·외부 호출 | 경계 |
| 암호 관련 | 실수 잦음 |
| 권한 판단 | 누락 잦음 |

권한 판단이 빠지는 결함은 자동 도구가 잘 찾지 못한다. 사람이 봐야 하는 영역이므로 리뷰 시간을 여기에 쓴다.

![리뷰 우선순위](/img/posts/code-review-security-focus.svg)

## 무엇을 묻나

| 질문 | 목적 |
| --- | --- |
| 이 값은 어디서 오나 | 신뢰 여부 |
| 누가 이것을 할 수 있나 | 권한 |
| 실패하면 어떻게 되나 | 오류 처리 |
| 이 기록에 무엇이 남나 | 노출 |

값이 어디서 오는지 묻는 것만으로 많은 것이 드러난다. 사용자 입력이 몇 단계를 거쳐 도달하는 경우 중간에 검증이 빠진 자리가 보인다.

## 자동 검사와 나눈다

| 대상 | 주체 |
| --- | --- |
| 알려진 패턴 | 도구 |
| 의존성 취약점 | 도구 |
| 비밀값 노출 | 도구 |
| 업무 로직 | 사람 |
| 권한 설계 | 사람 |

도구가 찾을 수 있는 것을 사람이 보는 것은 낭비다. 도구 결과를 먼저 붙이고 사람은 그 밖의 것을 본다.

![도구와 사람의 역할](/img/posts/code-review-security-focus-2.svg)

## 변경 범위를 좁게 본다

\`\`\`bash
# 보안에 민감한 경로가 변경됐는지
git diff --name-only origin/main...HEAD \
  | grep -E '(auth|permission|payment|admin|crypto)' | head

# 새로 추가된 의존성
git diff origin/main...HEAD -- package.json | grep '^+.*"' | head
\`\`\`

변경 목록에서 민감한 영역이 보이면 그 부분에 시간을 더 쓴다. 전체를 같은 깊이로 보는 것보다 효과적이다.

## 지적하는 방식도 중요하다

무엇이 잘못됐는지만 말하면 방어적인 반응이 나온다. 어떤 상황에서 문제가 되는지와 어떻게 고치면 되는지를 함께 적으면 받아들여지기 쉽고, 리뷰를 받는 쪽도 배우게 된다. 같은 지적이 반복되면 개인의 문제가 아니라 도구나 기본 구조로 막을 수 있는지 검토할 신호다.

## 참고

- OWASP — 코드 리뷰 지침
- NIST SP 800-218 — 안전한 개발 관행
- CWE Top 25`,
    diagram: {
      type: 'bars',
      caption: '리뷰 우선순위',
      unit: '점',
      items: [
        { label: '권한 판단', value: 95, note: '도구가 못 찾음' },
        { label: '인증 코드', value: 90, note: '영향 큼' },
        { label: '입력 처리', value: 75, note: '삽입 경로' },
        { label: '화면 문구', value: 10, note: '낮음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '도구와 사람의 역할',
      x: ['도구', '사람'],
      y: ['알려진 패턴', '업무 로직'],
      cells: ['적합', '낭비', '부적합', '적합'],
    },
  },
  {
    slug: 'static-analysis-tuning',
    title: '정적 분석 결과를 다듬는 일',
    body: `정적 분석 도구를 처음 돌리면 결과가 수천 건 나오고, 그 목록을 본 순간 아무도 쓰지 않게 된다. 도구를 쓸모 있게 만드는 작업의 대부분은 결과를 줄이고 신뢰할 수 있게 만드는 일이다.

## 왜 많이 나오나

| 원인 | 설명 |
| --- | --- |
| 기본 규칙 전부 | 우리와 무관 |
| 의존성 포함 | 남의 코드 |
| 생성 코드 | 자동 생성 |
| 문맥 부족 | 실제로는 안전 |

우리 코드가 아닌 부분이 결과의 상당수를 차지한다. 범위를 우리 코드로 좁히는 것만으로 크게 준다.

![결과를 줄이는 단계](/img/posts/static-analysis-tuning.svg)

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 범위 제한 | 우리 코드만 |
| 규칙 선별 | 해당하는 것 |
| 심각도 기준 | 높은 것부터 |
| 예외 등록 | 확인한 것 |
| 기준선 설정 | 기존은 제외 |

기존 결과를 기준선으로 잡고 새로 추가되는 것만 보면 즉시 쓸 수 있게 된다. 쌓인 것은 별도 과제로 두고 천천히 줄인다.

## 예외를 어떻게 다루나

| 항목 | 내용 |
| --- | --- |
| 사유 기록 | 왜 안전한가 |
| 범위 최소 | 그 줄만 |
| 기한 | 재검토 |
| 검토자 | 누가 판단 |

사유 없이 예외로 표시하면 나중에 그것이 맞는 판단이었는지 알 수 없다. 한 줄이라도 이유를 적게 한다.

![예외 처리 규칙](/img/posts/static-analysis-tuning-2.svg)

## 현재 상태를 본다

\`\`\`bash
# 심각도별 결과 수
npx semgrep --config auto --json 2>/dev/null \
  | jq -r '.results[].extra.severity' | sort | uniq -c

# 예외로 표시된 항목 수
grep -rc 'nosemgrep\\|eslint-disable' --include='*.ts' src/ 2>/dev/null \
  | grep -v ':0' | wc -l
\`\`\`

예외 표시가 많으면 규칙이 현실과 맞지 않는 것이다. 개별 예외를 늘리기 전에 규칙 자체를 조정한다.

## 개발 흐름에 붙인다

결과를 별도 화면에서 봐야 하면 아무도 보지 않는다. 변경 요청에 바로 표시되게 하면 고치는 비용이 가장 작은 시점에 보게 되고, 그 자리에서 처리된다. 도구의 정확도보다 결과가 어디에 나타나는지가 실제 효과를 더 크게 좌우한다. 실행 시간도 짧게 유지한다. 검사에 몇십 분이 걸리면 결과를 기다리지 않고 병합하게 된다.

## 참고

- OWASP — 정적 분석 도구
- NIST SP 800-218 — 자동화된 검사
- 각 분석 도구 설정 문서`,
    diagram: {
      type: 'steps',
      caption: '결과를 줄이는 단계',
      steps: [
        { label: '범위 제한', note: '우리 코드' },
        { label: '규칙 선별', note: '해당하는 것' },
        { label: '기준선 설정', note: '새것만' },
        { label: '흐름에 붙임', note: '변경 요청' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '예외 처리 규칙',
      steps: [
        { label: '사유 기록', note: '왜 안전한가' },
        { label: '범위 최소', note: '그 줄만' },
        { label: '검토자', note: '누가 판단' },
        { label: '기한', note: '재검토' },
      ],
    },
  },
  {
    slug: 'secret-in-repository',
    title: '저장소에 들어간 비밀값 처리하기',
    body: `키나 비밀번호가 저장소에 커밋되는 일은 계속 일어난다. 발견했을 때 파일에서 지우는 것만으로는 부족하고, 이력에 남아 있는 한 그 값은 이미 공개된 것으로 봐야 한다.

## 어디에 남나

| 위치 | 설명 |
| --- | --- |
| 커밋 이력 | 지워도 남음 |
| 복제본 | 다른 사람 |
| 병합 요청 | 화면 기록 |
| 빌드 기록 | 파이프라인 |
| 백업 | 저장소 사본 |

이력을 고쳐도 다른 사람이 이미 복제한 사본에는 남아 있다. 공개 저장소라면 외부 서비스가 이미 색인했을 수 있다.

![비밀값이 남는 범위](/img/posts/secret-in-repository.svg)

## 무엇을 먼저 하나

| 순서 | 조치 |
| --- | --- |
| 1 | 값 무효화 |
| 2 | 새 값 발급 |
| 3 | 사용처 교체 |
| 4 | 이력 정리 |
| 5 | 재발 방지 |

이력을 지우는 것보다 값을 바꾸는 것이 먼저다. 이력 정리는 시간이 걸리고 그 사이에도 값은 유효하기 때문이다.

## 어떻게 막나

| 방법 | 시점 |
| --- | --- |
| 커밋 전 검사 | 로컬 |
| 푸시 전 검사 | 서버 |
| 정기 스캔 | 사후 |
| 교육 | 예방 |

커밋하기 전에 걸러 내는 검사가 가장 효과적이다. 들어가고 나서 지우는 것보다 훨씬 싸다.

![막는 지점](/img/posts/secret-in-repository-2.svg)

## 이력을 훑는다

\`\`\`bash
# 이력 전체에서 키 형태 문자열
git log --all -p 2>/dev/null \
  | grep -oE '(AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{32,}|ghp_[A-Za-z0-9]{36})' \
  | sort -u | head

# 커밋 전 검사가 설치돼 있는지
ls .git/hooks/pre-commit 2>/dev/null && echo '설치됨' || echo '없음'
\`\`\`

하나라도 나오면 그 값을 즉시 무효화한다. 언제 커밋됐는지 확인하면 노출 기간을 특정할 수 있다.

## 애초에 들어가지 않게 한다

설정 파일을 저장소에 두지 않고 환경별로 주입하는 구조면 이 문제가 거의 사라진다. 예시 파일만 저장소에 두고 실제 값은 시크릿 저장소에서 가져오게 하면, 실수로 커밋할 파일 자체가 없어진다. 검사로 막는 것보다 구조로 막는 편이 확실하다. 어떤 값이 비밀값인지 팀 안에서 합의해 두는 것도 필요하다. 내부 주소나 계정 이름을 비밀로 볼지에 따라 처리가 달라지고, 기준이 없으면 사람마다 다르게 판단한다.

## 참고

- OWASP — 비밀값 관리
- NIST SP 800-57 — 키 관리
- 각 저장소 서비스 비밀값 스캔 문서`,
    diagram: {
      type: 'layers',
      caption: '비밀값이 남는 범위',
      layers: [
        { label: '커밋 이력', note: '지워도 남음' },
        { label: '복제본', note: '다른 사람' },
        { label: '빌드 기록', note: '파이프라인' },
        { label: '외부 색인', note: '공개 저장소' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '막는 지점',
      steps: [
        { label: '커밋 전', note: '가장 싸다' },
        { label: '푸시 전', note: '서버 검사' },
        { label: '정기 스캔', note: '사후' },
        { label: '구조 변경', note: '근본' },
      ],
    },
  },
  {
    slug: 'crypto-library-use',
    title: '암호 기능을 직접 만들지 않기',
    body: `암호 알고리즘을 직접 구현하면 안 된다는 말은 널리 알려져 있지만, 라이브러리를 쓰면서도 잘못 쓰는 경우가 더 흔하다. 어떻게 쓰느냐에서 대부분의 문제가 생긴다.

## 어디서 틀리나

| 항목 | 실수 |
| --- | --- |
| 초기화 벡터 | 고정값 사용 |
| 운영 모드 | 안전하지 않은 것 |
| 키 생성 | 난수 아님 |
| 비교 | 시간 차이 |
| 무결성 | 암호화만 |

암호화만 하고 무결성을 확인하지 않으면 값이 조작되어도 알 수 없다. 두 가지를 함께 제공하는 방식을 쓰는 것이 기본이다.

![잘못 쓰는 지점](/img/posts/crypto-library-use.svg)

## 무엇을 쓰나

| 용도 | 선택 |
| --- | --- |
| 대칭 암호 | 인증된 모드 |
| 비밀번호 | 전용 해시 함수 |
| 난수 | 암호학적 생성기 |
| 서명 | 표준 알고리즘 |
| 키 유도 | 전용 함수 |

비밀번호에 일반 해시를 쓰면 빠르게 계산되어 대량 시도에 취약하다. 느리게 설계된 전용 함수를 써야 한다.

## 난수가 중요하다

| 용도 | 필요 |
| --- | --- |
| 토큰 | 예측 불가 |
| 초기화 벡터 | 매번 다름 |
| 키 | 충분한 길이 |
| 임시 값 | 재사용 금지 |

일반 난수 함수는 예측 가능해 토큰 생성에 쓰면 안 된다. 암호학적 생성기를 쓰는지 확인하는 것만으로 큰 문제 하나가 걸러진다.

![올바른 선택](/img/posts/crypto-library-use-2.svg)

## 코드를 훑는다

\`\`\`bash
# 위험한 함수 사용
grep -rInE 'Math\\.random|createCipher\\(|MD5|SHA1\\b|ECB' \
  --include='*.ts' --include='*.js' src/ 2>/dev/null | head -20

# 고정된 초기화 벡터
grep -rInE "iv\\s*=\\s*['\\\"]" --include='*.ts' src/ 2>/dev/null | head
\`\`\`

이 목록에서 나오는 것들은 대부분 실제 문제다. 오래된 예제 코드를 참고하면서 들어오는 경우가 많다.

## 높은 수준의 도구를 쓴다

개별 함수를 조합하는 대신 안전한 기본값이 정해진 상위 수준 라이브러리를 쓰면 선택의 여지가 줄어들어 실수도 줄어든다. 알고리즘과 모드를 고를 수 있다는 것은 잘못 고를 수 있다는 뜻이기도 하다. 선택지가 적은 도구가 이 영역에서는 더 안전하다.

## 참고

- OWASP — Cryptographic Storage Cheat Sheet
- NIST SP 800-175B — 암호 표준
- CWE-327 — 위험한 암호 알고리즘 사용`,
    diagram: {
      type: 'layers',
      caption: '잘못 쓰는 지점',
      layers: [
        { label: '무결성 누락', note: '암호화만' },
        { label: '초기화 벡터', note: '고정값' },
        { label: '난수', note: '예측 가능' },
        { label: '비교', note: '시간 차이' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '올바른 선택',
      steps: [
        { label: '인증된 모드', note: '무결성 포함' },
        { label: '전용 해시', note: '비밀번호' },
        { label: '암호학적 난수', note: '예측 불가' },
        { label: '상위 라이브러리', note: '선택지 축소' },
      ],
    },
  },
  {
    slug: 'deserialization-risk',
    title: '직렬화된 데이터를 되살릴 때',
    body: `저장하거나 전송한 객체를 다시 객체로 만드는 과정에서, 그 데이터가 신뢰할 수 없는 것이면 임의의 코드가 실행될 수 있다. 눈에 잘 띄지 않는 데다 영향이 크다.

## 어디서 생기나

| 경로 | 설명 |
| --- | --- |
| 세션 저장 | 객체 직렬화 |
| 캐시 | 저장 형식 |
| 메시지 큐 | 전달 형식 |
| 파일 업로드 | 내용 |
| 쿠키 | 클라이언트 보관 |

클라이언트가 보관하는 값을 그대로 되살리는 구성이 가장 위험하다. 사용자가 그 값을 마음대로 바꿀 수 있기 때문이다.

![되살리기가 위험해지는 흐름](/img/posts/deserialization-risk.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 단순 형식 사용 | 객체 대신 데이터 |
| 서명 | 변조 탐지 |
| 형식 제한 | 허용 목록 |
| 신뢰 경계 | 내부만 |

객체를 통째로 직렬화하는 대신 필요한 값만 단순한 형식으로 주고받으면 이 문제가 사라진다. 대부분의 경우 그렇게 바꿀 수 있다.

## 서명으로 막는 방식

| 항목 | 내용 |
| --- | --- |
| 비밀 키 | 서버만 |
| 검증 우선 | 되살리기 전 |
| 키 회전 | 가능하게 |
| 실패 처리 | 거절 |

서명을 검증한 뒤에 되살려야 한다. 순서가 바뀌면 검증 전에 이미 코드가 실행된다.

![안전한 처리 순서](/img/posts/deserialization-risk-2.svg)

## 사용처를 찾는다

\`\`\`bash
# 역직렬화 함수 사용 지점
grep -rInE 'pickle\\.loads|yaml\\.load\\(|unserialize\\(|readObject\\(' \
  --include='*.py' --include='*.php' --include='*.java' . 2>/dev/null | head -20

# 안전한 형식 사용 여부
grep -rIn 'yaml.safe_load' --include='*.py' . 2>/dev/null | wc -l
\`\`\`

안전하지 않은 함수가 나오면 입력이 어디서 오는지 확인한다. 내부에서만 오는 값이면 위험이 낮지만 그 전제가 유지되는지 봐야 한다.

## 형식 자체를 바꾸는 것이 낫다

방어를 겹겹이 쌓는 것보다 애초에 코드 실행으로 이어지지 않는 형식을 쓰는 편이 확실하다. 구조화된 데이터 형식을 쓰고 명시적으로 값을 꺼내 객체를 만들면, 데이터가 조작돼도 잘못된 값이 들어올 뿐 코드가 실행되지는 않는다. 성능 차이는 대개 무시할 만하다.

## 참고

- OWASP — Deserialization Cheat Sheet
- CWE-502 — 신뢰할 수 없는 데이터의 역직렬화
- NIST SP 800-218 — 안전한 개발 관행`,
    diagram: {
      type: 'flow',
      caption: '되살리기가 위험해지는 흐름',
      steps: [
        { label: '직렬화된 값', note: '클라이언트 보관' },
        { label: '값 조작', note: '사용자가' },
        { label: '되살리기', note: '검증 없이' },
        { label: '코드 실행', note: '객체 생성 중', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 처리 순서',
      steps: [
        { label: '서명 검증', note: '먼저' },
        { label: '형식 확인', note: '허용 목록' },
        { label: '값 추출', note: '명시적으로' },
        { label: '객체 생성', note: '우리 코드로' },
      ],
    },
  },
  {
    slug: 'command-injection-paths',
    title: '외부 명령을 부를 때의 위험',
    body: `프로그램 안에서 시스템 명령을 실행하는 코드는 편리하지만, 그 명령에 사용자 입력이 섞이면 임의의 명령이 실행된다. 이미지 변환이나 파일 처리를 외부 도구에 맡기는 경우에 자주 나타난다.

## 인젝션은 어디서 섞이나

| 상황 | 설명 |
| --- | --- |
| 파일 이름 | 사용자 지정 |
| 변환 옵션 | 입력값 |
| 경로 | 조합 |
| 검색 조건 | 전달 |
| 압축 해제 | 대상 |

파일 이름에 특수 문자를 넣는 것만으로 명령이 나뉘는 경우가 있다. 이름을 그대로 쓰지 않는 것이 기본이다.

![명령이 나뉘는 방식](/img/posts/command-injection-paths.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 셸 거치지 않기 | 인자 배열 |
| 허용 목록 | 정해진 값 |
| 라이브러리 사용 | 명령 대신 |
| 인자 검증 | 형식 |

셸을 거치지 않고 인자를 배열로 전달하면 문자열이 명령으로 해석되지 않는다. 대부분의 언어가 이 방식을 지원한다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 실행 방식 | 셸 여부 |
| 입력 출처 | 사용자인가 |
| 검증 | 어떤 형식 |
| 실행 권한 | 어떤 계정 |

같은 코드라도 실행 계정의 권한에 따라 피해가 달라진다. 최소 권한으로 실행하면 명령이 실행돼도 할 수 있는 일이 제한된다.

![막는 방법의 강도](/img/posts/command-injection-paths-2.svg)

## 사용처를 훑는다

\`\`\`bash
# 셸을 거치는 실행
grep -rInE 'exec\\(|execSync\\(|shell=True|system\\(|popen\\(' \
  --include='*.ts' --include='*.py' src/ 2>/dev/null | head -20

# 인자 배열 방식 사용 여부
grep -rIn 'spawn(\\|execFile(' --include='*.ts' src/ 2>/dev/null | wc -l
\`\`\`

셸을 거치는 호출이 나오면 입력이 어디서 오는지 따라간다. 상수만 들어가는 경우가 대부분이지만 그중 몇 개가 실제 문제다.

## 라이브러리로 대체할 수 있는지 본다

이미지 크기 조정이나 압축 해제 같은 작업은 대부분 라이브러리로 할 수 있다. 외부 명령을 부르지 않으면 이 종류의 위험이 통째로 사라지고, 실행 환경에 그 도구가 설치돼 있는지 신경 쓸 필요도 없어진다. 성능이나 기능 때문에 외부 도구가 꼭 필요한 경우에만 남기고 나머지는 바꾸는 편이 낫다.

## 참고

- OWASP — OS Command Injection Defense
- CWE-78 — 명령 삽입
- NIST SP 800-218 — 안전한 개발 관행`,
    diagram: {
      type: 'flow',
      caption: '명령이 나뉘는 방식',
      steps: [
        { label: '사용자 입력', note: '파일 이름' },
        { label: '문자열 결합', note: '명령 구성' },
        { label: '셸 해석', note: '특수 문자' },
        { label: '추가 명령 실행', note: '의도 밖', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '막는 방법의 강도',
      unit: '점',
      items: [
        { label: '라이브러리 사용', value: 95, note: '명령 없음' },
        { label: '인자 배열', value: 85, note: '셸 미경유' },
        { label: '허용 목록', value: 70, note: '값 제한' },
        { label: '문자 이스케이프', value: 35, note: '실수 여지' },
      ],
    },
  },
  {
    slug: 'race-condition-security',
    title: '동시에 일어날 때 생기는 결함',
    body: `혼자 쓸 때는 멀쩡한 코드가 동시에 여러 요청이 들어오면 다르게 동작한다. 잔액을 확인하고 차감하는 사이에 다른 요청이 끼어들면 잔액보다 많이 쓰는 일이 가능해진다.

## 결제 처리에서 어디서 생기나

| 상황 | 결과 |
| --- | --- |
| 잔액 차감 | 초과 사용 |
| 쿠폰 사용 | 중복 적용 |
| 재고 차감 | 초과 판매 |
| 한도 확인 | 초과 |
| 상태 전이 | 이중 처리 |

확인과 처리가 나뉘어 있으면 그 사이가 틈이 된다. 코드를 읽을 때는 보이지 않다가 동시 요청에서 드러난다.

![확인과 처리 사이의 틈](/img/posts/race-condition-security.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 원자적 갱신 | 한 번에 |
| 조건부 갱신 | 값 확인 포함 |
| 잠금 | 순서 보장 |
| 유일 제약 | 중복 차단 |

데이터베이스에서 조건을 포함한 갱신을 한 번에 수행하면 틈이 사라진다. 갱신된 행 수를 확인해 성공 여부를 판단한다.

## 어떤 잠금을 쓰나

| 방식 | 특성 |
| --- | --- |
| 낙관적 | 충돌 시 재시도 |
| 비관적 | 먼저 잠금 |
| 분산 잠금 | 여러 서버 |
| 큐 직렬화 | 순서 보장 |

충돌이 드물면 낙관적 방식이 성능에 유리하다. 자주 충돌하는 자원은 잠금을 먼저 잡는 편이 낫다.

![막는 방법](/img/posts/race-condition-security-2.svg)

## 동시에 보내 본다

\`\`\`bash
# 같은 요청을 동시에 여러 번 보낸다
for i in $(seq 1 20); do
  curl -s -X POST https://example.com/api/coupon/use \
    -H "Authorization: Bearer $TOKEN" -d '{"code":"ABC"}' &
done; wait

# 몇 번 성공했는지 확인
\`\`\`

한 번만 성공해야 하는데 여러 번 성공하면 결함이다. 순차로 보내면 재현되지 않으므로 동시에 보내야 한다.

## 시험에 넣기 어렵다

이 결함은 일반적인 시험에서 재현되지 않아 운영에서 처음 발견되는 경우가 많다. 금액이나 재고처럼 중요한 자원에 대해서는 동시 요청 시험을 따로 만들어 두는 것이 값어치가 있다. 한 번 만들어 두면 이후 변경에서도 계속 확인되고, 이 종류의 결함은 사후에 바로잡는 비용이 특히 크다.

## 참고

- OWASP — 비즈니스 로직 취약점
- CWE-362 — 동시 실행 경쟁 조건
- 각 데이터베이스 잠금 문서`,
    diagram: {
      type: 'flow',
      caption: '확인과 처리 사이의 틈',
      steps: [
        { label: '잔액 확인', note: '충분함' },
        { label: '다른 요청 진입', note: '같은 확인' },
        { label: '양쪽 차감', note: '각각 처리' },
        { label: '초과 사용', note: '잔액 음수', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '막는 방법',
      unit: '점',
      items: [
        { label: '원자적 갱신', value: 95, note: '틈 없음' },
        { label: '유일 제약', value: 90, note: '중복 차단' },
        { label: '비관적 잠금', value: 80, note: '순서 보장' },
        { label: '코드 논리만', value: 20, note: '동시성에 약함' },
      ],
    },
  },
  {
    slug: 'logging-sensitive-data',
    title: '로그에 남으면 안 되는 것들',
    body: `디버깅을 위해 요청 내용을 통째로 남기다 보면 비밀번호나 카드 번호가 그대로 기록된다. 로그는 여러 사람이 보고 오래 보관되므로, 한 번 들어간 값은 넓게 퍼진다.

## 개인정보가 어떻게 들어가나

| 항목 | 경로 |
| --- | --- |
| 비밀번호 | 요청 본문 |
| 토큰 | 헤더 |
| 카드 번호 | 결제 요청 |
| 주민등록번호 | 본인 확인 |
| 건강 정보 | 업무 데이터 |

인증 헤더가 통째로 기록되는 구성이 흔하다. 그 토큰으로 다른 사람이 접근할 수 있으므로 비밀번호와 같은 수준으로 다뤄야 한다.

![민감 정보가 로그로 가는 경로](/img/posts/logging-sensitive-data.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 허용 목록 | 남길 것만 |
| 가림 처리 | 일부만 |
| 구조화 로그 | 필드 단위 |
| 공통 함수 | 한곳에서 |

무엇을 지울지 정하는 대신 무엇을 남길지 정하면 새 필드가 추가돼도 안전하다. 차단 목록 방식은 반드시 빠지는 것이 생긴다.

## 어디까지 남기나

| 항목 | 처리 |
| --- | --- |
| 식별자 | 남김 |
| 값의 길이 | 남김 |
| 값 자체 | 가림 |
| 오류 원인 | 남김 |

값 자체가 없어도 길이나 형식만으로 디버깅이 되는 경우가 많다. 실제로 필요한 것이 무엇인지 따져 보면 대부분 값은 필요 없다.

![로그에 남기는 기준](/img/posts/logging-sensitive-data-2.svg)

## 지금 들어가는지 본다

\`\`\`bash
# 로그에 민감한 값이 있는지
grep -crIoE '"password"\\s*:\\s*"[^"]+"|Bearer [A-Za-z0-9._-]{20,}' \
  /var/log/app/ 2>/dev/null | grep -v ':0' | head

# 요청 전체를 남기는 코드
grep -rIn 'JSON.stringify(req' --include='*.ts' src/ 2>/dev/null | head
\`\`\`

하나라도 나오면 그 로그는 민감 정보를 담고 있다. 이미 기록된 것의 보관 기간과 접근 권한도 함께 확인해야 한다.

## 이미 들어간 것도 처리한다

가림 처리를 붙여도 이전 기록에는 값이 남아 있다. 보관 기간이 지날 때까지 기다릴지, 삭제할지, 접근을 제한할지 판단해야 한다. 개인정보가 포함됐다면 그 자체로 처리 근거가 필요한 상태이므로 방치하면 안 된다.

## 참고

- OWASP — 로깅 지침
- CWE-532 — 로그 파일을 통한 정보 노출
- 개인정보보호법 — 안전성 확보조치`,
    diagram: {
      type: 'flow',
      caption: '민감 정보가 로그로 가는 경로',
      steps: [
        { label: '디버깅 목적', note: '요청 전체 기록' },
        { label: '값 포함', note: '비밀번호·토큰' },
        { label: '수집·보관', note: '오래' },
        { label: '넓은 접근', note: '여러 사람', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '로그에 남기는 기준',
      layers: [
        { label: '식별자', note: '남김' },
        { label: '오류 원인', note: '남김' },
        { label: '값의 형식', note: '길이만' },
        { label: '값 자체', note: '가림' },
      ],
    },
  },
  {
    slug: 'test-code-security',
    title: '시험 코드가 운영에 남기는 구멍',
    body: `시험을 위해 만든 계정, 우회 경로, 고정된 값이 운영에 남는 일이 있다. 시험 코드는 리뷰에서 가볍게 다뤄지기 때문에 이런 것들이 통과하기 쉽다.

## 빌드 시스템에 무엇이 남나

| 항목 | 위험 |
| --- | --- |
| 시험 계정 | 알려진 비밀번호 |
| 우회 조건 | 인증 건너뛰기 |
| 고정 토큰 | 유효한 값 |
| 디버그 경로 | 정보 노출 |
| 시험 데이터 | 실제와 혼동 |

특정 조건에서 인증을 건너뛰는 코드가 가장 위험하다. 시험 환경에서만 동작하게 만들었는데 그 판단 기준이 조작 가능한 경우가 있다.

![시험 코드가 남는 경로](/img/posts/test-code-security.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 빌드 분리 | 운영에서 제외 |
| 환경 판단 | 안전한 기준 |
| 계정 정리 | 배포 시 |
| 검사 추가 | 빌드 확인 |

시험용 코드를 운영 빌드에 포함하지 않는 구조가 가장 확실하다. 실행 시점에 판단하는 방식은 그 판단이 틀릴 수 있다.

## 환경을 어떻게 판단하나

| 기준 | 안전도 |
| --- | --- |
| 빌드 시점 결정 | 안전 |
| 서버 설정 | 보통 |
| 요청 헤더 | 위험 |
| 호스트 이름 | 보통 |

요청에 담긴 값으로 환경을 판단하면 사용자가 그 값을 보내 우회할 수 있다. 실제로 그렇게 구현된 사례가 있다.

![환경 판단 기준](/img/posts/test-code-security-2.svg)

## 배포본을 확인한다

\`\`\`bash
# 시험 관련 문자열이 배포 빌드에 남아 있는지
grep -rIcE 'test-|mock|__DEV__|skipAuth|bypass' dist/ 2>/dev/null | grep -v ':0' | head

# 알려진 시험 계정이 운영에 있는지
psql -At -c "select email from users where email like '%test%' or email like '%demo%'" \
  2>/dev/null | head
\`\`\`

운영 데이터베이스에 시험 계정이 있으면 정리한다. 비밀번호가 단순하거나 공유되고 있을 가능성이 높다.

## 시험 코드도 리뷰한다

시험 코드라고 리뷰를 건너뛰면 그곳에 들어간 우회 경로가 걸러지지 않는다. 같은 기준으로 보되 시험 코드에서만 허용되는 것이 무엇인지 정해 두면 판단이 빨라진다. 시험이 통과하도록 검증을 느슨하게 만드는 변경이 들어오는 경우도 있어, 그런 변경은 특히 주의해서 봐야 한다.

## 참고

- OWASP — 안전한 개발 지침
- NIST SP 800-218 — 개발 환경 분리
- CWE-489 — 활성화된 디버그 코드`,
    diagram: {
      type: 'flow',
      caption: '시험 코드가 남는 경로',
      steps: [
        { label: '시험 편의', note: '우회 추가' },
        { label: '가벼운 리뷰', note: '시험 코드라서' },
        { label: '빌드 포함', note: '분리 없음' },
        { label: '운영에서 동작', note: '우회 가능', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '환경 판단 기준',
      unit: '점',
      items: [
        { label: '빌드 시점', value: 95, note: '코드 자체가 없음' },
        { label: '서버 설정', value: 70, note: '조작 어려움' },
        { label: '호스트 이름', value: 55, note: '보통' },
        { label: '요청 헤더', value: 10, note: '우회 가능' },
      ],
    },
  },
  {
    slug: 'dependency-lockfile-policy',
    title: '의존성 버전을 고정하는 이유',
    body: `버전을 느슨하게 지정하면 빌드할 때마다 다른 코드가 들어올 수 있다. 어제 통과한 것이 오늘 깨지고, 더 나쁘게는 침해된 버전이 조용히 들어온다.

## 무엇이 달라지나

| 지정 | 결과 |
| --- | --- |
| 정확한 버전 | 항상 같음 |
| 범위 지정 | 바뀔 수 있음 |
| 최신 | 매번 다름 |
| 잠금 파일 | 재현 가능 |

잠금 파일이 있으면 범위로 지정해도 실제로는 고정된다. 그 파일을 저장소에 함께 두는 것이 전제다.

![버전 지정과 재현성](/img/posts/dependency-lockfile-policy.svg)

## 무엇이 위험한가

| 상황 | 설명 |
| --- | --- |
| 침해된 배포 | 악성 코드 |
| 갑작스러운 변경 | 동작 차이 |
| 삭제된 버전 | 빌드 실패 |
| 전이 의존성 | 간접 변경 |

전이 의존성은 우리가 고정하지 않으면 계속 바뀐다. 잠금 파일이 이 부분까지 고정해 준다.

## 어떻게 관리하나

| 방법 | 내용 |
| --- | --- |
| 잠금 파일 커밋 | 필수 |
| 갱신 주기 | 정기 |
| 변경 리뷰 | 잠금 파일 |
| 무결성 검증 | 해시 |

잠금 파일의 변경을 리뷰에서 확인하면 예상하지 못한 패키지가 들어오는 것이 보인다. 한 줄 고쳤는데 잠금 파일이 크게 바뀌면 이유를 확인한다.

![관리 항목](/img/posts/dependency-lockfile-policy-2.svg)

## 상태를 확인한다

\`\`\`bash
# 잠금 파일이 저장소에 있는지
ls package-lock.json yarn.lock pnpm-lock.yaml 2>/dev/null

# 설치가 잠금 파일과 일치하는지 검증
npm ci --dry-run 2>&1 | tail -3
\`\`\`

잠금 파일이 없으면 재현 가능한 빌드가 아니다. 빌드할 때마다 다른 결과가 나올 수 있는 상태다.

## 고정과 갱신은 반대가 아니다

고정한다고 낡은 버전을 계속 쓰라는 뜻이 아니다. 고정은 언제 무엇이 바뀌는지 우리가 정한다는 뜻이고, 갱신은 정기적으로 계획해서 한다. 자동 갱신 도구를 붙이고 시험이 통과하면 병합하는 구조를 만들면 두 가지를 함께 얻을 수 있다. 갱신이 자주 일어날수록 한 번에 들어오는 변경이 작아 문제를 찾기도 쉬워진다.

## 참고

- NIST SP 800-161 — 공급망 위험 관리
- OWASP — 의존성 관리
- 각 패키지 관리자 잠금 파일 문서`,
    diagram: {
      type: 'matrix',
      caption: '버전 지정과 재현성',
      x: ['잠금 파일 있음', '없음'],
      y: ['정확한 버전', '범위 지정'],
      cells: ['재현 가능', '재현 가능', '재현 가능', '매번 다름'],
    },
    diagram2: {
      type: 'steps',
      caption: '관리 항목',
      steps: [
        { label: '잠금 파일 커밋', note: '필수' },
        { label: '변경 리뷰', note: '예상 밖 확인' },
        { label: '무결성 검증', note: '해시' },
        { label: '정기 갱신', note: '계획적으로' },
      ],
    },
  },
]
