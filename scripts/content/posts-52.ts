import type { SeedPost } from './types'

/** 510~519 — API 와 요청 처리에서 새는 자리 */
export const posts52: SeedPost[] = [
  {
    slug: 'rate-limit-design',
    title: '요청 제한을 어디에 걸 것인가',
    body: `요청 제한은 대개 "분당 몇 회"라는 숫자 하나로 정해진다. 그런데 막아야 할 것은 요청 수가 아니라 특정 행위의 반복이다. 무엇을 기준으로 세는지를 정하지 않으면 숫자를 아무리 조여도 막고 싶은 것은 그대로 지나간다.

## 무엇을 기준으로 세나?

같은 분당 60회라도 무엇을 단위로 세느냐에 따라 전혀 다른 통제가 된다. IP 기준은 공유 회선 뒤의 정상 사용자를 함께 막고, 계정 기준은 계정을 여러 개 만들면 뚫린다.

| 기준 | 막히는 것 | 새는 곳 |
| --- | --- | --- |
| IP | 단순 반복 | 프록시·모바일 회선 |
| 계정 | 계정 단위 남용 | 계정 다수 생성 |
| 세션 | 자동화 | 재로그인 |
| 기기 지문 | 재접속 | 위조 가능 |
| 대상 자원 | 특정 값 탐색 | 여러 자원 분산 |

로그인처럼 실패가 의미 있는 엔드포인트는 계정과 IP 양쪽을 함께 센다. 한쪽만 세면 한 계정에 여러 IP 로 들어오는 시도나, 한 IP 에서 여러 계정을 훑는 시도 중 하나를 놓친다.

![기준별로 막히는 범위](/img/posts/rate-limit-design.svg)

## 엔드포인트마다 값이 다르다

전체에 같은 값을 걸면 비싼 요청과 싼 요청이 같은 대우를 받는다. 비용과 위험이 큰 곳을 따로 조인다.

| 유형 | 상대적 제한 |
| --- | --- |
| 로그인·비밀번호 재설정 | 아주 낮게 |
| 검색·목록 조회 | 보통 |
| 파일 업로드 | 낮게 |
| 정적 자원 | 사실상 없음 |
| 내부 호출 | 별도 정책 |

## 막았을 때 무엇을 돌려주나

429 를 돌려주면서 언제 다시 시도할 수 있는지 알려 주지 않으면 클라이언트가 즉시 재시도해 상황을 더 나쁘게 만든다. 재시도 시점을 헤더로 알려 주는 것이 방어의 일부다.

| 항목 | 값 |
| --- | --- |
| 상태 코드 | 429 |
| Retry-After | 대기 초 |
| 남은 횟수 | 헤더로 노출 |
| 본문 | 이유는 모호하게 |

본문에 "이 계정은 5회 실패했습니다" 같은 것을 적으면 계정 존재 여부가 새어 나간다. 사람에게는 충분하되 공격자에게는 정보가 되지 않는 선을 잡는다.

![제한 응답의 구성](/img/posts/rate-limit-design-2.svg)

## 지금 무엇이 몰리는지 본다

값을 정하기 전에 실제 분포를 본다. 대개 상위 몇 개 경로가 전체의 대부분이다.

\`\`\`bash
# 경로별 요청 수 상위
awk '{print $7}' /var/log/nginx/access.log | sort | uniq -c | sort -rn | head -10

# 로그인 실패가 몰리는 출발지
grep ' 401 ' /var/log/nginx/access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head -10
\`\`\`

한 출발지가 압도적이면 그것은 제한이 아니라 차단 대상일 수 있다. 제한은 정상 사용자 사이에 섞인 남용을 다루는 수단이다.

## 제한을 켜기 전에 관찰만 해 본다

처음부터 막으면 정상 사용자가 걸린 것을 알아차리기 어렵다. 같은 규칙을 기록만 하는 모드로 일주일 돌려 보고, 걸리는 요청이 정말 막아도 되는 것인지 확인한 뒤에 켠다. 이 순서를 건너뛰면 장애 원인을 찾는 데 며칠이 든다.

## 참고

- OWASP API Security Top 10 — 자원 소비 제한
- RFC 6585 — 429 Too Many Requests
- NIST SP 800-53 — 자원 가용성 보호`,
    diagram: {
      type: 'matrix',
      caption: '세는 기준과 효과',
      x: ['단일 출발지', '분산'],
      y: ['한 계정 집중', '여러 계정'],
      cells: ['IP 로 충분', '계정 기준 필요', '계정 기준', '둘 다 필요'],
    },
    diagram2: {
      type: 'steps',
      caption: '제한 응답',
      steps: [
        { label: '429 반환', note: '상태 코드' },
        { label: 'Retry-After', note: '대기 시간' },
        { label: '남은 횟수', note: '헤더' },
        { label: '이유는 모호하게', note: '정보 노출 방지' },
      ],
    },
  },
  {
    slug: 'retry-storm-prevention',
    title: '재시도가 장애를 키우지 않게 하려면',
    body: `한 서비스가 느려지면 그것을 부르던 쪽이 재시도를 시작한다. 재시도가 늘면 부하가 늘고, 부하가 늘면 더 느려진다. 처음의 작은 지연이 몇 분 만에 전면 장애가 되는 경로가 이것이다.

## 재시도가 왜 트래픽 폭주를 만드나?

정상일 때 초당 100건이던 호출이 3회 재시도 정책 아래에서는 최대 400건이 된다. 회복하려는 순간에 평소의 네 배가 몰리므로 회복 자체가 막힌다. 트래픽 폭주가 외부가 아니라 내부에서 만들어지는 셈이다.

| 요소 | 효과 |
| --- | --- |
| 즉시 재시도 | 같은 순간에 몰림 |
| 고정 간격 | 주기적으로 몰림 |
| 다단 호출 | 단계마다 곱해짐 |
| 무제한 재시도 | 회복까지 계속 |

다단 호출에서 곱해지는 것이 특히 위험하다. A→B→C 구조에서 각 단계가 3회씩 재시도하면 C 는 최대 9배를 받는다.

![재시도가 쌓이는 경로](/img/posts/retry-storm-prevention.svg)

## 간격을 벌리고 흩뿌린다

지수적으로 간격을 늘리되, 거기에 무작위 값을 더한다. 무작위 값이 없으면 모두가 같은 시점에 다시 몰린다.

| 회차 | 기본 간격 | 실제 대기 |
| --- | --- | --- |
| 1 | 1초 | 0.5~1.5초 |
| 2 | 2초 | 1~3초 |
| 3 | 4초 | 2~6초 |
| 4 | 중단 | — |

## 재시도해도 되는 것과 아닌 것

모든 실패를 재시도하면 안 된다. 400 대 응답은 다시 보내도 같은 답이 오고, 요청 수만 늘린다.

| 응답 | 재시도 |
| --- | --- |
| 연결 실패·타임아웃 | 한다 |
| 503·429 | Retry-After 를 따른다 |
| 500 | 제한적으로 |
| 400·401·403 | 하지 않는다 |
| 409 | 하지 않는다 |

![응답별 판단](/img/posts/retry-storm-prevention-2.svg)

## 끊어 주는 장치를 둔다

실패가 일정 비율을 넘으면 한동안 호출 자체를 막고 즉시 실패를 돌려준다. 상대가 회복할 틈을 주는 것이 결국 전체 회복을 앞당긴다.

\`\`\`bash
# 재시도 폭주가 시작됐는지 — 초당 요청과 5xx 비율을 함께 본다
tail -f /var/log/nginx/access.log | awk '{c++; if ($9 ~ /^5/) e++}
  END {print "요청", c, "오류", e}'

# 같은 요청이 반복되는지 (재시도 표식이 있다면)
grep -o 'X-Retry-Count: [0-9]*' /var/log/app.log | sort | uniq -c
\`\`\`

재시도 횟수를 헤더에 실어 보내면 받는 쪽에서 폭주를 구분할 수 있다. 첫 요청과 네 번째 요청을 같게 보면 무슨 일이 벌어지는지 알 수 없다.

## 보안 관점에서 한 가지 더

재시도 폭주는 서비스 거부와 구별하기 어렵다. 외부 공격을 의심해 대응을 시작했는데 원인이 자기 쪽 재시도였던 경우가 흔하다. 내부 호출에 출처 표시를 남겨 두면 이 구분에 몇 시간을 아낀다.

## 참고

- AWS Architecture Blog — 지수 백오프와 지터
- Google SRE Book — 부하 분산과 과부하 처리
- OWASP API Security Top 10 — 자원 소비`,
    diagram: {
      type: 'bars',
      caption: '재시도로 늘어나는 요청량',
      unit: '배',
      items: [
        { label: '재시도 없음', value: 1, note: '기준' },
        { label: '1단계 3회', value: 4, note: '4배' },
        { label: '2단계 중첩', value: 9, note: '곱해짐' },
        { label: '3단계 중첩', value: 27, note: '통제 불능' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '재시도 판단',
      x: ['일시적 실패', '영구 실패'],
      y: ['멱등함', '아님'],
      cells: ['재시도', '재시도 안 함', '조건부', '절대 안 함'],
    },
  },
  {
    slug: 'idempotency-key-design',
    title: '멱등 키를 설계할 때 정할 것',
    body: `같은 요청이 두 번 도착하는 일은 예외가 아니라 일상이다. 네트워크가 끊기고 클라이언트가 재시도하면 서버는 같은 요청을 두 번 받는다. 결제처럼 두 번 실행되면 안 되는 일에서는 이 상황을 처리하는 규칙이 반드시 필요하다.

## 키를 누가 만드나?

멱등 키는 클라이언트가 만들어 보내야 한다. 서버가 만들면 재시도할 때 다른 키가 되어 같은 요청인지 알 수 없다. 이것이 설계에서 가장 자주 틀리는 지점이다.

| 방식 | 결과 |
| --- | --- |
| 클라이언트 생성 | 재시도해도 같은 키 |
| 서버 생성 | 재시도마다 달라짐 |
| 요청 본문 해시 | 같은 내용이면 같은 키 |
| 사용자·시각 조합 | 경계에서 충돌 |

요청 본문 해시는 편리해 보이지만, 의도적으로 같은 금액을 두 번 보내는 정상 요청까지 하나로 합쳐 버린다. 사용자가 실제로 두 번 사려는 경우를 구분할 수 없다.

![키 생성 주체에 따른 차이](/img/posts/idempotency-key-design.svg)

## 키를 얼마나 보관하나

보관 기간이 짧으면 늦게 온 재시도를 새 요청으로 처리한다. 길면 저장 비용이 는다. 클라이언트의 재시도 정책보다 넉넉히 잡는 것이 기준이다.

| 상황 | 권장 |
| --- | --- |
| 즉시 재시도 | 수 분 |
| 배치 재전송 | 수 시간 |
| 사람이 다시 누름 | 24시간 |
| 정산 대사 | 며칠 |

## 같은 키로 다른 내용이 오면

키는 같은데 금액이 다른 요청이 오는 경우가 실제로 생긴다. 클라이언트 버그이거나 공격이다. 조용히 첫 결과를 돌려주면 문제가 감춰진다.

| 상황 | 처리 |
| --- | --- |
| 같은 키·같은 내용 | 첫 결과 반환 |
| 같은 키·다른 내용 | 422 로 거절 |
| 처리 중인 키 | 409 로 대기 안내 |
| 만료된 키 | 새 요청으로 처리 |

![중복 요청 처리 흐름](/img/posts/idempotency-key-design-2.svg)

## 경쟁 상태를 막는다

두 요청이 거의 동시에 오면 둘 다 "처음"으로 보일 수 있다. 키를 먼저 선점하고 처리하는 순서여야 한다.

\`\`\`sql
-- 키를 유일 제약으로 두고 먼저 넣는다. 두 번째는 여기서 실패한다.
CREATE TABLE idempotency (
  key         TEXT PRIMARY KEY,
  status      TEXT NOT NULL,
  response    JSONB,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 선점에 성공한 요청만 실제 처리를 진행한다
INSERT INTO idempotency (key, status) VALUES ($1, 'processing')
ON CONFLICT (key) DO NOTHING
RETURNING key;
\`\`\`

반환된 행이 없으면 이미 다른 요청이 처리 중이거나 끝난 것이다. 애플리케이션 코드에서 조회한 뒤 없으면 삽입하는 방식은 두 요청이 동시에 들어오면 둘 다 통과한다.

## 결제 밖에서도 쓸 곳

멱등은 결제만의 개념이 아니다. 메일 발송, 외부 통보, 포인트 적립처럼 두 번 실행되면 되돌리기 어려운 모든 곳에 같은 규칙이 필요하다. 되돌릴 수 있는지를 기준으로 어디에 적용할지 정하면 목록이 금방 나온다.

## 참고

- Stripe API — 멱등 요청 설계
- RFC 9110 — 멱등 메서드의 정의
- PCI DSS — 결제 처리 무결성`,
    diagram: {
      type: 'flow',
      caption: '중복 요청 처리',
      steps: [
        { label: '클라이언트가 키 생성', note: '재시도해도 동일' },
        { label: '서버가 키 선점', note: '유일 제약' },
        { label: '선점 성공만 처리', note: '나머지는 대기' },
        { label: '결과 저장·반환', note: '재시도에 같은 답' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '키와 내용의 조합',
      x: ['같은 내용', '다른 내용'],
      y: ['처리 완료', '처리 중'],
      cells: ['첫 결과 반환', '422 거절', '409 대기', '422 거절'],
    },
  },
  {
    slug: 'webhook-signature-verify',
    title: '웹훅 서명을 검증하는 순서와 함정',
    body: `웹훅은 외부가 우리 엔드포인트를 호출하는 통로다. 주소만 알면 누구나 두드릴 수 있으므로, 보낸 쪽이 정말 그 서비스인지 확인하는 절차가 없으면 임의의 데이터를 밀어 넣을 수 있는 입구가 된다.

## 무엇을 확인하는 검증인가?

서명 검증은 세 가지를 한꺼번에 본다. 보낸 쪽이 키를 아는지, 내용이 바뀌지 않았는지, 그리고 오래된 요청을 다시 보낸 것이 아닌지다. 앞의 둘만 보고 세 번째를 빠뜨리는 구현이 흔하다.

| 확인 | 수단 |
| --- | --- |
| 보낸 주체 | 공유 비밀로 만든 서명 |
| 내용 무결성 | 본문 전체를 서명 대상에 |
| 재전송 여부 | 타임스탬프와 허용 오차 |

타임스탬프를 서명 대상에 포함하지 않으면, 가로챈 요청을 몇 달 뒤에 그대로 다시 보내도 서명이 유효하다.

![검증 순서](/img/posts/webhook-signature-verify.svg)

## 본문을 언제 읽는지가 중요하다

많은 프레임워크가 JSON 을 자동으로 파싱한다. 파싱된 객체를 다시 문자열로 만들어 서명을 계산하면 공백이나 키 순서가 달라져 검증이 실패한다. 원본 바이트를 그대로 들고 있어야 한다.

| 실수 | 결과 |
| --- | --- |
| 파싱 후 재직렬화 | 서명 불일치 |
| 인코딩 변환 | 불일치 |
| 앞뒤 공백 제거 | 불일치 |
| 원본 보관 | 정상 |

## 비교 방식의 함정

서명을 일반 문자열 비교로 확인하면 앞에서부터 몇 글자가 맞는지에 따라 응답 시간이 미세하게 달라진다. 이 차이를 측정해 한 글자씩 맞춰 가는 공격이 가능하다. 길이가 같든 다르든 같은 시간이 걸리는 비교를 써야 한다.

| 항목 | 방법 |
| --- | --- |
| 비교 | 상수 시간 비교 함수 |
| 키 보관 | 시크릿 저장소 |
| 키 회전 | 두 키 동시 허용 |
| 실패 로그 | 서명값은 남기지 않음 |

![서명 대상 구성](/img/posts/webhook-signature-verify-2.svg)

## 검증을 실제로 해 본다

수신 쪽을 만들었으면 잘못된 요청이 실제로 거절되는지 확인한다.

\`\`\`bash
BODY='{"event":"test"}'
TS=$(date +%s)
SIG=$(printf '%s.%s' "$TS" "$BODY" | openssl dgst -sha256 -hmac "$SECRET" -hex | awk '{print $2}')

# 정상 요청 — 200 이어야 한다
curl -s -o /dev/null -w '%{http_code}\\n' -X POST https://example.com/hooks \\
  -H "X-Timestamp: $TS" -H "X-Signature: $SIG" -d "$BODY"

# 본문만 바꾼 요청 — 거절돼야 한다
curl -s -o /dev/null -w '%{http_code}\\n' -X POST https://example.com/hooks \\
  -H "X-Timestamp: $TS" -H "X-Signature: $SIG" -d '{"event":"tampered"}'
\`\`\`

두 번째가 200 으로 통과하면 서명이 본문을 덮고 있지 않은 것이다. 검증 코드가 있다는 사실과 검증이 동작한다는 사실은 다르다.

## 검증을 통과한 뒤에도

서명이 맞다고 내용까지 믿을 이유는 없다. 보낸 쪽 서비스가 침해됐거나 잘못된 값을 보낼 수 있다. 금액이나 상태 같은 중요한 값은 웹훅 내용을 그대로 쓰지 말고 발신처 API 로 다시 조회해 확인하는 편이 안전하다.

## 참고

- OWASP Cheat Sheet — 웹훅 보안
- RFC 2104 — HMAC 정의
- Stripe·GitHub 웹훅 서명 규격`,
    diagram: {
      type: 'steps',
      caption: '검증 순서',
      steps: [
        { label: '원본 본문 보관', note: '파싱 전 바이트' },
        { label: '타임스탬프 확인', note: '허용 오차 안인가' },
        { label: '서명 계산', note: '시각 + 본문' },
        { label: '상수 시간 비교', note: '타이밍 노출 방지' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '서명이 덮어야 하는 것',
      layers: [
        { label: '타임스탬프', note: '재전송 차단' },
        { label: '본문 전체', note: '변조 차단' },
        { label: '경로·메서드', note: '전용 검증' },
        { label: '공유 비밀', note: '발신자 확인' },
      ],
    },
  },
  {
    slug: 'callback-url-validation',
    title: '콜백 주소를 어디까지 허용할 것인가',
    body: `로그인 뒤 돌아갈 주소, 결제 완료 후 이동할 주소처럼 요청이 주소를 지정하는 자리가 있다. 이 값을 그대로 믿고 이동시키면 우리 도메인을 발판 삼아 다른 곳으로 보내는 통로가 된다.

## 왜 검증이 어려운가?

주소는 사람이 보기에 단순하지만 파서마다 해석이 다르다. 사람의 눈과 코드의 해석이 갈리는 지점을 공격이 파고든다.

| 입력 | 사람의 해석 | 실제 |
| --- | --- | --- |
| \`//evil.example\` | 경로 | 외부 주소 |
| \`https://ours.kr@evil.example\` | 우리 도메인 | evil 로 감 |
| \`https://ours.kr.evil.example\` | 우리 도메인 | 다른 도메인 |
| \`/\\evil.example\` | 경로 | 브라우저에 따라 외부 |

문자열이 우리 도메인으로 시작하는지 보는 검사는 두 번째와 세 번째를 통과시킨다. 접근 통제를 문자열 비교로 하면 반드시 이런 구멍이 생긴다.

![주소 해석이 갈리는 자리](/img/posts/callback-url-validation.svg)

## 허용 목록이 유일하게 안전한 방법

주소를 파싱해 호스트를 꺼내고, 미리 정한 목록과 정확히 일치하는지 본다. 부분 일치나 정규식은 쓰지 않는다.

| 방식 | 안전성 |
| --- | --- |
| 접두사 비교 | 뚫림 |
| 정규식 | 대개 뚫림 |
| 호스트 완전 일치 | 안전 |
| 상대 경로만 허용 | 가장 안전 |

돌아갈 곳이 항상 우리 서비스 안이라면 주소 대신 경로만 받는다. 앞에 슬래시 하나만 오는 형태로 제한하면 외부로 나갈 방법이 없다.

## 열린 전달이 만드는 피해

우리 도메인의 링크가 외부로 보내는 통로가 되면 피싱 메일의 신뢰도가 올라간다. 수신자는 익숙한 도메인을 보고 누르고, 도착지는 전혀 다른 곳이다.

| 영향 | 설명 |
| --- | --- |
| 피싱 신뢰도 | 우리 도메인이 미끼가 됨 |
| 토큰 유출 | 조각이나 질의에 실려 감 |
| 인가 흐름 오염 | 인가 코드가 공격자에게 |
| 평판 하락 | 메일 필터가 도메인을 의심 |

![허용 방식별 안전성](/img/posts/callback-url-validation-2.svg)

## 지금 어디가 열려 있는지 본다

주소를 받아 이동시키는 자리를 먼저 찾는다.

\`\`\`bash
# 리다이렉트 파라미터 이름을 훑는다
grep -rniE 'redirect_uri|returnUrl|next=|callback|continue=' --include='*.{ts,js,py,go}' . \\
  | grep -v node_modules | head -20

# 실제로 열려 있는지 시험한다 — 302 의 Location 이 외부면 열린 것이다
curl -s -o /dev/null -D - 'https://example.com/login?next=//evil.example' | grep -i '^location'
\`\`\`

응답의 Location 이 외부 도메인을 가리키면 그 자리가 열린 전달이다. 로그인 뒤에만 동작하는 경로라면 세션을 붙여 같은 시험을 반복한다.

## 인가 흐름에서는 더 엄격하게

OAuth 의 redirect_uri 는 특히 엄격해야 한다. 인가 코드가 그 주소로 전달되므로, 허용 목록이 느슨하면 코드가 통째로 넘어간다. 등록된 주소와 완전히 같은지 보고, 질의 문자열 추가도 허용하지 않는 것이 기본이다.

## 참고

- OWASP — Unvalidated Redirects and Forwards
- RFC 6749 §10.6 — 인가 코드 리다이렉트 공격
- OAuth 2.0 Security Best Current Practice`,
    diagram: {
      type: 'matrix',
      caption: '검증 방식과 결과',
      x: ['문자열 비교', '파싱 후 호스트'],
      y: ['외부 허용', '내부만'],
      cells: ['뚫림', '안전', '대개 뚫림', '가장 안전'],
    },
    diagram2: {
      type: 'flow',
      caption: '열린 전달이 쓰이는 경로',
      steps: [
        { label: '우리 도메인 링크', note: '신뢰를 얻음' },
        { label: '파라미터에 외부 주소', note: '검증 통과' },
        { label: '외부로 이동', note: '사용자는 모름' },
        { label: '자격 증명 입력', note: '피싱 성공', danger: true },
      ],
    },
  },
  {
    slug: 'bulk-operation-guard',
    title: '대량 처리 요청에 제동을 거는 법',
    body: `한 번에 여러 건을 처리하는 기능은 편리한 만큼 사고도 크다. 실수로 조건을 잘못 넣으면 수만 건이 한 번에 바뀌고, 그 결과를 되돌리는 데 며칠이 든다. 개별 처리에서는 생기지 않는 종류의 위험이다.

## 대량 엔드포인트는 무엇이 다른가?

개별 처리에서는 한 번의 실수가 한 건에 그친다. 대량 처리에서는 같은 실수가 데이터 전체에 적용된다. 그래서 권한이 같더라도 통제는 달라야 한다.

| 구분 | 개별 | 대량 |
| --- | --- | --- |
| 실수의 범위 | 1건 | 조건에 걸린 전부 |
| 되돌리기 | 쉬움 | 어려움 |
| 탐지 | 눈에 띔 | 한 번의 요청 |
| 권한 판단 | 건별 | 조건 전체 |

권한 판단이 특히 문제다. 개별 요청이면 그 건에 대한 권한만 보면 되지만, 조건으로 지정된 대량 요청은 걸린 모든 건에 권한이 있는지 확인해야 한다. 이 확인을 건너뛴 엔드포인트가 흔하다.

![개별과 대량의 차이](/img/posts/bulk-operation-guard.svg)

## 상한을 둔다

한 요청이 건드릴 수 있는 최대 건수를 정한다. 그 이상은 거절하거나 여러 번 나눠 보내게 한다.

| 작업 | 상한 예 |
| --- | --- |
| 조회 | 1,000건 |
| 수정 | 100건 |
| 삭제 | 50건 |
| 외부 발송 | 20건 |

되돌리기 어려울수록 상한을 낮게 잡는다. 상한이 불편하다는 요구가 오면 그때 절차를 만들지, 처음부터 무제한으로 열지 않는다.

## 조건을 먼저 보여준다

실행 전에 몇 건이 걸리는지 알려 주고 확인을 받는다. 이 한 단계가 대부분의 사고를 막는다.

| 단계 | 내용 |
| --- | --- |
| 1 | 조건으로 대상 수 계산 |
| 2 | 건수와 표본 제시 |
| 3 | 사용자 확인 |
| 4 | 실행·기록 |

![실행 전 확인 절차](/img/posts/bulk-operation-guard-2.svg)

## 무엇이 바뀌었는지 남긴다

대량 작업은 결과를 되돌릴 수 있어야 한다. 무엇이 어떻게 바뀌었는지 기록해 두면 복구가 가능하다.

\`\`\`sql
-- 실행 전에 대상과 이전 값을 남긴다
CREATE TABLE bulk_log (
  job_id      UUID NOT NULL,
  target_id   BIGINT NOT NULL,
  before_data JSONB NOT NULL,
  changed_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 되돌릴 때는 이 기록을 역으로 적용한다
INSERT INTO bulk_log (job_id, target_id, before_data)
SELECT $1, id, to_jsonb(t) FROM targets t WHERE <조건>;
\`\`\`

기록 없이 실행한 대량 작업은 백업 복원 말고는 되돌릴 방법이 없다. 백업 복원은 그 사이의 정상 변경까지 함께 지운다.

## 누가 언제 했는지

대량 작업은 감사 로그에 반드시 남긴다. 실행자, 조건, 대상 건수, 실행 시각을 함께 적는다. 조건을 빠뜨리면 나중에 왜 그 데이터가 바뀌었는지 재현할 수 없다. 조건 문자열 자체를 남기는 것이 핵심이고, 대상 목록이 너무 크면 조건과 건수만이라도 남긴다.

## 참고

- OWASP API Security Top 10 — 자원 소비와 권한
- NIST SP 800-53 — 변경 통제와 감사
- CIS Controls — 데이터 무결성 보호`,
    diagram: {
      type: 'bars',
      caption: '실수 한 번의 영향 범위',
      unit: '건',
      items: [
        { label: '개별 처리', value: 1, note: '한 건' },
        { label: '상한 50', value: 50, note: '제한됨' },
        { label: '상한 없음', value: 400, note: '조건에 따라' },
        { label: '조건 오류', value: 900, note: '전체' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '실행 전 확인',
      steps: [
        { label: '대상 수 계산', note: '조건 적용' },
        { label: '건수·표본 제시', note: '사람이 본다' },
        { label: '확인 받기', note: '되돌리기 어려움' },
        { label: '이전 값 기록', note: '복구 대비' },
      ],
    },
  },
  {
    slug: 'search-query-abuse',
    title: '검색 기능이 데이터 유출 통로가 될 때',
    body: `검색은 읽기 기능이라 위험이 낮게 다뤄진다. 그런데 검색은 조건을 바꿔 가며 반복할 수 있고, 결과의 유무 자체가 정보가 된다. 한 건씩 확인하는 것만으로도 데이터베이스 접근 없이 내용을 복원할 수 있다.

## 무엇이 새는가?

결과 목록만 보이는 검색에서도 여러 가지가 흘러나온다. 값을 바꿔 가며 물어보면 존재 여부가 드러나고, 그것이 쌓이면 목록이 된다.

| 흘러나오는 것 | 방법 |
| --- | --- |
| 값의 존재 | 결과 0건인지 아닌지 |
| 전체 규모 | 총 건수 표시 |
| 값의 분포 | 조건별 건수 비교 |
| 권한 범위 | 보이는 것과 아닌 것 |

총 건수를 보여 주는 기능이 특히 많은 것을 알려 준다. 조건을 조금씩 바꿔 가며 건수 변화를 보면 개별 값을 좁혀 갈 수 있다.

![검색으로 값을 좁히는 과정](/img/posts/search-query-abuse.svg)

## 조회 상한과 조회 이력

읽기에도 통제가 필요하다. 한 사람이 하루에 볼 수 있는 양에 상한을 두고, 누가 무엇을 조회했는지 남긴다.

| 통제 | 효과 |
| --- | --- |
| 건당 결과 상한 | 한 번에 긁는 양 제한 |
| 일일 누적 상한 | 반복 수집 억제 |
| 조회 이력 | 사후 확인 가능 |
| 조건 기록 | 무엇을 찾았는지 |

상한에 걸린 사용자를 즉시 막기보다 기록하고 관찰하는 편이 낫다. 정상 업무로 많이 조회하는 사람이 있고, 그들을 막으면 우회 경로가 생긴다.

## 결과를 어디까지 보여줄지

목록에 필요 이상의 항목을 실어 보내는 경우가 많다. 화면에 표시하지 않는 값도 응답에는 들어 있다.

| 항목 | 목록에 필요한가 |
| --- | --- |
| 식별자 | 필요 |
| 표시용 이름 | 필요 |
| 연락처·주민번호 | 불필요 |
| 내부 상태값 | 불필요 |
| 전체 주소 | 대개 불필요 |

![응답에 실리는 것](/img/posts/search-query-abuse-2.svg)

## 실제 응답을 확인한다

화면이 아니라 응답을 본다. 화면에서 가려진 값이 응답에는 그대로 있는 경우가 흔하다.

\`\`\`bash
# 검색 응답에 어떤 필드가 실려 오는지 전부 본다
curl -s 'https://example.com/api/search?q=kim' -H "Authorization: Bearer $TOKEN" \\
  | python3 -c 'import json,sys; d=json.load(sys.stdin); print(sorted(d["items"][0].keys()))'

# 상한이 걸려 있는지 — 큰 값을 요청해 본다
curl -s 'https://example.com/api/search?q=a&limit=100000' -H "Authorization: Bearer $TOKEN" \\
  | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["items"]))'
\`\`\`

요청한 만큼 그대로 돌아오면 상한이 없는 것이다. 목록 응답에 필요 없는 필드가 보이면 그것부터 뺀다.

## 권한이 조건에 반영되는가

검색 조건과 권한 조건이 분리돼 있으면 언젠가 어긋난다. 권한 조건을 질의의 일부로 강제하는 구조라야 새 검색 기능을 만들 때마다 빠뜨리지 않는다. 화면마다 권한을 확인하는 방식은 화면 수만큼 실수할 기회를 만든다.

## 참고

- OWASP API Security Top 10 — 과도한 데이터 노출
- 개인정보보호법 — 접속기록과 조회 통제
- NIST SP 800-53 — 정보 흐름 통제`,
    diagram: {
      type: 'flow',
      caption: '검색으로 값을 좁히는 과정',
      steps: [
        { label: '조건으로 질의', note: '결과 유무 확인' },
        { label: '건수 변화 관찰', note: '범위 좁힘' },
        { label: '반복', note: '자동화 가능' },
        { label: '값 복원', note: '직접 조회 없이', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '검색에 필요한 통제',
      layers: [
        { label: '결과 건수 상한', note: '한 번에 긁는 양' },
        { label: '일일 누적 상한', note: '반복 수집' },
        { label: '조회 이력', note: '사후 확인' },
        { label: '응답 필드 최소화', note: '노출 자체를 줄임' },
      ],
    },
  },
  {
    slug: 'pagination-data-leak',
    title: '페이지 나누기에서 데이터가 새는 자리',
    body: `목록을 나눠 주는 기능은 단순해 보이지만 접근 통제가 자주 빠진다. 첫 페이지는 제대로 걸러 놓고 다음 페이지를 가져오는 경로에서 같은 조건이 적용되지 않는 경우가 있다.

## 어디서 어긋나나?

페이지를 나누는 방식마다 새는 자리가 다르다. 커서 방식은 커서 값 자체가 정보를 담고 있어 조작 대상이 된다.

| 방식 | 새는 자리 |
| --- | --- |
| 오프셋 | 큰 값으로 범위 밖 접근 |
| 커서 | 커서 값 위조 |
| 페이지 번호 | 권한 재확인 누락 |
| 키셋 | 마지막 키 조작 |

커서를 그대로 노출하면 그 안에 담긴 정렬 키를 임의로 바꿔 보낼 수 있다. 서명하지 않은 커서는 사용자가 편집할 수 있는 질의 조건과 같다.

![페이지 방식별 취약 지점](/img/posts/pagination-data-leak.svg)

## 접근 통제를 어디에 두나

권한 조건이 첫 조회에만 붙고 다음 페이지 조회에는 빠지는 구조가 문제의 핵심이다. 조건은 페이지와 무관하게 항상 적용돼야 한다.

| 구조 | 결과 |
| --- | --- |
| 화면 진입 시 1회 확인 | 다음 페이지에서 누락 |
| 요청마다 확인 | 안전 |
| 질의에 조건 내장 | 가장 안전 |

## 총 건수도 정보다

접근할 수 없는 항목까지 포함한 총 건수를 돌려주면, 목록에 보이지 않아도 그만큼이 존재한다는 사실이 드러난다. 권한 밖 항목은 세지도 않아야 한다.

| 응답 항목 | 주의 |
| --- | --- |
| 총 건수 | 권한 적용 후 계산 |
| 다음 페이지 유무 | 같은 조건으로 판단 |
| 마지막 페이지 번호 | 총 건수와 같은 문제 |

![권한 조건의 위치](/img/posts/pagination-data-leak-2.svg)

## 경계에서 시험한다

정상 범위가 아니라 경계와 그 바깥을 넣어 본다.

\`\`\`bash
# 아주 큰 오프셋 — 범위를 넘어가면 무엇이 오는지
curl -s 'https://example.com/api/items?offset=999999&limit=20' -H "Authorization: Bearer $TOKEN" | head -c 300

# 음수와 0 — 검증이 없으면 질의가 엉뚱하게 돈다
curl -s 'https://example.com/api/items?offset=-1&limit=20' -H "Authorization: Bearer $TOKEN" | head -c 300

# 다른 사용자 계정의 커서를 그대로 써 본다
curl -s "https://example.com/api/items?cursor=$OTHER_CURSOR" -H "Authorization: Bearer $TOKEN" | head -c 300
\`\`\`

마지막 시험이 핵심이다. 다른 사람의 커서로 내 토큰이 남의 데이터를 받아 오면 커서에 권한 판단이 섞여 있는 것이다.

## 큰 오프셋 자체의 문제

오프셋이 커지면 데이터베이스가 앞의 행을 모두 읽고 버린다. 접근 통제와 별개로 성능 문제가 되고, 큰 값을 반복해 보내는 것만으로 부하를 만들 수 있다. 오프셋 상한을 두거나 키셋 방식으로 바꾸면 두 문제가 함께 해결된다.

## 참고

- OWASP API Security Top 10 — 객체 수준 인가
- NIST SP 800-53 — 최소 권한과 정보 흐름
- PostgreSQL 문서 — LIMIT/OFFSET 의 비용`,
    diagram: {
      type: 'matrix',
      caption: '방식별 위험',
      x: ['권한 매 요청 확인', '첫 요청만'],
      y: ['커서 서명함', '안 함'],
      cells: ['안전', '조건 누락', '커서 위조', '둘 다 뚫림'],
    },
    diagram2: {
      type: 'steps',
      caption: '권한 조건의 자리',
      steps: [
        { label: '질의에 내장', note: '빠뜨릴 수 없음' },
        { label: '요청마다 적용', note: '페이지 무관' },
        { label: '건수도 적용 후', note: '존재 노출 방지' },
        { label: '커서 서명', note: '위조 차단' },
      ],
    },
  },
  {
    slug: 'export-function-abuse',
    title: '내보내기 기능을 통제하는 기준',
    body: `엑셀이나 CSV 로 내보내는 기능은 업무에 꼭 필요하지만, 개인정보가 조직 밖으로 나가는 가장 흔한 경로이기도 하다. 화면에서 한 건씩 보는 것과 파일로 만 건을 받는 것은 같은 권한이라도 전혀 다른 일이다.

## 왜 따로 통제하나?

화면 조회는 흔적이 남고 양이 제한된다. 내보내기는 한 번에 전체를 만들고, 그 파일은 통제 밖으로 나간다. 나간 뒤에는 어디로 갔는지 알 방법이 없다.

| 구분 | 화면 조회 | 파일 내보내기 |
| --- | --- | --- |
| 한 번의 양 | 수십 건 | 수만 건 |
| 이후 통제 | 가능 | 불가능 |
| 흔적 | 조회 이력 | 파일은 추적 불가 |
| 2차 유출 | 어려움 | 쉬움 |

![내보내기 통제 지점](/img/posts/export-function-abuse.svg)

## 승인을 붙일 기준

모든 내보내기에 승인을 요구하면 업무가 막힌다. 위험이 큰 조합에만 붙인다.

| 조건 | 처리 |
| --- | --- |
| 개인정보 포함·소량 | 기록만 |
| 개인정보 포함·대량 | 승인 필요 |
| 개인정보 없음 | 기록만 |
| 전체 기간·전체 대상 | 승인 필요 |

건수와 항목 두 축으로 보면 기준이 단순해진다. 정보주체를 식별할 수 있는 항목이 들어 있고 건수가 많으면 승인이다.

## 무엇을 빼고 내보낼지

내보내기 화면에서 항목을 고르게 하고, 민감한 항목은 기본값에서 빼 둔다. 기본값이 전체 선택이면 대부분 그대로 내보낸다.

| 항목 | 기본 |
| --- | --- |
| 식별번호·주민번호 | 제외 |
| 연락처 | 제외 |
| 이름 | 선택 |
| 통계용 값 | 포함 |

![건수와 항목에 따른 처리](/img/posts/export-function-abuse-2.svg)

## 나간 파일에 표시를 남긴다

누가 언제 내보냈는지 파일 자체에 남기면, 유출됐을 때 경로를 좁힐 수 있다.

\`\`\`bash
# 내보낸 파일에 실행자와 시각을 주석 행으로 넣는다
{
  echo "# exported_by=$USER exported_at=$(date -Iseconds) job=$JOB_ID"
  cat data.csv
} > export-$JOB_ID.csv

# 파일 해시를 기록해 두면 나중에 같은 파일인지 대조할 수 있다
sha256sum export-$JOB_ID.csv | tee -a /var/log/export-hashes.log
\`\`\`

행마다 보이지 않는 표식을 넣는 방식도 있지만, 가공하면 사라지므로 과신하지 않는다. 기록의 목적은 추적이 아니라 억제에 가깝다.

## 기록을 실제로 보는가

내보내기 이력을 남기기만 하고 아무도 보지 않으면 억제 효과가 없다. 월 단위로 상위 실행자와 대량 건을 훑는 자리를 두면, 그 사실이 알려지는 것만으로 불필요한 내보내기가 준다. 이상한 것을 잡는 것보다 보고 있다는 사실이 더 크게 작용한다.

## 참고

- 개인정보보호법 — 개인정보의 안전성 확보조치 기준
- ISO/IEC 27002 — 정보 전송 보안
- NIST SP 800-53 — 데이터 반출 통제`,
    diagram: {
      type: 'layers',
      caption: '내보내기 통제 지점',
      layers: [
        { label: '항목 선택', note: '민감 항목 기본 제외' },
        { label: '건수 상한', note: '대량은 승인' },
        { label: '실행 기록', note: '누가 무엇을' },
        { label: '정기 검토', note: '보고 있다는 신호' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '승인 기준',
      x: ['소량', '대량'],
      y: ['개인정보 포함', '미포함'],
      cells: ['기록', '승인', '기록', '기록'],
    },
  },
  {
    slug: 'soft-delete-risk',
    title: '지운 것처럼 보이는 데이터의 위험',
    body: `삭제 버튼을 눌렀는데 실제로는 표시만 바뀌는 구조가 흔하다. 복구를 위해 필요한 설계지만, 사용자에게는 지워졌다고 말해 놓고 데이터는 남아 있다는 뜻이기도 하다. 이 간극이 법적 문제와 보안 문제를 동시에 만든다.

## 무엇이 문제인가?

지워졌다고 안내받은 사용자는 그 데이터가 없다고 믿는다. 실제로는 남아 있으므로, 파기 의무가 걸린 정보라면 의무를 지키지 못한 상태가 된다.

| 문제 | 내용 |
| --- | --- |
| 파기 의무 | 지웠다고 했는데 남아 있음 |
| 조회 경로 | 필터를 빠뜨린 질의에 노출 |
| 백업 | 지운 뒤에도 계속 복제 |
| 통계 | 삭제 건이 집계에 섞임 |

필터 누락이 가장 자주 터진다. 새로 만든 질의나 관리 도구에서 삭제 표시를 보지 않으면 지운 데이터가 그대로 나온다.

![지운 데이터가 남는 경로](/img/posts/soft-delete-risk.svg)

## 두 단계로 나눈다

표시만 바꾸는 단계와 실제로 지우는 단계를 나누고, 사이에 기간을 둔다. 사용자에게도 그 기간을 알린다.

| 단계 | 상태 | 기간 |
| --- | --- | --- |
| 삭제 요청 | 조회 불가·복구 가능 | 30일 |
| 실제 파기 | 복구 불가 | 이후 |
| 백업에서 제거 | 순환 주기 경과 | 최대 90일 |

백업을 잊는 경우가 많다. 운영 데이터베이스에서 지워도 백업에는 남아 있고, 복원하면 되살아난다. 백업 보관 주기까지 포함해 파기 완료 시점을 정의해야 한다.

## 사용자에게 무엇을 말하나

"즉시 삭제"라고 안내하면 사실과 다르다. 복구 기간이 있다면 그 기간을 알리고, 그 안에는 되살릴 수 있음을 함께 말한다.

| 안내 | 정확성 |
| --- | --- |
| 즉시 삭제됩니다 | 사실과 다름 |
| 30일 후 완전히 삭제됩니다 | 정확 |
| 30일 안에는 복구할 수 있습니다 | 정확 |

![파기 단계](/img/posts/soft-delete-risk-2.svg)

## 남아 있는 것을 실제로 센다

삭제 표시된 데이터가 얼마나 쌓여 있는지, 파기 기한을 넘긴 것은 없는지 본다.

\`\`\`sql
-- 삭제 표시 후 기한을 넘긴 데이터
SELECT count(*) AS 파기_대상
FROM users
WHERE deleted_at IS NOT NULL
  AND deleted_at < now() - interval '30 days';

-- 삭제 표시를 보지 않는 질의가 있는지는 코드에서 찾는다
-- deleted_at 조건이 빠진 users 조회를 훑는다
\`\`\`

파기 대상이 0 이 아니면 파기 작업이 돌지 않고 있는 것이다. 기한을 정해 두고 실행을 자동화하지 않으면 이 값은 계속 늘어난다.

## 지우지 않아야 하는 것과 구분한다

거래 기록처럼 법으로 보존해야 하는 데이터가 있다. 이런 것은 삭제 요청이 와도 지울 수 없고, 대신 식별자를 분리해 개인을 특정할 수 없게 만드는 방법을 쓴다. 무엇을 지우고 무엇을 남길지 항목 단위로 정해 두지 않으면 요청이 올 때마다 판단이 달라진다.

## 참고

- 개인정보보호법 — 파기 의무와 예외
- ISO/IEC 27002 — 정보 삭제
- NIST SP 800-88 — 매체 정보 완전 삭제 지침`,
    diagram: {
      type: 'flow',
      caption: '지운 데이터가 남는 경로',
      steps: [
        { label: '삭제 요청', note: '표시만 변경' },
        { label: '필터 누락 질의', note: '그대로 노출', danger: true },
        { label: '백업에 복제', note: '계속 남음' },
        { label: '복원 시 부활', note: '파기 실패' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '파기 단계',
      steps: [
        { label: '조회 차단', note: '복구 가능' },
        { label: '기간 경과', note: '30일' },
        { label: '실제 파기', note: '복구 불가' },
        { label: '백업 순환', note: '최대 90일' },
      ],
    },
  },
]
