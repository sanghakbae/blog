import type { SeedPost } from './types'

/** 311~320 — 취약점 진단 실무 */
export const posts32: SeedPost[] = [
  {
    slug: 'web-assessment-process',
    title: '웹 취약점 진단 절차와 산출물 정리',
    body: `진단을 도구 실행으로 이해하면 결과가 도구 보고서 그대로 나온다. 그 보고서는 개발팀이 고칠 수 없는 형태다. 쓸모 있는 진단은 범위를 정하고, 기능을 이해하고, 도구와 사람이 나눠 확인하고, 고칠 수 있는 단위로 정리하는 과정이다. 각 단계의 산출물이 다음 단계의 입력이 된다.

## 어떤 순서로 진행하는가?

| 단계 | 하는 일 | 산출물 |
| --- | --- | --- |
| 범위 합의 | 대상·기간·금지 행위 | 범위 문서 |
| 정보 수집 | 기능 목록, 권한 구조 | 기능·역할 표 |
| 자동 점검 | 도구로 넓게 훑기 | 원시 결과 |
| 수동 점검 | 인가·업무 논리 확인 | 확인된 결함 |
| 검증 | 오탐 제거, 재현 절차 확정 | 재현 가능한 목록 |
| 보고 | 위험도와 조치 방안 | 보고서 |
| 재점검 | 조치 확인 | 조치 확인서 |

자동 점검만으로는 인가 결함을 못 찾는다. 도구는 "이 요청이 통했다"는 것만 알고 "이 사용자가 이것을 봐도 되는가"를 모른다.

![단계별 산출물 연결](/img/posts/web-assessment-process.svg)

## 범위 합의에서 반드시 적을 것

여기서 빠지면 진단 중에 다툼이 생긴다. 특히 서비스 거부와 데이터 변경은 명시적으로 정해야 한다.

\`\`\`
대상        도메인·API·모바일 앱, 제외 대상 명시
계정        역할별 시험 계정 최소 2개씩 (권한 경계 시험용)
기간        점검 시간대, 부하 시험 가능 시간
금지        서비스 거부, 실 데이터 변경·삭제, 사회공학
연락        장애 발생 시 즉시 연락 경로
데이터      진단 중 확보한 데이터의 취급과 파기
\`\`\`

같은 역할의 계정이 두 개 필요한 이유는 수평 권한 경계를 시험하기 위해서다. 계정이 하나면 다른 사용자 자료 접근 시험을 할 수 없다.

![계정 구성](/img/posts/web-assessment-process-2.svg)

## 자동과 수동의 분담

| 결함 유형 | 자동 | 수동 |
| --- | --- | --- |
| 알려진 취약 버전 | 잘 찾는다 | 불필요 |
| 인젝션·스크립팅 | 상당히 찾는다 | 우회 확인 |
| 설정 오류 | 잘 찾는다 | 영향 판단 |
| 인가 누락 | 거의 못 찾는다 | 필수 |
| 업무 논리 결함 | 못 찾는다 | 필수 |
| 경쟁 조건 | 못 찾는다 | 필수 |

시간 배분을 여기에 맞춘다. 자동 점검은 짧게 돌리고, 확보한 시간을 인가와 업무 논리에 쓴다.

## 진단 자료를 어떻게 남기는가

재현 절차가 없는 결함은 고쳐지지 않는다. 요청과 응답을 그대로 남기고, 최소한의 단계로 줄여 적는다.

\`\`\`bash
# 확인된 결함마다 재현 요청을 파일로 남긴다
cat > findings/2026-09-08-idor-orders.http <<'EOF'
# 사용자 A 의 토큰으로 사용자 B 의 주문을 조회
GET /api/orders/10482 HTTP/1.1
Host: stg.example.com
Authorization: Bearer <A 계정 토큰>

# 기대: 403 / 실제: 200 + B 의 주문 상세
EOF
\`\`\`

## 실제로 이렇게 터진다

점검 범위를 도메인 목록으로만 정한 사례가 있다. 실제 서비스는 여러 하위 도메인과 API 서버에 걸쳐 있었고, 그중 절반이 범위 밖이었다. 보고서는 깨끗했지만 실제 공격면은 그대로였다.

또 하나는 계정을 하나만 받은 경우다. 권한 차이로 생기는 문제를 확인할 수 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 도구를 돌리면 점검이다 | 도구는 일부만 본다 |
| 범위는 도메인이면 된다 | 기능과 권한 단위로 정해야 한다 |
| 계정 하나면 충분하다 | 권한별로 필요하다 |
| 운영 환경에서 해야 정확하다 | 위험이 크다 |
| 보고서 제출로 끝난다 | 재점검까지가 절차다 |

## 준비 단계에서 정할 것

| 항목 | 내용 |
| --- | --- |
| 범위 | 도메인·API·기능 목록 |
| 제외 | 하면 안 되는 것 |
| 계정 | 권한별 최소 2개씩 |
| 환경 | 운영 유사 환경 |
| 시간대 | 부하 시험 시간 |
| 연락 체계 | 장애 시 즉시 연락 |
| 데이터 | 시험 데이터 준비 |

권한별 계정 두 개씩이 중요하다. 같은 권한의 두 계정이 있어야 다른 사용자 데이터 접근을 시험할 수 있다.

## 진행 순서

1. 정보 수집 — 무엇이 있는지
2. 공격면 정리 — 진입점 목록
3. 자동 점검 — 알려진 유형
4. 수동 점검 — 인가·업무 로직
5. 심화 — 발견된 문제의 확장
6. 정리 및 보고
7. 조치 후 재점검

3번에 시간을 다 쓰는 경우가 많다. 자동 점검은 빨리 끝내고 4번에 시간을 쓴다. 실제 위험은 대부분 거기서 나온다.

## 시간 배분

| 단계 | 비중 |
| --- | --- |
| 정보 수집·공격면 | 20% |
| 자동 점검 | 10% |
| 인가·업무 로직 | 45% |
| 심화 확인 | 15% |
| 정리 | 10% |

## 기록

발견 즉시 기록한다. 재현 절차, 요청·응답, 시각, 사용 계정. 나중에 재구성하려면 시간이 더 든다. 특히 재현 절차는 조치 담당자가 확인할 때 그대로 쓴다.

## 참고

- OWASP Web Security Testing Guide
- OWASP Application Security Verification Standard
- NIST SP 800-115, 정보보안 시험·평가 지침`,
    diagram: {
      type: 'flow',
      caption: '진단 흐름',
      steps: [
        { label: '범위 합의', note: '금지 행위 명시' },
        { label: '기능 파악', note: '역할·권한 표' },
        { label: '자동 + 수동', note: '분담 명확히' },
        { label: '검증과 보고', note: '재현 절차 포함' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '결함 유형별 수동 점검 비중',
      unit: '%',
      items: [
        { label: '업무 논리', value: 95 },
        { label: '인가 누락', value: 90 },
        { label: '경쟁 조건', value: 85 },
        { label: '인젝션', value: 40 },
        { label: '취약 버전', value: 5 },
      ],
    },
  },
  {
    slug: 'authz-test-scenarios',
    title: '인가 우회 점검 시나리오 만드는 법',
    body: `인가 결함은 자동 도구가 거의 못 찾는다. 요청이 200 을 돌려주는 것이 정상인지 결함인지 판단하려면 그 자원의 주인이 누구인지 알아야 하고, 그것은 업무 지식이다. 그래서 인가 점검은 시나리오를 미리 설계하고 표로 만들어 하나씩 확인하는 방식이 효율적이다.

## 어떤 축으로 시나리오를 만드는가?

| 축 | 시험 내용 |
| --- | --- |
| 수평 | 같은 역할의 다른 사용자 자원 접근 |
| 수직 | 낮은 역할이 높은 기능 호출 |
| 미인증 | 토큰 없이 호출 |
| 만료 | 만료·폐기된 토큰으로 호출 |
| 다른 조직 | 테넌트 경계 넘기 |
| 상태 | 취소·삭제된 자원 조작 |
| 경로 | 화면 없이 API 직접 호출 |

일곱 개 축을 기능마다 곱하면 시나리오 표가 만들어진다. 기능이 40개면 280개인데, 대부분 같은 패턴이라 도구로 반복할 수 있다.

![시나리오 조합](/img/posts/authz-test-scenarios.svg)

## 반복을 자동화한다

토큰만 바꿔 같은 요청을 보내고 응답 코드를 비교하는 방식이면 대량 확인이 가능하다.

\`\`\`bash
# 같은 요청을 역할별 토큰으로 보내 응답을 비교한다
while IFS=' ' read -r method path; do
  for role in admin user_a user_b none; do
    tok=$(cat "tokens/$role" 2>/dev/null)
    code=$(curl -s -o /dev/null -w '%{http_code}' -X "$method" \\
      "https://stg.example.com$path" \\
      \${tok:+-H "Authorization: Bearer $tok"})
    printf '%-6s %-40s %-8s %s\\n' "$method" "$path" "$role" "$code"
  done
done < endpoints.txt | tee authz-matrix.txt
\`\`\`

결과 표에서 기대와 다른 칸을 찾는다. user_b 가 user_a 의 자원에 200 을 받은 칸, none 이 200 을 받은 칸이 결함이다.

![결과 판정](/img/posts/authz-test-scenarios-2.svg)

## 놓치기 쉬운 지점

| 지점 | 이유 |
| --- | --- |
| 목록 API | 필터로 가려도 개수·집계가 새어 나감 |
| 파일 내려받기 | 별도 경로라 검증이 빠진다 |
| 내보내기 | 조회는 막고 내보내기는 열려 있다 |
| 웹훅·콜백 | 인증 경로가 다르다 |
| 관리자 API | 화면만 숨기고 API 는 열려 있다 |
| 상태 변경 | 조회는 막고 수정은 안 막는다 |

조회 권한만 시험하고 변경 권한을 빠뜨리는 경우가 특히 흔하다. GET 뿐 아니라 PUT·PATCH·DELETE 를 같은 표로 확인한다.

## 결함을 어떻게 쓰는가

인가 결함은 재현이 쉬워서 보고가 명확해야 한다. 누구의 토큰으로 누구의 자원에 접근했는지, 무엇이 노출됐는지를 한 줄로 적는다. 그리고 같은 원인으로 발생하는 다른 엔드포인트를 함께 나열한다 — 하나만 고치면 나머지가 남는다.

## 실제로 이렇게 터진다

인가 점검을 관리자 화면 접근 여부로만 한 사례가 있다. 화면은 막혀 있었지만 API 는 열려 있었다. 화면을 거치지 않고 직접 호출하면 그대로 동작했다.

또 하나는 식별자를 순차 번호로 두고 소유자 확인을 하지 않은 경우다. 번호를 바꾸면 다른 사람의 주문이 보였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 화면이 막히면 된다 | API 를 직접 부른다 |
| 로그인만 확인하면 된다 | 무엇에 대한 권한인지가 핵심이다 |
| 식별자를 추측 못 한다 | 순차 번호는 쉽다 |
| 자동 도구가 찾는다 | 인가는 자동화가 어렵다 |
| 읽기만 되면 위험이 낮다 | 유출은 읽기에서 난다 |

## 시나리오 목록

| 시나리오 | 확인 |
| --- | --- |
| 비로그인으로 호출 | 인증 요구 |
| 다른 사용자 식별자 | 소유자 검증 |
| 낮은 권한으로 상위 기능 | 권한 검증 |
| 다른 조직 자원 | 테넌트 분리 |
| 삭제된 계정으로 | 세션 무효화 |
| 권한 변경 후 기존 세션 | 즉시 반영 |
| 상태를 건너뛴 요청 | 흐름 검증 |
| 대량 조회 | 건수 제한 |

## 조합으로 본다

인가 시험은 주체와 대상, 동작의 조합이다.

| 축 | 값 |
| --- | --- |
| 주체 | 비로그인·일반·다른 일반·관리자 |
| 대상 | 자기 것·남의 것·없는 것·삭제된 것 |
| 동작 | 조회·생성·수정·삭제 |

이 조합을 표로 만들고 하나씩 확인한다. 빈칸 없이 채우면 인가 결함이 거의 남지 않는다.

## 자동화

완전 자동화는 어렵지만 반복 부분은 자동화할 수 있다.

\`\`\`bash
# 같은 요청을 권한이 다른 두 계정으로 보내 응답을 비교한다
for tok in "$TOKEN_A" "$TOKEN_B"; do
  curl -s -o /dev/null -w '%{http_code} ' -H "Authorization: Bearer $tok" \
    "https://api.example.com/orders/1001"
done; echo
\`\`\`

두 응답이 모두 200 이면 소유자 검증이 없는 것이다. 이 방식으로 주요 엔드포인트를 훑으면 명백한 결함이 빠르게 드러난다.

## 회귀 시험

인가 결함은 코드 변경으로 다시 생긴다. 발견된 항목을 자동 시험으로 만들어 파이프라인에 넣는다. 한 번 고친 것이 다시 열리는 일이 실제로 자주 있다.

## 참고

- OWASP — Authorization Testing Guide, Access Control Cheat Sheet
- OWASP API Security Top 10 — 객체 수준 인가
- NIST SP 800-53 AC-3 Access Enforcement`,
    diagram: {
      type: 'matrix',
      caption: '기대 응답',
      x: ['자기 자원', '남의 자원'],
      y: ['인증됨', '미인증'],
      cells: ['200 정상', '403 이어야 함', '401 이어야 함', '401 이어야 함'],
    },
    diagram2: {
      type: 'layers',
      caption: '점검 축',
      layers: [
        { label: '수평·수직', note: '역할 간 경계' },
        { label: '미인증·만료', note: '토큰 상태' },
        { label: '테넌트', note: '조직 경계' },
        { label: '메서드별', note: '조회와 변경' },
      ],
    },
  },
  {
    slug: 'api-spec-testing',
    title: 'API 명세로 진단 범위 정하는 방법',
    body: `화면을 눌러 가며 요청을 수집하는 방식으로는 API 의 절반도 못 찾는다. 화면에서 안 쓰는 엔드포인트, 옛 버전, 내부용 경로가 남아 있고 그것들이 대개 검증이 약하다. API 명세가 있으면 범위를 훨씬 넓고 정확하게 잡을 수 있다. 없으면 만드는 것부터가 진단의 일부다.

## 엔드포인트 명세와 실제가 어긋나는 지점

| 상황 | 위험 |
| --- | --- |
| 명세에 없는 엔드포인트 | 검토·시험을 받지 않았다 |
| 명세에 있으나 사라진 것 | 문서만 남음, 혼선 |
| 필드가 명세보다 많다 | 의도치 않은 데이터 노출 |
| 필드가 명세보다 적다 | 클라이언트 오류 |
| 인증 표기 누락 | 인증 없이 열려 있음 |
| 구 버전 경로 | 새 검증이 적용되지 않음 |

명세보다 응답 필드가 많은 것이 개인정보 노출의 흔한 원인이다. 화면에 안 보여도 응답에는 들어 있다.

![명세와 실제의 차이](/img/posts/api-spec-testing.svg)

## 명세를 진단 입력으로 바꾼다

명세에서 엔드포인트 목록을 뽑아 앞서의 인가 점검 표에 넣는다. 이 변환을 한 번 만들어 두면 이후 진단이 훨씬 빠르다.

\`\`\`bash
# OpenAPI 명세에서 메서드·경로·인증 요구 여부를 뽑는다
python3 - openapi.json <<'PY'
import json, sys
spec = json.load(open(sys.argv[1]))
glob = bool(spec.get('security'))
for path, ops in spec['paths'].items():
    for m, op in ops.items():
        if m not in ('get','post','put','patch','delete'): continue
        sec = op.get('security', None)
        need = glob if sec is None else bool(sec)
        print(f"{m.upper():6} {path:45} 인증 {'필요' if need else '없음'}")
PY
\`\`\`

인증이 필요 없다고 표기된 경로를 먼저 확인한다. 의도한 공개인지, 표기 누락인지 판단해야 한다.

![명세 활용](/img/posts/api-spec-testing-2.svg)

## 명세에 없는 것을 어떻게 찾는가

| 수단 | 찾는 것 |
| --- | --- |
| 프런트엔드 번들 검색 | 화면이 부르는 실제 경로 |
| 모바일 앱 문자열 | 앱 전용 API |
| 게이트웨이 라우팅 설정 | 등록된 전체 경로 |
| 접근 로그 | 실제로 호출되는 경로 |
| 소스 저장소의 라우터 정의 | 구현된 전체 |

접근 로그가 가장 정확하다. 실제 트래픽에 나타나는 경로는 살아 있는 경로다.

\`\`\`bash
# 접근 로그에서 실제 호출된 경로를 뽑아 명세와 대조한다
awk '{print $6, $7}' /var/log/nginx/access.log |
  sed 's/[0-9a-f-]\\{8,\\}/{id}/g; s/[0-9]\\{3,\\}/{id}/g' |
  sort -u > /tmp/live-paths.txt
comm -23 /tmp/live-paths.txt /tmp/spec-paths.txt | head -30    # 명세에 없는 것
\`\`\`

## 진단 후 명세를 관리 대상으로 만든다

명세를 코드에서 생성하고, 변경이 검토를 거치게 하면 어긋남이 줄어든다. 그리고 파이프라인에서 명세 대비 실제 응답을 비교하는 검사를 넣으면, 새 필드가 조용히 추가되는 것을 잡을 수 있다.

## 실제로 이렇게 터진다

명세에 없는 엔드포인트가 살아 있던 사례가 있다. 문서화되지 않은 관리용 API 였고 인증도 없었다. 점검은 명세 기준으로만 진행돼 발견되지 않았다.

또 하나는 명세와 구현이 어긋난 경우다. 명세에는 필수 필드였지만 구현은 없어도 통과시켰다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 명세대로 구현돼 있다 | 어긋나는 경우가 흔하다 |
| 명세에 있는 것만 있다 | 문서화 안 된 것이 있다 |
| 명세 기반 점검이면 충분하다 | 명세 밖을 함께 봐야 한다 |
| 스키마 검증이 있으면 안전하다 | 적용되지 않는 경로가 있다 |
| 문서는 개발 산출물이다 | 공격면 목록이기도 하다 |

## 명세를 어떻게 쓰나

| 용도 | 방법 |
| --- | --- |
| 시험 케이스 생성 | 파라미터 조합 자동 생성 |
| 경계값 도출 | 타입·범위에서 |
| 인가 시험 대상 목록 | 엔드포인트 전수 |
| 구현 일치 확인 | 응답 스키마 검증 |

## 명세 밖 찾기

| 방법 | 대상 |
| --- | --- |
| 클라이언트 코드 분석 | 앱·프런트에서 호출하는 주소 |
| 자바스크립트 번들 검색 | 엔드포인트 문자열 |
| 경로 추측 | 흔한 관리 경로 |
| 버전 변형 | v1 이 있으면 v0, v2 |
| 메서드 변형 | GET 만 문서화, PUT 이 살아 있음 |

메서드 변형이 자주 걸린다. 문서에 GET 만 있는 경로에 다른 메서드를 보내면 동작하는 경우가 있다.

\`\`\`bash
# 프런트 번들에서 API 경로 추출 — 명세와 대조한다
curl -s https://example.com/assets/main.js \
  | grep -oE '"/api/[a-zA-Z0-9/_{}-]+"' | tr -d '"' | sort -u

# 명세에 정의된 경로
python3 -c "
import json,sys; print('\n'.join(json.load(open('openapi.json'))['paths']))" | sort
\`\`\`

두 목록의 차이가 문서화되지 않은 영역이다.

## 스키마 검증 적용 범위

명세에 스키마가 있어도 구현에서 검증하지 않으면 소용없다. 어느 경로에 검증이 걸려 있는지 확인한다. 미들웨어로 전역 적용하는 것이 누락을 막는 방법이다.

## 명세 관리

| 원칙 | 이유 |
| --- | --- |
| 코드에서 생성 | 어긋남 방지 |
| 변경 시 리뷰 | 새 공격면 인지 |
| 폐기 엔드포인트 제거 | 실제로 지운다 |
| 내부 API 도 문서화 | 목록에서 빠지지 않게 |

## 참고

- OpenAPI 명세, 보안 스키마 정의
- OWASP API Security Top 10 — 자원 관리 미흡
- OWASP — Web Security Testing Guide, API 시험 절차`,
    diagram: {
      type: 'flow',
      caption: '범위 확정',
      steps: [
        { label: '명세 수집', note: '없으면 생성' },
        { label: '실제 경로 수집', note: '로그·번들·라우터' },
        { label: '차이 목록화', note: '명세 밖 경로' },
        { label: '점검 표에 투입', note: '인가 시나리오' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '차이 판정',
      x: ['명세에 있음', '없음'],
      y: ['실제 존재', '존재하지 않음'],
      cells: ['정상', '검토 대상', '문서 정리', '해당 없음'],
    },
  },
  {
    slug: 'scanner-triage',
    title: '진단 도구 결과에서 오탐 걸러내기',
    body: `도구 보고서를 그대로 전달하면 개발팀의 신뢰를 잃는다. 수백 건 중 상당수가 오탐이고, 실제 결함도 위험도가 도구 기준으로 매겨져 있어 우선순위가 실제와 맞지 않는다. 결과를 걸러 내는 작업이 진단의 실질적인 가치다.

## 어떤 것이 오탐으로 나오는가?

| 도구 판정 | 실제 |
| --- | --- |
| 취약한 버전 사용 | 그 기능을 안 쓰는 경우 |
| 스크립팅 가능 | 출력 지점이 없거나 이스케이프됨 |
| 정보 노출 | 공개해도 되는 정보 |
| 보안 헤더 누락 | 앞단 프록시가 추가함 |
| 인증 없이 접근 | 의도된 공개 API |
| 디렉터리 목록 | 정적 자산 전용 경로 |

의존성 스캐너의 결과가 특히 그렇다. 취약한 함수를 우리 코드가 호출하지 않으면 도달 가능성이 없다.

![도구 결과의 분류](/img/posts/scanner-triage.svg)

## 세 가지 질문으로 판정한다

각 항목에 같은 세 질문을 던지면 판정이 빨라지고 일관성이 생긴다.

\`\`\`
1. 도달 가능한가?     그 코드·경로가 실제로 실행되는가
2. 조건이 성립하는가?  인증·권한·설정 전제가 실제 환경과 같은가
3. 영향이 무엇인가?    성립하면 무엇을 얻는가 (데이터·권한·가용성)
\`\`\`

세 질문 모두 답이 있으면 실제 결함이다. 하나라도 막히면 오탐이거나 위험도가 낮다.

![판정 흐름](/img/posts/scanner-triage-2.svg)

## 의존성 결과를 재현으로 확정한다

판정이 애매하면 재현을 시도한다. 재현되지 않는 항목은 보고서에 넣지 않거나 별도 절로 분리한다.

\`\`\`bash
# 취약 의존성의 도달 가능성 확인 — 우리 코드가 그 함수를 부르는가
npm ls lodash 2>/dev/null | head
grep -rn "from 'lodash'" src/ | head          # 직접 사용 여부
grep -rn 'merge\\|defaultsDeep\\|zipObjectDeep' src/ | head   # 취약 함수 사용

# 보안 헤더가 실제 응답에 있는지 (프록시 추가 여부 포함)
curl -sD - -o /dev/null https://example.com | grep -iE 'strict-transport|content-security|x-content-type'
\`\`\`

## 걸러 낸 근거를 남긴다

오탐으로 판정한 항목은 이유를 기록한다. 기록이 없으면 다음 진단에서 같은 항목을 처음부터 다시 판단해야 하고, 판단하는 사람이 바뀌면 결론도 바뀐다. 다음 진단에서 같은 항목이 다시 나오고, 근거가 없으면 처음부터 다시 판단해야 한다. 그리고 판정 근거가 남아 있으면 심사와 감사 대응에서 그대로 쓸 수 있다.

| 기록 항목 | 내용 |
| --- | --- |
| 항목 식별자 | 도구·규칙 이름 |
| 판정 | 오탐 / 위험 없음 / 조치 예정 |
| 근거 | 도달 불가 이유, 대체 통제 |
| 유효 기간 | 코드가 바뀌면 다시 본다 |

## 실제로 이렇게 터진다

자동 점검 결과 3천 건을 그대로 개발팀에 전달한 사례가 있다. 개발팀은 확인을 포기했고, 그 안에 있던 진짜 문제도 함께 묻혔다.

또 하나는 오탐을 걸러내면서 진짜 문제를 함께 제외한 경우다. 비슷해 보였지만 조건이 달랐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 도구가 판정한다 | 사람이 확인해야 한다 |
| 심각도가 우선순위다 | 도달 가능성이 더 중요하다 |
| 전부 전달하는 것이 성실하다 | 묻힌다 |
| 오탐은 도구 탓이다 | 설정과 맥락의 문제다 |
| 건수가 성과다 | 조치된 수가 성과다 |

## 분류 절차

1. 중복을 묶는다 — 같은 원인의 여러 위치
2. 도달 가능성을 확인한다 — 실제로 노출되나
3. 재현한다 — 실제로 동작하나
4. 영향을 판단한다 — 성공하면 무엇이 되나
5. 우선순위를 매긴다
6. 조치 방법과 함께 전달한다

3번을 건너뛴 항목은 전달하지 않는다. 재현되지 않는 지적을 받은 개발팀은 다음부터 신뢰하지 않는다.

## 우선순위 기준

| 축 | 질문 |
| --- | --- |
| 노출 | 인터넷에서 접근 가능한가 |
| 인증 | 인증 없이 가능한가 |
| 영향 | 데이터·권한·가용성 |
| 난이도 | 특별한 조건이 필요한가 |
| 악용 사례 | 실제로 쓰이고 있나 |

인증 없이 인터넷에서 가능한 것이 최우선이다. 심각도 등급보다 이 조합이 실질적이다.

## 오탐 관리

오탐으로 판정한 것은 근거와 함께 기록한다. 다음 점검에서 같은 항목이 또 나오기 때문이다.

| 기록 항목 | 내용 |
| --- | --- |
| 항목 | 무엇 |
| 판정 | 오탐 사유 |
| 판정자 | 누가 |
| 유효 기간 | 언제까지 |
| 재검토 조건 | 무엇이 바뀌면 |

유효 기간을 두는 이유는, 코드가 바뀌면 오탐이 진짜가 될 수 있기 때문이다.

## 전달 형식

| 항목 | 내용 |
| --- | --- |
| 무엇이 문제인가 | 한 줄 |
| 어디인가 | 파일·경로 |
| 어떻게 재현하나 | 절차 |
| 무엇이 위험한가 | 구체적 결과 |
| 어떻게 고치나 | 예시 코드 |
| 언제까지 | 기한 |

마지막 두 항목이 조치율을 정한다. 고치는 방법이 없으면 미뤄진다.

## 참고

- OWASP — Vulnerability Management Guide
- CVSS 명세 — 환경 지표를 통한 조정
- NIST SP 800-115, 시험 결과 분석`,
    diagram: {
      type: 'flow',
      caption: '판정 절차',
      steps: [
        { label: '도달 가능성', note: '실행되는 경로인가' },
        { label: '전제 조건', note: '실제 환경과 같은가' },
        { label: '영향 판단', note: '얻는 것이 무엇인가' },
        { label: '재현 또는 배제', note: '근거 기록' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '결과 분류',
      x: ['도달 가능', '도달 불가'],
      y: ['영향 큼', '영향 작음'],
      cells: ['즉시 조치', '기록 후 보류', '일정 조치', '오탐 처리'],
    },
  },
  {
    slug: 'blackbox-limits',
    title: '소스 없이 하는 점검의 한계와 보완',
    body: `외부에서만 보는 점검은 실제 공격자의 시야와 같다는 장점이 있다. 그러나 놓치는 것이 분명히 있고, 무엇을 못 봤는지 모른 상태로 "점검 완료" 가 되는 것이 문제다. 한계를 명시하고 다른 수단으로 보완하는 것이 정직한 진단이다.

## 모의침투로 무엇을 못 보는가?

| 못 보는 것 | 이유 |
| --- | --- |
| 조건부로만 실행되는 경로 | 외부에서 조건을 만들 수 없다 |
| 오류 처리 분기 | 오류를 유발할 수 없는 경우 |
| 암호 구현의 세부 | 응답만으로는 알 수 없다 |
| 권한 판정 위치 | 통과했는지 우회했는지 구분 어려움 |
| 배치·비동기 처리 | 외부 요청과 연결되지 않는다 |
| 비밀값 관리 방식 | 저장소 내부 |
| 남아 있는 미사용 코드 | 호출 경로가 없다 |

권한 판정 위치를 모르는 것이 특히 크다. 화면이 막혀 있어도 서버가 막고 있는지 클라이언트가 숨긴 것인지 외부에서는 구분이 어렵다.

![외부 시야의 경계](/img/posts/blackbox-limits.svg)

## 어떻게 보완하는가

방법을 섞으면 각자의 사각이 줄어든다. 전부 할 수 없으면 위험이 큰 영역만 골라 깊게 본다.

\`\`\`
외부 점검     실제 노출 확인. 우선순위 판단에 유리
소스 검토     조건 분기·암호 구현·권한 판정 위치 확인
설정 검토     인프라·클라우드 설정, 외부에서 안 보이는 통제
설계 검토     신뢰 경계와 데이터 흐름
로그 확인     실제로 무엇이 호출되고 기록되는지
\`\`\`

![방법별 사각 보완](/img/posts/blackbox-limits-2.svg)

## 최소한 받아야 하는 정보

완전한 소스가 아니어도 몇 가지만 있으면 점검 품질이 크게 올라간다.

| 자료 | 쓰임 |
| --- | --- |
| 역할·권한 표 | 인가 시나리오 설계 |
| API 명세 | 범위 확정 |
| 데이터 흐름도 | 민감 데이터 경로 |
| 아키텍처 도식 | 신뢰 경계 |
| 시험 계정 | 역할별 2개 이상 |
| 최근 변경 이력 | 새 기능 우선 점검 |

이 자료를 받는 것이 소스를 받는 것보다 현실적이고, 효과도 크다. 소스 전체를 받으면 읽는 데만 시간이 다 가고, 정작 위험한 지점을 놓치기 쉽다. 반면 역할 표와 데이터 흐름도가 있으면 어디를 깊게 볼지 바로 정해진다. 자료를 못 받는 경우에는 점검 초반에 시간을 들여 기능을 훑고 직접 표를 만드는 편이 낫다. 그 표가 없으면 이후 모든 판단이 추측에 기대게 된다.

## 보고서에 한계를 적는다

무엇을 점검하지 않았는지 적지 않으면, 점검한 범위가 전체로 오해된다. 다음 세 가지를 명시한다.

\`\`\`
점검 범위    실제로 요청을 보낸 대상과 기능
미점검       접근 권한이 없어 못 본 것, 시간상 제외한 것
가정         환경이 운영과 같다고 가정한 부분
\`\`\`

## 실제로 이렇게 터진다

외부 점검에서 문제가 없다고 나온 서비스가 몇 달 뒤 침해된 사례가 있다. 침해 경로는 내부 관리 화면이었고, 외부에서는 보이지 않았다.

또 하나는 점검 기간에 특정 기능이 배포되지 않았던 경우다. 점검 대상에 없던 코드가 문제였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 통과하면 안전하다 | 보이는 범위만 봤다 |
| 외부 시각이 실제 위험이다 | 내부 경로가 더 많이 쓰인다 |
| 점검 결과는 오래 간다 | 코드가 바뀌면 무효다 |
| 도구가 다 본다 | 로직은 못 본다 |
| 한 번이면 된다 | 주기가 필요하다 |

## 무엇을 못 보나

| 영역 | 이유 |
| --- | --- |
| 인증 없이 접근 불가한 영역 | 계정이 없으면 못 본다 |
| 내부 전용 시스템 | 외부에서 도달 불가 |
| 배치·비동기 처리 | 외부 요청과 무관 |
| 조건부 실행 코드 | 특정 상태에서만 |
| 설정 문제 | 응답에 드러나지 않는다 |
| 저장 데이터 보호 | 외부에서 확인 불가 |
| 의존성 취약점 | 버전 노출이 없으면 |

## 보완 방법

| 방법 | 보완하는 영역 |
| --- | --- |
| 인증 계정 제공 | 로그인 후 영역 |
| 소스 코드 검토 | 로직·조건부 경로 |
| 설정 검토 | 인프라·서버 설정 |
| 의존성 분석 | 라이브러리 취약점 |
| 내부망 점검 | 내부 전용 시스템 |
| 아키텍처 검토 | 설계 결함 |

외부 점검만으로 보안 상태를 판단하지 않는다. 여러 방법을 조합해야 전체가 보인다.

## 조합 설계

| 주기 | 방법 |
| --- | --- |
| 상시 | 의존성·정적 분석 |
| 배포 시 | 자동 점검 |
| 분기 | 인증 계정 기반 점검 |
| 반기 | 소스 코드 검토 |
| 연 1회 | 종합 모의해킹 |
| 변경 시 | 해당 영역 집중 점검 |

## 결과 해석

점검 보고서에 "취약점 없음" 이 있다면 그것이 무엇을 뜻하는지 확인한다.

| 표현 | 실제 의미 |
| --- | --- |
| 취약점 없음 | 점검 범위·방법으로 발견하지 못함 |
| 안전함 | 그런 판정은 불가능하다 |
| 조치 완료 | 재점검으로 확인했나 |

보고서에 범위와 방법, 제약이 명시돼 있어야 결과를 제대로 읽을 수 있다. 그것이 없는 보고서는 해석할 수 없다.

## 참고

- OWASP Web Security Testing Guide — 화이트박스와 블랙박스 비교
- NIST SP 800-115, 시험 기법 선택
- OWASP ASVS — 검증 수준 정의`,
    diagram: {
      type: 'layers',
      caption: '보완 조합',
      layers: [
        { label: '외부 점검', note: '노출 확인' },
        { label: '소스 검토', note: '분기·구현' },
        { label: '설정 검토', note: '인프라 통제' },
        { label: '설계 검토', note: '신뢰 경계' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '자료 유무와 품질',
      x: ['권한 표·명세 제공', '미제공'],
      y: ['계정 여러 개', '계정 하나'],
      cells: ['인가 점검 가능', '추정 기반', '표면만', '기능 확인 수준'],
    },
  },
  {
    slug: 'risk-rating-findings',
    title: '진단 결과 위험도 산정 기준 세우기',
    body: `같은 결함이 진단자에 따라 상·중·하로 다르게 매겨지면, 개발팀은 우선순위를 신뢰하지 않는다. 도구가 매긴 점수를 그대로 쓰는 것도 문제다 — 그 점수는 일반적인 환경 기준이고 우리 환경의 조건이 반영되지 않았다. 기준을 문서로 정하고 그대로 적용해야 일관성이 생긴다.

## 무엇으로 결정하는가?

| 요소 | 판단 내용 |
| --- | --- |
| 영향 | 무엇을 얻는가 — 데이터·권한·가용성 |
| 범위 | 한 사용자인가 전체인가 |
| 난이도 | 인증 필요 여부, 필요한 조건 수 |
| 노출 | 인터넷인가 내부망인가 |
| 대체 통제 | 다른 통제가 실질적으로 막는가 |

영향과 범위가 주된 축이고, 나머지는 조정 요소로 쓰는 것이 실무적이다. 요소를 많이 쓰면 계산이 복잡해지고 결과에 대한 합의가 어려워진다.

![위험도 결정 축](/img/posts/risk-rating-findings.svg)

## 등급 정의를 문서로 고정한다

문장으로 정의해 두면 판정이 흔들리지 않는다. 예시를 함께 적는 것이 중요하다.

\`\`\`
치명   인증 없이 전체 사용자 데이터 접근 또는 서버 권한 획득
       예) 인증 우회, 원격 코드 실행, 전체 목록 노출
높음   인증 후 다른 사용자 데이터 접근 또는 권한 상승
       예) 인가 누락, 관리 기능 노출
보통   조건이 필요하거나 영향 범위가 제한적
       예) 저장형 스크립팅(관리자 화면), 정보 노출
낮음   직접 피해가 없고 다른 공격의 보조 수단
       예) 버전 노출, 보안 헤더 누락
\`\`\`

![등급 판정 예](/img/posts/risk-rating-findings-2.svg)

## 조치 기한을 등급에 연결한다

등급만 매기고 기한을 정하지 않으면 우선순위가 실제 작업으로 이어지지 않는다.

| 등급 | 조치 기한 | 임시 조치 |
| --- | --- | --- |
| 치명 | 24~72시간 | 기능 차단 검토 |
| 높음 | 2주 | 접근 제한 |
| 보통 | 1~2개월 | 필요 시 |
| 낮음 | 다음 정기 배포 | 불필요 |

기한을 넘긴 항목은 위험 수용으로 처리하고 책임자의 승인을 받는다. 그냥 남겨 두면 아무도 결정하지 않은 상태로 누적된다.

## 우리 환경 조건을 반영한다

CVSS 를 쓴다면 환경 지표를 채워 조정한다. 인터넷에 노출되지 않은 시스템의 원격 취약점은 실제 위험이 낮고, 반대로 개인정보를 다루는 시스템의 정보 노출은 기본 점수보다 높다.

\`\`\`
조정 상향   개인정보·결제 데이터 취급, 인터넷 노출, 다수 사용자 영향
조정 하향   내부망 전용, 추가 인증 필요, 대체 통제 존재
기록        조정한 이유를 항목마다 남긴다
\`\`\`

## 실제로 이렇게 터진다

점수 계산 도구가 준 등급을 그대로 옮긴 사례가 있다. 내부망에서만 접근 가능한 서버의 원격 실행 취약점이 최고 등급으로 나왔고, 인터넷에 열린 인증 우회는 중간으로 나왔다. 조치 순서가 뒤바뀌었다.

또 하나는 등급 기준을 문서화하지 않은 경우다. 담당자마다 다르게 매겨 일관성이 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 표준 점수가 정답이다 | 환경 맥락이 빠져 있다 |
| 등급이 높으면 먼저 고친다 | 도달 가능성이 먼저다 |
| 기준은 상식으로 통한다 | 문서화해야 일관된다 |
| 등급은 바뀌지 않는다 | 환경이 바뀌면 바뀐다 |
| 낮은 등급은 무시해도 된다 | 조합되면 커진다 |

## 등급을 정하는 축

| 축 | 질문 |
| --- | --- |
| 도달 가능성 | 누가 접근할 수 있나 |
| 인증 요구 | 인증 없이 되나 |
| 악용 난이도 | 특별한 조건이 필요한가 |
| 영향 범위 | 몇 명·몇 시스템 |
| 영향 종류 | 데이터·권한·가용성 |
| 법적 영향 | 신고 의무가 생기나 |

## 실무 기준표

| 등급 | 조건 |
| --- | --- |
| 심각 | 인터넷 + 인증 불필요 + 데이터·권한 전면 |
| 높음 | 인터넷 + 인증 필요 + 중대 영향 / 내부 + 전면 영향 |
| 중간 | 제한된 조건 + 부분 영향 |
| 낮음 | 특수 조건 필요 + 제한적 영향 |
| 정보 | 위험은 없으나 개선 권고 |

이 표를 조직 문서로 만들고 모든 점검에 같은 기준을 쓴다. 기준이 같아야 시간에 따른 추이를 볼 수 있다.

## 조합 위험

낮은 등급 여러 개가 이어지면 높은 위험이 된다.

| 조합 | 결과 |
| --- | --- |
| 정보 노출 + 약한 인증 | 계정 탈취 |
| 사용자 열거 + 잠금 없음 | 대량 시도 |
| 파일 업로드 + 경로 노출 | 코드 실행 |
| 열린 리다이렉트 + 인증 흐름 | 토큰 탈취 |

각각은 낮게 매겨지지만 함께 있으면 조치 우선순위가 올라간다. 보고서에 연결 관계를 명시한다.

## 등급 조정 기록

환경 정보로 등급을 조정했다면 근거를 남긴다.

| 항목 | 예 |
| --- | --- |
| 기본 등급 | 높음 |
| 조정 후 | 중간 |
| 근거 | 해당 기능이 내부망에서만 접근 가능 |
| 재검토 조건 | 외부 공개 시 |

재검토 조건이 중요하다. 나중에 그 조건이 바뀌면 등급이 올라간다.

## 참고

- CVSS v4.0 명세 — 환경 지표
- OWASP Risk Rating Methodology
- NIST SP 800-30, 위험 평가 수행 지침`,
    diagram: {
      type: 'matrix',
      caption: '영향과 범위',
      x: ['전체 사용자', '한 사용자'],
      y: ['권한·서버 획득', '정보 노출'],
      cells: ['치명', '높음', '높음', '보통'],
    },
    diagram2: {
      type: 'bars',
      caption: '등급별 조치 기한(일)',
      unit: '일',
      items: [
        { label: '치명', value: 3 },
        { label: '높음', value: 14 },
        { label: '보통', value: 45 },
        { label: '낮음', value: 90 },
      ],
    },
  },
  {
    slug: 'retest-verification',
    title: '재점검 절차와 조치 확인 기준',
    body: `조치했다는 응답을 받고 목록에서 지우면 결함이 남는다. 화면에서만 막고 API 는 그대로인 경우, 한 엔드포인트만 고치고 같은 원인의 나머지를 놓친 경우가 흔하다. 재점검은 진단의 마지막 단계가 아니라, 진단의 결과를 실제 개선으로 바꾸는 단계다.

## 무엇을 확인해야 조치로 인정하는가?

| 확인 항목 | 내용 |
| --- | --- |
| 원래 재현 절차 | 그대로 실행해 막혔는지 |
| 우회 시도 | 인코딩·메서드·경로 변형 |
| 동일 원인 | 같은 패턴의 다른 지점 |
| 부작용 | 정상 기능이 깨지지 않았는지 |
| 적용 범위 | 전 환경에 배포됐는지 |
| 회귀 방지 | 테스트가 추가됐는지 |

동일 원인 확인이 가장 자주 빠진다. 인가 누락 하나를 보고하면 그 엔드포인트만 고쳐지고, 같은 구조의 다른 열 곳은 남는다.

![조치 확인 범위](/img/posts/retest-verification.svg)

## 인가 우회 시도는 어디까지 하는가

간단한 변형만으로도 불완전한 조치가 드러난다. 입력값 차단으로 고친 경우가 특히 그렇다.

\`\`\`bash
# 원래 결함이 막혔는지, 변형으로 우회되는지 함께 본다
BASE='https://stg.example.com/api/files'
for v in "../../etc/passwd" "..%2f..%2fetc%2fpasswd" "....//....//etc/passwd" \\
         "%2e%2e%2f%2e%2e%2fetc%2fpasswd" "..\\\\..\\\\etc\\\\passwd"; do
  printf '%-40s %s\\n' "$v" \\
    "$(curl -s -o /dev/null -w '%{http_code}' "$BASE?path=$v" -H "Authorization: Bearer $TOK")"
done

# 메서드 변형 — GET 만 막은 경우가 있다
for m in GET POST PUT PATCH DELETE HEAD; do
  printf '%-7s %s\\n' "$m" \\
    "$(curl -s -o /dev/null -w '%{http_code}' -X "$m" "$BASE/10482" -H "Authorization: Bearer $TOK_A")"
done
\`\`\`

![우회 시험 축](/img/posts/retest-verification-2.svg)

## 조치 상태를 어떻게 분류하는가

이분법으로 나누면 애매한 것이 억지로 한쪽에 들어간다. 네 가지로 나누는 것이 실제에 맞다.

| 상태 | 의미 | 다음 행동 |
| --- | --- | --- |
| 조치 완료 | 재현 불가, 우회 불가 | 종료 |
| 부분 조치 | 일부 경로만 | 잔여 목록 재발행 |
| 대체 통제 | 근본 수정은 아니나 실질 차단 | 기한 두고 재검토 |
| 위험 수용 | 고치지 않기로 결정 | 승인자·사유·기한 기록 |

부분 조치를 완료로 처리하지 않는 것이 중요하다. 잔여 항목을 새 결함으로 발행해 추적한다.

## 재발을 어떻게 막는가

같은 결함이 다음 진단에 다시 나오면 조치가 코드가 아니라 개별 수정에 머문 것이다. 공통 계층에 검증을 넣고, 회귀 테스트를 추가하고, 같은 실수가 가능한 지점을 코드 검사 규칙으로 만든다. 이 세 가지가 갖춰지면 결함이 유형 단위로 사라진다.

## 실제로 이렇게 터진다

조치 완료 보고를 받고 재점검 없이 종결한 사례가 있다. 몇 달 뒤 같은 취약점이 다시 발견됐다. 처음에 제대로 고쳐지지 않았던 것이다.

또 하나는 재점검에서 해당 경로만 확인한 경우다. 같은 원인의 다른 위치는 그대로였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 조치 보고면 충분하다 | 확인해야 한다 |
| 같은 요청으로 확인하면 된다 | 우회 경로를 봐야 한다 |
| 재점검은 빠르다 | 원인 확산 확인이 필요하다 |
| 한 번 고치면 안 돌아온다 | 회귀가 흔하다 |
| 재점검은 형식이다 | 여기서 미흡 조치가 드러난다 |

## 재점검에서 확인할 것

| 항목 | 방법 |
| --- | --- |
| 원래 재현 절차 | 동일하게 시도 |
| 우회 변형 | 인코딩·대소문자·경로 변형 |
| 같은 원인의 다른 위치 | 코드 전체 검색 |
| 부작용 | 정상 기능 동작 |
| 근본 원인 | 증상만 막았나 |

두 번째와 세 번째가 핵심이다. 특정 문자열만 차단하는 식의 조치는 우회된다.

## 조치 유형별 판정

| 조치 | 판정 |
| --- | --- |
| 근본 원인 제거 | 완료 |
| 입력 검증 추가 | 우회 확인 후 완료 |
| 특정 패턴 차단 | 미흡 — 우회 가능 |
| 기능 비활성화 | 임시 — 계획 필요 |
| 접근 제한 | 완화 — 위험 잔존 |
| 위험 수용 | 승인 기록 필요 |

"완화" 와 "완료" 를 구분해 기록한다. 완화는 조건이 바뀌면 다시 위험해진다.

## 회귀 방지

\`\`\`bash
# 발견된 취약 패턴을 파이프라인 검사 항목으로 추가한다
# 예: 문자열 결합으로 만든 질의
grep -rnE 'execute\(.*\+.*\)|query\(.*[+].*\)' src/ && exit 1 || echo ok
\`\`\`

같은 문제가 다시 들어오지 않게 하려면 자동 검사가 필요하다. 재점검 후 이 작업까지 하는 것이 절차다.

## 기한 관리

| 등급 | 조치 기한 | 재점검 |
| --- | --- | --- |
| 심각 | 즉시~3일 | 조치 즉시 |
| 높음 | 2주 | 1주 내 |
| 중간 | 1개월 | 다음 정기 |
| 낮음 | 3개월 | 다음 정기 |

기한을 넘긴 항목은 위험 수용 절차로 넘긴다. 책임자가 명시적으로 승인하게 하면 기한 관리가 실제로 작동한다.

## 참고

- OWASP — Vulnerability Management, 재점검 절차
- NIST SP 800-115, 시험 후속 조치
- OWASP ASVS — 검증 항목의 반복 확인`,
    diagram: {
      type: 'flow',
      caption: '재점검 절차',
      steps: [
        { label: '원 절차 재현', note: '막혔는지' },
        { label: '변형 시도', note: '우회 가능성' },
        { label: '동일 원인 탐색', note: '다른 지점' },
        { label: '상태 분류', note: '네 가지' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '조치 판정',
      x: ['우회 불가', '우회 가능'],
      y: ['전 지점 적용', '일부 지점'],
      cells: ['조치 완료', '부분 조치', '부분 조치', '미조치'],
    },
  },
  {
    slug: 'test-environment-prep',
    title: '진단 환경 준비와 데이터 격리 기준',
    body: `운영 환경에서 진단하면 장애와 데이터 손상 위험이 있고, 시험 환경에서 하면 운영과 달라 결과가 어긋난다. 어느 쪽을 택하든 그 차이를 알고 시작해야 한다. 그리고 진단 중에 확보한 데이터를 어떻게 다룰지 정하지 않으면, 진단 자체가 개인정보 유출 경로가 된다.

## 어느 환경에서 할 것인가?

| 환경 | 장점 | 위험·한계 |
| --- | --- | --- |
| 운영 | 실제 설정과 데이터 | 장애·데이터 손상 |
| 스테이징 | 안전 | 설정·데이터가 다름 |
| 운영 복제 | 설정 동일 | 실 데이터 취급 문제 |
| 전용 진단 환경 | 통제 가능 | 유지 비용 |

스테이징에서 점검할 때 가장 자주 어긋나는 것은 앞단 구성이다. 운영에는 방화벽과 프록시가 있고 스테이징에는 없으면, 실제로는 막히는 것이 결함으로 보고된다.

![환경 차이가 만드는 오차](/img/posts/test-environment-prep.svg)

## 환경 차이를 먼저 확인한다

점검 전에 대조표를 만들면 결과 해석이 정확해진다.

\`\`\`
확인 항목   앞단 구성(WAF·프록시·CDN), 보안 헤더, 인증 방식,
            데이터 규모, 외부 연동 여부, 로그 수집, 버전
방법        같은 요청을 양쪽에 보내 응답 헤더·코드를 비교
기록        다른 항목을 보고서의 가정 절에 적는다
\`\`\`

\`\`\`bash
# 양쪽 응답을 비교해 구성 차이를 찾는다
for h in stg.example.com www.example.com; do
  echo "== $h"
  curl -sD - -o /dev/null "https://$h/" |
    grep -iE 'server|via|x-cache|strict-transport|content-security|x-frame'
done
\`\`\`

![대조 절차](/img/posts/test-environment-prep-2.svg)

## 진단 데이터를 어떻게 다루는가

점검 중 확보한 응답에는 실 개인정보가 들어 있을 수 있다. 그 자료의 취급을 계약과 절차에 명시한다.

| 항목 | 기준 |
| --- | --- |
| 저장 위치 | 지정된 암호화 저장소만 |
| 반출 | 금지, 보고서에는 마스킹 후 최소만 |
| 보관 기간 | 보고 종료 후 30일 등으로 한정 |
| 파기 | 확인서 제출 |
| 개인 장비 | 사용 금지 또는 전체 디스크 암호화 필수 |

보고서에 증적을 붙일 때는 마스킹이 필요하다. 보고서가 여러 부서로 돌아다니면서 그 자체가 유출 경로가 되는 경우가 실제로 있다.

## 시험 계정과 데이터 준비

역할별 계정 두 개씩, 그리고 상태별 데이터가 필요하다. 취소된 주문, 만료된 쿠폰, 삭제된 게시물처럼 상태 전이가 끝난 데이터가 있어야 상태 검증 결함을 찾을 수 있다.

\`\`\`
계정        역할별 2개(수평 경계용) + 미인증 + 만료 토큰
데이터      정상·취소·삭제·만료 상태 각각
연동        외부 결제·발송은 시험 모드로 전환 확인
정리        점검 후 생성한 데이터와 계정 회수
\`\`\`

## 실제로 이렇게 터진다

점검을 운영 환경에서 진행한 사례가 있다. 부하 시험 중 서비스가 느려졌고, 시험 중 생성된 데이터가 실제 고객 화면에 나타났다.

또 하나는 시험 환경을 만들었지만 운영과 설정이 달랐던 경우다. 시험에서 발견된 문제가 운영에는 없었고, 운영의 문제는 시험에 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 운영에서 해야 정확하다 | 위험이 이득보다 크다 |
| 시험 환경은 아무거나 된다 | 설정이 같아야 의미 있다 |
| 데이터는 복사하면 된다 | 개인정보가 함께 간다 |
| 환경은 점검 때만 필요하다 | 상시 유지가 효율적이다 |
| 규모가 작아도 된다 | 부하 관련 문제는 안 보인다 |

## 갖춰야 할 조건

| 항목 | 요건 |
| --- | --- |
| 설정 | 운영과 동일 |
| 데이터 | 가공된 실데이터 유사본 |
| 규모 | 최소한 비율 유지 |
| 연동 | 외부 연동 모의 |
| 계정 | 권한별 시험 계정 |
| 격리 | 운영 자원 접근 불가 |
| 초기화 | 반복 시험 가능 |

외부 연동 모의가 중요하다. 실제 결제·발송으로 이어지면 시험이 사고가 된다.

## 데이터 준비

| 데이터 | 처리 |
| --- | --- |
| 개인 식별 정보 | 가공 |
| 결제 정보 | 시험용 값 |
| 자격 증명 | 별도 발급 |
| 파일 첨부 | 가공 또는 생성 |
| 관계 데이터 | 참조 무결성 유지 |

운영 데이터를 그대로 복사하는 것이 가장 흔한 실수다. 시험 환경은 통제가 약하므로 유출 위험이 크다.

## 점검 중 통제

| 항목 | 조치 |
| --- | --- |
| 외부 발송 | 차단 또는 수신함 고정 |
| 결제 | 모의 게이트웨이 |
| 알림 | 담당자에게만 |
| 데이터 반출 | 금지 |
| 시험 종료 후 | 환경 초기화 |

## 부득이 운영에서 한다면

| 조건 | 필수 |
| --- | --- |
| 사전 승인 | 서면 |
| 시간대 | 트래픽 낮은 시간 |
| 금지 항목 | 부하·파괴적 시험 |
| 연락 체계 | 즉시 중단 가능 |
| 데이터 | 시험 계정으로만 |
| 기록 | 모든 행위 |

부하 시험과 파괴적 시험은 운영에서 하지 않는다. 이 두 가지가 실제 장애를 만든다.

## 유지

점검 때만 환경을 만들면 준비에 시간이 든다. 상시 유지하면서 배포 파이프라인에 포함시키면, 점검뿐 아니라 개발 검증에도 쓸 수 있다.

## 참고

- NIST SP 800-115, 시험 환경 준비와 데이터 취급
- OWASP Web Security Testing Guide — 준비 단계
- 개인정보보호법, 개인정보 처리 위탁 시 안전조치`,
    diagram: {
      type: 'matrix',
      caption: '환경 선택',
      x: ['설정 동일', '설정 다름'],
      y: ['실 데이터 없음', '실 데이터 있음'],
      cells: ['이상적', '결과 오차', '데이터 위험', '위험 + 오차'],
    },
    diagram2: {
      type: 'layers',
      caption: '준비 항목',
      layers: [
        { label: '환경 대조', note: '앞단·헤더·버전' },
        { label: '계정 준비', note: '역할별 2개' },
        { label: '상태별 데이터', note: '취소·만료·삭제' },
        { label: '데이터 취급 규칙', note: '보관·파기' },
      ],
    },
  },
  {
    slug: 'vulnerability-report-writing',
    title: '취약점 보고서 읽히게 쓰는 방법',
    body: `보고서의 목적은 결함을 증명하는 것이 아니라 고쳐지게 만드는 것이다. 그런데 대부분의 보고서는 도구 출력과 이론 설명으로 채워져 있고, 개발자가 무엇을 어디에서 바꿔야 하는지는 적혀 있지 않다. 읽는 사람이 셋이라는 점을 전제로 쓰면 구성이 달라진다.

## 누가 읽는가?

| 독자 | 필요한 것 |
| --- | --- |
| 경영진 | 무엇이 위험하고 무엇을 결정해야 하는가 |
| 보안 담당 | 전체 목록, 위험도, 기한 |
| 개발자 | 재현 절차, 원인 위치, 고치는 방법 |

한 문서로 셋을 만족시키려면 층을 나눠야 한다. 앞에 요약, 중간에 목록, 뒤에 항목별 상세를 두는 구성이 널리 쓰인다.

![독자별 필요 정보](/img/posts/vulnerability-report-writing.svg)

## 항목 하나를 어떻게 쓰는가

여섯 개 요소가 있으면 개발자가 바로 착수할 수 있다. 순서도 중요하다 — 무엇이 문제인지를 먼저 쓴다.

\`\`\`
제목       무엇이 가능한지 한 줄로 (기법 이름이 아니라 결과로)
영향       공격자가 얻는 것, 영향 범위
재현       최소 단계. 요청·응답 원문 포함
원인       어느 코드·설정에서 비롯되는지
조치       무엇을 어떻게 바꾸면 되는지, 대안 포함
검증       고친 뒤 무엇을 확인하면 되는지
\`\`\`

제목을 결과로 쓰는 것이 차이를 만든다. "IDOR 취약점" 보다 "다른 사용자의 주문 상세를 조회할 수 있다" 가 우선순위 판단에 바로 쓰인다.

![항목 구성](/img/posts/vulnerability-report-writing-2.svg)

## 조치 방안을 어떻게 쓰는가

"입력값을 검증하십시오" 같은 문장은 아무것도 바꾸지 못한다. 코드 수준으로 내려가야 한다.

\`\`\`ts
// 현재 (server/routes/orders.ts:42)
const order = await db.order.findUnique({ where: { id: Number(req.params.id) } })
return res.json(order)

// 권고 — 소유 관계를 조건에 넣는다
const order = await db.order.findFirst({
  where: { id: Number(req.params.id), userId: req.actor.id },
})
if (!order) return res.status(404).end()      // 존재 여부도 노출하지 않는다
return res.json(pick(order, PUBLIC_FIELDS))   // 응답 필드도 좁힌다
\`\`\`

같은 원인이 여러 곳에 있으면 공통 계층 수정을 함께 제안한다. 개별 수정만 권고하면 다음 진단에 같은 결함이 다시 나온다.

## 증적을 붙일 때의 원칙

| 원칙 | 이유 |
| --- | --- |
| 최소한만 | 보고서가 유출 경로가 되지 않게 |
| 마스킹 | 실 개인정보 노출 방지 |
| 원문 유지 | 요청·응답의 핵심 줄은 그대로 |
| 시각 기록 | 로그 대조를 위해 |

## 실제로 이렇게 터진다

보고서에 도구 출력을 그대로 붙인 사례가 있다. 개발팀은 무엇을 고쳐야 할지 알 수 없었고, 회신에 몇 차례 왕복이 필요했다.

또 하나는 재현 절차가 빠진 경우다. 개발자가 재현하지 못해 "확인 불가" 로 종결됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 상세할수록 좋다 | 필요한 것이 명확해야 한다 |
| 도구 출력이 근거다 | 재현 절차가 근거다 |
| 조치 방법은 개발팀 몫이다 | 제시해야 처리된다 |
| 기술 용어가 정확하다 | 읽는 사람에 맞춰야 한다 |
| 보고서는 기록용이다 | 조치를 만드는 문서다 |

## 항목 하나의 구성

| 항목 | 내용 |
| --- | --- |
| 제목 | 무엇이 문제인지 한 줄 |
| 등급과 근거 | 왜 이 등급인가 |
| 위치 | 파일·경로·엔드포인트 |
| 재현 절차 | 번호 매긴 단계 |
| 증거 | 요청·응답, 화면 |
| 영향 | 성공하면 무엇이 되나 |
| 조치 방법 | 구체적 코드 또는 설정 |
| 참고 | 관련 자료 |

재현 절차와 조치 방법이 조치율을 정한다. 이 둘이 부실하면 나머지가 아무리 좋아도 처리되지 않는다.

## 재현 절차 작성

| 원칙 | 이유 |
| --- | --- |
| 번호를 매긴다 | 어디서 막혔는지 소통 |
| 전제 조건을 적는다 | 계정·상태 |
| 실제 값을 쓴다 | 예시 값이 아니라 |
| 예상 결과와 실제 결과 | 무엇이 다른가 |
| 복사 가능한 형태 | 요청 전문 |

## 영향 서술

"권한 상승이 가능하다" 보다 "일반 사용자가 다른 사용자의 주문 내역과 연락처를 조회할 수 있다" 가 낫다. 구체적일수록 우선순위 판단이 쉽다.

| 나쁜 표현 | 나은 표현 |
| --- | --- |
| 정보 노출 | 회원 이메일 전체 조회 가능 |
| 인증 우회 | 로그인 없이 관리 기능 실행 |
| 설정 미흡 | 백업 파일 누구나 내려받기 가능 |

## 요약 페이지

경영진과 개발팀이 다른 것을 본다. 앞에 한 페이지 요약을 둔다.

| 요약에 담을 것 |
| --- |
| 점검 범위와 기간 |
| 등급별 건수 |
| 즉시 조치가 필요한 항목 |
| 반복되는 근본 원인 |
| 전반적 판단 |

반복되는 근본 원인이 가장 가치 있는 내용이다. 개별 항목보다 그것을 고치면 여러 건이 함께 해결된다.

## 참고

- OWASP — Vulnerability Disclosure Cheat Sheet, 보고서 작성 지침
- NIST SP 800-115, 결과 보고
- CVSS v4.0 — 점수 근거 제시 방법`,
    diagram: {
      type: 'layers',
      caption: '보고서 구조',
      layers: [
        { label: '요약', note: '경영진 판단용' },
        { label: '결함 목록', note: '위험도·기한' },
        { label: '항목별 상세', note: '재현·원인·조치' },
        { label: '부록', note: '범위·가정·미점검' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '조치 방안의 수준',
      x: ['코드 수준 제시', '원칙만 제시'],
      y: ['공통 계층 제안', '개별 수정만'],
      cells: ['유형 단위 해결', '반복 발생', '일부 해결', '변화 없음'],
    },
  },
  {
    slug: 'bug-bounty-operation',
    title: '버그 바운티 운영 판단 기준과 준비',
    body: `외부 제보를 받으면 내부에서 못 찾는 결함이 들어온다. 그러나 준비 없이 열면 저품질 제보가 밀려들고, 대응에 인력이 갈려 나가고, 제보자와 분쟁이 생긴다. 시작 전에 갖춰야 할 것이 분명히 있고, 그것이 없다면 취약점 제보 창구부터 운영하는 것이 순서다.

## 시작해도 되는 상태인가?

| 조건 | 왜 필요한가 |
| --- | --- |
| 내부 진단이 한 바퀴 돌았다 | 쉬운 결함이 남아 있으면 비용만 든다 |
| 조치 절차가 있다 | 제보를 받아도 못 고치면 무의미 |
| 대응 담당이 정해져 있다 | 회신 지연이 분쟁의 원인 |
| 범위를 정할 수 있다 | 무엇이 대상인지 불명확하면 다툼 |
| 보상 예산이 있다 | 사후 협상은 신뢰를 잃는다 |

내부 진단 없이 시작하면 초기에 대량의 유효 제보가 들어와 예산과 인력이 동시에 소진된다.

![준비 상태 판단](/img/posts/bug-bounty-operation.svg)

## 정책 문서에 반드시 적을 것

제보자와 우리 양쪽을 보호하는 문서다. 특히 면책 범위와 금지 행위를 명확히 적는다.

\`\`\`
대상 범위      도메인·앱 목록, 제외 대상 명시
금지 행위      서비스 거부, 실 사용자 데이터 접근·변경, 사회공학, 물리 침입
데이터 취급    실 데이터를 발견하면 즉시 중단·신고, 보관 금지
회신 기한      접수 확인 N일, 1차 판정 N일
보상 기준      등급별 금액 범위와 판정 기준
면책           정책을 지킨 제보에 대해 법적 조치를 하지 않는다
중복·자동화    중복 제보와 도구 출력 그대로의 제보 처리 방침
\`\`\`

![정책 구성 요소](/img/posts/bug-bounty-operation-2.svg)

## 운영 부담을 어떻게 줄이는가

| 수단 | 효과 |
| --- | --- |
| 범위를 좁게 시작 | 제보량 조절 |
| 관심 없는 항목 명시 | 저품질 제보 감소 |
| 재현 절차 필수 | 판정 시간 단축 |
| 등급별 정액 보상 | 협상 부담 제거 |
| 중복 판정 기준 공개 | 분쟁 감소 |

"보상하지 않는 항목" 목록이 실제로 가장 효과가 크다. 보안 헤더 누락, 버전 노출, 자동 도구 출력 등을 미리 적어 두면 제보량이 크게 줄어든다.

## 제보를 받은 뒤의 절차

\`\`\`
접수      자동 회신 + 접수 번호
1차 판정  재현 가능성 확인 (재현 안 되면 정보 요청)
중복 확인 기존 항목·내부 인지 여부 대조
등급 산정 우리 위험도 기준 적용
조치      기한 부여, 진행 상황 회신
보상      조치 여부와 무관하게 유효 판정 시 지급
종료      제보자 확인, 공개 여부 협의
\`\`\`

조치 완료 전에 보상하는 것이 신뢰 유지에 낫다. 조치가 늦어지는 것은 우리 사정이고, 제보의 가치는 제보 시점에 결정된다. 회신이 느린 것이 분쟁의 가장 흔한 원인이라는 점도 기억할 만하다. 판정에 시간이 걸리더라도 진행 상황을 알려 주면 대부분의 제보자는 기다린다. 아무 회신이 없으면 공개 압박으로 이어지고, 그때는 기술 문제가 아니라 대외 커뮤니케이션 문제가 된다.

## 실제로 이렇게 터진다

포상 제도를 열었지만 운영 인력을 두지 않은 사례가 있다. 첫 달에 수백 건이 접수됐고, 대부분 저품질이었다. 회신이 늦어지자 제보자들이 공개 채널에 불만을 올렸다.

또 하나는 범위를 명확히 하지 않은 경우다. 제보자가 운영 시스템에 부하를 주는 시험을 했고, 실제 장애가 났다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 열면 좋은 제보가 온다 | 저품질이 대부분이다 |
| 비용이 적게 든다 | 운영 인력이 가장 큰 비용이다 |
| 내부 점검을 대체한다 | 보완 관계다 |
| 범위는 나중에 정한다 | 먼저 정하지 않으면 사고가 난다 |
| 포상금이 핵심이다 | 처리 속도와 태도가 더 중요하다 |

## 시작 전 조건

| 조건 | 이유 |
| --- | --- |
| 내부 점검 체계 | 기본적인 것은 미리 고쳐 둔다 |
| 조치 능력 | 제보를 고칠 수 있어야 한다 |
| 전담 인력 | 분류·회신·조율 |
| 범위 문서 | 무엇을 해도 되는지 |
| 법적 보호 | 선의의 제보자 면책 |
| 예산 | 포상금과 운영 |

내부 점검 없이 시작하면 이미 알려진 유형의 제보가 쏟아진다. 기본을 정리한 뒤 여는 것이 비용이 적다.

## 범위 문서에 담을 것

| 항목 | 내용 |
| --- | --- |
| 대상 | 도메인·앱 목록 |
| 제외 | 제3자 서비스, 특정 시스템 |
| 금지 행위 | 부하 시험, 사회공학, 실데이터 접근 |
| 제외 유형 | 이미 아는 것, 위험 없는 것 |
| 시험 계정 | 발급 방법 |
| 신고 방법 | 양식과 채널 |
| 처리 기한 | 회신·판정·조치 |
| 포상 기준 | 등급별 금액 |

금지 행위를 명시하지 않으면 사고가 난다. 특히 부하 시험과 실제 사용자 데이터 접근을 분명히 금지한다.

## 운영 지표

| 지표 | 목표 |
| --- | --- |
| 첫 회신까지 | 3일 이내 |
| 판정까지 | 2주 이내 |
| 조치까지 | 등급별 기한 |
| 유효 제보 비율 | 추적 |
| 중복 비율 | 추적 |

첫 회신 속도가 제보자 만족도를 좌우한다. 자동 접수 확인이라도 즉시 보낸다.

## 제보 처리

1. 접수 확인 회신
2. 범위 확인
3. 재현
4. 중복 확인
5. 등급 판정과 회신
6. 조치
7. 포상 지급
8. 조치 완료 통보

8번을 빠뜨리는 경우가 많다. 제보자는 자신의 제보가 실제로 고쳐졌는지 알고 싶어 한다.

## 비공개로 시작하기

처음부터 공개로 열지 않는다. 초대 기반으로 소수와 시작해 절차를 다듬은 뒤 넓힌다. 운영 부담을 예측할 수 있고, 초기 저품질 유입도 적다.

## 참고

- ISO/IEC 29147, 취약점 공개
- ISO/IEC 30111, 취약점 처리 절차
- OWASP — Vulnerability Disclosure Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '운영 준비 순서',
      steps: [
        { label: '내부 진단', note: '쉬운 결함 제거' },
        { label: '제보 창구', note: '절차 검증' },
        { label: '정책 문서', note: '범위·면책' },
        { label: '보상 프로그램', note: '좁게 시작' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '시작 판단',
      x: ['조치 절차 있음', '없음'],
      y: ['내부 진단 완료', '미완료'],
      cells: ['시작 가능', '제보만 쌓임', '비용 과다', '시작 부적절'],
    },
  },
]
