import type { SeedPost } from './types'

/** 520~529 — 로그·시각·노출 */
export const posts53: SeedPost[] = [
  {
    slug: 'log-sampling-tradeoff',
    title: '로그를 표본으로 줄일 때 잃는 것',
    body: `로그 비용이 부담스러워지면 표본만 남기자는 말이 나온다. 요청의 10%만 기록하면 용량도 10분의 1이 된다. 그런데 보안에서 찾으려는 것은 대개 드문 사건이고, 표본은 드문 것을 먼저 버린다.

## 무엇이 먼저 사라지나?

무작위 표본은 빈도에 비례해 남긴다. 하루에 백만 번 일어나는 정상 요청은 10%만 남겨도 십만 건이 남지만, 하루에 세 번 일어난 침입 시도는 남지 않을 확률이 높다.

| 사건 | 하루 발생 | 10% 표본에 남을 확률 |
| --- | --- | --- |
| 정상 요청 | 1,000,000 | 사실상 100% |
| 인증 실패 | 500 | 사실상 100% |
| 권한 변경 | 12 | 72% |
| 관리자 접근 | 3 | 27% |

드문 사건일수록 통째로 사라진다. 그런데 조사에서 필요한 것은 정확히 그 드문 사건이다.

![표본이 남기는 것과 버리는 것](/img/posts/log-sampling-tradeoff.svg)

## 로그 수집에서 나누는 기준

표본을 쓸 곳과 쓰면 안 되는 곳을 나눈다. 판단 기준은 "이 줄이 없어도 조사가 되는가"다.

| 유형 | 표본 |
| --- | --- |
| 정상 응답 요청 로그 | 가능 |
| 정적 자원 접근 | 가능 |
| 인증·권한 관련 | 전량 |
| 데이터 조회·반출 | 전량 |
| 오류 응답 | 전량 |

오류는 전량 남긴다. 오류는 이미 드물고, 표본으로 줄여 봐야 용량이 크게 줄지 않으면서 조사 가치만 잃는다.

## 표본을 쓴다면 이렇게

같은 사용자나 같은 추적 ID 의 요청은 통째로 남기거나 통째로 버린다. 요청 단위로 무작위 표본을 뽑으면 한 흐름의 중간만 남아 이어 읽을 수 없다.

| 방식 | 결과 |
| --- | --- |
| 요청별 무작위 | 흐름이 끊김 |
| 추적 ID 기준 | 흐름이 온전 |
| 사용자 기준 | 특정 사용자 전체 |
| 오류 시 전량 | 문제 구간 보존 |

![표본 방식의 차이](/img/posts/log-sampling-tradeoff-2.svg)

## 줄이기 전에 무엇이 용량을 먹는지 본다

표본은 마지막 수단이다. 대개 한두 소스가 전체의 절반을 차지하고, 그것만 손봐도 문제가 풀린다.

\`\`\`bash
# 소스별 줄 수 — 어디가 큰지부터
awk '{print $5}' /var/log/app.log | sort | uniq -c | sort -rn | head -10

# 같은 문구가 반복되는지 — 디버그 출력이 운영에 남아 있는 경우가 많다
awk '{$1=$2=$3=""; print}' /var/log/app.log | sort | uniq -c | sort -rn | head -10
\`\`\`

같은 문장이 수십만 번 반복되면 그것은 로그가 아니라 소음이다. 그 한 줄을 없애는 것이 표본보다 낫다.

## 줄인 뒤에 확인할 것

표본을 켜고 나면 지난 사고를 다시 조사해 본다. 그때 썼던 로그가 지금 기준으로도 남아 있는지 보면 된다. 남아 있지 않다면 기준이 잘못된 것이다. 이 확인 없이 켜면, 다음 사고 때 조사할 수 없다는 사실을 그제야 알게 된다.

## 참고

- NIST SP 800-92 — 로그 관리 지침
- OWASP Logging Cheat Sheet
- ISO/IEC 27002 — 이벤트 로깅`,
    diagram: {
      type: 'bars',
      caption: '10% 표본에 남을 확률',
      unit: '%',
      items: [
        { label: '정상 요청', value: 100, note: '백만 건' },
        { label: '인증 실패', value: 100, note: '500건' },
        { label: '권한 변경', value: 72, note: '12건' },
        { label: '관리자 접근', value: 27, note: '3건' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '표본을 쓸 곳',
      x: ['흔함', '드묾'],
      y: ['조사에 필요', '불필요'],
      cells: ['추적 ID 단위', '전량 보존', '표본 가능', '아예 끄기'],
    },
  },
  {
    slug: 'timezone-in-logs',
    title: '로그 시각대가 섞이면 생기는 일',
    body: `사고 조사는 시간순으로 사건을 늘어놓는 일에서 시작한다. 그런데 서버마다 시각대가 다르면 그 줄 세우기가 처음부터 틀어진다. 아홉 시간 어긋난 로그 두 줄을 나란히 놓고 원인과 결과를 뒤집어 읽게 된다.

## 왜 섞이나?

한 곳에서 정하지 않으면 각자의 기본값을 따른다. 기본값은 만든 사람과 환경에 따라 다르다.

| 출처 | 흔한 기본값 |
| --- | --- |
| 클라우드 인스턴스 | UTC |
| 사내 서버 | KST |
| 컨테이너 | UTC |
| 애플리케이션 프레임워크 | 시스템 따름 |
| 데이터베이스 | 설정에 따라 |
| 외부 서비스 로그 | 그쪽 기준 |

같은 시스템 안에서도 접속 로그는 UTC, 애플리케이션 로그는 KST 인 경우가 흔하다.

![시각대가 섞인 로그](/img/posts/timezone-in-logs.svg)

## 로그 수집 단계에서 정한다

저장은 UTC 로 통일하고, 보여 줄 때만 지역 시각으로 바꾼다. 저장까지 지역 시각으로 하면 서머타임이 있는 지역의 기록이 한 시간 겹치거나 비는 구간이 생긴다.

| 단계 | 기준 |
| --- | --- |
| 기록 | UTC, 오프셋 표기 |
| 저장 | UTC |
| 조회 화면 | 보는 사람의 지역 |
| 보고서 | 기준을 명시 |

오프셋을 함께 적는 형식을 쓰면 나중에 해석이 갈리지 않는다. 시각만 있고 기준이 없는 값은 언제나 추측이 된다.

## 밀리초와 순서

초 단위까지만 기록하면 같은 초에 일어난 사건의 순서를 알 수 없다. 공격은 대개 자동화돼 있어 같은 초에 여러 건이 몰린다.

| 정밀도 | 같은 초 내 순서 |
| --- | --- |
| 초 | 알 수 없음 |
| 밀리초 | 대개 구분됨 |
| 마이크로초 | 확실 |
| 순번 부여 | 확실 |

![기록 형식](/img/posts/timezone-in-logs-2.svg)

## 지금 어긋나 있는지 본다

서버마다 무엇을 쓰는지 한 번에 훑는다.

\`\`\`bash
# 시각대와 동기화 상태
timedatectl status | grep -E 'Time zone|synchronized'

# 로그가 실제로 어떤 형식인지 — 오프셋이 붙어 있는가
head -3 /var/log/app.log
head -3 /var/log/nginx/access.log
\`\`\`

**synchronized: no** 가 나오면 시각대 이전에 동기화부터 문제다. 그 서버의 로그는 다른 서버와 나란히 놓을 수 없다.

## 외부에서 받은 기록

결제사나 클라우드 사업자가 주는 로그는 그쪽 기준으로 온다. 우리 로그와 대조하기 전에 변환하는 절차를 문서에 적어 둔다. 조사 중에 매번 계산하면 실수가 나고, 그 실수는 대개 사건 순서를 뒤집는다.

## 참고

- RFC 3339 — 날짜·시각 표기
- NIST SP 800-92 — 로그 관리 지침
- ISO/IEC 27002 — 시각 동기화`,
    diagram: {
      type: 'flow',
      caption: '시각대가 섞였을 때',
      steps: [
        { label: '서버마다 다른 기준', note: 'UTC·KST 혼재' },
        { label: '수집소에 그대로', note: '변환 없음' },
        { label: '시간순 정렬', note: '9시간 어긋남', danger: true },
        { label: '원인·결과 뒤바뀜', note: '조사 실패' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '기록 형식',
      layers: [
        { label: 'UTC 저장', note: '기준 통일' },
        { label: '오프셋 표기', note: '해석 고정' },
        { label: '밀리초', note: '순서 구분' },
        { label: '지역 시각은 화면에서', note: '변환은 마지막에' },
      ],
    },
  },
  {
    slug: 'clock-skew-security',
    title: '시계가 어긋나면 무엇이 깨지나',
    body: `시계는 평소에 신경 쓰이지 않다가 어긋난 순간 여러 가지를 한꺼번에 무너뜨린다. 보안 장치 상당수가 "언제"를 기준으로 판단하기 때문이다. 몇 분의 차이가 로그인 실패와 검증 실패로 나타난다.

## 인증서와 토큰은 무엇에 기대나?

시각이 맞다는 전제 위에 서 있는 것이 생각보다 많다. 어긋나면 각각 다른 증상으로 나타나 원인을 찾기 어렵다.

| 기능 | 어긋났을 때 |
| --- | --- |
| 토큰 만료 | 유효한데 거절, 만료됐는데 통과 |
| 다단계 인증 코드 | 계속 틀림 |
| 인증서 검증 | 아직 유효하지 않음 |
| 서명 검증 | 재전송 방지가 오작동 |
| 로그 순서 | 사건 순서 뒤집힘 |
| 접근 시간 제한 | 엉뚱한 시간에 허용 |

다단계 인증 코드가 계속 틀리는 것이 가장 먼저 눈에 띄는 증상이다. 사용자는 앱이 고장 났다고 생각하지만 실제로는 서버 시계가 밀린 것이다.

![시각에 기대는 장치들](/img/posts/clock-skew-security.svg)

## 허용 오차를 어떻게 잡나

완전히 같을 수는 없으므로 오차를 허용한다. 넓게 잡으면 재전송 공격의 창이 넓어지고, 좁게 잡으면 정상 요청이 거절된다.

| 대상 | 흔한 허용 오차 |
| --- | --- |
| 다단계 인증 코드 | 앞뒤 1단계 |
| 웹훅 서명 | 5분 |
| 토큰 발급 시각 | 1분 |
| 인증서 유효기간 | 수 분 |

허용 오차를 늘리는 것으로 문제를 덮지 않는다. 오차가 자꾸 넘친다면 동기화가 안 되고 있다는 신호다.

## 동기화가 끊기는 자리

시각 동기화는 네트워크에 기대므로 막히기 쉽다. 방화벽이 조용히 끊어 놓은 경우가 흔하다.

| 원인 | 증상 |
| --- | --- |
| UDP 123 차단 | 서서히 밀림 |
| 내부 서버만 지정 | 그 서버가 틀리면 전부 틀림 |
| 가상 머신 일시정지 | 재개 후 크게 어긋남 |
| 컨테이너 기본값 | 호스트를 따름 |

![동기화가 끊기는 자리](/img/posts/clock-skew-security-2.svg)

## 지금 얼마나 어긋나 있는지 본다

\`\`\`bash
# 동기화 상태와 오차
timedatectl show -p NTPSynchronized -p TimeUSec
chronyc tracking 2>/dev/null | grep -E 'System time|Last offset'

# 여러 서버를 한 번에 — 각자의 시각을 받아 비교한다
for h in web1 web2 db1; do
  printf '%-6s %s\\n' "$h" "$(ssh -o ConnectTimeout=3 "$h" date -u +%FT%T 2>/dev/null || echo 접속실패)"
done
\`\`\`

몇 초 이상 벌어진 서버가 있으면 그 서버에서 나온 로그는 다른 서버와 나란히 놓기 전에 보정해야 한다.

## 시계를 믿을 수 없을 때

조사 중에 시각이 어긋난 것을 알게 되면, 그 서버의 로그를 버릴 것이 아니라 오차를 기록해 두고 보정해 읽는다. 오차를 모른 채 읽는 것보다 낫다. 그리고 보정했다는 사실을 조사 기록에 남긴다 — 나중에 다른 사람이 같은 로그를 보고 다른 결론에 이르는 것을 막는다.

## 참고

- RFC 5905 — NTP 프로토콜
- NIST SP 800-53 — 시각 스탬프 신뢰성
- RFC 6238 — TOTP 와 시간 동기화`,
    diagram: {
      type: 'layers',
      caption: '시각에 기대는 장치',
      layers: [
        { label: '토큰 만료', note: '유효성 판단' },
        { label: '다단계 인증', note: '코드 생성' },
        { label: '인증서', note: '유효기간' },
        { label: '재전송 방지', note: '허용 오차' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '동기화가 끊기는 자리',
      steps: [
        { label: 'UDP 123 차단', note: '서서히 밀림' },
        { label: '단일 내부 서버', note: '그것이 틀리면 전부' },
        { label: '가상머신 재개', note: '크게 어긋남' },
        { label: '컨테이너', note: '호스트를 따름' },
      ],
    },
  },
  {
    slug: 'audit-log-tampering',
    title: '감사 로그를 못 고치게 만드는 법',
    body: `감사 로그는 무슨 일이 있었는지를 증명하기 위해 남긴다. 그런데 그 로그를 고칠 수 있는 사람이 로그에 기록되는 사람과 같다면, 증명의 힘이 없다. 침입자가 가장 먼저 하는 일 중 하나가 흔적을 지우는 것이다.

## 누가 고칠 수 있나?

권한 구조를 그려 보면 대개 같은 계정이 양쪽을 다 갖고 있다. 이것을 나누는 것이 첫 단계다.

| 역할 | 로그 생성 | 로그 삭제 |
| --- | --- | --- |
| 관리자 계정 | 한다 | 대개 가능 |
| 서버 root | 한다 | 가능 |
| 로그 수집기 | 받는다 | 설정에 따라 |
| 보안 담당 | 읽는다 | 분리 필요 |

서버에서 로그를 만드는 계정이 그 파일을 지울 수 있으면, 서버를 장악한 사람은 기록도 장악한다.

![기록과 삭제 권한의 분리](/img/posts/audit-log-tampering.svg)

## 밖으로 내보내는 것이 핵심이다

같은 서버 안에서 아무리 보호해도 그 서버를 장악하면 끝난다. 생기는 즉시 다른 곳으로 보내면, 침입자가 지울 수 있는 것은 아직 못 보낸 몇 초치뿐이다.

| 방식 | 지울 수 있는 범위 |
| --- | --- |
| 서버 안 파일만 | 전부 |
| 주기적 전송 | 마지막 주기 |
| 즉시 전송 | 수 초 |
| 추가 전용 저장소 | 사실상 없음 |

## 고쳤는지 알아내는 장치

지우지 못하게 하는 것과 지운 것을 알아내는 것은 다른 일이다. 둘 다 필요하다.

| 수단 | 알 수 있는 것 |
| --- | --- |
| 줄마다 순번 | 빠진 구간 |
| 앞 줄의 해시 포함 | 중간 수정 |
| 주기적 전체 해시 | 묶음 단위 변조 |
| 수집소 도착 시각 | 지연·누락 |

앞 줄의 해시를 포함하면 중간 한 줄을 고치는 순간 그 뒤가 전부 어긋난다. 고치려면 그 뒤를 다 다시 써야 하고, 이미 밖으로 나간 사본과는 맞출 수 없다.

![연결된 해시](/img/posts/audit-log-tampering-2.svg)

## 지금 상태를 확인한다

\`\`\`bash
# 로그 파일을 누가 고칠 수 있나
ls -la /var/log/audit/ 2>/dev/null | head
lsattr /var/log/audit/audit.log 2>/dev/null

# 추가 전용 속성을 걸면 지우거나 덮어쓸 수 없다
chattr +a /var/log/audit/audit.log

# 전송이 실제로 되고 있는지 — 수집소에 최근 기록이 있는가
journalctl -u rsyslog -n 5 --no-pager
\`\`\`

추가 전용 속성은 root 가 풀 수 있어 완전한 방어는 아니다. 그래도 자동화된 지우기 스크립트는 여기서 멈추고, 속성을 푸는 행위 자체가 기록에 남는다.

## 이 규칙은 우리에게도 적용된다

감사 로그를 관리자도 고칠 수 없게 만들면 불편한 순간이 온다. 잘못 기록된 항목을 지우고 싶어진다. 그때 지울 수 있게 만들어 두면 그 구조는 침입자에게도 열려 있다. 잘못된 기록은 지우는 대신 정정하는 기록을 덧붙이는 방식으로 다룬다.

## 참고

- NIST SP 800-53 — 감사 정보 보호
- ISO/IEC 27002 — 로그 정보 보호
- CIS Controls — 감사 로그 관리`,
    diagram: {
      type: 'matrix',
      caption: '권한 분리',
      x: ['같은 계정', '분리됨'],
      y: ['서버 안 보관', '밖으로 전송'],
      cells: ['무방비', '지연만큼 위험', '전량 삭제 가능', '안전'],
    },
    diagram2: {
      type: 'flow',
      caption: '연결된 해시',
      steps: [
        { label: '줄마다 앞 해시 포함', note: '사슬' },
        { label: '한 줄 수정', note: '뒤가 어긋남' },
        { label: '전부 다시 써야', note: '비용 급증' },
        { label: '밖의 사본과 대조', note: '변조 드러남' },
      ],
    },
  },
  {
    slug: 'log-forwarding-loss',
    title: '로그 전송에서 조용히 사라지는 것',
    body: `로그가 수집소에 다 도착하고 있다고 믿는 근거는 대개 없다. 전송은 실패해도 소리를 내지 않고, 화면에는 도착한 것만 보인다. 없는 줄을 알아차리려면 있어야 할 줄이 몇 개인지 알아야 한다.

## 어디서 사라지나?

경로마다 버려지는 지점이 있다. 대부분 부하가 몰릴 때 조용히 버린다 — 정확히 사고가 나는 시점이다.

| 지점 | 버려지는 이유 |
| --- | --- |
| 애플리케이션 버퍼 | 가득 차면 덮어씀 |
| UDP 전송 | 재전송 없음 |
| 수집기 큐 | 상한 초과 |
| 네트워크 | 순간 단절 |
| 수집소 수용량 | 초당 상한 |

UDP 전송이 가장 흔한 원인이다. 설정이 간단해서 기본으로 쓰이는데, 네트워크가 잠깐 막히면 그 구간이 통째로 사라지고 아무 기록도 남지 않는다.

![전송 경로와 유실 지점](/img/posts/log-forwarding-loss.svg)

## 로그 수집에서 유실을 줄이는 선택

| 선택 | 대가 |
| --- | --- |
| TCP 전송 | 지연 증가 |
| 디스크 버퍼 | 디스크 사용 |
| 재시도 | 중복 가능 |
| 배압 적용 | 애플리케이션이 느려짐 |

배압은 논쟁이 되는 선택이다. 로그를 못 보내면 서비스를 늦출 것인가, 로그를 버릴 것인가. 인증·결제처럼 기록이 곧 증거인 구간은 늦추는 쪽이 맞고, 일반 요청 로그는 버리는 쪽이 맞다.

## 없어진 것을 알아내는 법

보낸 쪽과 받은 쪽의 수를 비교한다. 숫자를 세지 않으면 유실은 영원히 보이지 않는다.

| 수단 | 알 수 있는 것 |
| --- | --- |
| 줄 순번 | 빠진 구간 |
| 주기적 하트비트 | 전송 중단 |
| 양쪽 건수 비교 | 유실률 |
| 수집기 드롭 지표 | 버린 수 |

![양쪽에서 세기](/img/posts/log-forwarding-loss-2.svg)

## 실제로 새는지 확인한다

\`\`\`bash
# 수집기가 버린 수 — 이 값이 0 이 아니면 이미 새고 있다
curl -s localhost:24231/metrics 2>/dev/null | grep -i 'retry\\|drop\\|buffer'

# 보낸 쪽과 받은 쪽의 한 시간치 건수 비교
ssh web1 "grep -c '' /var/log/app.log"
curl -s 'localhost:9200/logs-*/_count?q=host:web1' | grep -o '"count":[0-9]*'
\`\`\`

두 숫자가 크게 다르면 그 차이가 유실이다. 매일 같은 시각에 재서 기록해 두면 어느 날부터 새기 시작했는지 알 수 있다.

## 하트비트가 가장 값싸다

모든 것을 갖추기 어렵다면 하트비트 하나만이라도 둔다. 각 서버가 1분마다 같은 형식의 한 줄을 보내고, 수집소에서 그 줄이 끊기면 경보를 낸다. 전송이 통째로 멈춘 상황을 며칠 뒤가 아니라 1분 뒤에 알게 된다. 부분 유실은 못 잡지만, 실무에서 더 자주 겪는 것은 부분 유실이 아니라 전면 중단이다.

## 참고

- NIST SP 800-92 — 로그 전송과 보관
- RFC 5424 — syslog 프로토콜
- OWASP Logging Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '전송 경로의 유실 지점',
      steps: [
        { label: '애플리케이션 버퍼', note: '가득 차면 덮어씀' },
        { label: 'UDP 전송', note: '재전송 없음', danger: true },
        { label: '수집기 큐', note: '상한 초과' },
        { label: '수집소', note: '초당 상한' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '유실을 알아내는 법',
      steps: [
        { label: '줄 순번', note: '빠진 구간' },
        { label: '하트비트', note: '전송 중단' },
        { label: '양쪽 건수 비교', note: '유실률' },
        { label: '드롭 지표', note: '버린 수' },
      ],
    },
  },
  {
    slug: 'debug-endpoint-exposure',
    title: '디버그 경로가 열려 있을 때 생기는 일',
    body: `개발 중에 만든 진단용 경로는 편리해서 지우기 아깝다. 그래서 운영에 그대로 올라가고, 아무도 기억하지 못한 채 몇 년을 남는다. 그 엔드포인트 하나가 내부 구조를 통째로 알려 주는 경우가 많다.

## 엔드포인트가 무엇을 알려주나?

진단용 경로는 원래 문제를 빨리 찾으라고 만든 것이라 숨김이 없다. 그 성질이 그대로 공격자에게 쓰인다.

| 경로 유형 | 드러나는 것 |
| --- | --- |
| 환경 변수 출력 | 자격 증명·연결 문자열 |
| 설정 덤프 | 내부 주소·버전 |
| 스레드·힙 덤프 | 메모리 안의 값 |
| 라우팅 목록 | 숨긴 경로 전부 |
| 상태 콘솔 | 원격 실행까지 |

환경 변수 출력이 가장 위험하다. 데이터베이스 비밀번호와 외부 서비스 키가 한 화면에 나온다.

![진단 경로가 드러내는 것](/img/posts/debug-endpoint-exposure.svg)

## 왜 남아 있나

지우자는 결정이 안 나는 이유가 있다. 그 이유를 인정하고 다른 방법을 주어야 사라진다.

| 이유 | 대안 |
| --- | --- |
| 장애 때 필요 | 인증 뒤에 두기 |
| 지우면 누가 불편 | 대체 수단 제공 |
| 있는지 모름 | 목록화 |
| 프레임워크 기본 제공 | 설정으로 끄기 |

프레임워크가 기본으로 켜 두는 경우가 많다. 만든 적이 없으니 목록에도 없고, 그래서 점검에서도 빠진다.

## 어떻게 다룰지 정한다

전부 지우는 것이 가장 안전하지만 현실적이지 않을 때가 있다. 단계를 둔다.

| 조치 | 강도 |
| --- | --- |
| 운영 빌드에서 제외 | 가장 확실 |
| 내부망에서만 접근 | 강함 |
| 인증·인가 요구 | 보통 |
| 경로 이름 바꾸기 | 거의 무의미 |

경로 이름을 바꿔 숨기는 것은 방어가 아니다. 목록을 훑는 자동화 도구는 흔한 이름을 수천 개 갖고 있다.

![조치의 강도](/img/posts/debug-endpoint-exposure-2.svg)

## 열려 있는지 직접 본다

흔한 경로부터 두드려 본다. 인증 없이 200 이 오면 그 자리가 문제다.

\`\`\`bash
for p in /actuator/env /actuator/heapdump /debug/pprof /__debug__ /server-status \\
         /.env /config /phpinfo.php /swagger-ui.html /api-docs; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "https://example.com$p")
  [ "$code" = "200" ] && echo "열림 $p"
done
\`\`\`

200 이 아니어도 401 과 404 는 다르다. 401 이면 그 경로가 존재한다는 뜻이고, 인증만 뚫리면 접근된다.

## 문서 경로도 같은 문제다

API 명세 화면은 디버그 경로는 아니지만 성격이 같다. 모든 엔드포인트와 파라미터 구조를 한 화면에 정리해 보여 준다. 공개 API 라면 열어 두는 것이 맞지만, 내부용 API 의 명세가 인증 없이 열려 있다면 공격자에게 지도를 준 셈이다.

## 참고

- OWASP Top 10 — 보안 설정 오류
- OWASP API Security Top 10 — 부적절한 자산 관리
- CIS Benchmarks — 서비스 최소화`,
    diagram: {
      type: 'layers',
      caption: '진단 경로가 드러내는 것',
      layers: [
        { label: '환경 변수', note: '자격 증명' },
        { label: '설정 덤프', note: '내부 주소' },
        { label: '메모리 덤프', note: '처리 중인 값' },
        { label: '라우팅 목록', note: '숨긴 경로' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '조치의 강도',
      unit: '점',
      items: [
        { label: '빌드에서 제외', value: 100, note: '가장 확실' },
        { label: '내부망 한정', value: 80, note: '강함' },
        { label: '인증 요구', value: 55, note: '보통' },
        { label: '이름 변경', value: 5, note: '거의 무의미' },
      ],
    },
  },
  {
    slug: 'health-check-exposure',
    title: '상태 확인 경로가 알려주는 것들',
    body: `상태 확인 경로는 로드밸런서가 서버의 생사를 묻는 자리다. 단순해 보여서 인증 없이 열어 두는데, 거기에 무엇을 담느냐에 따라 내부 구조를 그대로 알려 주는 창이 된다.

## 무엇까지 담겨 있나?

살아 있는지만 답하면 될 것을, 진단을 편하게 하려고 자꾸 정보를 더한다. 그 정보가 밖에서도 보인다.

| 담긴 것 | 알려 주는 것 |
| --- | --- |
| 버전 문자열 | 알려진 취약점 조회 |
| 의존 서비스 목록 | 내부 구성 |
| 데이터베이스 호스트 | 내부 주소 |
| 큐 적체 수 | 부하 상태 |
| 빌드 커밋 해시 | 소스 추적 |

버전과 내부 호스트명 둘만 있어도 공격 계획을 세우기에 충분하다. 알려진 취약점을 찾아보고 어디를 노릴지 정할 수 있다.

![상태 응답에 담기는 것](/img/posts/health-check-exposure.svg)

## 두 가지로 나눈다

바깥에 답하는 것과 안에서 보는 것을 나눈다. 같은 경로에 둘 다 담으려 하면 결국 많은 쪽으로 기운다.

| 대상 | 응답 |
| --- | --- |
| 외부·로드밸런서 | 200 또는 503, 본문 없음 |
| 내부 모니터링 | 상세 상태 |
| 운영자 | 의존성별 진단 |

외부용은 본문 없이 상태 코드만 돌려주면 충분하다. 로드밸런서는 본문을 읽지 않는다.

## 부하를 만들지 않게

상태 확인은 초당 여러 번 호출된다. 그 안에서 데이터베이스를 조회하면 상태 확인이 곧 부하가 된다. 더 나쁘게는, 밖에서 그 경로를 반복 호출하는 것만으로 서비스 거부를 만들 수 있다.

| 구현 | 위험 |
| --- | --- |
| 즉시 200 반환 | 없음 |
| 캐시된 상태 반환 | 낮음 |
| 매번 DB 조회 | 부하 증폭 |
| 매번 외부 API 호출 | 심각 |

![구현 방식과 부하](/img/posts/health-check-exposure-2.svg)

## 지금 무엇을 답하는지 본다

\`\`\`bash
# 흔한 경로에 무엇이 담겨 오는지
for p in /health /healthz /status /ping /actuator/health /api/health; do
  echo "--- $p"
  curl -s -m 5 "https://example.com$p" | head -c 300; echo
done
\`\`\`

**{"status":"UP"}** 한 줄이면 적절하다. 의존 서비스 이름과 주소가 나열돼 있으면 그것은 내부용 응답이 밖으로 나온 것이다.

## 엔드포인트 목록에 넣어 둔다

상태 확인 경로는 "기능"이 아니라서 API 목록에서 빠지고, 그래서 점검 대상에서도 빠진다. 인증 없이 열리는 경로 목록을 따로 만들어 두고 거기에 포함시킨다. 그 목록이 곧 외부에서 두드릴 수 있는 면적이고, 그것을 알고 있는 것과 모르는 것의 차이가 크다.

## 참고

- OWASP API Security Top 10 — 부적절한 자산 관리
- Kubernetes 문서 — liveness·readiness 프로브 설계
- CIS Benchmarks — 불필요한 정보 노출 제거`,
    diagram: {
      type: 'layers',
      caption: '상태 응답에 담기는 것',
      layers: [
        { label: '살아 있는가', note: '외부에 필요한 전부' },
        { label: '버전', note: '취약점 조회 단서' },
        { label: '의존 서비스', note: '내부 구성' },
        { label: '내부 호스트', note: '다음 표적' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '구현과 위험',
      x: ['캐시·즉시 응답', '매번 조회'],
      y: ['본문 없음', '상세 본문'],
      cells: ['안전', '부하 증폭', '정보 노출', '둘 다'],
    },
  },
  {
    slug: 'admin-ui-hardening',
    title: '관리 화면을 단단하게 만드는 순서',
    body: `관리 화면은 한 번 뚫리면 다른 방어를 전부 건너뛴다. 그 안에서는 이미 권한이 있는 상태로 움직이기 때문이다. 그래서 같은 접근 통제라도 관리 화면에는 한 단계 더 두꺼운 것을 건다.

## 접근 통제를 어디부터 조이나?

효과가 큰 순서가 있다. 위에서부터 하나씩 해 나가면 적은 노력으로 대부분의 위험이 줄어든다.

| 순서 | 조치 |
| --- | --- |
| 1 | 접근 경로 제한 |
| 2 | 다단계 인증 필수 |
| 3 | 세션 수명 단축 |
| 4 | 위험한 작업에 재인증 |
| 5 | 모든 행위 기록 |

접근 경로 제한이 첫 번째인 이유는 단순하다. 화면에 닿지 못하면 나머지 공격이 시작되지도 않는다.

![조이는 순서](/img/posts/admin-ui-hardening.svg)

## 경로를 어떻게 제한하나

완전히 내부망에 두는 것이 가장 강하지만 원격 근무와 부딪힌다. 현실적인 선택지를 놓고 고른다.

| 방법 | 강도 | 불편 |
| --- | --- | --- |
| 내부망 전용 | 매우 강함 | 큼 |
| VPN 경유 | 강함 | 보통 |
| 출발지 주소 제한 | 보통 | 보통 |
| 기기 인증서 | 강함 | 초기 설정 |
| 공개 + 인증만 | 약함 | 없음 |

기기 인증서는 설정만 끝나면 사용자가 느끼는 불편이 거의 없어 균형이 좋다.

## 위험한 작업을 나눈다

관리 화면 안에서도 무게가 다르다. 목록을 보는 것과 전체를 지우는 것을 같은 인증으로 다루지 않는다.

| 작업 | 추가 확인 |
| --- | --- |
| 조회 | 없음 |
| 개별 수정 | 없음 |
| 권한 변경 | 재인증 |
| 대량 삭제 | 재인증 + 확인 |
| 설정 변경 | 재인증 |

![작업별 확인 단계](/img/posts/admin-ui-hardening-2.svg)

## 지금 상태를 확인한다

관리 경로가 밖에서 보이는지부터 본다.

\`\`\`bash
# 밖에서 관리 경로에 닿는가
for p in /admin /admin/login /wp-admin /manager /console; do
  printf '%-14s %s\\n' "$p" "$(curl -s -o /dev/null -w '%{http_code}' https://example.com$p)"
done

# 세션 쿠키 속성 — Secure·HttpOnly·SameSite 가 붙어 있는가
curl -s -D - -o /dev/null https://example.com/admin/login | grep -i '^set-cookie'
\`\`\`

401 이나 302 가 와도 그 경로가 존재한다는 것은 알려진 셈이다. 그것 자체가 문제는 아니지만, 접근 경로 제한이 없다면 무차별 시도의 표적이 된다.

## 화면을 나누는 것도 방어다

관리 기능을 일반 서비스와 같은 애플리케이션에 두면, 일반 서비스의 취약점이 관리 기능까지 닿는다. 별도 애플리케이션으로 분리하면 공격 경로가 끊긴다. 처음부터 나누기는 어렵지만, 새로 만드는 관리 기능만이라도 분리된 쪽에 두면 시간이 지나며 경계가 생긴다.

## 참고

- OWASP Top 10 — 접근 통제 실패
- NIST SP 800-53 — 특권 기능 보호
- CIS Controls — 관리자 접근 통제`,
    diagram: {
      type: 'steps',
      caption: '조이는 순서',
      steps: [
        { label: '접근 경로 제한', note: '닿지 못하게' },
        { label: '다단계 인증', note: '자격 증명만으로 안 됨' },
        { label: '세션 단축', note: '탈취 창 축소' },
        { label: '재인증·기록', note: '위험한 작업' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '작업별 확인',
      x: ['되돌릴 수 있음', '어려움'],
      y: ['영향 범위 넓음', '좁음'],
      cells: ['재인증', '재인증 + 확인', '그대로', '재인증'],
    },
  },
  {
    slug: 'dashboard-access-control',
    title: '대시보드 접근 권한을 나누는 기준',
    body: `모니터링 대시보드는 대개 사내 누구나 볼 수 있게 열려 있다. 투명하게 공유하자는 취지지만, 대시보드에는 운영 데이터가 그대로 올라오고 그중에는 보여서는 안 되는 것이 섞여 있다.

## 대시보드에 무엇이 올라오나?

지표만 있을 것 같지만, 로그를 붙인 패널이나 예시 값에 실제 데이터가 딸려 온다.

| 패널 유형 | 섞여 들어오는 것 |
| --- | --- |
| 오류 로그 목록 | 요청 본문·사용자 식별자 |
| 느린 질의 | 질의문과 파라미터 |
| 상위 사용자 | 계정·이메일 |
| 지역별 접속 | 개별 IP |
| 결제 실패 | 주문 정보 |

느린 질의 패널이 특히 자주 새는 자리다. 질의문에 파라미터가 그대로 들어 있어 실제 값이 화면에 뜬다.

![대시보드에 딸려 오는 것](/img/posts/dashboard-access-control.svg)

## 접근 통제를 무엇으로 나누나

사람이 아니라 데이터를 기준으로 나눈다. 직급이 아니라 그 지표가 개인을 식별하는지를 본다.

| 등급 | 예 | 접근 |
| --- | --- | --- |
| 공개 | 응답 시간, 가동률 | 전사 |
| 운영 | 오류율, 큐 길이 | 개발·운영 |
| 민감 | 개인 식별 가능 | 담당자 |
| 규제 | 결제·개인정보 | 승인 필요 |

전사 공개 대시보드에는 개인을 식별할 수 있는 값이 하나도 없어야 한다. 이 기준 하나만 지켜도 대부분의 문제가 사라진다.

## 나누는 방법

권한을 사람마다 관리하면 금방 무너진다. 대시보드를 등급별로 묶고 묶음 단위로 권한을 준다.

| 방법 | 유지 비용 |
| --- | --- |
| 개인별 권한 | 매우 큼 |
| 폴더·묶음 단위 | 적정 |
| 데이터 소스 분리 | 확실하지만 무거움 |
| 질의 단계 마스킹 | 근본적 |

![접근 통제를 거는 지점](/img/posts/dashboard-access-control-2.svg)

## 지금 누가 무엇을 보는지 센다

\`\`\`bash
# 그라파나 예 — 공개 폴더와 대시보드 수
curl -s -H "Authorization: Bearer $TOKEN" 'https://grafana.example.com/api/search?type=dash-folder' \\
  | python3 -c 'import json,sys; [print(d["title"]) for d in json.load(sys.stdin)]'

# 익명 접근이 켜져 있는지 — 켜져 있으면 링크만 알면 누구나 본다
curl -s -o /dev/null -w '%{http_code}\\n' https://grafana.example.com/api/search
\`\`\`

인증 없이 200 이 오면 익명 접근이 켜진 것이다. 사내망에만 있다고 안심하기 어렵다 — 사내망에 들어온 공격자가 가장 먼저 보는 것이 대시보드다.

## 공유 링크가 만드는 구멍

대시보드 도구 대부분이 인증 없이 열리는 공유 링크를 만들 수 있다. 회의 때 편하게 쓰려고 만든 링크가 만료 없이 남는다. 링크 목록을 주기적으로 훑고, 만들 때 기한을 붙이는 관행을 두면 이 경로가 막힌다.

## 참고

- NIST SP 800-53 — 정보 접근 통제
- 개인정보보호법 — 접근 권한의 차등 부여
- OWASP — 민감 정보 노출 방지`,
    diagram: {
      type: 'layers',
      caption: '대시보드에 딸려 오는 것',
      layers: [
        { label: '지표', note: '수치만' },
        { label: '오류 로그', note: '요청 본문' },
        { label: '느린 질의', note: '파라미터 값' },
        { label: '상위 사용자', note: '계정 식별자' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '통제를 거는 지점',
      steps: [
        { label: '질의 단계 마스킹', note: '가장 근본' },
        { label: '데이터 소스 분리', note: '확실' },
        { label: '폴더 단위 권한', note: '유지 가능' },
        { label: '공유 링크 기한', note: '새는 경로' },
      ],
    },
  },
  {
    slug: 'metric-cardinality-cost',
    title: '지표 차원이 늘면 생기는 비용',
    body: `지표에 이름표를 하나 더 붙이는 일은 코드 한 줄이다. 그런데 그 한 줄이 저장 비용을 몇 배로 만들고, 어느 날 모니터링 시스템 자체를 멈추게 한다. 보안 지표를 설계할 때 특히 자주 밟는 함정이다.

## 왜 곱해지나?

시계열은 이름표 조합마다 따로 저장된다. 이름표가 하나 늘면 기존 조합 수에 그 이름표의 값 수만큼 곱해진다.

| 이름표 조합 | 시계열 수 |
| --- | --- |
| 서비스(10) | 10 |
| + 경로(50) | 500 |
| + 상태 코드(8) | 4,000 |
| + 사용자 ID(50,000) | 2억 |

사용자 ID 를 붙이는 순간 끝난다. 값의 종류가 무한에 가까운 것을 이름표로 쓰면 안 된다.

![이름표가 곱해지는 방식](/img/posts/metric-cardinality-cost.svg)

## 붙이면 안 되는 이름표

값의 종류가 늘어날 수 있는 것은 전부 위험하다.

| 이름표 | 위험 |
| --- | --- |
| 사용자 ID·이메일 | 무한 |
| 요청 ID·추적 ID | 무한 |
| 전체 경로(파라미터 포함) | 매우 큼 |
| IP 주소 | 매우 큼 |
| 타임스탬프 | 무한 |

경로는 파라미터를 치환해서 붙인다. **/users/12345** 가 아니라 **/users/:id** 로 묶으면 값의 종류가 경로 수만큼으로 줄어든다.

## 보안 지표에서 자주 하는 실수

"누가" 를 알고 싶어서 사용자 식별자를 이름표로 붙이는 경우가 많다. 그것은 지표가 아니라 로그로 다룰 일이다.

| 알고 싶은 것 | 맞는 도구 |
| --- | --- |
| 인증 실패가 늘었나 | 지표 |
| 누가 실패했나 | 로그 |
| 어느 서비스에서 | 지표(이름표) |
| 어떤 요청이었나 | 로그·추적 |

![지표와 로그의 경계](/img/posts/metric-cardinality-cost-2.svg)

## 지금 몇 개인지 센다

\`\`\`bash
# 시계열 수 상위 지표 — 무엇이 폭발했는지
curl -s 'http://prometheus:9090/api/v1/status/tsdb' \\
  | python3 -c 'import json,sys; d=json.load(sys.stdin)["data"]; [print(x["value"], x["name"]) for x in d["seriesCountByMetricName"][:10]]'

# 특정 지표의 이름표별 값 종류
curl -s 'http://prometheus:9090/api/v1/label/user_id/values' \\
  | python3 -c 'import json,sys; print(len(json.load(sys.stdin)["data"]), "종류")'
\`\`\`

한 지표가 전체 시계열의 절반을 차지하고 있다면 그 지표의 이름표를 먼저 본다. 대개 하나가 원인이다.

## 늦게 발견하면 되돌리기 어렵다

이미 저장된 시계열은 이름표를 뗀다고 사라지지 않는다. 보관 기간이 지나야 정리되고, 그동안 조회 성능은 계속 나쁘다. 그래서 새 지표를 추가할 때 이름표의 값 종류가 몇 개가 될지 먼저 세어 보는 습관이 저장 비용보다 훨씬 싸다.

## 참고

- Prometheus 문서 — 이름표 설계 주의사항
- Google SRE Book — 모니터링 지표 선택
- OpenTelemetry — 지표와 추적의 구분`,
    diagram: {
      type: 'bars',
      caption: '이름표를 더할 때 시계열 수',
      unit: '개',
      items: [
        { label: '서비스', value: 10, note: '기준' },
        { label: '+경로', value: 500, note: '50배' },
        { label: '+상태코드', value: 4000, note: '8배' },
        { label: '+사용자', value: 200000, note: '통제 불능' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '지표와 로그의 경계',
      x: ['값 종류 적음', '많음'],
      y: ['추세를 본다', '개별을 본다'],
      cells: ['지표 이름표', '로그로', '지표', '로그·추적'],
    },
  },
]
