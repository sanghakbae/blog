import type { SeedPost } from './types'

/** 590~599 — 배포와 변경을 다루는 일 */
export const posts60: SeedPost[] = [
  {
    slug: 'release-approval-gate',
    title: '배포 승인을 형식이 아니게 만들기',
    body: `배포 전 승인 절차는 대부분의 조직에 있다. 그런데 승인을 누르는 사람이 무엇을 보고 판단하는지 정해져 있지 않으면, 그 단계는 기록만 남기고 아무것도 거르지 못한다.

## 파이프라인에서 무엇을 보고 승인하나

| 항목 | 판단 근거 |
| --- | --- |
| 변경 범위 | 어떤 코드가 바뀌었나 |
| 위험 신호 | 인증·권한·결제에 닿나 |
| 시험 결과 | 통과했나, 새로 추가됐나 |
| 되돌리기 | 실패하면 어떻게 |
| 시점 | 지금 배포해도 되는 시간인가 |

되돌리기 계획이 없는 변경은 승인하지 않는다는 규칙 하나만으로도 사고의 크기가 달라진다.

![승인이 형식이 되는 경로](/img/posts/release-approval-gate.svg)

## 전부에 같은 절차를 두지 않는다

모든 배포에 같은 무게의 승인을 요구하면 승인이 통과 의례가 된다. 위험에 따라 단계를 나눈다.

| 변경 | 절차 |
| --- | --- |
| 문구·스타일 | 자동 통과 |
| 일반 기능 | 코드 리뷰로 충분 |
| 인증·권한·결제 | 별도 승인 |
| 데이터 구조 변경 | 승인 + 되돌리기 계획 |
| 인프라 설정 | 승인 + 시간대 제한 |

무엇이 어느 칸에 들어가는지 자동으로 판단할 수 있어야 한다. 사람이 매번 분류하면 그 판단이 또 빠진다.

## 자동으로 위험을 표시한다

바뀐 파일 경로만 봐도 상당수는 분류된다.

| 경로 패턴 | 표시 |
| --- | --- |
| 인증·세션 관련 | 높음 |
| 권한 판단 | 높음 |
| 결제·정산 | 높음 |
| 마이그레이션 | 높음 |
| 화면 문구 | 낮음 |

![위험에 따라 나눈 절차](/img/posts/release-approval-gate-2.svg)

## 변경 내용을 자동으로 요약한다

\`\`\`bash
# 배포에 포함된 변경 중 위험 경로에 닿는 것
git diff --name-only origin/main...HEAD \
  | grep -E '(auth|session|permission|payment|migrations)/' \
  | sed 's/^/  위험 경로: /'

# 마이그레이션이 포함됐는지 — 되돌리기 계획이 필요한 신호
git diff --name-only origin/main...HEAD | grep -c 'migrations/'
\`\`\`

이 결과를 승인 요청에 자동으로 붙이면 승인자가 무엇을 볼지 고민하지 않아도 된다. 판단에 필요한 것을 눈앞에 놓는 것이 절차를 살아 있게 만든다.

## 승인자를 정하는 기준

그 코드를 이해하는 사람이 승인해야 뜻이 있다. 직급으로 정하면 내용을 모르는 사람이 누르게 된다. 코드 소유자를 지정해 두고 그 영역의 변경은 소유자가 보게 하면, 승인이 실제 검토가 된다.

## 참고

- NIST SP 800-53 — 변경 통제
- CIS Controls — 변경 관리 절차
- ISO/IEC 27002 — 변경 관리`,
    diagram: {
      type: 'flow',
      caption: '승인이 형식이 되는 경로',
      steps: [
        { label: '모든 배포에 승인', note: '같은 무게' },
        { label: '볼 것이 정해지지 않음', note: '판단 근거 없음' },
        { label: '통과 의례가 됨', note: '내용 안 봄', danger: true },
        { label: '위험 변경도 통과', note: '거르지 못함' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '위험에 따라 나눈 절차',
      x: ['되돌리기 쉬움', '어려움'],
      y: ['민감 영역', '일반'],
      cells: ['별도 승인', '승인 + 계획', '리뷰로 충분', '승인'],
    },
  },
  {
    slug: 'rollback-safety',
    title: '되돌릴 수 있는 배포와 없는 배포',
    body: `배포를 되돌리면 된다는 말은 코드에만 해당한다. 데이터 구조가 바뀌었거나 외부에 알림이 나갔다면 되돌리기는 이전 상태로 가는 것이 아니라 새로운 상태로 가는 것이다.

## 백업으로도 되돌아가지 않는 것

| 대상 | 되돌리기 |
| --- | --- |
| 애플리케이션 코드 | 된다 |
| 설정 값 | 대개 된다 |
| 데이터 구조 | 어렵다 |
| 이미 쓰인 데이터 | 안 된다 |
| 보낸 메일·알림 | 안 된다 |
| 외부에 전달된 이벤트 | 안 된다 |

칸을 지우는 변경이 대표적이다. 되돌려도 그 사이에 들어온 데이터의 그 칸은 비어 있다.

![되돌릴 수 없는 것들](/img/posts/rollback-safety.svg)

## 어떻게 나누면 되돌릴 수 있나?

데이터 구조 변경은 한 번에 하지 않고 단계로 나눈다. 각 단계가 앞뒤 버전과 함께 동작해야 한다.

| 단계 | 내용 |
| --- | --- |
| 1 | 새 칸 추가, 옛 칸 유지 |
| 2 | 양쪽에 쓰기 |
| 3 | 읽기를 새 칸으로 |
| 4 | 옛 칸 쓰기 중단 |
| 5 | 옛 칸 제거 |

각 단계 사이에 배포가 들어가므로 어느 시점에 되돌려도 서비스가 동작한다. 한 번에 바꾸면 되돌릴 창이 없다.

## 시간 제한을 둔다

되돌리기는 시간이 지날수록 어려워진다. 그 사이 쌓인 데이터가 새 구조를 전제로 하기 때문이다.

| 경과 | 되돌리기 |
| --- | --- |
| 수 분 | 안전 |
| 수 시간 | 데이터 확인 필요 |
| 수 일 | 사실상 앞으로 고치기 |

![단계로 나눈 변경](/img/posts/rollback-safety-2.svg)

## 되돌릴 수 있는지 미리 확인한다

\`\`\`bash
# 마이그레이션에 되돌리기 정의가 있는지
grep -rL 'down\|rollback\|revert' db/migrations/*.sql 2>/dev/null | head

# 되돌리기를 시험 환경에서 실제로 돌려 본다
npm run migrate:up && npm run migrate:down && npm run migrate:up
\`\`\`

되돌리기 정의가 없는 마이그레이션은 배포 전에 걸러야 한다. 정의가 있어도 실제로 돌려 보지 않으면 그것이 동작하는지 알 수 없다.

## 앞으로 고치는 선택

되돌리기가 어려운 상황이라면 앞으로 고치는 편이 빠를 때가 있다. 문제는 그 판단을 장애 한가운데에서 해야 한다는 점이다. 배포 전에 "이것은 되돌릴 수 있는가, 아니면 앞으로 고쳐야 하는가"를 적어 두면, 그 순간에 고민하지 않아도 된다.

## 참고

- Google SRE Book — 안전한 릴리스
- NIST SP 800-53 — 구성 변경 통제
- Martin Fowler — 확장·수축 패턴`,
    diagram: {
      type: 'layers',
      caption: '되돌릴 수 없는 것들',
      layers: [
        { label: '코드', note: '되돌아감' },
        { label: '데이터 구조', note: '어려움' },
        { label: '쌓인 데이터', note: '안 됨' },
        { label: '나간 알림', note: '안 됨' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '단계로 나눈 변경',
      steps: [
        { label: '새 칸 추가', note: '옛 칸 유지' },
        { label: '양쪽에 쓰기', note: '둘 다 최신' },
        { label: '읽기 전환', note: '새 칸으로' },
        { label: '옛 칸 제거', note: '마지막에' },
      ],
    },
  },
  {
    slug: 'canary-release-security',
    title: '일부에게만 먼저 여는 배포의 보안 면',
    body: `새 버전을 일부 사용자에게만 먼저 열면 문제를 작게 발견할 수 있다. 보안 관점에서는 두 가지가 더해진다 — 취약점이 생겼을 때 노출 범위가 좁고, 이상 징후를 비교로 알아차릴 수 있다.

## 무엇을 비교하나

| 지표 | 무엇을 알려 주나 |
| --- | --- |
| 오류율 | 깨진 기능 |
| 인증 실패율 | 인증 흐름 변경 |
| 권한 거부율 | 접근 통제 변경 |
| 응답 시간 | 자원 소모 |
| 외부 호출 수 | 의도치 않은 요청 |

권한 거부율의 변화가 특히 유용하다. 갑자기 늘면 통제가 과하게 걸린 것이고, 갑자기 줄면 통제가 빠진 것이다.

![비교로 드러나는 것](/img/posts/canary-release-security.svg)

## 누구에게 먼저 여나

| 대상 | 적합성 |
| --- | --- |
| 내부 사용자 | 초기 확인 |
| 무작위 일부 | 통계적 비교 |
| 특정 지역 | 영향 격리 |
| 특정 고객 | 부적절 — 동의 없이 |

특정 고객을 대상으로 삼는 것은 피한다. 문제가 생기면 그 고객만 피해를 보고, 그 사실을 설명하기 어렵다.

## 언제 멈출 것인가

기준을 미리 정하지 않으면 판단이 늦어진다. 숫자로 적어 두고 자동으로 멈추게 한다.

| 신호 | 조치 |
| --- | --- |
| 오류율 2배 | 자동 중단 |
| 인증 실패 급증 | 자동 중단 |
| 새 외부 도메인 호출 | 확인 필요 |
| 응답 시간 악화 | 관찰 |

![중단 기준](/img/posts/canary-release-security-2.svg)

## 비교가 되는지 확인한다

\`\`\`bash
# 두 버전의 오류율을 나란히 본다
for v in stable canary; do
  total=$(grep -c "version=$v" /var/log/app.log)
  err=$(grep "version=$v" /var/log/app.log | grep -c ' 5[0-9][0-9] ')
  printf '%-8s 요청 %6s · 오류 %4s · %s%%\n' "$v" "$total" "$err" \
    "$(awk "BEGIN{printf \"%.2f\", $err*100/($total?$total:1)}")"
done
\`\`\`

로그에 버전 표시가 없으면 비교 자체가 불가능하다. 배포 방식을 바꾸기 전에 그 표시부터 넣는 것이 순서다.

## 단계적 배포가 흔히 실패하는 지점

일부에게만 열었는데 그 일부가 대표성이 없으면 비교가 뜻을 잃는다. 내부 사용자만으로 시작하면 실제 사용자의 기기·회선·사용 패턴이 빠져 있어, 통과했다는 사실이 아무것도 보장하지 않는다. 무작위로 고르되 충분한 수가 되어야 통계적 비교가 성립한다.

관찰 기간도 함께 정한다. 몇 분 만에 판단하면 느리게 드러나는 문제를 놓치고, 며칠을 끌면 두 버전을 함께 운영하는 부담이 커진다. 무엇을 보려는지에 따라 기간이 정해진다 — 오류율은 몇 분이면 드러나지만 자원 누수는 몇 시간이 걸린다.

## 되돌리기가 전제다

일부에게만 열었어도 문제가 생기면 되돌려야 한다. 되돌릴 수 없는 변경을 이 방식으로 내보내면, 문제를 일찍 발견하고도 할 수 있는 일이 없다. 단계적 배포는 되돌리기와 짝을 이룰 때 값을 한다.

## 참고

- Google SRE Book — 점진적 롤아웃
- NIST SP 800-53 — 변경 영향 분석
- OWASP — 배포 보안`,
    diagram: {
      type: 'bars',
      caption: '비교로 드러나는 것',
      unit: '%',
      items: [
        { label: '오류율', value: 85, note: '깨진 기능' },
        { label: '권한 거부', value: 80, note: '통제 변화' },
        { label: '인증 실패', value: 75, note: '흐름 변경' },
        { label: '응답 시간', value: 45, note: '자원 소모' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '중단 기준',
      steps: [
        { label: '숫자로 미리 정함', note: '판단 지연 방지' },
        { label: '자동 중단', note: '사람 없이' },
        { label: '되돌리기', note: '전제 조건' },
        { label: '원인 확인 후 재개', note: '같은 기준으로' },
      ],
    },
  },
  {
    slug: 'change-approval-security',
    title: '긴급 변경에 절차를 남기는 방법',
    body: `장애가 났을 때는 절차를 지킬 여유가 없다. 그래서 긴급 변경 경로를 열어 두는데, 그 경로가 평상시에도 쓰이기 시작하면 변경 통제 전체가 무너진다.

## 보안 정책을 우회하는 길이 되는 이유

| 이유 | 결과 |
| --- | --- |
| 정규 절차가 느림 | 급하지 않아도 우회 |
| 긴급 기준이 모호 | 판단이 사람마다 |
| 사후 검토 없음 | 쓰고 끝 |
| 쓴 사람에게 이득 | 빨리 끝남 |

정규 절차가 느린 것이 근본 원인인 경우가 많다. 우회 경로를 막는 것보다 정규 경로를 빠르게 만드는 편이 효과가 크다.

![긴급 경로가 상시가 되는 경로](/img/posts/change-approval-security.svg)

## 긴급을 어떻게 정의하나

| 상황 | 긴급인가 |
| --- | --- |
| 서비스 중단 | 그렇다 |
| 데이터 유실 진행 중 | 그렇다 |
| 취약점 악용 중 | 그렇다 |
| 고객 요청 | 아니다 |
| 일정이 촉박 | 아니다 |

정의를 적어 두고 그 밖의 경우에 쓰면 사후 검토에서 지적한다. 지적이 없으면 정의는 문서에만 남는다.

## 사후에 무엇을 하나

| 항목 | 내용 |
| --- | --- |
| 무엇을 바꿨나 | 변경 내용 기록 |
| 왜 긴급이었나 | 판단 근거 |
| 정규 절차로 재확인 | 코드 리뷰 |
| 재발 방지 | 같은 일이 또 생기나 |

긴급 변경의 사후 검토를 24시간 안에 하는 규칙을 두면, 급하게 넣은 코드가 그대로 남는 것을 막을 수 있다.

![사후 검토 절차](/img/posts/change-approval-security-2.svg)

## 얼마나 쓰이는지 센다

\`\`\`bash
# 긴급 경로로 나간 배포 비율
total=$(git log --since='3 months ago' --grep='' --oneline | wc -l)
hot=$(git log --since='3 months ago' --grep='hotfix\|emergency\|긴급' --oneline | wc -l)
echo "전체 $total · 긴급 $hot · 비율 $(awk "BEGIN{printf \"%.1f\", $hot*100/($total?$total:1)}")%"
\`\`\`

긴급 배포가 전체의 일할을 넘으면 그것은 긴급이 아니라 기본 경로다. 그 시점에는 정규 절차를 손봐야 한다.

## 절차가 막는 것과 못 막는 것

변경 통제는 실수를 줄이지만 악의를 막지는 못한다. 승인 권한을 가진 사람이 스스로 악의적 변경을 넣으면 절차는 그것을 통과시킨다. 그래서 승인과 배포를 다른 사람이 하게 하고, 배포된 것이 승인된 것과 같은지 확인하는 장치가 따로 필요하다.

## 참고

- NIST SP 800-53 — 변경 통제와 긴급 변경
- ISO/IEC 27002 — 변경 관리 절차
- CIS Controls — 변경 승인`,
    diagram: {
      type: 'flow',
      caption: '긴급 경로가 상시가 되는 경로',
      steps: [
        { label: '정규 절차가 느림', note: '며칠 걸림' },
        { label: '긴급 경로 사용', note: '급하지 않아도', danger: true },
        { label: '사후 검토 없음', note: '지적도 없음' },
        { label: '기본 경로가 됨', note: '통제 무력화' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '사후 검토 절차',
      steps: [
        { label: '24시간 안에', note: '기억이 남았을 때' },
        { label: '변경 내용 기록', note: '무엇을' },
        { label: '긴급 판단 근거', note: '왜' },
        { label: '정규 리뷰', note: '다시 확인' },
      ],
    },
  },
  {
    slug: 'infrastructure-review',
    title: '인프라 변경을 코드 리뷰처럼 보기',
    body: `인프라를 코드로 관리하면 변경이 파일로 남는다. 그런데 그 파일의 리뷰는 애플리케이션 코드만큼 꼼꼼하지 않은 경우가 많다. 한 줄이 방화벽을 열거나 권한을 넓히는데도 그렇다.

## 인프라 코드에서 어떤 줄이 위험한가?

| 변경 | 영향 |
| --- | --- |
| 접근 대역 확대 | 외부 노출 |
| 권한 정책 완화 | 최소 권한 이탈 |
| 저장소 공개 설정 | 데이터 노출 |
| 로그 수집 중단 | 탐지 공백 |
| 암호화 해제 | 보호 상실 |

로그 수집을 끄는 변경은 눈에 띄지 않는다. 기능에 영향이 없고 비용만 줄기 때문에 통과되기 쉽다.

![위험한 한 줄](/img/posts/infrastructure-review.svg)

## 자동으로 걸러 낸다

사람이 전부 보기는 어렵다. 정책 검사 도구가 위험한 패턴을 표시하게 하고, 사람은 표시된 것만 본다.

| 검사 | 예 |
| --- | --- |
| 공개 접근 | 전체 대역 허용 |
| 넓은 권한 | 모든 자원·모든 동작 |
| 암호화 누락 | 저장소·전송 |
| 태그 누락 | 담당자 불명 |
| 로그 설정 변경 | 수집 중단 |

## 계획 결과를 리뷰한다

파일만 보면 실제로 무엇이 바뀌는지 알기 어렵다. 적용 전 계획 결과를 함께 붙이면 영향이 드러난다.

| 표시 | 주의 |
| --- | --- |
| 생성 | 비용·노출 |
| 변경 | 다운타임 |
| 삭제 | 되돌릴 수 없음 |
| 교체 | 식별자 변경 |

삭제와 교체가 특히 중요하다. 이름만 바꿨는데 자원이 통째로 교체되는 경우가 있다.

![계획 결과에서 볼 것](/img/posts/infrastructure-review-2.svg)

## 위험한 변경을 뽑아낸다

\`\`\`bash
# 접근을 넓히는 변경을 찾는다
git diff origin/main...HEAD -- '*.tf' '*.yaml' \
  | grep -E '^\+' | grep -E '0\.0\.0\.0/0|"\*"|public|Allow' | head -20

# 로그·감사 설정이 꺼지는 변경
git diff origin/main...HEAD | grep -E '^\-.*(logging|audit|cloudtrail).*(true|enabled)' | head
\`\`\`

두 번째 검사가 특히 값을 한다. 제거된 줄에서 로그 설정을 찾으면 탐지 공백이 생기는 변경이 드러난다.

## 드리프트도 함께 본다

코드에 없는 변경이 손으로 들어가면 코드와 실제가 어긋난다. 그 상태에서 코드를 적용하면 손으로 넣은 설정이 지워진다. 정기적으로 차이를 검사해 어긋난 것을 코드로 되돌리거나, 실제를 코드에 반영한다. 차이를 방치하면 어느 쪽이 맞는지 아무도 모르게 된다.

## 참고

- CIS Benchmarks — 클라우드 설정 기준
- NIST SP 800-53 — 구성 관리
- OWASP — IaC 보안`,
    diagram: {
      type: 'layers',
      caption: '위험한 한 줄',
      layers: [
        { label: '접근 대역 확대', note: '외부 노출' },
        { label: '권한 완화', note: '최소 권한 이탈' },
        { label: '암호화 해제', note: '보호 상실' },
        { label: '로그 중단', note: '탐지 공백' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '계획 결과에서 볼 것',
      x: ['되돌릴 수 있음', '없음'],
      y: ['영향 큼', '작음'],
      cells: ['확인 후 진행', '별도 승인', '진행', '확인'],
    },
  },
  {
    slug: 'config-drift-watch',
    title: '코드와 실제 설정이 어긋날 때',
    body: `인프라를 코드로 정의해 두어도 콘솔에서 손으로 고치는 일이 생긴다. 급해서, 혹은 코드를 고칠 줄 몰라서다. 그 순간부터 코드는 실제를 설명하지 못하고, 그 사실을 아무도 모른 채 시간이 간다.

## 어긋나면 무엇이 문제인가

| 문제 | 설명 |
| --- | --- |
| 코드를 믿을 수 없음 | 문서로서 쓸모 상실 |
| 적용이 위험해짐 | 손으로 넣은 설정이 지워짐 |
| 복구가 불완전 | 코드로 다시 세우면 다름 |
| 감사에서 설명 불가 | 무엇이 실제인지 모름 |

복구 상황에서 가장 크게 드러난다. 코드로 환경을 다시 세웠는데 동작하지 않고, 왜 그런지 아는 사람이 없다.

![코드와 실제가 벌어지는 과정](/img/posts/config-drift-watch.svg)

## 어떻게 발견하나

| 방법 | 주기 |
| --- | --- |
| 계획 실행으로 차이 확인 | 매일 |
| 변경 이벤트 감시 | 실시간 |
| 설정 이력 비교 | 주 1회 |
| 수동 변경 권한 제한 | 예방 |

매일 계획을 돌려 차이가 없는지 보는 것이 가장 단순하다. 차이가 나오면 그날 안에 처리한다 — 쌓이면 원인을 찾기 어려워진다.

## 어긋난 것을 어떻게 처리하나

| 판단 | 처리 |
| --- | --- |
| 손으로 넣은 것이 맞다 | 코드에 반영 |
| 코드가 맞다 | 적용해 되돌림 |
| 둘 다 아니다 | 정책 검토 |
| 이유를 모름 | 조사 대상 |

이유를 모르는 변경은 그 자체로 조사할 이유다. 누가 왜 바꿨는지 설명되지 않는 설정 변경은 침해의 흔적일 수 있다.

![어긋난 것 처리](/img/posts/config-drift-watch-2.svg)

## 차이를 매일 본다

\`\`\`bash
# 적용하지 않고 차이만 본다. 0 이면 어긋남 없음.
terraform plan -detailed-exitcode -no-color > /tmp/plan.txt 2>&1
case $? in
  0) echo '어긋남 없음' ;;
  2) echo '차이 있음'; grep -E '^\s+[~+-]' /tmp/plan.txt | head -20 ;;
  *) echo '계획 실패 — 설정이나 자격 증명을 확인한다' ;;
esac
\`\`\`

계획 실패를 차이 없음으로 다루면 안 된다. 자격 증명 만료로 계획이 안 돌고 있는데 조용히 통과되는 경우가 있다.

## 손으로 고치는 것을 막을 것인가

전면 금지는 장애 대응을 어렵게 한다. 대신 손으로 고친 뒤 코드에 반영하는 절차를 짧게 만들고, 반영되지 않은 변경이 며칠 이상 남으면 알림이 가게 한다. 금지보다 되돌아오는 경로를 쉽게 만드는 쪽이 실제로 지켜진다.

## 참고

- NIST SP 800-128 — 보안 구성 관리
- CIS Controls — 구성 기준선 유지
- HashiCorp — 드리프트 탐지`,
    diagram: {
      type: 'flow',
      caption: '코드와 실제가 벌어지는 과정',
      steps: [
        { label: '급한 수정', note: '콘솔에서 직접' },
        { label: '코드에 반영 안 함', note: '나중에 하려다' },
        { label: '차이 누적', note: '아무도 모름', danger: true },
        { label: '복구 시 실패', note: '설명 불가' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '어긋난 것 처리',
      x: ['이유 설명됨', '모름'],
      y: ['손이 맞음', '코드가 맞음'],
      cells: ['코드에 반영', '조사', '적용해 되돌림', '조사'],
    },
  },
  {
    slug: 'internal-registry-setup',
    title: '내부 저장소를 두는 이유와 함정',
    body: `외부 패키지 저장소를 그대로 쓰면 빌드가 남의 서비스에 매달린다. 내부 저장소를 두면 그 의존이 줄고 검사할 지점이 생기지만, 설정을 잘못하면 오히려 공격 통로가 만들어진다.

## 의존성을 내부에 두면 무엇을 얻나

| 이점 | 내용 |
| --- | --- |
| 가용성 | 외부가 멈춰도 빌드됨 |
| 속도 | 가까운 곳에서 받음 |
| 검사 지점 | 들어오는 것을 훑을 수 있음 |
| 기록 | 무엇을 받았는지 남음 |
| 삭제 대응 | 사라진 패키지 보관 |

외부에서 패키지가 삭제되는 일이 실제로 있다. 내부에 사본이 있으면 그날 빌드가 멈추지 않는다.

![내부 저장소가 만드는 지점](/img/posts/internal-registry-setup.svg)

## 이름 혼동이라는 함정

내부 저장소와 외부를 함께 쓰는 설정에서, 같은 이름의 패키지가 양쪽에 있으면 어느 쪽을 받을지가 문제가 된다. 외부 것을 우선하는 설정이면 공격자가 같은 이름을 외부에 올려 우리 빌드에 코드를 넣을 수 있다.

| 설정 | 결과 |
| --- | --- |
| 외부 우선 | 이름 혼동에 취약 |
| 내부 우선 | 안전, 다만 이름 관리 필요 |
| 범위별 분리 | 가장 안전 |
| 버전 높은 것 | 가장 위험 |

내부 패키지에 고유한 범위나 접두사를 붙이고, 그 범위는 내부에서만 찾게 설정하면 이 문제가 닫힌다.

## 통과시킬 것을 정한다

| 검사 | 내용 |
| --- | --- |
| 알려진 취약점 | 차단 또는 경고 |
| 라이선스 | 허용 목록 |
| 서명 | 있으면 검증 |
| 설치 스크립트 | 표시 |

전부 차단하면 개발이 멈추고, 전부 통과시키면 저장소를 둔 뜻이 없다. 차단은 좁게, 표시는 넓게 두는 편이 균형이 맞다.

![들어오는 것을 거르는 기준](/img/posts/internal-registry-setup-2.svg)

## 설정을 확인한다

\`\`\`bash
# 어느 저장소를 보는지, 범위별 설정이 있는지
npm config get registry
cat .npmrc 2>/dev/null | grep -E 'registry|@'

# 내부 범위가 외부로 새지 않는지 — 외부에 같은 이름이 있는지 확인
npm view @our-scope/internal-lib 2>&1 | head -3
\`\`\`

마지막 확인이 중요하다. 내부 범위 이름이 외부에 이미 존재한다면 누군가 선점한 것이고, 설정이 어긋나는 순간 그것을 받게 된다.

## 사본을 얼마나 두나

모든 패키지의 모든 버전을 영구 보관하면 저장 비용이 계속 는다. 운영에 쓰이는 버전과 그 직전 버전만 남기고 나머지는 정리하되, 삭제 전에 어떤 빌드가 그것을 쓰는지 확인한다. 오래된 프로젝트가 옛 버전에 고정돼 있는 경우가 있다.

## 참고

- NIST SP 800-161 — 공급망 위험 관리
- OWASP — 의존성 혼동 공격
- SLSA — 빌드 출처 보증`,
    diagram: {
      type: 'layers',
      caption: '내부 저장소가 만드는 지점',
      layers: [
        { label: '가용성', note: '외부 장애 무관' },
        { label: '검사 지점', note: '들어오는 것 훑기' },
        { label: '기록', note: '무엇을 받았나' },
        { label: '사본 보관', note: '삭제 대응' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '이름 혼동 위험',
      x: ['범위 분리', '이름 공유'],
      y: ['내부 우선', '외부 우선'],
      cells: ['안전', '주의', '안전', '취약'],
    },
  },
  {
    slug: 'package-name-confusion',
    title: '비슷한 이름의 패키지가 들어오는 길',
    body: `의존성 목록에 한 글자 다른 이름이 들어와도 눈으로는 잘 걸리지 않는다. 공격자는 흔한 이름의 오타나 내부 이름과 같은 이름을 외부에 올려 두고 기다린다.

## 어떤 이름이 쓰이나

| 수법 | 예 |
| --- | --- |
| 오타 | 한 글자 빠지거나 바뀜 |
| 구분자 바꿈 | 하이픈과 밑줄 |
| 접두사 붙임 | 유명 이름 앞뒤에 단어 |
| 내부 이름 선점 | 사내 패키지와 같은 이름 |
| 버전만 높게 | 같은 이름, 더 높은 버전 |

내부 이름 선점이 가장 조용하다. 우리 사내 패키지 이름을 외부에 올려 두면, 설정이 어긋나는 순간 우리 빌드가 그것을 받는다.

![이름이 비슷한 패키지가 들어오는 길](/img/posts/package-name-confusion.svg)

## 왜 눈으로 안 걸리나

| 이유 | 설명 |
| --- | --- |
| 익숙한 이름으로 읽음 | 뇌가 보정 |
| 잠금 파일 변경이 큼 | 수천 줄 |
| 간접 의존성 | 목록에 없음 |
| 자동 갱신 | 사람이 안 봄 |

익숙한 이름으로 읽어 버리는 것이 근본이다. 소리 내어 읽거나 기계가 비교하게 하는 편이 낫다.

## 어떻게 막나

| 방법 | 효과 |
| --- | --- |
| 내부 범위 사용 | 이름 충돌 자체 차단 |
| 내부 저장소 우선 | 외부 선점 무력화 |
| 새 이름 알림 | 추가된 것만 검토 |
| 유사도 검사 | 기존 이름과 비교 |
| 설치 스크립트 차단 | 피해 축소 |

새 이름 알림과 유사도 검사를 함께 두면, 사람이 볼 목록이 몇 줄로 줄어든다.

![막는 방법](/img/posts/package-name-confusion-2.svg)

## 새로 들어온 이름을 뽑는다

\`\`\`bash
# 이번 변경에서 새로 추가된 패키지 이름
git diff HEAD~1 -- package-lock.json \
  | grep '^+.*"resolved"' | sed 's#.*/\([^/]*\)/-/.*#\\1#' | sort -u > /tmp/new.txt

# 기존 이름과 얼마나 비슷한지 — 비슷한데 다른 것이 위험하다
comm -23 /tmp/new.txt <(npm ls --all --parseable 2>/dev/null \
  | sed 's#.*node_modules/##' | sort -u) | head -20
\`\`\`

새 이름이 나왔는데 직접 추가한 적이 없다면, 어느 패키지가 그것을 끌고 왔는지부터 확인한다.

## 잠금 파일이 있어도 새는 자리

잠금 파일이 있으면 같은 것이 설치된다고 믿기 쉽다. 그런데 잠금 파일을 새로 만드는 순간, 그 시점의 저장소 설정에 따라 다른 것이 들어올 수 있다. 새 개발자가 처음 설치하거나 잠금 파일을 지우고 다시 만드는 경우가 그렇다.

빌드 서버에서는 잠금 파일을 그대로 쓰는 명령을 쓴다. 잠금 파일과 목록이 어긋나면 설치가 실패하게 하는 것이 맞다. 조용히 새로 만들어 진행하면 무엇이 들어왔는지 아무도 모른 채 배포된다.

## 우리 이름을 지키는 쪽도 있다

사내에서만 쓰는 패키지 이름을 외부 저장소에 미리 등록해 두는 방법이 있다. 빈 패키지를 올려 이름을 선점하면 남이 그 이름을 쓸 수 없다. 근본 해결은 아니지만, 설정 실수가 났을 때 받아 오는 것이 우리 것이라는 점에서 한 겹이 된다.

## 참고

- OWASP — 의존성 혼동 공격
- NIST SP 800-161 — 공급망 위험 관리
- npm 문서 — 범위와 저장소 설정`,
    diagram: {
      type: 'flow',
      caption: '이름이 비슷한 패키지가 들어오는 길',
      steps: [
        { label: '공격자가 이름 선점', note: '외부 저장소에' },
        { label: '설정이 외부 우선', note: '또는 오타 입력', danger: true },
        { label: '설치 스크립트 실행', note: '빌드 서버 권한' },
        { label: '자격 증명 유출', note: '코드보다 먼저' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '막는 방법별 효과',
      unit: '점',
      items: [
        { label: '내부 범위', value: 90, note: '충돌 차단' },
        { label: '내부 저장소 우선', value: 85, note: '선점 무력화' },
        { label: '새 이름 알림', value: 60, note: '검토 대상 축소' },
        { label: '눈으로 확인', value: 20, note: '보정되어 읽힘' },
      ],
    },
  },
  {
    slug: 'license-compliance-scan',
    title: '라이선스 검사를 보안 점검에 넣는 이유',
    body: `라이선스는 법무의 일처럼 보이지만, 확인 과정이 공급망 점검과 거의 같다. 무엇을 쓰고 있는지 목록을 만들고 각각의 조건을 확인하는 일이라, 한 번에 처리하는 편이 효율적이다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 라이선스 종류 | 허용 목록 안인가 |
| 전파 조건 | 우리 코드에 영향 |
| 고지 의무 | 표시해야 하는가 |
| 상업적 사용 | 제한이 있는가 |
| 변경 여부 | 라이선스가 바뀌었나 |

라이선스가 중간에 바뀌는 일이 있다. 버전을 올렸더니 조건이 달라져 있는 경우다.

![라이선스 확인 항목](/img/posts/license-compliance-scan.svg)

## 공급망 점검과 겹치는 부분

| 작업 | 라이선스 | 보안 |
| --- | --- | --- |
| 의존성 목록 작성 | 필요 | 필요 |
| 간접 의존성 포함 | 필요 | 필요 |
| 버전별 확인 | 필요 | 필요 |
| 변경 감시 | 필요 | 필요 |

목록을 만드는 일이 둘의 공통 작업이다. 한 번 만들어 두면 양쪽에 쓴다.

## 허용 목록을 정한다

| 분류 | 처리 |
| --- | --- |
| 자유로운 조건 | 허용 |
| 고지 필요 | 허용, 표시 |
| 전파 조건 있음 | 검토 |
| 상업적 제한 | 차단 |
| 라이선스 불명 | 차단 |

라이선스가 표시되지 않은 패키지는 차단하는 편이 맞다. 조건을 모르는 코드를 쓰는 것은 조건이 나쁜 코드를 쓰는 것보다 위험하다.

![분류와 처리](/img/posts/license-compliance-scan-2.svg)

## 목록을 뽑는다

\`\`\`bash
# 라이선스별 패키지 수
npx license-checker --summary 2>/dev/null | head -20

# 라이선스가 불명인 것 — 가장 먼저 확인할 대상
npx license-checker --json 2>/dev/null \
  | python3 -c 'import json,sys; d=json.load(sys.stdin); [print(k) for k,v in d.items() if v.get("licenses") in (None,"UNKNOWN")]' | head
\`\`\`

불명 항목이 나오면 그 패키지의 저장소를 직접 확인한다. 도구가 못 읽었을 뿐 파일에는 있는 경우가 많다.

## 간접 의존성까지 봐야 하는 이유

직접 추가한 패키지의 라이선스만 보면 절반도 보지 못한 것이다. 실제로 배포물에 들어가는 코드는 간접 의존성이 훨씬 많고, 그중 하나가 전파 조건이 있는 라이선스면 그 영향이 우리 코드에 미친다.

도구가 전체 트리를 훑게 하고, 결과를 빌드 산출물로 남긴다. 사람이 필요할 때만 훑으면 그 시점의 상태만 알게 되고, 그 사이에 바뀐 것은 드러나지 않는다.

## 배포물에 무엇을 넣나

고지 의무가 있는 라이선스를 쓰면 배포물에 그 내용을 포함해야 한다. 웹 서비스에서는 별도 페이지를 두는 방식이 흔하다. 이 파일도 빌드에서 자동 생성하면 의존성이 바뀔 때마다 따라 갱신된다 — 사람이 관리하면 반드시 낡는다.

## 참고

- SPDX — 라이선스 식별자 표준
- NIST SP 800-161 — 공급망 구성 요소 관리
- OpenChain — 라이선스 준수 절차`,
    diagram: {
      type: 'steps',
      caption: '라이선스 확인 항목',
      steps: [
        { label: '종류 확인', note: '허용 목록' },
        { label: '전파 조건', note: '우리 코드 영향' },
        { label: '고지 의무', note: '표시 필요' },
        { label: '변경 감시', note: '버전마다' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '분류와 처리',
      x: ['조건 명확', '불명'],
      y: ['제한 적음', '많음'],
      cells: ['허용', '차단', '검토', '차단'],
    },
  },
  {
    slug: 'detection-coverage-gap',
    title: '탐지 공백을 찾아내는 점검 순서',
    body: `탐지 규칙이 몇 개인지는 탐지 능력을 말해 주지 않는다. 중요한 것은 일어날 수 있는 일 중 무엇이 규칙에 걸리지 않는가다. 그 공백은 세어 보기 전까지 보이지 않는다.

## 무엇을 기준으로 재나

| 기준 | 내용 |
| --- | --- |
| 공격 기법 분류 | 알려진 기법에 대응되나 |
| 자산 범위 | 모든 시스템이 보이나 |
| 로그 수집 | 볼 수 있는 데이터가 있나 |
| 시간대 | 야간·주말에도 도나 |

로그 수집이 근본이다. 데이터가 없으면 규칙을 아무리 잘 만들어도 걸리지 않는다. 공백은 대개 규칙이 아니라 데이터에서 시작한다.

![공백이 생기는 층](/img/posts/detection-coverage-gap.svg)

## 층별로 확인한다

| 층 | 물어볼 것 |
| --- | --- |
| 데이터 | 이 사건이 로그에 남나 |
| 수집 | 그 로그가 수집소에 오나 |
| 규칙 | 그 로그를 보는 규칙이 있나 |
| 대응 | 걸렸을 때 누가 보나 |

네 층 중 하나라도 비면 탐지는 성립하지 않는다. 규칙만 세는 점검은 위의 두 층을 건너뛴다.

## 표를 만들어 본다

| 기법 유형 | 데이터 | 규칙 | 상태 |
| --- | --- | --- | --- |
| 자격 증명 대입 | 있음 | 있음 | 탐지 |
| 권한 상승 | 있음 | 없음 | 공백 |
| 데이터 반출 | 없음 | 없음 | 맹점 |
| 지속성 확보 | 부분 | 있음 | 부분 |

데이터가 없는 칸이 가장 무겁다. 규칙을 만들 수조차 없으므로 수집부터 손봐야 한다.

![층별 점검](/img/posts/detection-coverage-gap-2.svg)

## 시험으로 확인한다

규칙이 있다는 것과 걸린다는 것은 다르다. 안전한 방법으로 신호를 만들어 실제로 경보가 오는지 본다.

\`\`\`bash
# 인증 실패를 의도적으로 만들어 경보가 오는지 확인한다
for i in $(seq 1 10); do
  curl -s -o /dev/null https://example.com/login -d 'id=probe&pw=wrong'
done
echo '10회 실패 생성 — 경보 도착까지 시간을 잰다'

# 수집소에 그 기록이 도착했는지
sleep 30
curl -s 'localhost:9200/logs-*/_count?q=user:probe' | grep -o '"count":[0-9]*'
\`\`\`

기록이 도착하지 않으면 수집 문제이고, 도착했는데 경보가 없으면 규칙 문제다. 어느 층인지 구분하는 것이 이 시험의 목적이다.

## 공백을 다 메울 필요는 없다

모든 기법에 규칙을 두는 것은 비용이 맞지 않는다. 우리 환경에서 실제로 가능한 경로를 골라 그쪽부터 메운다. 쓰지 않는 기술에 대한 규칙은 소음만 만든다. 공백 목록의 목적은 전부 채우는 것이 아니라, 무엇이 비어 있는지 알고 선택하는 것이다.

## 참고

- MITRE ATT&CK — 기법 분류와 커버리지
- NIST SP 800-137 — 지속적 모니터링
- MITRE — 탐지 성숙도 평가`,
    diagram: {
      type: 'layers',
      caption: '공백이 생기는 층',
      layers: [
        { label: '데이터', note: '로그에 남나' },
        { label: '수집', note: '수집소에 오나' },
        { label: '규칙', note: '보는 규칙이 있나' },
        { label: '대응', note: '누가 보나' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '층별 점검 결과',
      x: ['데이터 있음', '없음'],
      y: ['규칙 있음', '없음'],
      cells: ['탐지', '규칙 불가', '공백', '맹점'],
    },
  },
]
