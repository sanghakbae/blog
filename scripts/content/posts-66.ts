import type { SeedPost } from './types'

/** 650~659 — 공급망과 의존성 */
export const posts66: SeedPost[] = [
  {
    slug: 'dependency-update-policy',
    title: '의존성을 언제 올릴지 정해 두기',
    body: `의존성 업데이트를 미루면 취약점이 쌓이고, 무턱대고 올리면 서비스가 깨진다. 판단을 매번 하면 결국 미루게 되므로 규칙으로 정해 둔다.

## 무엇을 기준으로 나누나

| 구분 | 처리 |
| --- | --- |
| 보안 수정 | 즉시 |
| 패치 버전 | 자동 병합 |
| 마이너 버전 | 주간 검토 |
| 메이저 버전 | 계획 수립 |
| 중단 예정 | 대체 검토 |

보안 수정과 패치 버전은 사람의 판단이 거의 필요 없다. 이 둘만 자동화해도 밀린 의존성의 대부분이 정리된다.

![업데이트 구분과 처리](/img/posts/dependency-update-policy.svg)

## 자동화의 전제

| 항목 | 내용 |
| --- | --- |
| 시험 자동화 | 깨짐 탐지 |
| 잠금 파일 | 재현 가능 |
| 되돌리기 | 빠른 복귀 |
| 배포 분리 | 단계적 반영 |

시험이 없으면 자동 병합은 위험하다. 반대로 시험이 갖춰져 있으면 사람이 보는 것보다 자동이 더 안전하다.

## 밀린 것을 정리하는 순서

| 단계 | 내용 |
| --- | --- |
| 목록화 | 현재 버전 차이 |
| 위험 정렬 | 알려진 취약점 |
| 묶음 처리 | 관련 패키지 |
| 반영 | 단계적으로 |

한 번에 전부 올리면 무엇이 깨졌는지 찾기 어렵다. 관련된 것끼리 묶어 여러 번에 나눈다.

![정리하는 순서](/img/posts/dependency-update-policy-2.svg)

## 지금 얼마나 밀렸는지 본다

\`\`\`bash
# 오래된 패키지와 알려진 취약점
npm outdated 2>/dev/null | head -20
npm audit --audit-level=high 2>/dev/null | tail -20

# 잠금 파일이 최신 상태인지
git diff --stat HEAD -- package-lock.json 2>/dev/null
\`\`\`

목록이 길어도 위험한 것은 몇 개다. 심각도 높은 것만 먼저 처리하면 부담이 줄고, 그 과정에서 자동화의 필요성이 드러난다.

## 버려진 패키지를 찾는다

오래 갱신되지 않은 패키지는 취약점이 발견돼도 고쳐지지 않는다. 마지막 배포일과 관리자 수를 확인해 위험한 것을 골라 두면, 문제가 터지기 전에 대체를 검토할 수 있다. 이런 패키지는 대개 작고 대체가 어렵지 않으며, 미루면 급할 때 바꾸게 된다.

## 참고

- OWASP — 취약한 구성 요소 사용
- NIST SP 800-161 — 공급망 위험 관리
- CIS Controls — 소프트웨어 자산 관리`,
    diagram: {
      type: 'layers',
      caption: '업데이트 구분과 처리',
      layers: [
        { label: '보안 수정', note: '즉시' },
        { label: '패치', note: '자동 병합' },
        { label: '마이너', note: '주간 검토' },
        { label: '메이저', note: '계획 수립' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '정리하는 순서',
      steps: [
        { label: '목록화', note: '버전 차이' },
        { label: '위험 정렬', note: '취약점 기준' },
        { label: '묶음 처리', note: '관련끼리' },
        { label: '단계 반영', note: '나눠서' },
      ],
    },
  },
  {
    slug: 'sbom-practical-use',
    title: '구성 목록을 실제로 쓰는 방법',
    body: `구성 목록은 만들어 두고 쓰지 않으면 파일 한 개다. 취약점이 발표된 날 몇 분 안에 영향 범위를 답할 수 있어야 값어치가 생긴다.

## SBOM을 언제 쓰나

| 상황 | 용도 |
| --- | --- |
| 취약점 발표 | 영향 범위 확인 |
| 라이선스 점검 | 사용 조건 |
| 고객 요구 | 구성 제출 |
| 사고 조사 | 무엇이 돌았나 |
| 변경 추적 | 무엇이 바뀌었나 |

영향 범위 확인이 가장 자주 쓰인다. 널리 쓰이는 패키지에 문제가 생기면 그날 안에 답해야 한다.

![구성 목록의 쓰임](/img/posts/sbom-practical-use.svg)

## 어디서 만드나

| 시점 | 특성 |
| --- | --- |
| 빌드 중 | 가장 정확 |
| 이미지 스캔 | 사후 |
| 저장소 분석 | 선언된 것만 |
| 실행 중 | 실제 로드된 것 |

빌드 중에 생성하는 것이 정확하다. 선언 파일만 보면 전이 의존성이 빠지고, 실제 들어간 것과 다를 수 있다.

## 보관과 조회

| 항목 | 내용 |
| --- | --- |
| 버전별 보관 | 배포마다 |
| 검색 가능 | 패키지명으로 |
| 연결 | 어느 서비스인지 |
| 보관 기간 | 운영 중인 것 |

배포마다 보관하지 않으면 지금 도는 버전의 구성을 알 수 없다. 이미지 식별자와 묶어 두는 방식이 간단하다.

![보관 구조](/img/posts/sbom-practical-use-2.svg)

## 영향 범위를 찾는다

\`\`\`bash
# 특정 패키지가 어느 이미지에 들어 있는지
PKG='log4j-core'
for f in /var/sbom/*.json; do
  grep -q "\\"$PKG\\"" "$f" && echo "포함: $(basename "$f")"
done | head -20

# 버전까지 확인
jq -r ".components[] | select(.name==\\"$PKG\\") | .version" /var/sbom/*.json 2>/dev/null | sort -u
\`\`\`

이 명령이 몇 초 안에 답을 주면 구성 목록이 제 역할을 하는 것이다. 답이 나오지 않으면 목록이 있어도 없는 것과 같다.

## 정확도를 검증한다

생성한 목록이 실제와 맞는지 가끔 확인한다. 컨테이너 안에서 설치된 패키지를 직접 나열해 목록과 대조하면 빠진 것이 드러난다. 언어 런타임이 동적으로 가져오는 구성 요소가 특히 자주 빠지고, 그 부분은 별도 수집이 필요하다. 운영체제 패키지와 언어 패키지를 함께 담는 것도 중요하다. 둘을 따로 관리하면 조회할 때 두 곳을 봐야 하고, 실제 사고에서는 그 구분 없이 하나의 답이 필요하다.

## 참고

- NTIA — SBOM 최소 요소
- NIST SP 800-161 — 공급망 위험 관리
- CycloneDX·SPDX 명세`,
    diagram: {
      type: 'flow',
      caption: '구성 목록의 쓰임',
      steps: [
        { label: '취약점 발표', note: '그날' },
        { label: '목록 조회', note: '몇 초' },
        { label: '영향 범위', note: '어느 서비스' },
        { label: '조치 우선순위', note: '판단 가능' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '보관 구조',
      layers: [
        { label: '배포별 생성', note: '빌드 중' },
        { label: '식별자 연결', note: '이미지' },
        { label: '검색 가능', note: '패키지명' },
        { label: '서비스 매핑', note: '어디에' },
      ],
    },
  },
  {
    slug: 'build-pipeline-trust',
    title: '빌드 시스템이 표적이 되는 이유',
    body: `빌드 시스템은 코드에 접근하고, 비밀값을 쥐고, 결과물을 배포한다. 여기 하나가 뚫리면 코드 리뷰를 통과한 것과 다른 결과물이 배포된다.

## 어디가 약한가

| 지점 | 문제 |
| --- | --- |
| 빌드 스크립트 | 리뷰 없이 변경 |
| 외부 액션 | 태그 참조 |
| 비밀값 노출 | 로그·환경 |
| 캐시 오염 | 공유 캐시 |
| 산출물 변조 | 서명 없음 |

외부에서 가져다 쓰는 빌드 구성 요소를 태그로 참조하면 그 태그가 가리키는 내용이 바뀔 수 있다. 커밋 해시로 고정해야 한다.

![빌드 시스템의 약한 지점](/img/posts/build-pipeline-trust.svg)

## 권한을 좁힌다

| 항목 | 내용 |
| --- | --- |
| 기본 권한 | 읽기만 |
| 작업별 부여 | 필요한 것만 |
| 분기 제한 | 보호 분기만 |
| 외부 기여 | 비밀값 미노출 |

외부 기여자의 요청에서 비밀값이 노출되는 구성이 흔한 취약점이다. 기본 설정을 확인한다.

## 결과물을 고정한다

| 방법 | 내용 |
| --- | --- |
| 이미지 서명 | 출처 증명 |
| 해시 기록 | 무엇이 나왔나 |
| 재현 가능 빌드 | 같은 입력 같은 출력 |
| 배포 시 검증 | 서명 확인 |

서명하고 배포 시점에 검증하면, 다른 경로로 들어온 산출물이 거부된다. 빌드와 배포 사이의 구간이 닫힌다.

![산출물을 고정하는 순서](/img/posts/build-pipeline-trust-2.svg)

## 지금 설정을 본다

\`\`\`bash
# 외부 액션이 태그로 참조되는지 — 해시 고정이 필요하다
grep -rn 'uses:' .github/workflows/ 2>/dev/null \
  | grep -v '@[0-9a-f]\\{40\\}' | head -20

# 기본 토큰 권한 설정
grep -rn 'permissions:' .github/workflows/ 2>/dev/null | head
\`\`\`

권한 설정이 아예 없으면 저장소 기본값이 적용된다. 그 기본값이 쓰기 권한이면 모든 작업이 쓰기 권한으로 돈다.

## 빌드 환경을 일회용으로

빌드마다 새 환경을 쓰면 이전 빌드가 남긴 것이 다음에 영향을 주지 않는다. 오래 살아 있는 빌드 서버는 상태가 쌓이고, 그 상태가 조작되면 조용히 결과물이 바뀐다. 관리형 실행기를 쓰거나 컨테이너로 격리하는 방식 둘 다 이 문제를 해결한다. 빌드 로그도 함께 본다. 비밀값이 로그에 그대로 찍히는 경우가 흔하고, 그 로그는 대개 넓게 공개돼 있다. 가림 처리가 동작하는지 한 번 확인해 두면 오래 열려 있던 구멍 하나가 닫힌다.

## 참고

- SLSA — 공급망 무결성 수준
- NIST SP 800-218 — 안전한 소프트웨어 개발
- OWASP — CI/CD 보안 위험`,
    diagram: {
      type: 'flow',
      caption: '빌드 시스템의 약한 지점',
      steps: [
        { label: '코드 병합', note: '리뷰 통과' },
        { label: '빌드 실행', note: '외부 구성 요소' },
        { label: '산출물 생성', note: '서명 없으면 불명' },
        { label: '배포', note: '검증 없이 통과', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '산출물을 고정하는 순서',
      steps: [
        { label: '해시 기록', note: '무엇이 나왔나' },
        { label: '서명', note: '출처 증명' },
        { label: '저장', note: '변조 방지' },
        { label: '배포 시 검증', note: '거부 가능' },
      ],
    },
  },
  {
    slug: 'package-typosquatting',
    title: '이름이 비슷한 패키지에 속지 않기',
    body: `공개 저장소에는 널리 쓰이는 패키지와 한 글자 다른 이름의 악성 패키지가 올라온다. 설치 명령을 한 번 잘못 치면 그 코드가 빌드 환경에서 실행된다.

## 어떻게 속이나

| 수법 | 예 |
| --- | --- |
| 한 글자 변경 | 철자 오류 |
| 구분자 변경 | 하이픈·밑줄 |
| 범위 이름 제거 | 조직 접두사 |
| 내부 이름 선점 | 사내 패키지명 |
| 인기 패키지 위장 | 설명 복사 |

내부 패키지 이름을 공개 저장소에 선점하는 수법이 특히 위험하다. 설정에 따라 공개 저장소가 우선되면 그쪽이 설치된다.

![속이는 수법](/img/posts/package-typosquatting.svg)

## 언제 실행되나

| 시점 | 설명 |
| --- | --- |
| 설치 스크립트 | 설치 즉시 |
| 첫 임포트 | 코드 실행 시 |
| 빌드 중 | 파이프라인에서 |
| 배포 후 | 운영 환경 |

설치 시점에 실행되는 스크립트가 있어 임포트하지 않아도 동작한다. 실수로 설치했다가 지우는 것으로는 부족하다.

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 내부 저장소 경유 | 승인된 것만 |
| 범위 이름 고정 | 조직 접두사 |
| 잠금 파일 검증 | 변경 리뷰 |
| 설치 스크립트 차단 | 실행 금지 |
| 신규 패키지 검토 | 추가 시 확인 |

설치 스크립트 실행을 막는 설정만으로도 즉시 실행되는 경로가 닫힌다. 일부 패키지가 동작하지 않을 수 있어 예외 목록이 필요하다.

![막는 층](/img/posts/package-typosquatting-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 내부 이름이 공개 저장소에 선점돼 있는지
for p in $(jq -r '.dependencies|keys[]' package.json 2>/dev/null | grep -v '^@'); do
  code=$(curl -s -o /dev/null -w '%{http_code}' "https://registry.npmjs.org/$p")
  echo "$p $code"
done | head -20

# 설치 스크립트를 가진 의존성
npm ls --all --json 2>/dev/null | grep -c 'postinstall'
\`\`\`

내부용으로만 쓰는 이름이 공개 저장소에도 있으면 확인이 필요하다. 우리가 올린 것이 아니라면 선점된 상태다.

## 새 의존성 추가를 절차로

패키지를 추가할 때 이름, 관리자, 배포 이력, 주간 내려받기 수를 한 번 확인하는 것으로 대부분이 걸러진다. 리뷰에서 새 의존성이 눈에 띄게 표시되도록 해 두면 자연스럽게 확인이 들어간다. 추가는 쉽고 제거는 어려우므로 들어올 때 보는 편이 싸다.

## 참고

- OWASP — 의존성 혼동 공격
- NIST SP 800-161 — 공급망 위험 관리
- 패키지 저장소 사업자 보안 문서`,
    diagram: {
      type: 'flow',
      caption: '속이는 수법',
      steps: [
        { label: '유사 이름 등록', note: '한 글자 차이' },
        { label: '오타 설치', note: '사람 실수' },
        { label: '설치 스크립트', note: '즉시 실행', danger: true },
        { label: '비밀값 반출', note: '빌드 환경' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '막는 층',
      layers: [
        { label: '내부 저장소', note: '승인된 것만' },
        { label: '범위 이름', note: '조직 접두사' },
        { label: '스크립트 차단', note: '즉시 실행 방지' },
        { label: '추가 검토', note: '사람 확인' },
      ],
    },
  },
  {
    slug: 'vendor-security-review',
    title: '업체를 고를 때 무엇을 묻나',
    body: `외부 서비스를 도입하면 그 업체의 보안 수준이 우리 수준의 일부가 된다. 계약 전에 확인하지 않으면 나중에 바꾸기 어렵다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 인증 보유 | 심사받은 체계 |
| 데이터 처리 | 위치·암호화 |
| 접근 통제 | 그쪽 직원 |
| 사고 이력 | 과거와 대응 |
| 종료 절차 | 반환·파기 |

사고 이력 자체보다 그때 어떻게 대응했는지가 더 많은 것을 알려 준다. 숨긴 이력이 있는 업체는 다음에도 숨긴다.

![업체 검토 항목](/img/posts/vendor-security-review.svg)

## 협력업체 규모에 맞게 묻는다

| 도입 규모 | 검토 수준 |
| --- | --- |
| 개인정보 다수 | 상세 실사 |
| 내부 데이터 | 문서 검토 |
| 공개 정보만 | 간이 확인 |
| 시험용 | 최소 |

모든 도입에 같은 절차를 요구하면 검토가 병목이 되고 우회가 생긴다. 규모에 따라 나누는 것이 실제로 지켜진다.

## 문서로만 판단하지 않는다

| 방법 | 내용 |
| --- | --- |
| 인증서 유효성 | 범위·기간 |
| 점검 보고서 | 발견 사항 |
| 실제 설정 확인 | 시험 계정 |
| 참조 확인 | 기존 고객 |

인증서의 적용 범위가 우리가 쓸 서비스를 포함하는지 확인한다. 회사는 인증을 받았는데 그 서비스는 범위 밖인 경우가 있다.

![검토 수준의 구분](/img/posts/vendor-security-review-2.svg)

## 도입 후에도 본다

\`\`\`bash
# 사용 중인 외부 서비스의 보안 관련 공지를 정기 확인한다
curl -s https://status.example-vendor.com/api/v2/incidents.json 2>/dev/null \
  | jq -r '.incidents[:5][] | "\\(.created_at) \\(.name)"'
\`\`\`

한 번 검토하고 잊으면 그 업체의 변화가 보이지 않는다. 계약 갱신 시점에 다시 확인하는 절차를 넣어 두면 주기가 생긴다.

## 대체 가능성을 미리 본다

도입 시점에 나중에 바꿀 수 있는지도 함께 본다. 데이터를 표준 형식으로 내보낼 수 있는지, 대체 서비스가 있는지를 확인해 두면 문제가 생겼을 때 선택지가 있다. 바꿀 수 없는 상태가 되면 보안 요구를 관철할 힘도 사라진다.

## 참고

- NIST SP 800-161 — 공급망 위험 관리
- 개인정보보호위원회 — 위탁 가이드라인
- ISO/IEC 27036 — 공급자 관계 보안`,
    diagram: {
      type: 'layers',
      caption: '업체 검토 항목',
      layers: [
        { label: '데이터 처리', note: '위치·암호화' },
        { label: '접근 통제', note: '그쪽 직원' },
        { label: '사고 이력', note: '대응 방식' },
        { label: '종료 절차', note: '반환·파기' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검토 수준의 구분',
      unit: '시간',
      items: [
        { label: '개인정보 다수', value: 40, note: '상세 실사' },
        { label: '내부 데이터', value: 12, note: '문서 검토' },
        { label: '공개 정보', value: 3, note: '간이 확인' },
        { label: '시험용', value: 1, note: '최소' },
      ],
    },
  },
  {
    slug: 'open-source-license-risk',
    title: '오픈소스 라이선스가 만드는 제약',
    body: `보안 검토에서 라이선스는 흔히 빠진다. 그런데 조건을 어기면 법적 분쟁이 되고, 최악의 경우 코드 공개 요구까지 간다.

## 어떤 조건이 있나

| 유형 | 요구 |
| --- | --- |
| 허용형 | 고지만 |
| 약한 전염형 | 수정분 공개 |
| 강한 전염형 | 결합물 공개 |
| 네트워크 조건 | 서비스 제공 시 |
| 상업 제한 | 용도 제한 |

네트워크로 서비스하는 것도 배포로 보는 조건이 있다. 우리가 배포하지 않으니 괜찮다는 판단이 틀릴 수 있다.

![라이선스 유형과 요구](/img/posts/open-source-license-risk.svg)

## 의존성에서 어디가 문제인가

| 상황 | 문제 |
| --- | --- |
| 전이 의존성 | 모르고 포함 |
| 라이선스 변경 | 버전 올리며 |
| 이중 라이선스 | 선택 필요 |
| 표기 누락 | 고지 의무 |
| 코드 복사 | 조각 반입 |

전이 의존성에 딸려 들어오는 경우가 가장 많다. 직접 고른 것만 보면 놓친다.

## 어떻게 관리하나

| 방법 | 내용 |
| --- | --- |
| 자동 검사 | 빌드 중 |
| 허용 목록 | 사전 승인 |
| 차단 목록 | 즉시 거절 |
| 고지 파일 생성 | 자동화 |
| 예외 승인 | 법무 판단 |

허용 목록과 차단 목록을 정해 두면 대부분이 자동으로 처리된다. 애매한 것만 사람에게 온다.

![관리 흐름](/img/posts/open-source-license-risk-2.svg)

## 지금 구성을 본다

\`\`\`bash
# 의존성의 라이선스 분포
npx license-checker --summary 2>/dev/null | head -20

# 주의가 필요한 조건이 있는지
npx license-checker --json 2>/dev/null \
  | jq -r 'to_entries[] | select(.value.licenses | test("GPL|AGPL")) | .key' | head
\`\`\`

전염 조건이 있는 라이선스가 나오면 그것이 어디에 쓰이는지 확인한다. 개발 도구로만 쓰는 것과 배포물에 포함되는 것은 판단이 다르다. 직접 연결해 쓰는지 별도 프로세스로 부르는지도 갈린다. 같은 라이브러리라도 결합 방식에 따라 요구 조건이 달라지므로, 판단이 필요한 경우에는 사용 방식을 함께 적어 법무에 넘긴다.

## 고지 파일을 갖춘다

대부분의 라이선스는 고지만 하면 되는데 그 고지가 빠져 있는 경우가 많다. 빌드 과정에서 고지 파일을 자동 생성해 배포물에 포함하면 이 부분은 해결된다. 수작업으로 관리하면 의존성이 바뀔 때마다 낡는다.

## 참고

- OSI — 승인된 라이선스 목록
- SPDX — 라이선스 식별자
- 한국저작권위원회 — 오픈소스 라이선스 안내`,
    diagram: {
      type: 'layers',
      caption: '라이선스 유형과 요구',
      layers: [
        { label: '허용형', note: '고지만' },
        { label: '약한 전염형', note: '수정분' },
        { label: '강한 전염형', note: '결합물' },
        { label: '네트워크 조건', note: '서비스 제공' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '관리 흐름',
      steps: [
        { label: '자동 검사', note: '빌드 중' },
        { label: '목록 대조', note: '허용·차단' },
        { label: '예외 판단', note: '법무' },
        { label: '고지 생성', note: '자동' },
      ],
    },
  },
  {
    slug: 'internal-package-registry',
    title: '내부 저장소를 두면 달라지는 것',
    body: `외부 공개 저장소에서 직접 가져오는 구성은 편하지만 통제가 없다. 사이에 내부 저장소를 두면 무엇이 들어오는지 보이고 막을 수 있다.

## 패키지 관리에서 무엇이 가능해지나

| 기능 | 내용 |
| --- | --- |
| 목록 파악 | 무엇을 쓰나 |
| 차단 | 특정 패키지 |
| 검사 | 반입 시 스캔 |
| 캐시 | 외부 장애 대비 |
| 내부 배포 | 사내 패키지 |

외부 저장소 장애나 패키지 삭제에도 빌드가 계속된다. 실제로 널리 쓰이던 패키지가 삭제돼 빌드가 멈춘 사례가 있었다.

![내부 저장소의 위치](/img/posts/internal-package-registry.svg)

## 어떻게 구성하나

| 방식 | 특성 |
| --- | --- |
| 프록시 | 요청 시 가져와 보관 |
| 미러 | 전체 복제 |
| 승인형 | 등록된 것만 |
| 혼합 | 언어별 다르게 |

프록시 방식이 부담이 적다. 처음 요청될 때 가져와 보관하므로 전체를 복제할 필요가 없다.

## 반입 시 검사

| 검사 | 내용 |
| --- | --- |
| 알려진 취약점 | 데이터베이스 대조 |
| 라이선스 | 조건 확인 |
| 설치 스크립트 | 존재 여부 |
| 배포 시점 | 너무 새것 |
| 서명 | 있는 경우 |

배포된 지 며칠 안 된 버전을 잠시 보류하는 정책이 악성 패키지 대응에 효과적이다. 대부분의 악성 배포는 며칠 안에 발견되어 내려간다.

![반입 검사 항목](/img/posts/internal-package-registry-2.svg)

## 설정을 확인한다

\`\`\`bash
# 패키지 관리자가 내부 저장소를 보고 있는지
npm config get registry
pip config list 2>/dev/null | grep index-url

# 외부로 직접 나가는 요청이 남아 있는지 — 프록시 로그에서
grep -c 'registry.npmjs.org' /var/log/proxy/access.log 2>/dev/null
\`\`\`

내부 저장소를 두고도 개인 설정으로 외부를 직접 보는 경우가 있다. 네트워크에서 외부 저장소 접근을 막아야 실제로 강제된다.

## 운영 부담을 줄인다

내부 저장소가 멈추면 모든 빌드가 멈춘다. 가용성을 갖추고, 캐시된 내용의 보관 기간을 넉넉히 잡고, 복구 절차를 준비한다. 도입 효과가 큰 만큼 의존도도 커지므로 운영 계획을 함께 세워야 한다. 저장 공간도 미리 잡는다. 프록시 방식이라도 시간이 지나면 캐시가 상당히 커지고, 공간이 차면 새 패키지를 가져오지 못해 빌드가 멈춘다. 오래 쓰이지 않은 것을 정리하는 규칙을 처음부터 넣어 둔다.

## 참고

- NIST SP 800-161 — 공급망 위험 관리
- OWASP — 의존성 관리
- 패키지 저장소 도구 문서`,
    diagram: {
      type: 'flow',
      caption: '내부 저장소의 위치',
      steps: [
        { label: '개발자 요청', note: '설치' },
        { label: '내부 저장소', note: '검사·보관' },
        { label: '외부 저장소', note: '필요 시만' },
        { label: '빌드', note: '외부 장애 무관' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '반입 검사 항목',
      steps: [
        { label: '취약점 대조', note: '알려진 것' },
        { label: '라이선스', note: '조건' },
        { label: '설치 스크립트', note: '존재 여부' },
        { label: '배포 시점', note: '너무 새것 보류' },
      ],
    },
  },
  {
    slug: 'code-signing-basics',
    title: '코드 서명이 실제로 보장하는 것',
    body: `서명은 그 코드가 누구에게서 나왔고 그 뒤로 바뀌지 않았음을 보인다. 그 코드가 안전하다는 뜻은 아니다. 무엇을 보장하고 무엇을 보장하지 않는지 구분해야 한다.

## 무엇을 보장하나

| 항목 | 보장 |
| --- | --- |
| 출처 | 누가 만들었나 |
| 무결성 | 바뀌지 않았나 |
| 시점 | 언제 서명했나 |
| 안전성 | 보장 안 함 |
| 품질 | 보장 안 함 |

서명된 악성 코드도 존재한다. 서명은 책임 소재를 명확히 하는 장치이지 안전 증명이 아니다.

![서명이 보장하는 범위](/img/posts/code-signing-basics.svg)

## 서명 키를 지키는 것이 전부다

| 위험 | 결과 |
| --- | --- |
| 키 유출 | 임의 서명 가능 |
| 빌드 서버 보관 | 노출 면적 |
| 공유 사용 | 추적 불가 |
| 회수 지연 | 피해 확대 |

키가 유출되면 공격자가 우리 이름으로 서명한다. 하드웨어 모듈이나 관리형 서명 서비스로 키를 꺼낼 수 없게 두는 것이 기본이다.

## 검증이 있어야 의미가 있다

| 지점 | 검증 |
| --- | --- |
| 배포 시 | 서명 확인 |
| 실행 전 | 정책 적용 |
| 갱신 시 | 같은 서명자 |
| 실패 시 | 거부 |

서명만 하고 어디서도 검증하지 않으면 아무 효과가 없다. 검증하는 지점을 정하는 것이 서명하는 것보다 중요하다.

![서명과 검증의 흐름](/img/posts/code-signing-basics-2.svg)

## 검증을 확인한다

\`\`\`bash
# 컨테이너 이미지 서명 확인
cosign verify --key cosign.pub registry.example.com/app:1.2.3 2>&1 | head -5

# 서명되지 않은 이미지가 배포되는지 — 정책 확인
kubectl get validatingwebhookconfigurations 2>/dev/null | grep -i sign
\`\`\`

검증 정책이 없으면 서명하지 않은 이미지도 그대로 실행된다. 정책을 켜기 전에 기존 이미지의 서명 상태를 먼저 정리한다.

## 시각 확인을 붙인다

서명 시점을 신뢰할 수 있는 기관이 확인해 주면, 나중에 키가 만료되거나 폐기돼도 그 이전 서명은 유효하게 남는다. 이것이 없으면 키를 바꿀 때마다 과거 산출물을 전부 다시 서명해야 한다. 처음 구성할 때 넣어 두는 편이 훨씬 싸다.

## 참고

- SLSA — 출처 증명
- NIST SP 800-218 — 안전한 개발 관행
- Sigstore 프로젝트 문서`,
    diagram: {
      type: 'matrix',
      caption: '서명이 보장하는 범위',
      x: ['보장함', '보장 안 함'],
      y: ['출처·무결성', '안전성·품질'],
      cells: ['누가·안 바뀜', '해당 없음', '해당 없음', '별도 검토 필요'],
    },
    diagram2: {
      type: 'flow',
      caption: '서명과 검증의 흐름',
      steps: [
        { label: '빌드', note: '산출물' },
        { label: '서명', note: '키 보호' },
        { label: '배포', note: '저장소' },
        { label: '검증', note: '없으면 무의미' },
      ],
    },
  },
  {
    slug: 'artifact-repository-access',
    title: '산출물 저장소의 권한 나누기',
    body: `빌드 결과물이 모이는 저장소는 배포의 출발점이다. 여기에 쓸 수 있으면 운영에 코드를 넣을 수 있는 것과 같다.

## 누가 무엇을 할 수 있나

| 역할 | 권한 |
| --- | --- |
| 빌드 시스템 | 쓰기 |
| 배포 시스템 | 읽기 |
| 개발자 | 읽기 |
| 관리자 | 설정 |
| 외부 | 없음 |

사람에게 쓰기 권한을 주지 않는 것이 핵심이다. 산출물은 빌드를 거쳐서만 들어와야 코드와 결과물이 연결된다.

![저장소 권한 구분](/img/posts/artifact-repository-access.svg)

## 덮어쓰기를 막는다

| 항목 | 설정 |
| --- | --- |
| 같은 태그 재배포 | 금지 |
| 삭제 | 제한 |
| 변경 불가 태그 | 켜기 |
| 보관 기간 | 운영 중인 것 |

같은 태그로 다른 내용을 올릴 수 있으면 검증한 것과 배포되는 것이 달라진다. 변경 불가 설정을 켜는 것으로 닫힌다.

## 파이프라인 단계로 구분해서 둔다

| 저장소 | 용도 |
| --- | --- |
| 개발 | 자유로운 실험 |
| 후보 | 검증 대상 |
| 운영 | 승인된 것만 |

단계를 나누면 운영 저장소에 들어가는 시점이 통제 지점이 된다. 승격 과정에서 검증을 건다.

![단계별 승격](/img/posts/artifact-repository-access-2.svg)

## 지금 권한을 본다

\`\`\`bash
# 컨테이너 저장소에 쓰기 권한을 가진 주체
aws ecr get-repository-policy --repository-name app 2>/dev/null \
  | jq -r '.policyText' | jq -r '.Statement[] | "\\(.Principal) \\(.Action)"'

# 태그 변경 불가 설정
aws ecr describe-repositories \
  --query 'repositories[].[repositoryName,imageTagMutability]' --output text 2>/dev/null
\`\`\`

변경 가능으로 되어 있으면 같은 태그가 다른 내용을 가리킬 수 있다. 운영 저장소부터 바꾼다.

## 기록을 남긴다

누가 언제 무엇을 올렸는지 남아야 사고 때 역추적이 된다. 저장소 접근 기록을 감사 로그로 모으고, 사람 계정으로 올라온 산출물이 있으면 확인 대상으로 삼는다. 자동화된 경로만 쓰이고 있다면 그 기록은 조용하고, 그 조용함 자체가 건강한 신호다. 급한 상황에서 사람이 직접 올려야 하는 경우를 대비해 예외 절차를 만들어 둔다. 절차가 없으면 급할 때 권한을 임시로 열고 그대로 두게 된다. 승인과 기한이 붙은 예외 경로가 오히려 안전하다.

## 참고

- SLSA — 산출물 관리
- NIST SP 800-218 — 안전한 개발 관행
- 컨테이너 저장소 사업자 권한 문서`,
    diagram: {
      type: 'layers',
      caption: '저장소 권한 구분',
      layers: [
        { label: '빌드 시스템', note: '쓰기' },
        { label: '배포 시스템', note: '읽기' },
        { label: '개발자', note: '읽기' },
        { label: '사람의 쓰기', note: '금지' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '단계별 승격',
      steps: [
        { label: '개발', note: '자유' },
        { label: '후보', note: '검증 대상' },
        { label: '승격 검증', note: '통제 지점' },
        { label: '운영', note: '승인된 것만' },
      ],
    },
  },
  {
    slug: 'upstream-incident-response',
    title: '쓰던 도구에서 사고가 났을 때',
    body: `우리가 쓰는 라이브러리나 서비스에서 침해가 발표되면, 우리 시스템은 멀쩡해도 영향권에 들어간다. 그때 무엇을 할지 미리 정해 두면 그날이 덜 혼란스럽다.

## 무엇을 먼저 확인하나

| 항목 | 내용 |
| --- | --- |
| 사용 여부 | 실제로 쓰나 |
| 버전 | 영향 범위인가 |
| 노출 조건 | 우리 설정에서 |
| 자격 증명 | 공유했나 |
| 대체 여부 | 임시 조치 |

쓰고 있는지부터 확인하는 데 시간이 걸리면 그날의 대부분이 거기서 간다. 구성 목록이 있으면 몇 분이다.

![상위 사고 대응 순서](/img/posts/upstream-incident-response.svg)

## 자격 증명을 본다

| 대상 | 조치 |
| --- | --- |
| 그 서비스 토큰 | 회전 |
| 연동 키 | 회전 |
| 웹훅 비밀 | 회전 |
| 공유 계정 | 비밀번호 변경 |

침해된 업체에 우리 자격 증명이 보관돼 있었다면 그것부터 바꾼다. 업체 발표를 기다리지 않아도 되는 조치다.

## 임시 조치의 선택지

| 방법 | 내용 |
| --- | --- |
| 기능 끄기 | 해당 부분만 |
| 연동 차단 | 통신 중단 |
| 우회 구현 | 임시 대체 |
| 버전 고정 | 안전한 것으로 |

전체를 멈추는 대신 해당 기능만 끄는 선택지를 미리 확인해 두면 영향이 작다.

![임시 조치의 범위](/img/posts/upstream-incident-response-2.svg)

## 영향을 확인한다

\`\`\`bash
# 해당 패키지가 어느 버전으로 들어 있는지
npm ls affected-package 2>/dev/null | head -20

# 그 서비스로 나간 통신 기록 — 언제부터 언제까지
grep 'vendor-domain.com' /var/log/proxy/access.log 2>/dev/null | wc -l
\`\`\`

통신 기록이 있으면 노출 기간을 특정할 수 있다. 기록이 없으면 보수적으로 가장 긴 기간을 가정해야 한다.

## 발표를 기다리지 않는다

업체 발표는 대개 늦고 초기 내용은 축소돼 있다. 우리가 통제할 수 있는 조치, 곧 자격 증명 회전과 통신 차단은 먼저 한다. 나중에 영향이 없던 것으로 밝혀져도 잃은 것은 약간의 수고뿐이다. 무엇을 확인했고 무엇을 조치했는지 짧게 남겨 둔다. 같은 업체에서 다시 사고가 났을 때 그 기록이 출발점이 되고, 고객이나 감독기관이 물었을 때도 바로 답할 수 있다.

## 참고

- NIST SP 800-161 — 공급망 사고 대응
- NIST SP 800-61 — 사고 대응 안내
- 한국인터넷진흥원 — 취약점 대응 권고`,
    diagram: {
      type: 'steps',
      caption: '상위 사고 대응 순서',
      steps: [
        { label: '사용 여부', note: '구성 목록' },
        { label: '버전 확인', note: '영향 범위' },
        { label: '자격 증명 회전', note: '먼저 가능' },
        { label: '임시 조치', note: '기능 단위' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '임시 조치의 범위',
      unit: '점',
      items: [
        { label: '기능만 끄기', value: 20, note: '영향 최소' },
        { label: '연동 차단', value: 45, note: '부분 중단' },
        { label: '버전 고정', value: 30, note: '재배포 필요' },
        { label: '전체 중단', value: 100, note: '마지막' },
      ],
    },
  },
]
