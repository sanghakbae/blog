import type { SeedPost } from './types'

/** 121~130 — 개인정보 처리와 인프라 운영 */
export const posts13: SeedPost[] = [
  {
    slug: 'data-subject-request',
    title: '정보주체 요구 처리 절차 설계',
    body: `열람·정정·삭제·처리정지 요구는 법이 정한 기한 안에 답해야 하고, 답변에는 우리가 그 사람의 데이터를 어디에 얼마나 가지고 있는지가 들어간다. 이때 드러나는 문제는 대개 개인정보 자체가 아니라 데이터가 어디에 흩어져 있는지 아무도 모른다는 사실이다. 요구 처리 절차를 만드는 일은 사실상 데이터 지도를 만드는 일이다.

## 왜 기한을 넘기게 되는가?

요구가 들어오면 담당자가 시스템마다 사람을 찾아 물어본다. 로그, 백업, 데이터 웨어하우스, 협력업체로 넘긴 사본까지 확인하려면 며칠이 걸리고, 한 곳이라도 응답이 늦으면 기한을 넘긴다. 요구가 드물다는 이유로 절차를 만들지 않으면 매번 처음부터 다시 한다.

![요구 접수부터 회신까지의 처리 경로](/img/posts/data-subject-request.svg)

## 요구 유형별 처리

| 유형 | 처리 | 유의점 |
| --- | --- | --- |
| 열람 | 보유 항목과 이용 내역 제공 | 타인 정보가 섞이지 않게 |
| 정정 | 원본과 파생 사본 모두 | 파생 데이터 갱신 누락 주의 |
| 삭제 | 파기 후 결과 통지 | 법정 보존 의무와 충돌 확인 |
| 처리정지 | 처리 중단, 보관은 가능 | 삭제와 구분 |
| 동의 철회 | 해당 목적 처리 중단 | 다른 목적 처리와 분리 |
| 제3자 제공 내역 | 제공 이력 제공 | 제공 기록 상시 축적 필요 |

## 삭제할 수 없는 경우를 미리 정리한다

전자상거래 기록이나 회계 자료처럼 법이 보존을 요구하는 항목은 삭제 요구 대상에서 빠진다. 그러나 "일부는 못 지운다"를 즉석에서 판단하면 답변이 흔들린다. 항목별로 근거 법령과 보존 기간을 표로 만들어 두고 그 표를 근거로 회신해야 한다.

## 사람이 아니라 시스템이 처리하게

요구 처리 창구를 만들면 요구 건수는 늘어난다. 첫 몇 건은 수동으로 처리하되, 반복되는 조회를 스크립트로 만들어 두면 그다음부터는 사람이 검토만 한다.

\`\`\`
1. 본인 확인 — 과도한 정보를 추가로 요구하지 않는다
2. 데이터 지도 기준으로 보유 시스템 조회
3. 처리 결과 취합과 법정 보존 항목 표시
4. 기한 내 회신, 거부 시 사유와 이의 절차 안내
5. 처리 이력 보관 — 감사 대응 증적
\`\`\`

## 바로 확인하기

데이터 지도가 있으면 조회가 스크립트 한 번으로 끝난다. 없다면 만드는 것이 첫 작업이다.

\`\`\`bash
# 시스템별 보유 여부 조회를 한 번에 — 결과를 회신 문서로 취합한다
SUBJECT="user-38214"
for sys in users orders payments support marketing analytics; do
  printf '%-12s ' "$sys"
  ./bin/lookup --system "$sys" --subject "$SUBJECT" --count-only
done
\`\`\`

삭제 요구는 파생 데이터까지 확인해야 완료된다.

\`\`\`sql
-- 원본을 지운 뒤 파생 테이블에 남았는지 확인
SELECT 'orders' AS t, count(*) FROM orders WHERE user_id = $1
UNION ALL SELECT 'events', count(*) FROM events WHERE user_id = $1
UNION ALL SELECT 'mail_log', count(*) FROM mail_log WHERE user_id = $1;
\`\`\`

## 참고

- 개인정보보호법 제35조~제37조, 정보주체의 권리
- 개인정보 처리방침 작성 지침
- ISO/IEC 27701, 정보주체 요구 처리 통제`,
    diagram: {
      type: 'steps',
      caption: '정보주체 요구 처리 절차',
      steps: [
        { label: '접수와 본인 확인', note: '추가 정보 최소 수집' },
        { label: '보유 현황 조회', note: '데이터 지도 기준' },
        { label: '처리와 예외 판정', note: '법정 보존 항목 표시' },
        { label: '기한 내 회신', note: '거부 시 사유와 이의 절차' },
      ],
    },
  },
  {
    slug: 'cross-border-transfer',
    title: '개인정보 국외 이전 점검 항목',
    body: `해외 클라우드와 외부 서비스를 쓰는 순간 국외 이전이 발생한다. 서버 지역을 국내로 골랐다고 끝나지 않는다. 지원 조직이 해외에서 접근하거나 로그가 해외 지역으로 복제되면 그 자체가 이전이다. 점검의 출발점은 계약서가 아니라 데이터가 실제로 어디를 지나가는지 그리는 일이다.

## 무엇이 이전에 해당하는가?

- 해외 지역 저장소에 데이터를 보관
- 해외 지원 조직이 원격으로 조회
- 로그·백업이 해외 지역으로 복제
- 해외 사업자의 분석·광고 스크립트로 전송
- 모델 학습이나 추론을 위해 외부 사업자에 전달
- 재위탁으로 다른 국가의 사업자가 처리

접근만으로도 이전이 성립한다는 점이 자주 누락된다.

![데이터가 국경을 넘는 다섯 가지 경로](/img/posts/cross-border-transfer.svg)

## 점검 항목

| 항목 | 확인 |
| --- | --- |
| 이전 항목 | 어떤 개인정보 항목이 넘어가는가 |
| 이전 국가 | 저장 지역과 접근 지역 모두 |
| 이전 목적 | 계약 이행에 필요한 범위인가 |
| 보유 기간 | 이전받는 자의 보유·파기 기준 |
| 근거 | 동의, 계약, 인증 등 어떤 근거인가 |
| 재위탁 | 재위탁 여부와 승인 절차 |
| 공개 | 처리방침에 이전 사실 기재 |

## 처리방침이 실제와 맞아야 한다

가장 흔한 지적은 처리방침에 적힌 이전 대상과 실제 사용 중인 서비스 목록이 다른 것이다. 서비스가 추가될 때 처리방침을 함께 갱신하는 절차가 없으면 반드시 벌어진다. 새 외부 서비스 도입을 검토 절차에 넣는 것이 근본 대책이다.

## 지역 고정은 설정으로 확인한다

지역을 지정했더라도 일부 기능은 다른 지역을 쓴다. 백업 복제 대상, 로그 수집 지역, 관리 콘솔의 접근 위치, 부가 기능의 처리 지역을 각각 확인해야 한다.

\`\`\`
확인할 곳
  저장소 지역 설정과 복제 대상 지역
  백업 보관 지역
  로그·모니터링 데이터의 처리 지역
  지원 요청 시 접근 주체의 소속 국가
  부가 기능(번역·분석·알림)의 처리 지역
\`\`\`

## 바로 확인하기

우리 웹 페이지가 어느 나라의 서버로 데이터를 보내는지부터 본다. 브라우저에서 나가는 요청이 실제 이전 경로다.

\`\`\`bash
# 페이지가 참조하는 외부 도메인 목록 — 처리방침과 대조한다
curl -s https://www.example.com | grep -oE 'https://[a-z0-9.-]+' \\
  | sed 's|https://||' | sort -u | grep -v 'example\\.com$'

# 각 도메인의 응답 지역 추정
for d in $(cat third-parties.txt); do
  printf '%-32s ' "$d"
  curl -sI "https://$d" | grep -iE 'server|cf-ray|x-amz-cf-pop' | tr -d '\\r' | paste -sd' ' -
done
\`\`\`

## 참고

- 개인정보보호법 제28조의8, 국외 이전
- 개인정보 국외 이전 운영 고시
- ISO/IEC 27018, 클라우드 개인정보 통제`,
    diagram: {
      type: 'matrix',
      caption: '저장 지역과 접근 지역',
      x: ['국내 접근', '해외 접근'],
      y: ['국내 저장', '해외 저장'],
      cells: ['이전 없음', '접근으로 이전 발생', '이전 해당', '이전 해당 · 근거 필수'],
    },
  },
  {
    slug: 'consent-management',
    title: '동의 획득 설계와 철회 처리',
    body: `동의는 받는 순간보다 관리하는 기간이 길다. 언제 어떤 문안으로 무엇에 동의했는지 남아 있지 않으면, 나중에 그 처리가 적법했는지 증명할 수 없다. 그리고 철회는 동의만큼 쉬워야 한다. 가입은 두 번 눌러 끝나는데 철회는 전화를 해야 한다면 그 동의는 유효하지 않게 볼 수 있다.

## 어떤 동의가 무효가 되는가?

- 필수와 선택을 구분하지 않고 한 번에 받는 경우
- 선택 동의를 거부하면 서비스 이용이 막히는 경우
- 기본값으로 체크돼 있는 경우
- 문안이 뭉뚱그려져 있어 무엇에 동의하는지 알 수 없는 경우
- 목적이 추가됐는데 예전 동의를 근거로 쓰는 경우

![동의 항목 분리와 이력 보관 구조](/img/posts/consent-management.svg)

## 동의 이력에 남길 항목

| 항목 | 이유 |
| --- | --- |
| 동의 시각 | 적법 처리 기간 판단 |
| 동의 항목 | 목적별로 분리해 기록 |
| 문안 버전 | 그때 무엇을 보여줬는지 |
| 획득 경로 | 화면, 앱, 서면 |
| 처리 근거 | 동의·계약·법령 중 무엇인지 |
| 철회 시각 | 처리 중단 시점 확인 |

문안 버전이 특히 자주 빠진다. 문안을 고치면 이전 동의와 이후 동의가 다른 내용이 되므로 버전 없이는 무엇에 동의했는지 재구성할 수 없다.

## 동의가 근거가 아닌 경우를 구분한다

계약 이행에 필요한 처리는 동의를 받을 대상이 아니다. 필수라고 표시해 동의를 받으면 오히려 동의의 자유로움을 훼손한다. 처리 목적마다 근거를 먼저 정하고, 동의가 필요한 것만 동의로 받는다.

\`\`\`
계약 이행   주문 처리, 배송, 결제  →  동의 대상 아님
법령 의무   전자상거래 기록 보존   →  동의 대상 아님
동의        마케팅 수신, 맞춤 광고 →  항목별 개별 동의
정당한 이익 부정 이용 방지        →  범위와 영향 검토 필요
\`\`\`

## 철회 경로를 동의 경로와 같은 깊이로

화면에서 두 번 눌러 동의했다면 두 번 눌러 철회할 수 있어야 한다. 철회 후에는 해당 목적의 처리를 즉시 멈추고, 이미 전달한 제3자에게도 철회를 전파해야 한다.

## 바로 확인하기

동의 이력과 실제 처리가 일치하는지 대조한다. 어긋나면 그 자체가 위반이다.

\`\`\`sql
-- 마케팅 동의가 없거나 철회된 사용자에게 발송된 이력
SELECT m.user_id, m.sent_at
  FROM mail_log m
  LEFT JOIN consents c
    ON c.user_id = m.user_id AND c.purpose = 'marketing' AND c.revoked_at IS NULL
 WHERE m.channel = 'marketing'
   AND (c.user_id IS NULL OR m.sent_at > c.revoked_at)
 ORDER BY m.sent_at DESC
 LIMIT 50;
\`\`\`

동의 화면 자체도 점검 대상이다.

\`\`\`
체크리스트
  선택 항목이 기본 해제 상태인가
  선택 거부 시에도 가입이 완료되는가
  항목마다 목적과 보유 기간이 표시되는가
  철회 경로가 설정 화면 한 곳에 모여 있는가
\`\`\`

## 참고

- 개인정보보호법 제15조·제22조, 동의 요건
- 개인정보 수집·이용 동의 문안 작성 지침
- ISO/IEC 27701, 동의 관리 통제`,
    diagram: {
      type: 'flow',
      caption: '동의의 수명 주기',
      steps: [
        { label: '근거 판정', note: '동의가 필요한 목적만' },
        { label: '항목별 동의', note: '필수와 선택 분리' },
        { label: '이력 보관', note: '시각·항목·문안 버전' },
        { label: '철회 전파', note: '제3자까지 중단' },
      ],
    },
  },
  {
    slug: 'wifi-segmentation',
    title: '사무실 무선 네트워크 구간 분리',
    body: `사무실 무선 네트워크는 조직에서 가장 통제가 느슨한 구간이다. 회의실에 앉은 누구나 접속하고, 개인 기기와 업무 노트북과 프린터와 IoT 기기가 같은 대역에 섞인다. 이 상태에서 침해가 하나 생기면 대역 안 전체가 후속 대상이 된다. 대책은 복잡한 장비가 아니라 목적별 구간 분리와 단말 간 통신 차단이다.

## 무엇이 문제인가?

- 방문자와 직원이 같은 대역을 쓴다
- 공유 비밀번호를 쓰고 퇴사자에게도 남는다
- 단말 간 통신이 열려 있어 옆 기기를 바로 볼 수 있다
- 프린터·카메라·회의 장비가 업무망에 함께 있다
- 무선 접속만으로 내부 시스템에 도달한다

![목적별 무선 구간 분리](/img/posts/wifi-segmentation.svg)

## 구간 설계

| 구간 | 인증 | 접근 범위 |
| --- | --- | --- |
| 업무 | 기업용 인증서·계정 기반 | 업무 시스템, 인가 후 접근 |
| 방문자 | 격리된 접속 | 인터넷만 |
| 기기 | 사전 등록 기반 | 지정 서버만 |
| 개인 기기 | 계정 기반 | 인터넷과 지정 서비스 |
| 운영·관리 | 유선 또는 별도 인증 | 관리 대역 |

## 공유 비밀번호를 없앤다

무선 접속에 공유 비밀번호를 쓰면 누가 접속했는지 알 수 없고 회수도 불가능하다. 계정 기반 인증으로 바꾸면 접속 이력이 사람 단위로 남고, 퇴사 처리로 접속이 함께 끊긴다. 이 하나만 바꿔도 통제 수준이 달라진다.

## 단말 간 통신을 막는다

같은 구간 안에서 단말끼리 통신할 필요가 있는 경우는 드물다. 단말 격리를 켜면 감염된 노트북이 옆 기기를 탐색하는 경로가 사라진다. 프린터처럼 예외가 필요한 경우는 예외 규칙으로 좁혀 허용한다.

\`\`\`
업무 구간   단말 격리 켬, 프린터 서버만 예외
방문자      단말 격리 켬, 내부 대역 전체 차단, 대역폭 제한
기기 구간   지정 목적지만 허용, 인터넷 차단
관리 대역   무선 접속 금지
\`\`\`

## 무선을 신뢰 경계로 쓰지 않는다

무선에 접속했다는 사실만으로 내부 시스템에 접근할 수 있으면, 무선 인증이 뚫리는 순간 전부 열린다. 접속 위치가 아니라 사용자와 기기 상태로 접근을 판단하는 방식으로 옮겨야 한다.

## 바로 확인하기

방문자 구간에서 내부 대역이 보이는지 직접 확인하는 것이 가장 빠르다.

\`\`\`bash
# 방문자 구간에 접속한 상태에서 실행
ip route | head -5

# 내부 대역이 응답하면 분리가 안 된 것이다
for net in 10.0.0.1 10.10.0.1 192.168.10.1 172.16.0.1; do
  printf '%-16s ' "$net"
  ping -c1 -W1 "$net" >/dev/null 2>&1 && echo '도달 — 분리 실패' || echo '차단'
done

# 같은 구간의 다른 단말이 보이는지 (단말 격리 확인)
arp -a | head -20
\`\`\`

## 참고

- NIST SP 800-153, 무선 랜 보안 지침
- IEEE 802.1X 인증 규격
- CIS Controls v8, 12 네트워크 인프라 관리`,
    diagram: {
      type: 'layers',
      caption: '무선 접속 통제 계층',
      layers: [
        { label: '목적별 구간 분리', note: '업무·방문자·기기·관리' },
        { label: '계정 기반 인증', note: '공유 비밀번호 폐지' },
        { label: '단말 간 통신 차단', note: '횡적 탐색 제거' },
        { label: '접근은 신원으로', note: '위치를 신뢰 근거로 쓰지 않음' },
      ],
    },
  },
  {
    slug: 'egress-filtering',
    title: '아웃바운드 통제와 SSRF 차단',
    body: `방화벽 정책은 들어오는 트래픽에 집중하고 나가는 트래픽은 전부 허용된 경우가 많다. 그런데 침해의 다음 단계는 대부분 나가는 방향이다. 명령 수신, 데이터 유출, 추가 도구 내려받기가 모두 아웃바운드다. 서버 측 요청 위조로 내부 자원을 긁는 것도 결국 서버가 스스로 내보내는 요청이다.

## 어떤 공격이 아웃바운드 통제로 막히는가?

| 공격 단계 | 아웃바운드 차단 효과 |
| --- | --- |
| 명령 제어 채널 수립 | 큼. 목적지 제한으로 차단 |
| 추가 도구 내려받기 | 큼 |
| 데이터 대량 유출 | 큼. 목적지와 용량 제한 |
| SSRF 로 메타데이터 접근 | 큼. 내부 대역 차단 |
| 암호화폐 채굴 | 큼 |
| 초기 침입 | 없음. 인바운드 문제 |

![기본 차단 후 필요한 목적지만 허용](/img/posts/egress-filtering.svg)

## 기본 차단으로 시작하기 어렵다면

운영 중인 시스템에 갑자기 기본 차단을 걸면 장애가 난다. 순서를 나눠 접근한다.

\`\`\`
1. 로그만 수집 — 어디로 나가는지 목록화 (2~4주)
2. 목록 정리 — 필요한 목적지와 정체 불명 목적지 분류
3. 신규 자산부터 기본 차단 적용
4. 기존 자산은 위험도 순으로 전환
5. 내부 대역과 메타데이터 주소는 즉시 차단
\`\`\`

내부 대역 차단은 장애 위험이 낮고 SSRF 방어 효과가 즉각적이므로 가장 먼저 할 수 있다.

## SSRF 는 애플리케이션과 네트워크 양쪽에서 막는다

애플리케이션에서 주소를 검사해도 DNS 재바인딩과 리다이렉트로 우회된다. 네트워크에서 내부 대역으로 나가는 요청을 차단하면 우회 여부와 무관하게 막힌다. 두 층을 함께 두는 이유다.

- 애플리케이션 — 해석된 IP 를 검사, 리다이렉트 추적 금지, 프로토콜 제한
- 네트워크 — 사설 대역과 메타데이터 주소로 나가는 요청 차단
- 프록시 — 외부 호출을 전용 프록시로만 허용

## 도메인 기반 허용이 필요한 이유

목적지 IP 는 자주 바뀐다. IP 목록으로 관리하면 곧 어긋난다. 프록시를 두고 도메인 기준으로 허용하면 관리가 가능해지고, 허용된 도메인으로 나간 요청의 이력도 남는다.

## 바로 확인하기

서버에서 나가는 방향이 실제로 막혀 있는지 확인한다. 메타데이터 주소가 최우선이다.

\`\`\`bash
# 메타데이터 주소 — 응답이 오면 SSRF 시 즉시 자격 증명 유출 경로가 된다
curl -s -m 3 http://169.254.169.254/latest/meta-data/ && echo '도달 — 차단 필요'

# 임의 외부 목적지로 나가는지
for target in 1.1.1.1:53 example.com:443 example.com:8443 example.com:22; do
  host=\${target%%:*}; port=\${target##*:}
  printf '%-24s ' "$target"
  nc -z -w2 "$host" "$port" >/dev/null 2>&1 && echo '허용' || echo '차단'
done
\`\`\`

애플리케이션 쪽 검사는 해석된 주소를 기준으로 한다.

\`\`\`ts
import { lookup } from 'node:dns/promises'
import ipaddr from 'ipaddr.js'

async function assertPublicHost(url: string) {
  const u = new URL(url)
  if (!['http:', 'https:'].includes(u.protocol)) throw new Error('protocol')
  const { address } = await lookup(u.hostname)
  const range = ipaddr.parse(address).range()
  if (range !== 'unicast') throw new Error('private address: ' + range)
  return address // 이 주소로 직접 연결한다 — 재조회 사이의 변경을 막는다
}
\`\`\`

## 참고

- OWASP Cheat Sheet — Server Side Request Forgery Prevention
- MITRE ATT&CK — Command and Control, Exfiltration
- CIS Controls v8, 13 네트워크 모니터링과 방어`,
    diagram: {
      type: 'steps',
      caption: '아웃바운드 통제 도입 순서',
      steps: [
        { label: '내부 대역 즉시 차단', note: '메타데이터 주소 포함' },
        { label: '나가는 목적지 수집', note: '로그만 켜고 관찰' },
        { label: '프록시 경유 전환', note: '도메인 기준 허용' },
        { label: '기본 차단 적용', note: '신규 자산부터' },
      ],
    },
  },
  {
    slug: 'waf-tuning',
    title: 'WAF 규칙 튜닝과 오탐 줄이기',
    body: `WAF 를 켜자마자 정상 요청이 막히면 곧 차단을 끄고 탐지만 남기게 된다. 그러면 도입한 의미가 사라진다. 실패하지 않는 순서는 탐지 모드로 며칠 관찰하고, 오탐이 많은 규칙을 경로 단위로 예외 처리한 뒤, 위험이 큰 규칙군부터 차단으로 올리는 것이다. WAF 는 취약점을 고치는 도구가 아니라 고치는 동안 시간을 벌어주는 도구다.

## 탐지 규칙은 왜 오탐을 내는가?

- 본문에 코드나 SQL 문자열이 정상적으로 들어가는 기능
- 마크다운·HTML 편집기 입력
- 파일 업로드의 이진 데이터
- 긴 JSON 본문과 base64 문자열
- 관리자 화면의 정규식·쿼리 입력

공통점은 "정상 입력이 공격 문자열과 구별되지 않는" 기능이라는 점이다. 이런 경로는 탐지 규칙을 끄는 것이 아니라 경로를 좁혀 예외로 둔다.

![탐지에서 차단으로 올리는 단계](/img/posts/waf-tuning.svg)

## 단계별 전환

| 단계 | 설정 | 기간 |
| --- | --- | --- |
| 관찰 | 전체 탐지 규칙을 기록 전용으로 | 1~2주 |
| 정리 | 오탐이 많은 탐지 규칙 예외 처리 | 1주 |
| 부분 차단 | 인젝션·경로 탐색 규칙군 차단 | 2주 |
| 확대 | 나머지 규칙군 차단 | 점진 |
| 유지 | 배포마다 오탐 확인 | 상시 |

## 예외는 좁게, 기한과 함께

규칙 전체를 끄면 그 규칙이 막던 모든 경로가 열린다. 예외는 경로·파라미터·규칙 번호 세 가지를 모두 지정해 좁힌다. 그리고 기한을 붙인다. 기한이 없는 예외는 영구 구멍이 된다.

\`\`\`
나쁨   규칙 942100 전체 비활성화
보통   /admin/* 에서 규칙 942100 비활성화
좋음   /admin/query 의 sql 파라미터에서만 942100 비활성화, 만료 2026-12-31
\`\`\`

## 가상 패치로 시간을 번다

취약점이 공개됐고 우리 코드 수정에 시간이 걸릴 때, 해당 요청 형태만 차단하는 규칙을 임시로 넣는다. 반드시 원인 수정 티켓과 연결하고, 수정 완료 후 규칙을 제거한다. 임시 규칙이 쌓여 남는 것이 WAF 운영에서 가장 흔한 부채다.

## 지표로 판단한다

차단 건수는 좋은 지표가 아니다. 차단 대부분이 자동 스캐너라면 숫자만 크다. 오탐률, 예외 규칙 수, 기한 초과 예외 수, 차단 모드 적용 비율을 본다.

## 바로 확인하기

차단이 실제로 동작하는지는 무해한 시험 요청으로 확인한다.

\`\`\`bash
# 명백한 공격 패턴 — 403 이 나와야 한다
curl -s -o /dev/null -w '%{http_code}\\n' "https://www.example.com/?q=%27%20OR%201=1--"
curl -s -o /dev/null -w '%{http_code}\\n' "https://www.example.com/?f=../../../../etc/passwd"

# 정상 요청은 통과해야 한다 — 오탐 확인
curl -s -o /dev/null -w '%{http_code}\\n' -X POST https://www.example.com/api/posts \\
  -H 'Content-Type: application/json' \\
  -d '{"body":"SELECT 문 사용법을 정리한 글입니다. 예: SELECT * FROM t"}'
\`\`\`

로그에서 오탐 후보를 뽑는다. 로그인한 사용자의 차단 건이 가장 유력한 후보다.

\`\`\`
오탐 후보 = 차단 건 중
  인증된 세션에서 발생
  같은 사용자에게 반복 발생
  특정 경로에 집중
  요청 본문이 정상 업무 데이터
\`\`\`

## 참고

- OWASP ModSecurity Core Rule Set 문서
- OWASP Web Application Firewall 프로젝트 자료
- NIST SP 800-41, 방화벽 정책 관리`,
    diagram: {
      type: 'bars',
      caption: '단계별 오탐 건수 추이',
      unit: '건/일',
      items: [
        { label: '도입 직후', value: 120, note: '탐지 모드' },
        { label: '예외 정리 후', value: 24, note: '경로 단위 예외' },
        { label: '부분 차단', value: 9, note: '규칙군별 전환' },
        { label: '안정 운영', value: 3, note: '배포마다 확인' },
      ],
    },
  },
  {
    slug: 'cdn-security',
    title: 'CDN 에서 막을 것과 못 막을 것',
    body: `CDN 을 앞에 두면 트래픽 폭주와 단순 공격은 상당히 걸러진다. 그러나 인가 결함, 데이터 노출, 논리 취약점은 그대로 통과한다. 더 중요한 것은 CDN 도입 자체가 새로운 위험을 만든다는 점이다. 원본 서버가 여전히 직접 노출돼 있거나 캐시 키 설계가 잘못되면 CDN 이 문제를 확대한다.

## 무엇이 막히고 무엇이 통과하는가?

| 공격 | CDN 효과 |
| --- | --- |
| 대역폭 소진형 트래픽 폭주 | 대부분 흡수 |
| 프로토콜 계층 공격 | 흡수 |
| 자동 스캐너 | 규칙으로 상당 부분 차단 |
| 인가 누락 | 통과 |
| 대량 데이터 조회 오용 | 통과 |
| 논리 결함 악용 | 통과 |
| 느린 대량 요청 | 부분적 |

![CDN 도입이 만드는 새 경로](/img/posts/cdn-security.svg)

## 원본 서버를 숨기는 것이 전제다

CDN 을 붙였는데 원본 IP 로 직접 접속이 가능하면 공격자는 CDN 을 그냥 건너뛴다. 원본은 CDN 대역에서 오는 요청만 받도록 제한하고, 공유 비밀 헤더를 함께 검증한다. 과거 DNS 이력과 인증서 로그로 원본 주소가 드러나는 경우가 많으므로 IP 교체도 필요하다.

\`\`\`
원본 보호
  방화벽에서 CDN 대역만 허용
  CDN 이 붙이는 공유 비밀 헤더 검증
  원본 IP 를 교체하고 과거 이력 정리
  원본 도메인을 별도로 두지 않는다
\`\`\`

## 캐시 키를 정확히 설계한다

개인화된 응답이 캐시되면 다른 사용자에게 전달된다. 응답이 달라지는 모든 요소가 캐시 키에 들어가야 하고, 로그인 사용자 응답은 캐시하지 않는 것이 기본이다. 반대로 캐시 키에 불필요한 헤더를 넣으면 적중률이 떨어진다.

- 인증된 응답은 캐시 금지를 기본값으로
- 응답이 달라지는 헤더는 Vary 로 명시
- 쿼리 파라미터 정규화 — 순서와 불필요한 파라미터 처리
- 오류 응답의 캐시 시간은 짧게

## 요청 수 제한은 CDN 에 두는 것이 유리하다

원본에 도달하기 전에 걸러지므로 자원 소모가 없고, 지역 단위로 적용할 수 있다. 다만 사용자 식별이 아이피 수준이라 정교하지 않다. 계정 단위 제한은 애플리케이션에 남겨야 한다.

## 바로 확인하기

원본 직접 접속이 막혔는지, 개인화 응답이 캐시되는지 두 가지를 본다.

\`\`\`bash
# 원본 IP 로 직접 요청 — 차단돼야 한다
curl -s -o /dev/null -w '%{http_code}\\n' --resolve "www.example.com:443:$ORIGIN_IP" \\
  https://www.example.com/

# 인증된 응답이 캐시되는지 — HIT 가 나오면 위험
curl -sI https://www.example.com/me -H "Cookie: session=$SESSION" \\
  | grep -iE 'cache-control|age|x-cache|cf-cache-status'
\`\`\`

캐시 키에 없는 헤더로 응답이 달라지는지도 확인한다.

\`\`\`bash
for lang in ko en; do
  printf '%-4s ' "$lang"
  curl -s https://www.example.com/ -H "Accept-Language: $lang" | md5sum
done
# 해시가 다르면 Accept-Language 가 Vary 에 있어야 한다
\`\`\`

## 참고

- RFC 9111 — HTTP Caching
- OWASP Cheat Sheet — Denial of Service
- PortSwigger — Web cache poisoning`,
    diagram: {
      type: 'layers',
      caption: 'CDN 을 포함한 방어 배치',
      layers: [
        { label: 'CDN', note: '트래픽 폭주·스캐너·요청 수 제한' },
        { label: '원본 접근 제한', note: 'CDN 대역과 비밀 헤더' },
        { label: '애플리케이션', note: '인가와 논리 검증' },
        { label: '데이터 계층', note: '조회 상한과 이력' },
      ],
    },
  },
  {
    slug: 'vpn-to-ztna',
    title: 'VPN 에서 ZTNA 로 넘어가는 순서',
    body: `VPN 의 문제는 암호화가 아니라 접속 성공 후에 주어지는 범위다. 한 번 붙으면 대역 전체가 보이고, 단말이 감염됐는지는 고려하지 않는다. 제로 트러스트 접근 방식은 이 범위를 자원 단위로 좁힌다. 다만 전면 교체는 위험하므로, 새 자원부터 새 방식으로 붙이고 기존 VPN 을 점차 축소하는 순서가 현실적이다.

## 무엇이 달라지는가?

| 항목 | VPN | ZTNA |
| --- | --- | --- |
| 접근 단위 | 네트워크 대역 | 애플리케이션·자원 |
| 판단 시점 | 접속 시 1회 | 요청마다 |
| 판단 근거 | 자격 증명 | 사용자·기기 상태·맥락 |
| 횡적 이동 | 가능 | 자원 간 차단 |
| 감사 | 접속 로그 | 자원별 접근 로그 |
| 사용자 경험 | 접속 전환 필요 | 상시 연결 |

![대역 접근과 자원 단위 접근](/img/posts/vpn-to-ztna.svg)

## 어디부터 옮기는가?

한꺼번에 바꿀 수 없으니 옮기기 쉽고 효과가 큰 것부터 고른다.

\`\`\`
1순위  웹 기반 내부 도구 — 프록시만 붙이면 끝난다
2순위  관리 콘솔·배스천 접근 — 위험도가 가장 높다
3순위  협력업체 접근 — 대역 접근을 자원 접근으로 좁힌다
4순위  개발자 데이터베이스 접근 — 프로토콜 지원 확인 필요
후순위 레거시 사내 시스템 — 프로토콜 제약으로 마지막
\`\`\`

## 기기 상태를 판단에 넣는 것이 핵심이다

사용자 인증만으로는 VPN 과 크게 다르지 않다. 디스크 암호화, 화면 잠금, 보안 업데이트 적용, 단말 보호 도구 동작 여부를 확인해 접근 여부와 범위를 정해야 실질적인 차이가 생긴다. 이 판단이 없으면 이름만 바뀐 VPN 이 된다.

## 예외가 전체를 무력화한다

"레거시 시스템 때문에" 남겨둔 전면 VPN 경로가 있으면 공격자는 그 경로를 쓴다. 남은 VPN 은 접근 가능 대역을 좁히고, 사용자와 사용 시간을 제한하며, 사용 이력을 별도로 감시해야 한다.

## 바로 확인하기

접속 후 무엇이 보이는지가 실질적인 통제 범위다. 접속한 상태에서 확인한다.

\`\`\`bash
# VPN 접속 후 보이는 대역 — 넓으면 그만큼이 통제 범위다
ip route | grep -v default

# 자원 단위 접근이 실제로 좁혀졌는지
for svc in 10.20.0.10:5432 10.20.0.11:6379 10.20.1.5:22 10.20.2.9:443; do
  host=\${svc%%:*}; port=\${svc##*:}
  printf '%-20s ' "$svc"
  nc -z -w2 "$host" "$port" >/dev/null 2>&1 && echo '접근 가능' || echo '차단'
done
\`\`\`

전환 진행도는 지표로 관리한다.

\`\`\`
전환율     ZTNA 로 접근하는 내부 자원 비율
잔존 경로  전면 VPN 을 쓰는 사용자 수
기기 준수  정책 충족 단말 비율
접근 로그  자원별 접근 기록 보유 비율
\`\`\`

## 참고

- NIST SP 800-207, Zero Trust Architecture
- CISA Zero Trust Maturity Model
- NIST SP 800-46, 원격 접속 보안`,
    diagram: {
      type: 'flow',
      caption: '요청마다 이루어지는 접근 판단',
      steps: [
        { label: '사용자 인증', note: '피싱 저항 수단 권장' },
        { label: '기기 상태 확인', note: '암호화·패치·보호 도구' },
        { label: '자원별 인가', note: '대역이 아니라 자원 단위' },
        { label: '접근 기록', note: '자원별 감사 로그' },
      ],
    },
  },
  {
    slug: 'asset-eol',
    title: '지원 종료 자산 식별과 교체 계획',
    body: `지원이 끝난 소프트웨어는 취약점이 발견돼도 패치가 나오지 않는다. 즉 그 시점부터 위험은 시간에 비례해 쌓이기만 한다. 그런데 지원 종료는 사고를 일으키지 않고 조용히 지나가기 때문에 아무도 알아채지 못한다. 필요한 것은 자산마다 지원 종료일을 미리 알고, 교체 작업을 예산과 일정에 넣는 절차다.

## 왜 교체가 미뤄지는가?

- 자산 목록에 지원 종료일 필드가 없어 아무도 추적하지 않는다
- 교체가 기능 개선이 아니어서 우선순위에서 밀린다
- 의존하는 다른 구성 요소가 함께 올라가야 한다
- 교체 실패 시 되돌릴 방법이 준비돼 있지 않다
- 담당자가 바뀌면서 이력이 끊긴다

![지원 종료일 기준 교체 계획 수립](/img/posts/asset-eol.svg)

## 자산 목록에 넣어야 할 유형별 항목

| 유형 | 확인 대상 |
| --- | --- |
| 운영체제 | 배포판 지원 종료일과 확장 지원 여부 |
| 런타임 | 언어 버전 지원 종료일 |
| 프레임워크 | 주 버전 지원 정책 |
| 데이터베이스 | 주 버전 지원 종료와 마이그레이션 경로 |
| 컨테이너 이미지 | 베이스 이미지의 상위 배포판 지원 |
| 관리형 서비스 | 공급자 버전 강제 업그레이드 일정 |
| 네트워크 장비 | 하드웨어 지원 종료와 펌웨어 제공 |
| 인증서·라이브러리 | 만료와 유지보수 중단 |

## 종료 12개월 전을 기준으로 삼는다

종료일에 맞춰 시작하면 늦다. 12개월 전에 교체 과제를 등록하고, 6개월 전에 작업을 시작하며, 3개월 전에는 완료를 목표로 둔다. 남은 기간이 3개월 미만인 자산은 위험으로 등록해 관리자가 인지하게 한다.

\`\`\`
D-12개월  교체 과제 등록, 담당과 예산 지정
D-6개월   작업 시작, 호환성 검증
D-3개월   전환 완료 목표
D-0       종료. 남아 있으면 위험 등록과 보완 통제
\`\`\`

## 교체할 수 없다면 위험을 좁힌다

즉시 교체가 불가능한 경우도 있다. 그때는 방치가 아니라 보완 통제를 걸고 기한을 정한다. 네트워크 접근을 좁히고, 인터넷 노출을 없애고, 로그 감시를 강화하고, 교체 계획의 재검토 시점을 기록한다.

## 바로 확인하기

우리가 실제로 어떤 버전을 쓰는지 수집하는 것이 첫 단계다. 자산 목록에 버전 필드가 없으면 종료일 관리도 불가능하다.

\`\`\`bash
# 운영체제와 커널
cat /etc/os-release | grep -E '^(NAME|VERSION)='

# 컨테이너 이미지의 베이스 배포판 버전 일괄 수집
for img in $(kubectl get pods -A -o jsonpath='{..image}' | tr ' ' '\\n' | sort -u); do
  printf '%-50s ' "$img"
  docker run --rm --entrypoint sh "$img" -c 'cat /etc/os-release 2>/dev/null | grep ^VERSION_ID=' \\
    2>/dev/null || echo '확인 불가'
done

# 런타임 버전
node -v; python3 -V; java -version 2>&1 | head -1
\`\`\`

수집한 버전을 공개된 지원 종료일 자료와 대조해 남은 일수를 계산하고, 3개월 미만은 위험 목록으로 올린다. 자산 목록과 대조해 목록에 없는 자산이 나오면 그것부터 등록한다.

## 참고

- CIS Controls v8, 2 소프트웨어 자산 관리
- NIST SP 800-40, 패치 관리 계획
- endoflife.date 등 공개 지원 종료일 데이터`,
    diagram: {
      type: 'steps',
      caption: '지원 종료 대응 절차',
      steps: [
        { label: '버전 수집', note: '자산 목록에 버전 필드 추가' },
        { label: '종료일 대조', note: '남은 일수 계산' },
        { label: '교체 과제 등록', note: '12개월 전 예산 반영' },
        { label: '잔존 자산 위험 등록', note: '보완 통제와 기한' },
      ],
    },
  },
  {
    slug: 'byod-security',
    title: '개인 기기 업무 사용 통제 기준',
    body: `개인 기기 사용을 금지한다고 적어두면 통제가 되는 것이 아니라 보이지 않게 된다. 메일과 메신저는 이미 개인 휴대폰에 들어가 있고, 급할 때 개인 노트북으로 접속한다. 그래서 실효성 있는 접근은 금지가 아니라, 무엇을 어디까지 허용하고 그 기기에 어떤 조건을 요구할지 정하는 것이다.

## 무엇을 통제 대상으로 볼 것인가?

기기를 통제하는 것이 목적이 아니라 업무 데이터가 남는 것을 통제하는 것이 목적이다. 이 구분이 정책의 방향을 정한다.

- 업무 데이터가 기기에 저장되는가
- 저장되지 않고 화면에만 표시되는가
- 기기 분실 시 데이터를 지울 수 있는가
- 회사가 개인 영역을 들여다보게 되는가

![기기에 데이터가 남는 경우와 남지 않는 경우](/img/posts/byod-security.svg)

## 허용 수준 설계

| 접근 유형 | 개인 기기 | 요구 조건 |
| --- | --- | --- |
| 메일·메신저 | 허용 | 화면 잠금, 원격 삭제 가능한 업무 영역 |
| 웹 내부 도구 | 허용 | 브라우저 접근만, 다운로드 제한 |
| 코드 저장소 | 조건부 | 회사 기기 권장, 불가 시 원격 개발 환경 |
| 운영 시스템 접근 | 금지 | 회사 관리 기기만 |
| 개인정보 조회 | 금지 | 회사 관리 기기·지정 네트워크 |

## 데이터를 남기지 않는 쪽이 관리가 쉽다

기기를 통제하려면 개인 기기에 관리 도구를 설치해야 하고, 이는 개인정보와 사생활 문제를 만든다. 반대로 화면만 전달하는 방식(원격 데스크톱, 웹 기반 도구, 다운로드 차단)을 쓰면 기기에 남는 것이 없으므로 기기 통제 자체가 덜 필요해진다.

\`\`\`
접근 방식별 잔여 데이터
  파일 다운로드        기기에 남는다 — 회수 불가
  업무용 앱 컨테이너   앱 영역만 원격 삭제 가능
  브라우저 열람 전용   캐시 외 잔존 최소
  원격 데스크톱        기기에 남지 않는다
\`\`\`

## 개인 영역과 업무 영역을 분리한다

업무 영역만 관리 대상으로 두면, 회사는 개인 사진과 메시지에 접근하지 않고 퇴사 시 업무 영역만 삭제한다. 무엇을 볼 수 있고 무엇을 볼 수 없는지 문서로 알리는 것이 동의를 얻는 조건이다.

## 최소 요구 조건은 짧게

요구 조건이 길면 아무도 지키지 않는다. 화면 잠금, 최신 보안 업데이트, 디스크 암호화, 업무 앱을 통한 접근 네 가지 정도로 좁히고, 조건을 못 맞추면 접근 범위를 줄이는 방식이 실효성이 높다.

## 바로 확인하기

정책보다 실제 접근 현황을 보는 것이 정확하다. 관리되지 않는 기기에서의 접근을 찾는다.

\`\`\`bash
# 관리 기기 목록에 없는 기기에서 온 접근
comm -13 <(sort managed-devices.txt) <(awk '{print $3}' access.log | sort -u) \\
  | head -20

# 접근 유형별 분포 — 다운로드가 개인 기기에서 발생하는지
grep -E 'GET /(files|export)/' access.log \\
  | awk '{print $3}' | sort | uniq -c | sort -rn | head
\`\`\`

## 참고

- NIST SP 800-46, 원격 근무와 개인 기기 보안
- NIST SP 800-124, 모바일 기기 관리 지침
- CIS Controls v8, 1 기업 자산 목록`,
    diagram: {
      type: 'matrix',
      caption: '기기 관리 여부와 데이터 잔존',
      x: ['데이터 잔존', '데이터 잔존 없음'],
      y: ['관리 기기', '개인 기기'],
      cells: ['원격 삭제로 대응', '가장 안전', '회수 불가 — 금지', '조건부 허용'],
    },
  },
]
