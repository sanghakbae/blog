import type { SeedPost } from './types'

/** 431~440 — 개발 보안 */
export const posts44: SeedPost[] = [
  {
    slug: 'sast-false-positive',
    title: '정적 분석 오탐을 다루는 실무 기준',
    body: `정적 분석 도구를 켜면 수천 건이 나온다. 그중 실제 문제는 일부다. 오탐을 걸러내는 기준과 절차가 없으면 개발팀이 도구 자체를 무시하게 된다.

## 왜 오탐이 많은가?

| 원인 | 설명 |
| --- | --- |
| 문맥 부족 | 호출 경로를 다 보지 못함 |
| 검증 인식 실패 | 사내 검증 함수를 모름 |
| 프레임워크 미지원 | 자동 처리되는 것을 놓침 |
| 시험 코드 포함 | 운영 코드가 아님 |
| 생성 코드 | 사람이 쓴 것이 아님 |

사내 검증 함수를 인식하지 못하는 것이 가장 흔하다. 도구에 알려 주면 대량으로 줄어든다.

![오탐 원인과 대응](/img/posts/sast-false-positive.svg)

## 줄이는 순서

1. 검사 대상에서 시험·생성 코드를 제외한다
2. 사내 검증·정화 함수를 도구에 등록한다
3. 규칙별 발생 건수를 보고 상위를 검토한다
4. 우리 환경에 맞지 않는 규칙을 끈다
5. 남은 것을 등급별로 처리한다

1번과 2번만으로 절반 이상 줄어드는 경우가 많다.

## 판정 기준

| 판정 | 조건 |
| --- | --- |
| 진짜 문제 | 외부 입력이 위험 지점에 도달 |
| 조건부 | 특정 경로에서만 |
| 오탐 | 검증이 이미 있음 |
| 해당 없음 | 실행되지 않는 코드 |

판정할 때 "이 값이 어디서 오는가" 를 추적한다. 외부 입력이 아니면 대부분 문제가 아니다.

![판정 흐름](/img/posts/sast-false-positive-2.svg)

## 오탐 억제 기록

\`\`\`
// 정적 분석 억제 — 반드시 사유를 함께 적는다
// 입력이 내부 상수 목록에서만 오므로 주입 위험 없음. 검토: 2026-09-10
\`\`\`

억제할 때 사유를 코드에 남긴다. 사유 없는 억제 주석은 리뷰에서 거부한다. 나중에 코드가 바뀌어 실제 위험이 되어도 억제가 남아 있게 되기 때문이다.

## 파이프라인 적용

| 단계 | 처리 |
| --- | --- |
| 변경분 검사 | 새로 들어온 것만 차단 |
| 전체 검사 | 야간, 추이 관찰 |
| 기존 항목 | 별도 목록으로 관리 |

기존 코드의 모든 지적을 차단 조건으로 걸면 배포가 막힌다. 새로 추가되는 것만 막고 기존은 계획적으로 줄인다.

이 방식은 코드 품질이 나빠지지 않게 하면서 부담을 줄인다. 시간이 지나면 기존 항목도 자연히 줄어든다.

## 실제로 이렇게 터진다

도구를 켜고 3천 건을 그대로 개발팀에 전달한 사례가 있다. 확인이 불가능한 양이었고, 그 뒤로 도구 결과를 아무도 보지 않았다. 그 안에 실제 주입 취약점이 두 건 있었다.

또 하나는 억제 주석을 사유 없이 남발한 경우다. 코드가 바뀌어 실제 위험이 된 뒤에도 억제가 남아 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 전부 전달하는 것이 성실하다 | 묻힌다 |
| 오탐은 도구 탓이다 | 설정으로 줄어든다 |
| 억제는 나쁜 것이다 | 사유가 있으면 정상이다 |
| 기존 지적도 다 막아야 한다 | 배포가 멈춘다 |

## 지적 건수를 줄이는 절차

| 순서 | 조치 | 효과 |
| --- | --- | --- |
| 1 | 시험·생성 코드 제외 | 큼 |
| 2 | 사내 검증 함수 등록 | 큼 |
| 3 | 안 맞는 규칙 비활성화 | 중간 |
| 4 | 변경분만 차단 | 큼 |
| 5 | 기존 항목 계획 처리 | 지속 |

## 규칙별 발생량 보기

상위 몇 개가 전체의 대부분을 차지한다. 거기서부터 줄인다.

\`\`\`bash
# 규칙별 지적 건수
jq -r '.runs[].results[].ruleId' sarif.json | sort | uniq -c | sort -rn | head -15

# 시험 코드가 포함돼 있는지
jq -r '.runs[].results[].locations[].physicalLocation.artifactLocation.uri' sarif.json \
  | grep -cE 'test|spec|mock|fixture'
\`\`\`

시험 코드 비중이 크면 제외 설정만으로 절반이 준다. 남은 상위 규칙의 표본을 열어 사내 검증 함수를 인식하지 못하는 것인지 확인한다.

## 파이프라인 적용 방식

기존 지적을 전부 차단 조건으로 걸면 배포가 멈춘다. 새로 들어오는 것만 막는다.

| 대상 | 처리 |
| --- | --- |
| 변경분의 새 지적 | 차단 |
| 변경분의 기존 지적 | 경고 |
| 미변경 파일 | 목록 관리 |
| 심각도 높음 | 차단 |
| 정보성 | 기록만 |

이 방식이면 코드가 나빠지지 않으면서 기존 부채를 계획적으로 줄일 수 있다. 시간이 지나면 기존 항목도 자연히 준다.

## 억제 관리

억제 주석이 쌓이면 그 자체가 부채가 된다.

| 항목 | 규칙 |
| --- | --- |
| 사유 | 필수 |
| 검토일 | 표기 |
| 리뷰 | 억제 추가 시 필수 |
| 정기 확인 | 반기 |
| 만료 | 코드 변경 시 재확인 |

사유 없는 억제는 리뷰에서 거부한다. 나중에 코드가 바뀌어 실제 위험이 되어도 억제가 남아 있게 된다.

## 참고

- OWASP — Source Code Analysis Tools
- NIST SP 500-268 — 정적 분석 도구 기능 명세
- 각 도구의 사용자 정의 규칙 문서`,
    diagram: {
      type: 'bars',
      caption: '오탐 원인 비중',
      unit: '%',
      items: [
        { label: '검증 인식 실패', value: 35, note: '사내 함수' },
        { label: '문맥 부족', value: 28, note: '호출 경로' },
        { label: '시험·생성 코드', value: 22, note: '제외 대상' },
        { label: '규칙 부적합', value: 15, note: '끄면 됨' },
      ],
    },
    diagram2: {
      type: 'flow',
      caption: '판정 절차',
      steps: [
        { label: '입력 출처 확인', note: '외부인가' },
        { label: '검증 경로 확인', note: '이미 있나' },
        { label: '도달 가능성', note: '실행되나' },
        { label: '판정과 기록', note: '사유 필수' },
      ],
    },
  },
  {
    slug: 'code-review-security-checklist',
    title: '코드 리뷰에서 던져야 할 세 가지 질문',
    body: `리뷰에서 보안 문제를 잡으려면 무엇을 볼지 알아야 한다. 전체 코드를 정독하는 방식은 지속되지 않는다. 위험이 몰리는 지점 몇 곳을 정해 두고 그곳을 집중해서 본다.

## 어디를 보나?

| 지점 | 확인 |
| --- | --- |
| 외부 입력 진입 | 검증이 있나 |
| 인가 판정 | 누가 이걸 할 수 있나 |
| 질의 생성 | 문자열 결합인가 |
| 출력 | 이스케이프가 있나 |
| 파일 경로 | 사용자 값이 들어가나 |
| 외부 호출 | 주소가 어디서 오나 |
| 로그 | 민감 정보가 들어가나 |

인가 판정이 가장 자주 빠진다. 기능은 동작하지만 누구나 부를 수 있는 상태로 병합된다.

![리뷰에서 집중할 지점](/img/posts/code-review-security-checklist.svg)

## 물어볼 질문

| 질문 | 찾는 것 |
| --- | --- |
| 이 값은 어디서 오나 | 신뢰 경계 |
| 누가 이 함수를 부를 수 있나 | 인가 |
| 실패하면 어떻게 되나 | 오류 처리 |
| 두 번 호출되면 | 멱등성 |
| 남의 데이터를 볼 수 있나 | 소유자 검증 |
| 로그에 무엇이 남나 | 정보 노출 |

"이 값은 어디서 오나" 하나만 습관이 되어도 많은 문제가 걸린다.

## 자동화와 사람의 분담

| 유형 | 담당 |
| --- | --- |
| 알려진 패턴 | 도구 |
| 비밀 값 포함 | 도구 |
| 의존성 취약점 | 도구 |
| 인가 누락 | 사람 |
| 업무 로직 오류 | 사람 |
| 설계 문제 | 사람 |

도구가 잡을 수 있는 것을 사람이 보면 시간이 낭비된다. 사람은 도구가 못 보는 것을 본다.

![도구와 사람의 역할](/img/posts/code-review-security-checklist-2.svg)

## 리뷰를 돕는 장치

| 장치 | 효과 |
| --- | --- |
| 경로별 리뷰어 지정 | 민감 영역에 전문가 |
| 변경 크기 제한 | 큰 변경은 제대로 못 본다 |
| 템플릿의 보안 항목 | 작성자가 먼저 확인 |
| 위험 표시 | 민감 경로 변경 알림 |

변경 크기가 중요하다. 수천 줄 변경은 아무도 제대로 보지 못한다. 나누어 올리게 하는 것이 리뷰 품질을 올린다.

## 문화

지적을 개인에 대한 비판으로 만들지 않는다. 코드에 대한 질문으로 표현하고, 왜 그것이 문제인지 설명한다. 설명 없이 고치라고만 하면 다음에도 같은 코드가 온다.

리뷰어도 모르는 것이 있다. 확신이 없으면 보안 담당에게 확인을 요청하는 경로를 열어 둔다.

리뷰 항목을 체크리스트로 만들되 길게 만들지 않는다. 다섯 개를 넘으면 아무도 다 보지 않는다. 우리 조직에서 실제로 자주 나온 문제 유형을 골라 짧게 유지하고, 사고가 생기면 그 유형을 추가한다.

## 실제로 이렇게 터진다

기능은 완벽하지만 인가 검사가 없는 코드가 병합된 사례가 있다. 리뷰어 세 명이 봤고 아무도 "누가 이걸 부를 수 있나" 를 묻지 않았다.

또 하나는 3천 줄 변경을 한 번에 올린 경우다. 아무도 제대로 보지 못했고 승인만 눌렀다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 사람이 많이 보면 안전하다 | 질문이 없으면 못 본다 |
| 전체를 읽어야 한다 | 위험 지점에 집중한다 |
| 큰 변경도 볼 수 있다 | 나눠야 보인다 |
| 도구가 잡아 준다 | 인가는 못 잡는다 |

## 세 가지 질문

| 질문 | 잡는 것 |
| --- | --- |
| 이 값은 어디서 오나 | 신뢰 경계 |
| 누가 이걸 부를 수 있나 | 인가 |
| 남의 데이터를 볼 수 있나 | 소유자 검증 |

## 변경 크기 관리

큰 변경은 아무도 제대로 보지 못한다. 실제 크기를 재 본다.

\`\`\`bash
# 최근 병합된 변경의 크기 분포
git log --merges --since='3 months ago' --format='%H' \
  | while read -r h; do git show --stat --format='' "$h" | tail -1; done \
  | grep -oE '[0-9]+ insertion' | grep -oE '[0-9]+' | sort -n | tail -10
\`\`\`

수천 줄짜리 병합이 자주 보이면 리뷰 품질을 기대할 수 없다. 민감 경로에는 변경 크기 상한을 두거나 경로별 리뷰어를 지정한다.

## 경로별 리뷰어 지정

모든 코드를 같은 사람이 볼 수 없다. 민감 경로에 전문가를 배정한다.

| 경로 | 리뷰어 |
| --- | --- |
| 인증·인가 | 보안 담당 필수 |
| 결제 | 결제 담당 + 보안 |
| 개인정보 처리 | 개인정보 담당 |
| 인프라 코드 | 인프라 담당 |
| 일반 기능 | 팀 내 |

경로별 리뷰어 지정 기능을 쓰면 자동으로 배정된다. 사람이 기억해서 부르는 방식은 빠뜨린다.

## 리뷰 문화

지적 방식이 다음 코드를 정한다.

| 피할 표현 | 나은 표현 |
| --- | --- |
| 이거 취약해요 | 이 값이 어디서 오나요 |
| 왜 이렇게 했나요 | 이 경우는 어떻게 되나요 |
| 고치세요 | 이렇게 하면 어떨까요 |
| 기본인데 | 이건 놓치기 쉬운데 |

질문 형태로 던지면 작성자가 스스로 문제를 발견하는 경우가 많고, 그렇게 배운 것이 남는다.

## 참고

- OWASP — Code Review Guide
- CWE Top 25 — 흔한 약점 목록
- 각 언어별 보안 코딩 가이드`,
    diagram: {
      type: 'layers',
      caption: '리뷰 집중 지점',
      layers: [
        { label: '입력 진입', note: '검증 확인' },
        { label: '인가 판정', note: '가장 자주 누락' },
        { label: '데이터 접근', note: '소유자 검증' },
        { label: '출력·로그', note: '정보 노출' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '역할 분담',
      x: ['패턴으로 탐지 가능', '불가'],
      y: ['빈번', '드묾'],
      cells: ['도구 자동화', '사람 리뷰', '도구 + 확인', '설계 검토'],
    },
  },
  {
    slug: 'secret-scanning-pipeline',
    title: '커밋 전부터 막는 비밀 값 검사 계층',
    body: `자격 증명이 저장소에 커밋되는 사고는 계속 일어난다. 발견 시점에는 이미 이력에 남아 있고, 공개 저장소라면 몇 분 안에 자동 도구가 찾아낸다. 커밋 전에 막는 것이 유일하게 확실한 방법이다.

## 어디서 막을 수 있나?

| 지점 | 효과 |
| --- | --- |
| 로컬 커밋 전 | 최선 — 이력에 안 남음 |
| 푸시 전 | 좋음 |
| 저장소 수신 시 | 차단 가능 |
| 파이프라인 검사 | 사후 발견 |
| 정기 스캔 | 늦음 |

로컬에서 막는 것이 유일하게 이력을 깨끗하게 유지한다. 다만 개발자가 설정해야 하므로 강제하기 어렵다.

![차단 지점별 효과](/img/posts/secret-scanning-pipeline.svg)

## 여러 겹으로 건다

1. 커밋 전 검사 도구를 설정 파일로 배포한다
2. 저장소 수신 시 검사를 켠다
3. 파이프라인에 검사를 넣는다
4. 정기 전체 스캔을 돌린다
5. 발견 시 자동으로 담당자에게 알린다

로컬 설정은 개발자가 끌 수 있으므로 서버 측 검사가 반드시 함께 있어야 한다.

## 탐지 방식

| 방식 | 특징 |
| --- | --- |
| 형식 패턴 | 알려진 키 형식 |
| 엔트로피 | 무작위 문자열 |
| 검증 호출 | 실제 유효한지 확인 |
| 사용자 정의 | 사내 형식 |

엔트로피 기반은 오탐이 많다. 형식 패턴을 우선하고, 사내 자격 증명 형식을 사용자 정의 규칙으로 추가한다.

![탐지 방식과 오탐률](/img/posts/secret-scanning-pipeline-2.svg)

## 이미 커밋됐다면

\`\`\`
1. 해당 자격 증명을 즉시 무효화한다   ← 가장 급함
2. 새 자격 증명을 발급하고 배포한다
3. 노출 기간의 사용 흔적을 조사한다
4. 이력 정리를 검토한다              ← 효과 제한적
5. 재발 방지 검사를 추가한다
\`\`\`

이력을 지우는 것보다 무효화가 먼저다. 이미 복제된 저장소와 캐시에 남아 있으므로 이력 정리만으로는 안전해지지 않는다.

## 근본 대응

검사는 사고를 줄이지만 없애지 못한다. 애초에 자격 증명이 코드에 들어갈 이유를 없애는 것이 근본이다.

| 방법 | 효과 |
| --- | --- |
| 비밀 관리 서비스 | 코드에 값이 없음 |
| 환경별 주입 | 배포 시점에 |
| 워크로드 신원 | 장기 자격 증명 자체를 없앰 |
| 로컬 개발용 별도 값 | 운영 값 접근 불필요 |

로컬 개발에 운영 자격 증명이 필요하면 언젠가 커밋된다. 개발용 값을 따로 제공한다.

## 실제로 이렇게 터진다

키가 커밋된 것을 발견하고 이력에서 지운 사례가 있다. 무효화는 하지 않았고, 이미 복제된 저장소와 캐시에 남아 있던 값으로 며칠 뒤 접근이 있었다.

또 하나는 로컬 검사만 설정한 경우다. 개발자가 검사를 건너뛰고 푸시했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 이력을 지우면 안전하다 | 무효화가 먼저다 |
| 로컬 검사면 충분하다 | 끌 수 있다 |
| 공개 저장소만 위험하다 | 비공개도 유출된다 |
| 검사가 있으면 안 들어온다 | 근본은 코드에 안 두는 것 |

## 발견 시 순서

| 순서 | 조치 |
| --- | --- |
| 1 | 즉시 무효화 |
| 2 | 새 값 발급·배포 |
| 3 | 노출 기간 사용 흔적 조사 |
| 4 | 이력 정리 검토 |
| 5 | 검사 규칙 추가 |

## 이력 검사

현재 파일만 보면 이미 커밋된 것을 놓친다.

\`\`\`bash
# 이력 전체에서 개인키 형식
git log --all -p --diff-filter=AM \
  | grep -nE 'BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY|AKIA[0-9A-Z]{16}' | head

# 현재 파일
grep -rlE 'BEGIN (RSA |EC )?PRIVATE KEY' . --exclude-dir=.git
\`\`\`

하나라도 나오면 그 값을 먼저 무효화한다. 이력 정리는 그다음이고, 효과도 제한적이다. 이미 복제된 저장소에는 남아 있다.

## 검사 계층 배치

한 계층만으로는 부족하다. 각각의 역할이 다르다.

| 계층 | 역할 | 우회 가능 |
| --- | --- | --- |
| 커밋 전 | 이력 보호 | 개발자가 끌 수 있음 |
| 푸시 전 | 마지막 로컬 방어 | 같음 |
| 저장소 수신 | 서버 강제 | 불가 |
| 파이프라인 | 이중 확인 | 불가 |
| 정기 스캔 | 잔여 탐지 | 불가 |

서버 측 검사가 반드시 있어야 한다. 로컬 검사만으로는 강제되지 않는다.

## 코드에 값을 두지 않는 구조

검사는 사고를 줄이지만 없애지 못한다. 자격 증명이 코드에 들어갈 이유를 없앤다.

| 상황 | 대안 |
| --- | --- |
| 로컬 개발 | 개발 전용 값 제공 |
| 시험 코드 | 모의 값 |
| 설정 파일 | 환경별 주입 |
| 배포 | 비밀 관리 서비스 |
| 서비스 간 인증 | 워크로드 신원 |

로컬 개발에 운영 값이 필요하면 언젠가 커밋된다. 개발용 값을 제공하는 것이 가장 효과적이다.

## 참고

- OWASP — Secrets Management Cheat Sheet
- NIST SP 800-57 — 키 관리
- 각 저장소 서비스의 비밀 스캔 기능 문서`,
    diagram: {
      type: 'layers',
      caption: '방어 계층',
      layers: [
        { label: '커밋 전 검사', note: '이력 보호' },
        { label: '수신 시 차단', note: '서버 강제' },
        { label: '파이프라인 검사', note: '이중 확인' },
        { label: '정기 스캔', note: '잔여 탐지' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '탐지 방식별 정확도',
      unit: '점',
      items: [
        { label: '형식 패턴', value: 90, note: '오탐 적음' },
        { label: '검증 호출', value: 95, note: '실제 유효성' },
        { label: '사용자 정의', value: 85, note: '사내 형식' },
        { label: '엔트로피', value: 45, note: '오탐 많음' },
      ],
    },
  },
  {
    slug: 'dependency-update-strategy',
    title: '의존성 갱신을 지속 가능하게 만들기',
    body: `자동 갱신 도구를 켜면 매주 수십 개의 갱신 요청이 온다. 처음에는 처리하다가 쌓이면 전부 무시하게 되고, 그 안에 있던 보안 갱신도 함께 밀린다.

## 왜 쌓이는가?

| 원인 | 설명 |
| --- | --- |
| 요청이 너무 많음 | 개별 처리 부담 |
| 보안과 일반 혼재 | 급한 것이 묻힘 |
| 시험 부족 | 병합이 두렵다 |
| 주요 버전 변경 | 코드 수정 필요 |
| 담당 불명확 | 아무도 안 봄 |

보안 갱신과 일반 갱신을 구분하지 않는 것이 핵심 문제다.

![갱신 유형 분리](/img/posts/dependency-update-strategy.svg)

## 유형별 처리

| 유형 | 처리 |
| --- | --- |
| 보안 갱신 | 즉시 검토, 우선 병합 |
| 패치 버전 | 주 1회 일괄 |
| 부 버전 | 월 1회 일괄 |
| 주 버전 | 계획 수립 후 개별 |

일괄 처리가 부담을 크게 줄인다. 한 번에 여러 개를 묶어 시험하고 병합한다.

## 자동 병합 조건

| 조건 | 자동 병합 |
| --- | --- |
| 패치 버전 + 시험 통과 | 가능 |
| 부 버전 + 시험 통과 | 검토 후 |
| 주 버전 | 불가 |
| 시험 실패 | 불가 |
| 관리자 변경된 패키지 | 불가 |

자동 병합을 켜려면 시험이 충분해야 한다. 시험이 부실하면 갱신이 조용히 기능을 깨뜨린다.

![자동화 가능 범위](/img/posts/dependency-update-strategy-2.svg)

## 주요 버전 처리

주 버전 갱신은 호환성이 깨진다. 미루면 나중에 여러 버전을 한 번에 올려야 해서 더 어려워진다.

| 방법 | 적용 |
| --- | --- |
| 분기별 계획 | 하나씩 처리 |
| 변경 내역 확인 | 영향 범위 파악 |
| 단계 적용 | 개발 → 시험 → 운영 |
| 되돌림 준비 | 문제 시 |

## 취약점 대응 기준

\`\`\`
심각·직접 의존       24시간 내 검토, 3일 내 조치
심각·간접 의존       1주 내
높음                2주 내
중간 이하            정기 갱신 주기
악용 확인됨          즉시
\`\`\`

간접 의존성은 우리가 직접 올릴 수 없는 경우가 있다. 상위 패키지가 갱신될 때까지 기다리거나, 강제 버전 지정으로 우회한다.

## 정리도 함께

쓰지 않는 의존성을 제거하면 갱신 대상 자체가 준다. 분기마다 미사용 패키지를 확인하고 지운다. 의존성 수가 적으면 관리 부담과 공격면이 함께 줄어든다.

갱신을 미룬 기간이 곧 노출 기간이다. 이 숫자를 측정해 두면 개선이 필요한지 판단할 수 있다. 보안 갱신 공개일부터 우리 운영 반영까지 걸린 시간을 기록하고 추이를 본다.

## 실제로 이렇게 터진다

자동 갱신 요청이 매주 40건씩 쌓인 사례가 있다. 두 달 뒤 아무도 보지 않게 됐고, 그 안에 있던 심각 취약점 패치도 함께 밀렸다.

또 하나는 자동 병합을 켰지만 시험이 부실했던 경우다. 갱신 하나가 조용히 기능을 깨뜨렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 자동 갱신을 켜면 최신이 된다 | 쌓이면 아무도 안 본다 |
| 하나씩 검토해야 안전하다 | 부담에 멈춘다 |
| 자동 병합은 위험하다 | 시험이 충분하면 안전하다 |
| 간접 의존성은 어쩔 수 없다 | 강제 버전으로 우회한다 |

## 분리 처리

| 유형 | 처리 |
| --- | --- |
| 보안 갱신 | 즉시, 개별 |
| 패치 버전 | 주 1회 일괄 |
| 부 버전 | 월 1회 일괄 |
| 주 버전 | 분기 계획 |

## 밀린 정도 재기

보안 갱신이 얼마나 늦게 반영되는지가 실질 지표다.

\`\`\`bash
# 공개된 취약점 중 미조치 항목의 경과일
npm audit --json 2>/dev/null \
  | jq -r '.vulnerabilities | to_entries[] | select(.value.severity=="critical" or .value.severity=="high")
           | "\(.value.severity) \(.key)"' | sort | head -20
\`\`\`

심각·높음 항목이 목록에 오래 남아 있으면 처리 흐름이 막힌 것이다. 보안 갱신만 별도 경로로 빼서 즉시 처리하고, 일반 갱신은 일괄로 미룬다.

## 자동 병합 조건 설계

시험이 충분해야 자동 병합이 안전하다. 조건을 명확히 한다.

| 조건 | 자동 병합 |
| --- | --- |
| 패치 버전 + 전체 시험 통과 | 가능 |
| 부 버전 + 전체 시험 통과 | 검토 후 |
| 보안 갱신 + 시험 통과 | 우선 병합 |
| 주 버전 | 불가 |
| 관리자 변경된 패키지 | 불가 |
| 시험 실패 | 불가 |

시험 범위가 좁으면 자동 병합이 위험하다. 자동화 전에 시험 커버리지를 먼저 확인한다.

## 간접 의존성 처리

우리가 직접 올릴 수 없는 경우가 있다.

| 상황 | 대응 |
| --- | --- |
| 상위 패키지가 곧 갱신 | 대기 |
| 갱신 계획 없음 | 강제 버전 지정 |
| 대체 가능 | 상위 패키지 교체 |
| 대체 불가 | 위험 수용·보완 |

강제 버전 지정은 호환성 문제를 만들 수 있으므로 시험으로 확인한다. 임시 조치이므로 상위 패키지가 갱신되면 해제한다.

## 참고

- OWASP — Dependency-Check 문서
- NIST — 소프트웨어 공급망 보안 지침
- 각 생태계의 의존성 관리 도구 문서`,
    diagram: {
      type: 'matrix',
      caption: '갱신 처리 방식',
      x: ['보안 관련', '일반'],
      y: ['호환 유지', '변경 있음'],
      cells: ['즉시 병합', '일괄 처리', '우선 검토', '계획 수립'],
    },
    diagram2: {
      type: 'bars',
      caption: '유형별 처리 주기',
      unit: '일',
      items: [
        { label: '보안 갱신', value: 1, note: '즉시' },
        { label: '패치 버전', value: 7, note: '주 1회' },
        { label: '부 버전', value: 30, note: '월 1회' },
        { label: '주 버전', value: 90, note: '분기 계획' },
      ],
    },
  },
  {
    slug: 'test-data-management',
    title: '시험 데이터에서 관계를 유지하며 가명처리',
    body: `운영 데이터를 복사해 시험 환경에 쓰면 편하다. 문제는 시험 환경의 접근 통제가 운영보다 약하고, 개발자 노트북까지 데이터가 퍼진다는 점이다.

## 왜 운영 데이터를 쓰나?

| 이유 | 대안 |
| --- | --- |
| 현실적인 규모 | 대량 생성 도구 |
| 데이터 관계 유지 | 일관된 변환 |
| 예외 사례 포함 | 사례 수집 후 가공 |
| 준비가 쉬움 | 자동화로 해결 |

이유가 정당하므로 금지만으로는 안 된다. 대안을 제공해야 바뀐다.

![운영 데이터와 가공 데이터](/img/posts/test-data-management.svg)

## 가공 방법

| 항목 | 처리 |
| --- | --- |
| 이름 | 사전 기반 대체 |
| 연락처 | 형식 유지 무작위 |
| 이메일 | 도메인 고정, 계정 대체 |
| 주민등록번호 | 유효하지 않은 형식으로 |
| 주소 | 상위 행정구역만 |
| 카드 번호 | 시험용 번호 |
| 금액 | 범위 유지 변형 |

이메일 도메인을 시험용으로 고정하는 것이 중요하다. 실제 주소가 남아 있으면 시험 중 메일이 발송된다.

## 관계를 유지한다

무작위로 바꾸면 조인이 깨진다. 같은 원본 값이 항상 같은 대체 값이 되도록 결정적 변환을 쓴다.

\`\`\`
대체값 = 사전[ HMAC(비밀키, 원본값) % 사전크기 ]
\`\`\`

비밀키를 알면 되돌릴 수 있으므로 키를 안전하게 보관한다. 되돌릴 필요가 없다면 키를 폐기하는 편이 안전하다.

![결정적 변환으로 관계 유지](/img/posts/test-data-management-2.svg)

## 생성 방식도 고려한다

가공보다 생성이 나은 경우가 있다. 규모가 크지 않고 관계가 단순하면 처음부터 만드는 편이 안전하다.

| 방식 | 장점 | 단점 |
| --- | --- | --- |
| 운영 가공 | 현실적 | 유출 위험 잔존 |
| 완전 생성 | 위험 없음 | 현실성 부족 |
| 혼합 | 균형 | 준비 부담 |

## 시험 환경 통제

가공했더라도 시험 환경 자체의 통제가 필요하다.

| 항목 | 설정 |
| --- | --- |
| 접근 | 인증 필수 |
| 외부 노출 | 차단 |
| 외부 발송 | 차단 |
| 결제 연동 | 모의 |
| 보존 기간 | 짧게 |
| 반출 | 금지 |

외부 발송 차단이 특히 중요하다. 가공이 불완전해도 실제 사람에게 도달하지 않는다.

가공 도구를 만들어 두면 반복 비용이 사라진다. 운영 스냅샷을 받아 자동으로 가공해 시험 환경에 넣는 흐름을 만들면, 개발자가 운영 데이터를 직접 만질 이유가 없어진다. 절차가 편해야 지켜진다.

## 실제로 이렇게 터진다

운영 데이터를 그대로 복사한 시험 환경이 인터넷에 노출된 사례가 있다. 접근 통제가 약했고, 실제 고객 정보가 그대로 있었다.

또 하나는 가공했지만 이메일 도메인을 그대로 둔 경우다. 시험 중 실제 고객에게 메일이 나갔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 시험 환경이라 위험이 낮다 | 데이터는 같다 |
| 이름만 바꾸면 된다 | 조합으로 특정된다 |
| 발송은 안 될 것이다 | 설정하지 않으면 나간다 |
| 금지하면 안 쓴다 | 대안이 있어야 바뀐다 |

## 필수 차단

| 항목 | 설정 |
| --- | --- |
| 외부 메일 | 차단 또는 수신함 고정 |
| 결제 | 모의 게이트웨이 |
| 문자·알림 | 차단 |
| 외부 API | 모의 응답 |
| 반출 | 금지 |

## 시험 환경 데이터 점검

가공이 실제로 됐는지 확인한다.

\`\`\`bash
# 실제 형식의 개인정보가 남아 있는지
psql -h test-db -Atc "SELECT count(*) FROM users
                      WHERE email !~ '@example\.(com|org)$'
                         OR phone ~ '^01[016789][0-9]{7,8}$'"
\`\`\`

0 이 아니면 가공이 불완전한 것이다. 특히 이메일 도메인이 실제 값이면 시험 중 메일이 실제로 발송된다.

외부 발송 차단이 걸려 있는지도 함께 확인한다. 가공이 불완전해도 도달하지 않는다면 피해가 없다.

## 데이터 준비 방식 선택

가공과 생성 중 무엇이 나은지는 규모와 관계 복잡도로 정한다.

| 조건 | 선택 |
| --- | --- |
| 규모 크고 관계 복잡 | 가공 |
| 규모 작고 관계 단순 | 생성 |
| 성능 시험 | 생성 + 대량 |
| 특정 사례 재현 | 가공 |
| 외부 공유 | 생성 |

외부에 넘겨야 하는 데이터는 생성이 안전하다. 가공은 아무리 잘해도 원본에서 파생된 것이다.

## 갱신 주기

시험 데이터가 낡으면 실제와 달라져 시험 가치가 떨어진다.

| 항목 | 주기 |
| --- | --- |
| 데이터 갱신 | 분기 |
| 가공 규칙 검토 | 반기 |
| 새 항목 반영 | 스키마 변경 시 |
| 환경 초기화 | 월 |

환경을 주기적으로 초기화하면 시험 중 쌓인 데이터가 정리되고, 초기화 절차 자체도 검증된다.

## 참고

- 개인정보보호위원회 — 가명정보 처리 가이드라인
- OWASP — Test Data Management
- ISO/IEC 27001 — 시험 데이터 보호 통제`,
    diagram: {
      type: 'matrix',
      caption: '데이터 준비 방식',
      x: ['현실성 필요', '불필요'],
      y: ['규모 큼', '작음'],
      cells: ['가공', '생성', '가공', '생성'],
    },
    diagram2: {
      type: 'layers',
      caption: '시험 환경 통제',
      layers: [
        { label: '데이터 가공', note: '개인정보 제거' },
        { label: '접근 통제', note: '인증 필수' },
        { label: '외부 연동 차단', note: '발송·결제' },
        { label: '보존 제한', note: '짧은 주기 초기화' },
      ],
    },
  },
  {
    slug: 'api-versioning-security',
    title: 'API 버전 관리에서 생기는 보안 공백',
    body: `새 버전을 내면서 옛 버전을 남겨 두면 호환성은 유지되지만, 보안 개선이 옛 버전에는 반영되지 않는 경우가 많다. 공격자는 당연히 약한 쪽을 쓴다.

## 엔드포인트 버전 간 어떤 공백이 생기나?

| 상황 | 결과 |
| --- | --- |
| 인가 로직 개선이 신 버전에만 | 옛 버전으로 우회 |
| 입력 검증 강화 미반영 | 옛 경로로 주입 |
| 속도 제한 미적용 | 옛 경로로 대량 요청 |
| 응답 필드 축소 미반영 | 옛 버전이 더 많이 반환 |
| 로그 개선 미반영 | 추적 공백 |

응답 필드가 특히 문제다. 개인정보 노출을 줄이려고 필드를 뺐는데 옛 버전은 그대로 반환한다.

![버전별 보안 수준 차이](/img/posts/api-versioning-security.svg)

## 원칙

1. 보안 관련 변경은 모든 활성 버전에 적용한다
2. 공통 계층에서 처리해 버전별 구현을 피한다
3. 버전마다 종료 일정을 정한다
4. 사용 현황을 측정한다
5. 사용이 없으면 즉시 닫는다

2번이 근본 대응이다. 인가와 검증을 공통 미들웨어에서 처리하면 버전이 달라도 같은 규칙이 적용된다.

## 종료 절차

| 단계 | 기간 |
| --- | --- |
| 종료 예고 | 6개월 전 |
| 응답 헤더 경고 | 3개월 전 |
| 사용자 개별 통지 | 2개월 전 |
| 제한적 차단 | 1개월 전 |
| 완전 종료 | 예정일 |

제한적 차단은 하루 중 일정 시간만 막는 방식이다. 아직 쓰고 있는 곳이 문제를 인지하게 만든다.

![종료 절차](/img/posts/api-versioning-security-2.svg)

## 사용 현황 측정

\`\`\`bash
# 버전별 호출량과 호출 주체 — 종료 판단의 근거
psql -Atc "SELECT api_version, client_id, count(*) c
           FROM api_log WHERE at > now() - interval '30 days'
           GROUP BY 1,2 ORDER BY 1, c DESC"
\`\`\`

주체별로 보면 누구에게 연락해야 하는지 알 수 있다. 소수가 남아 있다면 개별 지원으로 전환을 도울 수 있다.

## 버전을 늘리지 않는 방법

호환성을 깨지 않는 변경은 버전을 올리지 않아도 된다. 필드 추가, 선택적 파라미터, 새 엔드포인트는 기존 클라이언트에 영향이 없다. 버전을 남발하면 관리 대상만 늘어난다.

깨는 변경이 꼭 필요한지 먼저 검토한다. 대부분의 경우 호환을 유지하는 설계가 가능하다.

## 실제로 이렇게 터진다

개인정보 노출을 줄이려고 응답 필드를 뺐지만 옛 버전에는 반영하지 않은 사례가 있다. 클라이언트가 옛 버전을 계속 호출했고 필드는 그대로 나갔다.

또 하나는 종료 일정 없이 버전을 쌓은 경우다. 다섯 개 버전을 동시에 유지하게 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 옛 버전은 곧 안 쓴다 | 계속 쓴다 |
| 보안 수정은 새 버전만 | 공격자는 약한 쪽을 쓴다 |
| 종료하면 항의가 온다 | 예고하면 옮긴다 |
| 버전을 나누면 안전하다 | 관리 대상만 는다 |

## 공통 처리로 옮길 것

| 항목 | 위치 |
| --- | --- |
| 인증 | 공통 계층 |
| 인가 | 공통 계층 |
| 속도 제한 | 공통 계층 |
| 응답 필드 필터 | 공통 계층 |
| 로깅 | 공통 계층 |

## 버전별 사용 현황

종료 판단의 근거가 된다.

\`\`\`bash
psql -Atc "SELECT api_version, count(DISTINCT client_id) 클라이언트, count(*) 호출
           FROM api_log WHERE at > now() - interval '30 days'
           GROUP BY 1 ORDER BY 1"
\`\`\`

클라이언트 수가 적으면 개별 연락으로 전환을 도울 수 있다. 호출은 많은데 클라이언트가 하나면 그쪽만 옮기면 된다.

보안 수정이 모든 활성 버전에 적용됐는지도 확인한다. 공통 계층에서 처리하고 있으면 이 걱정이 사라진다.

## 버전 증가를 억제한다

호환을 깨지 않는 변경은 버전을 올리지 않아도 된다.

| 변경 | 버전 필요 |
| --- | --- |
| 응답 필드 추가 | 불필요 |
| 선택 파라미터 추가 | 불필요 |
| 새 엔드포인트 | 불필요 |
| 응답 필드 제거 | 필요 |
| 필수 파라미터 추가 | 필요 |
| 의미 변경 | 필요 |

필드 제거가 필요하다면 먼저 사용 여부를 확인한다. 아무도 쓰지 않으면 버전을 올리지 않고 제거할 수 있다.

## 종료 판단

언제 닫을지 정하는 기준이 있어야 계속 미루지 않는다.

| 기준 | 판단 |
| --- | --- |
| 호출 0건 30일 | 즉시 종료 |
| 내부 클라이언트만 | 전환 후 종료 |
| 소수 외부 클라이언트 | 개별 지원 후 종료 |
| 다수 외부 | 예고 기간 확대 |

호출이 없는 버전을 열어 두는 것은 공격면만 남기는 것이다. 사용 현황을 정기적으로 보고 정리한다.

## 참고

- OWASP API Security Top 10
- REST API 설계 지침 자료
- 각 사업자의 API 수명 주기 정책`,
    diagram: {
      type: 'matrix',
      caption: '버전 관리 상태',
      x: ['공통 계층 처리', '버전별 구현'],
      y: ['종료 계획 있음', '없음'],
      cells: ['안전', '공백 위험', '부채 누적', '전면 위험'],
    },
    diagram2: {
      type: 'steps',
      caption: '종료 절차',
      steps: [
        { label: '종료 예고', note: '6개월 전' },
        { label: '응답 경고', note: '헤더 표시' },
        { label: '개별 통지', note: '잔여 사용자' },
        { label: '제한적 차단', note: '인지 유도' },
        { label: '완전 종료', note: '예정일' },
      ],
    },
  },
  {
    slug: 'error-message-design',
    title: '오류 메시지에서 정보를 흘리지 않으려면',
    body: `친절한 오류 메시지가 공격자에게도 친절하다. 계정이 존재하는지, 어떤 데이터베이스를 쓰는지, 내부 경로가 무엇인지가 메시지로 드러난다.

## 무엇이 흘러나가나?

| 메시지 | 노출 정보 |
| --- | --- |
| 존재하지 않는 계정입니다 | 계정 존재 여부 |
| 비밀번호가 틀렸습니다 | 계정 존재 확인 |
| 스택 트레이스 | 내부 구조·라이브러리 |
| 데이터베이스 오류 원문 | 스키마·질의 구조 |
| 파일을 찾을 수 없음 | 경로 구조 |
| 권한이 없습니다 | 자원 존재 여부 |

마지막 항목이 자주 간과된다. 없는 자원에 404 를, 권한 없는 자원에 403 을 주면 존재 여부가 드러난다.

![메시지가 드러내는 정보](/img/posts/error-message-design.svg)

## 사용자에게 줄 것

| 상황 | 메시지 |
| --- | --- |
| 로그인 실패 | 아이디 또는 비밀번호가 올바르지 않습니다 |
| 권한 없음 | 요청을 처리할 수 없습니다 |
| 없는 자원 | 요청을 처리할 수 없습니다 |
| 서버 오류 | 일시적인 문제입니다. 코드: A1B2C3 |
| 입력 오류 | 어떤 필드가 왜 잘못됐는지 |

입력 형식 오류는 구체적으로 알려 준다. 이것은 정보 노출이 아니라 사용성 문제다.

## 오류 코드를 준다

사용자에게 짧은 식별 코드를 주고, 상세는 서버 로그에 남긴다. 문의가 오면 그 코드로 로그를 찾는다.

\`\`\`javascript
const ref = randomUUID().slice(0, 8)
logger.error({ ref, err, userId, path })          // 상세는 로그에
res.status(500).json({ message: '일시적인 문제입니다', ref })   // 사용자에게는 코드만
\`\`\`

![상세는 로그로, 사용자에게는 코드만](/img/posts/error-message-design-2.svg)

## 응답 차이도 정보다

메시지를 통일해도 응답 시간이나 상태 코드가 다르면 구분할 수 있다.

| 요소 | 통일 |
| --- | --- |
| 메시지 | 동일하게 |
| 상태 코드 | 동일하게 |
| 응답 시간 | 유사하게 |
| 응답 크기 | 유사하게 |

로그인에서 계정이 없을 때 비밀번호 검증을 건너뛰면 응답이 빨라진다. 없는 계정에도 더미 해시로 검증을 수행해 시간을 맞춘다.

## 환경별 설정

개발 환경에서는 상세 오류가 필요하다. 운영에서만 감추도록 설정하되, 그 설정이 실제로 운영에 적용됐는지 확인한다. 기본값이 상세 표시인 프레임워크가 있다.

배포 후 의도적으로 오류를 발생시켜 응답을 확인하는 절차를 점검 항목에 넣는다.

## 실제로 이렇게 터진다

로그인 실패 메시지를 "존재하지 않는 계정" 과 "비밀번호 오류" 로 나눈 사례가 있다. 공격자가 이 차이로 유효한 계정 목록을 만들었고, 그 목록에 대량 대입을 시도했다.

또 하나는 운영에서 스택 트레이스가 그대로 노출된 경우다. 프레임워크 기본값이 상세 표시였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 친절한 메시지가 좋다 | 공격자에게도 친절하다 |
| 메시지만 통일하면 된다 | 응답 시간도 신호다 |
| 운영은 자동으로 감춘다 | 설정에 달렸다 |
| 오류 코드는 불친절하다 | 문의 처리가 빨라진다 |

## 통일할 것

| 요소 | 처리 |
| --- | --- |
| 메시지 | 동일 |
| 상태 코드 | 동일 |
| 응답 시간 | 유사하게 |
| 응답 크기 | 유사하게 |

## 응답 차이 확인

메시지를 통일해도 다른 신호가 남는다.

\`\`\`bash
for u in "exists@example.com" "nouser@example.com"; do
  printf '%-28s ' "$u"
  curl -s -o /dev/null -w '%{http_code} %{time_total}\n' -X POST \
    -H 'Content-Type: application/json' \
    -d "{\"email\":\"$u\",\"password\":\"wrong\"}" \
    https://api.example.com/login
done
\`\`\`

상태 코드나 응답 시간이 다르면 계정 존재 여부가 드러난다. 없는 계정에도 더미 해시로 검증을 수행해 시간을 맞춘다.

## 오류 유형별 처리

모든 오류를 감출 필요는 없다. 사용자가 고칠 수 있는 것은 구체적으로 알려 준다.

| 유형 | 메시지 |
| --- | --- |
| 입력 형식 오류 | 구체적으로 |
| 필수 항목 누락 | 어떤 항목인지 |
| 권한 없음 | 일반 메시지 |
| 자원 없음 | 일반 메시지 |
| 서버 오류 | 참조 코드만 |
| 외부 연동 실패 | 재시도 안내 |

입력 오류를 감추면 사용성만 나빠지고 보안에는 도움이 안 된다. 사용자가 스스로 고칠 수 있는 정보는 준다.

## 로그와 응답 분리

상세는 로그로, 사용자에게는 코드만 준다.

| 위치 | 내용 |
| --- | --- |
| 응답 | 일반 메시지 + 참조 코드 |
| 서버 로그 | 스택·요청 맥락·사용자 |
| 오류 수집 도구 | 상세, 민감 정보 제외 |
| 알림 | 발생률 급증 시 |

참조 코드로 로그를 찾을 수 있어야 문의 처리가 빨라진다. 코드를 무작위로 만들고 로그에 함께 기록한다.

## 참고

- OWASP — Error Handling Cheat Sheet
- CWE-209 — 민감 정보를 포함한 오류 메시지
- OWASP — Authentication Cheat Sheet`,
    diagram: {
      type: 'matrix',
      caption: '메시지 상세도',
      x: ['운영 환경', '개발 환경'],
      y: ['입력 형식 오류', '내부 오류'],
      cells: ['구체적', '구체적', '코드만', '상세 표시'],
    },
    diagram2: {
      type: 'flow',
      caption: '오류 처리',
      steps: [
        { label: '오류 발생', note: '예외 포착' },
        { label: '참조 코드 생성', note: '무작위' },
        { label: '상세 로그 기록', note: '서버에만' },
        { label: '일반 메시지 응답', note: '코드 포함' },
      ],
    },
  },
  {
    slug: 'rate-limiting-design',
    title: '속도 제한을 요청자와 대상으로 나누기',
    body: `속도 제한은 자동화 공격을 막는 가장 효과적인 수단이지만, 잘못 설계하면 정상 사용자가 막히고 공격자는 우회한다. 무엇을 기준으로 얼마나 제한할지가 설계의 전부다.

## 무엇을 기준으로 세나?

| 기준 | 적합 |
| --- | --- |
| IP 주소 | 익명 요청, 공유 IP 주의 |
| 계정 | 인증된 요청 |
| API 키 | 외부 연동 |
| 대상 자원 | 특정 계정 표적 공격 |
| 조합 | 가장 정확 |

IP 만 쓰면 회사나 통신사 공유 IP 에서 정상 사용자가 함께 막힌다. 인증된 요청은 계정 기준이 정확하다.

![기준별 적용 범위](/img/posts/rate-limiting-design.svg)

## 대상별 제한

| 기능 | 제한 |
| --- | --- |
| 로그인 | 계정당 5회/분, IP 당 20회/분 |
| 비밀번호 재설정 | 계정당 3회/시간 |
| 인증 문자 발송 | 번호당 5회/일 |
| 검색 | 계정당 60회/분 |
| 대량 조회 | 계정당 10회/시간 |
| 일반 API | 계정당 600회/분 |

인증 관련 기능을 강하게 제한하는 것이 기본이다. 정상 사용자는 그렇게 자주 시도하지 않는다.

## 대상 기준도 필요하다

공격자가 여러 계정으로 한 사람을 노리는 경우가 있다. 요청자 기준만으로는 막히지 않는다.

| 공격 | 필요한 기준 |
| --- | --- |
| 한 계정에 비밀번호 대입 | 대상 계정 기준 |
| 한 번호에 문자 폭탄 | 대상 번호 기준 |
| 여러 IP 에서 분산 | 대상 기준 |

![요청자 기준과 대상 기준](/img/posts/rate-limiting-design-2.svg)

## 초과 시 처리

| 방식 | 특징 |
| --- | --- |
| 429 응답 | 표준, 재시도 시각 안내 |
| 지연 응답 | 자동화 비용 증가 |
| 추가 인증 요구 | 사용자 구분 |
| 임시 차단 | 강한 조치 |

재시도 가능 시각을 헤더로 알려 주면 정상 클라이언트가 적절히 대기한다. 알려 주지 않으면 계속 시도해 부하가 는다.

## 우회 대응

| 우회 | 대응 |
| --- | --- |
| IP 변경 | 계정·대상 기준 병행 |
| 느린 분산 시도 | 긴 기간 누적 카운트 |
| 헤더 위조 | 신뢰 프록시만 반영 |
| 다른 경로 | 모든 진입점에 적용 |

전달된 원본 주소 헤더를 검증 없이 믿으면 그것을 바꿔 우회한다. 신뢰하는 프록시가 붙인 값만 사용한다.

제한에 걸린 요청을 기록하고 추이를 본다. 정상 사용자가 자주 걸린다면 한도가 낮은 것이고, 특정 주체가 반복해 걸린다면 공격일 수 있다. 이 로그가 공격 탐지의 좋은 신호가 된다.

## 실제로 이렇게 터진다

IP 기준으로만 제한한 사례가 있다. 사무실 공용 IP 를 쓰는 고객사 직원들이 함께 차단됐고, 정작 분산된 공격은 걸리지 않았다.

또 하나는 대상 기준 제한이 없던 경우다. 여러 계정으로 한 사람의 번호에 인증 문자를 반복 발송했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| IP 기준이면 충분하다 | 공유 IP 와 분산 공격 |
| 계정 기준이면 완전하다 | 대상 표적 공격이 남는다 |
| 429 만 주면 된다 | 재시도 시각을 알려야 한다 |
| 엄격할수록 안전하다 | 정상 사용자가 막힌다 |

## 기준 조합

| 축 | 막는 것 |
| --- | --- |
| 계정 | 한 사람의 남용 |
| IP | 익명 자동화 |
| 대상 | 표적 공격 |
| 전역 | 전체 폭주 |

## 걸린 요청 분석

제한이 정상 사용자를 막고 있는지 본다.

\`\`\`bash
psql -Atc "SELECT rule, count(*) 건수,
                  count(DISTINCT account) 계정수,
                  count(DISTINCT ip) 주소수
           FROM rate_limit_log WHERE at > now() - interval '7 days'
           GROUP BY 1 ORDER BY 건수 DESC"
\`\`\`

계정 수가 많고 주소가 적으면 공유 IP 환경의 정상 사용자가 함께 막히는 것이다. 기준을 계정으로 옮긴다. 반대로 주소가 많고 계정이 적으면 분산 공격이다.

## 한도를 정하는 방법

감으로 정하면 정상 사용자가 막히거나 공격이 통과한다. 실제 분포를 본다.

| 단계 | 방법 |
| --- | --- |
| 1 | 현재 사용량 분포 측정 |
| 2 | 상위 백분위 확인 |
| 3 | 그보다 여유 있게 설정 |
| 4 | 걸린 요청 관찰 |
| 5 | 조정 |

정상 사용자의 99번째 백분위보다 조금 위로 잡으면 대부분 걸리지 않는다. 그보다 훨씬 위는 공격이다.

## 초과 시 응답

차단만 하면 정상 클라이언트가 계속 재시도해 부하가 는다.

| 항목 | 응답 |
| --- | --- |
| 상태 코드 | 429 |
| 재시도 시각 | 헤더로 안내 |
| 남은 한도 | 헤더로 안내 |
| 메시지 | 무엇이 초과됐는지 |

남은 한도를 알려 주면 클라이언트가 스스로 조절한다. 외부 공개 API 에서 특히 유용하다.

## 참고

- OWASP — Denial of Service Cheat Sheet
- RFC 6585 — HTTP 429 상태 코드
- OWASP — Credential Stuffing Prevention`,
    diagram: {
      type: 'matrix',
      caption: '제한 기준 조합',
      x: ['요청자 기준', '없음'],
      y: ['대상 기준', '없음'],
      cells: ['권장', '표적 공격 취약', '분산 공격 취약', '통제 없음'],
    },
    diagram2: {
      type: 'bars',
      caption: '기능별 제한 강도',
      unit: '회/분',
      items: [
        { label: '로그인', value: 5, note: '계정당' },
        { label: '재설정', value: 3, note: '시간당' },
        { label: '검색', value: 60, note: '계정당' },
        { label: '일반 API', value: 600, note: '계정당' },
      ],
    },
  },
  {
    slug: 'graphql-query-limits',
    title: 'GraphQL 에서 질의 비용을 제한하는 법',
    body: `클라이언트가 원하는 구조를 직접 요청하는 방식은 유연하지만, 그 유연성이 그대로 부하 공격 수단이 된다. 중첩된 질의 하나로 서버를 멈추게 할 수 있다.

## 어떤 질의가 위험한가?

| 유형 | 문제 |
| --- | --- |
| 깊은 중첩 | 재귀적 관계 탐색 |
| 넓은 목록 | 대량 데이터 반환 |
| 별칭 반복 | 같은 필드 수백 번 |
| 조각 재귀 | 무한 확장 |
| 배치 요청 | 여러 질의 동시 |

깊은 중첩이 가장 흔하다. 사용자가 게시글을 갖고 게시글이 작성자를 갖는 구조에서 이를 반복하면 지수적으로 커진다.

![중첩 질의가 만드는 부하](/img/posts/graphql-query-limits.svg)

## 제한 수단

| 수단 | 효과 |
| --- | --- |
| 깊이 제한 | 중첩 차단 |
| 복잡도 점수 | 종합 비용 |
| 목록 크기 상한 | 반환량 제한 |
| 별칭 수 제한 | 반복 차단 |
| 시간 제한 | 실행 시간 |
| 허용 질의 목록 | 가장 강함 |

허용 질의 목록 방식은 미리 등록한 질의만 실행하게 한다. 외부 공개 API 가 아니라면 이 방식이 가장 안전하다.

## 복잡도 계산

각 필드에 비용을 부여하고 합계가 상한을 넘으면 거부한다.

\`\`\`
필드 기본 비용     1
목록 필드          1 × 요청 개수
중첩               상위 비용 × 하위 비용
상한               1000
\`\`\`

목록 개수를 곱하는 것이 핵심이다. 개수를 지정하지 않으면 기본값을 적용하고, 그 기본값을 작게 둔다.

![복잡도 기반 차단](/img/posts/graphql-query-limits-2.svg)

## 인트로스펙션

스키마 전체를 조회하는 기능은 개발에 유용하지만 공격자에게도 지도를 준다. 운영에서는 끄거나 인증된 요청에만 허용한다.

| 환경 | 설정 |
| --- | --- |
| 개발 | 허용 |
| 시험 | 허용 |
| 운영 (내부) | 인증 후 허용 |
| 운영 (공개) | 차단 |

## 인가는 별개다

질의 비용을 제한해도 인가는 따로 확인해야 한다. 리졸버마다 요청자가 그 데이터를 볼 수 있는지 판단한다. 상위 필드에서만 확인하고 하위에서 빠뜨리는 실수가 흔하다.

특히 중첩된 관계를 따라갈 때 각 단계에서 권한을 확인한다. 내 게시글의 댓글 작성자 정보처럼 여러 단계를 거치면 중간에 권한 경계를 넘게 된다.

## 실제로 이렇게 터진다

중첩 관계를 따라가는 질의 하나로 서버가 멈춘 사례가 있다. 요청은 한 건이었고 데이터베이스 조회는 수만 건이었다.

또 하나는 인트로스펙션을 운영에서 열어 둔 경우다. 스키마 전체가 공개돼 공격 대상 목록이 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 요청 수 제한이면 된다 | 한 건이 비쌀 수 있다 |
| 깊이 제한이면 충분하다 | 넓이도 문제다 |
| 인트로스펙션은 필요하다 | 운영에서는 아니다 |
| 리졸버는 자동으로 안전하다 | 단계마다 인가가 필요하다 |

## 제한 조합

| 항목 | 설정 |
| --- | --- |
| 깊이 | 상한 |
| 복잡도 | 점수 상한 |
| 목록 크기 | 기본값 작게 |
| 별칭 수 | 상한 |
| 실행 시간 | 상한 |

## 비싼 질의 찾기

복잡도 상한을 정하려면 실제 질의를 봐야 한다.

\`\`\`bash
psql -Atc "SELECT left(operation_name, 40), max(depth) 최대깊이,
                  max(complexity) 최대복잡도, max(duration_ms) 최대시간
           FROM graphql_log WHERE at > now() - interval '7 days'
           GROUP BY 1 ORDER BY 최대복잡도 DESC LIMIT 15"
\`\`\`

정상 질의의 최대값보다 조금 위로 상한을 잡는다. 처음부터 낮게 잡으면 정상 기능이 막힌다. 인트로스펙션이 운영에서 열려 있는지도 함께 확인한다.

## 상한을 정하는 순서

정상 질의를 막지 않으면서 비싼 질의를 거르는 값을 찾는다.

| 순서 | 방법 |
| --- | --- |
| 1 | 실제 질의의 깊이·복잡도 분포 측정 |
| 2 | 정상 최대값 확인 |
| 3 | 여유를 두고 상한 설정 |
| 4 | 경고 모드로 관찰 |
| 5 | 차단으로 전환 |

처음부터 낮게 잡으면 정상 기능이 막힌다. 측정 없이 임의로 정하지 않는다.

## 내부와 외부 구분

같은 스키마라도 노출 대상에 따라 다른 정책을 적용한다.

| 대상 | 정책 |
| --- | --- |
| 내부 서비스 | 허용 질의 목록 |
| 자사 클라이언트 | 허용 질의 목록 |
| 파트너 | 복잡도 제한 |
| 공개 API | 복잡도 + 속도 제한 |
| 인트로스펙션 | 내부만 |

허용 질의 목록 방식은 클라이언트가 우리 것일 때 쓸 수 있다. 가장 강한 통제이면서 성능도 좋다.

## 참고

- OWASP — GraphQL Cheat Sheet
- GraphQL 공식 문서 — 보안 고려사항
- 각 구현체의 복잡도 제한 플러그인 문서`,
    diagram: {
      type: 'bars',
      caption: '제한 수단별 강도',
      unit: '점',
      items: [
        { label: '허용 질의 목록', value: 95, note: '내부용 최적' },
        { label: '복잡도 점수', value: 80, note: '유연성 유지' },
        { label: '깊이 제한', value: 60, note: '단순' },
        { label: '시간 제한', value: 45, note: '보조' },
      ],
    },
    diagram2: {
      type: 'flow',
      caption: '질의 처리',
      steps: [
        { label: '질의 수신', note: '구문 분석' },
        { label: '복잡도 계산', note: '상한 비교' },
        { label: '리졸버 실행', note: '단계별 인가' },
        { label: '응답', note: '크기 제한' },
      ],
    },
  },
  {
    slug: 'mass-assignment-prevention',
    title: '대량 할당을 막는 입출력 구조 분리',
    body: `요청 본문을 객체에 그대로 대입하면, 클라이언트가 보내지 말아야 할 필드까지 들어간다. 사용자 정보 수정 요청에 권한 필드를 끼워 넣어 관리자가 되는 사례가 여기서 나온다.

## 어떻게 일어나나?

프레임워크의 편의 기능이 원인이다. 요청 본문을 모델에 한 번에 대입하는 코드가 짧고 편해서 널리 쓰인다.

\`\`\`javascript
// 위험: 요청에 있는 모든 필드가 반영된다
await User.update(req.body, { where: { id: userId } })

// 안전: 허용 필드만 골라 쓴다
const { displayName, bio } = req.body
await User.update({ displayName, bio }, { where: { id: userId } })
\`\`\`

![요청 필드가 그대로 반영되는 경로](/img/posts/mass-assignment-prevention.svg)

## 위험한 필드

| 필드 | 결과 |
| --- | --- |
| 권한·역할 | 권한 상승 |
| 소유자 식별자 | 다른 사람 데이터로 |
| 결제 상태 | 무료 이용 |
| 인증 여부 | 검증 우회 |
| 생성·수정 시각 | 이력 조작 |
| 내부 상태 | 흐름 우회 |

소유자 식별자가 특히 위험하다. 이 값을 바꾸면 남의 데이터를 만들거나 가져갈 수 있다.

## 방어

| 방법 | 효과 |
| --- | --- |
| 허용 목록 | 가장 확실 |
| 전송 객체 분리 | 구조적 분리 |
| 스키마 검증 | 알 수 없는 필드 제거 |
| 차단 목록 | 새 필드 누락 위험 |

차단 목록은 새 필드가 추가될 때마다 갱신해야 해서 언젠가 빠진다. 허용 목록이 안전하다.

![허용 목록과 차단 목록](/img/posts/mass-assignment-prevention-2.svg)

## 전송 객체 분리

입력 전용 구조를 따로 정의하면 모델과 분리된다. 모델에 필드가 추가돼도 입력 구조는 그대로이므로 자동으로 안전하다.

\`\`\`
UserUpdateRequest  { displayName, bio }        ← 클라이언트가 보낼 수 있는 것
User               { id, email, role, ... }    ← 저장 모델
\`\`\`

## 응답도 같은 문제가 있다

모델을 그대로 응답으로 내보내면 내부 필드가 노출된다. 비밀번호 해시, 내부 상태, 다른 사용자 식별자가 포함될 수 있다. 응답도 전용 구조로 변환한다.

새 필드를 모델에 추가할 때 응답에 자동으로 포함되는 구조라면, 언젠가 노출하지 말아야 할 값이 나간다.

## 점검

\`\`\`bash
# 요청 본문을 통째로 넘기는 패턴
grep -rnE '\(req\.body[,)]|\(request\.body[,)]' src/ | head -20
\`\`\`

## 실제로 이렇게 터진다

프로필 수정 요청에 권한 필드를 넣자 그대로 반영된 사례가 있다. 일반 사용자가 관리자가 됐고, 감사 로그에는 정상적인 프로필 수정으로만 남았다.

또 하나는 응답에 모델을 그대로 내보낸 경우다. 새 필드를 추가하자 내부 상태 값이 함께 노출됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 화면에 없으면 안 보낸다 | 요청은 직접 만든다 |
| 차단 목록이면 된다 | 새 필드가 빠진다 |
| 요청만 조심하면 된다 | 응답도 같은 문제다 |
| 프레임워크가 막아 준다 | 편의 기능이 원인이다 |

## 입출력 전용 구조

| 방향 | 구조 |
| --- | --- |
| 입력 | 허용 필드만 정의 |
| 출력 | 공개 필드만 정의 |
| 모델 | 저장 전용 |
| 변환 | 명시적으로 |

## 위험 패턴 찾기

요청 본문을 통째로 넘기는 곳을 훑는다.

\`\`\`bash
grep -rnE '\(req\.body[,)]|\(request\.body[,)]|Object\.assign\([^,]+, *req\.' src/ | head -20
\`\`\`

각 위치에서 대상 모델에 권한이나 소유자 필드가 있는지 확인한다. 있으면 허용 목록으로 바꾼다.

응답 쪽도 같은 방식으로 본다. 모델을 그대로 직렬화하는 코드는 새 필드가 추가될 때 함께 노출된다.

## 프레임워크별 확인

편의 기능의 기본 동작이 다르다. 쓰는 프레임워크의 동작을 확인한다.

| 항목 | 확인 |
| --- | --- |
| 요청 바인딩 기본 동작 | 전체 필드인가 |
| 허용 목록 기능 | 있는가 |
| 알 수 없는 필드 | 무시·거부 |
| 중첩 객체 | 어떻게 처리 |
| 응답 직렬화 | 전체 필드인가 |

기본 동작이 전체 바인딩이면 명시적으로 제한해야 한다. 편의 기능이 곧 취약점이 되는 구조다.

## 입출력 구조 분리

모델과 전송 구조를 나누면 문제가 구조적으로 사라진다.

| 구조 | 역할 |
| --- | --- |
| 요청 구조 | 받을 수 있는 필드만 |
| 응답 구조 | 보낼 수 있는 필드만 |
| 도메인 모델 | 업무 로직 |
| 저장 모델 | 영속화 |

모델에 필드가 추가돼도 전송 구조는 그대로이므로 자동으로 안전하다. 변환 코드가 늘어나지만 그 비용이 사고보다 싸다.

## 참고

- OWASP API Security Top 10 — Mass Assignment
- CWE-915 — 속성의 부적절한 수정
- 각 프레임워크의 강한 파라미터 문서`,
    diagram: {
      type: 'flow',
      caption: '대량 할당',
      steps: [
        { label: '요청 본문 수신', note: '임의 필드 포함' },
        { label: '모델에 일괄 대입', note: '필터 없음' },
        { label: '권한 필드 반영', note: '의도치 않음' },
        { label: '권한 상승', note: '관리자 획득', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '방어 방식',
      x: ['허용 목록', '차단 목록'],
      y: ['전용 구조', '모델 직접'],
      cells: ['안전', '누락 위험', '비교적 안전', '위험'],
    },
  },
]
