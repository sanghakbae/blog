import type { SeedPost } from './types'

/** 530~539 — 개발 환경과 비밀값 */
export const posts54: SeedPost[] = [
  {
    slug: 'staging-data-hygiene',
    title: '시험 환경에 운영 데이터를 두지 않는 법',
    body: `시험 환경에 운영 데이터를 복사해 넣는 이유는 분명하다. 진짜 데이터라야 진짜 문제가 보인다. 그런데 그 순간 시험 환경은 운영과 같은 위험을 갖게 되고, 보호 수준은 운영보다 훨씬 낮다.

## 무엇이 달라지나?

같은 데이터인데 둘러싼 통제가 다르다. 공격자는 같은 정보를 더 쉬운 쪽에서 가져간다.

| 항목 | 운영 | 시험 |
| --- | --- | --- |
| 접근 권한 | 최소 인원 | 개발 전원 |
| 다단계 인증 | 필수 | 대개 없음 |
| 접속 기록 | 남김 | 안 남기는 경우 |
| 외부 노출 | 차단 | 편의상 열림 |
| 백업 보호 | 암호화 | 방치 |

시험 환경은 자주 지워졌다 만들어지므로 통제를 붙일 동기가 약하다. 그 약한 동기가 그대로 구멍이 된다.

![같은 데이터, 다른 보호 수준](/img/posts/staging-data-hygiene.svg)

## 개인정보를 어떻게 다루나?

복사하지 않는 것이 첫 번째 선택이다. 그래도 필요하다면 식별할 수 없게 바꿔서 넣는다.

| 방법 | 쓸모 | 안전성 |
| --- | --- | --- |
| 그대로 복사 | 가장 높음 | 가장 낮음 |
| 일부 항목만 제거 | 높음 | 낮음 |
| 가명처리 | 보통 | 높음 |
| 합성 데이터 | 낮음 | 가장 높음 |

항목 몇 개만 지우는 방식은 안전해 보이지만 재식별이 쉽다. 생년월일·성별·지역 셋만 남아도 상당수가 특정된다.

## 관계를 유지하면서 바꾸기

가명처리에서 가장 자주 깨지는 것이 데이터 사이의 연결이다. 같은 사람을 가리키던 값이 테이블마다 다르게 바뀌면 시험이 되지 않는다.

| 요건 | 방법 |
| --- | --- |
| 같은 값은 같게 | 결정적 변환 |
| 형식 유지 | 자릿수·패턴 보존 |
| 분포 유지 | 구간별 치환 |
| 되돌릴 수 없게 | 변환 키는 폐기 |

변환 키를 남겨 두면 가명처리가 아니라 암호화다. 되돌릴 수 있으면 원본과 같은 보호가 필요하다.

![가명처리에서 지킬 것](/img/posts/staging-data-hygiene-2.svg)

## 지금 들어 있는지 확인한다

시험 데이터베이스에 실제 값이 섞여 있는지 표본으로 본다.

\`\`\`sql
-- 형식이 실제 값처럼 보이는 행이 있는지
SELECT count(*) AS 실제형식_이메일
FROM users
WHERE email ~ '^[a-z0-9._%+-]+@(gmail|naver|daum|kakao)\\.(com|net)$';

-- 전화번호가 010 으로 시작하는 실제 형식인지
SELECT count(*) AS 실제형식_전화 FROM users WHERE phone ~ '^010[0-9]{8}$';
\`\`\`

0 이 아니면 운영 데이터가 섞여 있을 가능성이 높다. 가명처리된 데이터는 형식은 맞추되 실제 도메인이나 번호 대역을 쓰지 않는 편이 구분에 좋다.

## 복사 절차 자체를 통제한다

사람이 그때그때 복사하면 규칙이 지켜지지 않는다. 운영에서 시험으로 내려보내는 경로를 하나로 만들고 그 경로가 반드시 가명처리를 거치게 한다. 경로가 하나면 검사할 곳도 하나다. 여러 사람이 각자 덤프를 뜨는 상태에서는 무엇이 어디에 있는지 아무도 모른다.

## 참고

- 개인정보보호법 — 가명정보의 처리
- ISO/IEC 27002 — 시험 데이터의 보호
- NIST SP 800-122 — 개인식별정보 보호 지침`,
    diagram: {
      type: 'matrix',
      caption: '보호 수준의 차이',
      x: ['운영', '시험'],
      y: ['실제 데이터', '가공 데이터'],
      cells: ['통제됨', '가장 위험', '해당 없음', '안전'],
    },
    diagram2: {
      type: 'steps',
      caption: '가명처리에서 지킬 것',
      steps: [
        { label: '같은 값은 같게', note: '관계 유지' },
        { label: '형식 유지', note: '시험 가능' },
        { label: '분포 유지', note: '성능 재현' },
        { label: '변환 키 폐기', note: '되돌릴 수 없게' },
      ],
    },
  },
  {
    slug: 'demo-account-risk',
    title: '시연 계정이 오래 남아 생기는 일',
    body: `시연이나 교육을 위해 만든 계정은 만들 때의 목적이 끝나도 지워지지 않는다. 비밀번호는 팀 안에서 공유되고, 권한은 시연에 필요했던 만큼 넓다. 그 조합이 몇 달 남으면 공격자가 가장 먼저 찾는 문이 된다.

## 왜 위험한가?

시연 계정은 보안을 일부러 느슨하게 만든 계정이다. 그 느슨함이 목적을 잃은 뒤에도 남는다.

| 특징 | 결과 |
| --- | --- |
| 쉬운 비밀번호 | 추측 가능 |
| 여러 사람이 공유 | 누가 썼는지 모름 |
| 다단계 인증 면제 | 자격 증명만으로 진입 |
| 넓은 권한 | 진입 후 이동이 쉬움 |
| 만료 없음 | 영원히 남음 |

여러 사람이 공유한다는 점이 특히 나쁘다. 사고가 나도 누가 한 일인지 특정할 수 없어 대응이 멈춘다.

![시연 계정이 남는 경로](/img/posts/demo-account-risk.svg)

## 계정 회수를 어떻게 보장하나

지우는 것을 사람의 기억에 맡기면 반드시 남는다. 만들 때 끝나는 시점을 함께 정한다.

| 방법 | 효과 |
| --- | --- |
| 만료일 필수 입력 | 자동 정지 |
| 목적·요청자 기록 | 회수 판단 근거 |
| 분기 점검 | 놓친 것 회수 |
| 사용 없으면 정지 | 방치 계정 정리 |

만료일 없이는 만들 수 없게 하는 것이 가장 확실하다. 연장이 필요하면 다시 요청하게 하면 된다.

## 남겨야 한다면 이렇게

시연 계정을 계속 써야 하는 사정이 있다면 위험을 좁힌다.

| 조치 | 내용 |
| --- | --- |
| 데이터 분리 | 시연 전용 데이터만 |
| 권한 축소 | 읽기 위주 |
| 접근 경로 제한 | 특정 대역에서만 |
| 행위 기록 | 전부 남김 |
| 주기적 비밀번호 교체 | 공유 범위 정리 |

![위험을 좁히는 방법](/img/posts/demo-account-risk-2.svg)

## 지금 몇 개나 있는지 센다

이름으로 짐작되는 것부터 찾는다.

\`\`\`bash
# 흔한 이름 패턴
getent passwd | awk -F: '$1 ~ /demo|test|guest|sample|poc|temp/ {print $1}'

# 오래 쓰지 않은 계정 — 목적이 끝난 것들
lastlog -b 60 2>/dev/null | tail -n +2 | awk '{print $1}' | head -20
\`\`\`

이름으로 찾는 것은 시작일 뿐이다. 진짜 목록은 계정을 만들 때 기록한 목적 필드에서 나온다. 그 필드가 없다면 지금 만드는 것이 다음 정리를 가능하게 한다.

## 서비스 계정도 같은 문제다

사람 계정만이 아니다. 연동 시험을 위해 만든 API 키나 서비스 계정도 같은 성격을 갖는다. 오히려 사람 눈에 덜 띄어 더 오래 남는다. 계정 목록을 만들 때 사람과 기계를 같은 목록에 넣고 같은 기준으로 회수한다.

## 참고

- NIST SP 800-53 — 계정 관리와 비활성 계정
- CIS Controls — 계정 목록과 회수
- ISO/IEC 27002 — 접근권한의 검토`,
    diagram: {
      type: 'flow',
      caption: '시연 계정이 남는 경로',
      steps: [
        { label: '시연용 생성', note: '느슨한 설정' },
        { label: '목적 종료', note: '아무도 안 지움' },
        { label: '비밀번호 공유 확산', note: '추적 불가' },
        { label: '침입 경로', note: '가장 쉬운 문', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '남겨야 한다면',
      layers: [
        { label: '데이터 분리', note: '시연 전용' },
        { label: '권한 축소', note: '읽기 위주' },
        { label: '경로 제한', note: '특정 대역' },
        { label: '행위 기록', note: '전부' },
      ],
    },
  },
  {
    slug: 'seed-data-in-repo',
    title: '저장소에 들어간 초기 데이터의 문제',
    body: `개발을 시작할 때 넣는 초기 데이터는 편의를 위한 것이다. 그런데 그 파일이 저장소에 남으면 계정과 값이 코드와 함께 배포되고, 저장소를 보는 모든 사람이 그것을 본다.

## 무엇이 들어가나?

초기 데이터에는 동작을 확인하기 위한 값이 들어간다. 그 값이 운영까지 따라간다.

| 내용 | 위험 |
| --- | --- |
| 관리자 계정·비밀번호 | 운영에 그대로 생성 |
| API 키 예시 | 실제 키가 섞임 |
| 실제 고객 표본 | 개인정보 유출 |
| 내부 주소·호스트명 | 구조 노출 |
| 시험용 결제 정보 | 결제 경로 노출 |

가장 흔한 사고는 초기 관리자 계정이 운영에 그대로 만들어지는 것이다. 이름과 비밀번호가 저장소에 적혀 있으므로 누구나 들어올 수 있다.

![초기 데이터가 운영까지 가는 경로](/img/posts/seed-data-in-repo.svg)

## 저장소 설정으로 막는 것

파일이 들어가는 것 자체를 막는 편이 지우는 것보다 낫다. 한 번 들어가면 이력에 남아 지워도 사라지지 않는다.

| 수단 | 시점 |
| --- | --- |
| 무시 목록 | 실수로 추가 방지 |
| 커밋 전 검사 | 올라가기 전 |
| 저장소 검사 | 올라간 뒤 발견 |
| 이력 정리 | 마지막 수단 |

이력 정리는 모든 사람이 다시 받아야 하고 참조가 깨진다. 그래서 앞 단계에서 막는 것이 훨씬 싸다.

## 초기 데이터를 안전하게 두는 법

없앨 수 없다면 값을 빼고 구조만 남긴다.

| 항목 | 처리 |
| --- | --- |
| 비밀번호 | 실행 시 무작위 생성 |
| API 키 | 빈 값, 환경 변수에서 |
| 고객 데이터 | 합성 데이터 |
| 관리자 계정 | 운영에서는 만들지 않음 |

운영 환경에서는 초기 데이터 자체가 실행되지 않게 하는 것이 가장 확실하다. 환경을 보고 건너뛰는 조건 한 줄이면 된다.

![값을 빼는 방법](/img/posts/seed-data-in-repo-2.svg)

## 지금 들어 있는지 훑는다

\`\`\`bash
# 초기 데이터 파일에 값처럼 보이는 것이 있는지
grep -rniE 'password|passwd|secret|api[_-]?key|token' \\
  --include='*seed*' --include='*fixture*' --include='*sample*' . \\
  | grep -v node_modules | head -20

# 이력에 있었던 적이 있는지 — 지금 없어도 과거에 있었을 수 있다
git log --all --full-history -p -- '*seed*' | grep -niE 'password|secret' | head -10
\`\`\`

이력에서 나오면 그 값은 이미 노출된 것으로 보고 폐기한다. 파일을 지운 것만으로는 아무것도 해결되지 않는다.

## 편의와 안전이 부딪히는 자리

초기 데이터는 새로 합류한 사람이 빨리 시작하게 해 준다. 그 가치는 진짜다. 그래서 없애자는 결론보다, 값을 빼고 생성 절차를 자동화하는 쪽이 오래간다. 실행할 때마다 무작위 비밀번호를 만들고 화면에 한 번 보여 주면, 편의는 유지하면서 저장소에는 아무것도 남지 않는다.

## 참고

- OWASP — 안전하지 않은 기본 설정
- CIS Controls — 기본 계정 관리
- NIST SP 800-53 — 초기 인증자 관리`,
    diagram: {
      type: 'flow',
      caption: '초기 데이터가 운영까지 가는 경로',
      steps: [
        { label: '개발 편의로 작성', note: '계정·키 포함' },
        { label: '저장소에 커밋', note: '이력에 영구' },
        { label: '배포에 포함', note: '운영에서 실행' },
        { label: '기본 계정 생성', note: '누구나 진입', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '값을 빼는 방법',
      steps: [
        { label: '비밀번호 무작위', note: '실행 시 생성' },
        { label: '키는 환경 변수', note: '빈 값 유지' },
        { label: '합성 데이터', note: '실제 값 없음' },
        { label: '운영에서 건너뛰기', note: '조건 한 줄' },
      ],
    },
  },
  {
    slug: 'local-dev-credentials',
    title: '개발자 PC 의 자격 증명 관리',
    body: `개발자 노트북에는 여러 환경의 접근 수단이 쌓인다. 클라우드 키, 데이터베이스 비밀번호, 외부 서비스 토큰이 평문 파일로 흩어져 있다. 노트북 한 대가 그 전부에 이르는 열쇠 꾸러미가 된다.

## 어디에 쌓이나?

의식하고 저장한 것보다 도구가 자동으로 남긴 것이 많다.

| 위치 | 내용 |
| --- | --- |
| 홈 디렉터리 설정 파일 | 클라우드 키 |
| 셸 이력 | 명령에 붙인 비밀번호 |
| 도구 캐시 | 세션 토큰 |
| IDE 설정 | 데이터베이스 접속 정보 |
| 프로젝트 환경 파일 | 각종 키 |

셸 이력이 가장 자주 잊힌다. 명령 한 줄에 비밀번호를 붙여 실행한 기록이 평문으로 몇 달 남는다.

![자격 증명이 쌓이는 자리](/img/posts/local-dev-credentials.svg)

## 무엇부터 줄이나

전부 없앨 수는 없다. 오래 사는 것부터 짧게 만든다.

| 유형 | 조치 |
| --- | --- |
| 영구 클라우드 키 | 임시 자격 증명으로 |
| 운영 DB 비밀번호 | 배스천 경유·개인 계정 |
| 외부 서비스 토큰 | 만료 짧게 |
| 개인 SSH 키 | 암호 구문 필수 |

영구 키를 임시 자격 증명으로 바꾸는 것이 효과가 가장 크다. 유출돼도 몇 시간 뒤에는 쓸모가 없어진다.

## 디스크 자체를 보호한다

파일을 아무리 정리해도 노트북을 잃어버리면 끝이다. 디스크 암호화는 그 상황에서 유일하게 남는 방어다.

| 항목 | 상태 확인 |
| --- | --- |
| 디스크 암호화 | 켜져 있는가 |
| 화면 잠금 | 자동 잠금 시간 |
| 원격 삭제 | 등록돼 있는가 |
| 백업 암호화 | 백업도 보호되는가 |

![분실 시 남는 방어](/img/posts/local-dev-credentials-2.svg)

## 지금 무엇이 있는지 본다

\`\`\`bash
# 흔한 자격 증명 파일
ls -la ~/.aws/credentials ~/.config/gcloud ~/.kube/config ~/.docker/config.json 2>/dev/null

# 셸 이력에 남은 값 — 있으면 그 값은 폐기 대상이다
grep -niE 'password=|token=|secret=|api[_-]?key=' ~/.bash_history ~/.zsh_history 2>/dev/null | head

# 프로젝트마다 흩어진 환경 파일
find ~ -name '.env*' -not -path '*/node_modules/*' 2>/dev/null | head -20
\`\`\`

이력에서 값이 나오면 그 자격 증명은 이미 평문으로 몇 달 보관된 셈이다. 지우는 것과 함께 폐기하고 새로 받는다.

## 조직이 해 줘야 하는 것

개인에게 관리를 맡기면 사람마다 수준이 다르다. 임시 자격 증명을 발급하는 경로를 만들고, 그 경로가 직접 키를 받는 것보다 편해야 바뀐다. 불편한 안전한 방법과 편한 위험한 방법이 함께 있으면 사람들은 편한 쪽을 쓴다. 이것은 개인의 보안 의식 문제가 아니라 도구 설계 문제다.

## 참고

- NIST SP 800-63B — 인증자 수명 관리
- CIS Controls — 자격 증명 보호
- OWASP — Secrets Management Cheat Sheet`,
    diagram: {
      type: 'layers',
      caption: '자격 증명이 쌓이는 자리',
      layers: [
        { label: '설정 파일', note: '클라우드 키' },
        { label: '셸 이력', note: '평문으로 남음' },
        { label: '도구 캐시', note: '세션 토큰' },
        { label: '환경 파일', note: '프로젝트마다' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '분실 시 남는 방어',
      unit: '점',
      items: [
        { label: '디스크 암호화', value: 90, note: '핵심' },
        { label: '임시 자격 증명', value: 75, note: '수명 제한' },
        { label: '화면 잠금', value: 40, note: '보조' },
        { label: '파일 정리', value: 25, note: '분실엔 무력' },
      ],
    },
  },
  {
    slug: 'env-file-handling',
    title: '환경 파일을 다루는 규칙과 함정',
    body: `환경 파일은 설정과 비밀값을 코드에서 떼어 놓는 방법이다. 그런데 파일 하나에 둘이 섞여 있어서, 설정을 공유하려다 비밀값까지 함께 나가는 사고가 반복된다.

## 어디서 새나?

파일 자체는 저장소에 없는데도 내용이 나가는 경로가 여럿 있다.

| 경로 | 상황 |
| --- | --- |
| 실수로 커밋 | 무시 목록에 없는 변형 이름 |
| 메신저로 공유 | 새 팀원에게 전달 |
| 배포 이미지에 포함 | 빌드 맥락에 들어감 |
| 웹 서버가 서빙 | 문서 루트에 위치 |
| 오류 화면에 노출 | 설정 덤프 |

무시 목록에 **.env** 만 적어 두고 **.env.local** 이나 **.env.production** 은 빠뜨리는 경우가 흔하다.

![환경 파일이 새는 경로](/img/posts/env-file-handling.svg)

## 설정과 비밀값을 나눈다

둘을 한 파일에 두면 공유할 때마다 판단이 필요하다. 나눠 두면 판단이 필요 없다.

| 구분 | 예 | 보관 |
| --- | --- | --- |
| 설정 | 포트, 기능 켜기 | 저장소에 두어도 됨 |
| 참조 | 엔드포인트 주소 | 환경별 파일 |
| 비밀값 | 키, 비밀번호 | 시크릿 저장소 |

비밀값을 시크릿 저장소에 두면 환경 파일은 공유해도 되는 것만 남는다. 그때부터 실수가 사고로 이어지지 않는다.

## 예시 파일을 둔다

무엇이 필요한지 알려 주되 값은 주지 않는 파일을 둔다. 새로 합류한 사람이 그것을 보고 채운다.

| 파일 | 내용 | 저장소 |
| --- | --- | --- |
| 예시 파일 | 키 이름만 | 포함 |
| 실제 파일 | 값 포함 | 제외 |
| 문서 | 값을 어디서 받는지 | 포함 |

![나누는 기준](/img/posts/env-file-handling-2.svg)

## 지금 상태를 확인한다

\`\`\`bash
# 무시 목록이 변형 이름까지 덮는가
git check-ignore -v .env .env.local .env.production .env.test 2>&1 | head

# 이력에 들어간 적이 있는지
git log --all --oneline -- '.env*' | head

# 웹에서 접근되는지 — 200 이면 즉시 조치 대상이다
curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/.env
\`\`\`

웹에서 200 이 오면 그 안의 값은 전부 폐기 대상이다. 자동화된 스캐너가 이 경로를 늘 훑고 있어, 열려 있었다면 이미 읽혔다고 보는 편이 안전하다.

## 값이 바뀌었는지 알기 어렵다

환경 파일은 배포와 따로 움직여서 언제 무엇이 바뀌었는지 기록이 남지 않는다. 장애가 났을 때 코드는 그대로인데 동작이 다른 상황이 생긴다. 시크릿 저장소를 쓰면 변경 이력이 남아 이 문제도 함께 풀린다 — 비밀값을 옮길 이유가 유출 방지만은 아니다.

## 참고

- OWASP — Secrets Management Cheat Sheet
- 12 Factor App — 설정의 분리
- CIS Controls — 구성 정보 보호`,
    diagram: {
      type: 'flow',
      caption: '환경 파일이 새는 경로',
      steps: [
        { label: '변형 이름 생성', note: '.env.local' },
        { label: '무시 목록에 없음', note: '커밋됨', danger: true },
        { label: '이력에 영구 보관', note: '지워도 남음' },
        { label: '저장소 열람자 전원', note: '값 노출' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '나누는 기준',
      x: ['공유해도 됨', '안 됨'],
      y: ['환경마다 다름', '같음'],
      cells: ['환경별 파일', '시크릿 저장소', '저장소에 포함', '시크릿 저장소'],
    },
  },
  {
    slug: 'docker-build-secret',
    title: '이미지 빌드에서 비밀값이 남는 자리',
    body: `빌드 중에 사설 저장소에 접근하려면 자격 증명이 필요하다. 그 값을 어떻게 넣느냐에 따라 최종 이미지에 남기도 하고 남지 않기도 한다. 남은 값은 이미지를 받은 누구나 꺼낼 수 있다.

## 왜 지워도 남나?

컨테이너 이미지는 층으로 쌓인다. 앞 층에서 만든 파일을 뒤 층에서 지워도 앞 층은 그대로 남는다.

| 방식 | 최종 이미지 |
| --- | --- |
| 파일로 복사 후 삭제 | 앞 층에 남음 |
| 빌드 인자로 전달 | 이미지 메타데이터에 남음 |
| 환경 변수로 설정 | 실행 환경에 노출 |
| 빌드 시크릿 마운트 | 남지 않음 |

빌드 인자가 특히 함정이다. 코드에는 보이지 않는데 이미지 정보를 조회하면 그대로 나온다.

![층에 남는 값](/img/posts/docker-build-secret.svg)

## 베이스 이미지 선택도 영향을 준다

최종 이미지에 무엇이 들어 있느냐는 베이스 이미지가 절반을 정한다. 도구가 많이 들어 있으면 값을 꺼내기도 쉽다.

| 베이스 | 크기 | 공격 표면 |
| --- | --- | --- |
| 전체 배포판 | 큼 | 셸·패키지 관리자 포함 |
| 슬림 | 보통 | 일부 도구 |
| 최소 | 작음 | 셸 없음 |
| 스크래치 | 최소 | 실행 파일만 |

셸이 없으면 이미지를 받아 안에서 뒤지는 일이 크게 어려워진다.

## 올바르게 넣는 방법

빌드 전용 시크릿 마운트를 쓰면 그 층에서만 값이 보이고 이미지에는 남지 않는다.

| 단계 | 방법 |
| --- | --- |
| 빌드 중 접근 | 시크릿 마운트 |
| 여러 단계 빌드 | 앞 단계에서만 사용 |
| 최종 단계 | 결과물만 복사 |
| 실행 시 값 | 오케스트레이터가 주입 |

![여러 단계로 나누기](/img/posts/docker-build-secret-2.svg)

## 이미지에 남았는지 확인한다

만든 이미지를 받아 직접 뒤져 본다.

\`\`\`bash
# 빌드 인자와 환경 변수가 메타데이터에 남았는지
docker image inspect myapp:latest --format '{{json .Config.Env}}'
docker history --no-trunc myapp:latest | grep -iE 'secret|token|password|key' | head

# 층을 풀어 파일을 훑는다
docker save myapp:latest | tar -xO 2>/dev/null | grep -aoE '[A-Za-z0-9_-]{32,}' | head -5
\`\`\`

**docker history** 에 값이 그대로 보이면 그 값은 이미지를 받은 모두가 본 것이다. 이미지를 다시 만드는 것과 별개로 자격 증명을 폐기한다.

## 이미지 스캔을 붙이는 자리

빌드 직후에 검사하면 문제가 있는 이미지가 저장소에 올라가지 않는다. 배포 직전에 검사하면 이미 퍼진 뒤다. 파이프라인에서 검사 위치를 앞으로 당기는 것만으로 대응 비용이 크게 준다.

## 참고

- Docker 문서 — BuildKit 빌드 시크릿
- NIST SP 800-190 — 컨테이너 보안 지침
- CIS Docker Benchmark`,
    diagram: {
      type: 'layers',
      caption: '층에 남는 값',
      layers: [
        { label: '복사한 층', note: '값이 들어감' },
        { label: '삭제한 층', note: '앞 층은 그대로' },
        { label: '메타데이터', note: '빌드 인자 노출' },
        { label: '최종 이미지', note: '누구나 꺼냄' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '여러 단계로 나누기',
      steps: [
        { label: '빌드 단계', note: '시크릿 마운트' },
        { label: '산출물 생성', note: '값은 여기까지' },
        { label: '최종 단계', note: '결과물만 복사' },
        { label: '실행 시 주입', note: '이미지 밖에서' },
      ],
    },
  },
  {
    slug: 'ci-artifact-retention',
    title: '빌드 산출물을 얼마나 보관할 것인가',
    body: `파이프라인이 남기는 산출물은 조용히 쌓인다. 빌드 로그, 시험 보고서, 패키지, 컨테이너 이미지가 매 빌드마다 생기고 아무도 지우지 않는다. 비용 문제로 시작해서 보안 문제로 끝난다.

## 무엇이 위험한가?

산출물에는 빌드 당시의 환경이 담긴다. 그 안에 값이 섞여 있고, 보관 기간이 길수록 노출 창이 넓어진다.

| 산출물 | 담길 수 있는 것 |
| --- | --- |
| 빌드 로그 | 환경 변수 출력·연결 문자열 |
| 시험 보고서 | 시험 데이터·응답 본문 |
| 패키지 | 소스 맵·설정 파일 |
| 컨테이너 이미지 | 층에 남은 값 |
| 커버리지 | 소스 경로·구조 |

빌드 로그가 가장 자주 샌다. 디버그 모드로 돌린 한 번의 빌드에 환경 변수가 통째로 출력되고, 그 로그가 몇 년 남는다.

![산출물에 담기는 것](/img/posts/ci-artifact-retention.svg)

## 얼마나 두어야 하나

용도별로 다르다. 되돌릴 가능성이 있는 것만 길게 둔다.

| 산출물 | 보관 |
| --- | --- |
| 배포된 버전의 패키지 | 롤백 기간만큼 |
| 배포되지 않은 빌드 | 며칠 |
| 빌드 로그 | 30~90일 |
| 시험 보고서 | 30일 |
| 이미지 | 운영 중인 태그 + 직전 몇 개 |

배포되지 않은 빌드의 산출물은 거의 쓸모가 없다. 그런데 수적으로는 가장 많다.

## 접근 권한을 함께 본다

보관 기간만 줄이고 권한을 그대로 두면 절반만 해결된다. 산출물 저장소는 대개 사내 전체에 열려 있다.

| 대상 | 권한 |
| --- | --- |
| 배포 담당 | 읽기·쓰기 |
| 개발 팀 | 자기 프로젝트 읽기 |
| 전사 | 없음 |
| 외부 | 없음 |

![보관 기간과 노출](/img/posts/ci-artifact-retention-2.svg)

## 지금 쌓여 있는 양을 본다

\`\`\`bash
# GitHub Actions 산출물 용량 상위
gh api repos/:owner/:repo/actions/artifacts --paginate \\
  -q '.artifacts[] | "\\(.size_in_bytes) \\(.name) \\(.created_at)"' \\
  | sort -rn | head -10

# 오래된 것부터 지운다 (확인 후)
gh api repos/:owner/:repo/actions/artifacts -q '.artifacts[] | select(.expired==false) | .id' | head
\`\`\`

용량 상위 몇 개가 전체의 대부분이면 그 파이프라인의 설정만 고쳐도 해결된다. 전체 정책을 바꾸기 전에 그것부터 본다.

## 로그에 값이 찍히지 않게

보관 기간을 줄이는 것보다 애초에 찍히지 않게 하는 것이 낫다. 대부분의 CI 도구가 등록된 비밀값을 출력에서 가려 주지만, 값이 가공되거나 인코딩되면 가려지지 않는다. base64 로 바꾼 값이 그대로 로그에 찍히는 경우가 흔하다. 가림 기능을 믿되 의존하지는 않는다.

## 참고

- OWASP — CI/CD 보안 위험
- NIST SP 800-53 — 감사 기록 보관
- CIS Controls — 데이터 보관 정책`,
    diagram: {
      type: 'layers',
      caption: '산출물에 담기는 것',
      layers: [
        { label: '빌드 로그', note: '환경 변수' },
        { label: '시험 보고서', note: '응답 본문' },
        { label: '패키지', note: '소스 맵' },
        { label: '이미지', note: '층에 남은 값' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '보관 기간 권장',
      unit: '일',
      items: [
        { label: '배포 패키지', value: 180, note: '롤백 대비' },
        { label: '빌드 로그', value: 60, note: '조사용' },
        { label: '시험 보고서', value: 30, note: '추세' },
        { label: '미배포 빌드', value: 7, note: '쓸모 적음' },
      ],
    },
  },
  {
    slug: 'dependency-pin-policy',
    title: '의존성 버전을 고정하는 기준',
    body: `버전을 고정하면 갑작스러운 변화를 막지만 보안 수정도 함께 막는다. 느슨하게 두면 수정은 빨리 받지만 어느 날 모르는 코드가 들어온다. 둘 다 위험이라서 어느 쪽을 택할지 기준이 필요하다.

## 무엇을 막고 무엇을 놓치나?

| 방식 | 막는 것 | 놓치는 것 |
| --- | --- | --- |
| 완전 고정 | 예기치 않은 변경 | 보안 수정 |
| 패치만 허용 | 큰 변경 | 일부 수정 |
| 범위 지정 | 호환 범위 밖 | 범위 안의 변조 |
| 고정 없음 | 없음 | 통제 자체 |

완전 고정이 안전해 보이지만, 고정한 채 방치하면 알려진 취약점을 그대로 안고 간다. 고정은 자동 갱신 절차와 함께여야 뜻이 있다.

![고정 방식의 맞교환](/img/posts/dependency-pin-policy.svg)

## 어디를 고정하나

모든 의존성을 같게 다루지 않는다. 위치와 성격에 따라 다르다.

| 대상 | 방식 |
| --- | --- |
| 실행에 쓰이는 패키지 | 잠금 파일로 완전 고정 |
| 개발 도구 | 범위 허용 |
| 기반 이미지 | 다이제스트로 고정 |
| 시스템 패키지 | 배포판 정책 따름 |
| CI 액션 | 커밋 해시로 고정 |

CI 액션을 태그로 참조하면 그 태그가 가리키는 대상이 바뀔 수 있다. 커밋 해시로 고정하면 그 위험이 사라진다.

## 고정한 뒤에 필요한 것

고정은 절반이다. 나머지 절반은 언제 올릴지 정하는 일이다.

| 항목 | 주기 |
| --- | --- |
| 취약점 알림 확인 | 자동·즉시 |
| 패치 버전 갱신 | 주 1회 자동 |
| 마이너 버전 | 월 1회 검토 |
| 메이저 버전 | 계획적으로 |

![갱신 주기](/img/posts/dependency-pin-policy-2.svg)

## 지금 상태를 확인한다

\`\`\`bash
# 잠금 파일이 실제로 쓰이는지 — 없으면 빌드마다 다른 것이 들어온다
ls -la package-lock.json yarn.lock pnpm-lock.yaml 2>/dev/null

# 알려진 취약점
npm audit --omit=dev 2>/dev/null | tail -15

# 오래된 정도 — 몇 년 지난 것이 있는지
npm outdated 2>/dev/null | head -15
\`\`\`

취약점이 0 이어도 안심하기 이르다. 오래 갱신하지 않은 패키지는 아직 알려지지 않았을 뿐일 수 있고, 유지보수가 끊긴 패키지는 앞으로 수정이 나오지 않는다.

## 유지보수가 끊긴 패키지

버전 정책보다 중요한 판단이 하나 있다. 그 패키지가 아직 살아 있는가다. 마지막 커밋이 몇 년 전이고 이슈에 답이 없다면, 취약점이 나와도 고쳐 줄 사람이 없다. 이런 패키지는 버전을 어떻게 고정하든 위험이 줄지 않으므로 교체 계획을 세우는 편이 맞다.

## 참고

- OWASP — 취약한 구성 요소 사용
- SLSA — 빌드 재현성과 의존성 고정
- NIST SP 800-161 — 공급망 위험 관리`,
    diagram: {
      type: 'matrix',
      caption: '고정 방식의 맞교환',
      x: ['느슨함', '고정'],
      y: ['갱신 절차 있음', '없음'],
      cells: ['변경 유입', '안전', '방치·변조', '취약점 누적'],
    },
    diagram2: {
      type: 'steps',
      caption: '갱신 주기',
      steps: [
        { label: '취약점 알림', note: '즉시' },
        { label: '패치 갱신', note: '주 1회' },
        { label: '마이너 검토', note: '월 1회' },
        { label: '메이저 계획', note: '분기' },
      ],
    },
  },
  {
    slug: 'lockfile-review',
    title: '잠금 파일 변경을 제대로 읽는 법',
    body: `잠금 파일은 변경이 수천 줄이라 리뷰에서 늘 건너뛴다. 그런데 공급망 공격은 정확히 그 파일을 통해 들어온다. 전부 읽을 수는 없으니 무엇을 볼지 정하는 것이 유일한 방법이다.

## 무엇을 봐야 하나?

줄 수가 아니라 종류를 본다. 대부분의 변경은 버전 숫자 하나 때문에 생긴 연쇄이고, 위험한 것은 그중 몇 줄이다.

| 볼 것 | 이유 |
| --- | --- |
| 새로 추가된 패키지 이름 | 의도한 것인가 |
| 다운로드 주소 변경 | 저장소가 바뀌었나 |
| 무결성 해시 변경 | 같은 버전인데 바뀌었나 |
| 유지보수자 변경 | 소유권 이전 |
| 설치 스크립트 추가 | 설치만으로 코드 실행 |

같은 버전인데 해시가 바뀌었다면 그것 하나로 조사를 시작할 이유가 된다. 정상적인 경우 같은 버전은 같은 내용이다.

![잠금 파일에서 볼 것](/img/posts/lockfile-review.svg)

## 설치 스크립트가 왜 위험한가

패키지 설치 과정에서 실행되는 스크립트는 빌드 서버의 권한으로 돈다. 코드를 불러 쓰기도 전에 이미 실행된 뒤다.

| 시점 | 실행 주체 |
| --- | --- |
| 설치 스크립트 | 빌드 서버 |
| 빌드 스크립트 | 빌드 서버 |
| 애플리케이션 코드 | 운영 서버 |

개발자 노트북에서도 같은 일이 벌어진다. 의존성 하나를 설치하는 것만으로 그 사람의 자격 증명에 접근할 수 있다.

## 리뷰를 가능하게 만드는 장치

사람이 수천 줄을 읽게 하지 않는다. 기계가 걸러 낸 것만 사람이 본다.

| 장치 | 걸러 내는 것 |
| --- | --- |
| 새 패키지 알림 | 추가된 이름 |
| 해시 변경 알림 | 같은 버전의 변화 |
| 설치 스크립트 검사 | 실행 코드 유무 |
| 유지보수자 변경 추적 | 소유권 이전 |

![기계가 걸러 내고 사람이 본다](/img/posts/lockfile-review-2.svg)

## 변경을 직접 뽑아 본다

\`\`\`bash
# 이번 변경에서 새로 들어온 패키지 이름만
git diff HEAD~1 -- package-lock.json \\
  | grep '^+.*"resolved"' | sed 's#.*/\\([^/]*\\)/-/.*#\\1#' | sort -u | head -20

# 설치 스크립트를 가진 패키지
npm ls --all --json 2>/dev/null | grep -c '"scripts"'
\`\`\`

새로 들어온 이름이 익숙하지 않고 직접 추가한 적도 없다면, 어느 패키지가 그것을 끌고 왔는지부터 확인한다.

## 이름이 비슷한 패키지

공급망 공격 중 가장 단순하고 자주 성공하는 것이 이름을 비슷하게 만드는 방식이다. 한 글자 다르거나 하이픈 위치가 다른 이름이 잠금 파일에 조용히 들어온다. 리뷰에서 새 이름을 소리 내어 읽어 보는 것만으로도 상당수가 걸린다. 눈으로 훑으면 익숙한 이름으로 읽히기 때문이다.

## 참고

- OWASP — 의존성 혼동 공격
- SLSA — 출처 증명과 무결성
- npm 문서 — 잠금 파일의 무결성 해시`,
    diagram: {
      type: 'steps',
      caption: '잠금 파일에서 볼 것',
      steps: [
        { label: '새 패키지 이름', note: '의도한 것인가' },
        { label: '다운로드 주소', note: '저장소 변경' },
        { label: '무결성 해시', note: '같은 버전인데' },
        { label: '설치 스크립트', note: '설치만으로 실행' },
      ],
    },
    diagram2: {
      type: 'flow',
      caption: '기계가 걸러 내고 사람이 본다',
      steps: [
        { label: '수천 줄 변경', note: '전부 못 읽음' },
        { label: '기계가 분류', note: '새 이름·해시' },
        { label: '수십 줄로 축소', note: '읽을 수 있는 양' },
        { label: '사람이 판단', note: '의도 확인' },
      ],
    },
  },
  {
    slug: 'transitive-dependency-risk',
    title: '직접 넣지 않은 의존성이 위험한 이유',
    body: `직접 추가한 패키지는 몇 개 안 되는데 설치되는 것은 수백 개다. 나머지는 패키지가 끌고 온 것이고, 아무도 고른 적이 없다. 취약점은 대개 그 고르지 않은 쪽에서 나온다.

## 왜 눈에 안 보이나?

목록에는 직접 넣은 것만 적혀 있다. 실제로 실행되는 코드는 그 몇십 배다.

| 항목 | 흔한 규모 |
| --- | --- |
| 직접 추가 | 20~40개 |
| 실제 설치 | 500~1,500개 |
| 유지보수자 수 | 수백 명 |
| 우리가 검토한 것 | 직접 추가분뿐 |

수백 명의 서로 모르는 사람이 만든 코드가 같은 권한으로 실행된다. 그중 한 계정만 탈취돼도 우리 빌드에 코드가 들어온다.

![직접 넣은 것과 실제 들어오는 것](/img/posts/transitive-dependency-risk.svg)

## 무엇을 줄일 수 있나

전부 검토할 수는 없으니 수 자체를 줄인다.

| 방법 | 효과 |
| --- | --- |
| 기능 하나에 패키지 하나 쓰지 않기 | 큰 감소 |
| 표준 라이브러리로 대체 | 확실 |
| 무거운 패키지 교체 | 연쇄 제거 |
| 개발 전용으로 분리 | 운영 노출 감소 |

작은 유틸리티를 패키지로 받는 습관이 수를 가장 크게 늘린다. 몇 줄짜리 기능은 직접 쓰는 편이 검토 가능한 범위를 지킨다.

## 취약점이 나왔을 때

간접 의존성의 취약점은 우리가 직접 올릴 수 없다. 중간에 있는 패키지가 올려 줘야 한다.

| 상황 | 대응 |
| --- | --- |
| 중간 패키지가 이미 수정 | 갱신 |
| 아직 수정 안 됨 | 버전 강제 지정 |
| 유지보수 중단 | 교체 검토 |
| 실제로 쓰이지 않는 경로 | 위험 수용·기록 |

버전 강제 지정은 호환성을 깨뜨릴 수 있어 시험이 필요하다. 그래도 취약점을 안고 가는 것보다 낫다.

![대응 경로](/img/posts/transitive-dependency-risk-2.svg)

## 실제 규모를 센다

\`\`\`bash
# 직접 추가한 수와 전체 설치 수
node -e "const p=require('./package.json');console.log('직접',Object.keys({...p.dependencies,...p.devDependencies}).length)"
npm ls --all --parseable 2>/dev/null | wc -l

# 무엇이 그 패키지를 끌고 왔는지
npm ls <패키지이름> 2>/dev/null | head -20
\`\`\`

전체 수가 천 개를 넘는다면 검토라는 말이 무의미한 규모다. 그때는 개별 검토가 아니라 자동 탐지와 빠른 갱신 절차로 방향을 바꾼다.

## 구성 목록을 만들어 두는 이유

취약점 소식이 나왔을 때 우리가 그것을 쓰는지 아는 데 며칠이 걸리면, 그 며칠이 그대로 노출 기간이다. 구성 목록을 미리 만들어 두면 조회 한 번으로 끝난다. 목록을 만드는 일 자체가 방어는 아니지만, 대응 속도를 며칠에서 몇 분으로 바꾼다.

## 참고

- NIST SP 800-161 — 공급망 위험 관리
- CISA — SBOM 최소 요건
- OWASP — 의존성 점검 실무`,
    diagram: {
      type: 'bars',
      caption: '직접 넣은 것과 실제 설치',
      unit: '개',
      items: [
        { label: '직접 추가', value: 30, note: '검토함' },
        { label: '2단계', value: 180, note: '거의 못 봄' },
        { label: '3단계 이상', value: 700, note: '아무도 안 봄' },
        { label: '유지보수자', value: 400, note: '서로 모름' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '취약점 대응',
      x: ['중간 패키지 수정됨', '안 됨'],
      y: ['실제로 쓰임', '쓰이지 않음'],
      cells: ['갱신', '버전 강제', '갱신', '기록·관찰'],
    },
  },
]
