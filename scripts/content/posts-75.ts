import type { SeedPost } from './types'

/** 740~749 — 설계 단계에서 정하는 것 */
export const posts75: SeedPost[] = [
  {
    slug: 'threat-model-lightweight',
    title: '가볍게 위협 모델링을 시작하는 법',
    body: `위협 모델링은 거창한 절차로 알려져 있어 시작조차 하지 않는 경우가 많다. 그런데 한 시간짜리 대화만으로도 설계의 빈틈이 상당수 드러나고, 그 시점에 고치는 비용은 나중의 몇 분의 일이다.

## 무엇을 묻나

| 질문 | 목적 |
| --- | --- |
| 무엇을 만드나 | 범위 |
| 무엇이 잘못될 수 있나 | 위협 |
| 어떻게 막나 | 대응 |
| 충분한가 | 검토 |

네 가지 질문이면 충분하다. 도구나 표기법을 갖추려다 시작이 미뤄지는 경우가 훨씬 많으므로, 화이트보드와 한 시간으로 시작한다.

![가벼운 위협 모델링](/img/posts/threat-model-lightweight.svg)

## 어디에 경계가 있나

| 경계 | 예 |
| --- | --- |
| 사용자와 서버 | 입력 신뢰 |
| 서비스 간 | 내부 호출 |
| 외부 연동 | 제3자 |
| 권한 수준 | 일반·관리자 |

신뢰 경계를 넘는 지점마다 검증이 필요하다. 그림에 선을 긋고 그 선을 넘는 화살표를 세어 보면 확인할 목록이 자동으로 만들어진다. 내부 호출이라 안전하다고 넘기는 자리가 가장 자주 문제가 된다.

## 무엇이 잘못될 수 있나

| 범주 | 질문 |
| --- | --- |
| 사칭 | 남이 될 수 있나 |
| 변조 | 값을 바꿀 수 있나 |
| 부인 | 부정할 수 있나 |
| 노출 | 볼 수 있나 |
| 중단 | 멈출 수 있나 |
| 권한 상승 | 넘어설 수 있나 |

여섯 범주를 각 구성 요소에 대입해 보면 빠뜨리는 것이 줄어든다. 전부 적용할 필요는 없고, 그 구성 요소에 해당하는 것만 보면 된다.

![신뢰 경계와 검증](/img/posts/threat-model-lightweight-2.svg)

## 결과를 어디에 남기나

\`\`\`bash
# 설계 문서와 함께 저장소에 둔다
ls docs/threat-models/ 2>/dev/null | head
git log --oneline -3 -- docs/threat-models/ 2>/dev/null
\`\`\`

설계 문서 옆에 두고 함께 리뷰하면 별도 관리 부담이 없다. 나중에 설계가 바뀔 때 이 문서도 함께 고치게 되어 낡지 않는다.

## 전부 막으려 하지 않는다

나온 위협을 모두 막으려 하면 결론이 나지 않는다. 각 항목에 대해 막을지, 줄일지, 받아들일지, 다른 곳으로 넘길지를 정하고 그 판단을 기록한다. 받아들이기로 한 것도 기록에 남으면 나중에 재검토할 수 있고, 사고가 났을 때 몰랐던 것과 알고도 둔 것을 구분할 수 있다.

## 참고

- OWASP — 위협 모델링 안내
- Microsoft — STRIDE 방법론
- NIST SP 800-154 — 데이터 중심 위협 모델링`,
    diagram: {
      type: 'steps',
      caption: '가벼운 위협 모델링',
      steps: [
        { label: '무엇을 만드나', note: '범위' },
        { label: '무엇이 잘못되나', note: '위협' },
        { label: '어떻게 막나', note: '대응' },
        { label: '충분한가', note: '검토' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '신뢰 경계와 검증',
      layers: [
        { label: '사용자 입력', note: '전부 검증' },
        { label: '서비스 간', note: '자주 빠짐' },
        { label: '외부 연동', note: '제3자' },
        { label: '권한 경계', note: '상승 확인' },
      ],
    },
  },
  {
    slug: 'secure-defaults-design',
    title: '기본값을 안전한 쪽으로 정하기',
    body: `설정을 바꾸지 않은 상태가 안전하면 아무도 아무것도 하지 않아도 보안이 유지된다. 반대로 기본값이 느슨하면 설정을 바꾼 소수만 안전하고 나머지는 전부 노출된다.

## 안전한 설정의 기본값은 무엇인가

| 항목 | 안전한 쪽 |
| --- | --- |
| 공개 범위 | 비공개 |
| 권한 | 최소 |
| 암호화 | 켜짐 |
| 로그 | 수집 |
| 세션 수명 | 짧게 |

공개 범위를 비공개로 두는 것이 가장 효과가 크다. 사용자가 의도적으로 공개할 때만 공개되는 구성이면 실수로 노출되는 일이 사라진다.

![기본값이 정하는 결과](/img/posts/secure-defaults-design.svg)

## 왜 느슨하게 두나

| 이유 | 실제 |
| --- | --- |
| 사용 편의 | 초기 경험 |
| 문의 감소 | 막히면 묻는다 |
| 관행 | 예전부터 |
| 검토 부재 | 생각 안 함 |

초기 사용 경험 때문에 느슨하게 두는 판단이 많다. 그런데 처음에 한 번 겪는 불편과 영구적인 노출을 비교하면 어느 쪽이 나은지 분명하다.

## 예외를 어떻게 다루나

| 방법 | 내용 |
| --- | --- |
| 명시적 변경 | 사용자가 |
| 경고 표시 | 위험 안내 |
| 기간 제한 | 자동 복귀 |
| 기록 | 누가 언제 |

느슨한 설정으로 바꿀 때 무엇이 달라지는지 알려 주면 판단이 달라진다. 일정 기간 뒤 자동으로 안전한 값으로 돌아가게 하는 방법도 쓸 만하다.

![변경의 방향](/img/posts/secure-defaults-design-2.svg)

## 지금 기본값을 본다

\`\`\`bash
# 새로 만든 자원의 기본 설정 확인
grep -rInE 'default.*(public|true|all)' --include='*.ts' src/config/ 2>/dev/null | head -20
\`\`\`

기본값이 넓게 열려 있는 항목이 나오면 하나씩 검토한다. 바꿀 때 기존 사용자에게 어떤 영향이 가는지 함께 확인해야 한다.

## 이미 만들어진 것은 따로 본다

기본값을 바꿔도 이미 만들어진 자원에는 적용되지 않는다. 옛 기본값으로 만들어진 것들의 목록을 뽑아 개별로 처리해야 실제 상태가 달라진다. 이 작업을 건너뛰면 새 것만 안전하고 오래된 것이 계속 노출된 채 남는다.

## 참고

- OWASP — 안전한 설계 원칙
- NIST SP 800-53 — 기본 설정
- CIS Benchmarks — 안전한 구성`,
    diagram: {
      type: 'matrix',
      caption: '기본값이 정하는 결과',
      x: ['안전한 기본값', '느슨한 기본값'],
      y: ['설정 변경함', '안 함'],
      cells: ['안전', '안전', '안전', '노출'],
    },
    diagram2: {
      type: 'steps',
      caption: '변경의 방향',
      steps: [
        { label: '안전한 기본', note: '출발점' },
        { label: '명시적 변경', note: '사용자 선택' },
        { label: '경고 표시', note: '결과 안내' },
        { label: '자동 복귀', note: '기간 후' },
      ],
    },
  },
  {
    slug: 'fail-safe-behavior',
    title: '실패했을 때 어느 쪽으로 기울까',
    body: `검증 시스템이 응답하지 않거나 오류가 났을 때 통과시킬지 막을지는 설계 결정이다. 이 결정을 내리지 않으면 대개 통과시키는 쪽으로 구현되고, 그것이 장애 상황에서 통제가 사라지는 이유가 된다.

## 접근 통제는 어느 쪽이 맞나

| 상황 | 판단 |
| --- | --- |
| 인증 확인 실패 | 막음 |
| 권한 조회 실패 | 막음 |
| 부정 탐지 실패 | 상황에 따라 |
| 로그 기록 실패 | 계속 |
| 통계 수집 실패 | 계속 |

인증과 권한은 확인할 수 없으면 막는 것이 원칙이다. 반면 부가적인 기능이 실패했다고 서비스를 멈추면 가용성 문제가 생긴다.

![실패 시의 두 선택](/img/posts/fail-safe-behavior.svg)

## 막는 쪽의 위험

| 항목 | 내용 |
| --- | --- |
| 전면 중단 | 의존 서비스 장애 |
| 연쇄 실패 | 대기 누적 |
| 복구 지연 | 부하 |

의존하는 시스템이 멈추면 우리도 멈춘다. 그래서 막는 쪽을 택할 때는 그 시스템의 가용성을 함께 높여야 한다. 한쪽만 결정하고 다른 쪽을 준비하지 않으면 보안을 위해 서비스를 자주 멈추게 된다.

## 중간 선택지

| 방법 | 내용 |
| --- | --- |
| 캐시 사용 | 최근 결과 |
| 제한된 허용 | 읽기만 |
| 지연 검증 | 나중에 확인 |
| 등급별 처리 | 민감도에 따라 |

최근 조회 결과를 잠시 쓰는 방식이 현실적이다. 완전히 막지도 완전히 열지도 않으면서 짧은 장애를 넘길 수 있다.

![실패 시 처리 선택](/img/posts/fail-safe-behavior-2.svg)

## 어떻게 되어 있는지 본다

\`\`\`bash
# 권한 조회 실패 시 어떻게 처리하는지
grep -rInA5 'catch' --include='*.ts' src/auth/ 2>/dev/null \
  | grep -iE 'return true|allow|next\\(\\)' | head
\`\`\`

오류를 잡고 통과시키는 코드가 나오면 의도한 것인지 확인한다. 대개는 오류 처리를 대충 넣다가 그렇게 된 경우다.

## 시험해 봐야 안다

의존 시스템을 일부러 멈추고 무슨 일이 일어나는지 확인하지 않으면 실제 동작을 알 수 없다. 시험 환경에서 연결을 끊어 보고 어느 쪽으로 기우는지 관찰한다. 문서에 적힌 것과 실제 구현이 다른 경우가 자주 발견되고, 그 차이가 장애 때 드러나면 대응이 늦어진다.

## 참고

- OWASP — 오류 처리 지침
- NIST SP 800-160 — 시스템 보안 공학
- Google SRE — 연쇄 실패 대응`,
    diagram: {
      type: 'matrix',
      caption: '실패 시의 두 선택',
      x: ['막음', '통과'],
      y: ['보안', '가용성'],
      cells: ['유지', '상실', '위험', '유지'],
    },
    diagram2: {
      type: 'layers',
      caption: '실패 시 처리 선택',
      layers: [
        { label: '인증·권한', note: '막음' },
        { label: '캐시 사용', note: '짧은 장애' },
        { label: '제한 허용', note: '읽기만' },
        { label: '부가 기능', note: '계속' },
      ],
    },
  },
  {
    slug: 'tenant-isolation-layers',
    title: '여러 고객을 한 시스템에 담을 때',
    body: `하나의 시스템에 여러 고객의 데이터를 담으면 비용이 크게 줄지만, 한 줄의 실수가 다른 고객의 데이터를 노출하는 사고로 이어진다. 격리를 어느 층에서 할지가 이 구조의 핵심 결정이다.

## 네트워크 분할처럼 어느 층에서 나누나

| 층 | 격리 |
| --- | --- |
| 별도 인프라 | 가장 강함 |
| 별도 데이터베이스 | 강함 |
| 별도 스키마 | 중간 |
| 행 단위 구분 | 약함 |

행 단위로 나누면 조건 하나를 빠뜨리는 순간 다른 고객의 데이터가 나온다. 비용은 가장 적지만 코드의 정확성에 전부를 건다.

![격리 층과 비용](/img/posts/tenant-isolation-layers.svg)

## 조건을 어디서 붙이나

| 방법 | 안전도 |
| --- | --- |
| 각 질의마다 | 빠뜨리기 쉬움 |
| 공통 계층 | 낫다 |
| 데이터베이스 정책 | 가장 확실 |
| 연결별 설정 | 확실 |

데이터베이스 자체가 행 수준 정책으로 걸러 주면 코드에서 빠뜨려도 막힌다. 방어를 한 층 더 두는 셈이라 설정 비용에 비해 효과가 크다.

## 어디서 섞이나

| 지점 | 위험 |
| --- | --- |
| 캐시 키 | 구분 누락 |
| 파일 저장 | 경로 |
| 검색 색인 | 필터 |
| 배치 작업 | 전체 조회 |
| 오류 보고 | 다른 고객 정보 |

캐시 키에 고객 구분이 빠지면 다른 고객의 응답이 그대로 전달된다. 데이터베이스만 신경 쓰다 이런 곳에서 문제가 생긴다.

![섞이기 쉬운 지점](/img/posts/tenant-isolation-layers-2.svg)

## 섞이는지 시험한다

\`\`\`bash
# 다른 고객의 자원을 식별자로 직접 요청
curl -s -o /dev/null -w '%{http_code}\\n' \
  https://example.com/api/documents/other-tenant-doc-id \
  -H "Authorization: Bearer $TENANT_A_TOKEN"
\`\`\`

404가 아닌 200이 나오면 격리가 깨진 것이다. 이 시험을 자동화해 두면 새 기능이 추가될 때마다 확인된다.

## 큰 고객은 분리한다

모든 고객을 같은 방식으로 담을 필요는 없다. 규모가 크거나 규제 요구가 강한 고객은 별도 환경으로 분리하면, 그 고객의 요구를 맞추면서 전체 구조는 단순하게 유지할 수 있다. 처음부터 두 방식을 지원하도록 설계해 두면 나중에 고객이 요구할 때 대응할 수 있다.

## 참고

- OWASP — 접근 통제 지침
- NIST SP 800-53 — 경계 보호
- 클라우드 사업자 다중 테넌트 설계 문서`,
    diagram: {
      type: 'bars',
      caption: '격리 층과 비용',
      unit: '점',
      items: [
        { label: '별도 인프라', value: 95, note: '비용 큼' },
        { label: '별도 데이터베이스', value: 80, note: '균형' },
        { label: '별도 스키마', value: 55, note: '중간' },
        { label: '행 단위', value: 30, note: '코드 의존' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '섞이기 쉬운 지점',
      layers: [
        { label: '캐시 키', note: '구분 누락' },
        { label: '파일 경로', note: '저장소' },
        { label: '검색 색인', note: '필터' },
        { label: '배치 작업', note: '전체 조회' },
      ],
    },
  },
  {
    slug: 'feature-flag-safety',
    title: '기능 전환 스위치를 안전하게 쓰기',
    body: `기능을 코드 배포 없이 켜고 끌 수 있으면 배포 위험이 크게 줄어든다. 다만 그 스위치 자체가 권한 통제를 우회하는 경로가 되거나, 꺼진 기능이 실제로는 접근 가능한 상태로 남기도 한다.

## 어떤 문제가 생기나

| 상황 | 문제 |
| --- | --- |
| 화면만 숨김 | API는 열림 |
| 클라이언트 판단 | 조작 가능 |
| 스위치 권한 | 누구나 변경 |
| 정리 안 됨 | 오래된 분기 |

화면에서 버튼만 숨기고 서버에서 막지 않는 구현이 가장 흔하다. 주소를 직접 입력하거나 API를 부르면 그대로 동작한다.

![스위치가 뚫리는 지점](/img/posts/feature-flag-safety.svg)

## 어디서 판단하나

| 위치 | 신뢰 |
| --- | --- |
| 서버 | 신뢰 가능 |
| 클라이언트 | 표시용만 |
| 둘 다 | 권장 |

서버에서 판단하고 클라이언트는 그 결과로 화면만 정리하는 구성이 맞다. 클라이언트 판단은 사용자 경험을 위한 것이지 통제가 아니다.

## 파이프라인 배포처럼 누가 바꾸나

| 대상 | 권한 |
| --- | --- |
| 운영 환경 | 제한·승인 |
| 시험 환경 | 완화 |
| 위험한 기능 | 추가 확인 |
| 변경 기록 | 전부 |

운영 환경의 스위치를 바꾸는 것은 배포와 같은 영향을 가진다. 배포에 붙인 통제를 여기에도 붙여야 일관된다.

![판단 위치와 통제](/img/posts/feature-flag-safety-2.svg)

## 상태를 확인한다

\`\`\`bash
# 오래된 스위치 — 정리 대상
curl -s "$FLAG_API/flags" -H "Authorization: Bearer $TOKEN" 2>/dev/null \
  | jq -r '.[] | select(.updated_at < "'"$(date -v-180d +%F)"'") | .key' | head -20
\`\`\`

반년 넘게 바뀌지 않은 스위치는 이미 결론이 난 것이다. 코드에서 분기를 지우고 스위치도 없애면 복잡도가 줄어든다.

## 분기가 쌓이면 위험해진다

스위치가 늘어나면 조합의 수가 폭발하고, 어떤 조합에서 무슨 일이 일어나는지 아무도 모르게 된다. 시험도 모든 조합을 덮지 못한다. 결론이 난 스위치는 제때 정리하는 것을 작업의 일부로 정해 두면, 이 구조가 감당할 수 있는 수준으로 유지된다.

## 참고

- OWASP — 접근 통제 지침
- Martin Fowler — Feature Toggles
- NIST SP 800-53 — 변경 통제`,
    diagram: {
      type: 'flow',
      caption: '스위치가 뚫리는 지점',
      steps: [
        { label: '기능 비활성', note: '스위치 끔' },
        { label: '화면에서 숨김', note: '클라이언트' },
        { label: '서버 미검증', note: '누락' },
        { label: 'API 직접 호출', note: '동작함', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '판단 위치와 통제',
      x: ['서버 판단', '클라이언트만'],
      y: ['화면 정리', '접근 통제'],
      cells: ['가능', '가능', '가능', '없음'],
    },
  },
  {
    slug: 'rate-limit-criteria',
    title: 'API 속도 제한을 설계하는 기준',
    body: `속도 제한은 남용을 막는 가장 기본적인 장치인데, 기준을 대충 잡으면 정상 사용자가 막히거나 공격자가 그대로 통과한다. 무엇을 기준으로 세고 어디에 적용할지를 먼저 정해야 한다.

## 무엇을 기준으로 세나

| 기준 | 특성 |
| --- | --- |
| 주소별 | 공유 주소 영향 |
| 계정별 | 로그인 필요 |
| 키별 | 외부 연동 |
| 조합 | 실무적 |

공유 주소 뒤에 많은 사용자가 있는 환경에서는 주소 기준이 정상 사용자를 막는다. 로그인한 요청은 계정 기준으로, 그렇지 않은 요청은 주소 기준으로 나눠 적용한다.

![제한 기준의 구분](/img/posts/rate-limit-criteria.svg)

## 엔드포인트마다 어디에 다르게 거나

| 대상 | 강도 |
| --- | --- |
| 로그인 시도 | 엄격 |
| 검색·조회 | 보통 |
| 쓰기 | 보통 |
| 정적 자원 | 느슨 |
| 비용 큰 작업 | 엄격 |

모든 경로에 같은 제한을 걸면 어느 한쪽이 반드시 틀린다. 비용이 큰 작업과 인증 관련 경로에 별도 기준을 두는 것부터 시작한다.

## 넘었을 때 무엇을 하나

| 처리 | 특성 |
| --- | --- |
| 거절 | 명확 |
| 지연 | 완만 |
| 대기열 | 처리 보장 |
| 등급 하향 | 일부 기능 |

바로 거절하는 대신 응답을 늦추면 공격자에게는 부담이 되고 정상 사용자는 조금 느려질 뿐이다. 남은 한도를 응답 헤더로 알려 주면 연동하는 쪽에서 조절할 수 있다.

![경로별 제한 강도](/img/posts/rate-limit-criteria-2.svg)

## 실제로 걸리는지 본다

\`\`\`bash
# 짧은 시간에 반복 요청
for i in $(seq 1 50); do
  curl -s -o /dev/null -w '%{http_code} ' https://example.com/api/search?q=a
done; echo
\`\`\`

전부 200이면 제한이 없는 것이다. 429가 나오기 시작하는 지점이 실제 기준이므로, 의도한 값과 같은지 확인한다.

## 분산 환경에서는 공유해야 한다

서버마다 따로 세면 실제 허용량이 서버 수만큼 늘어난다. 중앙 저장소에 세거나 대략적인 분산 방식을 쓰되, 어느 쪽이든 실제 허용량이 얼마인지 계산해 두어야 한다. 이 부분을 확인하지 않아 제한이 사실상 없는 상태로 운영되는 경우가 드물지 않다.

## 참고

- OWASP — API 보안 Top 10
- RFC 6585 — 429 응답 코드
- NIST SP 800-53 — 자원 가용성`,
    diagram: {
      type: 'matrix',
      caption: '제한 기준의 구분',
      x: ['로그인함', '익명'],
      y: ['공유 주소', '단독 주소'],
      cells: ['계정 기준', '주소 기준 주의', '계정 기준', '주소 기준'],
    },
    diagram2: {
      type: 'bars',
      caption: '경로별 제한 강도',
      unit: '회/분',
      items: [
        { label: '로그인 시도', value: 5, note: '엄격' },
        { label: '비용 큰 작업', value: 10, note: '엄격' },
        { label: '검색·쓰기', value: 60, note: '보통' },
        { label: '정적 자원', value: 600, note: '느슨' },
      ],
    },
  },
  {
    slug: 'input-validation-layers',
    title: '입력 검증을 어느 층에 둘까',
    body: `검증을 여러 곳에 흩어 두면 어디서 무엇을 확인하는지 알 수 없고, 한 곳에만 두면 그 경로를 지나지 않는 요청에서 빠진다. 층마다 역할을 나누는 것이 정리하는 방법이다.

## 어느 층에서 무엇을

| 층 | 역할 |
| --- | --- |
| 클라이언트 | 사용자 안내 |
| 입구 | 형식·크기 |
| 업무 로직 | 규칙 |
| 저장소 | 제약 |

클라이언트 검증은 통제가 아니라 안내다. 서버에서 같은 검증을 반드시 다시 해야 하고, 그 사실을 전제로 화면을 만들어야 한다.

![검증 층의 역할](/img/posts/input-validation-layers.svg)

## 대량 할당은 입구에서 무엇을 보나

| 항목 | 내용 |
| --- | --- |
| 형식 | 스키마 |
| 크기 | 상한 |
| 타입 | 문자열인지 |
| 필수 항목 | 존재 |
| 알 수 없는 필드 | 거절 |

정의되지 않은 필드를 거절하면 대량 할당 문제가 함께 막힌다. 관대하게 받아들이는 설정이 기본인 경우가 많아 확인이 필요하다.

## 허용할 것을 정한다

| 방식 | 특성 |
| --- | --- |
| 허용 목록 | 안전 |
| 차단 목록 | 우회 가능 |
| 정규식 | 실수 여지 |
| 길이 제한 | 보조 |

무엇을 막을지 나열하는 방식은 새로운 표현이 나올 때마다 뚫린다. 무엇을 허용할지 정하는 편이 관리하기 쉽고 안전하다.

![검증 방식의 안전도](/img/posts/input-validation-layers-2.svg)

## 빠진 곳을 찾는다

\`\`\`bash
# 스키마 검증 없이 본문을 쓰는 처리 함수
grep -rInE 'req\\.body' --include='*.ts' src/routes/ 2>/dev/null \
  | grep -v 'validate\\|schema\\|parse' | head -20
\`\`\`

검증을 거치지 않고 본문을 쓰는 지점이 나오면 확인한다. 새로 추가된 경로에서 빠지는 경우가 대부분이다.

## 저장소 제약도 함께 건다

코드에서 검증해도 다른 경로로 데이터가 들어올 수 있다. 배치 작업이나 관리 도구, 직접 접속이 그런 경로다. 저장소 자체에 길이와 형식 제약을 걸어 두면 어느 경로로 들어오든 걸러진다. 제약이 성능에 주는 영향은 대개 무시할 만하고, 잘못된 데이터가 쌓이는 것을 막는 값어치가 더 크다.

## 참고

- OWASP — 입력 검증 지침
- CWE-20 — 부적절한 입력 검증
- NIST SP 800-53 — 입력 처리`,
    diagram: {
      type: 'layers',
      caption: '검증 층의 역할',
      layers: [
        { label: '클라이언트', note: '안내만' },
        { label: '입구', note: '형식·크기' },
        { label: '업무 로직', note: '규칙' },
        { label: '저장소', note: '제약' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검증 방식의 안전도',
      unit: '점',
      items: [
        { label: '허용 목록', value: 90, note: '안전' },
        { label: '스키마 검증', value: 85, note: '구조 고정' },
        { label: '정규식', value: 55, note: '실수 여지' },
        { label: '차단 목록', value: 20, note: '우회 가능' },
      ],
    },
  },
  {
    slug: 'error-message-content',
    title: '오류 메시지에 무엇을 적을까',
    body: `오류 메시지는 사용자를 돕기 위한 것인데, 상세할수록 공격자에게도 정보를 준다. 사용자에게 필요한 것과 조사에 필요한 것을 나누면 두 요구를 모두 맞출 수 있다.

## 무엇이 새나

| 내용 | 위험 |
| --- | --- |
| 스택 추적 | 구조 노출 |
| 질의문 | 스키마 |
| 파일 경로 | 서버 구조 |
| 버전 정보 | 취약점 대조 |
| 계정 존재 여부 | 목록 작성 |

계정이 있는지 없는지 알려 주는 메시지가 가장 흔한 문제다. 로그인과 비밀번호 찾기 양쪽에서 같은 정보가 새는 경우가 많다.

![정보가 새는 경로](/img/posts/error-message-content.svg)

## 어떻게 나누나

| 대상 | 내용 |
| --- | --- |
| 사용자 | 무엇을 하면 되나 |
| 로그 | 상세 원인 |
| 식별자 | 둘을 연결 |

오류마다 식별자를 만들어 사용자에게 보여 주고 상세는 기록에 남기면, 문의가 들어왔을 때 그 식별자로 바로 찾을 수 있다. 사용자는 짧은 메시지만 보고 조사에 필요한 정보는 전부 남는다.

## 사용자에게 무엇을 주나

| 항목 | 내용 |
| --- | --- |
| 무엇이 잘못됐나 | 간단히 |
| 무엇을 하면 되나 | 행동 |
| 문의 방법 | 막혔을 때 |
| 식별자 | 추적용 |

무엇을 하면 되는지 적혀 있지 않은 메시지는 사용자를 막다른 곳에 세운다. 다시 시도하라는 안내조차 없으면 문의가 늘어난다.

![메시지 구성](/img/posts/error-message-content-2.svg)

## 지금 나가는 것을 본다

\`\`\`bash
# 운영 환경 응답에 상세 정보가 포함되는지
curl -s https://example.com/api/nonexistent | head -20
curl -s -X POST https://example.com/api/users -d 'invalid' | head -20
\`\`\`

스택 추적이나 내부 경로가 보이면 설정 문제다. 대개 개발 모드 설정이 운영에 남은 경우이고 설정 한 줄로 바뀐다.

## 시간 차이도 정보다

응답 내용을 같게 맞춰도 처리 시간이 다르면 구분된다. 계정이 없을 때 비밀번호 검증을 건너뛰면 그쪽이 빨라지고, 그 차이로 계정 존재를 알 수 있다. 민감한 판단에서는 응답 시간도 비슷하게 맞추는 처리가 필요하다.

## 참고

- OWASP — 오류 처리 지침
- CWE-209 — 민감 정보를 포함한 오류 메시지
- NIST SP 800-53 — 오류 처리`,
    diagram: {
      type: 'layers',
      caption: '정보가 새는 경로',
      layers: [
        { label: '스택 추적', note: '구조 전체' },
        { label: '질의문', note: '스키마' },
        { label: '버전 정보', note: '취약점 대조' },
        { label: '계정 존재', note: '목록 작성' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '메시지 구성',
      steps: [
        { label: '무엇이 잘못됐나', note: '간단히' },
        { label: '무엇을 하면 되나', note: '행동' },
        { label: '식별자', note: '추적용' },
        { label: '상세는 로그로', note: '분리' },
      ],
    },
  },
  {
    slug: 'async-job-security',
    title: '비동기 작업에서 놓치는 검증',
    body: `요청을 받아 큐에 넣고 나중에 처리하는 구조에서는 권한 확인이 어느 시점에 이뤄지는지가 모호해진다. 요청 시점에만 확인하면 처리 시점에는 이미 권한이 없을 수 있다.

## 어디서 어긋나나

| 상황 | 문제 |
| --- | --- |
| 권한 변경 | 요청 후 회수 |
| 계정 정지 | 대기 중 |
| 자원 삭제 | 대상 사라짐 |
| 재시도 | 오래된 요청 |
| 지연 | 시간 경과 |

퇴사 처리된 사람이 요청해 둔 작업이 며칠 뒤 실행되는 상황이 실제로 발생한다. 요청 시점에는 권한이 있었기 때문이다.

![시점 차이가 만드는 문제](/img/posts/async-job-security.svg)

## 최소 권한은 어느 시점에 확인하나

| 시점 | 내용 |
| --- | --- |
| 요청 시 | 빠른 거절 |
| 처리 시 | 실제 권한 |
| 둘 다 | 권장 |

두 시점 모두에서 확인하는 것이 맞다. 요청 시 확인은 사용자에게 빠르게 알려 주기 위한 것이고, 처리 시 확인이 실제 통제다.

## 작업 내용을 어떻게 다루나

| 항목 | 내용 |
| --- | --- |
| 요청자 | 함께 저장 |
| 권한 정보 | 참조로 |
| 입력값 | 검증된 것 |
| 재시도 한도 | 무한 방지 |

권한 정보를 작업에 복사해 두면 그 시점의 권한으로 고정된다. 참조만 저장하고 처리 시점에 조회하는 편이 안전하다.

![권한 확인 시점](/img/posts/async-job-security-2.svg)

## 큐 자체도 보호한다

\`\`\`bash
# 큐에 접근 가능한 주체 확인
aws sqs get-queue-attributes --queue-url "$QUEUE_URL" \
  --attribute-names Policy 2>/dev/null | jq -r '.Attributes.Policy' | head

# 대기 중인 오래된 메시지
aws sqs get-queue-attributes --queue-url "$QUEUE_URL" \
  --attribute-names ApproximateAgeOfOldestMessage 2>/dev/null
\`\`\`

큐에 직접 메시지를 넣을 수 있으면 검증을 건너뛰고 작업을 실행시킬 수 있다. 큐 접근 권한을 좁히는 것이 기본이다.

## 실패한 작업을 관리한다

처리에 실패한 작업이 계속 재시도되면 부하가 되고, 조용히 버려지면 사용자는 완료된 줄 안다. 실패 한도를 정하고 초과한 작업은 별도로 모아 사람이 확인하게 한다. 그 목록을 정기적으로 보지 않으면 실패가 쌓이는 것을 아무도 모르게 되고, 나중에 대량으로 발견된다.

## 참고

- OWASP — 접근 통제 지침
- NIST SP 800-53 — 최소 권한
- 각 메시지 큐 보안 문서`,
    diagram: {
      type: 'flow',
      caption: '시점 차이가 만드는 문제',
      steps: [
        { label: '요청', note: '권한 있음' },
        { label: '큐 대기', note: '시간 경과' },
        { label: '권한 회수', note: '그 사이' },
        { label: '처리 실행', note: '권한 없이', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '권한 확인 시점',
      x: ['요청 시', '처리 시'],
      y: ['빠른 거절', '실제 통제'],
      cells: ['가능', '불가', '불가', '가능'],
    },
  },
  {
    slug: 'idor-object-authz',
    title: '식별자로 남의 자원에 닿는 문제',
    body: `주소나 요청에 담긴 식별자를 바꿔 다른 사람의 자원에 접근하는 문제는 가장 흔하면서도 자동 도구로 잘 발견되지 않는다. 사람이 설계 단계에서 막아야 하는 종류의 결함이다.

## 왜 자주 생기나

| 원인 | 설명 |
| --- | --- |
| 인증만 확인 | 인가 누락 |
| 순차 식별자 | 추측 쉬움 |
| 화면 기준 설계 | 목록에 없으면 안 보임 |
| 새 경로 추가 | 검증 누락 |

로그인했는지만 확인하고 그 자원이 그 사람 것인지 확인하지 않는 구현이 원인이다. 화면에서는 자기 것만 보이니 문제가 드러나지 않는다.

![식별자를 바꿔 접근하는 흐름](/img/posts/idor-object-authz.svg)

## 접근 통제로 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 소유자 조건 | 질의에 포함 |
| 공통 검사 | 한곳에서 |
| 간접 참조 | 사용자별 번호 |
| 추측 어려운 식별자 | 보조 |

질의에 소유자 조건을 함께 넣으면 남의 자원은 조회 자체가 되지 않는다. 조회 후 비교하는 방식보다 안전하다.

## 식별자 형태도 본다

| 형태 | 특성 |
| --- | --- |
| 순차 번호 | 추측 가능 |
| 임의 문자열 | 추측 어려움 |
| 사용자별 번호 | 노출 없음 |

추측하기 어려운 식별자는 보조 수단이지 통제가 아니다. 검색 결과나 공유 링크로 식별자가 알려질 수 있으므로 권한 확인을 대신할 수 없다.

![막는 방법의 위치](/img/posts/idor-object-authz-2.svg)

## 전 경로를 시험한다

\`\`\`bash
# 다른 계정의 자원 식별자로 모든 경로를 시도
for ep in documents orders messages invoices; do
  printf '%s ' "$ep"
  curl -s -o /dev/null -w '%{http_code}\\n' \
    "https://example.com/api/$ep/$OTHER_ID" -H "Authorization: Bearer $TOKEN"
done
\`\`\`

200이 나오는 경로가 있으면 그곳이 결함이다. 조회만이 아니라 수정과 삭제 경로도 함께 시험해야 한다.

## 자동 시험에 넣는다

이 결함은 새 경로가 추가될 때마다 다시 생긴다. 두 계정을 만들어 서로의 자원에 접근해 보는 시험을 자동화해 두면, 새 경로가 추가될 때 빠진 검증이 바로 걸린다. 사람이 기억해서 확인하는 방식으로는 반드시 빠지는 종류의 문제라 자동화의 효과가 특히 크다.

## 참고

- OWASP — 접근 통제 취약점
- CWE-639 — 사용자 제어 키를 통한 인가 우회
- OWASP API 보안 Top 10`,
    diagram: {
      type: 'flow',
      caption: '식별자를 바꿔 접근하는 흐름',
      steps: [
        { label: '정상 로그인', note: '자기 계정' },
        { label: '식별자 확인', note: '주소에서' },
        { label: '값 변경', note: '다른 번호' },
        { label: '남의 자원 접근', note: '소유자 미확인', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '막는 방법의 위치',
      layers: [
        { label: '질의 조건', note: '가장 확실' },
        { label: '공통 검사', note: '한곳에서' },
        { label: '간접 참조', note: '노출 축소' },
        { label: '식별자 형태', note: '보조' },
      ],
    },
  },
]
