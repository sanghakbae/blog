import type { SeedPost } from './types'

/** 391~400 — 컨테이너와 쿠버네티스 */
export const posts40: SeedPost[] = [
  {
    slug: 'pod-security-standards',
    title: '파드 보안 표준을 단계적으로 적용하기',
    body: `쿠버네티스에서 파드가 요구할 수 있는 권한은 매우 넓다. 호스트 파일 시스템 접근, 특권 모드, 호스트 네트워크 사용까지 가능하다. 표준 수준을 정해 두면 이런 요구를 네임스페이스 단위로 막을 수 있다.

## 어떤 수준이 있나?

| 수준 | 허용 범위 |
| --- | --- |
| 특권 | 제한 없음 |
| 기본 | 알려진 권한 상승 차단 |
| 제한 | 강화된 모범 사례 강제 |

운영 워크로드는 제한 수준을 목표로 한다. 처음부터 제한을 걸면 기존 워크로드가 뜨지 않으므로 단계가 필요하다.

![세 수준과 차단되는 설정](/img/posts/pod-security-standards.svg)

## 제한 수준에서 막히는 것

| 설정 | 이유 |
| --- | --- |
| 특권 모드 | 호스트 장악 가능 |
| 호스트 네트워크·PID | 격리 무력화 |
| 호스트 경로 마운트 | 파일 시스템 접근 |
| 루트 실행 | 권한 상승 기반 |
| 추가 권한 부여 | 필요 이상 |
| 쓰기 가능 루트 | 변조 용이 |

루트 실행 금지가 가장 자주 걸린다. 많은 공개 이미지가 루트로 동작하도록 만들어져 있다.

## 적용 순서

1. 전 네임스페이스에 경고 모드로 제한 수준을 설정한다
2. 배포 시 경고를 수집한다
3. 위반 워크로드 목록을 만든다
4. 이미지와 매니페스트를 고친다
5. 새 네임스페이스부터 강제 수준을 적용한다
6. 기존 네임스페이스를 순차 전환한다

경고 모드가 핵심이다. 막지 않고 알려 주므로 현황을 파악하면서 고칠 수 있다.

![경고에서 강제까지](/img/posts/pod-security-standards-2.svg)

## 이미지를 고치는 방법

\`\`\`dockerfile
# 루트가 아닌 사용자로 실행한다
RUN adduser --system --uid 10001 app
USER 10001
\`\`\`

매니페스트에서도 명시한다. 이미지가 바뀌어도 클러스터 설정이 유지된다.

\`\`\`yaml
securityContext:
  runAsNonRoot: true
  runAsUser: 10001
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: ["ALL"]
\`\`\`

읽기 전용 루트를 켜면 쓰기가 필요한 경로를 임시 볼륨으로 따로 마운트해야 한다. 이 작업이 번거로워 보이지만, 어떤 경로에 쓰는지 파악하는 계기가 된다.

## 예외 처리

모니터링 에이전트처럼 호스트 접근이 필요한 워크로드가 있다. 이런 것은 별도 네임스페이스에 두고 그 네임스페이스만 수준을 낮춘다. 예외를 네임스페이스로 격리하면 어디에 예외가 있는지 명확해진다.

## 실제로 이렇게 터진다

제한 수준을 전 네임스페이스에 한 번에 강제한 사례가 있다. 루트로 동작하는 공개 이미지를 쓰던 워크로드가 전부 뜨지 않았고, 급하게 되돌렸다.

또 하나는 예외 네임스페이스를 만들어 두고 그곳에 일반 워크로드까지 옮긴 경우다. 예외가 통로가 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 켜면 바로 적용된다 | 기존 워크로드가 막힌다 |
| 경고 모드는 의미 없다 | 현황 파악의 유일한 수단 |
| 예외는 편의다 | 격리해야 관리된다 |
| 이미지만 고치면 된다 | 매니페스트에도 명시한다 |

## 전환 확인 항목

| 항목 | 확인 |
| --- | --- |
| 루트 실행 | 비루트 전환 |
| 쓰기 경로 | 임시 볼륨 |
| 추가 권한 | 전부 제거 |
| 호스트 마운트 | 제거 |
| 예외 범위 | 네임스페이스 격리 |

## 경고 모드로 현황 보기

강제 전에 무엇이 걸리는지 센다.

\`\`\`bash
for ns in $(kubectl get ns -o name | cut -d/ -f2); do
  kubectl label --overwrite ns "$ns" \
    pod-security.kubernetes.io/warn=restricted >/dev/null
done
kubectl get events -A --field-selector reason=FailedCreate -o wide | head -20
\`\`\`

배포 시 경고가 뜨는 워크로드가 조치 대상이다. 목록을 만들어 팀별로 나누고, 이미지와 매니페스트를 고친 순서대로 해당 네임스페이스를 강제로 전환한다.

## 참고

- Kubernetes 공식 문서 — Pod Security Standards
- CIS Kubernetes Benchmark
- NSA·CISA — Kubernetes Hardening Guidance`,
    diagram: {
      type: 'layers',
      caption: '보안 수준',
      layers: [
        { label: '특권', note: '제한 없음 — 예외만' },
        { label: '기본', note: '권한 상승 차단' },
        { label: '제한', note: '운영 목표' },
        { label: '예외 네임스페이스', note: '격리된 예외' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '전환 절차',
      steps: [
        { label: '경고 모드 설정', note: '차단하지 않음' },
        { label: '위반 수집', note: '목록 작성' },
        { label: '이미지·매니페스트 수정', note: '비루트 전환' },
        { label: '강제 전환', note: '네임스페이스별' },
      ],
    },
  },
  {
    slug: 'k8s-rbac-audit',
    title: '쿠버네티스 권한을 실제로 점검하는 방법',
    body: `클러스터 권한은 역할과 바인딩이 여러 겹으로 쌓여 최종 권한을 눈으로 계산하기 어렵다. 누가 무엇을 할 수 있는지 확인하려면 도구의 도움이 필요하다.

## 무엇을 먼저 보나?

| 항목 | 위험 |
| --- | --- |
| 클러스터 관리자 바인딩 | 전권 |
| 비밀 읽기 권한 | 자격 증명 노출 |
| 파드 생성 권한 | 특권 파드로 상승 가능 |
| 역할 바인딩 생성 권한 | 스스로 권한 상승 |
| 노드 접근 권한 | 호스트 접근 |
| 임의 자원 접근 | 와일드카드 |

파드 생성 권한이 사실상 관리자 권한이 될 수 있다는 점이 자주 간과된다. 특권 파드를 띄우면 노드를 장악할 수 있다.

![권한 상승 경로](/img/posts/k8s-rbac-audit.svg)

## 조회

\`\`\`bash
# 클러스터 관리자 권한을 가진 주체
kubectl get clusterrolebindings -o json \
  | jq -r '.items[] | select(.roleRef.name=="cluster-admin")
           | .subjects[]? | "\(.kind) \(.name)"' | sort -u

# 비밀을 읽을 수 있는 역할
kubectl get clusterroles -o json \
  | jq -r '.items[] | select(.rules[]? | (.resources[]? == "secrets")
           and (.verbs[]? == "get" or .verbs[]? == "list")) | .metadata.name'
\`\`\`

## 서비스 계정 토큰

기본 서비스 계정 토큰이 자동으로 마운트되면, 파드가 침해됐을 때 그 토큰으로 API 서버에 접근할 수 있다. 필요 없으면 끈다.

\`\`\`yaml
automountServiceAccountToken: false
\`\`\`

파드가 API 서버를 부를 필요가 없다면 이 설정을 기본으로 한다. 대부분의 애플리케이션 파드가 여기 해당한다.

![토큰 마운트와 접근 경로](/img/posts/k8s-rbac-audit-2.svg)

## 정기 점검 항목

| 항목 | 주기 |
| --- | --- |
| 관리자 바인딩 목록 | 월 |
| 와일드카드 사용 역할 | 분기 |
| 미사용 서비스 계정 | 분기 |
| 외부 사용자 매핑 | 인사 변동 시 |
| 네임스페이스별 권한 | 반기 |

관리자 바인딩은 늘어나기만 하고 줄지 않는다. 매달 목록을 보고 이유를 물으면 자연히 줄어든다.

## 최소 권한 설계

역할을 네임스페이스 단위로 만들고 클러스터 범위 역할은 최소화한다. 개발자에게는 자기 네임스페이스의 배포 권한만 주고, 노드나 클러스터 자원 접근은 제외한다. 조회가 필요하면 읽기 전용 역할을 따로 만든다.

## 실제로 이렇게 터진다

개발자에게 파드 생성 권한을 준 사례가 있다. 특권 파드를 만들 수 있었고, 그것으로 노드 파일 시스템에 접근해 다른 파드의 비밀을 읽을 수 있었다. 관리자 권한을 준 적은 없었다.

또 하나는 기본 서비스 계정 토큰이 모든 파드에 마운트된 경우다. 파드 하나가 뚫리자 API 서버 접근이 열렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 관리자 권한만 위험하다 | 파드 생성이 사실상 같다 |
| 토큰은 필요하니 마운트한다 | 대부분 필요 없다 |
| 네임스페이스 권한은 안전하다 | 그 안에서 상승이 가능하다 |
| 목록은 가끔 보면 된다 | 늘기만 한다 |

## 상승 경로 차단

| 권한 | 함께 필요한 통제 |
| --- | --- |
| 파드 생성 | 파드 보안 표준 |
| 비밀 읽기 | 네임스페이스 제한 |
| 역할 바인딩 생성 | 소수만 |
| 노드 접근 | 금지 |

## 상승 경로 가진 주체 찾기

관리자 바인딩만 보면 놓친다. 파드 생성과 비밀 읽기도 함께 본다.

\`\`\`bash
kubectl get clusterrolebindings -o json | jq -r '
  .items[] | select(.roleRef.name=="cluster-admin")
  | .subjects[]? | "cluster-admin \(.kind) \(.name)"'

kubectl get clusterroles -o json | jq -r '
  .items[] | select(any(.rules[]?; (.resources[]? == "pods") and (.verbs[]? == "create")))
  | "pod-create \(.metadata.name)"'
\`\`\`

두 목록을 합친 것이 실질적인 관리자 후보다. 파드 보안 표준이 강제되어 있으면 두 번째 목록의 위험이 크게 준다.

## 참고

- Kubernetes 공식 문서 — RBAC 인가
- CIS Kubernetes Benchmark — RBAC 항목
- NSA·CISA — Kubernetes Hardening Guidance`,
    diagram: {
      type: 'flow',
      caption: '권한 상승 경로',
      steps: [
        { label: '파드 생성 권한', note: '개발자에게 흔함' },
        { label: '특권 파드 생성', note: '표준 미적용 시' },
        { label: '호스트 접근', note: '노드 파일 시스템' },
        { label: '클러스터 장악', note: '자격 증명 획득', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '토큰 마운트 판단',
      x: ['API 접근 필요', '불필요'],
      y: ['권한 제한됨', '넓은 권한'],
      cells: ['허용', '마운트 끔', '재검토', '즉시 조치'],
    },
  },
  {
    slug: 'container-image-minimal',
    title: '컨테이너 이미지를 작게 만들면 얻는 것',
    body: `이미지에 들어 있는 모든 패키지가 취약점 목록에 오른다. 애플리케이션이 쓰지 않는 셸, 패키지 관리자, 디버깅 도구가 그대로 들어 있으면 공격자에게도 그것이 도구가 된다.

## 무엇이 줄어드나?

| 항목 | 효과 |
| --- | --- |
| 취약점 수 | 패키지가 적으면 적다 |
| 공격 도구 | 셸·네트워크 도구 부재 |
| 이미지 크기 | 배포 속도 |
| 스캔 시간 | 파이프라인 단축 |
| 패치 부담 | 갱신할 것이 적다 |

셸이 없으면 침해 후 실행할 수 있는 것이 크게 줄어든다. 원격 코드 실행에 성공해도 다음 단계가 어렵다.

![기본 이미지 선택에 따른 차이](/img/posts/container-image-minimal.svg)

## 기본 이미지 선택

| 유형 | 크기 | 특징 |
| --- | --- | --- |
| 일반 배포판 | 큼 | 도구 다수 |
| 경량 배포판 | 중간 | 패키지 관리자 있음 |
| 배포 전용 이미지 | 작음 | 셸 없음 |
| 정적 바이너리 + 빈 이미지 | 최소 | 아무것도 없음 |

정적 링크가 가능한 언어라면 빈 이미지에 바이너리만 넣을 수 있다. 취약점 목록이 거의 비게 된다.

## 다단계 빌드

\`\`\`dockerfile
FROM golang:1.23 AS build
WORKDIR /src
COPY . .
RUN CGO_ENABLED=0 go build -o /app ./cmd/server

FROM gcr.io/distroless/static:nonroot
COPY --from=build /app /app
USER nonroot
ENTRYPOINT ["/app"]
\`\`\`

빌드 도구는 첫 단계에만 있고 최종 이미지에는 바이너리만 남는다. 컴파일러와 빌드 의존성이 운영 환경에 가지 않는다.

![다단계 빌드로 남는 것](/img/posts/container-image-minimal-2.svg)

## 디버깅은 어떻게 하나

셸이 없으면 문제가 생겼을 때 들어가서 볼 수 없다. 대신 임시 디버그 컨테이너를 붙이는 기능을 쓴다.

\`\`\`bash
# 실행 중 파드에 디버그 컨테이너를 임시로 붙인다
kubectl debug -it pod/myapp --image=busybox --target=app
\`\`\`

이 방식은 필요할 때만 도구를 가져오고 끝나면 사라진다. 상시로 도구를 넣어 두는 것보다 안전하다.

## 그 밖에 확인할 것

| 항목 | 확인 |
| --- | --- |
| 빌드 캐시 잔여 | 최종 이미지에 없어야 |
| 비밀 값 | 레이어에 남지 않게 |
| 설정 파일 | 실행 시 주입 |
| 사용자 | 비루트 |
| 레이블 | 출처·버전 표시 |

비밀 값이 중간 레이어에 남는 사고가 흔하다. 나중 레이어에서 지워도 앞 레이어에 남아 있으므로, 애초에 넣지 않는다.

이미지를 줄이는 작업은 한 번 하면 유지된다. 기본 이미지를 바꾸고 다단계 빌드를 적용하면 이후 빌드는 자동으로 작은 이미지를 만든다. 초기에 며칠 걸리는 작업이지만 취약점 대응 부담이 계속 줄어든다.

## 실제로 이렇게 터진다

일반 배포판 기반 이미지를 쓰다 취약점 알림이 매주 수십 건 오던 사례가 있다. 대부분 애플리케이션이 쓰지도 않는 패키지였고, 대응 부담에 지쳐 알림을 껐다.

또 하나는 빌드 단계 비밀이 중간 레이어에 남은 경우다. 최종 이미지에서 지웠지만 이력에 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 취약점이 많은 건 어쩔 수 없다 | 기본 이미지를 바꾸면 준다 |
| 지운 파일은 없어진다 | 앞 레이어에 남는다 |
| 디버깅 도구가 필요하다 | 임시 컨테이너로 붙인다 |
| 작게 만들기 어렵다 | 다단계 빌드면 된다 |

## 전환 효과

| 항목 | 변화 |
| --- | --- |
| 취약점 알림 | 크게 감소 |
| 이미지 크기 | 감소 |
| 배포 시간 | 단축 |
| 침해 후 활동 | 제한 |

## 이미지별 취약점 수 비교

기본 이미지를 바꾸면 얼마나 줄어드는지 실제로 재 본다.

\`\`\`bash
for img in app:current app:distroless; do
  printf '%-24s ' "$img"
  trivy image --quiet --severity HIGH,CRITICAL --format json "$img" \
    | jq '[.Results[]?.Vulnerabilities // [] | length] | add // 0'
done
\`\`\`

숫자 차이가 전환의 근거가 된다. 대개 한 자릿수로 떨어진다. 이 결과를 보여 주면 팀 설득이 쉬워진다.

## 참고

- OWASP — Docker Security Cheat Sheet
- CIS Docker Benchmark
- 각 배포 전용 이미지 프로젝트 문서`,
    diagram: {
      type: 'bars',
      caption: '기본 이미지별 취약점 수 경향',
      unit: '건',
      items: [
        { label: '일반 배포판', value: 120, note: '패키지 다수' },
        { label: '경량 배포판', value: 35, note: '중간' },
        { label: '배포 전용', value: 8, note: '셸 없음' },
        { label: '정적 + 빈 이미지', value: 1, note: '최소' },
      ],
    },
    diagram2: {
      type: 'flow',
      caption: '다단계 빌드',
      steps: [
        { label: '빌드 단계', note: '컴파일러·의존성' },
        { label: '바이너리 생성', note: '정적 링크' },
        { label: '최소 이미지로 복사', note: '바이너리만' },
        { label: '비루트 실행', note: '운영 배포' },
      ],
    },
  },
  {
    slug: 'k8s-node-hardening',
    title: '쿠버네티스 노드 자체를 지키는 방법',
    body: `파드 수준 통제에 집중하다 보면 그것이 올라가는 노드가 소홀해진다. 노드가 뚫리면 그 위의 모든 파드와 비밀에 접근할 수 있으므로, 노드는 별도의 보호 대상이다.

## 무엇이 위험한가?

| 항목 | 결과 |
| --- | --- |
| 노드 접속 허용 | 직접 조작 |
| 컨테이너 런타임 소켓 노출 | 특권 획득 |
| 노드 자격 증명 | 클러스터 접근 |
| 마운트된 비밀 | 다른 파드의 값 |
| 커널 취약점 | 컨테이너 탈출 |

런타임 소켓이 파드에 마운트되면 사실상 노드 관리자 권한이다. 이 설정을 어드미션 정책으로 차단한다.

![노드 침해가 미치는 범위](/img/posts/k8s-node-hardening.svg)

## 강화 항목

| 항목 | 설정 |
| --- | --- |
| 직접 접속 | 원칙적 차단 |
| 최소 운영체제 | 컨테이너 전용 배포판 |
| 자동 갱신 | 활성화 |
| 노드 권한 | 최소 |
| 소켓 마운트 | 차단 |
| 커널 옵션 | 강화 적용 |

컨테이너 전용 배포판을 쓰면 불필요한 패키지가 없어 공격면이 크게 준다. 셸 접속 자체가 제한된다.

## 노드 접근 통제

관리가 필요하면 오케스트레이터를 통해 한다. 직접 접속을 열어 두면 통제 지점이 늘어난다.

| 작업 | 방법 |
| --- | --- |
| 로그 확인 | 중앙 수집 |
| 디버깅 | 임시 디버그 파드 |
| 갱신 | 노드 교체 |
| 문제 해결 | 노드 격리 후 조사 |

노드를 고치기보다 교체하는 방식이 안전하다. 상태를 남기지 않는 구성이면 교체가 쉽다.

![노드 관리 방식](/img/posts/k8s-node-hardening-2.svg)

## 노드 권한 제한

노드에 붙은 클라우드 역할이 넓으면 파드 하나가 뚫려도 그 권한을 쓸 수 있다.

| 방법 | 효과 |
| --- | --- |
| 노드 역할 최소화 | 기본 |
| 파드별 신원 사용 | 노드 권한 미사용 |
| 메타데이터 접근 차단 | 자격 증명 탈취 방지 |

메타데이터 서비스 접근을 파드에서 차단하는 것이 중요하다. 그러지 않으면 파드가 노드 자격 증명을 가져갈 수 있다.

## 점검

\`\`\`bash
# 런타임 소켓을 마운트한 파드 — 즉시 검토 대상
kubectl get pods -A -o json \
  | jq -r '.items[] | select(.spec.volumes[]?.hostPath.path | test("docker.sock|containerd.sock"))
           | "\(.metadata.namespace)/\(.metadata.name)"'
\`\`\`

## 노드 그룹 분리

민감한 워크로드를 별도 노드 그룹에 두면, 다른 워크로드가 침해돼도 같은 노드에 있지 않다. 결제나 개인정보 처리 파드를 분리하는 것이 흔한 구성이다.

## 실제로 이렇게 터진다

로그 수집을 위해 컨테이너 런타임 소켓을 파드에 마운트한 사례가 있다. 그 파드가 침해되자 노드의 모든 컨테이너를 조작할 수 있었다.

또 하나는 파드에서 메타데이터 서비스 접근을 막지 않은 경우다. 파드 하나로 노드 역할의 클라우드 자격 증명을 가져갔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 파드만 지키면 된다 | 노드가 뚫리면 전부다 |
| 소켓 마운트는 흔한 방식이다 | 사실상 노드 관리자 권한 |
| 노드 접속은 필요하다 | 대부분 대체 가능 |
| 관리형이면 안전하다 | 설정은 우리 몫 |

## 차단 항목

| 항목 | 방법 |
| --- | --- |
| 런타임 소켓 마운트 | 어드미션 정책 |
| 메타데이터 접근 | 네트워크 정책 |
| 호스트 경로 마운트 | 파드 보안 표준 |
| 노드 직접 접속 | 권한 회수 |

## 노드 접근 경로 점검

파드에서 노드나 클라우드 자격 증명에 닿는 경로를 찾는다.

\`\`\`bash
# 런타임 소켓·호스트 경로를 마운트한 파드
kubectl get pods -A -o json | jq -r '
  .items[] | select(any(.spec.volumes[]?; .hostPath != null))
  | "\(.metadata.namespace)/\(.metadata.name) \(.spec.volumes[] | select(.hostPath).hostPath.path)"' | head -20
\`\`\`

소켓이나 시스템 경로가 보이면 즉시 검토한다. 이어서 파드에서 메타데이터 주소로 요청이 나가는지 네트워크 정책으로 막혀 있는지 확인한다.

## 참고

- CIS Kubernetes Benchmark — 노드 구성
- NSA·CISA — Kubernetes Hardening Guidance
- 각 관리형 서비스의 노드 보안 문서`,
    diagram: {
      type: 'flow',
      caption: '노드 침해 경로',
      steps: [
        { label: '파드 침해', note: '애플리케이션 취약점' },
        { label: '소켓·메타데이터 접근', note: '차단 안 됨' },
        { label: '노드 권한 획득', note: '자격 증명' },
        { label: '클러스터 확산', note: '다른 파드 접근', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '강화 계층',
      layers: [
        { label: '최소 운영체제', note: '공격면 축소' },
        { label: '직접 접속 차단', note: '통제 지점 단일화' },
        { label: '소켓·메타데이터 차단', note: '권한 탈취 방지' },
        { label: '노드 그룹 분리', note: '영향 범위 제한' },
      ],
    },
  },
  {
    slug: 'k8s-secret-handling',
    title: '쿠버네티스 비밀 객체를 안전하게 쓰는 법',
    body: `비밀 객체는 기본적으로 인코딩만 되어 있고 암호화되어 있지 않다. 저장소에 그대로 남고, 읽기 권한이 있으면 누구나 볼 수 있다. 이름만으로 안전하다고 생각하면 안 된다.

## 어디가 문제인가?

| 항목 | 상태 |
| --- | --- |
| 인코딩 | 암호화가 아니다 |
| 저장소 저장 | 기본은 평문 |
| 접근 권한 | 넓게 부여되기 쉽다 |
| 환경 변수 노출 | 프로세스 목록·오류 보고 |
| 매니페스트 | 저장소에 커밋됨 |
| 백업 | 함께 백업 |

매니페스트를 저장소에 커밋하는 것이 가장 흔한 사고다. 인코딩된 값이라 눈에 잘 띄지 않는다.

![비밀이 노출되는 경로](/img/posts/k8s-secret-handling.svg)

## 개선 순서

1. 저장소 수준 암호화를 켠다
2. 비밀 읽기 권한을 좁힌다
3. 매니페스트에서 비밀 값을 제거한다
4. 외부 비밀 관리 서비스와 연동한다
5. 환경 변수 대신 파일 마운트를 쓴다
6. 교체 절차를 만든다

1번은 클러스터 설정 하나로 되고 효과가 크다. 저장소가 유출돼도 복호화가 필요해진다.

## 외부 연동

외부 비밀 관리 서비스에서 값을 가져오는 방식이 권장된다. 클러스터에는 참조만 두고 실제 값은 외부에 있다.

| 방식 | 특징 |
| --- | --- |
| 연동 컨트롤러 | 외부 값을 동기화 |
| 사이드카 주입 | 파일로 제공 |
| 애플리케이션 직접 조회 | 코드 변경 필요 |

연동 컨트롤러 방식이 애플리케이션 변경 없이 적용된다.

![외부 관리 서비스 연동](/img/posts/k8s-secret-handling-2.svg)

## 환경 변수보다 파일

환경 변수는 프로세스 목록에 보이고 오류 보고 도구가 수집하기도 한다. 파일로 마운트하면 이런 노출이 줄고, 값이 바뀌었을 때 다시 읽을 수도 있다.

\`\`\`yaml
volumeMounts:
  - name: db-secret
    mountPath: /etc/secrets
    readOnly: true
\`\`\`

## 저장소에 들어가지 않게

\`\`\`bash
# 커밋 전 검사 — 인코딩된 비밀 객체를 찾는다
grep -rn 'kind: Secret' --include='*.yaml' . \
  | cut -d: -f1 | sort -u | xargs grep -l 'data:' 2>/dev/null
\`\`\`

비밀 값을 담은 매니페스트는 저장소에 두지 않는다. 값이 필요한 자리는 참조로 두고, 배포 시점에 주입한다. 이미 커밋됐다면 값을 교체하는 것이 먼저다. 이력에서 지우는 것만으로는 부족하다.

## 실제로 이렇게 터진다

비밀 매니페스트를 저장소에 커밋한 사례가 있다. 인코딩된 값이라 눈에 띄지 않았고, 저장소가 공개로 바뀌면서 그대로 노출됐다.

또 하나는 저장소 수준 암호화를 켜지 않은 경우다. 클러스터 백업이 유출되자 모든 비밀이 평문으로 나왔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 비밀 객체는 암호화된다 | 기본은 인코딩만 |
| 인코딩이면 안 보인다 | 한 줄로 복원된다 |
| 읽기 권한은 넓어도 된다 | 모든 비밀이 열린다 |
| 환경 변수가 편하다 | 노출 경로가 많다 |

## 개선 순서

| 순서 | 조치 | 난이도 |
| --- | --- | --- |
| 1 | 저장소 암호화 활성화 | 낮음 |
| 2 | 읽기 권한 축소 | 낮음 |
| 3 | 매니페스트에서 값 제거 | 중간 |
| 4 | 외부 관리 서비스 연동 | 중간 |
| 5 | 파일 마운트 전환 | 낮음 |

## 저장소 암호화 확인

설정 하나로 되고 효과가 크므로 먼저 확인한다.

\`\`\`bash
# 저장 데이터 암호화 설정 여부 (관리형 서비스는 콘솔·API 로 확인)
kubectl get secrets -A --no-headers | wc -l
kubectl auth can-i list secrets --all-namespaces --as=system:serviceaccount:default:default
\`\`\`

두 번째 명령이 yes 를 반환하면 기본 서비스 계정이 전 비밀을 읽을 수 있다는 뜻이다. 권한 축소가 급하다. 비밀 개수가 많으면 외부 관리 서비스 연동을 검토한다.

## 참고

- Kubernetes 공식 문서 — Secrets, 저장 데이터 암호화
- NSA·CISA — Kubernetes Hardening Guidance
- OWASP — Secrets Management Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '노출 경로',
      steps: [
        { label: '매니페스트 작성', note: '인코딩된 값' },
        { label: '저장소 커밋', note: '이력에 남음' },
        { label: '클러스터 저장', note: '기본 평문' },
        { label: '환경 변수 노출', note: '프로세스·오류 보고', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '개선 계층',
      layers: [
        { label: '저장소 암호화', note: '설정 하나' },
        { label: '권한 축소', note: '읽기 제한' },
        { label: '외부 관리 연동', note: '값은 밖에' },
        { label: '파일 마운트', note: '환경 변수 대신' },
      ],
    },
  },
  {
    slug: 'admission-control-policy',
    title: '어드미션 컨트롤로 배포를 검사하는 방법',
    body: `배포된 뒤에 문제를 찾으면 이미 실행 중이다. 클러스터가 자원을 받아들이는 시점에 검사하면 조건에 맞지 않는 것을 아예 들이지 않을 수 있다.

## 배포 시 무엇을 검사하나?

| 항목 | 예 |
| --- | --- |
| 이미지 출처 | 승인된 저장소만 |
| 이미지 태그 | latest 금지 |
| 서명 검증 | 서명된 이미지만 |
| 자원 제한 | 요청·상한 필수 |
| 보안 설정 | 비루트, 권한 제한 |
| 레이블 | 소유자·환경 필수 |
| 네트워크 | 호스트 네트워크 금지 |

이미지 출처 제한이 효과가 가장 크다. 외부 저장소에서 임의 이미지를 가져오는 경로가 막힌다.

![배포 요청이 검사를 거치는 흐름](/img/posts/admission-control-policy.svg)

## 쿠버네티스 정책 작성 방식

| 방식 | 특징 |
| --- | --- |
| 내장 기능 | 설정만으로 기본 검사 |
| 정책 엔진 | 선언형 규칙, 유연 |
| 사용자 웹훅 | 임의 로직, 관리 부담 |

정책 엔진을 쓰는 것이 일반적이다. 규칙을 코드로 관리하고 시험할 수 있다.

## 경고에서 차단으로

정책을 바로 차단으로 걸면 배포가 멈춘다. 경고 모드로 시작해 위반 현황을 보고, 정리한 뒤 차단으로 바꾼다.

| 단계 | 모드 | 기간 |
| --- | --- | --- |
| 1 | 감사만 | 2주 |
| 2 | 경고 | 2주 |
| 3 | 신규만 차단 | 1개월 |
| 4 | 전체 차단 | 이후 |

![단계별 전환](/img/posts/admission-control-policy-2.svg)

## 가용성 고려

검사 웹훅이 응답하지 않으면 배포가 전부 막힌다. 실패 시 동작을 어떻게 할지 정해야 한다.

| 설정 | 결과 |
| --- | --- |
| 실패 시 거부 | 안전하지만 장애 위험 |
| 실패 시 허용 | 가용성 우선, 검사 우회 |

중요한 검사는 거부로, 보조적인 검사는 허용으로 두는 방식이 균형이 좋다. 웹훅 자체를 여러 개 띄워 가용성을 확보하는 것이 먼저다.

## 정책 시험

\`\`\`bash
# 정책이 의도대로 동작하는지 배포 전에 확인한다
kubectl apply --dry-run=server -f deployment.yaml
\`\`\`

서버 측 시험 실행을 쓰면 실제 검사를 거치되 자원은 만들어지지 않는다. 파이프라인에 넣으면 배포 전에 위반을 발견할 수 있다.

정책 자체도 시험 대상이다. 허용되어야 할 매니페스트와 거부되어야 할 매니페스트를 준비해 두고, 정책을 바꿀 때마다 확인한다.

## 실제로 이렇게 터진다

검사 웹훅을 이중화하지 않고 실패 시 거부로 설정한 사례가 있다. 웹훅 파드가 죽자 클러스터 전체에서 배포가 막혔다.

또 하나는 정책을 바로 차단으로 걸어 기존 워크로드의 재시작이 실패한 경우다. 스케일 아웃이 되지 않아 장애로 이어졌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 정책은 배포 때만 평가된다 | 재시작·스케일에도 적용된다 |
| 실패 시 거부가 안전하다 | 가용성 위험이 크다 |
| 바로 차단하면 빠르다 | 장애가 난다 |
| 정책은 시험이 필요 없다 | 회귀가 생긴다 |

## 안전한 운영

| 항목 | 설정 |
| --- | --- |
| 웹훅 가용성 | 다중 인스턴스 |
| 실패 동작 | 중요 정책만 거부 |
| 적용 단계 | 감사 → 경고 → 차단 |
| 정책 시험 | 통과·거부 예시 보관 |
| 예외 | 네임스페이스 지정 |

## 정책 시험 세트

정책을 바꿀 때마다 통과·거부 예시로 확인한다.

\`\`\`bash
for f in policy-tests/should-pass/*.yaml; do
  kubectl apply --dry-run=server -f "$f" >/dev/null 2>&1 || echo "통과해야 하는데 거부: $f"
done
for f in policy-tests/should-fail/*.yaml; do
  kubectl apply --dry-run=server -f "$f" >/dev/null 2>&1 && echo "거부해야 하는데 통과: $f"
done
\`\`\`

이 시험을 파이프라인에 넣으면 정책 변경으로 생기는 회귀를 배포 전에 잡는다.

## 참고

- Kubernetes 공식 문서 — Admission Controllers
- CNCF 정책 엔진 프로젝트 문서
- NSA·CISA — Kubernetes Hardening Guidance`,
    diagram: {
      type: 'flow',
      caption: '검사 흐름',
      steps: [
        { label: '배포 요청', note: 'API 서버 수신' },
        { label: '인증·인가', note: '기본 검사' },
        { label: '어드미션 검사', note: '정책 평가' },
        { label: '수락 또는 거부', note: '자원 생성 여부' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '전환 단계',
      steps: [
        { label: '감사만', note: '현황 파악' },
        { label: '경고', note: '배포는 진행' },
        { label: '신규 차단', note: '기존 유예' },
        { label: '전체 차단', note: '정리 완료 후' },
      ],
    },
  },
  {
    slug: 'container-runtime-detection',
    title: '컨테이너 실행 중 이상 행위를 탐지하기',
    body: `이미지 스캔은 빌드 시점의 취약점을 본다. 실행 중에 무엇이 일어나는지는 다른 문제다. 정상 이미지로 시작한 컨테이너가 외부에서 내려받은 코드를 실행하는 상황은 스캔으로 알 수 없다.

## 무엇을 탐지하나?

| 행위 | 의미 |
| --- | --- |
| 예상 밖 프로세스 실행 | 침해 신호 |
| 셸 실행 | 대부분의 앱에서 비정상 |
| 컨테이너 탈출 시도 | 심각 |
| 민감 파일 접근 | 자격 증명 탐색 |
| 예상 밖 네트워크 연결 | 외부 통신 |
| 권한 상승 시도 | 상승 시도 |

셸 실행이 가장 유용한 신호다. 애플리케이션 컨테이너에서 셸이 뜰 일은 거의 없다.

![빌드 시점과 실행 시점의 관찰 범위](/img/posts/container-runtime-detection.svg)

## 어떻게 관찰하나

| 방식 | 특징 |
| --- | --- |
| 커널 이벤트 수집 | 세밀, 부하 있음 |
| eBPF 기반 | 세밀, 부하 적음 |
| 감사 로그 | 노드 단위 |
| 오케스트레이터 이벤트 | 거친 수준 |

eBPF 기반 도구가 최근 표준적인 선택이다. 커널 모듈 없이 세밀한 관찰이 가능하다.

## 기준선을 만든다

정상 동작을 모르면 이상을 판단할 수 없다. 워크로드마다 실행되는 프로세스와 접근하는 파일, 연결하는 대상을 며칠 수집해 기준선으로 삼는다.

| 항목 | 기준선 |
| --- | --- |
| 실행 프로세스 | 목록 |
| 열리는 파일 | 경로 패턴 |
| 네트워크 대상 | 주소·포트 |
| 시스템 호출 | 사용 범위 |

![기준선과 이상 판단](/img/posts/container-runtime-detection-2.svg)

## 경보 설계

전부 경보로 만들면 잡음이 된다. 등급을 나눈다.

| 등급 | 예 |
| --- | --- |
| 즉시 대응 | 컨테이너 탈출, 자격 증명 파일 접근 |
| 조사 | 셸 실행, 예상 밖 연결 |
| 기록만 | 기준선 밖 파일 접근 |

## 자동 대응

탐지 후 자동으로 컨테이너를 격리하거나 종료하는 구성이 가능하다. 다만 오탐이 서비스 중단이 되므로, 처음에는 기록만 하고 오탐률을 확인한 뒤 자동 조치를 켠다. 종료보다 네트워크 격리가 안전한 첫 자동 조치다.

런타임 탐지 도구는 노드마다 에이전트를 띄우므로 자원을 소모한다. 도입 전에 부하를 측정하고, 관찰 범위를 필요한 만큼만 켠다. 모든 시스템 호출을 추적하면 부하가 크므로, 관심 있는 유형만 선택하는 편이 실용적이다.

## 실제로 이렇게 터진다

이미지 스캔은 깨끗했지만 실행 중 컨테이너가 외부에서 스크립트를 받아 채굴을 시작한 사례가 있다. 빌드 시점에는 없던 코드라 스캔으로는 알 수 없었다.

또 하나는 에이전트를 일부 노드에만 배포한 경우다. 배포율은 80%였고 공격은 나머지 20%에서 일어났다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 이미지 스캔이면 충분하다 | 실행 중 변화를 못 본다 |
| 배포율이 높으면 된다 | 사각지대가 표적이 된다 |
| 탐지되면 막힌다 | 대응을 구성해야 한다 |
| 부하가 없다 | 자원을 소모한다 |

## 도입 시 확인

| 항목 | 확인 |
| --- | --- |
| 에이전트 상태 | 비정상 노드 |
| 부하 | 노드 자원 |
| 기준선 | 정상 동작 수집 |
| 경보 등급 | 대응 차등 |
| 자동 조치 | 격리부터 |

## 사각지대 확인

배포율이 아니라 정상 동작 노드 수를 본다.

\`\`\`bash
kubectl get nodes -o name | wc -l
kubectl get pods -n security -l app=runtime-agent \
  --field-selector status.phase=Running -o name | wc -l
\`\`\`

두 숫자가 다르면 그 차이가 사각지대다. 에이전트가 뜨지 않는 노드는 대개 자원 부족이나 노드 유형 차이가 원인이다. 주간 점검 항목에 넣는다.

## 참고

- CNCF 런타임 보안 프로젝트 문서
- MITRE ATT&CK — Containers 매트릭스
- NSA·CISA — Kubernetes Hardening Guidance`,
    diagram: {
      type: 'matrix',
      caption: '관찰 범위',
      x: ['빌드 시점', '실행 시점'],
      y: ['알려진 취약점', '행위 이상'],
      cells: ['이미지 스캔', '런타임 탐지', '탐지 어려움', '런타임 탐지'],
    },
    diagram2: {
      type: 'steps',
      caption: '도입 순서',
      steps: [
        { label: '기준선 수집', note: '정상 동작 파악' },
        { label: '규칙 조정', note: '오탐 제거' },
        { label: '경보 등급 분리', note: '대응 차등' },
        { label: '자동 조치', note: '격리부터' },
      ],
    },
  },
  {
    slug: 'image-signing-verification',
    title: '컨테이너 이미지 서명을 검증까지 연결하기',
    body: `이미지에 서명하는 것과 서명을 검증하는 것은 다른 일이다. 서명만 하고 검증하지 않으면 아무 효과가 없다. 클러스터가 서명되지 않은 이미지를 거부해야 비로소 통제가 된다.

## 무엇을 보장하나?

| 항목 | 보장 |
| --- | --- |
| 출처 | 우리 파이프라인이 만들었다 |
| 무결성 | 만든 뒤 바뀌지 않았다 |
| 시점 | 언제 만들어졌다 |
| 조건 | 어떤 커밋·브랜치에서 |

태그는 바뀔 수 있으므로 다이제스트로 참조하고 서명을 검증하는 조합이 필요하다.

![서명 생성과 검증의 연결](/img/posts/image-signing-verification.svg)

## 서명 방식

| 방식 | 키 관리 |
| --- | --- |
| 장기 키 | 보관 부담 |
| 키 없는 서명 | 신원 기반, 관리 부담 적음 |
| 하드웨어 보관 | 강함, 자동화 어려움 |

키 없는 방식은 파이프라인의 신원으로 서명하고 투명성 로그에 기록한다. 장기 키를 관리할 필요가 없어 널리 쓰인다.

## 파이프라인에서

\`\`\`bash
# 빌드 후 서명 — 파이프라인 신원 사용
cosign sign --yes "$IMAGE@$DIGEST"

# 출처 증명 첨부
cosign attest --yes --predicate provenance.json "$IMAGE@$DIGEST"
\`\`\`

## 클러스터에서 검증

어드미션 정책으로 서명되지 않은 이미지를 거부한다. 검증 조건에 서명 주체를 명시해야 의미가 있다. 아무 서명이나 통과시키면 공격자가 자기 서명을 붙일 수 있다.

| 조건 | 확인 |
| --- | --- |
| 서명 존재 | 필수 |
| 서명 주체 | 우리 파이프라인 |
| 저장소 | 우리 저장소 |
| 워크플로 | 지정 파일 |

![검증 조건이 없으면 의미가 없다](/img/posts/image-signing-verification-2.svg)

## 단계적 적용

1. 파이프라인에서 서명을 시작한다
2. 검증을 감사 모드로 켠다
3. 서명되지 않은 이미지 목록을 만든다
4. 외부 이미지를 내부 저장소로 미러링하고 서명한다
5. 검증을 차단 모드로 전환한다

4번이 가장 많은 작업이다. 공개 저장소 이미지를 그대로 쓰고 있다면 미러링 구조를 먼저 만들어야 한다.

## 외부 이미지 처리

공개 이미지를 검토한 뒤 내부 저장소에 복사하고 우리 서명을 붙인다. 이렇게 하면 검증 조건을 하나로 유지할 수 있고, 외부 저장소 장애나 삭제에도 영향받지 않는다.

## 실제로 이렇게 터진다

서명은 시작했지만 검증을 켜지 않은 사례가 있다. 파이프라인이 서명을 붙였고 클러스터는 아무것도 확인하지 않았다. 서명이 장식이 됐다.

또 하나는 검증을 켰지만 서명 주체를 확인하지 않은 경우다. 아무 서명이나 통과했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 서명하면 안전하다 | 검증해야 의미가 있다 |
| 서명 존재만 보면 된다 | 주체를 확인해야 한다 |
| 태그로 참조하면 된다 | 다이제스트여야 고정된다 |
| 외부 이미지는 예외다 | 미러링 후 서명한다 |

## 검증 조건

| 항목 | 확인 |
| --- | --- |
| 서명 존재 | 필수 |
| 서명 주체 | 우리 파이프라인 |
| 저장소 | 우리 저장소 |
| 워크플로 | 지정 파일 |
| 참조 방식 | 다이제스트 |

## 검증이 실제로 걸리는지

서명되지 않은 이미지를 배포해 본다.

\`\`\`bash
kubectl run test-unsigned --image=alpine:latest --dry-run=server -o name \
  && echo '통과 — 검증이 없거나 조건이 느슨하다' \
  || echo '차단됨'
\`\`\`

통과하면 검증이 동작하지 않는 것이다. 이어서 다른 주체가 서명한 이미지도 시험한다. 그것까지 통과하면 서명 존재만 보고 주체를 확인하지 않는 상태다.

## 참고

- Sigstore 프로젝트 문서
- SLSA 프레임워크
- NSA·CISA — 소프트웨어 공급망 보안 지침`,
    diagram: {
      type: 'flow',
      caption: '서명과 검증',
      steps: [
        { label: '빌드', note: '파이프라인' },
        { label: '서명', note: '신원 기반' },
        { label: '저장소 등록', note: '다이제스트 고정' },
        { label: '클러스터 검증', note: '조건 확인 후 수락' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검증 조건',
      x: ['주체 확인', '서명 존재만'],
      y: ['차단 모드', '감사 모드'],
      cells: ['효과 있음', '우회 가능', '현황 파악', '의미 없음'],
    },
  },
  {
    slug: 'sidecar-trust',
    title: '사이드카 컨테이너를 신뢰 경계로 볼 것인가',
    body: `사이드카는 같은 파드 안에서 네트워크와 일부 자원을 공유한다. 편리한 구조지만, 사이드카가 침해되면 주 컨테이너의 통신과 파일에 접근할 수 있다는 뜻이기도 하다.

## 무엇을 공유하나?

| 자원 | 공유 여부 |
| --- | --- |
| 네트워크 이름공간 | 공유 |
| 로컬 통신 | 서로 접근 가능 |
| 볼륨 | 마운트한 것 |
| 프로세스 이름공간 | 설정에 따라 |
| 서비스 계정 토큰 | 공유 |
| 파일 시스템 | 기본 분리 |

네트워크 공유가 핵심이다. 사이드카는 주 컨테이너의 로컬 포트에 접근할 수 있으므로, 인증 없이 열린 관리 포트가 있으면 그대로 노출된다.

![파드 안에서 공유되는 것](/img/posts/sidecar-trust.svg)

## 위험한 조합

| 상황 | 위험 |
| --- | --- |
| 외부 제공 사이드카 | 코드 신뢰 필요 |
| 주 컨테이너의 관리 포트 개방 | 로컬 접근 |
| 공유 볼륨에 비밀 | 사이드카가 읽음 |
| 프로세스 이름공간 공유 | 메모리 접근 가능 |
| 서비스 계정 토큰 마운트 | 사이드카도 사용 |

외부에서 가져온 사이드카 이미지는 우리 코드와 같은 신뢰 수준으로 다뤄야 한다. 로그 수집기나 프록시도 예외가 아니다.

## 줄이는 방법

1. 주 컨테이너의 관리 포트를 로컬에도 열지 않거나 인증을 건다
2. 볼륨 마운트를 필요한 컨테이너에만 한다
3. 프로세스 이름공간 공유를 끈다
4. 사이드카 이미지를 검토하고 서명을 검증한다
5. 사이드카에도 최소 권한 설정을 적용한다

3번은 기본값이 분리이므로 명시적으로 켜지 않으면 된다. 디버깅을 위해 켜 두고 잊는 경우가 있다.

![신뢰 수준별 배치](/img/posts/sidecar-trust-2.svg)

## 대안

사이드카 대신 노드 단위 에이전트나 별도 서비스로 분리하는 방법이 있다. 파드 자원을 공유하지 않으므로 경계가 명확해진다.

| 방식 | 격리 | 편의 |
| --- | --- | --- |
| 사이드카 | 낮음 | 높음 |
| 노드 에이전트 | 중간 | 중간 |
| 별도 서비스 | 높음 | 낮음 |

로그 수집처럼 모든 파드에 필요한 기능은 노드 에이전트가 적합하다. 파드마다 사이드카를 붙이면 자원 소모도 크다.

## 점검

\`\`\`bash
# 프로세스 이름공간을 공유하는 파드 — 검토 대상
kubectl get pods -A -o json \
  | jq -r '.items[] | select(.spec.shareProcessNamespace == true)
           | "\(.metadata.namespace)/\(.metadata.name)"'
\`\`\`

## 실제로 이렇게 터진다

외부에서 가져온 로그 수집 사이드카를 붙인 사례가 있다. 그 컨테이너가 파드의 서비스 계정 토큰을 읽을 수 있었고, 주 컨테이너의 관리 포트에도 접근할 수 있었다.

또 하나는 디버깅을 위해 프로세스 이름공간 공유를 켜 두고 잊은 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 사이드카는 보조 역할이다 | 같은 신뢰 수준이다 |
| 파일 시스템이 분리돼 안전하다 | 네트워크는 공유한다 |
| 공식 이미지면 괜찮다 | 검토 대상이다 |
| 편의를 위한 설정은 무해하다 | 남으면 경로가 된다 |

## 확인 항목

| 항목 | 확인 |
| --- | --- |
| 로컬 관리 포트 | 인증 여부 |
| 볼륨 마운트 | 필요한 것만 |
| 프로세스 공유 | 꺼져 있나 |
| 토큰 마운트 | 필요한가 |
| 이미지 출처 | 검토·서명 |

## 파드 내 공유 범위 확인

의도하지 않은 공유 설정이 남아 있는지 본다.

\`\`\`bash
kubectl get pods -A -o json | jq -r '
  .items[]
  | select(.spec.shareProcessNamespace == true or .spec.hostNetwork == true
           or .spec.hostPID == true)
  | "\(.metadata.namespace)/\(.metadata.name) pid=\(.spec.shareProcessNamespace // false) net=\(.spec.hostNetwork // false)"'
\`\`\`

디버깅을 위해 켜 두고 잊은 설정이 나오는 경우가 많다. 주 컨테이너의 로컬 관리 포트에 인증이 걸려 있는지도 함께 확인한다.

## 참고

- Kubernetes 공식 문서 — 파드 개념과 이름공간 공유
- NSA·CISA — Kubernetes Hardening Guidance
- OWASP — Kubernetes Security Cheat Sheet`,
    diagram: {
      type: 'layers',
      caption: '파드 내 공유 범위',
      layers: [
        { label: '네트워크 이름공간', note: '항상 공유' },
        { label: '서비스 계정 토큰', note: '공유' },
        { label: '볼륨', note: '마운트한 것만' },
        { label: '프로세스 이름공간', note: '설정 시 공유' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '배치 방식별 격리',
      unit: '점',
      items: [
        { label: '별도 서비스', value: 90, note: '경계 명확' },
        { label: '노드 에이전트', value: 65, note: '중간' },
        { label: '사이드카', value: 35, note: '자원 공유' },
        { label: '같은 프로세스', value: 10, note: '격리 없음' },
      ],
    },
  },
  {
    slug: 'k8s-audit-log-use',
    title: '쿠버네티스 감사 로그를 조사에 쓰는 법',
    body: `클러스터에서 무슨 일이 있었는지는 감사 로그에만 남는다. 기본 설정으로는 기록되지 않거나 너무 거칠어서, 정작 조사할 때 필요한 정보가 없는 경우가 많다.

## 무엇을 기록하나?

| 수준 | 내용 |
| --- | --- |
| 없음 | 기록하지 않음 |
| 메타데이터 | 누가 무엇을 요청했나 |
| 요청 | 요청 본문 포함 |
| 요청·응답 | 응답까지 포함 |

전부 최고 수준으로 기록하면 로그가 감당되지 않는다. 자원별로 수준을 나눈다.

![자원별 기록 수준 설계](/img/posts/k8s-audit-log-use.svg)

## 수준 배분

| 자원 | 수준 |
| --- | --- |
| 비밀 | 메타데이터만 — 본문에 값이 있다 |
| 역할·바인딩 | 요청 본문 |
| 파드 생성 | 요청 본문 |
| 설정 맵 | 메타데이터 |
| 조회 요청 | 없음 또는 메타데이터 |
| 상태 확인 | 없음 |

비밀 자원은 요청 본문을 기록하면 안 된다. 값이 로그에 남는다.

## 조사에서 보는 것

| 질문 | 확인 |
| --- | --- |
| 누가 이 자원을 바꿨나 | 사용자와 시각 |
| 어디서 접속했나 | 출발지 주소 |
| 어떤 권한으로 | 인가 결정 |
| 무엇이 거부됐나 | 실패 요청 |
| 비정상 패턴이 있나 | 빈도·시각 |

거부된 요청이 특히 유용하다. 권한 탐색 시도가 여기 남는다.

![조사 시 확인 순서](/img/posts/k8s-audit-log-use-2.svg)

## 경보를 걸 항목

\`\`\`
비밀 대량 조회        짧은 시간에 여러 개
역할 바인딩 생성      권한 부여
특권 파드 생성        정책 우회 시도
노드 접근            노드 프록시 사용
익명 접근 성공        인증 없이 통과
반복 거부            권한 탐색
\`\`\`

익명 접근이 성공하는 것은 설정 오류다. 즉시 확인해야 한다.

## 보관

감사 로그는 클러스터 밖으로 즉시 내보낸다. 클러스터가 침해되면 로그도 함께 위험해진다. 별도 계정이나 별도 시스템에 보관하고, 삭제되지 않도록 보호한다.

\`\`\`bash
# 최근 권한 관련 변경 조회
jq -r 'select(.objectRef.resource=="rolebindings" or .objectRef.resource=="clusterrolebindings")
       | select(.verb=="create" or .verb=="update")
       | "\(.requestReceivedTimestamp) \(.user.username) \(.objectRef.name)"' audit.log | tail -20
\`\`\`

감사 정책을 처음 만들 때는 넓게 켜고 며칠 뒤 양을 본다. 예상보다 많으면 조회 요청부터 줄인다. 조회는 건수가 많고 정보 가치는 낮다.

로그가 있어도 찾을 수 없으면 소용없다. 자주 쓰는 조회를 미리 만들어 절차서에 넣어 둔다. 사고 시점에 질의를 새로 작성하면 시간이 걸리고 실수도 생긴다.

## 실제로 이렇게 터진다

감사 로그를 켜지 않은 클러스터에서 침해가 발생한 사례가 있다. 누가 무엇을 했는지 알 방법이 없어 조사가 추정으로 끝났다.

또 하나는 비밀 자원의 요청 본문까지 기록한 경우다. 로그 자체에 비밀 값이 평문으로 남았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 기본으로 남는다 | 설정해야 남는다 |
| 전부 기록하면 안전하다 | 비밀 값이 남는다 |
| 클러스터 안에 두면 된다 | 함께 위험해진다 |
| 조회 요청도 남겨야 한다 | 양이 감당되지 않는다 |

## 경보 항목

| 이벤트 | 판단 |
| --- | --- |
| 익명 접근 성공 | 설정 오류 |
| 비밀 대량 조회 | 즉시 조사 |
| 역할 바인딩 생성 | 확인 |
| 특권 파드 생성 | 확인 |
| 반복 거부 | 권한 탐색 |

## 조사에 쓰는 질의

사고 때 새로 작성하지 않도록 미리 만들어 둔다.

\`\`\`bash
# 최근 권한 관련 변경
jq -r 'select(.objectRef.resource | test("rolebindings|clusterrolebindings"))
       | select(.verb == "create" or .verb == "update")
       | "\(.requestReceivedTimestamp) \(.user.username) \(.objectRef.name)"' audit.log | tail -20

# 익명 접근 성공
jq -r 'select(.user.username == "system:anonymous")
       | select(.responseStatus.code < 400)
       | "\(.requestReceivedTimestamp) \(.verb) \(.requestURI)"' audit.log | head
\`\`\`

두 번째 질의에 결과가 나오면 설정 오류다. 즉시 확인한다.

## 참고

- Kubernetes 공식 문서 — Auditing
- NSA·CISA — Kubernetes Hardening Guidance
- CIS Kubernetes Benchmark — 로깅 항목`,
    diagram: {
      type: 'matrix',
      caption: '기록 수준 설계',
      x: ['민감 값 포함', '미포함'],
      y: ['변경 요청', '조회 요청'],
      cells: ['메타데이터', '요청 본문', '메타데이터', '기록 안 함'],
    },
    diagram2: {
      type: 'steps',
      caption: '조사 순서',
      steps: [
        { label: '시각 범위 확정', note: '사건 전후' },
        { label: '주체 식별', note: '사용자·서비스 계정' },
        { label: '행위 추적', note: '생성·변경 요청' },
        { label: '거부 기록 확인', note: '탐색 시도' },
      ],
    },
  },
]
