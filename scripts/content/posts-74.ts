import type { SeedPost } from './types'

/** 730~739 — 인증 흐름을 설계할 때 */
export const posts74: SeedPost[] = [
  {
    slug: 'oauth-flow-selection',
    title: '외부 로그인 흐름을 고르는 기준',
    body: `외부 로그인 연동에는 여러 흐름이 있고 각각 전제하는 환경이 다르다. 비밀값을 지킬 수 있는지, 사용자가 직접 개입하는지에 따라 답이 갈리며, 편의로 고르면 토큰이 주소나 기록에 노출되는 구성이 된다.

## OAuth 흐름은 어떻게 나뉘나

| 흐름 | 적용 |
| --- | --- |
| 인가 코드 | 웹·앱 기본 |
| 코드 + 증명 키 | 공개 클라이언트 |
| 클라이언트 자격 | 서버 간 |
| 기기 흐름 | 입력 제한 기기 |
| 암시적 | 쓰지 않음 |

암시적 흐름은 토큰이 주소에 담겨 기록에 남는다. 지금은 권장되지 않으며 코드 흐름에 증명 키를 붙이는 방식이 대체한다.

![흐름 선택 기준](/img/posts/oauth-flow-selection.svg)

## 비밀값을 지킬 수 있나

| 유형 | 보관 |
| --- | --- |
| 서버 | 가능 |
| 단일 페이지 앱 | 불가 |
| 모바일 앱 | 불가 |
| 기기 | 불가 |

비밀값을 지킬 수 없는 클라이언트는 증명 키 방식을 써야 한다. 앱에 넣은 비밀값은 비밀이 아니다.

## 돌아올 주소를 고정한다

| 설정 | 내용 |
| --- | --- |
| 정확히 일치 | 원칙 |
| 부분 일치 | 위험 |
| 임의 허용 | 토큰 탈취 |

돌아올 주소를 느슨하게 등록하면 인가 코드가 공격자에게 전달된다. 정확히 일치하는 값만 등록한다.

![클라이언트 유형별 선택](/img/posts/oauth-flow-selection-2.svg)

## 설정을 확인한다

\`\`\`bash
# 등록된 돌아올 주소에 와일드카드가 있는지
curl -s "https://idp.example.com/api/clients/$CLIENT_ID" \
  -H "Authorization: Bearer $ADMIN_TOKEN" 2>/dev/null \
  | jq -r '.redirect_uris[]'

# 증명 키 요구 설정
curl -s "https://idp.example.com/api/clients/$CLIENT_ID" \
  -H "Authorization: Bearer $ADMIN_TOKEN" 2>/dev/null | jq '.require_pkce'
\`\`\`

와일드카드가 있거나 증명 키를 요구하지 않으면 그 설정부터 고친다. 코드 변경 없이 바꿀 수 있는 부분이다.

## 상태 값을 반드시 쓴다

요청과 응답을 묶는 임의 값을 넣지 않으면 다른 사람의 인가 코드를 우리 사용자에게 붙이는 공격이 가능하다. 요청마다 새로 만들고 돌아왔을 때 대조한다. 많은 라이브러리가 자동으로 처리하지만 직접 구현했다면 이 부분이 빠져 있는 경우가 흔하다. 검증된 라이브러리를 쓰는 편이 낫다. 이 영역은 명세가 복잡하고 빠뜨리기 쉬운 검증이 많아 직접 구현할 이유가 거의 없다.

## 참고

- OAuth 2.0 Security Best Current Practice
- OpenID Connect 명세
- OWASP — 인증 지침`,
    diagram: {
      type: 'steps',
      caption: '흐름 선택 기준',
      steps: [
        { label: '비밀값 보관 가능?', note: '서버 여부' },
        { label: '사용자 개입 필요?', note: '위임 여부' },
        { label: '입력 가능한 기기?', note: '화면·키보드' },
        { label: '흐름 결정', note: '전제에 맞게' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '클라이언트 유형별 선택',
      x: ['비밀값 보관 가능', '불가'],
      y: ['사용자 로그인', '서버 간'],
      cells: ['인가 코드', '코드 + 증명 키', '클라이언트 자격', '해당 없음'],
    },
  },
  {
    slug: 'sso-account-linking',
    title: '여러 로그인 수단을 한 계정에 묶기',
    body: `같은 사람이 여러 방식으로 로그인할 수 있게 하면 편리하지만, 계정을 어떻게 이어 붙이느냐에 따라 탈취 경로가 생긴다.

## 무엇으로 묶나

| 기준 | 위험 |
| --- | --- |
| 이메일 주소 | 검증 여부에 달림 |
| 외부 식별자 | 안전 |
| 전화번호 | 재사용 가능 |
| 이름 | 안 됨 |

이메일로 묶을 때 그 이메일이 검증됐는지 확인하지 않으면, 남의 이메일을 등록한 계정으로 기존 계정에 들어간다.

![계정 연결의 위험](/img/posts/sso-account-linking.svg)

## 안전하게 묶는 순서

| 단계 | 내용 |
| --- | --- |
| 기존 계정 확인 | 같은 이메일 |
| 본인 확인 | 기존 방식으로 |
| 연결 승인 | 명시적 |
| 기록 | 연결 이력 |

기존 계정에 로그인한 상태에서 새 수단을 추가하게 하면 가장 안전하다. 로그인하지 않은 상태에서 자동으로 묶으면 위험하다.

## 해제도 생각한다

| 상황 | 처리 |
| --- | --- |
| 마지막 수단 | 해제 금지 |
| 비밀번호 없음 | 설정 요구 |
| 외부 계정 삭제 | 대체 수단 |
| 제공자 중단 | 이전 경로 |

로그인 수단이 하나뿐인데 그것을 해제하면 계정에 들어갈 방법이 없어진다. 마지막 수단은 해제할 수 없게 막는다.

![연결과 해제의 규칙](/img/posts/sso-account-linking-2.svg)

## 현재 상태를 본다

\`\`\`bash
# 검증되지 않은 이메일로 연결된 계정
psql -At -c "select u.id, i.provider from identities i
  join users u on u.id = i.user_id
  where i.email_verified = false" 2>/dev/null | head -10

# 로그인 수단이 하나뿐인 계정 비율
psql -At -c "select count(*) filter (where n=1)::float/count(*)
  from (select user_id, count(*) n from identities group by 1) t" 2>/dev/null
\`\`\`

검증되지 않은 이메일로 묶인 계정이 있으면 그 연결을 다시 확인해야 한다. 이미 생긴 것은 개별 확인이 필요하다.

## 제공자를 옮길 수 있게

외부 제공자가 서비스를 중단하거나 정책을 바꾸면 그 방식으로 로그인하던 사용자가 막힌다. 이메일로 복구할 수 있는 경로를 항상 열어 두고, 제공자별 식별자를 저장해 두면 나중에 이전이 가능하다. 한 제공자에만 의존하는 구성은 그 제공자의 결정에 우리 서비스가 묶인다.

## 참고

- OpenID Connect 명세
- OWASP — 인증 지침
- OAuth 2.0 Security Best Current Practice`,
    diagram: {
      type: 'flow',
      caption: '계정 연결의 위험',
      steps: [
        { label: '외부 계정 생성', note: '남의 이메일로' },
        { label: '이메일 미검증', note: '제공자에 따라' },
        { label: '자동 연결', note: '같은 주소' },
        { label: '기존 계정 접근', note: '탈취', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '연결과 해제의 규칙',
      steps: [
        { label: '로그인 상태에서', note: '연결' },
        { label: '검증된 이메일만', note: '자동 매칭' },
        { label: '연결 이력', note: '기록' },
        { label: '마지막 수단', note: '해제 금지' },
      ],
    },
  },
  {
    slug: 'passwordless-login-design',
    title: '비밀번호 없는 로그인 설계하기',
    body: `비밀번호를 없애면 유출과 재사용 문제가 사라진다. 대신 다른 수단의 특성을 이해하고 그에 맞는 보호를 붙여야 한다.

## 패스키 말고 무엇이 있나

| 방식 | 특성 |
| --- | --- |
| 하드웨어 키 | 가장 강함 |
| 기기 생체 인증 | 강하고 편함 |
| 메일 링크 | 메일 보안에 의존 |
| 문자 코드 | 번호 탈취 위험 |
| 인증 앱 | 기기 의존 |

메일 링크 방식은 메일 계정이 뚫리면 함께 뚫린다. 편리하지만 보안 수준은 메일 계정과 같아진다.

![방식별 보안 수준](/img/posts/passwordless-login-design.svg)

## 복구 경로를 설계한다

| 상황 | 대응 |
| --- | --- |
| 기기 분실 | 대체 수단 |
| 키 분실 | 백업 키 |
| 메일 접근 불가 | 본인 확인 |
| 번호 변경 | 재등록 |

복구 경로가 가장 약한 고리가 된다. 강한 인증을 붙여도 복구가 허술하면 그쪽으로 뚫린다.

## 두 개 이상 등록하게 한다

| 구성 | 결과 |
| --- | --- |
| 하나만 | 잃으면 막힘 |
| 두 개 | 하나 잃어도 |
| 백업 코드 | 최후 수단 |

등록 시점에 두 번째 수단을 함께 만들게 하면 나중에 복구 문의가 크게 준다. 그 시점이 가장 협조적이다.

![복구 경로 구성](/img/posts/passwordless-login-design-2.svg)

## 등록 상태를 본다

\`\`\`bash
# 인증 수단이 하나뿐인 계정
psql -At -c "select count(*) from (
  select user_id, count(*) n from auth_methods group by 1) t
  where n = 1" 2>/dev/null

# 방식별 사용 비율
psql -At -c "select method, count(*) from auth_methods group by 1 order by 2 desc" 2>/dev/null
\`\`\`

수단이 하나뿐인 계정이 많으면 복구 문의가 계속 들어온다. 추가 등록을 안내하는 것이 지원 부담을 줄인다.

## 전환은 단계적으로

비밀번호를 한 번에 없애면 준비되지 않은 사용자가 막힌다. 새 수단을 먼저 도입해 함께 쓰게 하고, 등록 비율이 충분히 오르면 비밀번호를 선택 사항으로 내린다. 마지막에 없애는 순서가 무리가 없고, 각 단계에서 무엇이 문제인지 확인할 기회도 생긴다. 지원 문서를 함께 준비한다. 새 방식은 익숙하지 않아 문의가 늘어나고, 화면마다 짧은 안내를 붙여 두면 그 부담이 줄어든다.

## 참고

- FIDO2·WebAuthn 명세
- NIST SP 800-63B — 인증 지침
- OWASP — 인증 지침`,
    diagram: {
      type: 'bars',
      caption: '방식별 보안 수준',
      unit: '점',
      items: [
        { label: '하드웨어 키', value: 95, note: '가장 강함' },
        { label: '기기 생체 인증', value: 85, note: '편의도 좋음' },
        { label: '인증 앱', value: 70, note: '기기 의존' },
        { label: '메일 링크', value: 45, note: '메일에 의존' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '복구 경로 구성',
      layers: [
        { label: '주 수단', note: '일상 사용' },
        { label: '보조 수단', note: '기기 분실 시' },
        { label: '백업 코드', note: '최후' },
        { label: '본인 확인', note: '사람 개입' },
      ],
    },
  },
  {
    slug: 'session-revocation-design',
    title: '세션을 확실히 끊어 내는 구조',
    body: `로그아웃이나 비밀번호 변경 후에도 기존 세션이 살아 있으면 탈취된 접근이 계속된다. 끊는 구조가 없으면 만료를 기다리는 수밖에 없다.

## 언제 끊어야 하나

| 상황 | 범위 |
| --- | --- |
| 로그아웃 | 해당 세션 |
| 비밀번호 변경 | 전체 |
| 권한 변경 | 전체 |
| 계정 정지 | 전체 |
| 이상 탐지 | 전체 |

비밀번호를 바꾸는 이유가 대개 탈취 우려이므로, 그때 다른 세션을 끊지 않으면 변경의 효과가 없다.

![세션이 남는 경로](/img/posts/session-revocation-design.svg)

## 어떻게 끊나

| 방식 | 특성 |
| --- | --- |
| 서버 저장 세션 | 즉시 가능 |
| 무상태 토큰 | 끊기 어려움 |
| 폐기 목록 | 조회 필요 |
| 발급 시점 기준 | 간단 |

계정마다 기준 시각을 두고 그 이전에 발급된 토큰을 거부하는 방식이 간단하면서 효과적이다. 목록을 관리할 필요가 없다.

## 무상태 토큰의 대가

| 장점 | 단점 |
| --- | --- |
| 조회 불필요 | 끊기 어려움 |
| 확장 쉬움 | 즉시 반영 안 됨 |
| 분산에 적합 | 수명이 곧 위험 |

무상태 토큰을 쓰면 수명이 곧 노출 기간이 된다. 짧게 두고 갱신 토큰으로 이어 가는 구성이 균형점이다.

![끊는 방식의 비교](/img/posts/session-revocation-design-2.svg)

## 실제로 끊기는지 시험한다

\`\`\`bash
# 비밀번호 변경 후 기존 토큰이 여전히 동작하는지
OLD=$TOKEN
curl -s -X POST https://example.com/api/password -H "Authorization: Bearer $OLD" \
  -d '{"new":"..."}' >/dev/null
curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/api/me \
  -H "Authorization: Bearer $OLD"
\`\`\`

200이 나오면 끊기지 않은 것이다. 변경 직후에 확인해야 의미가 있다.

## 사용자에게 보여 준다

현재 로그인된 기기 목록을 보여 주고 개별로 끊을 수 있게 하면, 사용자가 스스로 이상을 발견하고 조치한다. 낯선 기기가 목록에 있으면 바로 알 수 있고, 그것이 탈취를 알리는 가장 빠른 경로가 되기도 한다. 목록에 마지막 접속 시각과 대략적인 위치를 함께 보여 준다. 전부 로그아웃 버튼도 함께 둔다. 어느 것이 낯선 것인지 판단하기 어려울 때 한 번에 끊고 다시 로그인하는 선택지가 있으면 사용자가 스스로 조치할 수 있다.

## 참고

- OWASP — 세션 관리 지침
- NIST SP 800-63B — 세션 관리
- OAuth 2.0 Security Best Current Practice`,
    diagram: {
      type: 'flow',
      caption: '세션이 남는 경로',
      steps: [
        { label: '탈취 인지', note: '사용자' },
        { label: '비밀번호 변경', note: '조치' },
        { label: '기존 토큰 유효', note: '끊지 않음' },
        { label: '접근 계속', note: '조치 무력', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '끊는 방식의 비교',
      x: ['서버 저장', '무상태'],
      y: ['즉시 반영', '확장성'],
      cells: ['가능', '기준 시각 필요', '조회 부담', '유리'],
    },
  },
  {
    slug: 'step-up-authentication',
    title: '민감한 동작에 인증을 더하기',
    body: `로그인 한 번으로 모든 동작을 허용하면 세션이 탈취됐을 때 피해가 전부로 이어진다. 중요한 지점에서 다시 확인하면 그 범위가 줄어든다.

## 어디에 두나

| 동작 | 확인 |
| --- | --- |
| 비밀번호 변경 | 현재 비밀번호 |
| 이메일 변경 | 재인증 |
| 인증 수단 변경 | 재인증 |
| 고액 거래 | 추가 인증 |
| 계정 삭제 | 재인증 |

인증 수단을 바꾸는 동작이 가장 중요하다. 여기가 뚫리면 공격자가 계정을 완전히 가져간다.

![추가 확인이 필요한 지점](/img/posts/step-up-authentication.svg)

## 무엇으로 확인하나

| 수단 | 강도 |
| --- | --- |
| 하드웨어 키 | 가장 강함 |
| 생체 인증 | 강함 |
| 인증 앱 코드 | 보통 |
| 현재 비밀번호 | 약함 |
| 메일 확인 | 약함 |

현재 비밀번호를 묻는 방식은 그 비밀번호가 이미 유출됐다면 소용이 없다. 다른 요소를 쓰는 편이 낫다.

## 유효 시간을 둔다

| 설정 | 효과 |
| --- | --- |
| 매번 | 불편 |
| 수 분 유지 | 균형 |
| 세션 전체 | 효과 없음 |

한 번 확인한 뒤 몇 분간은 다시 묻지 않는 구성이 일반적이다. 연속 작업의 불편을 줄이면서 효과는 유지된다.

![확인 수단의 강도](/img/posts/step-up-authentication-2.svg)

## 우회 경로를 찾는다

\`\`\`bash
# 재인증 없이 민감한 동작이 되는지
for ep in /api/email /api/password /api/mfa /api/account; do
  printf '%s ' "$ep"
  curl -s -o /dev/null -w '%{http_code}\\n' -X POST "https://example.com$ep" \
    -H "Authorization: Bearer $TOKEN" -d '{}'
done
\`\`\`

401이나 403이 아닌 응답이 나오는 경로가 있으면 확인이 빠진 것이다. 화면에서는 물어보는데 API에서는 검증하지 않는 경우가 흔하다.

## 서버에서 판단한다

재인증 여부를 화면 흐름으로만 통제하면 API를 직접 부르는 경로에서 빠진다. 서버가 그 세션이 최근에 재인증했는지 확인하고 아니면 거절해야 한다. 화면은 사용자 경험을 위한 것이고 통제는 서버에 있어야 한다는 원칙이 여기서도 그대로 적용된다.

## 참고

- NIST SP 800-63B — 인증 보증 수준
- OWASP — 인증 지침
- OAuth 2.0 Security Best Current Practice`,
    diagram: {
      type: 'layers',
      caption: '추가 확인이 필요한 지점',
      layers: [
        { label: '인증 수단 변경', note: '가장 중요' },
        { label: '이메일 변경', note: '복구 경로' },
        { label: '고액 거래', note: '금전' },
        { label: '계정 삭제', note: '되돌릴 수 없음' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '확인 수단의 강도',
      unit: '점',
      items: [
        { label: '하드웨어 키', value: 95, note: '가장 강함' },
        { label: '생체 인증', value: 85, note: '강함' },
        { label: '인증 앱 코드', value: 65, note: '보통' },
        { label: '현재 비밀번호', value: 30, note: '유출 시 무용' },
      ],
    },
  },
  {
    slug: 'account-recovery-flow',
    title: '계정 복구가 뒷문이 되지 않게',
    body: `복구 절차는 정당한 사용자를 돕는 기능이지만, 가장 약한 인증 경로가 되기 쉽다. 강한 인증을 붙여도 복구가 허술하면 그쪽이 표적이 된다.

## 어디가 약한가

| 방식 | 문제 |
| --- | --- |
| 메일 링크 | 메일 계정 의존 |
| 문자 코드 | 번호 탈취 |
| 질문 답변 | 추측·공개 정보 |
| 상담원 확인 | 사회공학 |
| 서류 제출 | 위조 |

질문 답변 방식은 답이 공개 정보인 경우가 많다. 출신 학교나 반려동물 이름은 사회관계망에 있다.

![복구 경로의 약한 고리](/img/posts/account-recovery-flow.svg)

## 어떻게 강화하나

| 방법 | 내용 |
| --- | --- |
| 여러 요소 요구 | 하나로 부족 |
| 지연 | 취소 기회 |
| 알림 | 기존 수단으로 |
| 이력 확인 | 평소 패턴 |
| 한도 제한 | 복구 후 |

복구 요청이 들어오면 기존 연락 수단으로 알리고 일정 시간 지연한다. 정당한 사용자는 기다릴 수 있고, 탈취 시도는 그 사이에 막힌다.

## 복구 후를 제한한다

| 항목 | 제한 |
| --- | --- |
| 고액 거래 | 일정 기간 |
| 연락처 변경 | 추가 확인 |
| 데이터 내려받기 | 지연 |
| 인증 수단 변경 | 제한 |

복구 직후에 민감한 동작을 제한하면 잘못된 복구의 피해가 줄어든다. 정상 사용자에게는 며칠의 불편이다.

![복구 절차의 단계](/img/posts/account-recovery-flow-2.svg)

## 절차를 시험한다

\`\`\`bash
# 복구 요청 시 기존 수단으로 알림이 가는지
curl -s -X POST https://example.com/api/recover \
  -d '{"email":"user@example.com"}' -o /dev/null -w '%{http_code}\\n'

# 복구 토큰의 수명
psql -At -c "select expires_at - created_at from recovery_tokens
  order by created_at desc limit 1" 2>/dev/null
\`\`\`

복구 토큰의 수명이 길면 메일이 나중에 노출돼도 쓸 수 있다. 짧게 두고 한 번 쓰면 무효화한다.

## 상담 경로를 정한다

자동 절차로 해결되지 않는 경우에 상담원이 개입하게 되는데, 그 경로가 가장 약하다. 무엇을 확인해야 하는지 명확히 정하고, 예외를 만들 수 있는 권한을 제한하며, 모든 처리를 기록한다. 급하다고 재촉하는 요청이 사회공학의 전형이므로, 재촉에 대응하지 않는 절차 자체가 방어가 된다.

## 참고

- NIST SP 800-63B — 계정 복구
- OWASP — Forgot Password Cheat Sheet
- 한국인터넷진흥원 — 인증 안내`,
    diagram: {
      type: 'bars',
      caption: '복구 경로의 약한 고리',
      unit: '점',
      items: [
        { label: '질문 답변', value: 90, note: '공개 정보' },
        { label: '상담원 확인', value: 80, note: '사회공학' },
        { label: '문자 코드', value: 60, note: '번호 탈취' },
        { label: '메일 링크', value: 45, note: '메일 의존' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '복구 절차의 단계',
      steps: [
        { label: '요청 접수', note: '기록' },
        { label: '기존 수단 알림', note: '즉시' },
        { label: '지연', note: '취소 기회' },
        { label: '복구 후 제한', note: '일정 기간' },
      ],
    },
  },
  {
    slug: 'machine-identity-auth',
    title: '서비스 간 인증을 어떻게 할까',
    body: `사람이 아닌 시스템끼리의 인증은 자격 증명이 어딘가에 보관되어야 한다. 그 보관 방식이 이 영역의 핵심 문제다.

## 키 관리를 어떻게 하나

| 방식 | 특성 |
| --- | --- |
| 정적 키 | 간단·회전 부담 |
| 짧은 수명 토큰 | 회전 자동 |
| 상호 인증서 | 강함·관리 부담 |
| 작업 부하 신원 | 비밀값 없음 |

실행 환경이 신원을 증명해 주는 방식이면 비밀값 자체가 없어진다. 클라우드나 오케스트레이션 환경에서 지원한다.

![인증 방식의 진화](/img/posts/machine-identity-auth.svg)

## 정적 키의 문제

| 항목 | 내용 |
| --- | --- |
| 회전 부담 | 수동 |
| 유출 시 | 오래 유효 |
| 공유 | 추적 불가 |
| 보관 | 여러 곳 |

정적 키는 한 번 만들면 몇 년씩 쓰인다. 회전이 번거로워 미루고, 그러는 사이 여러 곳에 복사된다.

## 수명을 짧게

| 수명 | 노출 창 |
| --- | --- |
| 무기한 | 영구 |
| 1년 | 매우 김 |
| 1일 | 제한적 |
| 1시간 | 짧음 |

수명이 짧으면 유출돼도 곧 무효가 된다. 자동 갱신 구조가 있어야 짧게 둘 수 있다.

![수명과 노출 창](/img/posts/machine-identity-auth-2.svg)

## 지금 쓰는 것을 본다

\`\`\`bash
# 오래된 접근 키
aws iam list-users --query 'Users[].UserName' --output text 2>/dev/null \
  | tr '\t' '\n' | while read -r u; do
  aws iam list-access-keys --user-name "$u" \
    --query 'AccessKeyMetadata[].[UserName,CreateDate]' --output text 2>/dev/null
done | awk '$2 < "'"$(date -v-365d +%Y-%m-%d)"'"' | head
\`\`\`

1년 넘은 키가 나오면 회전 대상이다. 회전 절차가 없으면 그것부터 만든다.

## 신원을 나눈다

여러 서비스가 같은 자격 증명을 쓰면 문제가 생겼을 때 어느 쪽인지 알 수 없고, 회전할 때 전부 멈춘다. 서비스마다 다른 신원을 주면 추적도 되고 회전도 개별로 할 수 있다. 초기에 조금 번거롭지만 규모가 커질수록 차이가 벌어진다. 어떤 신원이 어디에 쓰이는지 목록으로 관리한다. 회전하거나 폐기할 때 무엇이 영향받는지 알 수 없으면 손대지 못하고, 그렇게 쓰이지 않는 자격 증명이 계속 살아 있게 된다.

## 참고

- NIST SP 800-63 — 신원 확인
- SPIFFE — 작업 부하 신원 명세
- 클라우드 사업자 역할 기반 인증 문서`,
    diagram: {
      type: 'steps',
      caption: '인증 방식의 진화',
      steps: [
        { label: '정적 키', note: '회전 부담' },
        { label: '짧은 토큰', note: '자동 갱신' },
        { label: '상호 인증서', note: '양방향 확인' },
        { label: '작업 부하 신원', note: '비밀값 없음' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '수명과 노출 창',
      unit: '시간',
      items: [
        { label: '1시간', value: 1, note: '짧음' },
        { label: '1일', value: 24, note: '제한적' },
        { label: '1년', value: 8760, note: '매우 김' },
        { label: '무기한', value: 20000, note: '영구' },
      ],
    },
  },
  {
    slug: 'api-key-vs-token',
    title: '키와 토큰을 어떻게 나눠 쓸까',
    body: `외부에 제공하는 API 보안에서 키와 토큰은 서로 다른 문제를 푼다. 키는 누구인지 알리는 데 쓰고 토큰은 무엇을 할 수 있는지 정하는 데 쓰는데, 둘을 섞어 쓰면 회전도 권한 관리도 어려워진다.

## 무엇이 다른가

| 항목 | 키 | 토큰 |
| --- | --- | --- |
| 수명 | 길다 | 짧다 |
| 발급 | 사람이 | 자동 |
| 권한 | 고정 | 범위 지정 |
| 회전 | 수동 | 자동 |

키는 식별에, 토큰은 권한 부여에 쓰는 것이 기본 구분이다. 키 하나에 모든 권한을 붙이면 범위를 좁힐 방법이 없다.

![키와 토큰의 역할](/img/posts/api-key-vs-token.svg)

## 어떻게 조합하나

| 구성 | 내용 |
| --- | --- |
| 키로 토큰 발급 | 일반적 |
| 키 직접 사용 | 단순한 경우 |
| 키 없이 토큰 | 사용자 위임 |

키로 짧은 수명의 토큰을 받아 쓰는 구성이 표준적이다. 키가 유출돼도 회전하면 되고, 토큰은 곧 만료된다.

## API 보안에서 범위를 나눈다

| 항목 | 내용 |
| --- | --- |
| 읽기 전용 | 조회만 |
| 특정 자원 | 일부만 |
| 기간 제한 | 임시 |
| 출발지 제한 | 대역 |

외부 연동에 전체 권한 키를 주는 대신 필요한 범위만 담은 키를 발급하면, 그쪽이 뚫려도 피해가 제한된다.

![권한 범위 구성](/img/posts/api-key-vs-token-2.svg)

## 현재 발급 상태를 본다

\`\`\`bash
# 발급된 키의 권한 범위 분포
psql -At -c "select scopes, count(*) from api_keys
  where revoked_at is null group by 1 order by 2 desc" 2>/dev/null | head

# 오래 쓰인 키
psql -At -c "select id, created_at from api_keys
  where revoked_at is null and created_at < now() - interval '1 year'" 2>/dev/null | head
\`\`\`

전체 권한 키가 대부분이면 범위 기능이 있어도 쓰이지 않는 것이다. 발급 화면의 기본값을 좁게 바꾸면 분포가 달라진다.

## 회전을 쉽게 만든다

회전이 어려우면 아무도 하지 않는다. 키를 두 개까지 동시에 유효하게 두고 새 키로 옮긴 뒤 옛 키를 지우는 방식이면 중단 없이 바꿀 수 있다. 이 기능이 없으면 회전할 때마다 연동이 끊기고, 그래서 회전하지 않게 된다. 보안 기능은 쓰기 쉬워야 쓰인다. 마지막 사용 시각도 함께 보여 준다. 오래 쓰이지 않은 키는 지워도 되는지 판단할 수 있고, 유출 여부를 확인할 때도 단서가 된다.

## 참고

- OAuth 2.0 명세
- OWASP — API 보안 Top 10
- NIST SP 800-63 — 인증 지침`,
    diagram: {
      type: 'matrix',
      caption: '키와 토큰의 역할',
      x: ['키', '토큰'],
      y: ['식별', '권한 부여'],
      cells: ['주 용도', '부가', '부가', '주 용도'],
    },
    diagram2: {
      type: 'layers',
      caption: '권한 범위 구성',
      layers: [
        { label: '전체 권한', note: '피해야 함' },
        { label: '자원 범위', note: '일부만' },
        { label: '읽기 전용', note: '조회' },
        { label: '출발지 제한', note: '추가 조건' },
      ],
    },
  },
  {
    slug: 'impersonation-feature-risk',
    title: '관리자가 사용자로 들어가는 기능',
    body: `고객 지원을 위해 관리자가 사용자 화면으로 들어가는 기능은 유용하지만, 통제 없이 두면 가장 강력한 내부 접근 경로가 된다.

## 내부자 접근이 왜 위험한가

| 항목 | 내용 |
| --- | --- |
| 전체 데이터 접근 | 그 사용자 전부 |
| 동작 수행 | 대신 실행 |
| 기록 혼동 | 누가 한 것인지 |
| 남용 탐지 어려움 | 정상 접근처럼 |

기록에 그 사용자가 한 것으로 남으면 나중에 구분할 수 없다. 대리 접근이라는 표시가 반드시 필요하다.

![대리 접근의 구조](/img/posts/impersonation-feature-risk.svg)

## 어떻게 통제하나

| 방법 | 내용 |
| --- | --- |
| 사유 입력 | 필수 |
| 승인 | 일정 조건 |
| 기간 제한 | 짧게 |
| 읽기 전용 | 기본 |
| 사용자 알림 | 접근 사실 |

읽기 전용을 기본으로 하고 동작이 필요하면 별도 권한을 요구하면 위험이 크게 준다. 대부분의 지원 업무는 보기만 하면 된다.

## 무엇을 기록하나

| 항목 | 내용 |
| --- | --- |
| 관리자 신원 | 실제 누구 |
| 대상 사용자 | 누구에게 |
| 사유 | 왜 |
| 시각·기간 | 언제부터 |
| 수행 동작 | 무엇을 |

모든 기록에 실제 조작자를 함께 남겨야 한다. 대상 사용자만 남으면 대리 접근이 흔적 없이 묻힌다.

![통제 요소](/img/posts/impersonation-feature-risk-2.svg)

## 사용 현황을 본다

\`\`\`bash
# 대리 접근 기록 — 사용 빈도가 높은 관리자
awk -F, 'NR>1 {n[$2]++} END {for (a in n) print n[a], a}' \
  /var/log/impersonation.csv 2>/dev/null | sort -rn | head

# 사유가 비어 있는 건
awk -F, 'NR>1 && $5=="" {print}' /var/log/impersonation.csv 2>/dev/null | wc -l
\`\`\`

사유가 비어 있는 기록이 있으면 필수 입력이 강제되지 않는 것이다. 사용 빈도가 유독 높은 계정은 업무 특성인지 확인한다.

## 사용자에게 알리는 문제

접근 사실을 사용자에게 알리면 투명하지만, 지원 과정에서 매번 알림이 가면 혼란을 준다. 사용자가 요청한 지원 건은 알리지 않고 그 외의 접근은 알리는 식으로 구분할 수 있다. 어느 쪽이든 이용 약관에 어떤 경우에 접근하는지 적어 두어야 한다. 접근 기록을 사용자가 직접 볼 수 있게 하는 방법도 있다. 투명성이 높아지고, 내부에서도 기록이 공개된다는 사실이 남용을 줄이는 효과를 낸다.

## 참고

- OWASP — 접근 통제 지침
- 개인정보보호법 — 접근 권한 관리
- NIST SP 800-53 — 감사와 책임 추적`,
    diagram: {
      type: 'flow',
      caption: '대리 접근의 구조',
      steps: [
        { label: '관리자 인증', note: '본인 계정' },
        { label: '대상 지정', note: '사유 입력' },
        { label: '대리 세션', note: '표시 유지' },
        { label: '기록', note: '실제 조작자' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '통제 요소',
      layers: [
        { label: '사유 필수', note: '기록' },
        { label: '읽기 전용', note: '기본' },
        { label: '기간 제한', note: '짧게' },
        { label: '알림', note: '투명성' },
      ],
    },
  },
  {
    slug: 'brute-force-protection',
    title: '반복 시도를 막는 방식 고르기',
    body: `로그인 시도를 무제한 허용하면 결국 뚫린다. 그런데 막는 방식에 따라 정상 사용자가 피해를 보거나 서비스 거부의 도구가 되기도 한다.

## 비밀번호 보호에 어떤 방식이 있나

| 방식 | 문제 |
| --- | --- |
| 계정 잠금 | 서비스 거부 도구 |
| 주소 기준 제한 | 공유 주소 영향 |
| 점진적 지연 | 균형 |
| 추가 확인 | 사용자 부담 |
| 기기 신뢰 | 첫 접속 부담 |

계정 잠금은 공격자가 남의 계정을 일부러 잠그는 데 쓸 수 있다. 널리 쓰이지만 부작용이 분명하다.

![차단 방식의 부작용](/img/posts/brute-force-protection.svg)

## 점진적으로 늦춘다

| 시도 | 지연 |
| --- | --- |
| 1~3회 | 없음 |
| 4~6회 | 수 초 |
| 7~10회 | 수십 초 |
| 그 이상 | 분 단위 |

지연을 점차 늘리면 자동화 공격은 사실상 불가능해지고 정상 사용자는 거의 영향을 받지 않는다. 잠그지 않으므로 서비스 거부에도 쓰이지 않는다.

## 무엇을 기준으로 세나

| 기준 | 특성 |
| --- | --- |
| 계정별 | 표적 공격 |
| 주소별 | 분산 공격에 약함 |
| 조합 | 실무적 |
| 전역 | 대규모 시도 |

한 주소에서 여러 계정을 시도하는 공격과 여러 주소에서 한 계정을 시도하는 공격이 다르다. 두 기준을 함께 본다.

![기준별 탐지 대상](/img/posts/brute-force-protection-2.svg)

## 동작을 시험한다

\`\`\`bash
# 반복 시도가 실제로 늦춰지는지
for i in $(seq 1 12); do
  s=$(date +%s%N)
  curl -s -o /dev/null -X POST https://example.com/api/login \
    -d '{"email":"t@example.com","password":"wrong"}'
  e=$(date +%s%N)
  echo "$i: $(( (e-s)/1000000 ))ms"
done
\`\`\`

시도 횟수가 늘어도 응답 시간이 같으면 제한이 없는 것이다. 응답 코드만 보고 판단하면 놓칠 수 있다.

## 성공과 실패의 응답을 맞춘다

없는 계정과 있는 계정의 응답이 다르면 계정 존재 여부가 드러난다. 메시지도 응답 시간도 같게 맞춰야 한다. 비밀번호 확인을 건너뛰면 없는 계정 쪽이 빨라지므로, 없는 계정에도 같은 계산을 수행하는 처리가 필요하다. 가입 화면에서도 같은 문제가 생긴다. 이미 있는 주소라고 알려 주면 계정 목록을 만들 수 있으므로, 메일로 안내하는 방식으로 바꾸는 편이 낫다.

## 참고

- OWASP — 인증 지침
- NIST SP 800-63B — 인증 시도 제한
- CWE-307 — 과도한 인증 시도 제한 실패`,
    diagram: {
      type: 'matrix',
      caption: '차단 방식의 부작용',
      x: ['공격 차단', '정상 영향'],
      y: ['계정 잠금', '점진적 지연'],
      cells: ['효과적', '서비스 거부', '효과적', '거의 없음'],
    },
    diagram2: {
      type: 'matrix',
      caption: '기준별 탐지 대상',
      x: ['한 계정', '여러 계정'],
      y: ['한 주소', '여러 주소'],
      cells: ['계정 기준', '주소 기준', '전역 기준', '조합 필요'],
    },
  },
]
