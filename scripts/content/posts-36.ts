import type { SeedPost } from './types'

/** 351~360 — 웹 취약점 심화 */
export const posts36: SeedPost[] = [
  {
    slug: 'response-splitting',
    title: 'HTTP 응답 분할이 아직 남아 있는 자리',
    body: `사용자 입력이 응답 헤더에 그대로 들어가면 줄바꿈 문자를 넣어 헤더를 끊고 새 응답을 만들 수 있다. 현대 프레임워크는 대부분 막지만, 직접 헤더를 다루는 코드와 프록시 계층에서는 여전히 나온다.

## 보안 헤더에 사용자 입력이 어디로 들어가나?

| 위치 | 예 |
| --- | --- |
| 이동 주소 | 요청 값으로 지정 |
| 쿠키 값 | 사용자 이름·설정 |
| 사용자 정의 헤더 | 추적 식별자 |
| 파일 이름 헤더 | 업로드 원본명 |
| 언어·지역 헤더 | 선택 값 |

파일 이름 헤더가 자주 문제가 된다. 원본 파일명을 그대로 넣으면 특수 문자가 함께 들어간다.

![입력이 헤더에 들어가는 경로](/img/posts/response-splitting.svg)

## 무엇이 가능해지나

| 결과 | 설명 |
| --- | --- |
| 캐시 오염 | 조작된 응답이 저장 |
| 스크립트 실행 | 응답 본문 주입 |
| 쿠키 설정 | 세션 고정 |
| 리다이렉트 조작 | 피싱 |

캐시 계층이 있으면 피해가 한 사람에 그치지 않는다. 조작된 응답이 다른 사용자에게도 나간다.

## 방어

1. 헤더 값에서 줄바꿈 문자를 제거하거나 거부한다
2. 프레임워크의 헤더 설정 함수를 쓴다 — 직접 문자열을 만들지 않는다
3. 파일 이름은 인코딩하거나 서버가 정한 값을 쓴다
4. 이동 주소는 목록과 대조한다
5. 프록시 계층의 헤더 처리도 확인한다

2번이 핵심이다. 대부분의 프레임워크는 헤더 설정 시 제어 문자를 걸러낸다. 문자열을 직접 조합하는 코드가 위험하다.

![차단 지점](/img/posts/response-splitting-2.svg)

## 파일 이름 처리

\`\`\`javascript
// 원본 이름을 그대로 넣지 않는다
const safe = encodeURIComponent(original).replace(/['()]/g, escape)
res.setHeader('Content-Disposition', \`attachment; filename*=UTF-8''\${safe}\`)
\`\`\`

인코딩된 형식을 쓰면 특수 문자와 한글이 모두 안전하게 처리된다.

## 점검

\`\`\`bash
# 응답 헤더에 요청 값이 반영되는지 확인
curl -sI "https://example.com/redirect?to=%0d%0aX-Injected:%20test" | grep -i x-injected
\`\`\`

주입한 헤더가 응답에 나타나면 취약하다. 프록시가 걸러 주는 경우도 있으므로 각 계층에서 확인한다.

## 프록시 계층

앞단과 뒷단의 헤더 처리 방식이 다르면 한쪽이 통과시킨 것을 다른 쪽이 다르게 해석한다. 요청 스머글링과 같은 원리다. 구성 요소 조합을 문서로 관리하고 변경 시 확인한다.

## 실제로 이렇게 터진다

파일 다운로드 기능에서 원본 파일명을 헤더에 그대로 넣은 사례가 있다. 사용자가 업로드할 때 파일명에 줄바꿈을 넣어 두었고, 다운로드 시 그 이름이 응답 헤더를 끊었다. 앞단 캐시가 조작된 응답을 저장했고, 같은 주소로 접근한 다른 사용자에게 그대로 나갔다.

또 하나는 언어 설정을 쿠키로 저장하면서 값을 검증하지 않은 경우다. 링크 한 번으로 임의 쿠키를 심을 수 있었다.

## 어디를 먼저 보나

| 우선순위 | 대상 |
| --- | --- |
| 1 | 파일 다운로드 헤더 |
| 2 | 리다이렉트 주소 |
| 3 | 쿠키 설정 값 |
| 4 | 사용자 정의 추적 헤더 |
| 5 | 프록시 전달 헤더 |

파일 다운로드가 가장 흔하다. 원본 파일명을 보존하려는 요구가 있어 값이 그대로 들어가기 쉽다.

## 조치 후 확인할 것

값을 걸러 내는 것으로 끝내지 않고, 실제로 응답이 어떻게 나가는지 본다. 프레임워크가 걸러 주더라도 앞단 프록시가 다시 조합하는 구성이면 그 지점에서 다시 생길 수 있다.

| 확인 | 방법 |
| --- | --- |
| 애플리케이션 응답 | 직접 호출 |
| 프록시 통과 응답 | 외부에서 호출 |
| 캐시 저장 여부 | Age 헤더 |
| 로그 | 비정상 헤더 기록 |

## 참고

- CWE-113 — HTTP 응답 분할
- OWASP — Input Validation Cheat Sheet
- RFC 9110 — HTTP 의미론`,
    diagram: {
      type: 'flow',
      caption: '주입 경로',
      steps: [
        { label: '입력 수신', note: '요청 파라미터' },
        { label: '헤더에 삽입', note: '검증 없음' },
        { label: '줄바꿈으로 헤더 종료', note: '새 응답 시작' },
        { label: '조작된 응답', note: '캐시 오염·스크립트', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '차단 지점',
      layers: [
        { label: '입력 검증', note: '제어 문자 거부' },
        { label: '프레임워크 함수', note: '자동 필터' },
        { label: '값 인코딩', note: '파일 이름 등' },
        { label: '프록시 확인', note: '계층 간 해석' },
      ],
    },
  },
  {
    slug: 'websocket-authorization',
    title: '웹소켓 연결의 인가를 어디서 하는가',
    body: `웹소켓은 최초 업그레이드 요청에서만 일반 HTTP 헤더가 오간다. 그 뒤로는 양방향 메시지만 흐르므로, 연결 시점에 한 번 확인한 권한이 연결이 유지되는 동안 계속 유효한 것처럼 취급되기 쉽다.

## 연결 시점 확인만으로 충분한가?

충분하지 않다. 권한은 연결 도중에도 바뀐다.

| 상황 | 문제 |
| --- | --- |
| 권한 회수 | 연결이 살아 있어 계속 수신 |
| 계정 비활성화 | 같은 문제 |
| 토큰 만료 | 검사 지점이 없다 |
| 구독 대상 변경 | 다른 자원을 요청 |

![연결 시점과 메시지 시점의 검사 지점](/img/posts/websocket-authorization.svg)

## 두 지점에서 확인한다

| 지점 | 확인 |
| --- | --- |
| 업그레이드 요청 | 인증, 출처, 기본 권한 |
| 메시지 수신 | 구독 대상 권한, 토큰 유효성 |

메시지마다 데이터베이스를 조회하면 느리다. 연결 시점에 권한 정보를 캐시하고, 권한 변경 이벤트가 오면 무효화하는 방식이 실용적이다.

## 업그레이드 요청에서 볼 것

\`\`\`
Origin        허용 목록과 대조 — 브라우저는 이 헤더를 붙인다
인증          쿠키 또는 첫 메시지의 토큰
경로·파라미터  구독 대상이 이 사용자의 것인가
\`\`\`

Origin 검사가 특히 중요하다. 웹소켓에는 동일 출처 정책이 적용되지 않으므로, 다른 사이트에서 사용자의 쿠키로 연결을 열 수 있다.

## 연결 수명 관리

| 항목 | 설정 |
| --- | --- |
| 최대 연결 시간 | 토큰 수명 이하 |
| 유휴 종료 | 일정 시간 무통신 시 |
| 재인증 | 주기적 토큰 갱신 요구 |
| 권한 변경 시 | 즉시 종료 |

최대 연결 시간을 토큰 수명보다 길게 두면, 만료된 자격 증명으로 계속 통신하게 된다.

## 인가 정보를 어디에 두는가

연결마다 권한 정보를 메모리에 두면 인스턴스가 재시작될 때 사라지고, 인스턴스가 여러 개면 일관성이 깨진다. 권한 변경 이벤트를 모든 인스턴스가 받아야 하므로 발행·구독 구조가 필요하다. 규모가 작다면 짧은 만료를 두고 주기적으로 다시 확인하는 편이 단순하고 안전하다. 복잡한 무효화 구조보다 짧은 수명이 관리하기 쉽다.

![권한 정보 보관과 무효화 경로](/img/posts/websocket-authorization-2.svg)

## 메시지 검증

수신 메시지는 외부 입력이다. 형식과 크기, 빈도를 모두 제한한다.

| 항목 | 제한 |
| --- | --- |
| 메시지 크기 | 상한 |
| 초당 메시지 수 | 상한 |
| 구독 수 | 연결당 상한 |
| 형식 | 스키마 검증 |

## 실제로 이렇게 터진다

알림 기능을 웹소켓으로 만들면서 구독 대상을 클라이언트가 지정하게 한 사례가 있다. 다른 사용자의 식별자를 보내면 그 사람의 알림이 그대로 흘러왔다. 최초 연결에서 인증은 했지만 구독 권한은 확인하지 않았다.

또 하나는 퇴사 처리 후에도 연결이 살아 있던 경우다. 계정은 막혔지만 이미 열린 연결로 며칠간 사내 알림을 계속 받았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 연결 시 인증하면 끝이다 | 구독마다 확인해야 한다 |
| 계정을 막으면 끊긴다 | 연결은 살아 있다 |
| 브라우저가 출처를 막는다 | 웹소켓에는 동일 출처 정책이 없다 |
| 내부 서비스라 괜찮다 | 브라우저에서 직접 연결된다 |

## 점검 방법

연결을 두 계정으로 열고 서로의 대상을 구독해 본다. 메시지가 오면 구독 권한 검사가 없는 것이다.

| 시험 | 기대 |
| --- | --- |
| 다른 사용자 식별자 구독 | 거부 |
| 권한 회수 후 기존 연결 | 종료 |
| 다른 출처에서 연결 | 거부 |
| 토큰 만료 후 | 재인증 요구 |

## 참고

- RFC 6455 — The WebSocket Protocol
- OWASP — WebSocket Security Cheat Sheet
- MDN — WebSocket 보안 고려사항`,
    diagram: {
      type: 'steps',
      caption: '검사 지점',
      steps: [
        { label: '업그레이드 요청', note: '인증·출처·기본 권한' },
        { label: '연결 수립', note: '권한 정보 캐시' },
        { label: '메시지 수신', note: '구독 대상 권한 확인' },
        { label: '권한 변경 이벤트', note: '캐시 무효화 또는 연결 종료' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검사 누락의 결과',
      x: ['연결 시 검사', '없음'],
      y: ['메시지 검사 있음', '없음'],
      cells: ['안전', '무단 연결 가능', '권한 회수 미반영', '전면 노출'],
    },
  },
  {
    slug: 'cors-preflight-pitfalls',
    title: 'CORS 설정에서 반복되는 실수',
    body: `교차 출처 요청을 열어 주는 설정은 한 줄로 끝나 보이지만, 그 한 줄이 인증된 요청까지 허용하면 다른 사이트가 사용자의 자격 증명으로 우리 API 를 부를 수 있게 된다.

## CORS 설정에서 어떤 조합이 위험한가?

| 설정 | 위험 |
| --- | --- |
| 출처 전체 허용 + 자격 증명 허용 | 규격상 금지, 우회 구현이 위험 |
| 요청 출처를 그대로 반영 | 사실상 전체 허용 |
| 부분 문자열 검사 | 유사 도메인 우회 |
| 하위 도메인 전체 허용 | 하나 탈취되면 전부 |
| null 출처 허용 | 샌드박스 프레임에서 접근 |

가장 흔한 실수는 요청의 출처 헤더를 그대로 응답에 넣는 것이다. 검사 없이 반영하면 어떤 사이트든 허용된다.

![출처 검사 방식에 따른 결과 차이](/img/posts/cors-preflight-pitfalls.svg)

## 안전한 검사

\`\`\`javascript
const ALLOWED = new Set(['https://app.example.com', 'https://admin.example.com'])

function corsOrigin(origin) {
  return ALLOWED.has(origin) ? origin : null   // 정확히 일치할 때만
}
\`\`\`

부분 문자열이나 정규식 대신 정확한 일치를 쓴다. 정규식은 점을 이스케이프하지 않아 우회되는 경우가 많다.

## 사전 요청을 이해한다

단순하지 않은 요청은 브라우저가 먼저 확인 요청을 보낸다. 이 응답에서 허용하는 메서드와 헤더를 정한다.

| 항목 | 주의 |
| --- | --- |
| 허용 메서드 | 필요한 것만 |
| 허용 헤더 | 와일드카드 지양 |
| 캐시 시간 | 짧게 — 정책 변경 반영 |
| 노출 헤더 | 필요한 것만 |

캐시 시간을 길게 두면 정책을 바꿔도 브라우저가 옛 정책을 쓴다.

## CORS 는 무엇을 막지 않는가

| 항목 | 사실 |
| --- | --- |
| 서버 간 요청 | 막지 않는다 |
| 요청 자체 | 도달한다, 응답 읽기만 제한 |
| 브라우저 외 클라이언트 | 적용되지 않는다 |
| 데이터 변경 | 단순 요청은 이미 실행된다 |

브라우저 정책일 뿐 서버 측 인가를 대신하지 않는다. 인가는 별도로 있어야 한다.

## 보안 헤더 설정을 어디에서 관리하나

허용 출처 목록이 코드 여러 곳에 흩어져 있으면 한쪽만 고쳐지는 일이 생긴다. 게이트웨이, 애플리케이션, 정적 파일 서버가 각자 다른 정책을 쓰는 경우가 실제로 흔하다. 정책을 한곳에서 정의하고 배포 시점에 주입하는 구조로 만들면 어긋남이 줄어든다. 환경마다 목록이 다르므로 설정 값으로 분리하는 것도 함께 정한다.

![허용 목록을 한곳에서 관리한다](/img/posts/cors-preflight-pitfalls-2.svg)

## 점검

\`\`\`bash
# 임의 출처를 그대로 반영하는지 — 반영하면 사실상 전체 허용이다
curl -sI -H 'Origin: https://attacker.example' https://api.example.com/me \
  | grep -i 'access-control-allow'
\`\`\`

## 실제로 이렇게 터진다

개발 편의를 위해 모든 출처를 허용하고 자격 증명도 함께 허용한 설정이 운영에 그대로 올라간 사례가 있다. 사용자가 다른 사이트를 방문하는 것만으로 그 사이트가 우리 API 를 사용자 권한으로 호출할 수 있었다.

또 하나는 하위 도메인 전체를 허용한 경우다. 방치된 하위 도메인 하나가 선점당하자 그것이 허용 출처가 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| CORS 가 API 를 보호한다 | 브라우저 정책일 뿐이다 |
| 허용하지 않으면 요청이 안 간다 | 단순 요청은 이미 도달한다 |
| 개발 설정은 운영에 안 간다 | 자주 그대로 간다 |
| 하위 도메인은 우리 것이다 | 방치된 것이 선점된다 |

## 환경별 분리

설정 값을 코드에 두지 않고 환경 변수로 주입하면 개발 설정이 운영에 따라가지 않는다.

| 환경 | 허용 출처 |
| --- | --- |
| 로컬 | localhost 지정 포트 |
| 개발 | 개발 도메인 |
| 스테이징 | 스테이징 도메인 |
| 운영 | 운영 도메인만 |

배포 시 실제 응답 헤더를 확인하는 항목을 점검 목록에 넣는다.

## 참고

- MDN — 교차 출처 리소스 공유(CORS)
- OWASP — CORS 설정 오류 점검 항목
- Fetch Standard — CORS protocol`,
    diagram: {
      type: 'matrix',
      caption: '출처 검사 방식',
      x: ['자격 증명 허용', '미허용'],
      y: ['정확 일치 검사', '요청 출처 반영'],
      cells: ['안전', '대체로 안전', '전면 노출', '데이터 노출'],
    },
    diagram2: {
      type: 'flow',
      caption: '사전 요청 흐름',
      steps: [
        { label: '브라우저 사전 요청', note: 'OPTIONS' },
        { label: '서버 정책 응답', note: '허용 메서드·헤더' },
        { label: '본 요청 전송', note: '허용된 경우만' },
        { label: '응답 읽기 허용', note: '출처가 맞을 때', danger: false },
      ],
    },
  },
  {
    slug: 'client-side-path-traversal',
    title: '클라이언트 측 경로 조작이 만드는 문제',
    body: `프런트엔드 코드가 사용자 값으로 요청 경로를 조합하면, 상위 디렉터리 참조로 의도하지 않은 엔드포인트를 부를 수 있다. 서버 취약점이 아니라 클라이언트 코드의 문제다.

## 경로 탐색이 클라이언트에서 어떻게 생기나?

\`\`\`javascript
// 위험: 사용자 값이 경로에 들어간다
await fetch(\`/api/users/\${userId}/profile\`)
// userId 가 "1/../../admin/config" 이면 다른 경로가 된다
\`\`\`

브라우저가 경로를 정규화하므로 상위 참조가 실제로 적용된다. 서버는 정상 요청으로 받는다.

![경로 정규화가 만드는 우회](/img/posts/client-side-path-traversal.svg)

## 무엇이 가능해지나

| 결과 | 설명 |
| --- | --- |
| 다른 엔드포인트 호출 | 의도 밖 기능 |
| 인증 토큰 전송 | 다른 대상에 |
| 상태 변경 요청 | 부작용 발생 |
| 다른 도메인 요청 | 프로토콜 상대 경로 |

토큰이 함께 전송되는 것이 위험하다. 클라이언트가 붙이는 인증 헤더가 의도하지 않은 대상으로 간다.

## 방어

| 방법 | 효과 |
| --- | --- |
| 값 인코딩 | 슬래시가 경로 구분자가 되지 않음 |
| 형식 검증 | 예상 형식만 |
| 경로 조합 함수 | 안전한 결합 |
| 절대 경로 고정 | 변수 위치 제한 |

인코딩이 가장 간단하다. 값에 포함된 슬래시가 문자로 처리된다.

\`\`\`javascript
await fetch(\`/api/users/\${encodeURIComponent(userId)}/profile\`)
\`\`\`

![방어 방법](/img/posts/client-side-path-traversal-2.svg)

## 서버에서도 확인한다

클라이언트만 고치면 요청을 직접 만드는 경우에 막히지 않는다. 서버에서 경로 파라미터의 형식을 검증하고, 인가를 확인한다.

| 계층 | 확인 |
| --- | --- |
| 클라이언트 | 값 인코딩 |
| 게이트웨이 | 경로 정규화 후 라우팅 |
| 서버 | 형식 검증·인가 |

## 점검

\`\`\`bash
# 프런트 코드에서 경로에 변수를 직접 넣는 곳
grep -rnE 'fetch\\(\`[^\`]*\$\\{' src/ | grep -v encodeURIComponent | head -20
\`\`\`

인코딩 없이 변수를 경로에 넣는 곳이 검토 대상이다. 값이 서버에서 온 식별자라도 조작될 수 있다.

이 유형은 자동 점검 도구가 잘 찾지 못한다. 서버 응답만 보면 정상이고, 문제는 클라이언트가 어떤 요청을 만드는지에 있다. 코드 리뷰에서 경로 조합 부분을 보는 것이 실질적인 방법이다.

프런트엔드 코드에서 요청을 만드는 위치를 한곳으로 모으면 검토가 쉬워진다. 각 화면에서 직접 호출하지 않고 공통 계층을 거치게 하면, 그 계층에서 인코딩을 강제할 수 있다.

## 실제로 이렇게 터진다

관리 화면에서 사용자 상세를 부를 때 식별자를 그대로 경로에 넣은 사례가 있다. 식별자가 사용자 입력에서 왔고, 상위 참조를 넣자 다른 관리 API 가 호출됐다. 그 요청에는 관리자 토큰이 함께 실렸다.

또 하나는 파일 미리보기 기능에서 파일 경로를 요청 경로에 조합한 경우다. 다른 폴더의 파일을 열 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 서버에서 온 값은 안전하다 | 조작될 수 있다 |
| 프런트 문제라 위험이 작다 | 토큰이 함께 나간다 |
| 서버가 막아 준다 | 정상 경로로 보인다 |
| 도구가 찾아 준다 | 클라이언트 조합은 잘 못 찾는다 |

## 요청 계층을 하나로

각 화면에서 직접 호출하지 않고 공통 요청 계층을 거치게 하면 인코딩을 한곳에서 강제할 수 있다.

| 항목 | 공통 계층에서 |
| --- | --- |
| 경로 조합 | 인코딩 적용 |
| 인증 헤더 | 대상 확인 후 부착 |
| 기본 주소 | 고정 |
| 오류 처리 | 일관되게 |

## 참고

- CWE-22 — 경로 탐색
- OWASP — Input Validation Cheat Sheet
- URL Standard — 경로 정규화 규칙`,
    diagram: {
      type: 'flow',
      caption: '우회 흐름',
      steps: [
        { label: '값에 상위 참조 삽입', note: '사용자 조작' },
        { label: '경로 조합', note: '인코딩 없음' },
        { label: '브라우저 정규화', note: '다른 경로가 됨' },
        { label: '의도 밖 요청', note: '토큰 포함', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '방어 조합',
      x: ['클라이언트 인코딩', '없음'],
      y: ['서버 검증', '없음'],
      cells: ['안전', '우회 가능', '부분 방어', '취약'],
    },
  },
  {
    slug: 'json-parser-differences',
    title: 'JSON 파서 차이가 만드는 보안 문제',
    body: `같은 문서를 두 시스템이 다르게 해석하면, 한쪽이 검증한 값과 다른 쪽이 사용하는 값이 달라진다. 중복 키, 큰 수, 주석 처리 방식이 구현마다 다르다.

## 어떤 차이가 있나?

| 항목 | 구현 차이 |
| --- | --- |
| 중복 키 | 첫 값·마지막 값·오류 |
| 큰 정수 | 정밀도 손실·문자열 유지 |
| 주석 | 허용·거부 |
| 후행 쉼표 | 허용·거부 |
| 제어 문자 | 허용·거부 |
| 대소문자 키 | 구분·병합 |

중복 키 처리가 가장 위험하다. 검증 계층이 첫 값을 보고 처리 계층이 마지막 값을 쓰면 검증이 무의미해진다.

![파서 차이가 만드는 검증 우회](/img/posts/json-parser-differences.svg)

## 무엇이 가능해지나

| 상황 | 결과 |
| --- | --- |
| 게이트웨이와 서버 파서 다름 | 검증 우회 |
| 서명 검증과 처리 파서 다름 | 서명 우회 |
| 언어 간 연동 | 값 불일치 |
| 큰 수 처리 | 금액 오차 |

금액을 큰 정수로 다루는 시스템에서 정밀도 손실이 실제 오차를 만든다. 문자열로 주고받는 것이 안전하다.

## 방어

1. 검증과 처리를 같은 파서로 한다
2. 중복 키를 오류로 처리한다
3. 스키마로 구조를 강제한다
4. 큰 수는 문자열로 주고받는다
5. 계층 간 파서 구현을 문서로 관리한다

1번이 근본이다. 원문을 한 번만 해석하고 그 결과를 계층 간에 전달하면 차이가 생기지 않는다.

![같은 파서로 통일한다](/img/posts/json-parser-differences-2.svg)

## 서명 검증 시 주의

서명은 원문 바이트에 대해 계산된다. 파싱한 뒤 다시 직렬화하면 원문과 달라져 검증이 실패하거나, 반대로 다른 내용에 대한 서명이 통과할 수 있다.

| 원칙 | 이유 |
| --- | --- |
| 원문 보관 | 서명 검증용 |
| 검증 후 파싱 | 순서 |
| 재직렬화 금지 | 원문 변형 |

## 점검

\`\`\`bash
# 중복 키를 어떻게 처리하는지 확인한다
echo '{"role":"user","role":"admin"}' | python3 -c 'import json,sys; print(json.load(sys.stdin))'
echo '{"role":"user","role":"admin"}' | node -e 'let d="";process.stdin.on("data",c=>d+=c).on("end",()=>console.log(JSON.parse(d)))'
\`\`\`

두 결과가 같은지 확인한다. 다르면 계층 간에 그 차이가 존재한다는 뜻이다.

## 실제로 이렇게 터진다

게이트웨이에서 권한 필드를 검증하고 뒷단 서비스가 처리하는 구조에서, 중복 키를 넣자 두 계층이 다른 값을 읽었다. 게이트웨이는 첫 값을 보고 통과시켰고 서비스는 마지막 값을 썼다.

또 하나는 금액을 큰 정수로 주고받으면서 한쪽 파서가 정밀도를 잃은 경우다. 정산에서 몇 원씩 차이가 났고 원인을 찾는 데 오래 걸렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| JSON 은 표준이라 같다 | 구현마다 차이가 있다 |
| 중복 키는 오류다 | 조용히 처리하는 구현이 많다 |
| 숫자는 정확하다 | 큰 정수에서 손실된다 |
| 스키마가 있으면 안전하다 | 파싱 이후 단계다 |

## 언어 간 연동에서 특히

서로 다른 언어로 만든 서비스가 연동하면 파서가 다를 수밖에 없다. 연동 규약에 다음을 명시한다.

| 항목 | 규약 |
| --- | --- |
| 중복 키 | 오류 처리 |
| 큰 수 | 문자열 전달 |
| 날짜 | 문자열 형식 고정 |
| 알 수 없는 필드 | 무시 또는 거부 명시 |

## 참고

- RFC 8259 — JSON 데이터 교환 형식
- CWE-436 — 해석 충돌
- OWASP — Input Validation Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '검증 우회',
      steps: [
        { label: '중복 키 문서 전송', note: '두 값 포함' },
        { label: '게이트웨이 검증', note: '첫 값 기준 통과' },
        { label: '서버 처리', note: '마지막 값 사용' },
        { label: '권한 상승', note: '검증 무의미', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '파서 일치 여부',
      x: ['같은 파서', '다른 파서'],
      y: ['스키마 검증', '없음'],
      cells: ['안전', '주의', '부분 방어', '우회 가능'],
    },
  },
  {
    slug: 'file-upload-content-check',
    title: '파일 업로드에서 확장자 검사가 부족한 이유',
    body: `업로드 검증을 확장자 목록으로만 하면 이름만 바꾼 파일이 통과한다. 이름은 사용자가 정하는 값이고, 서버와 브라우저는 파일을 이름이 아니라 내용과 응답 헤더로 해석한다.

## 무엇을 검사해야 하나?

| 검사 | 방법 |
| --- | --- |
| 실제 형식 | 파일 시작 바이트 확인 |
| 크기 | 상한 |
| 이름 | 서버가 새로 정한다 |
| 저장 위치 | 웹 경로 밖 |
| 응답 헤더 | 형식 고정, 다운로드 강제 |
| 실행 권한 | 제거 |

확장자는 참고 자료일 뿐이다. 시작 바이트로 실제 형식을 확인한다.

![이름 검사와 내용 검사의 차이](/img/posts/file-upload-content-check.svg)

## 이름을 서버가 정한다

사용자가 준 이름을 경로에 쓰면 상위 디렉터리 이동, 특수문자, 길이 문제가 모두 따라온다. 무작위 식별자를 만들고 원래 이름은 메타데이터로만 보관한다.

\`\`\`
저장 키      2f9c1a7e-...-b3.bin      서버가 생성
원본 이름    보고서 최종(1).pdf        데이터베이스 컬럼
표시 이름    내려받기 시 헤더로 지정
\`\`\`

## 응답 헤더가 절반이다

저장은 안전해도 제공할 때 형식이 잘못되면 브라우저가 실행한다.

| 헤더 | 값 |
| --- | --- |
| 형식 | 저장 시 확인한 형식으로 고정 |
| 형식 추측 방지 | 활성화 |
| 다운로드 강제 | 첨부로 지정 |
| 콘텐츠 보안 정책 | 스크립트 실행 차단 |

사용자 업로드 파일은 별도 도메인에서 제공하는 것이 가장 안전하다. 같은 출처에서 제공하면 쿠키와 스크립트 실행 범위를 공유한다.

## 이미지도 안전하지 않다

| 위험 | 대응 |
| --- | --- |
| 메타데이터에 코드 | 재인코딩으로 제거 |
| 위치 정보 노출 | 메타데이터 제거 |
| 압축 폭탄 | 해제 크기 제한 |
| 벡터 이미지 스크립트 | 별도 정화 또는 금지 |

이미지를 다시 인코딩하면 대부분의 문제가 사라진다. 원본을 보관해야 한다면 제공용과 분리한다.

## 파일 업로드 기능을 설계할 때

기능을 만들기 전에 무엇을 받을지 정한다. 형식, 크기, 개수, 보존 기간을 먼저 정하면 검증 규칙이 자연히 정해진다. 반대로 일단 받고 나중에 제한하려 하면 이미 올라간 파일 때문에 규칙을 강하게 걸지 못한다. 업로드는 되돌리기 어려운 기능이므로 처음 설계가 오래 간다.

![받을 것을 먼저 정하고 규칙을 도출한다](/img/posts/file-upload-content-check-2.svg)

## 점검

\`\`\`bash
# 저장된 파일의 실제 형식과 확장자가 일치하는지
for f in uploads/*; do
  printf '%-40s %s\n' "$f" "$(file -b --mime-type "$f")"
done | head -20
\`\`\`

## 실제로 이렇게 터진다

이미지 업로드 기능에서 확장자만 검사한 사례가 있다. 스크립트 파일을 이미지 확장자로 바꿔 올렸고, 웹 경로에 저장된 그 파일이 서버 설정에 따라 실행됐다.

또 하나는 검사는 제대로 했지만 제공할 때 형식을 지정하지 않은 경우다. 브라우저가 내용을 추측해 스크립트로 해석했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 확장자 검사면 충분하다 | 이름은 사용자가 정한다 |
| 이미지는 안전하다 | 메타데이터와 형식 혼동이 있다 |
| 저장만 잘하면 된다 | 제공 헤더가 절반이다 |
| 같은 도메인이 편하다 | 스크립트 실행 범위를 공유한다 |

## 저장 후 제공까지

업로드는 저장과 제공 두 단계다. 한쪽만 안전하면 의미가 없다.

| 단계 | 확인 |
| --- | --- |
| 수신 | 크기·형식 |
| 처리 | 재인코딩·정화 |
| 저장 | 웹 경로 밖, 무작위 키 |
| 제공 | 형식 고정, 별도 도메인 |

## 참고

- OWASP — File Upload Cheat Sheet
- CWE-434 — Unrestricted Upload of File with Dangerous Type
- MDN — Content-Disposition, X-Content-Type-Options`,
    diagram: {
      type: 'steps',
      caption: '업로드 처리 순서',
      steps: [
        { label: '크기 확인', note: '상한 초과 시 거부' },
        { label: '내용 형식 확인', note: '시작 바이트' },
        { label: '재인코딩·정화', note: '이미지·문서' },
        { label: '무작위 키로 저장', note: '웹 경로 밖' },
        { label: '헤더 고정 제공', note: '별도 도메인' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검사 조합',
      x: ['내용 검사', '없음'],
      y: ['헤더 고정', '없음'],
      cells: ['안전', '실행 위험', '우회 가능', '전면 위험'],
    },
  },
  {
    slug: 'xxe-and-xml-parsers',
    title: 'XML 파서 기본 설정이 만드는 문제',
    body: `외부 엔티티를 해석하는 파서에 사용자 XML 을 넣으면, 문서 안의 선언 한 줄로 서버 파일을 읽거나 내부 주소로 요청을 보낼 수 있다. 코드 문제가 아니라 파서 기본값 문제인 경우가 많다.

## 왜 기본값이 문제인가?

오래된 규격 호환을 위해 외부 참조를 허용하는 파서가 있다. 애플리케이션이 그 기능을 쓰지 않아도 켜져 있으면 공격 경로가 된다.

| 결과 | 설명 |
| --- | --- |
| 서버 파일 읽기 | 설정·키 파일 노출 |
| 내부 요청 | 메타데이터·내부 API 접근 |
| 서비스 거부 | 중첩 엔티티 확장 |
| 포트 스캔 | 응답 차이로 확인 |

![외부 엔티티 선언이 서버 자원에 닿는 경로](/img/posts/xxe-and-xml-parsers.svg)

## 어디에 XML 이 숨어 있나

| 형식 | 내부 구조 |
| --- | --- |
| 문서 파일 | 압축된 XML |
| 표 계산 파일 | 압축된 XML |
| 벡터 이미지 | XML |
| 설정 파일 | XML |
| 인증 연동 메시지 | XML |
| 피드 | XML |

XML 을 직접 다루지 않아도 문서 업로드 기능이 있으면 해당한다.

## 대응

1. 외부 엔티티 해석을 끈다
2. 문서 형식 선언 자체를 거부한다
3. 가능하면 다른 형식을 쓴다
4. 파서 라이브러리를 최신으로 유지한다
5. 파싱을 격리된 환경에서 수행한다

가장 확실한 것은 문서 형식 선언을 아예 거부하는 것이다. 일반적인 데이터 교환에는 필요 없다.

\`\`\`java
// 안전한 파서 설정 예
var f = DocumentBuilderFactory.newInstance();
f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
f.setFeature("http://xml.org/sax/features/external-general-entities", false);
f.setFeature("http://xml.org/sax/features/external-parameter-entities", false);
f.setXIncludeAware(false);
f.setExpandEntityReferences(false);
\`\`\`

## 인증 연동에서 특히 주의

인증 메시지를 처리하는 구현은 XML 서명을 검증한다. 서명 검증 전에 파싱이 일어나므로, 파서 설정이 안전하지 않으면 인증되지 않은 요청으로도 공격이 가능하다.

## 점검

\`\`\`bash
# XML 파서 사용 위치와 설정 여부
grep -rnE 'DocumentBuilderFactory|SAXParser|XMLReader|etree\.parse|libxml' src/ \
  | head -20
\`\`\`

각 위치에서 외부 엔티티 관련 설정이 있는지 확인한다. 없으면 기본값으로 동작한다.

## 설정을 유지하는 방법

파서 설정을 코드 여러 곳에서 각자 하면 하나가 빠진다. 안전한 설정을 적용한 공통 팩토리를 만들고 그것만 쓰게 한다. 새 코드가 직접 파서를 만들면 검사에서 걸리도록 규칙을 넣는다. 라이브러리를 바꿀 때 설정이 초기화되는 경우도 있으므로, 의존성 갱신 후 한 번 확인하는 절차를 둔다.

![공통 팩토리로 설정을 강제한다](/img/posts/xxe-and-xml-parsers-2.svg)

## 실제로 이렇게 터진다

문서 업로드 기능에서 표 계산 파일을 처리하다 서버 설정 파일이 유출된 사례가 있다. 그 형식이 내부적으로 XML 이라는 것을 아무도 인식하지 못했다.

또 하나는 인증 연동 메시지를 처리하는 구현에서 서명 검증 전에 파싱이 일어난 경우다. 인증되지 않은 요청으로도 내부 주소에 접근할 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| XML 을 쓰지 않는다 | 문서 파일이 XML 이다 |
| 최신 라이브러리는 안전하다 | 기본 설정에 달렸다 |
| 파일 형식 검사로 막힌다 | 정상 파일 안에 들어간다 |
| 오래된 취약점이다 | 지금도 계속 발견된다 |

## 처리 격리

파싱을 별도 프로세스나 컨테이너에서 하면, 설정을 놓쳐도 접근 범위가 제한된다.

| 격리 수단 | 효과 |
| --- | --- |
| 네트워크 차단 | 외부 참조 실패 |
| 파일 접근 제한 | 읽을 것이 없음 |
| 자원 제한 | 확장 공격 완화 |
| 시간 제한 | 지연 공격 완화 |

## 참고

- OWASP — XML External Entity Prevention Cheat Sheet
- CWE-611 — Improper Restriction of XML External Entity Reference
- 각 언어별 XML 파서 보안 설정 문서`,
    diagram: {
      type: 'flow',
      caption: '외부 엔티티 처리',
      steps: [
        { label: 'XML 업로드', note: '문서 형식 선언 포함' },
        { label: '파서가 선언 해석', note: '기본 설정' },
        { label: '외부 참조 요청', note: '파일·내부 주소' },
        { label: '응답에 내용 포함', note: '정보 유출', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '차단 지점',
      layers: [
        { label: '문서 형식 선언 거부', note: '가장 확실' },
        { label: '외부 엔티티 비활성화', note: '파서 설정' },
        { label: '네트워크 차단', note: '외부 요청 금지' },
        { label: '격리 실행', note: '파싱 프로세스 분리' },
      ],
    },
  },
  {
    slug: 'open-redirect-chains',
    title: '열린 리다이렉트가 다른 공격으로 이어지는 방식',
    body: `주소를 그대로 받아 이동시키는 기능은 그 자체로는 피해가 작아 보인다. 문제는 이 기능이 다른 취약점의 연결 고리로 쓰인다는 점이다. 인증 흐름과 결합하면 토큰이 외부로 나간다.

## 무엇과 연결되나?

| 결합 | 결과 |
| --- | --- |
| 인증 코드 흐름 | 인가 코드가 공격자에게 |
| 토큰 전달 | 접근 토큰 유출 |
| 피싱 | 신뢰 도메인에서 시작하는 링크 |
| 서버 측 요청 위조 | 내부 주소로 우회 |
| 콘텐츠 보안 정책 우회 | 허용 도메인 경유 |

인증 연동에서 되돌아올 주소를 검증하지 않으면 그것이 곧 계정 탈취 경로가 된다.

![신뢰 도메인을 거쳐 외부로 나가는 경로](/img/posts/open-redirect-chains.svg)

## 되돌아올 주소 검증에서 자주 하는 실수

| 방식 | 우회 |
| --- | --- |
| 접두사 검사 | 유사 도메인 사용 |
| 부분 문자열 포함 | 경로에 도메인 문자열 삽입 |
| 상대 경로만 허용한다고 가정 | 두 슬래시로 시작하는 주소 |
| 인코딩 후 검사 | 여러 번 인코딩 |
| 목록 검사 후 재조합 | 조합 과정에서 변형 |

두 슬래시로 시작하는 주소가 상대 경로로 오해되는 경우가 흔하다. 브라우저는 이를 다른 호스트로 해석한다.

## 안전한 처리

1. 되돌아올 주소를 외부에서 받지 않는다 — 서버가 기억한다
2. 받아야 한다면 경로만 받고 호스트는 서버가 붙인다
3. 목록과 정확히 대조한다
4. 해석한 결과의 호스트를 확인한다
5. 외부로 나가는 경우 경유 안내 화면을 보여 준다

\`\`\`javascript
// 경로만 허용하고 호스트는 서버가 정한다
function safeNext(next) {
  if (typeof next !== 'string') return '/'
  if (!next.startsWith('/') || next.startsWith('//')) return '/'
  return next
}
\`\`\`

## 인증 연동에서

되돌아올 주소는 사전에 등록된 정확한 값만 허용한다. 부분 일치나 하위 경로 허용은 우회 여지를 만든다.

| 설정 | 권장 |
| --- | --- |
| 등록 방식 | 정확한 전체 주소 |
| 와일드카드 | 사용하지 않음 |
| 상태 값 | 필수 |
| 코드 검증자 | 필수 |

## 열린 리다이렉트와 oauth 인가 코드 흐름

인증 연동을 붙일 때 되돌아올 주소를 유연하게 두고 싶은 유혹이 있다. 개발·시험 환경을 함께 지원하려다 와일드카드를 넣게 되고, 그 설정이 운영에도 남는다. 환경마다 등록을 나누는 것이 번거로워 보여도, 계정 탈취 경로 하나를 없애는 값이 훨씬 크다. 등록 목록을 정기적으로 검토해 쓰지 않는 주소를 지운다.

![환경별로 되돌아올 주소를 나눈다](/img/posts/open-redirect-chains-2.svg)

## 점검

\`\`\`bash
# 이동 처리에 외부 입력이 들어가는 위치
grep -rnE 'redirect\(|location\s*=|sendRedirect' src/ | grep -iE 'req|query|param' | head
\`\`\`

## 실제로 이렇게 터진다

로그인 후 원래 페이지로 돌아가는 기능에서 주소를 요청 값으로 받은 사례가 있다. 공격자가 만든 링크로 로그인한 사용자가 외부 사이트로 이동했고, 그 과정에서 인가 코드가 함께 전달됐다.

또 하나는 외부 링크 경유 페이지에서 검증 없이 이동시킨 경우다. 우리 도메인으로 시작하는 피싱 링크가 만들어졌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 이동만 하니 위험이 낮다 | 토큰이 함께 나간다 |
| 접두사 검사면 된다 | 유사 도메인으로 우회된다 |
| 상대 경로는 안전하다 | 두 슬래시로 시작하면 아니다 |
| 사용자가 알아챈다 | 도메인이 우리 것으로 보인다 |

## 경유 화면

외부로 나가는 링크에 경유 안내 화면을 두면 사용자가 목적지를 확인할 수 있다.

| 대상 | 처리 |
| --- | --- |
| 내부 경로 | 바로 이동 |
| 등록된 외부 | 바로 이동 |
| 그 외 외부 | 경유 화면 |
| 형식 오류 | 홈으로 |

## 참고

- OWASP — Unvalidated Redirects and Forwards Cheat Sheet
- CWE-601 — URL Redirection to Untrusted Site
- OAuth 2.0 Security Best Current Practice — redirect URI 검증`,
    diagram: {
      type: 'flow',
      caption: '토큰 유출 경로',
      steps: [
        { label: '피싱 링크 전달', note: '신뢰 도메인 주소' },
        { label: '인증 수행', note: '사용자는 정상으로 인식' },
        { label: '되돌아올 주소로 이동', note: '검증 없음' },
        { label: '외부에 토큰 전달', note: '계정 탈취', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검증 방식별 안전도',
      unit: '점',
      items: [
        { label: '서버 보관', value: 100, note: '외부에서 받지 않음' },
        { label: '경로만 허용', value: 85, note: '호스트는 서버가' },
        { label: '정확 일치 목록', value: 80, note: '전체 주소' },
        { label: '접두사 검사', value: 20, note: '우회 가능' },
      ],
    },
  },
  {
    slug: 'web-cache-deception',
    title: '캐시 기만으로 남의 정보가 새는 경로',
    body: `캐시 서버가 정적 파일로 착각하도록 주소를 만들면, 로그인한 사용자의 개인 페이지가 캐시에 저장된다. 그 뒤 다른 사람이 같은 주소로 접근하면 저장된 내용을 받는다.

## 어떻게 성립하나?

| 조건 | 설명 |
| --- | --- |
| 확장자로 캐시 판단 | 경로 끝만 보고 결정 |
| 서버가 뒤 경로 무시 | 존재하지 않는 경로도 처리 |
| 캐시 키에 인증 미반영 | 사용자 구분 없음 |
| 응답에 캐시 금지 없음 | 저장 허용 |

네 조건이 함께 있어야 한다. 응답 헤더 하나만 제대로 있어도 성립하지 않는다.

![캐시 기만 성립 조건](/img/posts/web-cache-deception.svg)

## 무엇이 새나

| 페이지 | 노출 |
| --- | --- |
| 마이페이지 | 이름·연락처 |
| 주문 내역 | 구매 정보 |
| 설정 화면 | 계정 정보 |
| 토큰 포함 페이지 | 세션 탈취 |

토큰이 화면에 포함된 경우가 가장 위험하다. 캐시된 응답으로 다른 사람이 그 계정이 될 수 있다.

## 방어

| 방법 | 효과 |
| --- | --- |
| 인증 응답에 캐시 금지 | 가장 확실 |
| 캐시 규칙을 경로 기준으로 | 확장자 판단 지양 |
| 없는 경로는 오류 반환 | 무시하지 않음 |
| 캐시 키에 인증 반영 | 사용자별 구분 |
| 정적 파일 도메인 분리 | 구조적 분리 |

인증된 응답에 캐시 금지 헤더를 붙이는 것이 기본이다. 이것만 있어도 대부분 막힌다.

![방어 지점](/img/posts/web-cache-deception-2.svg)

## 서버 라우팅 확인

존재하지 않는 하위 경로를 서버가 조용히 무시하는 구성이 조건이 된다. 정확히 일치하는 경로만 처리하고 나머지는 404 를 반환하게 한다.

| 요청 | 기대 응답 |
| --- | --- |
| /mypage | 200 |
| /mypage/ | 200 또는 301 |
| /mypage/x.css | 404 |
| /mypage/../admin | 404 또는 정규화 후 판단 |

## 점검

\`\`\`bash
# 인증 상태로 가짜 확장자 경로를 요청해 응답과 캐시 헤더를 본다
curl -sI -H "Cookie: session=..." https://example.com/mypage/nonexistent.css \
  | grep -iE 'HTTP/|cache-control|x-cache|age'
\`\`\`

200 이 오면서 캐시 금지가 없으면 취약하다. 두 번째 요청에서 Age 값이 보이면 이미 캐시된 것이다.

캐시 규칙은 대개 인프라 담당이 관리하고 응답 헤더는 개발팀이 정한다. 두 쪽이 서로의 설정을 모르면 이런 조합이 생긴다. 캐시 규칙을 바꿀 때 어떤 경로가 영향을 받는지 함께 확인하는 절차를 둔다.

점검을 정기적으로 한다. 새 화면이 추가되거나 캐시 규칙이 바뀌면 조건이 다시 만들어질 수 있다. 인증이 필요한 주요 경로 몇 개를 골라 자동 점검에 넣으면 회귀를 잡을 수 있다.

## 실제로 이렇게 터진다

CDN 을 도입하면서 확장자 기준으로 캐시 규칙을 정한 사례가 있다. 로그인 사용자의 마이페이지 주소 뒤에 가짜 확장자를 붙인 링크가 돌았고, 그것을 누른 사용자의 개인정보가 캐시에 저장돼 다른 사람에게 노출됐다.

또 하나는 응답에 캐시 금지 헤더를 붙였지만 일부 경로에서 누락된 경우다. 프레임워크가 자동으로 붙이는 줄 알았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 로그인 페이지는 캐시되지 않는다 | 헤더가 없으면 저장된다 |
| CDN 이 알아서 구분한다 | 규칙대로만 한다 |
| 확장자 기준이 간단하다 | 우회 조건이 된다 |
| 없는 경로는 404 다 | 무시하는 구성이 많다 |

## 배포 전 확인

| 확인 | 방법 |
| --- | --- |
| 인증 응답 헤더 | 캐시 금지 여부 |
| 가짜 확장자 경로 | 404 반환 여부 |
| 캐시 규칙 | 경로 기준인가 |
| Age 헤더 | 인증 응답에 있으면 문제 |

## 참고

- OWASP — Web Cache Deception 관련 자료
- RFC 9111 — HTTP 캐싱
- 각 CDN 사업자의 캐시 규칙 문서`,
    diagram: {
      type: 'flow',
      caption: '기만 흐름',
      steps: [
        { label: '가짜 확장자 주소 접근', note: '피해자 유도' },
        { label: '서버가 개인 페이지 반환', note: '뒤 경로 무시' },
        { label: '캐시가 정적으로 판단', note: '저장' },
        { label: '공격자가 같은 주소 접근', note: '개인 정보 노출', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '차단 지점',
      layers: [
        { label: '응답 캐시 금지', note: '가장 확실' },
        { label: '경로 기준 캐시 규칙', note: '확장자 판단 지양' },
        { label: '엄격한 라우팅', note: '없는 경로 404' },
        { label: '도메인 분리', note: '구조적' },
      ],
    },
  },
  {
    slug: 'client-side-validation-trap',
    title: '클라이언트 검증만 있는 화면의 함정',
    body: `입력값 검사를 화면에서만 하면 사용자 경험은 좋아지지만 방어는 없다. 브라우저 개발자 도구나 직접 호출로 검사 자체를 건너뛸 수 있기 때문이다. 화면 검증은 안내이고 서버 검증이 통제다.

## 엔드포인트를 직접 부르면 무엇이 건너뛰어지나?

| 화면 검증 | 우회 방법 |
| --- | --- |
| 필수 입력 표시 | 요청 직접 전송 |
| 길이·형식 제한 | 요청 본문 수정 |
| 선택지 제한 | 값 임의 지정 |
| 비활성 버튼 | 해당 요청 직접 호출 |
| 숨긴 필드 | 값 변경 |
| 가격 표시 | 요청의 금액 필드 조작 |

가격이나 수량을 요청에서 받는 구조가 특히 위험하다. 서버가 다시 계산해야 한다.

![화면 검증과 서버 검증의 위치](/img/posts/client-side-validation-trap.svg)

## 서버에서 반드시 할 입력 검증

1. 모든 입력의 형식과 범위를 검증한다
2. 값이 아니라 식별자를 받고 값은 서버가 조회한다
3. 권한을 요청마다 확인한다
4. 상태 전이가 올바른지 확인한다
5. 계산은 서버에서 한다

2번이 핵심이다. 가격, 등급, 할인율을 요청에서 받지 않고 식별자로 조회한다.

\`\`\`javascript
// 나쁜 예: 금액을 요청에서 받는다
// const total = req.body.total

// 좋은 예: 상품 식별자로 서버가 계산한다
const items = await loadItems(req.body.itemIds)
const total = items.reduce((s, it) => s + it.price * it.qty, 0)
\`\`\`

## 화면 검증도 필요하다

없애라는 뜻이 아니다. 역할이 다르다.

| 계층 | 역할 |
| --- | --- |
| 화면 | 즉시 안내, 왕복 감소 |
| 서버 | 실제 통제 |
| 데이터베이스 | 최종 제약 |

세 계층의 규칙이 어긋나면 화면에서는 되는데 저장이 안 되는 상황이 생긴다. 규칙을 한곳에서 정의하고 공유하는 편이 낫다.

## 점검

\`\`\`bash
# 화면을 거치지 않고 직접 호출 — 서버 검증이 있으면 400 이어야 한다
curl -s -o /dev/null -w '%{http_code}\n' -X POST \
  -H 'Content-Type: application/json' -H "Authorization: Bearer $TOKEN" \
  -d '{"itemId":1,"qty":-5,"total":0}' https://api.example.com/orders
\`\`\`

음수 수량이나 0 원 주문이 200 으로 돌아오면 서버 검증이 없는 것이다.

## 규칙을 한곳에서 정의한다

같은 검증 규칙을 화면과 서버에 각각 쓰면 시간이 지나며 어긋난다. 스키마를 한 번 정의하고 양쪽에서 가져다 쓰는 구조가 유지하기 쉽다. 언어가 다르면 규칙을 데이터로 정의하고 생성하는 방법도 있다. 어느 쪽이든 사람이 두 곳을 손으로 맞추는 방식은 결국 어긋난다.

![규칙을 한곳에서 정의하고 양쪽에서 쓴다](/img/posts/client-side-validation-trap-2.svg)

## 실제로 이렇게 터진다

주문 금액을 요청 본문에서 받은 사례가 있다. 화면에서는 정상 금액이 계산돼 보였지만, 요청을 직접 만들면 1원짜리 주문이 통과했다. 결제 대행사 승인도 그 금액으로 났다.

또 하나는 등급 필드를 숨긴 입력으로 둔 경우다. 값을 바꾸자 할인율이 적용됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 화면에서 막으면 된다 | 요청을 직접 만든다 |
| 숨긴 필드는 안 보인다 | 개발자 도구로 보인다 |
| 앱은 수정이 어렵다 | 프록시로 요청을 바꾼다 |
| 서버 검증은 중복이다 | 유일한 통제다 |

## 값이 아니라 식별자를 받는다

| 나쁜 설계 | 나은 설계 |
| --- | --- |
| 금액을 받는다 | 상품 식별자를 받고 서버가 계산 |
| 등급을 받는다 | 계정에서 조회 |
| 할인율을 받는다 | 쿠폰 식별자로 조회 |
| 권한을 받는다 | 세션에서 판단 |

## 참고

- OWASP — Input Validation Cheat Sheet
- CWE-602 — Client-Side Enforcement of Server-Side Security
- OWASP API Security — Mass Assignment 방지`,
    diagram: {
      type: 'layers',
      caption: '검증 계층의 역할',
      layers: [
        { label: '화면 검증', note: '안내 — 통제 아님' },
        { label: '서버 검증', note: '실제 통제' },
        { label: '업무 규칙 확인', note: '상태·권한' },
        { label: '데이터베이스 제약', note: '최종 방어선' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검증 위치별 결과',
      x: ['서버 검증 있음', '없음'],
      y: ['화면 검증 있음', '없음'],
      cells: ['안전·편리', '조작 가능', '안전·불편', '전면 위험'],
    },
  },
]
