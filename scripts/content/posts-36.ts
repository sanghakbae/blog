import type { SeedPost } from './types'

/** 351~360 — 웹 취약점 심화 */
export const posts36: SeedPost[] = [
  {
    slug: 'http-request-smuggling',
    title: 'HTTP 요청 스머글링이 생기는 조건',
    body: `앞단 프록시와 뒷단 서버가 요청의 경계를 다르게 해석하면, 하나의 연결에 숨겨 보낸 두 번째 요청이 다음 사용자의 요청 앞에 끼어든다. 취약점은 어느 한쪽의 버그가 아니라 두 구현의 해석 차이에서 생긴다.

## 어떤 조건에서 생기는가?

| 조건 | 내용 |
| --- | --- |
| 연결 재사용 | 프록시와 서버가 연결을 유지한다 |
| 길이 해석 차이 | 본문 길이를 정하는 헤더를 다르게 본다 |
| 헤더 정규화 부재 | 중복·변형 헤더를 그대로 전달한다 |
| 오류 관대함 | 규격에 어긋난 요청을 받아 준다 |

네 가지가 함께 있어야 성립한다. 하나만 제거해도 공격이 되지 않는다.

![두 구현의 해석 차이가 요청 경계를 어긋나게 만든다](/img/posts/http-request-smuggling.svg)

## 무엇이 가능해지나

| 결과 | 설명 |
| --- | --- |
| 다른 사용자 요청 가로채기 | 앞에 끼어든 요청이 응답을 흡수 |
| 인증 우회 | 앞단의 접근 통제를 지나침 |
| 캐시 오염 | 잘못된 응답이 캐시에 저장 |
| 응답 큐 오염 | 사용자마다 엉뚱한 응답 |

가장 심각한 것은 응답이 뒤섞이는 상황이다. 로그인한 사용자의 응답이 다른 사람에게 간다.

## 어떻게 막나

1. 앞단에서 본문 길이 헤더를 정규화한다 — 중복이면 거부
2. 규격에 어긋난 요청은 통과시키지 않고 거부한다
3. 앞단과 뒷단의 구현·버전을 파악하고 알려진 차이를 확인한다
4. 뒷단 연결 재사용을 제한한다 — 성능과 교환
5. 프록시 계층 수를 줄인다

1번과 2번이 기본이다. 관대하게 처리하는 설정이 취약점을 만든다.

\`\`\`bash
# 프록시 계층 확인 — 계층이 많을수록 해석 차이 가능성이 커진다
curl -sI https://example.com | grep -iE 'via|server|x-cache|x-served-by'
\`\`\`

## 점검할 때 주의할 것

이 유형은 점검 자체가 다른 사용자에게 영향을 준다. 운영 환경에서 함부로 시험하지 않는다. 시험 환경에서 같은 프록시 구성으로 재현하고, 운영에서는 구성 검토로 대신한다.

## 요청 스머글링과 웹 방화벽의 관계

웹 방화벽은 앞단에서 요청을 본다. 스머글링은 그 앞단을 정상적으로 통과한 뒤 뒷단에서 다른 요청으로 쪼개지므로, 방화벽 규칙으로는 잡히지 않는다. 방어의 자리가 규칙이 아니라 구성에 있다는 뜻이다. 프록시와 서버의 조합을 문서로 정리하고, 구성 요소를 바꿀 때마다 이 조합이 안전한지 확인하는 절차를 둔다. 버전 하나를 올리는 것만으로 해석이 달라질 수 있다.

![프록시 계층 구성을 문서로 관리한다](/img/posts/http-request-smuggling-2.svg)

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 최신 버전이면 안전하다 | 조합에 따라 다르다 |
| 웹 방화벽이 막는다 | 앞단을 지나서 생긴다 |
| 드문 취약점이다 | 프록시가 여러 겹이면 흔하다 |
| 우리 코드 문제가 아니다 | 구성이 우리 책임이다 |

## 참고

- RFC 9112 — HTTP/1.1 메시지 문법
- OWASP — HTTP Request Smuggling
- PortSwigger Web Security Academy — Request smuggling`,
    diagram: {
      type: 'flow',
      caption: '요청 경계 어긋남',
      steps: [
        { label: '공격자 요청 전송', note: '모호한 길이 헤더' },
        { label: '프록시 해석', note: '하나의 요청으로 본다' },
        { label: '서버 해석', note: '두 개로 본다' },
        { label: '다음 사용자 요청과 결합', note: '응답 뒤섞임', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '제거할 조건',
      layers: [
        { label: '헤더 정규화', note: '중복 길이 헤더 거부' },
        { label: '엄격한 파싱', note: '규격 위반 요청 거부' },
        { label: '연결 재사용 제한', note: '뒷단 연결 분리' },
        { label: '계층 축소', note: '프록시 수 최소화' },
      ],
    },
  },
  {
    slug: 'websocket-authorization',
    title: '웹소켓 연결의 인가를 어디서 하는가',
    body: `웹소켓은 최초 업그레이드 요청에서만 일반 HTTP 헤더가 오간다. 그 뒤로는 양방향 메시지만 흐르므로, 연결 시점에 한 번 확인한 권한이 연결이 유지되는 동안 계속 유효한 것처럼 취급되기 쉽다.

## 연결 시점 확인만으로 충분한가?

충분하지 않다. 권한은 연결 도중에도 바뀐다.

| 상황 | 문제 |
| --- | --- |
| 권한 회수 | 연결이 살아 있어 계속 수신 |
| 계정 비활성화 | 같은 문제 |
| 토큰 만료 | 검사 지점이 없다 |
| 구독 대상 변경 | 다른 자원을 요청 |

![연결 시점과 메시지 시점의 검사 지점](/img/posts/websocket-authorization.svg)

## 두 지점에서 확인한다

| 지점 | 확인 |
| --- | --- |
| 업그레이드 요청 | 인증, 출처, 기본 권한 |
| 메시지 수신 | 구독 대상 권한, 토큰 유효성 |

메시지마다 데이터베이스를 조회하면 느리다. 연결 시점에 권한 정보를 캐시하고, 권한 변경 이벤트가 오면 무효화하는 방식이 실용적이다.

## 업그레이드 요청에서 볼 것

\`\`\`
Origin        허용 목록과 대조 — 브라우저는 이 헤더를 붙인다
인증          쿠키 또는 첫 메시지의 토큰
경로·파라미터  구독 대상이 이 사용자의 것인가
\`\`\`

Origin 검사가 특히 중요하다. 웹소켓에는 동일 출처 정책이 적용되지 않으므로, 다른 사이트에서 사용자의 쿠키로 연결을 열 수 있다.

## 연결 수명 관리

| 항목 | 설정 |
| --- | --- |
| 최대 연결 시간 | 토큰 수명 이하 |
| 유휴 종료 | 일정 시간 무통신 시 |
| 재인증 | 주기적 토큰 갱신 요구 |
| 권한 변경 시 | 즉시 종료 |

최대 연결 시간을 토큰 수명보다 길게 두면, 만료된 자격 증명으로 계속 통신하게 된다.

## 인가 정보를 어디에 두는가

연결마다 권한 정보를 메모리에 두면 인스턴스가 재시작될 때 사라지고, 인스턴스가 여러 개면 일관성이 깨진다. 권한 변경 이벤트를 모든 인스턴스가 받아야 하므로 발행·구독 구조가 필요하다. 규모가 작다면 짧은 만료를 두고 주기적으로 다시 확인하는 편이 단순하고 안전하다. 복잡한 무효화 구조보다 짧은 수명이 관리하기 쉽다.

![권한 정보 보관과 무효화 경로](/img/posts/websocket-authorization-2.svg)

## 메시지 검증

수신 메시지는 외부 입력이다. 형식과 크기, 빈도를 모두 제한한다.

| 항목 | 제한 |
| --- | --- |
| 메시지 크기 | 상한 |
| 초당 메시지 수 | 상한 |
| 구독 수 | 연결당 상한 |
| 형식 | 스키마 검증 |

## 참고

- RFC 6455 — The WebSocket Protocol
- OWASP — WebSocket Security Cheat Sheet
- MDN — WebSocket 보안 고려사항`,
    diagram: {
      type: 'steps',
      caption: '검사 지점',
      steps: [
        { label: '업그레이드 요청', note: '인증·출처·기본 권한' },
        { label: '연결 수립', note: '권한 정보 캐시' },
        { label: '메시지 수신', note: '구독 대상 권한 확인' },
        { label: '권한 변경 이벤트', note: '캐시 무효화 또는 연결 종료' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검사 누락의 결과',
      x: ['연결 시 검사', '없음'],
      y: ['메시지 검사 있음', '없음'],
      cells: ['안전', '무단 연결 가능', '권한 회수 미반영', '전면 노출'],
    },
  },
  {
    slug: 'cors-preflight-pitfalls',
    title: 'CORS 설정에서 반복되는 실수',
    body: `교차 출처 요청을 열어 주는 설정은 한 줄로 끝나 보이지만, 그 한 줄이 인증된 요청까지 허용하면 다른 사이트가 사용자의 자격 증명으로 우리 API 를 부를 수 있게 된다.

## CORS 설정에서 어떤 조합이 위험한가?

| 설정 | 위험 |
| --- | --- |
| 출처 전체 허용 + 자격 증명 허용 | 규격상 금지, 우회 구현이 위험 |
| 요청 출처를 그대로 반영 | 사실상 전체 허용 |
| 부분 문자열 검사 | 유사 도메인 우회 |
| 하위 도메인 전체 허용 | 하나 탈취되면 전부 |
| null 출처 허용 | 샌드박스 프레임에서 접근 |

가장 흔한 실수는 요청의 출처 헤더를 그대로 응답에 넣는 것이다. 검사 없이 반영하면 어떤 사이트든 허용된다.

![출처 검사 방식에 따른 결과 차이](/img/posts/cors-preflight-pitfalls.svg)

## 안전한 검사

\`\`\`javascript
const ALLOWED = new Set(['https://app.example.com', 'https://admin.example.com'])

function corsOrigin(origin) {
  return ALLOWED.has(origin) ? origin : null   // 정확히 일치할 때만
}
\`\`\`

부분 문자열이나 정규식 대신 정확한 일치를 쓴다. 정규식은 점을 이스케이프하지 않아 우회되는 경우가 많다.

## 사전 요청을 이해한다

단순하지 않은 요청은 브라우저가 먼저 확인 요청을 보낸다. 이 응답에서 허용하는 메서드와 헤더를 정한다.

| 항목 | 주의 |
| --- | --- |
| 허용 메서드 | 필요한 것만 |
| 허용 헤더 | 와일드카드 지양 |
| 캐시 시간 | 짧게 — 정책 변경 반영 |
| 노출 헤더 | 필요한 것만 |

캐시 시간을 길게 두면 정책을 바꿔도 브라우저가 옛 정책을 쓴다.

## CORS 는 무엇을 막지 않는가

| 항목 | 사실 |
| --- | --- |
| 서버 간 요청 | 막지 않는다 |
| 요청 자체 | 도달한다, 응답 읽기만 제한 |
| 브라우저 외 클라이언트 | 적용되지 않는다 |
| 데이터 변경 | 단순 요청은 이미 실행된다 |

브라우저 정책일 뿐 서버 측 인가를 대신하지 않는다. 인가는 별도로 있어야 한다.

## 보안 헤더 설정을 어디에서 관리하나

허용 출처 목록이 코드 여러 곳에 흩어져 있으면 한쪽만 고쳐지는 일이 생긴다. 게이트웨이, 애플리케이션, 정적 파일 서버가 각자 다른 정책을 쓰는 경우가 실제로 흔하다. 정책을 한곳에서 정의하고 배포 시점에 주입하는 구조로 만들면 어긋남이 줄어든다. 환경마다 목록이 다르므로 설정 값으로 분리하는 것도 함께 정한다.

![허용 목록을 한곳에서 관리한다](/img/posts/cors-preflight-pitfalls-2.svg)

## 점검

\`\`\`bash
# 임의 출처를 그대로 반영하는지 — 반영하면 사실상 전체 허용이다
curl -sI -H 'Origin: https://attacker.example' https://api.example.com/me \
  | grep -i 'access-control-allow'
\`\`\`

## 참고

- MDN — 교차 출처 리소스 공유(CORS)
- OWASP — CORS 설정 오류 점검 항목
- Fetch Standard — CORS protocol`,
    diagram: {
      type: 'matrix',
      caption: '출처 검사 방식',
      x: ['자격 증명 허용', '미허용'],
      y: ['정확 일치 검사', '요청 출처 반영'],
      cells: ['안전', '대체로 안전', '전면 노출', '데이터 노출'],
    },
    diagram2: {
      type: 'flow',
      caption: '사전 요청 흐름',
      steps: [
        { label: '브라우저 사전 요청', note: 'OPTIONS' },
        { label: '서버 정책 응답', note: '허용 메서드·헤더' },
        { label: '본 요청 전송', note: '허용된 경우만' },
        { label: '응답 읽기 허용', note: '출처가 맞을 때', danger: false },
      ],
    },
  },
  {
    slug: 'dom-clobbering',
    title: 'DOM 클로버링과 전역 변수 오염',
    body: `HTML 요소의 이름이나 식별자가 전역 객체의 속성으로 노출되는 브라우저 동작을 이용해, 스크립트가 참조하는 변수를 마크업만으로 바꿔치기할 수 있다. 스크립트를 넣지 못하는 상황에서도 동작을 바꾸는 수단이 된다.

## 스크립팅 없이 어떻게 가능한가?

브라우저는 식별자나 이름이 붙은 요소를 전역에서 접근할 수 있게 한다. 코드가 정의되지 않은 전역 변수를 참조하고 있으면, 그 자리에 요소가 들어간다.

\`\`\`html
<!-- 이 마크업만으로 window.config 가 존재하게 된다 -->
<a id="config"></a>
<a id="config" name="url" href="https://attacker.example"></a>
\`\`\`

![마크업이 전역 참조를 바꾸는 경로](/img/posts/dom-clobbering.svg)

## 무엇이 위험해지나

| 코드 패턴 | 결과 |
| --- | --- |
| 설정 객체가 없으면 기본값 사용 | 공격자 값이 설정이 된다 |
| 스크립트 주소를 변수에서 읽음 | 외부 스크립트 로드 |
| 정화 라이브러리 설정 참조 | 정화 우회 |
| 리다이렉트 주소 참조 | 열린 리다이렉트 |

정화 라이브러리의 설정을 오염시켜 정화 자체를 무력화하는 사례가 알려져 있다.

## 방어

| 방법 | 효과 |
| --- | --- |
| 변수를 명시적으로 선언 | 전역 오염이 덮이지 않는다 |
| 엄격 모드 사용 | 선언 없는 참조 오류 |
| 값의 형식 검증 | 요소가 아닌지 확인 |
| 사용자 HTML 에서 식별자·이름 제거 | 근본 차단 |
| 모듈 스코프 사용 | 전역 노출 자체를 줄인다 |

가장 확실한 것은 사용자가 넣은 HTML 에서 식별자와 이름 속성을 제거하는 것이다. 정화 설정에 이 항목이 포함돼 있는지 확인한다.

## 값 검증 예

\`\`\`javascript
// 전역에서 읽은 값이 DOM 요소일 수 있다고 가정한다
const raw = window.appConfig
const config = raw && typeof raw === 'object' && !(raw instanceof Node) ? raw : DEFAULTS
\`\`\`

## 사용자 HTML 을 허용한다면

| 속성 | 처리 |
| --- | --- |
| id | 제거 또는 접두사 부여 |
| name | 제거 |
| 이벤트 핸들러 | 제거 |
| 스타일 | 제한적 허용 |

접두사를 붙이는 방식이 실용적이다. 사용자 콘텐츠의 식별자를 별도 이름공간으로 격리한다.

## 크로스 사이트 스크립팅 점검과 함께 본다

사용자가 HTML 을 넣을 수 있는 지점을 먼저 찾는다. 게시글 본문, 댓글, 프로필 소개, 메일 템플릿 미리보기가 흔한 자리다. 그다음 전역 변수를 읽는 코드를 찾는다. 두 집합이 만나는 곳이 실제 위험 지점이다. 대부분의 경우 정화 설정에 속성 하나를 추가하는 것으로 끝나지만, 그 설정이 있는지 확인해 본 팀은 많지 않다.

![입력 지점과 전역 참조가 만나는 자리](/img/posts/dom-clobbering-2.svg)

## 참고

- PortSwigger — DOM clobbering
- DOMPurify 문서 — SANITIZE_NAMED_PROPS 옵션
- HTML Standard — named access on the Window object`,
    diagram: {
      type: 'flow',
      caption: '오염 경로',
      steps: [
        { label: '사용자 HTML 삽입', note: 'id·name 속성 포함' },
        { label: '브라우저가 전역 노출', note: '요소가 window 속성으로' },
        { label: '스크립트가 그 변수 참조', note: '기본값 대신' },
        { label: '동작 변경', note: '정화 우회·외부 로드', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '방어 계층',
      layers: [
        { label: '식별자·이름 제거', note: '입력 정화 단계' },
        { label: '변수 명시 선언', note: '전역 의존 제거' },
        { label: '값 형식 검증', note: '요소인지 확인' },
        { label: '콘텐츠 보안 정책', note: '외부 로드 제한' },
      ],
    },
  },
  {
    slug: 'prototype-pollution',
    title: '프로토타입 오염이 서버에서 위험한 이유',
    body: `객체 병합이나 경로 기반 대입에서 특수한 키를 걸러내지 않으면, 모든 객체가 공유하는 상위 객체에 속성이 추가된다. 클라이언트에서는 화면이 깨지는 정도지만, 서버에서는 인가 판정과 명령 실행까지 영향을 받는다.

## 인가 판정을 흔드는 오염은 어디서 생기는가?

| 패턴 | 위험 |
| --- | --- |
| 깊은 병합 함수 | 사용자 입력을 그대로 병합 |
| 경로 문자열로 대입 | 점 표기 경로 처리 |
| 질의 문자열 파싱 | 중첩 객체 지원 라이브러리 |
| JSON 역직렬화 후 확장 | 키 검증 없음 |

특수 키를 검사하지 않는 병합 함수가 가장 흔한 경로다.

![입력이 상위 객체를 오염시키는 흐름](/img/posts/prototype-pollution.svg)

## 서버에서 무엇이 되나

| 영향 | 예 |
| --- | --- |
| 인가 우회 | 기본값으로 읽히는 권한 플래그 |
| 설정 변조 | 기본 설정 객체에 값 주입 |
| 명령 실행 | 하위 프로세스 실행 옵션 오염 |
| 서비스 거부 | 필수 속성 덮어쓰기 |

인가 판정이 "속성이 없으면 기본값" 구조라면, 오염으로 그 기본값을 바꿀 수 있다.

## 방어

1. 병합·대입에서 특수 키를 거부한다
2. 프로토타입이 없는 객체로 데이터를 만든다
3. 구조를 스키마로 검증한 뒤 사용한다
4. 상위 객체를 동결한다
5. 안전한 병합 라이브러리를 쓴다

\`\`\`javascript
const BLOCKED = new Set(['__proto__', 'constructor', 'prototype'])

function safeAssign(target, key, value) {
  if (BLOCKED.has(key)) return          // 특수 키 거부
  target[key] = value
}

const data = Object.create(null)        // 프로토타입 없는 객체
\`\`\`

## 스키마 검증이 근본 대응이다

허용된 필드만 남기고 나머지를 버리면 특수 키가 애초에 들어오지 않는다. 검증 라이브러리에서 알 수 없는 필드를 제거하는 옵션을 켠다.

| 방식 | 효과 |
| --- | --- |
| 알 수 없는 필드 제거 | 높음 |
| 알 수 없는 필드 거부 | 높음 |
| 통과 허용 | 없음 |

## 클라이언트와 서버의 차이

브라우저에서 일어나면 화면이 깨지거나 스크립트가 실행되는 정도다. 서버에서 일어나면 프로세스 전체가 영향을 받고, 요청 하나의 오염이 다른 요청의 처리에도 남는다. 상태를 공유하는 서버에서는 한 번의 오염이 이후 모든 요청에 영향을 준다는 점이 특히 위험하다. 그래서 같은 취약점 유형이라도 서버 쪽 대응을 먼저 한다.

![클라이언트와 서버에서의 영향 차이](/img/posts/prototype-pollution-2.svg)

## 점검

\`\`\`bash
# 병합 계열 함수 사용 위치 — 사용자 입력이 들어가는지 확인
grep -rnE 'deepMerge|merge\(|extend\(|set\(.*req\.' src/ | head -20
\`\`\`

## 참고

- OWASP — Prototype Pollution Prevention Cheat Sheet
- Node.js 보안 모범 사례 문서
- CWE-1321 — Improperly Controlled Modification of Object Prototype`,
    diagram: {
      type: 'flow',
      caption: '오염과 영향',
      steps: [
        { label: '요청 본문 수신', note: '특수 키 포함' },
        { label: '깊은 병합 수행', note: '검증 없음' },
        { label: '상위 객체에 속성 추가', note: '전역 영향' },
        { label: '인가 판정 변경', note: '기본값이 바뀐다', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '대응별 효과',
      unit: '점',
      items: [
        { label: '스키마 검증', value: 95, note: '알 수 없는 필드 제거' },
        { label: '특수 키 차단', value: 80, note: '병합 함수 수정' },
        { label: '프로토타입 없는 객체', value: 70, note: '데이터 구조' },
        { label: '라이브러리 갱신', value: 40, note: '알려진 것만' },
      ],
    },
  },
  {
    slug: 'file-upload-content-check',
    title: '파일 업로드에서 확장자 검사가 부족한 이유',
    body: `업로드 검증을 확장자 목록으로만 하면 이름만 바꾼 파일이 통과한다. 이름은 사용자가 정하는 값이고, 서버와 브라우저는 파일을 이름이 아니라 내용과 응답 헤더로 해석한다.

## 무엇을 검사해야 하나?

| 검사 | 방법 |
| --- | --- |
| 실제 형식 | 파일 시작 바이트 확인 |
| 크기 | 상한 |
| 이름 | 서버가 새로 정한다 |
| 저장 위치 | 웹 경로 밖 |
| 응답 헤더 | 형식 고정, 다운로드 강제 |
| 실행 권한 | 제거 |

확장자는 참고 자료일 뿐이다. 시작 바이트로 실제 형식을 확인한다.

![이름 검사와 내용 검사의 차이](/img/posts/file-upload-content-check.svg)

## 이름을 서버가 정한다

사용자가 준 이름을 경로에 쓰면 상위 디렉터리 이동, 특수문자, 길이 문제가 모두 따라온다. 무작위 식별자를 만들고 원래 이름은 메타데이터로만 보관한다.

\`\`\`
저장 키      2f9c1a7e-...-b3.bin      서버가 생성
원본 이름    보고서 최종(1).pdf        데이터베이스 컬럼
표시 이름    내려받기 시 헤더로 지정
\`\`\`

## 응답 헤더가 절반이다

저장은 안전해도 제공할 때 형식이 잘못되면 브라우저가 실행한다.

| 헤더 | 값 |
| --- | --- |
| 형식 | 저장 시 확인한 형식으로 고정 |
| 형식 추측 방지 | 활성화 |
| 다운로드 강제 | 첨부로 지정 |
| 콘텐츠 보안 정책 | 스크립트 실행 차단 |

사용자 업로드 파일은 별도 도메인에서 제공하는 것이 가장 안전하다. 같은 출처에서 제공하면 쿠키와 스크립트 실행 범위를 공유한다.

## 이미지도 안전하지 않다

| 위험 | 대응 |
| --- | --- |
| 메타데이터에 코드 | 재인코딩으로 제거 |
| 위치 정보 노출 | 메타데이터 제거 |
| 압축 폭탄 | 해제 크기 제한 |
| 벡터 이미지 스크립트 | 별도 정화 또는 금지 |

이미지를 다시 인코딩하면 대부분의 문제가 사라진다. 원본을 보관해야 한다면 제공용과 분리한다.

## 파일 업로드 기능을 설계할 때

기능을 만들기 전에 무엇을 받을지 정한다. 형식, 크기, 개수, 보존 기간을 먼저 정하면 검증 규칙이 자연히 정해진다. 반대로 일단 받고 나중에 제한하려 하면 이미 올라간 파일 때문에 규칙을 강하게 걸지 못한다. 업로드는 되돌리기 어려운 기능이므로 처음 설계가 오래 간다.

![받을 것을 먼저 정하고 규칙을 도출한다](/img/posts/file-upload-content-check-2.svg)

## 점검

\`\`\`bash
# 저장된 파일의 실제 형식과 확장자가 일치하는지
for f in uploads/*; do
  printf '%-40s %s\n' "$f" "$(file -b --mime-type "$f")"
done | head -20
\`\`\`

## 참고

- OWASP — File Upload Cheat Sheet
- CWE-434 — Unrestricted Upload of File with Dangerous Type
- MDN — Content-Disposition, X-Content-Type-Options`,
    diagram: {
      type: 'steps',
      caption: '업로드 처리 순서',
      steps: [
        { label: '크기 확인', note: '상한 초과 시 거부' },
        { label: '내용 형식 확인', note: '시작 바이트' },
        { label: '재인코딩·정화', note: '이미지·문서' },
        { label: '무작위 키로 저장', note: '웹 경로 밖' },
        { label: '헤더 고정 제공', note: '별도 도메인' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검사 조합',
      x: ['내용 검사', '없음'],
      y: ['헤더 고정', '없음'],
      cells: ['안전', '실행 위험', '우회 가능', '전면 위험'],
    },
  },
  {
    slug: 'xxe-and-xml-parsers',
    title: 'XML 파서 기본 설정이 만드는 문제',
    body: `외부 엔티티를 해석하는 파서에 사용자 XML 을 넣으면, 문서 안의 선언 한 줄로 서버 파일을 읽거나 내부 주소로 요청을 보낼 수 있다. 코드 문제가 아니라 파서 기본값 문제인 경우가 많다.

## 왜 기본값이 문제인가?

오래된 규격 호환을 위해 외부 참조를 허용하는 파서가 있다. 애플리케이션이 그 기능을 쓰지 않아도 켜져 있으면 공격 경로가 된다.

| 결과 | 설명 |
| --- | --- |
| 서버 파일 읽기 | 설정·키 파일 노출 |
| 내부 요청 | 메타데이터·내부 API 접근 |
| 서비스 거부 | 중첩 엔티티 확장 |
| 포트 스캔 | 응답 차이로 확인 |

![외부 엔티티 선언이 서버 자원에 닿는 경로](/img/posts/xxe-and-xml-parsers.svg)

## 어디에 XML 이 숨어 있나

| 형식 | 내부 구조 |
| --- | --- |
| 문서 파일 | 압축된 XML |
| 표 계산 파일 | 압축된 XML |
| 벡터 이미지 | XML |
| 설정 파일 | XML |
| 인증 연동 메시지 | XML |
| 피드 | XML |

XML 을 직접 다루지 않아도 문서 업로드 기능이 있으면 해당한다.

## 대응

1. 외부 엔티티 해석을 끈다
2. 문서 형식 선언 자체를 거부한다
3. 가능하면 다른 형식을 쓴다
4. 파서 라이브러리를 최신으로 유지한다
5. 파싱을 격리된 환경에서 수행한다

가장 확실한 것은 문서 형식 선언을 아예 거부하는 것이다. 일반적인 데이터 교환에는 필요 없다.

\`\`\`java
// 안전한 파서 설정 예
var f = DocumentBuilderFactory.newInstance();
f.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true);
f.setFeature("http://xml.org/sax/features/external-general-entities", false);
f.setFeature("http://xml.org/sax/features/external-parameter-entities", false);
f.setXIncludeAware(false);
f.setExpandEntityReferences(false);
\`\`\`

## 인증 연동에서 특히 주의

인증 메시지를 처리하는 구현은 XML 서명을 검증한다. 서명 검증 전에 파싱이 일어나므로, 파서 설정이 안전하지 않으면 인증되지 않은 요청으로도 공격이 가능하다.

## 점검

\`\`\`bash
# XML 파서 사용 위치와 설정 여부
grep -rnE 'DocumentBuilderFactory|SAXParser|XMLReader|etree\.parse|libxml' src/ \
  | head -20
\`\`\`

각 위치에서 외부 엔티티 관련 설정이 있는지 확인한다. 없으면 기본값으로 동작한다.

## 설정을 유지하는 방법

파서 설정을 코드 여러 곳에서 각자 하면 하나가 빠진다. 안전한 설정을 적용한 공통 팩토리를 만들고 그것만 쓰게 한다. 새 코드가 직접 파서를 만들면 검사에서 걸리도록 규칙을 넣는다. 라이브러리를 바꿀 때 설정이 초기화되는 경우도 있으므로, 의존성 갱신 후 한 번 확인하는 절차를 둔다.

![공통 팩토리로 설정을 강제한다](/img/posts/xxe-and-xml-parsers-2.svg)

## 참고

- OWASP — XML External Entity Prevention Cheat Sheet
- CWE-611 — Improper Restriction of XML External Entity Reference
- 각 언어별 XML 파서 보안 설정 문서`,
    diagram: {
      type: 'flow',
      caption: '외부 엔티티 처리',
      steps: [
        { label: 'XML 업로드', note: '문서 형식 선언 포함' },
        { label: '파서가 선언 해석', note: '기본 설정' },
        { label: '외부 참조 요청', note: '파일·내부 주소' },
        { label: '응답에 내용 포함', note: '정보 유출', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '차단 지점',
      layers: [
        { label: '문서 형식 선언 거부', note: '가장 확실' },
        { label: '외부 엔티티 비활성화', note: '파서 설정' },
        { label: '네트워크 차단', note: '외부 요청 금지' },
        { label: '격리 실행', note: '파싱 프로세스 분리' },
      ],
    },
  },
  {
    slug: 'open-redirect-chains',
    title: '열린 리다이렉트가 다른 공격으로 이어지는 방식',
    body: `주소를 그대로 받아 이동시키는 기능은 그 자체로는 피해가 작아 보인다. 문제는 이 기능이 다른 취약점의 연결 고리로 쓰인다는 점이다. 인증 흐름과 결합하면 토큰이 외부로 나간다.

## 무엇과 연결되나?

| 결합 | 결과 |
| --- | --- |
| 인증 코드 흐름 | 인가 코드가 공격자에게 |
| 토큰 전달 | 접근 토큰 유출 |
| 피싱 | 신뢰 도메인에서 시작하는 링크 |
| 서버 측 요청 위조 | 내부 주소로 우회 |
| 콘텐츠 보안 정책 우회 | 허용 도메인 경유 |

인증 연동에서 되돌아올 주소를 검증하지 않으면 그것이 곧 계정 탈취 경로가 된다.

![신뢰 도메인을 거쳐 외부로 나가는 경로](/img/posts/open-redirect-chains.svg)

## 되돌아올 주소 검증에서 자주 하는 실수

| 방식 | 우회 |
| --- | --- |
| 접두사 검사 | 유사 도메인 사용 |
| 부분 문자열 포함 | 경로에 도메인 문자열 삽입 |
| 상대 경로만 허용한다고 가정 | 두 슬래시로 시작하는 주소 |
| 인코딩 후 검사 | 여러 번 인코딩 |
| 목록 검사 후 재조합 | 조합 과정에서 변형 |

두 슬래시로 시작하는 주소가 상대 경로로 오해되는 경우가 흔하다. 브라우저는 이를 다른 호스트로 해석한다.

## 안전한 처리

1. 되돌아올 주소를 외부에서 받지 않는다 — 서버가 기억한다
2. 받아야 한다면 경로만 받고 호스트는 서버가 붙인다
3. 목록과 정확히 대조한다
4. 해석한 결과의 호스트를 확인한다
5. 외부로 나가는 경우 경유 안내 화면을 보여 준다

\`\`\`javascript
// 경로만 허용하고 호스트는 서버가 정한다
function safeNext(next) {
  if (typeof next !== 'string') return '/'
  if (!next.startsWith('/') || next.startsWith('//')) return '/'
  return next
}
\`\`\`

## 인증 연동에서

되돌아올 주소는 사전에 등록된 정확한 값만 허용한다. 부분 일치나 하위 경로 허용은 우회 여지를 만든다.

| 설정 | 권장 |
| --- | --- |
| 등록 방식 | 정확한 전체 주소 |
| 와일드카드 | 사용하지 않음 |
| 상태 값 | 필수 |
| 코드 검증자 | 필수 |

## 열린 리다이렉트와 oauth 인가 코드 흐름

인증 연동을 붙일 때 되돌아올 주소를 유연하게 두고 싶은 유혹이 있다. 개발·시험 환경을 함께 지원하려다 와일드카드를 넣게 되고, 그 설정이 운영에도 남는다. 환경마다 등록을 나누는 것이 번거로워 보여도, 계정 탈취 경로 하나를 없애는 값이 훨씬 크다. 등록 목록을 정기적으로 검토해 쓰지 않는 주소를 지운다.

![환경별로 되돌아올 주소를 나눈다](/img/posts/open-redirect-chains-2.svg)

## 점검

\`\`\`bash
# 이동 처리에 외부 입력이 들어가는 위치
grep -rnE 'redirect\(|location\s*=|sendRedirect' src/ | grep -iE 'req|query|param' | head
\`\`\`

## 참고

- OWASP — Unvalidated Redirects and Forwards Cheat Sheet
- CWE-601 — URL Redirection to Untrusted Site
- OAuth 2.0 Security Best Current Practice — redirect URI 검증`,
    diagram: {
      type: 'flow',
      caption: '토큰 유출 경로',
      steps: [
        { label: '피싱 링크 전달', note: '신뢰 도메인 주소' },
        { label: '인증 수행', note: '사용자는 정상으로 인식' },
        { label: '되돌아올 주소로 이동', note: '검증 없음' },
        { label: '외부에 토큰 전달', note: '계정 탈취', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검증 방식별 안전도',
      unit: '점',
      items: [
        { label: '서버 보관', value: 100, note: '외부에서 받지 않음' },
        { label: '경로만 허용', value: 85, note: '호스트는 서버가' },
        { label: '정확 일치 목록', value: 80, note: '전체 주소' },
        { label: '접두사 검사', value: 20, note: '우회 가능' },
      ],
    },
  },
  {
    slug: 'race-condition-web',
    title: '웹 애플리케이션의 경쟁 조건 취약점',
    body: `같은 요청을 거의 동시에 여러 번 보내면, 확인과 실행 사이의 짧은 틈에서 검사가 여러 번 통과한다. 쿠폰 중복 사용, 잔액 초과 출금, 한도 우회가 이 구조에서 나온다.

## 왜 검사를 통과하는가?

확인과 반영이 분리돼 있으면 그 사이에 다른 요청이 끼어든다.

\`\`\`
요청 A: 잔액 확인(1000) → ... → 차감(1000)
요청 B:      잔액 확인(1000) → ... → 차감(1000)
결과: 잔액 1000 으로 2000 출금
\`\`\`

![확인과 반영 사이의 틈](/img/posts/race-condition-web.svg)

## 어디에서 생기나

| 기능 | 우회 대상 |
| --- | --- |
| 쿠폰·포인트 사용 | 1회 제한 |
| 출금·이체 | 잔액 |
| 재고 차감 | 수량 |
| 초대·추천 | 횟수 제한 |
| 투표·좋아요 | 중복 방지 |
| 계정 생성 | 중복 검사 |

한도나 횟수가 걸린 모든 기능이 대상이다.

## 대응 방법

| 방법 | 적용 |
| --- | --- |
| 조건부 갱신 | 상태가 맞을 때만 변경 |
| 데이터베이스 잠금 | 해당 행 잠금 |
| 고유 제약 | 중복 삽입 자체를 차단 |
| 원자적 연산 | 감소와 확인을 한 번에 |
| 멱등 키 | 같은 요청 한 번만 |

조건부 갱신이 가장 널리 쓰인다. 갱신 결과 행 수가 0 이면 이미 처리된 것이다.

\`\`\`sql
-- 확인과 차감을 한 문장으로 — 영향 행 수가 0 이면 실패로 처리한다
UPDATE accounts
SET balance = balance - 1000
WHERE id = 42 AND balance >= 1000;
\`\`\`

## 애플리케이션 잠금은 부족하다

인스턴스가 여러 개면 메모리 잠금은 의미가 없다. 분산 환경에서는 데이터베이스나 별도 잠금 서비스를 쓴다.

| 환경 | 수단 |
| --- | --- |
| 단일 인스턴스 | 메모리 잠금 가능 |
| 다중 인스턴스 | 데이터베이스·분산 잠금 |
| 서버리스 | 조건부 갱신·고유 제약 |

## 어디까지 방어할 것인가

모든 요청에 잠금을 걸면 성능이 떨어진다. 한도나 횟수가 걸린 기능, 금전이 오가는 기능에 집중한다. 조회 기능에는 필요 없다. 어떤 기능이 대상인지 목록으로 만들어 두면 새 기능을 추가할 때 판단이 빨라진다. 목록에 없는 기능이라도 한도 조건이 생기면 그때 추가한다.

![방어가 필요한 기능을 구분한다](/img/posts/race-condition-web-2.svg)

## 점검 방법

동시 요청을 보내 결과를 확인한다.

\`\`\`bash
# 같은 요청 20개를 동시에 — 성공 응답이 하나여야 한다
seq 20 | xargs -P 20 -I{} curl -s -o /dev/null -w '%{http_code}\n' \
  -X POST -H "Authorization: Bearer $TOKEN" \
  https://api.example.com/coupons/ABC/redeem | sort | uniq -c
\`\`\`

## 참고

- OWASP — Testing for Race Conditions
- CWE-362 — Concurrent Execution using Shared Resource
- PortSwigger — Race conditions in web applications`,
    diagram: {
      type: 'flow',
      caption: '동시 요청의 결과',
      steps: [
        { label: '동시 요청 전송', note: '같은 작업 여러 개' },
        { label: '각각 조건 확인', note: '모두 통과' },
        { label: '각각 실행', note: '한도 무시' },
        { label: '제한 우회', note: '중복 사용·초과 출금', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '대응 수단별 적용 범위',
      unit: '점',
      items: [
        { label: '고유 제약', value: 95, note: '중복 자체 차단' },
        { label: '조건부 갱신', value: 90, note: '대부분의 경우' },
        { label: '행 잠금', value: 85, note: '트랜잭션 필요' },
        { label: '메모리 잠금', value: 25, note: '단일 인스턴스만' },
      ],
    },
  },
  {
    slug: 'client-side-validation-trap',
    title: '클라이언트 검증만 있는 화면의 함정',
    body: `입력값 검사를 화면에서만 하면 사용자 경험은 좋아지지만 방어는 없다. 브라우저 개발자 도구나 직접 호출로 검사 자체를 건너뛸 수 있기 때문이다. 화면 검증은 안내이고 서버 검증이 통제다.

## 엔드포인트를 직접 부르면 무엇이 건너뛰어지나?

| 화면 검증 | 우회 방법 |
| --- | --- |
| 필수 입력 표시 | 요청 직접 전송 |
| 길이·형식 제한 | 요청 본문 수정 |
| 선택지 제한 | 값 임의 지정 |
| 비활성 버튼 | 해당 요청 직접 호출 |
| 숨긴 필드 | 값 변경 |
| 가격 표시 | 요청의 금액 필드 조작 |

가격이나 수량을 요청에서 받는 구조가 특히 위험하다. 서버가 다시 계산해야 한다.

![화면 검증과 서버 검증의 위치](/img/posts/client-side-validation-trap.svg)

## 서버에서 반드시 할 입력 검증

1. 모든 입력의 형식과 범위를 검증한다
2. 값이 아니라 식별자를 받고 값은 서버가 조회한다
3. 권한을 요청마다 확인한다
4. 상태 전이가 올바른지 확인한다
5. 계산은 서버에서 한다

2번이 핵심이다. 가격, 등급, 할인율을 요청에서 받지 않고 식별자로 조회한다.

\`\`\`javascript
// 나쁜 예: 금액을 요청에서 받는다
// const total = req.body.total

// 좋은 예: 상품 식별자로 서버가 계산한다
const items = await loadItems(req.body.itemIds)
const total = items.reduce((s, it) => s + it.price * it.qty, 0)
\`\`\`

## 화면 검증도 필요하다

없애라는 뜻이 아니다. 역할이 다르다.

| 계층 | 역할 |
| --- | --- |
| 화면 | 즉시 안내, 왕복 감소 |
| 서버 | 실제 통제 |
| 데이터베이스 | 최종 제약 |

세 계층의 규칙이 어긋나면 화면에서는 되는데 저장이 안 되는 상황이 생긴다. 규칙을 한곳에서 정의하고 공유하는 편이 낫다.

## 점검

\`\`\`bash
# 화면을 거치지 않고 직접 호출 — 서버 검증이 있으면 400 이어야 한다
curl -s -o /dev/null -w '%{http_code}\n' -X POST \
  -H 'Content-Type: application/json' -H "Authorization: Bearer $TOKEN" \
  -d '{"itemId":1,"qty":-5,"total":0}' https://api.example.com/orders
\`\`\`

음수 수량이나 0 원 주문이 200 으로 돌아오면 서버 검증이 없는 것이다.

## 규칙을 한곳에서 정의한다

같은 검증 규칙을 화면과 서버에 각각 쓰면 시간이 지나며 어긋난다. 스키마를 한 번 정의하고 양쪽에서 가져다 쓰는 구조가 유지하기 쉽다. 언어가 다르면 규칙을 데이터로 정의하고 생성하는 방법도 있다. 어느 쪽이든 사람이 두 곳을 손으로 맞추는 방식은 결국 어긋난다.

![규칙을 한곳에서 정의하고 양쪽에서 쓴다](/img/posts/client-side-validation-trap-2.svg)

## 참고

- OWASP — Input Validation Cheat Sheet
- CWE-602 — Client-Side Enforcement of Server-Side Security
- OWASP API Security — Mass Assignment 방지`,
    diagram: {
      type: 'layers',
      caption: '검증 계층의 역할',
      layers: [
        { label: '화면 검증', note: '안내 — 통제 아님' },
        { label: '서버 검증', note: '실제 통제' },
        { label: '업무 규칙 확인', note: '상태·권한' },
        { label: '데이터베이스 제약', note: '최종 방어선' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '검증 위치별 결과',
      x: ['서버 검증 있음', '없음'],
      y: ['화면 검증 있음', '없음'],
      cells: ['안전·편리', '조작 가능', '안전·불편', '전면 위험'],
    },
  },
]
