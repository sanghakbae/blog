import type { SeedPost } from './types'

/** 371~380 — 암호와 데이터 보호 */
export const posts38: SeedPost[] = [
  {
    slug: 'envelope-encryption-design',
    title: '봉투 암호화를 실무에서 설계하는 방법',
    body: `데이터를 직접 마스터 키로 암호화하면 키 서비스 호출이 데이터 양만큼 늘고 성능과 비용이 함께 문제가 된다. 데이터마다 별도 키를 만들고 그 키만 마스터 키로 감싸는 구조가 봉투 암호화다.

## 왜 두 겹으로 나누는가?

| 이유 | 설명 |
| --- | --- |
| 성능 | 큰 데이터를 로컬에서 암호화 |
| 비용 | 키 서비스 호출 횟수 감소 |
| 교체 | 마스터 키만 바꾸면 된다 |
| 범위 제한 | 데이터 키 하나가 노출돼도 일부만 |

마스터 키 교체가 쉬워지는 점이 특히 크다. 데이터를 다시 암호화하지 않고 감싼 키만 다시 감싸면 된다.

![데이터 키와 마스터 키의 관계](/img/posts/envelope-encryption-design.svg)

## 데이터 키를 어떻게 다루나

1. 키 서비스에서 데이터 키를 요청한다 — 평문과 암호문을 함께 받는다
2. 평문 키로 데이터를 암호화한다
3. 평문 키를 메모리에서 지운다
4. 암호문 키를 데이터와 함께 저장한다
5. 복호화할 때 암호문 키를 키 서비스에 보내 평문 키를 얻는다

3번을 빼먹으면 프로세스 메모리에 평문 키가 남는다. 사용 직후 지우는 습관이 필요하다.

## 데이터 키의 범위

| 범위 | 장단점 |
| --- | --- |
| 레코드마다 | 격리 최상, 호출 많음 |
| 테넌트마다 | 균형, 흔한 선택 |
| 테이블마다 | 단순, 격리 약함 |
| 전체 하나 | 봉투 구조의 의미 없음 |

테넌트 단위가 실무에서 균형이 좋다. 테넌트 삭제 요청이 오면 그 키만 지워도 데이터가 복호화 불가능해진다.

![키 범위에 따른 격리 수준](/img/posts/envelope-encryption-design-2.svg)

## 키 삭제로 데이터를 지우는 방법

대량 데이터를 물리적으로 지우는 것은 오래 걸리고 백업에도 남는다. 해당 데이터 키를 폐기하면 남은 암호문은 복호화할 수 없다. 규정상 파기로 인정되는지는 확인이 필요하지만, 실질적인 접근 차단으로는 즉시 효과가 있다.

## 주의할 점

| 항목 | 내용 |
| --- | --- |
| 암호문 키 분실 | 데이터 복구 불가 |
| 백업 | 암호문 키도 함께 백업 |
| 키 서비스 장애 | 복호화 불가 — 캐시 전략 |
| 문맥 정보 | 잘못된 대상에 쓰이지 않게 |

키 서비스 호출에 문맥 정보를 넣으면, 같은 데이터 키가 다른 테넌트 데이터에 쓰이는 실수를 막을 수 있다.

## 참고

- NIST SP 800-57 — 키 관리 권고
- 각 클라우드 사업자의 봉투 암호화 문서
- OWASP — Cryptographic Storage Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '암호화 흐름',
      steps: [
        { label: '데이터 키 요청', note: '평문·암호문 수신' },
        { label: '로컬 암호화', note: '평문 키 사용' },
        { label: '평문 키 폐기', note: '메모리에서 제거' },
        { label: '암호문 키 저장', note: '데이터와 함께' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '키 범위별 격리 수준',
      unit: '점',
      items: [
        { label: '레코드마다', value: 95, note: '호출 많음' },
        { label: '테넌트마다', value: 80, note: '균형' },
        { label: '테이블마다', value: 45, note: '단순' },
        { label: '전체 하나', value: 15, note: '의미 없음' },
      ],
    },
  },
  {
    slug: 'crypto-agility',
    title: '암호 알고리즘을 나중에 바꿀 수 있게 설계하기',
    body: `지금 안전한 알고리즘도 언젠가 바꿔야 한다. 알고리즘 이름이 코드 여기저기에 박혀 있으면 교체가 대규모 작업이 되고, 그래서 미뤄지다가 오래된 알고리즘이 계속 남는다.

## 무엇이 교체를 어렵게 하나?

| 원인 | 설명 |
| --- | --- |
| 알고리즘 하드코딩 | 코드 여러 곳에 흩어짐 |
| 형식에 버전 없음 | 어느 방식으로 암호화됐는지 모름 |
| 일괄 재암호화 전제 | 한 번에 바꿔야 한다고 가정 |
| 키와 알고리즘 결합 | 함께 바꿔야 함 |
| 시험 부재 | 바꾸면 무엇이 깨질지 모름 |

형식에 버전이 없는 것이 가장 큰 문제다. 저장된 값만 보고 어떻게 복호화할지 알 수 없으면 점진 전환이 불가능하다.

![저장 형식에 버전을 담는다](/img/posts/crypto-agility.svg)

## 저장 형식 설계

암호문에 버전과 알고리즘 식별자를 붙인다.

\`\`\`
v2:aesgcm256:<키 식별자>:<난스>:<암호문>:<태그>
\`\`\`

| 필드 | 역할 |
| --- | --- |
| 버전 | 형식 자체의 변경 |
| 알고리즘 | 복호화 방법 결정 |
| 키 식별자 | 어느 키로 |
| 난스 | 재사용 금지 |
| 태그 | 무결성 |

이 정보가 있으면 옛 형식과 새 형식이 공존할 수 있다.

## 점진 전환

1. 새 알고리즘으로 쓰기 시작한다
2. 읽을 때 옛 형식도 지원한다
3. 읽는 김에 새 형식으로 다시 쓴다
4. 옛 형식 사용 비율을 측정한다
5. 0 에 가까워지면 남은 것을 일괄 변환한다
6. 옛 형식 지원 코드를 제거한다

3번 방식이 부담이 가장 적다. 자주 읽히는 데이터부터 자연히 전환된다.

![점진 전환 진행 상황 측정](/img/posts/crypto-agility-2.svg)

## 코드 구조

암호화 호출을 한 모듈로 모은다. 애플리케이션 코드는 알고리즘 이름을 몰라도 되게 한다.

| 계층 | 책임 |
| --- | --- |
| 애플리케이션 | 무엇을 암호화할지 |
| 암호 모듈 | 어떻게 암호화할지 |
| 키 서비스 | 키 관리 |

## 전환 진행 측정

\`\`\`bash
# 저장된 암호문의 형식별 분포 — 전환 진행률
psql -Atc "SELECT split_part(secret, ':', 1) AS ver, count(*)
           FROM credentials GROUP BY 1 ORDER BY 2 DESC"
\`\`\`

이 숫자를 대시보드에 올려 두면 전환이 실제로 진행되는지 보인다. 측정하지 않으면 몇 년째 옛 형식이 남는다.

## 참고

- NIST SP 800-131A — 암호 알고리즘 전환 권고
- OWASP — Cryptographic Storage Cheat Sheet
- IETF — 암호 민첩성 관련 논의 문서`,
    diagram: {
      type: 'layers',
      caption: '암호문 형식',
      layers: [
        { label: '버전', note: '형식 변경 대응' },
        { label: '알고리즘 식별자', note: '복호화 방법' },
        { label: '키 식별자', note: '어느 키로' },
        { label: '난스와 태그', note: '재사용 방지·무결성' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '점진 전환',
      steps: [
        { label: '새 형식으로 쓰기', note: '옛 형식 읽기 유지' },
        { label: '읽을 때 재기록', note: '자연 전환' },
        { label: '비율 측정', note: '대시보드' },
        { label: '잔여 일괄 변환', note: '옛 코드 제거' },
      ],
    },
  },
  {
    slug: 'password-storage-migration',
    title: '비밀번호 해시 방식을 안전하게 옮기는 법',
    body: `오래된 해시 방식을 쓰고 있다는 것을 알아도, 평문 비밀번호가 없으니 한 번에 다시 계산할 수 없다. 사용자가 로그인할 때마다 조금씩 옮기는 방식이 현실적이다.

## 어떤 방식이 문제인가?

| 방식 | 상태 |
| --- | --- |
| 단순 해시 | 즉시 교체 필요 |
| 소금 없는 해시 | 즉시 교체 필요 |
| 반복 횟수가 낮은 방식 | 매개변수 상향 |
| 최신 방식, 낮은 매개변수 | 매개변수 상향 |
| 최신 방식, 적정 매개변수 | 유지 |

소금이 없으면 같은 비밀번호가 같은 해시가 되어, 유출 시 대량 복원이 가능하다.

![로그인 시점 재해시 흐름](/img/posts/password-storage-migration.svg)

## 로그인 시점 전환

1. 저장된 값의 형식을 확인한다
2. 옛 방식이면 옛 방식으로 검증한다
3. 검증에 성공하면 그 자리에서 새 방식으로 다시 해시한다
4. 저장 값을 교체한다
5. 옛 형식 비율을 추적한다

\`\`\`javascript
const ok = await verifyLegacy(password, row.hash)
if (ok) {
  row.hash = await argon2.hash(password)   // 이 시점에만 평문이 있다
  await save(row)
}
\`\`\`

## 로그인하지 않는 사용자

오래 접속하지 않은 계정은 옛 형식으로 남는다. 일정 기간이 지나면 다음 로그인 시 비밀번호 재설정을 요구하는 방식으로 정리한다.

| 기간 | 처리 |
| --- | --- |
| 6개월 | 로그인 시 자동 전환 |
| 12개월 | 다음 로그인에서 재설정 요구 |
| 24개월 | 계정 휴면 처리 |

즉시 전원 재설정을 요구하면 문의가 몰리고 이탈이 생긴다. 단계를 두는 편이 낫다.

![전환 진행과 잔여 계정 처리](/img/posts/password-storage-migration-2.svg)

## 이중 해시라는 방법

당장 전면 전환이 급하면, 기존 해시 값을 새 방식으로 한 번 더 감싸는 방법이 있다. 평문 없이도 즉시 적용할 수 있다. 다만 형식이 복잡해지므로 이후 정리 계획을 함께 세운다.

## 매개변수 선택

| 항목 | 기준 |
| --- | --- |
| 검증 소요 시간 | 서버에서 0.2~0.5초 |
| 메모리 사용 | 하드웨어 병렬 공격 억제 |
| 동시 로그인 부하 | 최대 동시 요청 기준 |

시간만 늘리면 로그인 폭주 시 서버가 버티지 못한다. 실제 최대 동시 로그인 수로 부하를 계산해 정한다.

## 참고

- OWASP — Password Storage Cheat Sheet
- NIST SP 800-63B — 기억된 비밀 요구사항
- Argon2 RFC 9106`,
    diagram: {
      type: 'flow',
      caption: '점진 전환',
      steps: [
        { label: '로그인 요청', note: '평문 수신' },
        { label: '옛 방식으로 검증', note: '형식 확인 후' },
        { label: '새 방식으로 재해시', note: '이 시점에만 가능' },
        { label: '저장 값 교체', note: '다음부터 새 방식' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '전환 진행 예시',
      unit: '%',
      items: [
        { label: '1개월', value: 45, note: '활성 사용자' },
        { label: '3개월', value: 78, note: '대부분 전환' },
        { label: '6개월', value: 92, note: '잔여는 휴면' },
        { label: '재설정 요구', value: 100, note: '남은 계정' },
      ],
    },
  },
  {
    slug: 'field-level-encryption',
    title: '필드 단위 암호화를 도입할 때의 대가',
    body: `테이블 전체가 아니라 특정 컬럼만 암호화하면 민감 정보를 좁게 보호할 수 있다. 대신 그 컬럼으로 검색하거나 정렬하는 기능이 사라진다. 무엇을 잃는지 먼저 확인해야 한다.

## 무엇을 얻고 무엇을 잃는가?

| 항목 | 변화 |
| --- | --- |
| 데이터베이스 관리자 열람 | 차단 |
| 백업 유출 시 | 보호 |
| 정확 일치 검색 | 결정적 암호화로 가능 |
| 범위 검색 | 불가 |
| 정렬 | 불가 |
| 부분 일치 | 불가 |
| 집계 | 제한적 |

검색이 필요한 컬럼을 암호화하면 기능이 깨진다. 설계 전에 그 컬럼의 사용 방식을 조사한다.

![암호화 대상 선정 기준](/img/posts/field-level-encryption.svg)

## 검색을 유지하는 방법

| 방법 | 가능한 것 | 한계 |
| --- | --- | --- |
| 결정적 암호화 | 정확 일치 | 같은 값이 같은 암호문 |
| 검색용 해시 컬럼 | 정확 일치 | 별도 컬럼 필요 |
| 부분 색인 | 접두사 검색 | 정보 일부 노출 |
| 애플리케이션 필터 | 전부 | 전체 복호화 필요 |

검색용 해시 컬럼이 실무에서 가장 많이 쓰인다. 원본은 안전하게 암호화하고, 소금을 섞은 해시를 별도 컬럼에 두어 일치 검색에 쓴다.

## 무엇을 암호화하나

| 데이터 | 판단 |
| --- | --- |
| 주민등록번호 | 필수 |
| 계좌·카드 번호 | 필수 |
| 연락처 | 권장 |
| 주소 | 권장 |
| 이름 | 상황에 따라 |
| 식별자 | 불필요 |

법령이 암호화를 요구하는 항목이 있다. 국내에서는 고유식별정보와 계좌 정보가 해당한다.

![키와 데이터의 분리 보관](/img/posts/field-level-encryption-2.svg)

## 키를 어디에 두나

애플리케이션이 키를 갖고 데이터베이스가 암호문만 가지면, 데이터베이스 유출만으로는 복호화되지 않는다. 키와 데이터가 같은 곳에 있으면 암호화의 의미가 절반으로 준다.

| 구성 | 보호 수준 |
| --- | --- |
| 애플리케이션 키 + 외부 키 서비스 | 높음 |
| 애플리케이션 설정 파일 키 | 중간 |
| 데이터베이스 내 키 | 낮음 |

## 성능 영향

\`\`\`bash
# 암호화 컬럼 조회의 응답 시간 변화를 미리 측정한다
psql -c "\\timing on" -c "SELECT count(*) FROM users WHERE phone_hash = '...'"
\`\`\`

복호화는 대량 조회에서 누적된다. 목록 화면에서 수백 건을 복호화한다면 그 비용을 미리 재 본다.

필드 단위 암호화는 도입보다 되돌리기가 어렵다. 이미 암호화된 컬럼으로 검색 기능을 새로 요구받으면 방법이 마땅치 않다. 그래서 설계 시점에 그 컬럼이 앞으로 어떻게 쓰일지 기획과 함께 확인하는 절차를 둔다. 개인정보 항목은 대체로 검색보다 조회에 쓰이므로 대부분 문제가 없지만, 확인하지 않고 진행하면 나중에 우회 구조를 만들게 된다.

## 참고

- 개인정보의 안전성 확보조치 기준 — 암호화 대상
- OWASP — Cryptographic Storage Cheat Sheet
- NIST SP 800-38D — 인증 암호화 모드`,
    diagram: {
      type: 'matrix',
      caption: '암호화 대상 판단',
      x: ['검색 필요', '불필요'],
      y: ['민감도 높음', '낮음'],
      cells: ['해시 컬럼 병행', '암호화', '색인 유지', '평문 유지'],
    },
    diagram2: {
      type: 'layers',
      caption: '키와 데이터 분리',
      layers: [
        { label: '외부 키 서비스', note: '마스터 키' },
        { label: '애플리케이션', note: '데이터 키 사용' },
        { label: '데이터베이스', note: '암호문만' },
        { label: '백업', note: '암호문만' },
      ],
    },
  },
  {
    slug: 'tls-configuration-audit',
    title: 'TLS 설정을 정기적으로 점검하는 항목',
    body: `한 번 제대로 설정한 통신 암호화도 시간이 지나면 낡는다. 새 취약점이 나오고 권장 설정이 바뀌기 때문이다. 설정한 뒤 몇 년간 손대지 않은 서버가 가장 흔한 문제다.

## 무엇을 점검하나?

| 항목 | 기준 |
| --- | --- |
| 프로토콜 버전 | 1.2 이상, 1.3 권장 |
| 암호 모음 | 전방향 비밀성 지원 |
| 인증서 서명 | 안전한 해시 |
| 키 길이 | 충분한 강도 |
| 인증서 체인 | 완전한 체인 제공 |
| 만료 | 갱신 여유 |
| 재협상 | 안전한 재협상만 |
| 압축 | 비활성화 |

인증서 체인이 불완전한 경우가 의외로 많다. 브라우저는 보완하지만 일부 클라이언트는 실패한다.

![점검 항목과 우선순위](/img/posts/tls-configuration-audit.svg)

## 자동 점검

\`\`\`bash
# 지원 프로토콜과 인증서 확인
for v in tls1 tls1_1 tls1_2 tls1_3; do
  printf '%-8s ' "$v"
  echo | openssl s_client -"$v" -connect example.com:443 2>/dev/null \
    | grep -q 'Cipher is' && echo 지원 || echo 미지원
done

# 체인 완전성
echo | openssl s_client -connect example.com:443 -showcerts 2>/dev/null \
  | grep -c 'BEGIN CERTIFICATE'
\`\`\`

## 내부 통신도 대상이다

외부 공개 서비스만 점검하고 내부 통신을 빼놓는 경우가 많다. 내부망이라도 침해 후 도청 경로가 되므로 같은 기준을 적용한다.

| 구간 | 점검 |
| --- | --- |
| 인터넷 → 로드밸런서 | 필수 |
| 로드밸런서 → 서버 | 필수 |
| 서버 → 데이터베이스 | 필수 |
| 서비스 간 | 권장 |
| 관리 접속 | 필수 |

![구간별 암호화 적용 현황](/img/posts/tls-configuration-audit-2.svg)

## 만료 관리

인증서 만료로 인한 장애는 예방 가능한 사고다. 자동 갱신을 걸고, 갱신 실패를 감지하는 감시를 별도로 둔다. 자동 갱신만 믿으면 실패했을 때 아무도 모른다.

| 감시 | 주기 |
| --- | --- |
| 만료 30일 전 | 경고 |
| 만료 7일 전 | 긴급 |
| 갱신 실패 | 즉시 |
| 외부에서 실제 확인 | 매일 |

## 점검 주기

| 대상 | 주기 |
| --- | --- |
| 공개 서비스 | 월 1회 자동 |
| 내부 서비스 | 분기 |
| 새 서비스 | 배포 전 |
| 권장 설정 변경 시 | 즉시 검토 |

점검을 사람이 하면 빠뜨린다. 외부에서 정기적으로 접속해 설정을 확인하는 작업을 자동화하고, 기준에서 벗어나면 알림을 받는다. 새로 만들어진 서비스가 자동으로 목록에 들어오게 하는 것도 중요하다. 목록에 없는 서버가 가장 오래된 설정을 쓰고 있는 경우가 많다.

내부 인증 기관을 쓰는 구간은 만료 관리가 더 허술한 경향이 있다. 공개 인증서와 달리 외부 감시 도구가 잡아 주지 않으므로 자체 목록으로 관리한다.

## 참고

- Mozilla — SSL Configuration Generator
- NIST SP 800-52 — TLS 구현 지침
- RFC 8446 — TLS 1.3`,
    diagram: {
      type: 'bars',
      caption: '점검 항목 우선순위',
      unit: '점',
      items: [
        { label: '만료 관리', value: 95, note: '장애 직결' },
        { label: '프로토콜 버전', value: 85, note: '옛 버전 제거' },
        { label: '암호 모음', value: 75, note: '전방향 비밀성' },
        { label: '체인 완전성', value: 60, note: '일부 클라이언트' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '암호화 적용 구간',
      layers: [
        { label: '인터넷 구간', note: '필수' },
        { label: '로드밸런서 뒤', note: '필수 — 자주 누락' },
        { label: '데이터베이스 연결', note: '필수' },
        { label: '서비스 간 통신', note: '권장' },
      ],
    },
  },
  {
    slug: 'random-number-misuse',
    title: '난수를 잘못 쓰면 생기는 문제',
    body: `토큰, 세션 식별자, 초기화 벡터는 예측할 수 없어야 한다. 일반 난수 함수는 속도를 위해 예측 가능한 알고리즘을 쓰므로, 몇 개의 출력만 보면 다음 값을 계산할 수 있다.

## 어디에 어떤 난수를 쓰나?

| 용도 | 필요한 난수 |
| --- | --- |
| 세션 식별자 | 암호학적 |
| 비밀번호 재설정 토큰 | 암호학적 |
| 초기화 벡터·난스 | 암호학적 |
| 소금 | 암호학적 |
| 초대 코드 | 암호학적 |
| 화면 애니메이션 | 일반 |
| 시험 데이터 | 일반 |

보안과 관련된 값은 모두 암호학적 난수를 쓴다. 판단이 애매하면 암호학적 쪽을 고른다.

![예측 가능한 값이 만드는 문제](/img/posts/random-number-misuse.svg)

## 흔한 실수

| 실수 | 결과 |
| --- | --- |
| 시각 기반 값 | 좁은 범위 추측 |
| 순차 번호 | 완전 예측 |
| 일반 난수 함수 | 상태 복원 후 예측 |
| 길이 부족 | 무차별 시도 |
| 난스 재사용 | 암호문 분석 가능 |

난스 재사용이 특히 위험하다. 일부 암호 모드에서는 같은 난스를 두 번 쓰면 평문을 복원할 수 있다.

## 올바른 사용

\`\`\`javascript
import { randomBytes, randomUUID } from 'node:crypto'

const token = randomBytes(32).toString('base64url')   // 토큰
const id = randomUUID()                                // 식별자
// Math.random() 은 이런 용도로 쓰지 않는다
\`\`\`

## 길이는 얼마나

| 용도 | 최소 길이 |
| --- | --- |
| 세션 식별자 | 16바이트 |
| 재설정 토큰 | 32바이트 |
| API 키 | 32바이트 |
| 초대 코드 | 사용성 고려, 만료 필수 |

사람이 입력해야 하는 코드는 짧게 만들 수밖에 없다. 이 경우 만료 시간을 짧게 하고 시도 횟수를 제한해 보완한다.

![길이와 만료의 교환](/img/posts/random-number-misuse-2.svg)

## 컨테이너 환경의 주의

컨테이너나 가상 머신이 부팅 직후에 엔트로피가 부족한 경우가 있다. 시스템 난수 장치가 준비되기 전에 키를 만들면 예측 가능한 값이 나올 수 있다. 최신 커널에서는 대부분 해결됐지만, 임베디드 환경에서는 여전히 확인이 필요하다.

## 점검

\`\`\`bash
# 보안 용도로 일반 난수를 쓰는 곳 찾기
grep -rnE 'Math\.random|rand\(\)|mt_rand' src/ \
  | grep -iE 'token|session|key|nonce|salt|code|secret'
\`\`\`

난수 문제는 코드 리뷰에서 잡기 쉬운 편이다. 보안 용도로 일반 난수 함수를 쓰는 패턴은 정적 분석 규칙으로도 걸린다. 규칙을 파이프라인에 넣어 두면 새로 들어오는 코드에서는 반복되지 않는다.

이미 발급된 값이 문제라면 교체가 필요하다. 예측 가능한 방식으로 만든 토큰은 그대로 두면 계속 위험하므로, 전부 무효화하고 다시 발급하는 편이 안전하다.

## 참고

- NIST SP 800-90A — 난수 생성기 권고
- OWASP — Cryptographic Storage Cheat Sheet
- CWE-338 — Use of Cryptographically Weak PRNG`,
    diagram: {
      type: 'flow',
      caption: '예측 공격',
      steps: [
        { label: '출력 몇 개 수집', note: '공개된 토큰' },
        { label: '생성기 상태 복원', note: '알고리즘 알려짐' },
        { label: '다음 값 계산', note: '예측' },
        { label: '타인 토큰 사용', note: '계정 탈취', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '길이와 만료',
      x: ['긴 값', '짧은 값'],
      y: ['짧은 만료', '긴 만료'],
      cells: ['안전', '허용 가능', '허용 가능', '무차별 시도 위험'],
    },
  },
  {
    slug: 'data-retention-automation',
    title: '보유 기간이 지난 데이터를 자동으로 지우기',
    body: `보유 기간을 정책 문서에 적어 두는 것과 실제로 지우는 것은 다른 일이다. 손으로 지우는 방식은 유지되지 않고, 몇 년 뒤 감사에서 정책과 현실이 다르다는 지적을 받는다.

## 왜 자동화가 필요한가?

| 이유 | 설명 |
| --- | --- |
| 규정 준수 | 보유 기간 초과 자체가 위반 |
| 유출 범위 축소 | 없는 데이터는 새지 않는다 |
| 저장 비용 | 계속 증가 |
| 성능 | 오래된 데이터가 조회를 느리게 |
| 인적 실수 | 손으로 하면 빠뜨린다 |

보유 기간 초과 보관은 그 자체로 규정 위반이다. 지우지 않은 것이 문제가 된다.

![데이터 유형별 보유 기간 설계](/img/posts/data-retention-automation.svg)

## 유형별 기간 정하기

| 데이터 | 근거 | 기간 |
| --- | --- | --- |
| 회원 정보 | 탈퇴 시 파기 | 즉시 또는 법정 기간 |
| 거래 기록 | 상법·전자상거래법 | 5년 |
| 접속 기록 | 개인정보 관련 규정 | 1년 이상 |
| 마케팅 동의 이력 | 동의 철회 시 | 철회 시 파기 |
| 로그 | 내부 기준 | 3~12개월 |
| 백업 | 복구 목적 | 정해진 세대 |

법정 보관 의무와 파기 의무가 충돌하는 경우가 있다. 이때는 분리 보관하고 접근을 제한한다.

## 구현 방식

| 방식 | 적용 |
| --- | --- |
| 데이터베이스 파티션 삭제 | 대량 로그 |
| 배치 삭제 | 일반 테이블 |
| 저장소 수명 주기 정책 | 객체 스토리지 |
| 색인 수명 주기 | 검색 색인 |
| 키 폐기 | 암호화된 데이터 |

파티션 단위 삭제가 가장 빠르다. 시간 기준으로 파티션을 나눠 두면 통째로 떼어 내면 된다.

![삭제 방식별 부담](/img/posts/data-retention-automation-2.svg)

## 백업까지 고려한다

운영 데이터를 지워도 백업에 남아 있으면 완전한 파기가 아니다. 백업 세대가 순환되기를 기다리거나, 백업에서도 지우는 절차가 필요하다. 실무에서는 백업 보존 기간을 짧게 유지하고 그 기간을 정책에 명시하는 방식을 쓴다.

## 삭제 기록

\`\`\`bash
# 삭제 배치의 실행 기록과 건수 — 감사 대응 자료가 된다
psql -Atc "SELECT run_at, target_table, deleted_rows
           FROM retention_log ORDER BY run_at DESC LIMIT 20"
\`\`\`

무엇을 언제 얼마나 지웠는지 기록한다. 감사에서 정책이 실제로 작동함을 보이는 근거가 된다.

삭제 작업은 되돌릴 수 없으므로 처음에는 대상만 뽑아 보고 실제 삭제는 하지 않는 모드로 돌린다. 며칠간 결과를 확인하고 예상과 맞으면 실제 삭제를 켠다. 잘못된 조건으로 필요한 데이터를 지우는 사고가 실제로 일어난다.

삭제량이 갑자기 늘면 조건이 잘못됐을 가능성이 있다. 일별 삭제 건수에 상한을 두고 초과하면 멈추게 하는 안전장치를 함께 넣는다.

## 참고

- 개인정보 보호법 — 보유 기간과 파기 의무
- 개인정보의 안전성 확보조치 기준
- ISO/IEC 27001 — 정보 보존과 폐기 통제`,
    diagram: {
      type: 'steps',
      caption: '보유 기간 관리',
      steps: [
        { label: '유형 분류', note: '법적 근거 확인' },
        { label: '기간 결정', note: '문서화' },
        { label: '자동 삭제 구현', note: '배치·수명 주기' },
        { label: '기록과 점검', note: '감사 대응' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '삭제 방식별 부담',
      unit: '점',
      items: [
        { label: '파티션 삭제', value: 95, note: '가장 빠름' },
        { label: '수명 주기 정책', value: 90, note: '객체 스토리지' },
        { label: '키 폐기', value: 85, note: '암호화 전제' },
        { label: '행 단위 삭제', value: 40, note: '부하 큼' },
      ],
    },
  },
  {
    slug: 'pseudonymization-practice',
    title: '가명처리를 실제 데이터에 적용하는 절차',
    body: `분석에 쓰기 위해 개인정보를 가명처리할 때, 어디까지 지워야 하는지가 늘 문제가 된다. 이름과 연락처만 지워서는 다른 항목의 조합으로 다시 알아볼 수 있는 경우가 많다.

## 무엇이 식별자인가?

| 유형 | 예 |
| --- | --- |
| 직접 식별자 | 이름, 주민등록번호, 연락처 |
| 준식별자 | 생년월일, 성별, 우편번호, 직업 |
| 민감 항목 | 질병, 신념, 소득 |
| 간접 단서 | 접속 시각 패턴, 위치 이력 |

준식별자 조합으로 재식별되는 경우가 실제 위험이다. 생년월일과 성별, 우편번호 세 가지만으로 상당수가 특정된다는 연구가 알려져 있다.

![직접 식별자와 준식별자 처리](/img/posts/pseudonymization-practice.svg)

## 처리 방법

| 방법 | 적용 |
| --- | --- |
| 삭제 | 분석에 불필요한 항목 |
| 대체 | 일관된 가명 식별자 |
| 범주화 | 나이를 구간으로 |
| 절단 | 우편번호 앞자리만 |
| 잡음 추가 | 수치 항목 |
| 상한·하한 | 극단값 처리 |

범주화가 가장 널리 쓰인다. 정확한 값 대신 구간을 쓰면 분석 가치를 많이 잃지 않으면서 식별 가능성이 크게 준다.

## 처리 수준 결정

\`\`\`
분석 목적 확인 → 필요한 항목만 선별 → 준식별자 식별
→ 처리 방법 결정 → 재식별 위험 평가 → 적용 → 기록
\`\`\`

목적에 필요 없는 항목은 처리가 아니라 삭제한다. 남겨 두면 위험만 남는다.

![처리 강도와 분석 가치의 균형](/img/posts/pseudonymization-practice-2.svg)

## 재식별 위험 평가

| 확인 | 방법 |
| --- | --- |
| 고유 조합 | 준식별자 조합별 인원 수 |
| 최소 집단 크기 | 일정 수 이상인지 |
| 외부 데이터 결합 | 공개 데이터와 대조 가능성 |
| 특이값 | 혼자만 해당하는 값 |

\`\`\`sql
-- 준식별자 조합으로 혼자인 행 — 재식별 위험이 높다
SELECT birth_year, gender, zip3, count(*) AS n
FROM pseudo_data GROUP BY 1,2,3 HAVING count(*) < 5;
\`\`\`

## 운영에서 지킬 것

가명처리된 데이터도 개인정보다. 추가 정보와 결합하면 다시 알아볼 수 있으므로, 결합 키를 별도로 분리 보관하고 접근을 통제한다. 처리 과정과 결과를 기록하고, 목적이 끝나면 파기한다.

가명처리는 한 번 하고 끝나는 작업이 아니다. 원본 데이터가 계속 쌓이면 같은 처리를 반복해야 하므로 절차를 코드로 만들어 둔다. 손으로 하면 처리 기준이 매번 조금씩 달라지고, 나중에 무엇을 어떻게 처리했는지 설명할 수 없게 된다.

처리 결과를 받는 분석 조직에도 제약이 있다. 재식별을 시도하지 않는다는 약속과 외부 반출 금지를 문서로 남긴다.

## 참고

- 개인정보 보호법 — 가명정보 처리 특례
- 개인정보보호위원회 — 가명정보 처리 가이드라인
- ISO/IEC 20889 — 비식별화 기법`,
    diagram: {
      type: 'layers',
      caption: '처리 대상 구분',
      layers: [
        { label: '직접 식별자', note: '삭제 또는 대체' },
        { label: '준식별자', note: '범주화·절단' },
        { label: '민감 항목', note: '필요 여부 재검토' },
        { label: '분석 항목', note: '가능한 보존' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '처리 강도',
      x: ['분석 가치 높음', '낮음'],
      y: ['재식별 위험 높음', '낮음'],
      cells: ['범주화·잡음', '삭제', '그대로 유지', '삭제'],
    },
  },
  {
    slug: 'secure-deletion',
    title: '데이터를 지웠다고 말할 수 있는 조건',
    body: `삭제 명령을 실행했다고 데이터가 사라지는 것은 아니다. 데이터베이스의 삭제 표시, 파일 시스템의 링크 해제, 저장 장치의 물리적 기록은 각각 다른 층위의 이야기다.

## 어디까지 지워야 지운 것인가?

| 계층 | 상태 |
| --- | --- |
| 애플리케이션 | 화면에서 안 보임 |
| 데이터베이스 행 | 삭제 표시, 공간 재사용 전까지 잔존 |
| 파일 시스템 | 링크 해제, 블록 잔존 |
| 저장 장치 | 실제 기록 잔존 |
| 백업 | 그대로 남음 |
| 로그·캐시 | 사본 잔존 |

로그와 캐시가 가장 자주 빠진다. 삭제 대상 데이터가 로그에 남아 있으면 지운 것이 아니다.

![삭제의 여러 층위](/img/posts/secure-deletion.svg)

## 유형별 방법

| 대상 | 방법 |
| --- | --- |
| 데이터베이스 행 | 삭제 후 공간 회수 |
| 파일 | 덮어쓰기 또는 암호 삭제 |
| SSD | 장치 제공 삭제 명령 |
| 클라우드 저장소 | 사업자 삭제 절차 |
| 암호화된 데이터 | 키 폐기 |
| 물리 매체 폐기 | 파쇄·소자 |

SSD 는 덮어쓰기가 확실하지 않다. 내부적으로 다른 블록에 기록하기 때문이다. 장치가 제공하는 삭제 명령이나 암호 삭제를 쓴다.

## 암호 삭제가 실용적이다

처음부터 암호화해 두면, 키를 폐기하는 것으로 접근을 차단할 수 있다. 대용량 데이터에서 물리적 삭제보다 훨씬 빠르고 확실하다.

| 조건 | 필요 |
| --- | --- |
| 처음부터 암호화 | 필수 |
| 키가 분리 보관 | 필수 |
| 키 사본 관리 | 백업에도 없어야 |
| 알고리즘 강도 | 충분해야 |

![암호 삭제의 전제 조건](/img/posts/secure-deletion-2.svg)

## 사본을 추적한다

지워야 할 데이터가 어디에 사본으로 존재하는지 목록이 있어야 한다. 없으면 삭제가 완전한지 확인할 수 없다.

| 사본 위치 | 확인 |
| --- | --- |
| 복제본 데이터베이스 | 전파 확인 |
| 검색 색인 | 별도 삭제 |
| 캐시 | 무효화 |
| 로그 | 보존 기간 |
| 백업 | 세대 순환 |
| 분석 시스템 | 별도 요청 |

## 삭제 요청 처리

\`\`\`bash
# 삭제 요청 처리 현황 — 각 사본까지 완료됐는지
psql -Atc "SELECT request_id, target, sink, done_at FROM deletion_tasks
           WHERE done_at IS NULL ORDER BY created_at"
\`\`\`

요청 하나가 여러 시스템의 작업으로 나뉜다. 전부 완료돼야 처리 완료다.

삭제 요청이 들어왔을 때 어디를 지워야 하는지 그 자리에서 찾는 방식은 반드시 빠뜨린다. 데이터가 흘러가는 경로를 미리 그려 두고, 새 시스템이 추가될 때 그 그림을 갱신한다. 이 그림이 삭제뿐 아니라 유출 조사에도 그대로 쓰인다.

외부 위탁 업체에 넘긴 데이터도 대상이다. 계약에 삭제 요청 전달과 증빙 제출 조항이 있어야 실제로 처리된다.

## 참고

- NIST SP 800-88 — 매체 소거 지침
- 개인정보 보호법 — 파기 의무와 방법
- 개인정보의 안전성 확보조치 기준`,
    diagram: {
      type: 'layers',
      caption: '삭제 층위',
      layers: [
        { label: '애플리케이션', note: '화면에서 제거' },
        { label: '데이터 저장소', note: '행·객체 삭제' },
        { label: '사본', note: '복제본·색인·캐시' },
        { label: '백업·매체', note: '세대 순환·소거' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '암호 삭제',
      steps: [
        { label: '저장 시 암호화', note: '데이터 키 사용' },
        { label: '키 분리 보관', note: '외부 서비스' },
        { label: '삭제 요청', note: '키 폐기' },
        { label: '복호화 불가', note: '사본 포함 전체' },
      ],
    },
  },
  {
    slug: 'backup-encryption-keys',
    title: '백업 암호화 키를 어디에 보관할 것인가',
    body: `백업을 암호화하면 유출 시 안전하지만, 복구해야 할 때 키가 없으면 백업도 없는 것과 같다. 재해 상황에서 키에 접근할 수 있는가가 백업 설계의 핵심 질문이다.

## 왜 어려운 문제인가?

| 상황 | 딜레마 |
| --- | --- |
| 키를 운영 환경에 둠 | 운영이 무너지면 접근 불가 |
| 키를 백업과 함께 둠 | 암호화 의미 상실 |
| 키를 한 사람이 보관 | 부재 시 복구 불가 |
| 키를 여러 곳에 둠 | 유출 가능성 증가 |

재해 복구 상황을 가정하고 설계해야 한다. 평시에 편한 방식이 재해 시에 작동하지 않는 경우가 많다.

![평시와 재해 시의 키 접근 경로](/img/posts/backup-encryption-keys.svg)

## 보관 방식

| 방식 | 재해 내성 | 보안 |
| --- | --- | --- |
| 별도 지역 키 서비스 | 높음 | 높음 |
| 키 분할 보관 | 높음 | 높음 |
| 하드웨어 토큰 다중 보관 | 중간 | 높음 |
| 봉인된 문서 금고 | 중간 | 중간 |
| 운영 환경 키 서비스 | 낮음 | 높음 |

키 분할은 여러 사람이 조각을 나눠 갖고, 일정 수 이상이 모여야 복원되는 방식이다. 한 사람의 부재나 배신에 견딘다.

## 복구 훈련이 검증이다

문서상 완벽해도 실제로 해 보지 않으면 작동을 보장할 수 없다.

| 훈련 항목 | 확인 |
| --- | --- |
| 키 접근 | 담당자가 실제로 얻을 수 있나 |
| 복호화 | 백업이 실제로 열리나 |
| 소요 시간 | 목표 시간 내인가 |
| 대체 담당 | 주 담당 부재 시 |
| 절차 문서 | 실제와 맞나 |

![복구 훈련에서 발견되는 문제](/img/posts/backup-encryption-keys-2.svg)

## 훈련에서 자주 나오는 문제

| 문제 | 빈도 |
| --- | --- |
| 담당자가 접근 권한 없음 | 높음 |
| 절차 문서가 낡음 | 높음 |
| 키 토큰 배터리·분실 | 중간 |
| 복호화 도구 버전 불일치 | 중간 |
| 소요 시간 초과 | 중간 |

권한 문제가 가장 흔하다. 조직 개편이나 이동 후 복구 권한이 정리되지 않은 상태로 남는다.

## 기록할 것

\`\`\`
키 식별자     백업 세트와의 대응
보관 위치     물리·논리 위치와 접근 방법
접근 권한자   이름과 대체자
교체 이력     언제 왜 바꿨나
훈련 이력     날짜와 결과
\`\`\`

이 문서 자체도 안전하게 보관하되, 재해 시 접근 가능해야 한다.

키 관리 부담이 크다고 백업 암호화를 포기하는 것은 잘못된 선택이다. 백업 매체는 이동하고 보관되고 폐기되는 과정에서 노출 기회가 많다. 대신 관리 부담을 줄이는 쪽으로 설계한다. 키 개수를 최소화하고, 자동화할 수 있는 부분은 자동화하고, 사람이 개입하는 지점은 절차로 고정한다.

복구 훈련은 반기에 한 번이면 충분하다. 자주 하기 어려우면 범위를 줄여서라도 정기적으로 한다.

## 참고

- NIST SP 800-57 — 키 관리 권고
- ISO/IEC 27031 — 사업 연속성을 위한 정보통신 준비
- 각 백업 솔루션의 키 관리 문서`,
    diagram: {
      type: 'matrix',
      caption: '키 보관 위치',
      x: ['재해 시 접근 가능', '불가'],
      y: ['보안 강함', '약함'],
      cells: ['권장', '재검토', '유출 위험', '최악'],
    },
    diagram2: {
      type: 'bars',
      caption: '훈련에서 발견되는 문제',
      unit: '%',
      items: [
        { label: '권한 미비', value: 42, note: '가장 흔함' },
        { label: '문서 불일치', value: 31, note: '절차 낡음' },
        { label: '도구 문제', value: 18, note: '버전·분실' },
        { label: '시간 초과', value: 9, note: '목표 미달' },
      ],
    },
  },
]
