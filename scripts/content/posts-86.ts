import type { SeedPost } from './types'

/** 850~859 — 서버와 런타임 */
export const posts86: SeedPost[] = [
  {
    slug: 'process-privilege-drop',
    title: '서비스 프로세스의 권한 낮추기',
    body: `웹 서버나 응용 프로그램이 관리자 권한으로 돌면, 그 프로세스에서 임의 코드가 실행됐을 때 시스템 전체가 넘어간다. 대부분의 서비스는 낮은 권한으로도 충분히 동작한다.

## 왜 높은 권한으로 도나

| 이유 | 대안 |
| --- | --- |
| 낮은 포트 사용 | 권한 부여·전달 |
| 파일 접근 | 소유권 조정 |
| 설치 편의 | 설정 |
| 컨테이너 기본값 | 사용자 지정 |

낮은 포트를 쓰려고 관리자 권한을 유지하는 경우가 흔하다. 시작할 때만 그 권한을 쓰고 곧바로 낮추는 방식이 표준적이다.

![권한에 따른 피해 범위](/img/posts/process-privilege-drop.svg)

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 실행 사용자 | 전용 계정 |
| 파일 소유권 | 필요한 것만 |
| 쓰기 권한 | 최소 |
| 셸 로그인 | 막기 |

서비스 전용 계정은 로그인할 수 없게 만든다. 그 계정으로 원격 접속이 되면 권한을 낮춘 의미가 줄어든다.

## 컨테이너에서는

| 설정 | 내용 |
| --- | --- |
| 사용자 지정 | 루트 아님 |
| 읽기 전용 | 파일 시스템 |
| 권한 상승 금지 | 설정 |
| 기능 제거 | 필요한 것만 |

이미지 기본값이 관리자인 경우가 많아 명시하지 않으면 그대로 돈다. 빌드 단계에서 사용자를 지정하는 것이 기본이다.

![낮추는 순서](/img/posts/process-privilege-drop-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 어떤 사용자로 돌고 있는지
ps -eo user,pid,comm --sort=user 2>/dev/null | grep -E 'nginx|node|python' | head

# 컨테이너 안에서
id 2>/dev/null
\`\`\`

관리자 계정으로 도는 서비스가 나오면 전환 대상이다. 전환할 때 파일 소유권과 로그 경로 권한을 함께 맞춰야 동작한다.

## 파일 권한도 함께 본다

프로세스 권한을 낮춰도 코드 디렉터리에 쓰기 권한이 있으면 코드를 바꿔 심을 수 있다. 실행할 파일은 읽기 전용으로 두고 쓰기가 필요한 경로만 따로 나눈다. 이 구분이 되어 있으면 침입자가 지속성을 확보하기 훨씬 어려워진다.

## 참고

- CIS Benchmarks — 운영체제 설정
- NIST SP 800-123 — 서버 보안
- NIST SP 800-190 — 컨테이너 보안`,
    diagram: {
      type: 'matrix',
      caption: '권한에 따른 피해 범위',
      x: ['관리자 권한', '전용 계정'],
      y: ['코드 실행 발생', '정상'],
      cells: ['시스템 전체', '그 서비스만', '해당 없음', '해당 없음'],
    },
    diagram2: {
      type: 'steps',
      caption: '낮추는 순서',
      steps: [
        { label: '전용 계정 생성', note: '로그인 금지' },
        { label: '소유권 정리', note: '필요한 것만' },
        { label: '권한 전환', note: '시작 직후' },
        { label: '쓰기 경로 분리', note: '코드는 읽기만' },
      ],
    },
  },
  {
    slug: 'runtime-hardening-flags',
    title: '실행 환경 설정으로 줄이는 위험',
    body: `언어 런타임과 서버 소프트웨어에는 보안에 영향을 주는 설정이 여럿 있고, 기본값이 편의 쪽으로 맞춰진 경우가 많다. 설정 몇 줄로 노출 면적이 줄어드는 항목을 정리해 둘 만하다.

## 무엇을 끄나

| 항목 | 이유 |
| --- | --- |
| 디버그 모드 | 정보 노출 |
| 상세 오류 | 구조 노출 |
| 디렉터리 목록 | 파일 노출 |
| 버전 표시 | 대조 가능 |
| 사용하지 않는 모듈 | 표면 축소 |

버전 표시를 끄는 것만으로 공격이 막히지는 않지만, 알려진 취약점을 대조하는 자동 도구의 표적에서는 빠진다.

![기본 설정의 노출](/img/posts/runtime-hardening-flags.svg)

## 보안 헤더는 무엇을 켜나

| 항목 | 효과 |
| --- | --- |
| 보안 헤더 | 브라우저 통제 |
| 요청 크기 제한 | 자원 보호 |
| 시간 제한 | 연결 정리 |
| 로그 | 추적 |

요청 크기와 시간 제한은 성능 설정처럼 보이지만 자원 고갈 공격을 막는 기본 장치다. 기본값이 넉넉한 경우가 많아 확인이 필요하다.

## 어떻게 관리하나

| 방법 | 내용 |
| --- | --- |
| 설정을 코드로 | 재현 가능 |
| 기준 문서 | 무엇을 왜 |
| 자동 점검 | 배포 시 |
| 기준선 비교 | 어긋남 확인 |

설정을 코드로 관리하면 새 서버에도 같은 값이 적용된다. 손으로 고친 설정은 다음 서버에서 반드시 빠진다.

![설정 관리 흐름](/img/posts/runtime-hardening-flags-2.svg)

## 지금 값을 본다

\`\`\`bash
# 응답 헤더에서 노출 정보 확인
curl -sI https://example.com | grep -iE 'server|x-powered-by'

# 오류 화면이 상세한지
curl -s https://example.com/nonexistent-path-xyz | head -5
\`\`\`

서버 이름과 버전이 그대로 나오면 설정으로 숨길 수 있다. 오류 화면에 스택이 보이면 운영 모드 설정이 빠진 것이다.

## 기준을 만들어 두고 비교한다

권고 설정 목록을 한 번 정리해 두면 새 서비스를 올릴 때마다 판단하지 않아도 된다. 공개된 벤치마크 문서를 기준으로 삼되 우리 환경에 맞지 않는 항목은 이유를 적고 빼면, 그 목록 자체가 검토의 결과물이 된다. 자동 점검에 붙이면 유지도 된다.

## 참고

- CIS Benchmarks — 서버 소프트웨어 설정
- NIST SP 800-123 — 서버 보안
- OWASP — 보안 설정 오류`,
    diagram: {
      type: 'layers',
      caption: '기본 설정의 노출',
      layers: [
        { label: '디버그 모드', note: '정보 전부' },
        { label: '상세 오류', note: '구조' },
        { label: '디렉터리 목록', note: '파일' },
        { label: '버전 표시', note: '대조 가능' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '설정 관리 흐름',
      steps: [
        { label: '기준 정리', note: '무엇을 왜' },
        { label: '코드로 관리', note: '재현' },
        { label: '배포 시 점검', note: '자동' },
        { label: '어긋남 확인', note: '기준선 비교' },
      ],
    },
  },
  {
    slug: 'memory-safety-basics',
    title: '메모리 안전이 문제가 되는 곳',
    body: `메모리를 직접 다루는 언어에서는 경계를 벗어난 접근 하나가 임의 코드 실행으로 이어진다. 웹 서비스에서 직접 쓰지 않더라도 이미지 처리나 압축 같은 라이브러리가 그런 코드로 되어 있다.

## 의존성 어디에 있나

| 위치 | 예 |
| --- | --- |
| 이미지 처리 | 변환 라이브러리 |
| 압축 해제 | 형식 파서 |
| 문서 변환 | 파일 처리 |
| 네트워크 파서 | 프로토콜 |
| 암호 라이브러리 | 저수준 |

사용자가 올린 파일을 파싱하는 코드가 가장 위험하다. 입력을 우리가 통제할 수 없고 형식이 복잡하기 때문이다.

![메모리 문제가 있는 자리](/img/posts/memory-safety-basics.svg)

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 갱신 유지 | 취약점 수정 |
| 격리 실행 | 별도 프로세스 |
| 권한 축소 | 최소 |
| 안전한 언어 | 대체 |
| 입력 제한 | 크기·형식 |

파일 처리를 별도 프로세스나 컨테이너에서 돌리면 문제가 생겨도 그 안에서 끝난다. 처리 시간과 메모리 상한도 함께 건다.

## 무엇을 먼저 보나

| 기준 | 내용 |
| --- | --- |
| 사용자 입력 처리 | 우선 |
| 외부 노출 | 다음 |
| 갱신 상태 | 밀린 것 |
| 대체 가능성 | 있으면 |

직접 쓰는 라이브러리만이 아니라 그것이 안에서 부르는 것까지 봐야 한다. 구성 목록이 있으면 이 확인이 쉬워진다.

![줄이는 방법](/img/posts/memory-safety-basics-2.svg)

## 어떤 것을 쓰는지 본다

\`\`\`bash
# 시스템 라이브러리 버전과 알려진 취약점
dpkg -l 2>/dev/null | grep -E 'libpng|libjpeg|zlib|imagemagick' | head

# 컨테이너 이미지 취약점 검사
trivy image --severity HIGH,CRITICAL myapp:latest 2>/dev/null | head -20
\`\`\`

이미지 처리 도구는 취약점이 자주 나오는 영역이다. 정기적으로 갱신하고 가능하면 처리 범위를 좁힌다.

## 우리가 직접 만들 일은 드물다

대부분의 서비스는 저수준 코드를 직접 쓰지 않으므로, 이 영역의 대응은 사실상 의존성 관리와 격리다. 무엇을 쓰고 있는지 알고, 갱신을 밀리지 않게 하고, 위험한 처리를 격리하는 세 가지면 실무에서 필요한 것의 대부분이 된다.

## 참고

- CWE-119 — 메모리 버퍼 경계 오류
- NIST SP 800-218 — 안전한 개발 관행
- OWASP — 취약한 구성 요소 사용`,
    diagram: {
      type: 'layers',
      caption: '메모리 문제가 있는 자리',
      layers: [
        { label: '파일 파서', note: '사용자 입력' },
        { label: '이미지 처리', note: '복잡한 형식' },
        { label: '압축 해제', note: '중첩' },
        { label: '프로토콜 파서', note: '네트워크' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '줄이는 방법',
      unit: '점',
      items: [
        { label: '격리 실행', value: 90, note: '범위 제한' },
        { label: '갱신 유지', value: 85, note: '알려진 것' },
        { label: '입력 제한', value: 65, note: '크기·형식' },
        { label: '권한 축소', value: 60, note: '피해 축소' },
      ],
    },
  },
  {
    slug: 'timeout-and-limits',
    title: '시간과 크기 제한을 어디에 걸까',
    body: `제한이 없는 지점이 하나만 있어도 그곳이 서비스를 멈추는 통로가 된다. 어디에 어떤 제한이 필요한지 목록으로 정리해 두면 새 기능을 만들 때마다 판단하지 않아도 된다.

## 서비스 거부를 막으려 어디에 거나

| 지점 | 제한 |
| --- | --- |
| 요청 본문 | 크기 |
| 업로드 | 크기·개수 |
| 처리 시간 | 상한 |
| 외부 호출 | 시간 |
| 질의 | 실행 시간 |
| 연결 | 유휴 시간 |

외부 호출에 시간 제한이 없으면 그 호출이 멈출 때 우리 연결이 쌓인다. 가장 자주 빠지는 자리다.

![제한이 필요한 지점](/img/posts/timeout-and-limits.svg)

## 값을 어떻게 정하나

| 기준 | 내용 |
| --- | --- |
| 정상 분포 | 측정값 |
| 상위 백분위 | 여유 |
| 자원 한계 | 용량 |
| 사용자 인내 | 경험 |

측정한 정상 처리 시간의 상위 백분위에 여유를 더해 정한다. 감으로 정하면 너무 짧거나 사실상 없는 값이 된다.

## 넘었을 때 무엇을 하나

| 처리 | 내용 |
| --- | --- |
| 명확한 오류 | 코드·메시지 |
| 기록 | 어느 지점 |
| 정리 | 자원 회수 |
| 경보 | 반복 시 |

제한에 걸린 요청이 늘어나면 공격이거나 성능 문제다. 어느 쪽이든 알아야 하므로 기록과 경보를 함께 둔다.

![제한 값을 정하는 근거](/img/posts/timeout-and-limits-2.svg)

## 빠진 곳을 찾는다

\`\`\`bash
# 시간 제한 없는 외부 호출
grep -rInE 'fetch\\(|axios\\.(get|post)' --include='*.ts' src/ 2>/dev/null \
  | grep -v 'timeout\\|signal' | head

# 서버 요청 크기 제한
grep -rInE 'bodyParser|client_max_body_size|limit:' nginx.conf src/ 2>/dev/null | head
\`\`\`

제한이 없는 호출이 나오면 하나씩 붙인다. 기본값이 무한인 라이브러리가 많으므로 명시하는 습관이 필요하다.

## 층마다 값이 어긋나면 문제가 된다

앞단과 응용 서버의 시간 제한이 다르면 한쪽이 먼저 끊어 오류 원인을 찾기 어려워진다. 바깥쪽을 길게, 안쪽을 짧게 두는 순서로 맞추면 안쪽에서 먼저 정리되고 그 이유가 기록에 남는다. 값을 표로 정리해 두면 이 순서가 유지된다. 값을 바꿀 때도 그 표를 먼저 고치면 어긋남이 생기지 않는다.

## 참고

- OWASP — 자원 소진 방지
- CWE-400 — 통제되지 않은 자원 소비
- Google SRE — 시간 제한 설계`,
    diagram: {
      type: 'layers',
      caption: '제한이 필요한 지점',
      layers: [
        { label: '요청 본문', note: '크기' },
        { label: '처리 시간', note: '상한' },
        { label: '외부 호출', note: '가장 자주 빠짐' },
        { label: '질의 실행', note: '데이터베이스' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '제한 값을 정하는 근거',
      steps: [
        { label: '정상 측정', note: '분포' },
        { label: '상위 백분위', note: '기준' },
        { label: '여유 더하기', note: '오탐 방지' },
        { label: '층별 정렬', note: '바깥이 길게' },
      ],
    },
  },
  {
    slug: 'health-check-exposure',
    title: '상태 점검 경로가 알려 주는 것',
    body: `서비스가 살아 있는지 확인하는 경로는 대개 인증 없이 열려 있다. 그 응답에 버전이나 의존 시스템 상태가 담기면 공격자에게 유용한 정보가 된다.

## 무엇이 담기나

| 항목 | 위험 |
| --- | --- |
| 버전 | 취약점 대조 |
| 의존 상태 | 구조 노출 |
| 연결 문자열 | 심각 |
| 내부 주소 | 구조 |
| 상세 오류 | 원인 노출 |

데이터베이스 연결 상태를 자세히 보여 주려다 호스트 이름이나 사용자 이름까지 담는 경우가 있다.

![점검 응답의 노출](/img/posts/health-check-exposure.svg)

## 엔드포인트를 어떻게 나누나

| 대상 | 내용 |
| --- | --- |
| 외부 점검 | 살아 있음만 |
| 내부 점검 | 상세 |
| 준비 상태 | 트래픽 수용 |
| 관리 경로 | 인증 필요 |

외부에는 상태 코드만 주고 상세는 내부 전용 경로에 두면 두 요구가 모두 해결된다.

## 무엇을 확인해야 하나

| 항목 | 내용 |
| --- | --- |
| 의존 시스템 | 연결 가능 |
| 자원 여유 | 메모리·연결 |
| 최근 오류율 | 임계 |
| 시작 완료 | 준비 |

살아 있음과 준비됨을 구분하지 않으면, 시작 중인 인스턴스에 트래픽이 들어가 오류가 난다. 두 경로를 나누는 것이 기본이다.

![점검 경로 구분](/img/posts/health-check-exposure-2.svg)

## 응답을 확인한다

\`\`\`bash
# 점검 경로의 응답 내용
curl -s https://example.com/health | head -20
curl -s https://example.com/actuator/health 2>/dev/null | head -20
\`\`\`

의존 시스템 이름이나 버전이 보이면 줄인다. 관리용 경로가 인증 없이 열려 있는 경우도 확인한다.

## 관리 경로 전체를 본다

상태 점검 외에도 지표 수집, 설정 조회, 디버그용 경로가 인증 없이 열려 있는 경우가 많다. 프레임워크가 기본으로 제공하는 것들이라 우리가 만들지 않았어도 존재한다. 어떤 경로가 열려 있는지 목록을 뽑아 필요 없는 것은 끄고 필요한 것은 내부에서만 접근되게 하면 이 영역이 정리된다. 프레임워크를 올릴 때 새 경로가 생기지 않았는지도 함께 본다.

## 참고

- OWASP — 정보 노출 방지
- CWE-200 — 민감 정보 노출
- 각 프레임워크 관리 경로 문서`,
    diagram: {
      type: 'layers',
      caption: '점검 응답의 노출',
      layers: [
        { label: '연결 문자열', note: '가장 심각' },
        { label: '내부 주소', note: '구조' },
        { label: '의존 상태', note: '구성' },
        { label: '버전', note: '대조 가능' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '점검 경로 구분',
      x: ['외부', '내부'],
      y: ['살아 있음', '상세 상태'],
      cells: ['상태 코드만', '가능', '숨김', '가능'],
    },
  },
  {
    slug: 'environment-variable-exposure',
    title: '환경 변수와 비밀값이 새는 경로',
    body: `설정과 비밀값을 환경 변수로 주입하는 방식은 널리 쓰이지만, 그 값이 프로세스 정보나 오류 화면, 디버그 경로를 통해 드러나는 경우가 있다.

## 어디서 보이나

| 경로 | 설명 |
| --- | --- |
| 프로세스 목록 | 같은 호스트 |
| 오류 화면 | 전체 출력 |
| 디버그 경로 | 설정 조회 |
| 하위 프로세스 | 상속 |
| 로그 | 시작 시 출력 |

시작할 때 설정을 전부 로그에 찍는 구현이 흔하다. 비밀값도 함께 찍힌다.

![환경 변수가 드러나는 경로](/img/posts/environment-variable-exposure.svg)

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 시크릿 저장소 | 실행 시 조회 |
| 파일 주입 | 권한 제한 |
| 출력 제외 | 이름 기준 |
| 하위 전달 차단 | 필요한 것만 |

비밀값을 파일로 주입하고 권한을 제한하면 프로세스 정보로는 보이지 않는다. 환경 변수보다 한 단계 안전하다.

## 무엇을 가리나

| 이름 패턴 | 처리 |
| --- | --- |
| 비밀·키·토큰 | 가림 |
| 비밀번호 | 가림 |
| 연결 문자열 | 가림 |
| 일반 설정 | 표시 |

이름에 특정 단어가 들어가면 자동으로 가리는 처리를 넣으면 새 값이 추가돼도 적용된다. 목록 방식은 빠진다.

![주입 방식의 안전도](/img/posts/environment-variable-exposure-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 시작 로그에 설정이 찍히는지
grep -iE 'env|config' /var/log/app/startup.log 2>/dev/null | head -5

# 프로세스 환경에서 비밀값 형태 확인
tr '\\0' '\\n' < /proc/self/environ 2>/dev/null | grep -icE 'secret|token|password'
\`\`\`

로그에 값이 보이면 출력 처리를 고친다. 이미 남은 로그의 보관 기간과 접근 권한도 함께 확인해야 한다.

## 값이 바뀔 때를 생각한다

환경 변수는 프로세스를 다시 시작해야 반영된다. 비밀값을 회전할 때마다 재시작이 필요하면 회전을 미루게 되므로, 실행 중에 다시 읽을 수 있는 구조가 회전 주기를 짧게 유지하는 데 도움이 된다. 시크릿 저장소를 쓰면 이 부분이 자연스럽게 해결된다. 회전 주기를 정해 두고 그 주기가 지켜지는지 확인하는 것까지가 관리다. 값이 언제 마지막으로 바뀌었는지 기록해 두면 그 확인이 간단해진다.

## 참고

- OWASP — 비밀값 관리
- NIST SP 800-57 — 키 관리
- 각 시크릿 저장소 문서`,
    diagram: {
      type: 'layers',
      caption: '환경 변수가 드러나는 경로',
      layers: [
        { label: '시작 로그', note: '가장 흔함' },
        { label: '오류 화면', note: '전체 출력' },
        { label: '프로세스 목록', note: '같은 호스트' },
        { label: '하위 프로세스', note: '상속' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '주입 방식의 안전도',
      unit: '점',
      items: [
        { label: '실행 시 조회', value: 95, note: '남지 않음' },
        { label: '파일 주입', value: 75, note: '권한 제한' },
        { label: '환경 변수', value: 45, note: '목록 노출' },
        { label: '코드에 포함', value: 5, note: '최악' },
      ],
    },
  },
  {
    slug: 'container-image-hygiene',
    title: '컨테이너 이미지를 가볍게 만들기',
    body: `이미지에 들어 있는 것이 많을수록 취약점도 많아지고 침입자가 쓸 도구도 많아진다. 필요한 것만 담는 것이 보안과 운영 양쪽에 도움이 된다.

## 무엇이 들어 있나

| 항목 | 필요성 |
| --- | --- |
| 실행 파일 | 필요 |
| 빌드 도구 | 불필요 |
| 셸 | 대개 불필요 |
| 패키지 관리자 | 불필요 |
| 디버그 도구 | 불필요 |

셸과 패키지 관리자가 없으면 침입자가 명령을 실행하거나 도구를 설치하기 어려워진다. 다만 문제가 생겼을 때 들여다보기도 어려워지므로 균형이 필요하다.

![이미지 구성과 표면](/img/posts/container-image-hygiene.svg)

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 다단계 빌드 | 결과물만 |
| 최소 기반 이미지 | 작은 것 |
| 불필요 제거 | 캐시·문서 |
| 계층 정리 | 중간 파일 |

빌드 단계와 실행 단계를 나누면 컴파일러와 개발 패키지가 최종 이미지에 남지 않는다. 크기와 취약점이 함께 줄어든다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 취약점 수 | 스캔 |
| 크기 | 추세 |
| 기반 이미지 나이 | 갱신 |
| 포함된 비밀값 | 검사 |

빌드 중에 쓴 비밀값이 중간 계층에 남는 경우가 있다. 최종 이미지에서 지워도 이전 계층에 남아 있으면 꺼낼 수 있다.

![줄이는 방법](/img/posts/container-image-hygiene-2.svg)

## 지금 이미지를 본다

\`\`\`bash
# 취약점과 크기
trivy image --severity HIGH,CRITICAL myapp:latest 2>/dev/null | tail -5
docker images myapp:latest --format '{{.Size}}'

# 계층에 비밀값이 남아 있는지
docker history --no-trunc myapp:latest 2>/dev/null | grep -icE 'secret|token|password'
\`\`\`

기반 이미지를 바꾸는 것만으로 취약점 수가 크게 줄어드는 경우가 많다. 크기 추세를 함께 보면 언제부터 늘었는지 알 수 있다.

## 갱신 주기를 정한다

기반 이미지를 고정해 두고 갱신하지 않으면 시간이 지날수록 취약점이 쌓인다. 정기적으로 다시 빌드하는 작업을 자동화하고, 기반 이미지에 보안 수정이 올라오면 자동으로 다시 빌드되게 하면 이 부분이 유지된다. 다시 빌드가 쉬운 구조를 만드는 것이 먼저다.

## 참고

- NIST SP 800-190 — 컨테이너 보안
- CIS Docker Benchmark
- OWASP — 컨테이너 보안 지침`,
    diagram: {
      type: 'bars',
      caption: '이미지 구성과 표면',
      unit: '점',
      items: [
        { label: '전체 배포판', value: 95, note: '표면 큼' },
        { label: '경량 배포판', value: 55, note: '보통' },
        { label: '최소 이미지', value: 25, note: '셸 없음' },
        { label: '정적 바이너리', value: 10, note: '가장 작음' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '줄이는 방법',
      steps: [
        { label: '다단계 빌드', note: '결과물만' },
        { label: '최소 기반', note: '작은 이미지' },
        { label: '중간 파일 정리', note: '캐시' },
        { label: '정기 재빌드', note: '갱신' },
      ],
    },
  },
  {
    slug: 'kernel-and-host-hardening',
    title: '커널과 호스트 설정에서 줄이는 것',
    body: `컨테이너를 돌리는 호스트나 가상 서버 자체의 설정도 통제 대상이다. 애플리케이션만 보다 보면 그 아래층이 기본값 그대로 남아 있는 경우가 많다.

## 방화벽과 서비스를 무엇으로 보나

| 항목 | 내용 |
| --- | --- |
| 불필요한 서비스 | 끄기 |
| 열린 포트 | 최소 |
| 커널 설정 | 권고값 |
| 자동 갱신 | 보안 패치 |
| 로그인 방식 | 키 기반 |

설치 시 기본으로 켜지는 서비스가 여럿 있다. 쓰지 않는 것을 끄면 표면이 줄고 패치할 대상도 줄어든다.

![호스트 설정 층](/img/posts/kernel-and-host-hardening.svg)

## 어떻게 적용하나

| 방법 | 내용 |
| --- | --- |
| 이미지에 반영 | 새 서버 |
| 설정 관리 도구 | 일괄 |
| 기준선 비교 | 어긋남 |
| 신규만 적용 | 점진적 |

새로 만드는 서버부터 적용하고 기존은 교체 시점에 맞추면 무리가 없다. 전부 한꺼번에 바꾸려 하면 진행되지 않는다.

## 무엇이 깨질 수 있나

| 변경 | 영향 |
| --- | --- |
| 서비스 중단 | 의존 확인 |
| 커널 설정 | 성능 |
| 로그인 제한 | 접근 |
| 방화벽 | 연결 |

방화벽 규칙을 조이다 관리 접속까지 막는 사고가 흔하다. 원격 작업 전에 복구 경로를 확보해 둔다.

![적용 순서](/img/posts/kernel-and-host-hardening-2.svg)

## 현재 상태를 본다

\`\`\`bash
# 열린 포트와 도는 서비스
ss -tlnp 2>/dev/null | awk 'NR>1 {print $4, $6}' | head -15

# 자동 보안 갱신 설정
systemctl is-enabled unattended-upgrades 2>/dev/null
\`\`\`

모르는 포트가 열려 있으면 그 프로세스를 확인한다. 자동 갱신이 꺼져 있으면 패치가 사람 손에 달려 있다는 뜻이다.

## 기준을 문서로 남긴다

공개된 벤치마크를 그대로 적용하면 우리 환경에서 깨지는 항목이 있다. 적용한 것과 뺀 것을 이유와 함께 적어 두면 심사에서도 설명할 수 있고, 다음 사람이 같은 판단을 반복하지 않는다. 문서 없이 설정만 남으면 왜 그런지 아무도 모르게 되고 결국 손대지 못한다.

## 참고

- CIS Benchmarks — 운영체제 설정
- NIST SP 800-123 — 서버 보안
- 한국인터넷진흥원 — 서버 보안 설정 안내`,
    diagram: {
      type: 'layers',
      caption: '호스트 설정 층',
      layers: [
        { label: '애플리케이션', note: '주로 보는 곳' },
        { label: '런타임', note: '설정' },
        { label: '운영체제', note: '서비스·포트' },
        { label: '커널', note: '권고값' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '적용 순서',
      steps: [
        { label: '기준 정리', note: '벤치마크' },
        { label: '이미지 반영', note: '새 서버' },
        { label: '점진 교체', note: '기존' },
        { label: '어긋남 점검', note: '정기' },
      ],
    },
  },
  {
    slug: 'scheduled-job-security',
    title: '예약 작업이 남기는 지속성 위험',
    body: `정기적으로 도는 작업은 한 번 만들어 두면 몇 년씩 돌아간다. 그래서 그 작업이 쓰는 계정과 권한, 실패 여부를 아무도 확인하지 않는 상태가 되기 쉽다.

## 무엇이 문제인가

| 항목 | 내용 |
| --- | --- |
| 넓은 권한 | 편의로 부여 |
| 실패 무시 | 조용함 |
| 담당 불명 | 만든 사람 부재 |
| 지속성 경로 | 침입자가 이용 |
| 중복 실행 | 겹침 |

침입자가 지속성을 확보할 때 예약 작업을 쓰는 것이 전형적이다. 정상 작업 사이에 하나가 섞여 있어도 눈에 띄지 않는다.

![예약 작업의 위험](/img/posts/scheduled-job-security.svg)

## 어떻게 관리하나

| 항목 | 내용 |
| --- | --- |
| 목록 관리 | 무엇이 언제 |
| 담당자 지정 | 각 작업 |
| 전용 계정 | 권한 최소 |
| 실패 경보 | 조용하지 않게 |
| 실행 기록 | 결과 |

성공을 알리는 대신 실패를 알리는 구조여야 한다. 성공 알림은 곧 무시되고 실패도 함께 묻힌다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 최근 변경 | 누가 언제 |
| 실행 계정 | 권한 |
| 스크립트 경로 | 쓰기 권한 |
| 실행 시간 | 예상 범위 |

작업이 참조하는 스크립트에 누구나 쓸 수 있으면 그 내용을 바꿔 심을 수 있다. 경로와 권한을 함께 본다.

![관리 항목](/img/posts/scheduled-job-security-2.svg)

## 지금 등록된 것을 본다

\`\`\`bash
# 등록된 예약 작업과 최근 변경
crontab -l 2>/dev/null; ls -la /etc/cron.d/ 2>/dev/null
find /etc/cron* /var/spool/cron -newermt '30 days ago' -type f 2>/dev/null

# 실행 계정과 스크립트 권한
awk '{print $6, $7}' /etc/cron.d/* 2>/dev/null | sort -u | head
\`\`\`

최근에 바뀐 작업이 있으면 누가 왜 바꿨는지 확인한다. 변경한 사람을 알 수 없으면 조사 대상이다.

## 실행 위치를 옮기는 것도 방법이다

서버마다 예약 작업이 흩어져 있으면 목록 관리가 어렵다. 작업 실행을 전용 시스템으로 모으면 무엇이 언제 돌았고 결과가 어땠는지 한곳에서 보이고, 권한도 그 시스템에서 통제된다. 서버에 직접 등록하는 경로를 막으면 목록이 낡지 않는다. 옮기기 어려운 작업만 남기고 그 목록을 짧게 유지한다.

## 참고

- MITRE ATT&CK — 예약 작업을 통한 지속성
- CIS Controls — 계정과 권한
- NIST SP 800-53 — 감사 기록`,
    diagram: {
      type: 'flow',
      caption: '예약 작업의 위험',
      steps: [
        { label: '작업 등록', note: '편의로 넓은 권한' },
        { label: '담당자 이동', note: '관리 중단' },
        { label: '실패 무시', note: '조용함' },
        { label: '침입 경로', note: '지속성 확보', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '관리 항목',
      steps: [
        { label: '목록화', note: '무엇이 언제' },
        { label: '담당자', note: '각 작업' },
        { label: '전용 계정', note: '권한 최소' },
        { label: '실패 경보', note: '조용하지 않게' },
      ],
    },
  },
  {
    slug: 'server-side-request-guard',
    title: '서버가 외부를 부를 때의 방어',
    body: `서버가 주소를 받아 요청을 보내는 기능은 편리하지만, 그 주소를 사용자가 정하면 내부망 접근 통로가 된다. 여러 기능에 흩어져 있어 한 곳만 고쳐서는 닫히지 않는다.

## SSRF는 어떤 기능에 있나

| 기능 | 설명 |
| --- | --- |
| 이미지 가져오기 | 주소 입력 |
| 링크 미리보기 | 자동 조회 |
| 웹훅 등록 | 사용자 지정 |
| 문서 변환 | 외부 자원 |
| 파일 가져오기 | 주소로 |

링크 미리보기는 사용자가 주소를 입력했다는 인식조차 없이 동작한다. 본문에 붙인 링크를 서버가 자동으로 조회한다.

![요청 위조가 성립하는 흐름](/img/posts/server-side-request-guard.svg)

## 어떻게 막나

| 층 | 방법 |
| --- | --- |
| 입력 | 형식 확인 |
| 이름 해석 | 주소 확인 |
| 대역 | 내부 차단 |
| 이동 | 매번 재확인 |
| 출구 | 프록시 통제 |

이름을 해석한 뒤 얻은 실제 주소로 판단해야 한다. 문자열만 보면 내부를 가리키는 이름을 걸러내지 못한다.

## 응답도 통제한다

| 항목 | 내용 |
| --- | --- |
| 크기 제한 | 상한 |
| 형식 확인 | 예상한 것 |
| 내용 미노출 | 결과만 |
| 시간 제한 | 짧게 |

응답을 사용자에게 그대로 보여 주면 내부 시스템의 내용이 새어 나간다. 성공 여부와 필요한 값만 전달한다.

![막는 층](/img/posts/server-side-request-guard-2.svg)

## 기능을 찾아본다

\`\`\`bash
# 사용자 입력 주소로 요청을 보내는 지점
grep -rInE 'fetch\\(|request\\(|get\\(' --include='*.ts' src/ 2>/dev/null \
  | grep -iE 'url|link|image|preview' | head -20
\`\`\`

목록에 나온 기능을 하나씩 확인한다. 공통 함수를 거치게 바꾸면 이후에 추가되는 기능도 자동으로 보호된다.

## 나가는 경로를 하나로 모은다

각 기능이 직접 외부를 부르면 검증도 각자 해야 한다. 나가는 요청을 전용 프록시나 공통 모듈로 모으면 검증이 한 곳에서 이뤄지고, 어디로 나가는지 기록도 함께 남는다. 새 기능이 그 경로를 쓰지 않으면 나가지 못하게 네트워크에서 막아 두면 강제된다.

## 참고

- OWASP — SSRF Prevention Cheat Sheet
- CWE-918 — 서버 측 요청 위조
- 클라우드 사업자 메타데이터 보호 문서`,
    diagram: {
      type: 'flow',
      caption: '요청 위조가 성립하는 흐름',
      steps: [
        { label: '주소 입력', note: '또는 링크' },
        { label: '서버가 조회', note: '자동' },
        { label: '내부 주소 접근', note: '검증 없음' },
        { label: '응답 노출', note: '내부 정보', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '막는 층',
      layers: [
        { label: '이름 해석 후 확인', note: '실제 주소' },
        { label: '내부 대역 차단', note: '목록' },
        { label: '이동 시 재확인', note: '매번' },
        { label: '출구 프록시', note: '한 곳으로' },
      ],
    },
  },
]
