import type { SeedPost } from './types'

/** 540~549 — 파싱과 입력 처리 */
export const posts55: SeedPost[] = [
  {
    slug: 'deserialization-allowlist',
    title: '역직렬화를 허용 목록으로 막는 법',
    body: `역직렬화는 바이트를 객체로 되살리는 일이다. 어떤 클래스를 되살릴지 제한하지 않으면, 공격자가 지정한 클래스가 만들어지고 그 과정에서 코드가 실행된다. 입력 검증을 아무리 해도 이미 늦은 지점에서 벌어진다.

## 왜 검증으로 못 막나?

검증은 값을 본다. 그런데 역직렬화는 값을 읽기 전에 객체를 만들면서 생성자와 초기화 코드를 돌린다. 값이 이상한지 판단할 기회가 오기 전에 실행이 끝난다.

| 단계 | 언제 |
| --- | --- |
| 바이트 수신 | 아직 안전 |
| 클래스 결정 | 공격자가 지정 |
| 객체 생성 | 초기화 코드 실행 |
| 값 검증 | 이미 늦음 |

그래서 방어는 세 번째 단계가 아니라 두 번째 단계에 걸어야 한다.

![실행 시점과 검증 시점](/img/posts/deserialization-allowlist.svg)

## 허용 목록이 유일한 방어다

차단 목록은 늘 새는데, 되살릴 수 있는 클래스가 너무 많고 조합으로 만들어지는 경로가 계속 발견되기 때문이다.

| 방식 | 결과 |
| --- | --- |
| 차단 목록 | 새 경로가 계속 나옴 |
| 허용 목록 | 적은 수만 통과 |
| 서명 검증 | 출처는 확인, 내용은 아님 |
| 형식 변경 | 근본적 |

허용 목록에 넣을 클래스는 대개 열 개를 넘지 않는다. 그보다 많다면 직렬화로 주고받는 구조 자체를 다시 볼 이유가 된다.

## 형식을 바꾸는 편이 낫다

언어의 직렬화 형식 대신 데이터만 담는 형식을 쓰면 이 문제가 사라진다. 클래스 이름을 담지 않으므로 무엇을 만들지 공격자가 정할 수 없다.

| 형식 | 클래스 지정 |
| --- | --- |
| 언어 기본 직렬화 | 가능 |
| JSON(형식 정보 없이) | 불가 |
| JSON(형식 정보 포함) | 가능 |
| 스키마 기반 | 불가 |

JSON 이라고 안전한 것이 아니다. 형식 정보를 담는 설정을 켜면 같은 문제가 그대로 생긴다.

![형식에 따른 위험](/img/posts/deserialization-allowlist-2.svg)

## 지금 쓰는 곳을 찾는다

어디서 역직렬화가 일어나는지부터 센다.

\`\`\`bash
# 언어 기본 직렬화 사용처
grep -rniE 'ObjectInputStream|readObject|pickle.loads|Marshal.load|unserialize\\(' \\
  --include='*.{java,py,rb,php}' . | grep -v node_modules | head -20

# 형식 정보를 담는 설정이 켜져 있는지
grep -rniE 'enableDefaultTyping|@JsonTypeInfo|TypeNameHandling' . | head -10
\`\`\`

형식 정보 설정이 켜져 있으면 그것부터 끈다. 대개 다형성 처리를 편하게 하려고 켠 것인데, 명시적 매핑으로 바꿀 수 있다.

## 신뢰하는 출처라도

내부 서비스끼리 주고받는 데이터라고 안심하지 않는다. 한 서비스가 뚫리면 그 통로가 그대로 다음 서비스로 가는 길이 된다. 내부 통신에도 허용 목록을 거는 것이 경계를 하나 더 만드는 방법이다.

## 참고

- OWASP — 안전하지 않은 역직렬화
- CWE-502 — Deserialization of Untrusted Data
- NIST SP 800-53 — 입력 검증`,
    diagram: {
      type: 'flow',
      caption: '실행 시점과 검증 시점',
      steps: [
        { label: '바이트 수신', note: '아직 안전' },
        { label: '클래스 결정', note: '공격자가 지정', danger: true },
        { label: '객체 생성', note: '코드 실행' },
        { label: '값 검증', note: '이미 늦음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '형식에 따른 위험',
      x: ['클래스 지정 가능', '불가'],
      y: ['외부 입력', '내부만'],
      cells: ['가장 위험', '안전', '경계 필요', '안전'],
    },
  },
  {
    slug: 'xml-external-entity',
    title: 'XML 처리에서 외부 개체를 막는 설정',
    body: `XML 은 문서 안에서 외부 자원을 불러올 수 있는 기능을 갖고 있다. 이 기능이 켜진 채로 외부에서 받은 문서를 처리하면, 서버의 파일을 읽거나 내부 주소로 요청을 보내게 만들 수 있다.

## 파일 업로드로 어떻게 들어오나?

XML 을 직접 받는 곳만 생각하면 놓친다. 겉보기에 XML 과 무관한 경로로 들어오는 경우가 더 많다.

| 경로 | 실제 내용 |
| --- | --- |
| API 요청 본문 | XML 형식 허용 |
| 파일 업로드 | 문서·표 계산 파일 |
| SVG 이미지 | XML 문서 |
| 설정 파일 가져오기 | XML |
| SOAP 연동 | XML |

파일 업로드가 가장 자주 놓치는 자리다. 사무용 문서 형식 상당수가 안에 XML 을 담고 있어, 이미지나 문서를 받는 기능이 그대로 입구가 된다.

![XML 이 들어오는 경로](/img/posts/xml-external-entity.svg)

## 무엇을 할 수 있게 되나

| 결과 | 설명 |
| --- | --- |
| 서버 파일 읽기 | 설정 파일·키 파일 |
| 내부 주소 요청 | 방화벽 안쪽 탐색 |
| 클라우드 메타데이터 | 임시 자격 증명 |
| 자원 고갈 | 중첩 개체 확장 |

클라우드에서 돌아가는 서비스라면 메타데이터 주소 접근이 가장 위험하다. 임시 자격 증명이 그대로 나온다.

## 끄는 것이 설정 한 줄이다

대부분의 파서가 기능을 끄는 방법을 제공한다. 문제는 기본값이 켜져 있는 파서가 아직 있다는 점이다.

| 설정 | 효과 |
| --- | --- |
| 외부 일반 개체 끄기 | 파일 읽기 차단 |
| 외부 파라미터 개체 끄기 | 우회 차단 |
| DTD 자체 금지 | 가장 확실 |
| 확장 한도 설정 | 자원 고갈 방지 |

DTD 를 아예 금지하는 것이 가장 단순하고 확실하다. DTD 가 필요한 문서를 다루는 경우는 실무에서 드물다.

![끄는 순서](/img/posts/xml-external-entity-2.svg)

## 실제로 막혀 있는지 시험한다

설정했다고 믿지 말고 문서를 하나 보내 본다.

\`\`\`bash
cat > /tmp/probe.xml <<'XML'
<?xml version="1.0"?>
<!DOCTYPE r [<!ENTITY x SYSTEM "file:///etc/hostname">]>
<r>&x;</r>
XML

# 응답에 호스트명이 섞여 나오면 막히지 않은 것이다
curl -s -X POST https://example.com/api/import \\
  -H 'Content-Type: application/xml' --data-binary @/tmp/probe.xml | head -c 300
\`\`\`

응답에 값이 보이지 않아도 안심하기 이르다. 결과가 화면에 안 나오면서 요청만 나가는 형태가 있어, 외부 주소를 넣어 요청이 도달하는지도 함께 본다.

## 라이브러리를 바꿀 때마다

파서를 바꾸거나 버전을 올리면 기본값이 달라질 수 있다. 설정을 코드에 명시해 두면 기본값이 바뀌어도 영향을 받지 않는다. 기본값에 기대는 방어는 다음 갱신에서 조용히 사라진다.

## 참고

- OWASP — XXE Prevention Cheat Sheet
- CWE-611 — XML External Entity Reference
- NIST SP 800-53 — 입력 검증과 파서 설정`,
    diagram: {
      type: 'layers',
      caption: 'XML 이 들어오는 경로',
      layers: [
        { label: 'API 본문', note: '직접 수신' },
        { label: '파일 업로드', note: '문서 안의 XML' },
        { label: 'SVG 이미지', note: 'XML 문서' },
        { label: '설정 가져오기', note: '관리 기능' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '끄는 순서',
      steps: [
        { label: 'DTD 금지', note: '가장 확실' },
        { label: '외부 개체 끄기', note: '파일 읽기 차단' },
        { label: '파라미터 개체', note: '우회 차단' },
        { label: '확장 한도', note: '자원 고갈' },
      ],
    },
  },
  {
    slug: 'server-side-template-injection',
    title: '서버 측 템플릿 인젝션이 생기는 자리',
    body: `템플릿 엔진은 문자열 안의 표현식을 평가한다. 사용자가 넣은 값이 템플릿 본문에 섞이면 그 값도 평가 대상이 되고, 표현식 하나로 서버에서 코드를 돌릴 수 있게 된다.

## 값과 템플릿은 왜 섞이나?

정상 사용법은 템플릿과 값을 나눠 넘기는 것이다. 문제는 그 규칙이 깨지는 몇 가지 흔한 상황이다.

| 상황 | 왜 섞이나 |
| --- | --- |
| 메일 본문 사용자 편집 | 템플릿 자체가 입력 |
| 다국어 문구 관리 | 번역 문자열에 표현식 |
| 사용자 정의 알림 양식 | 의도적으로 허용 |
| 오류 메시지 조립 | 문자열 이어 붙이기 |
| 관리자 화면의 미리보기 | 입력을 그대로 렌더 |

관리자만 쓰는 기능이라 괜찮다고 두는 경우가 많은데, 관리자 계정이 뚫리면 그 자리가 곧 서버 장악 경로가 된다.

![값과 템플릿이 섞이는 자리](/img/posts/server-side-template-injection.svg)

## 엔진마다 위험이 다르다

표현식으로 무엇까지 할 수 있는지가 엔진마다 다르다. 같은 실수라도 결과의 크기가 다르다.

| 엔진 성격 | 도달 범위 |
| --- | --- |
| 논리 최소화 설계 | 값 출력 정도 |
| 일반 표현식 허용 | 객체 탐색 |
| 언어 전체 노출 | 코드 실행 |

논리를 최소화한 엔진을 고르는 것이 설계 단계의 방어다. 나중에 실수하더라도 피해가 작다.

## 막는 방법

| 방법 | 효과 |
| --- | --- |
| 값은 항상 인자로 | 근본 해결 |
| 사용자 템플릿 금지 | 확실 |
| 샌드박스 모드 | 부분적 |
| 허용 변수만 노출 | 피해 축소 |

샌드박스는 우회 사례가 계속 나오므로 단독으로 믿지 않는다. 피해를 줄이는 층으로만 쓴다.

![방어의 층](/img/posts/server-side-template-injection-2.svg)

## 취약한지 확인한다

간단한 산술식을 넣어 결과가 계산되는지 본다.

\`\`\`bash
# 계산 결과가 돌아오면 표현식이 평가되고 있다
for p in '{{7*7}}' '\${7*7}' '<%= 7*7 %>' '#{7*7}'; do
  echo -n "$p → "
  curl -s -G https://example.com/preview --data-urlencode "tpl=$p" | grep -o '49' | head -1
  echo
done
\`\`\`

49 가 나오면 취약하다. 나오지 않아도 엔진마다 문법이 달라 다른 표기로 다시 시험해야 한다.

## 사용자에게 양식을 열어야 한다면

알림 양식을 사용자가 편집하게 하는 요구는 실제로 자주 있다. 그때는 템플릿 엔진을 쓰지 말고, 정해진 치환 표시만 바꿔 넣는 단순 치환기를 직접 만든다. 표현식을 평가하지 않으므로 이 문제 자체가 생기지 않는다. 기능은 조금 줄지만 대부분의 요구는 치환만으로 충족된다.

## 참고

- OWASP — Server-Side Template Injection
- CWE-1336 — 템플릿 엔진 인젝션
- PortSwigger Research — SSTI 탐지 기법`,
    diagram: {
      type: 'flow',
      caption: '값이 템플릿이 되는 순간',
      steps: [
        { label: '사용자 입력', note: '문자열' },
        { label: '템플릿에 이어 붙임', note: '경계 소실', danger: true },
        { label: '엔진이 평가', note: '표현식 실행' },
        { label: '서버에서 코드 실행', note: '장악' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '방어의 층',
      layers: [
        { label: '값은 인자로', note: '근본' },
        { label: '사용자 템플릿 금지', note: '확실' },
        { label: '논리 최소 엔진', note: '피해 축소' },
        { label: '샌드박스', note: '보조' },
      ],
    },
  },
  {
    slug: 'http-parameter-pollution',
    title: 'HTTP 파라미터 오염이 만드는 우회',
    body: `같은 이름의 파라미터를 여러 번 보내면 어떻게 되는지는 정해져 있지 않다. 웹 서버, 프레임워크, 애플리케이션이 각자 다르게 고르고, 그 차이가 검증을 지나치는 통로가 된다.

## 어떻게 갈리나?

같은 요청을 두고 구성 요소마다 다른 값을 본다. 앞단이 검사한 값과 뒷단이 쓰는 값이 다를 수 있다.

| 구성 요소 | 흔한 선택 |
| --- | --- |
| 일부 웹 서버 | 첫 번째 |
| 일부 프레임워크 | 마지막 |
| 일부 언어 | 배열로 수집 |
| 일부 방화벽 | 전부 검사 |

앞의 방화벽이 첫 번째 값을 보고 통과시켰는데 애플리케이션이 마지막 값을 쓰면, 검사는 통과하고 실제로는 다른 값이 처리된다.

![앞단과 뒷단이 다른 값을 볼 때](/img/posts/http-parameter-pollution.svg)

## 무엇을 우회할 수 있나

| 대상 | 우회 방식 |
| --- | --- |
| 입력 필터 | 앞 값은 정상, 뒤 값은 공격 |
| 권한 확인 | 대상 식별자를 둘 |
| 결제 금액 | 금액 파라미터 중복 |
| 리다이렉트 검증 | 주소를 둘 |

금액 파라미터 중복은 결제 흐름에서 실제로 문제가 된 적이 있다. 검증은 정상 금액을 보고 통과하고, 처리는 다른 금액을 쓴다.

## 엔드포인트에서 정하는 규칙

동작을 명시적으로 정해 두면 구성 요소 간 차이가 사라진다.

| 규칙 | 내용 |
| --- | --- |
| 중복이면 거절 | 가장 단순하고 확실 |
| 첫 값만 사용 | 명시적으로 지정 |
| 배열을 기대하는 곳만 허용 | 예외를 좁힘 |
| 앞단과 같은 규칙 | 차이 제거 |

중복을 거절하는 것이 가장 낫다. 정상 사용자가 같은 파라미터를 두 번 보낼 이유가 거의 없다.

![규칙을 정하는 자리](/img/posts/http-parameter-pollution-2.svg)

## 어떻게 동작하는지 직접 본다

배포된 구성에서 어느 값이 쓰이는지 확인한다.

\`\`\`bash
# 같은 이름을 두 번 보내고 응답이 어느 값을 반영하는지 본다
curl -s -G https://example.com/api/echo \\
  --data-urlencode 'id=first' --data-urlencode 'id=second' | head -c 200

# 본문 파라미터도 같은 시험을 한다
curl -s -X POST https://example.com/api/echo \\
  -d 'id=first' -d 'id=second' | head -c 200
\`\`\`

질의 문자열과 본문의 동작이 다를 수 있으므로 둘 다 본다. 프록시를 거치는 경로와 직접 접근하는 경로가 다른 경우도 있다.

## 헤더에도 같은 문제가 있다

파라미터만의 문제가 아니다. 같은 이름의 헤더가 여러 번 오는 경우도 처리가 갈린다. 특히 출발지 주소를 담는 헤더가 여러 개 오면 어느 것을 믿을지에 따라 접근 통제가 뒤집힌다. 신뢰할 프록시를 명시하고 그 프록시가 붙인 값만 쓰는 구조여야 이 문제가 닫힌다.

## 참고

- OWASP — HTTP Parameter Pollution
- RFC 9110 — 메시지 문법과 중복 필드
- CWE-235 — 파라미터 처리 불일치`,
    diagram: {
      type: 'flow',
      caption: '앞단과 뒷단이 다른 값을 볼 때',
      steps: [
        { label: '중복 파라미터 전송', note: '첫 값은 정상' },
        { label: '방화벽이 첫 값 검사', note: '통과' },
        { label: '앱이 마지막 값 사용', note: '공격 값', danger: true },
        { label: '검증 우회', note: '기록도 어긋남' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '규칙을 정하는 자리',
      steps: [
        { label: '중복이면 거절', note: '가장 확실' },
        { label: '첫 값만 사용', note: '명시적 지정' },
        { label: '배열은 예외만', note: '범위 축소' },
        { label: '앞단과 통일', note: '차이 제거' },
      ],
    },
  },
  {
    slug: 'unicode-normalization-bypass',
    title: '유니코드 정규화로 검증이 뚫리는 경우',
    body: `같은 글자를 나타내는 방법이 유니코드에는 여러 가지다. 검증할 때의 표현과 사용할 때의 표현이 다르면, 검사에서는 다른 문자열로 보이고 실제로는 같은 값이 된다.

## 무엇이 달라지나?

정규화는 여러 표현을 하나로 모으는 과정이다. 이 과정이 검증 뒤에 일어나면 검증한 값과 쓰는 값이 달라진다.

| 입력 | 정규화 후 |
| --- | --- |
| 전각 문자 | 반각 문자 |
| 합성 문자 | 결합 문자 분해·재조합 |
| 호환 문자 | 기본 문자 |
| 일부 특수 공백 | 보통 공백 |

관리자 이름을 막아 두었는데 다른 표현으로 우회하거나, 확장자 검사를 통과한 뒤 다른 확장자가 되는 식이다.

![검증과 정규화의 순서](/img/posts/unicode-normalization-bypass.svg)

## 접근 통제에서 특히 문제가 된다

이름이나 식별자를 비교해 권한을 정하는 곳이 위험하다. 비교 시점과 사용 시점의 표현이 다르면 통제가 뒤집힌다.

| 자리 | 위험 |
| --- | --- |
| 사용자 이름 중복 검사 | 같은 이름 두 개 |
| 권한 대상 비교 | 우회 |
| 파일 이름 검사 | 확장자 우회 |
| 도메인 비교 | 다른 도메인이 같게 보임 |

## 순서를 고정한다

해결은 단순하다. 정규화를 먼저 하고 그 결과를 검증하고 저장한다.

| 단계 | 순서 |
| --- | --- |
| 1 | 입력 수신 |
| 2 | 정규화 |
| 3 | 검증 |
| 4 | 저장·사용 |

저장까지 정규화된 값으로 하면 이후 비교에서도 어긋나지 않는다. 원본이 필요하면 따로 보관하되 판단에는 쓰지 않는다.

![순서를 고정한다](/img/posts/unicode-normalization-bypass-2.svg)

## 지금 어떻게 처리하는지 본다

\`\`\`bash
# 같은 글자의 다른 표현이 같게 처리되는지
python3 - <<'PY'
import unicodedata
pairs = [('ﬁle', 'file'), ('ａdmin', 'admin'), ('Ａ', 'A')]
for a, b in pairs:
    n = unicodedata.normalize('NFKC', a)
    print(f'{a!r} → {n!r} · b 와 같은가: {n == b}')
PY
\`\`\`

정규화 형식에 따라 결과가 다르다. 보안 판단에는 호환 문자까지 모으는 형식을 쓰는 편이 안전하다 — 다만 그 형식은 원래 글자를 바꾸므로 표시용 값에는 쓰지 않는다.

## 사람이 읽는 값과 기계가 쓰는 값

이름처럼 사람이 읽는 값은 원본을 보여 주고 싶고, 비교는 정규화된 값으로 해야 한다. 두 값을 함께 저장하고 용도를 나누면 둘 다 만족한다. 하나만 두고 상황마다 변환하면 어딘가에서 빠뜨린다.

## 참고

- Unicode Standard Annex #15 — 정규화 형식
- OWASP — 입력 검증 Cheat Sheet
- CWE-179 — 부적절한 검증 순서`,
    diagram: {
      type: 'flow',
      caption: '검증과 정규화의 순서',
      steps: [
        { label: '입력 수신', note: '여러 표현' },
        { label: '검증 먼저', note: '다른 값으로 보임', danger: true },
        { label: '정규화', note: '같은 값이 됨' },
        { label: '사용', note: '검증 우회 성립' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '고쳐야 할 순서',
      steps: [
        { label: '입력 수신', note: '원본 보관' },
        { label: '정규화', note: '표현 통일' },
        { label: '검증', note: '통일된 값으로' },
        { label: '저장·사용', note: '같은 값' },
      ],
    },
  },
  {
    slug: 'regex-denial-of-service',
    title: '정규식 하나가 서비스를 멈추는 이유',
    body: `정규식은 대개 순식간에 끝난다. 그런데 어떤 패턴은 입력 길이가 조금만 늘어도 걸리는 시간이 폭발적으로 늘어난다. 특정 문자열 하나를 보내는 것만으로 서비스 거부를 만들 수 있다.

## 왜 폭발하나?

되짚어 가며 다시 시도하는 방식으로 동작하는 엔진에서, 같은 위치를 여러 경로로 맞출 수 있는 패턴이 문제가 된다. 경로의 수가 입력 길이에 따라 기하급수로 는다.

| 패턴 성격 | 위험 |
| --- | --- |
| 반복 안의 반복 | 높음 |
| 겹치는 선택지 | 높음 |
| 선택적 그룹의 반복 | 높음 |
| 고정 문자열 | 없음 |

반복 안에 반복이 있는 모양이 대표적이다. 겉보기에는 평범한 검증식인데 특정 입력에서 몇 분씩 걸린다.

![입력 길이와 처리 시간](/img/posts/regex-denial-of-service.svg)

## 서비스 거부는 어디서 시작되나

정규식은 검증에 많이 쓰여서, 하필 외부 입력을 가장 먼저 만나는 자리에 있다.

| 자리 | 입력 출처 |
| --- | --- |
| 이메일·전화 검증 | 사용자 입력 |
| 로그 파싱 | 외부 시스템 |
| 라우팅 규칙 | 요청 경로 |
| 콘텐츠 필터 | 게시물 본문 |
| 사용자 정의 검색 | 사용자가 패턴 제공 |

사용자가 패턴 자체를 넣는 기능은 가장 위험하다. 그때는 공격자가 패턴과 입력을 모두 고를 수 있다.

## 어떻게 줄이나

| 방법 | 효과 |
| --- | --- |
| 입력 길이 상한 | 즉시 효과 |
| 패턴 단순화 | 근본 |
| 시간 제한 실행 | 피해 차단 |
| 선형 시간 엔진 | 문제 소멸 |
| 정규식 대신 파서 | 근본 |

입력 길이 상한이 가장 값싸다. 이메일이 1,000자를 넘을 이유가 없고, 상한만으로 대부분의 폭발이 사라진다.

![줄이는 방법](/img/posts/regex-denial-of-service-2.svg)

## 위험한 패턴을 찾는다

실제로 시간을 재 본다. 길이를 늘려 가며 걸리는 시간이 어떻게 변하는지 본다.

\`\`\`bash
python3 - <<'PY'
import re, time
pat = re.compile(r'^(a+)+$')          # 반복 안의 반복
for n in (18, 20, 22, 24):
    s = 'a' * n + '!'
    t = time.time(); pat.match(s)
    print(f'길이 {n}: {time.time() - t:.3f}초')
PY
\`\`\`

길이를 2씩 늘렸는데 시간이 두 배씩 뛰면 그 패턴은 위험하다. 실제 서비스의 검증식을 넣어 같은 방식으로 재 본다.

## 남의 패턴도 우리 문제다

의존성이 내부에서 쓰는 정규식도 같은 위험을 갖는다. 우리가 쓴 적 없는 패턴 때문에 서비스가 멈출 수 있다. 취약점 보고에 이 유형이 꾸준히 나오므로, 의존성 점검에서 이 항목을 가볍게 보지 않는 편이 좋다.

## 참고

- OWASP — Regular Expression Denial of Service
- CWE-1333 — 비효율적인 정규식 복잡도
- Russ Cox — 정규식 매칭의 시간 복잡도`,
    diagram: {
      type: 'bars',
      caption: '입력 길이와 처리 시간',
      unit: 'ms',
      items: [
        { label: '18자', value: 30, note: '보통' },
        { label: '20자', value: 120, note: '4배' },
        { label: '22자', value: 480, note: '또 4배' },
        { label: '24자', value: 1900, note: '폭발' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '줄이는 방법',
      steps: [
        { label: '입력 길이 상한', note: '가장 값쌈' },
        { label: '패턴 단순화', note: '근본' },
        { label: '시간 제한', note: '피해 차단' },
        { label: '선형 엔진', note: '문제 소멸' },
      ],
    },
  },
  {
    slug: 'zip-slip-extraction',
    title: '압축 해제에서 경로를 벗어나는 공격',
    body: `압축 파일 안의 항목 이름에는 경로가 들어간다. 그 이름을 그대로 믿고 풀면, 상위 디렉터리를 가리키는 이름 하나로 의도한 폴더 밖에 파일을 쓸 수 있다. 덮어쓰기 대상이 설정 파일이나 실행 파일이면 그대로 장악이다.

## 이름에 무엇이 들어올 수 있나

| 이름 | 결과 |
| --- | --- |
| 상위 디렉터리 표기 반복 | 폴더 밖으로 |
| 절대 경로 | 지정한 위치로 |
| 심볼릭 링크 | 링크가 가리키는 곳으로 |
| 아주 긴 이름 | 처리 오류 |
| 같은 이름 반복 | 덮어쓰기 |

경로 탐색 표기만 막고 심볼릭 링크를 놓치는 경우가 흔하다. 링크를 먼저 풀고 그 위에 파일을 쓰면 링크가 가리키는 위치에 쓰인다.

![이름 하나로 폴더를 벗어난다](/img/posts/zip-slip-extraction.svg)

## 어디서 압축을 푸나

사용자가 올린 압축 파일만 생각하면 놓친다.

| 자리 | 출처 |
| --- | --- |
| 첨부 파일 처리 | 사용자 |
| 백업 복원 | 내부지만 조작 가능 |
| 플러그인 설치 | 외부 배포자 |
| 빌드 산출물 전개 | 파이프라인 |
| 외부 데이터 수신 | 연동 상대 |

## 안전하게 푸는 절차

이름을 검사하는 것이 아니라 최종 경로를 검사한다. 이름 검사는 표현 방식에 따라 새지만, 합쳐진 절대 경로 비교는 새지 않는다.

| 단계 | 내용 |
| --- | --- |
| 1 | 목적지 절대 경로 계산 |
| 2 | 항목 경로를 합쳐 정규화 |
| 3 | 목적지 안에 있는지 확인 |
| 4 | 링크·특수 파일 거절 |
| 5 | 크기·개수 상한 |

![안전하게 푸는 절차](/img/posts/zip-slip-extraction-2.svg)

## 지금 안전한지 시험한다

경로 탐색 이름을 가진 압축 파일을 만들어 넣어 본다.

\`\`\`bash
# 상위 폴더를 가리키는 항목을 가진 압축 파일 만들기
mkdir -p /tmp/zt && cd /tmp/zt
printf 'probe' > probe.txt
python3 -c "
import zipfile
z = zipfile.ZipFile('/tmp/evil.zip','w')
z.writestr('../../probe-escaped.txt', 'escaped')
z.close()
print('만듦: /tmp/evil.zip')
"
# 이 파일을 업로드한 뒤, 목적지 밖에 probe-escaped.txt 가 생기는지 확인한다
\`\`\`

목적지 폴더 밖에 파일이 생기면 취약하다. 생기지 않아도 절대 경로와 심볼릭 링크로 각각 다시 시험한다.

## 크기도 함께 본다

경로만 막고 크기를 놓치면 다른 문제가 남는다. 작은 압축 파일이 풀리면서 디스크를 가득 채우는 경우가 있다. 푸는 중에 누적 크기를 세다가 상한을 넘으면 중단하는 처리를 함께 넣는다. 다 푼 뒤에 재면 이미 늦다.

## 참고

- CWE-22 — 경로 탐색
- OWASP — 파일 업로드 보안
- Snyk Research — Zip Slip 취약점 보고서`,
    diagram: {
      type: 'flow',
      caption: '이름 하나로 폴더를 벗어난다',
      steps: [
        { label: '압축 파일 수신', note: '항목 이름 포함' },
        { label: '이름 그대로 사용', note: '상위 표기', danger: true },
        { label: '목적지 밖에 쓰기', note: '설정·실행 파일' },
        { label: '덮어쓰기', note: '장악' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전하게 푸는 절차',
      steps: [
        { label: '절대 경로 계산', note: '목적지 기준' },
        { label: '합쳐 정규화', note: '이름이 아니라 결과' },
        { label: '안에 있는지 확인', note: '접두 비교' },
        { label: '링크·크기 제한', note: '나머지 경로' },
      ],
    },
  },
  {
    slug: 'mime-sniffing-risk',
    title: 'MIME 추론이 만드는 업로드 우회',
    body: `브라우저는 서버가 알려 준 형식을 늘 그대로 믿지 않는다. 내용을 보고 스스로 판단하는 경우가 있고, 그 판단이 서버의 검사와 다르면 업로드 검증이 무력해진다.

## 어디서 갈리나?

| 주체 | 무엇을 보나 |
| --- | --- |
| 업로드 검증 | 확장자 또는 선언된 형식 |
| 저장 | 파일 이름 |
| 응답 헤더 | 서버가 정한 형식 |
| 브라우저 | 내용의 앞부분 |

서버는 이미지라고 저장했는데 브라우저가 내용을 보고 HTML 로 판단하면, 그 파일을 여는 순간 스크립트가 우리 도메인에서 실행된다.

![검사와 해석이 갈리는 자리](/img/posts/mime-sniffing-risk.svg)

## 파일 업로드에서 지킬 것

확장자만 보는 검사는 언제나 새므로, 여러 겹으로 막는다.

| 조치 | 내용 |
| --- | --- |
| 내용 기반 형식 확인 | 앞부분 시그니처 |
| 허용 목록 | 확장자와 형식 모두 |
| 이름 재생성 | 원본 이름 쓰지 않음 |
| 추론 금지 헤더 | 브라우저 판단 차단 |
| 다른 도메인에서 서빙 | 피해 격리 |

추론 금지 헤더 한 줄이 가장 값싸다. 이것만 붙여도 브라우저가 서버의 선언을 따르게 된다.

## 다른 도메인에서 내보내는 이유

사용자 파일을 본 도메인에서 서빙하면, 그 파일이 스크립트로 해석되는 순간 우리 세션에 접근할 수 있다. 별도 도메인에 두면 해석되더라도 남의 집에서 실행되는 셈이라 피해가 닫힌다.

| 방식 | 사고 시 범위 |
| --- | --- |
| 본 도메인 | 세션·쿠키 접근 |
| 하위 도메인 | 설정에 따라 일부 |
| 완전히 다른 도메인 | 격리 |

![서빙 위치와 피해 범위](/img/posts/mime-sniffing-risk-2.svg)

## 지금 헤더가 붙는지 본다

\`\`\`bash
# 업로드된 파일 응답의 헤더
curl -s -D - -o /dev/null https://example.com/uploads/sample.png \\
  | grep -iE 'content-type|x-content-type-options|content-disposition'

# 확장자를 바꿔 올려도 통과하는지 — 내용은 HTML, 이름은 png
printf '<script>alert(1)</script>' > /tmp/probe.png
curl -s -o /dev/null -w '%{http_code}\\n' -F 'file=@/tmp/probe.png' https://example.com/upload
\`\`\`

**X-Content-Type-Options: nosniff** 가 없으면 먼저 붙인다. 업로드가 200 으로 통과하면 내용 기반 검사가 없다는 뜻이다.

## 이미지도 안전하지 않다

형식 검사를 통과한 진짜 이미지 안에도 스크립트를 숨길 수 있다. 특히 XML 기반 이미지 형식은 그 자체가 문서라 스크립트를 담는다. 이런 형식을 받아야 한다면 내용을 다시 그려 저장하거나, 위험한 요소를 제거하는 처리를 거친다. 검사만으로는 닫히지 않는다.

## 참고

- OWASP — 파일 업로드 보안 Cheat Sheet
- MDN — X-Content-Type-Options
- CWE-434 — 위험한 형식의 파일 업로드`,
    diagram: {
      type: 'matrix',
      caption: '검사와 해석이 갈릴 때',
      x: ['서버가 형식 선언', '선언 없음'],
      y: ['nosniff 있음', '없음'],
      cells: ['안전', '내용으로 판단', '안전', '가장 위험'],
    },
    diagram2: {
      type: 'layers',
      caption: '업로드 방어의 층',
      layers: [
        { label: '내용 기반 확인', note: '시그니처' },
        { label: '이름 재생성', note: '원본 미사용' },
        { label: 'nosniff 헤더', note: '추론 차단' },
        { label: '다른 도메인', note: '피해 격리' },
      ],
    },
  },
  {
    slug: 'content-disposition-abuse',
    title: '다운로드 헤더를 악용하는 방법',
    body: `파일을 내려받게 하는 응답 헤더에는 파일 이름이 들어간다. 그 이름을 사용자 입력에서 그대로 만들면, 헤더 구조를 깨뜨리거나 엉뚱한 이름으로 저장되게 만들 수 있다.

## 파일 업로드 이름은 어디서 오나

대개 업로드할 때의 원본 이름을 그대로 쓴다. 그 이름은 사용자가 정한 값이다.

| 출처 | 통제 여부 |
| --- | --- |
| 업로드 원본 이름 | 사용자 |
| 질의 문자열 | 사용자 |
| 데이터베이스의 제목 | 사용자 |
| 서버가 생성 | 통제됨 |

![이름이 헤더에 들어가는 경로](/img/posts/content-disposition-abuse.svg)

## 무엇이 가능해지나

| 방식 | 결과 |
| --- | --- |
| 줄바꿈 문자 주입 | 헤더 추가·응답 분리 |
| 따옴표 깨뜨리기 | 다른 속성 주입 |
| 실행 파일 확장자 | 위험한 이름으로 저장 |
| 아주 긴 이름 | 클라이언트 오류 |
| 경로 표기 포함 | 저장 위치 혼란 |

줄바꿈 주입이 가장 심각하다. 응답 헤더를 새로 만들어 붙일 수 있으면 캐시를 오염시키거나 다른 내용을 주입할 수 있다.

## 어떻게 만들어야 하나

| 규칙 | 내용 |
| --- | --- |
| 이름을 서버가 만든다 | 가장 확실 |
| 허용 문자만 남긴다 | 영숫자·일부 기호 |
| 확장자는 형식에서 결정 | 이름에서 가져오지 않음 |
| 국제 문자는 정해진 방식으로 | 인코딩 규격 준수 |
| 길이 제한 | 클라이언트 보호 |

한글 파일 이름을 그대로 넣으면 브라우저마다 다르게 보이므로, 규격에 정해진 인코딩 방식을 쓴다. 그 방식이 없으면 이름이 깨져 사용자가 파일을 못 찾는다.

![안전한 이름 만들기](/img/posts/content-disposition-abuse-2.svg)

## 실제 응답을 확인한다

\`\`\`bash
# 이름에 줄바꿈이나 따옴표를 넣어 요청하고 응답 헤더를 본다
curl -s -D - -o /dev/null -G https://example.com/download \\
  --data-urlencode 'name=report".txt; x=y' | grep -i 'content-disposition'

# 응답에 예상치 못한 헤더가 늘어났는지
curl -s -D - -o /dev/null -G https://example.com/download \\
  --data-urlencode $'name=a\\r\\nX-Injected: yes' | grep -i 'x-injected'
\`\`\`

주입한 헤더가 응답에 보이면 즉시 조치 대상이다. 대부분의 최신 서버가 줄바꿈을 막지만, 직접 헤더를 조립하는 코드에서는 여전히 통과한다.

## 첨부와 인라인의 차이

같은 헤더에 파일을 첨부로 내려받게 할지 화면에 바로 보여 줄지를 정하는 값이 있다. 사용자 파일을 화면에 바로 보여 주는 설정은 업로드 우회와 결합해 스크립트 실행으로 이어진다. 사용자 파일은 기본을 첨부로 두고, 화면에 보여 줘야 하는 형식만 좁게 허용한다.

## 참고

- RFC 6266 — Content-Disposition 헤더
- OWASP — 응답 분리와 헤더 주입
- CWE-113 — HTTP 응답 분리`,
    diagram: {
      type: 'flow',
      caption: '이름이 헤더에 들어가는 경로',
      steps: [
        { label: '사용자가 이름 지정', note: '업로드 시' },
        { label: '그대로 저장', note: '검증 없음' },
        { label: '헤더에 삽입', note: '구조 깨짐', danger: true },
        { label: '헤더 주입', note: '응답 조작' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 이름 만들기',
      steps: [
        { label: '서버가 생성', note: '가장 확실' },
        { label: '허용 문자만', note: '영숫자 위주' },
        { label: '확장자는 형식에서', note: '이름과 분리' },
        { label: '규격 인코딩', note: '국제 문자' },
      ],
    },
  },
  {
    slug: 'image-metadata-leak',
    title: '이미지 메타데이터로 새는 정보',
    body: `사진 파일에는 화면에 보이지 않는 정보가 함께 저장된다. 촬영 위치, 시각, 기기 정보, 때로는 편집한 사람의 이름까지 들어간다. 그 파일을 그대로 공개하면 그 정보도 함께 공개된다.

## 무엇이 들어 있나?

| 항목 | 내용 |
| --- | --- |
| 위치 좌표 | 촬영 장소 |
| 촬영 시각 | 정확한 시점 |
| 기기 정보 | 모델·일련번호 |
| 편집 도구 | 소프트웨어·계정명 |
| 축소판 | 편집 전 원본 일부 |

축소판이 특히 놓치기 쉽다. 사진에서 일부를 가리고 올렸는데 메타데이터 안의 축소판에는 가리기 전 모습이 남아 있는 경우가 있다.

![이미지에 함께 담기는 것](/img/posts/image-metadata-leak.svg)

## 개인정보 관점에서 무엇이 문제인가?

위치와 시각이 함께 있으면 개인의 이동 경로가 된다. 여러 장이 모이면 생활 반경이 드러난다. 사진 자체는 공개할 의도였어도 그 정보까지 공개할 의도는 아니었을 것이다.

| 조합 | 알 수 있는 것 |
| --- | --- |
| 위치 + 시각 | 그때 그곳에 있었음 |
| 여러 장의 위치 | 생활 반경·집 위치 |
| 기기 일련번호 | 같은 사람이 찍은 사진 묶기 |
| 편집 계정명 | 실명 노출 |

## 어디서 지우나

업로드 시점에 서버에서 지우는 것이 확실하다. 사용자에게 맡기면 대부분 그대로 올린다.

| 시점 | 확실성 |
| --- | --- |
| 촬영 기기 설정 | 사용자마다 다름 |
| 업로드 전 클라이언트 | 우회 가능 |
| 서버 수신 직후 | 확실 |
| 서빙 시점 | 원본은 남음 |

서버에서 다시 그려 저장하면 메타데이터가 함께 사라진다. 형식 검사와 이 처리를 한 번에 할 수 있어 효율도 좋다.

![지우는 시점](/img/posts/image-metadata-leak-2.svg)

## 지금 남아 있는지 본다

\`\`\`bash
# 공개된 이미지 몇 개를 받아 메타데이터를 본다
curl -s -o /tmp/s.jpg https://example.com/uploads/sample.jpg
exiftool /tmp/s.jpg 2>/dev/null | grep -iE 'gps|date|model|software|artist' | head

# exiftool 이 없으면 문자열로 흔적을 찾는다
strings /tmp/s.jpg | grep -iE 'gps|canon|nikon|iphone|adobe' | head
\`\`\`

GPS 항목이 나오면 위치가 그대로 공개된 것이다. 이미 올라간 파일은 지우는 것만으로 끝나지 않는다 — 캐시와 검색엔진에 남은 사본도 함께 처리해야 한다.

## 지우면서 남겨야 하는 것

전부 지우면 화면 회전 정보까지 사라져 사진이 옆으로 눕는다. 회전 정보는 반영해서 저장하고 나머지를 지우는 처리가 필요하다. 이것을 놓치면 메타데이터를 지운 뒤 사용자 문의가 늘어난다.

## 참고

- 개인정보보호법 — 개인정보의 범위
- OWASP — 민감 정보 노출
- EXIF 규격 — 메타데이터 항목 정의`,
    diagram: {
      type: 'layers',
      caption: '이미지에 함께 담기는 것',
      layers: [
        { label: '위치 좌표', note: '촬영 장소' },
        { label: '촬영 시각', note: '정확한 시점' },
        { label: '기기 정보', note: '모델·일련번호' },
        { label: '축소판', note: '편집 전 원본' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '지우는 시점별 확실성',
      unit: '점',
      items: [
        { label: '서버 수신 직후', value: 100, note: '확실' },
        { label: '서빙 시점', value: 60, note: '원본 잔존' },
        { label: '클라이언트', value: 35, note: '우회 가능' },
        { label: '기기 설정', value: 15, note: '제각각' },
      ],
    },
  },
]
