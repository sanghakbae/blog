import type { SeedPost } from './types'

/** 141~150 — 대응 절차와 개발·공급망 보안 */
export const posts15: SeedPost[] = [
  {
    slug: 'bec-response',
    title: '이메일 사칭 사기 대응 절차',
    body: `대표나 거래처를 사칭해 계좌 변경과 급한 송금을 요구하는 사기는 악성코드를 쓰지 않는다. 그래서 백신과 첨부파일 검사로는 걸리지 않고, 메일 본문에 링크도 없어 필터를 통과한다. 이 사기를 막는 것은 기술 통제가 아니라 금전 요청을 검증하는 업무 절차다. 그리고 그 절차는 메일 밖에서 확인하는 단계를 반드시 포함해야 한다.

## 어떻게 진행되는가?

먼저 메일 계정 한 곳을 장악하거나 유사 도메인을 만든다. 그다음 오가는 메일을 관찰해 결제 일정과 담당자를 파악하고, 실제 청구 시점에 맞춰 계좌 변경 안내를 보낸다. 내용이 업무 흐름과 정확히 일치하기 때문에 의심하기 어렵다.

![계정 장악에서 송금까지의 단계](/img/posts/bec-response.svg)

## 신호와 대응

| 신호 | 대응 |
| --- | --- |
| 계좌 변경 요청 | 등록된 연락처로 전화 확인 |
| 긴급성과 비밀 유지 요구 | 절차 준수를 우선 |
| 회신 주소가 발신과 다름 | 헤더 확인 |
| 유사 도메인 | 문자 치환 여부 확인 |
| 평소와 다른 문체·시간대 | 별도 경로 확인 |
| 새 거래처의 첫 결제 | 검증 강화 |

## 절차 하나가 대부분을 막는다

계좌 정보 변경은 메일로 처리하지 않는다는 규칙 하나가 이 사기의 성립 조건을 없앤다. 등록된 전화번호로 담당자에게 직접 확인하고, 확인자와 확인 시각을 기록한다. 새로 알려준 번호로 전화하면 사기범과 통화하게 되므로, 기존에 등록된 번호를 쓰는 것이 핵심이다.

\`\`\`
금전 요청 검증 규칙
  계좌 변경은 등록된 전화번호로 확인 — 메일에 적힌 번호 금지
  일정 금액 이상은 두 사람이 승인
  긴급 요청일수록 절차를 지킨다 — 긴급성은 사기의 도구
  확인 결과를 티켓에 기록
\`\`\`

## 메일 인증으로 사칭 범위를 줄인다

우리 도메인을 사칭한 메일은 발신자 인증 설정으로 상당 부분 차단할 수 있다. 다만 유사 도메인과 장악된 실제 계정에서 오는 메일은 인증을 통과하므로, 인증 설정만으로 끝나지 않는다는 점을 이해해야 한다.

## 사고 후 조치

송금이 이미 이뤄졌다면 시간이 관건이다. 은행에 즉시 지급 정지를 요청하고 수사기관에 신고한다. 동시에 장악된 계정이 우리 쪽인지 확인해야 한다. 계정이 장악됐다면 전달 규칙과 메일함 규칙이 심어져 있는지 반드시 확인한다.

## 바로 확인하기

메일함에 몰래 심긴 전달 규칙이 가장 흔한 흔적이다. 계정 단위로 훑는다.

\`\`\`bash
# 외부 주소로 자동 전달하는 규칙 찾기 (Google Workspace 기준)
gam all users print forwards | grep -v '@example.com'

# 메일함 필터 중 삭제·이동을 하는 규칙 — 대화를 숨기는 데 쓰인다
gam all users print filters | grep -Ei 'trash|shouldTrash|forwardTo'
\`\`\`

발신자 인증 설정은 공개 정보로 바로 확인된다.

\`\`\`bash
for r in "example.com" "_dmarc.example.com" "selector1._domainkey.example.com"; do
  printf '%-42s ' "$r"
  dig +short TXT "$r" | head -1
done
\`\`\`

## 참고

- FBI IC3 — Business Email Compromise 권고
- RFC 7489 — DMARC
- NIST SP 800-177, 전자메일 보안 지침`,
    diagram: {
      type: 'flow',
      caption: '사칭 사기의 진행 단계',
      steps: [
        { label: '계정 장악·유사 도메인', note: '피싱 또는 자격 증명 유출' },
        { label: '메일 관찰', note: '결제 일정과 담당자 파악' },
        { label: '계좌 변경 요청', note: '실제 청구 시점에 맞춤', danger: true },
        { label: '송금', note: '검증 절차 부재', danger: true },
      ],
    },
  },
  {
    slug: 'breach-notification',
    title: '유출 신고 기한과 통지문 준비',
    body: `개인정보 유출을 인지하면 법이 정한 기한 안에 정보주체에게 알리고 감독기관에 신고해야 한다. 문제는 그 시점에 유출 범위가 아직 확정되지 않는다는 것이다. 조사와 통지를 순차로 하면 기한을 넘기고, 급하게 쓴 통지문은 나중에 정정해야 한다. 그래서 통지문 골격과 판단 기준을 사고 전에 만들어 두어야 한다.

## 무엇을 먼저 정해야 하는가?

- 무엇을 "인지"로 볼 것인가 — 기한 계산의 시작점
- 누가 신고 여부를 판단하는가
- 통지 문안을 누가 승인하는가
- 고객 문의를 받을 창구는 어디인가
- 범위가 바뀌면 어떻게 추가 통지하는가

인지 시점의 정의가 특히 중요하다. 담당자가 이상을 발견한 시각, 침해로 판단한 시각, 개인정보 포함을 확인한 시각이 모두 다르고, 이 정의가 없으면 사후에 지연 여부를 다투게 된다.

![인지부터 통지까지의 병행 처리](/img/posts/breach-notification.svg)

## 통지문에 들어갈 항목

| 항목 | 내용 |
| --- | --- |
| 유출 항목 | 어떤 개인정보 항목인지 |
| 시점 | 발생과 인지 시점 |
| 경위 | 확인된 범위에서 사실만 |
| 피해 최소화 | 정보주체가 할 수 있는 조치 |
| 대응 조치 | 사업자가 취한 조치 |
| 접수 창구 | 담당 부서와 연락처 |

## 조사와 통지를 병행한다

조사가 끝나기를 기다리면 기한을 넘긴다. 확인된 사실만으로 1차 통지를 하고, 추가로 확인되면 갱신 통지를 한다. 1차 통지문에 "조사 진행 중이며 확인되는 대로 추가 안내한다"는 문장을 넣어두면 갱신이 자연스러워진다.

\`\`\`
인지 즉시   대응팀 소집, 인지 시각 기록
+수 시간    유출 항목과 개략 규모 확인, 신고 여부 판단
기한 내     감독기관 신고, 정보주체 통지, 접수 창구 개설
이후        조사 완료 후 갱신 통지, 재발 방지 대책 보고
\`\`\`

## 문안이 흔들리는 이유는 사실 확인 부족이다

"암호화되어 있어 피해가 없을 것으로 판단됩니다"라고 썼다가 나중에 평문 항목이 발견되면 신뢰를 잃는다. 확인되지 않은 안심 표현을 쓰지 않고, 확인된 것과 조사 중인 것을 구분해 쓰는 편이 결과적으로 낫다.

## 고객 문의를 준비한다

통지 직후 문의가 몰린다. 예상 질문과 답변을 미리 만들어 상담 인원과 공유하지 않으면 답변이 갈리고, 그 자체가 2차 문제가 된다.

## 바로 확인하기

사고 전에 준비 상태를 점검한다. 아래 항목에 즉시 답하지 못하면 그것이 이번 분기의 과제다.

\`\`\`
준비 점검
  인지 시점의 정의가 문서에 있는가
  신고 판단 권한자가 지정돼 있는가
  통지문 골격과 예상 문답이 준비돼 있는가
  통지 발송 수단(메일·문자·홈페이지 게시)이 시험됐는가
  대량 발송 시 수신자 목록을 안전하게 뽑을 수 있는가
  감독기관 신고 양식과 제출 경로를 확인했는가
\`\`\`

발송 준비는 실제로 시험해 봐야 안다.

\`\`\`bash
# 통지 대상 추출이 가능한지 — 사고 시점에 확인하면 늦다
./bin/export-subjects --incident-scope scope.json --fields email,phone \\
  --out notify.csv --dry-run

# 발송 경로 시험 (내부 주소로만)
./bin/notify --template templates/breach-1st.md --to internal-test@example.com
\`\`\`

## 참고

- 개인정보보호법 제34조, 유출 통지와 신고
- 개인정보 유출 등의 통지·신고 방법 고시
- NIST SP 800-61, 사고 대응 절차`,
    diagram: {
      type: 'steps',
      caption: '유출 인지 후 처리 순서',
      steps: [
        { label: '인지 시각 기록', note: '기한 계산의 기준' },
        { label: '항목·규모 확인', note: '조사와 병행' },
        { label: '신고와 1차 통지', note: '확인된 사실만' },
        { label: '갱신 통지', note: '조사 완료 후' },
      ],
    },
  },
  {
    slug: 'sbom-operations',
    title: 'SBOM 을 실제로 쓰는 방법',
    body: `구성 목록을 만들라는 요구는 늘어났지만, 만든 파일을 어디에 두고 무엇에 쓰는지 정하지 않으면 규정 대응용 산출물로 끝난다. 실제 가치는 하나다. 새 취약점이 공개된 날, 우리 시스템 중 어디에 그 구성 요소가 들어 있는지 몇 분 안에 답할 수 있는 것. 그 답을 얻는 구조를 만드는 일이 SBOM 운영이다.

## 왜 목록만 만들면 쓸모가 없는가?

빌드할 때 생성해 산출물 옆에 두면, 정작 필요한 순간에 어느 버전이 어느 환경에 배포돼 있는지 알 수 없다. 목록은 배포와 연결돼야 질의가 가능하다. 즉 SBOM 을 저장소에 모으고, 어떤 환경에 어떤 산출물이 떠 있는지와 함께 검색 가능해야 한다.

![생성부터 질의까지의 연결](/img/posts/sbom-operations.svg)

## 갖춰야 할 요소

| 요소 | 없으면 생기는 문제 |
| --- | --- |
| 빌드 시 자동 생성 | 목록이 실제와 어긋난다 |
| 산출물 식별자 연결 | 어느 이미지의 목록인지 모른다 |
| 배포 현황 연결 | 어느 환경에 있는지 모른다 |
| 중앙 저장과 검색 | 질의에 며칠 걸린다 |
| 보관 이력 | 과거 배포 조사 불가 |
| 서명 | 목록 자체의 신뢰 확보 |

## 사고 당일의 질의로 검증한다

운영이 되는지 확인하는 방법은 간단하다. 특정 구성 요소와 버전 범위를 주고 영향 대상을 뽑아보게 한다. 몇 분 안에 목록이 나오면 운영되는 것이고, 사람이 팀마다 물어봐야 하면 아직 아니다.

\`\`\`
질의   log4j-core < 2.17.1 을 포함한 배포는?
답     서비스 3개, 이미지 7개, 운영 환경 파드 42개
소요   3분
\`\`\`

## 직접 의존성과 전이 의존성을 구분한다

취약점 대응에서 이 구분이 우선순위를 정한다. 직접 의존성은 우리가 버전을 올릴 수 있고, 전이 의존성은 상위 패키지를 기다려야 한다. 후자는 임시 조치가 필요하므로 목록에서 구분해 뽑을 수 있어야 한다.

## 도달 가능성까지 보면 더 좋다

목록에 있다는 것과 그 취약 코드가 실행 경로에 있다는 것은 다르다. 도달 가능성 분석을 붙이면 대응 대상이 크게 줄어든다. 다만 완벽하지 않으므로, 판단 근거를 기록하고 예외에는 기한을 붙인다.

## 바로 확인하기

생성은 도구 한 줄이면 된다. 중요한 것은 산출물 식별자와 함께 저장하는 것이다.

\`\`\`bash
# 이미지에서 구성 목록 생성 (다이제스트 기준으로 고정)
DIGEST=$(crane digest example.com/app:1.42.0)
syft "example.com/app@$DIGEST" -o spdx-json > "sbom-$DIGEST.json"

# 저장소에 함께 올리고 서명한다
cosign attest --predicate "sbom-$DIGEST.json" --type spdx \\
  "example.com/app@$DIGEST"
\`\`\`

질의는 모아둔 목록에서 한다.

\`\`\`bash
# 특정 구성 요소를 포함한 산출물 찾기
grep -l '"name": "log4j-core"' sboms/*.json | while read -r f; do
  ver=$(python3 -c '
import json,sys
d=json.load(open(sys.argv[1]))
print(*[p["versionInfo"] for p in d.get("packages",[]) if p["name"]=="log4j-core"])' "$f")
  echo "$(basename "$f" .json) $ver"
done
\`\`\`

## 참고

- SPDX 및 CycloneDX 규격 문서
- NTIA — SBOM 최소 요소
- SLSA 공급망 보증 수준 문서`,
    diagram: {
      type: 'flow',
      caption: 'SBOM 이 질의 가능해지는 경로',
      steps: [
        { label: '빌드 시 생성', note: '다이제스트 기준' },
        { label: '중앙 저장', note: '산출물 식별자와 함께' },
        { label: '배포 현황 연결', note: '어느 환경에 있는가' },
        { label: '영향 범위 질의', note: '공개 당일 대응' },
      ],
    },
  },
  {
    slug: 'dependency-confusion',
    title: '의존성 혼동 공격과 사내 저장소',
    body: `사내에서만 쓰는 패키지 이름과 같은 이름을 공개 저장소에 올려두면, 패키지 관리자가 어느 쪽을 고를지 애매해지는 상황이 생긴다. 대부분의 도구는 더 높은 버전을 선호하므로 공개 저장소의 999 버전이 선택된다. 빌드 서버에서 임의 코드가 실행되는 결과로 이어지고, 이 공격은 취약점 없이 설정 하나로 성립한다.

## 왜 성립하는가?

여러 저장소를 함께 쓰도록 설정하면 도구는 양쪽을 모두 조회한다. 사내 저장소에만 있어야 할 이름이 공개 저장소에도 있으면, 버전 비교로 공개 쪽이 선택될 수 있다. 사내 패키지 이름은 오류 로그, 잠금 파일, 공개된 설정 예시에서 쉽게 드러난다.

![저장소 조회 순서와 버전 선택](/img/posts/dependency-confusion.svg)

## 대책 비교

| 대책 | 효과 | 비고 |
| --- | --- | --- |
| 이름 공간 선점 | 높음 | 조직 접두어를 공개 저장소에 등록 |
| 저장소 우선순위 고정 | 높음 | 도구별 설정 방식 확인 필요 |
| 단일 프록시 저장소 | 가장 높음 | 공개 패키지도 프록시 경유 |
| 잠금 파일 강제 | 중간 | 해시 검증과 함께 |
| 설치 스크립트 차단 | 중간 | 실행 시점 차단 |
| 이름 규칙 | 보조 | 접두어로 구분 |

## 프록시 하나로 모으는 것이 가장 확실하다

내부 패키지와 공개 패키지를 모두 하나의 프록시 저장소를 통해서만 받게 하면 선택 문제가 사라진다. 프록시에서 내부 이름은 외부 조회를 하지 않도록 설정하고, 외부 패키지는 캐시해 재현성도 함께 얻는다.

\`\`\`
빌드 서버  →  사내 프록시 저장소  →  공개 저장소(캐시)
                    ↑
              내부 패키지는 외부 조회 금지
\`\`\`

## 이름 공간을 선점한다

조직 범위 접두어를 공개 저장소에 등록해 두면 같은 이름을 남이 올릴 수 없다. 비용이 거의 들지 않는 예방책이고, 프록시 전환 전에도 즉시 적용할 수 있다.

## 설치 스크립트를 신뢰하지 않는다

패키지 설치 과정에서 실행되는 스크립트가 실제 피해 경로다. 빌드 환경에서 설치 스크립트 실행을 끄면, 잘못된 패키지를 받아도 코드 실행까지는 이어지지 않는다. 필요한 패키지만 예외로 허용한다.

## 바로 확인하기

우리 내부 패키지 이름이 공개 저장소에 존재하는지 확인하는 것이 첫 점검이다.

\`\`\`bash
# 잠금 파일에서 내부 패키지 이름 추출 후 공개 저장소 존재 여부 확인
python3 -c '
import json
d=json.load(open("package-lock.json"))
names={k.split("node_modules/")[-1] for k in d.get("packages",{}) if k}
print("\n".join(sorted(n for n in names if n.startswith("@example/") or n.startswith("example-"))))
' > internal.txt

while read -r p; do
  code=$(curl -s -o /dev/null -w '%{http_code}' "https://registry.npmjs.org/$p")
  [ "$code" = "200" ] && echo "공개 저장소에 존재: $p"
done < internal.txt
\`\`\`

설정으로 조회 경로를 고정한다.

\`\`\`ini
; .npmrc — 내부 범위는 사내 저장소로만 조회한다
@example:registry=https://npm.internal.example.com/
registry=https://npm.internal.example.com/
ignore-scripts=true
\`\`\`

## 참고

- OWASP — Dependency Confusion 개요
- SLSA 공급망 위협 모델
- 패키지 관리자별 저장소 우선순위 문서`,
    diagram: {
      type: 'matrix',
      caption: '저장소 구성과 혼동 위험',
      x: ['단일 프록시', '다중 저장소'],
      y: ['이름 선점 완료', '선점 없음'],
      cells: ['안전', '버전 비교로 선택 가능', '가장 안전', '가장 위험'],
    },
  },
  {
    slug: 'workflow-permissions',
    title: 'CI 워크플로 권한 축소와 위험',
    body: `파이프라인은 저장소 권한과 배포 자격 증명을 모두 가진 실행 환경이다. 그래서 워크플로 하나에 임의 코드를 실행시킬 수 있으면 저장소와 운영 환경이 함께 넘어간다. 가장 흔한 결함은 외부 기여자의 입력이 빌드 스크립트에서 그대로 실행되는 구성과, 필요 이상으로 넓은 기본 권한이다.

## 어디에서 코드 실행이 생기는가?

- 풀 리퀘스트 제목·본문·브랜치 이름을 스크립트에 문자열로 삽입
- 포크에서 온 워크플로가 비밀값에 접근
- 캐시를 포크와 공유
- 신뢰할 수 없는 스크립트를 브랜치 이름으로 참조
- 태그를 옮길 수 있는 방식으로 외부 액션 참조

브랜치 이름이나 제목에 명령을 넣는 방식은 즉시 시험해 볼 수 있을 만큼 단순하다.

![외부 입력이 파이프라인에서 실행되는 경로](/img/posts/workflow-permissions.svg)

## 설정 점검 항목

| 항목 | 안전한 설정 |
| --- | --- |
| 기본 권한 | 읽기 전용, 작업별로 필요한 것만 부여 |
| 포크 트리거 | 비밀값 접근 없는 트리거 사용 |
| 외부 액션 | 커밋 해시로 고정 |
| 비밀값 범위 | 환경별로 분리하고 승인 필요 |
| 로그 | 비밀값 마스킹 확인 |
| 산출물 | 임시 자격 증명이 포함되지 않게 |

## 외부 입력은 환경 변수로 전달한다

스크립트 안에 문자열로 끼워 넣으면 명령으로 해석된다. 환경 변수로 전달하고 스크립트에서 변수로 읽으면 그대로 문자열로 다뤄진다. 규칙 하나로 대부분의 삽입 경로가 닫힌다.

\`\`\`yaml
# 위험 — 제목이 명령으로 해석된다
- run: echo "PR: \${{ github.event.pull_request.title }}"

# 안전 — 환경 변수로 넘긴다
- run: echo "PR: $TITLE"
  env:
    TITLE: \${{ github.event.pull_request.title }}
\`\`\`

## 외부 액션은 해시로 고정한다

태그는 옮길 수 있다. 신뢰하던 액션의 태그가 다른 커밋을 가리키게 되면 그 시점부터 우리 파이프라인에서 다른 코드가 돈다. 커밋 해시로 고정하고, 갱신은 의존성 갱신과 같은 절차로 처리한다.

\`\`\`yaml
# 태그 참조 — 옮겨질 수 있다
- uses: actions/checkout@v4
# 해시 고정 — 내용이 바뀌지 않는다
- uses: actions/checkout@8f4b7f84864484a7bf31766abe9204da3cbe65b3
\`\`\`

## 배포 자격 증명은 단기 토큰으로

파이프라인에 장기 클라우드 키를 넣어두면 그 키가 유출 대상이 된다. 신원 연동으로 실행 시점에 단기 토큰을 받으면 저장할 비밀값이 사라진다. 이 전환이 파이프라인 보안에서 가장 효과가 큰 변경이다.

## 바로 확인하기

권한 설정과 태그 참조를 한 번에 훑는다.

\`\`\`bash
# 기본 권한 선언이 없는 워크플로 — 넓은 기본값이 적용된다
for f in .github/workflows/*.yml; do
  grep -q '^permissions:' "$f" || echo "권한 미선언: $f"
done

# 해시로 고정되지 않은 외부 액션
grep -rhoE 'uses: [^ ]+@[^ ]+' .github/workflows/ \
  | grep -vE '@[0-9a-f]{40}$' | sort -u

# 워크플로에서 신뢰할 수 없는 입력을 직접 삽입한 곳
grep -rnE 'github\.event\.(pull_request\.(title|body|head\.ref)|issue\.(title|body)|comment\.body)' \
  .github/workflows/ | grep 'run:'
\`\`\`

## 참고

- GitHub Actions 보안 강화 문서
- SLSA — 빌드 무결성 요건
- OWASP Top 10 CI/CD Security Risks`,
    diagram: {
      type: 'layers',
      caption: '파이프라인 권한 축소 계층',
      layers: [
        { label: '기본 권한 읽기 전용', note: '작업별 최소 부여' },
        { label: '외부 입력 격리', note: '환경 변수로 전달' },
        { label: '외부 액션 고정', note: '커밋 해시 참조' },
        { label: '단기 배포 토큰', note: '장기 키 제거' },
      ],
    },
  },
  {
    slug: 'secret-scanning',
    title: '저장소 비밀값 유출 탐지와 조치',
    body: `저장소에 커밋된 자격 증명은 브랜치를 지워도 이력에 남고, 공개 저장소라면 몇 분 안에 자동 수집된다. 그래서 조치의 첫 단계는 이력 정리가 아니라 회전이다. 노출된 값을 무효화하지 않으면 커밋을 지우는 작업은 의미가 없다. 탐지는 커밋되기 전에, 조치는 회전을 먼저 하는 순서로 만들어야 한다.

## 어떤 순서로 조치하는가?

\`\`\`
1. 값을 무효화한다 — 회전 또는 폐기가 최우선
2. 그 값으로 어떤 접근이 있었는지 감사 로그로 확인
3. 새 값을 비밀 관리 서비스에 저장하고 참조로 교체
4. 저장소 이력에서 제거 (되돌릴 수 없으므로 계획 후)
5. 커밋 전 탐지를 도입해 재발 방지
\`\`\`

순서를 바꾸면 대응이 실패한다. 이력 정리에 시간을 쓰는 동안 노출된 값은 계속 유효하다.

![노출 인지부터 재발 방지까지](/img/posts/secret-scanning.svg)

## 탐지 위치별 특성

| 위치 | 시점 | 특성 |
| --- | --- | --- |
| 커밋 훅 | 커밋 전 | 가장 빠름. 우회 가능 |
| 파이프라인 | 푸시 후 | 우회 불가. 이미 남음 |
| 저장소 전체 검사 | 사후 | 이력 포함 확인 |
| 공개 저장소 감시 | 사후 | 외부 유출 탐지 |
| 사용 이력 감시 | 사후 | 실제 악용 탐지 |

커밋 훅만으로는 부족하고 파이프라인 검사만으로도 부족하다. 둘을 함께 두는 것이 표준 구성이다.

## 오탐을 줄이는 방법

시험용 값과 예시 값이 많으면 경보가 무시된다. 예시 값은 명확히 무효한 형태로 통일하고, 시험 데이터는 별도 경로로 관리한다. 검출 규칙에 유효성 확인을 붙이면 실제 유효한 값만 걸러낼 수 있다.

## 무엇이 비밀값인지 정의한다

API 키만 보는 검사는 데이터베이스 접속 문자열, 개인키, 서명 키, 내부 주소를 놓친다. 우리 환경에서 유출되면 문제가 되는 값의 목록을 만들고 그 형태를 규칙으로 등록해야 한다.

## 바로 확인하기

이력 전체를 대상으로 검사하는 것이 시작이다. 최신 상태만 보면 이미 지워진 커밋을 놓친다.

\`\`\`bash
# 이력 전체 검사
gitleaks detect --source . --log-opts='--all' --report-path leaks.json
python3 -c '
import json
for f in json.load(open("leaks.json")):
    print(f["RuleID"], f["Commit"][:8], f["File"], f["StartLine"])' | sort -u | head -30

# 특정 형태의 값이 이력에 있었는지 직접 검색
git log --all -p -S 'BEGIN RSA PRIVATE KEY' --oneline | head
\`\`\`

노출된 값의 악용 여부는 감사 로그로 확인한다. 이 단계가 빠지면 피해 범위를 모른 채 종결하게 된다.

\`\`\`bash
aws cloudtrail lookup-events --max-results 50 \\
  --lookup-attributes AttributeKey=AccessKeyId,AttributeValue="$EXPOSED_KEY" \\
  --query 'Events[].[EventTime,EventName,SourceIPAddress]' --output table
\`\`\`

## 참고

- OWASP Cheat Sheet — Secrets Management
- NIST SP 800-53, IA-5 인증자 관리
- 저장소 서비스별 비밀값 검사 기능 문서`,
    diagram: {
      type: 'steps',
      caption: '노출된 비밀값 조치 순서',
      steps: [
        { label: '즉시 회전', note: '이력 정리보다 먼저' },
        { label: '악용 여부 확인', note: '감사 로그 질의' },
        { label: '참조로 교체', note: '비밀 관리 서비스' },
        { label: '커밋 전 탐지 도입', note: '재발 방지' },
      ],
    },
  },
  {
    slug: 'security-regression-test',
    title: '보안 회귀를 막는 테스트 작성',
    body: `같은 취약점이 반복되는 이유는 고칠 때 테스트를 남기지 않았기 때문이다. 기능 테스트는 "되는지"를 확인하고, 보안 테스트는 "안 되는지"를 확인한다. 후자가 없으면 리팩터링 한 번에 인가 검사가 사라져도 아무 테스트도 실패하지 않는다. 정적 분석 도구가 이 자리를 메워주지 않는다.

## 무엇을 테스트로 남기는가?

- 인가 — 남의 자원에 접근하면 거부되는가
- 입력 검증 — 정의되지 않은 필드가 거부되는가
- 인증 — 토큰 없이, 만료된 토큰으로 거부되는가
- 요청 수 제한 — 임계치에서 차단되는가
- 출력 — 응답에 민감 필드가 포함되지 않는가
- 오류 — 스택 트레이스가 노출되지 않는가

취약점을 하나 고칠 때마다 그 취약점을 재현하는 테스트를 하나 남기는 규칙이 가장 실효성이 높다.

![기능 테스트와 거부 테스트의 차이](/img/posts/security-regression-test.svg)

## 테스트 종류별 배치

| 종류 | 검증 대상 | 실행 위치 |
| --- | --- | --- |
| 단위 | 인가 판정 함수 | 매 커밋 |
| 통합 | 엔드포인트별 거부 동작 | 매 커밋 |
| 계약 | 응답 스키마의 민감 필드 | 매 커밋 |
| 정적 분석 | 알려진 위험 패턴 | 매 커밋 |
| 동적 검사 | 실행 중 애플리케이션 | 야간 |
| 의존성 | 알려진 취약점 | 매일 |

## 인가 테스트는 표로 만들면 빠짐없다

역할과 자원의 조합을 표로 정의하고, 표를 순회하는 테스트 하나로 전체를 검증한다. 새 역할이나 새 자원이 추가되면 표에 한 줄을 더하는 것으로 끝난다.

\`\`\`ts
const MATRIX = [
  // 역할,        자원,             기대 상태
  ['owner',      'own-order',      200],
  ['owner',      'other-order',    404],
  ['member',     'org-order',      200],
  ['member',     'other-org-order',404],
  ['anonymous',  'own-order',      401],
] as const

for (const [role, target, expected] of MATRIX) {
  test(\`\${role} → \${target} = \${expected}\`, async () => {
    const res = await request(app)
      .get(\`/orders/\${ids[target]}\`)
      .set(authHeader(role))
    expect(res.status).toBe(expected)
  })
}
\`\`\`

## 응답 스키마를 고정한다

민감 필드가 응답에 섞이는 사고는 모델에 필드를 추가할 때 생긴다. 응답 스키마를 명시하고 정의되지 않은 필드가 있으면 테스트가 실패하게 만들면, 추가되는 순간 잡힌다.

## 실패하는 테스트를 먼저 만든다

취약점 신고를 받으면 먼저 그것을 재현하는 테스트를 쓴다. 테스트가 실패하는 것을 확인한 뒤 코드를 고치고, 통과를 확인한다. 이 순서를 지키면 수정이 실제로 문제를 해결했다는 근거가 남는다.

## 바로 확인하기

파이프라인이 보안 테스트를 실제로 돌리고 있는지, 실패 시 배포가 멈추는지 확인한다.

\`\`\`bash
# 인가 관련 테스트가 몇 개인지 — 0이면 그것이 결론이다
npx vitest list 2>/dev/null | grep -ciE 'auth|권한|인가|forbidden|401|403'

# 일부러 실패시켜 파이프라인이 멈추는지 확인
npx vitest run --testNamePattern='member → other-org-order' --reporter=verbose
\`\`\`

취약점 대응 티켓에 테스트 링크를 필수 항목으로 두면 규칙이 유지된다.

\`\`\`
취약점 종결 조건
  원인 수정 커밋
  재현 테스트 추가 (링크 필수)
  같은 유형의 다른 위치 점검 결과
\`\`\`

## 참고

- OWASP Application Security Verification Standard (ASVS)
- OWASP Web Security Testing Guide
- NIST SP 800-218, 보안 개발 프레임워크`,
    diagram: {
      type: 'matrix',
      caption: '테스트 유형과 회귀 방지 효과',
      x: ['거부 동작 검증', '정상 동작만 검증'],
      y: ['자동 실행', '수동 점검'],
      cells: ['회귀 방지됨', '결함이 재도입된다', '주기 사이에 재도입', '사실상 방지 없음'],
    },
  },
  {
    slug: 'pentest-scoping',
    title: '모의침투 범위 설정과 결과 활용',
    body: `모의 침투는 보고서를 받는 것이 목적이 아니다. 목적은 우리가 모르던 경로를 찾고, 그 경로가 다시 열리지 않게 하는 것이다. 그런데 범위를 잘못 잡으면 이미 아는 내용만 담긴 보고서가 나온다. 자동 스캔으로 찾을 수 있는 것은 미리 처리하고, 사람이 아니면 못 찾는 영역에 시간을 쓰게 해야 한다.

## 무엇을 미리 정리하는가?

- 자동 스캐너로 찾을 수 있는 항목은 사전 조치
- 알려진 취약점과 패치 누락은 먼저 처리
- 보안 헤더와 설정 오류는 미리 교정
- 테스트 계정과 역할별 계정 준비
- 시험 데이터와 격리된 환경 준비

이 정리가 끝난 상태에서 시작하면, 같은 예산으로 논리 결함과 권한 설계 문제를 찾게 된다.

![사전 정리 여부에 따른 결과 차이](/img/posts/pentest-scoping.svg)

## 범위 정의 항목

| 항목 | 정해야 할 것 |
| --- | --- |
| 대상 | 도메인·API·앱·인프라 범위 |
| 계정 | 역할별 계정 제공 여부 |
| 정보 제공 | 소스 코드·설계 문서 제공 수준 |
| 금지 행위 | 서비스 거부, 소셜 엔지니어링, 데이터 파기 |
| 환경 | 운영·스테이징 여부와 데이터 |
| 기간 | 시험 기간과 재시험 포함 여부 |
| 연락 | 긴급 상황 담당자와 즉시 보고 기준 |

## 정보를 주는 편이 유리하다

정보를 감추고 시험하면 정찰에 시간을 쓴다. 실제 공격자는 시간이 무한하지만 시험 기간은 제한적이다. 코드와 설계를 제공하면 같은 기간에 더 깊은 곳을 본다. 외부에서 얼마나 보이는지 확인하는 것이 목적일 때만 정보를 제한한다.

## 즉시 보고 기준을 정한다

심각한 결함이 발견되면 보고서를 기다릴 이유가 없다. 어떤 수준이면 즉시 알릴지, 누구에게 어떤 경로로 알릴지 계약 단계에서 정한다. 데이터 유출 흔적이나 이미 침해된 증거가 발견되는 경우의 처리도 함께 정해야 한다.

## 결과를 처리하는 방식이 가치를 정한다

발견 항목을 그대로 티켓으로 옮기면 개별 수정으로 끝난다. 같은 원인에서 나온 항목을 묶어 근본 원인 단위로 처리하고, 각 항목에 재현 테스트를 남긴다. 재시험은 수정 확인용이므로 계약에 포함해 두는 편이 낫다.

\`\`\`
보고서 처리
  1. 같은 원인끼리 묶는다 — 인가 누락 12건 → 판정 함수 단일화 1건
  2. 근본 원인 단위로 티켓 생성
  3. 항목마다 재현 테스트 추가
  4. 기한 설정 — 심각도별 기준 적용
  5. 재시험으로 확인
  6. 유형을 코드 리뷰 점검표에 반영
\`\`\`

## 바로 확인하기

의뢰 전에 자동으로 찾을 수 있는 것을 먼저 처리한다. 아래 정도는 스스로 돌릴 수 있다.

\`\`\`bash
# 노출 포트와 서비스
nmap -Pn -sV --top-ports 200 app.example.com

# 보안 헤더 누락
curl -sI https://app.example.com | grep -icE \\
  'strict-transport-security|content-security-policy|x-content-type-options'

# 알려진 취약점이 있는 의존성
npm audit --audit-level=high

# 컨테이너 이미지 취약점
trivy image --severity HIGH,CRITICAL example.com/app:1.42.0
\`\`\`

## 참고

- PTES (Penetration Testing Execution Standard)
- OWASP Web Security Testing Guide
- NIST SP 800-115, 정보보안 시험 지침`,
    diagram: {
      type: 'steps',
      caption: '모의 침투 준비와 활용',
      steps: [
        { label: '사전 정리', note: '자동 탐지 항목 선처리' },
        { label: '범위·규칙 확정', note: '금지 행위와 즉시 보고 기준' },
        { label: '시험 수행', note: '역할별 계정 제공' },
        { label: '근본 원인 처리', note: '재현 테스트와 재시험' },
      ],
    },
  },
  {
    slug: 'design-review-gate',
    title: '설계 보안 검토를 병목 없이 넣기',
    body: `보안 검토를 모든 변경에 필수로 걸면 개발이 멈추고, 우회 경로가 생긴다. 반대로 아무 관문도 없으면 위험한 설계가 그대로 나간다. 실효성 있는 방식은 검토가 필요한 변경을 판별하는 기준을 공개하고, 대부분의 변경은 자기 점검으로 통과시키며, 소수의 변경만 사람이 보는 것이다.

## 어떤 변경이 검토 대상인가?

기준을 문서로 공개하면 개발자가 스스로 판단한다. 판단이 애매한 경우만 물어보게 된다.

- 인증·인가 방식의 변경
- 개인정보 항목의 신규 수집이나 목적 변경
- 외부 시스템과의 새로운 연동
- 새로운 데이터 저장소나 외부 서비스 도입
- 신뢰 경계를 넘는 통신 추가
- 암호화·키 관리 방식 변경
- 파일 업로드와 다운로드 기능 신설

![자기 점검과 검토 대상의 분기](/img/posts/design-review-gate.svg)

## 검토 단계 설계

| 변경 유형 | 절차 | 소요 |
| --- | --- | --- |
| 일반 기능 | 점검표 자기 확인 | 10분 |
| 신뢰 경계 변경 | 짧은 위협 모델링 | 60분 |
| 신규 서비스 | 설계 검토 회의 | 1~2회 |
| 외부 서비스 도입 | 검토와 계약 확인 | 별도 절차 |

## 점검표는 짧아야 쓰인다

스무 개가 넘는 점검표는 형식적으로 체크된다. 다섯에서 일곱 개로 줄이고, 각 항목이 실제 결함으로 이어진 사례와 연결해 두면 이해가 빨라진다.

\`\`\`
자기 점검 (모든 변경)
  1. 이 기능에 접근할 수 있는 사람을 서버에서 확인하는가
  2. 사용자 입력을 허용 목록으로 검증하는가
  3. 응답에 필요 이상의 필드가 포함되지 않는가
  4. 실패와 거부가 로그에 남는가
  5. 새 비밀값이 코드가 아닌 곳에 저장되는가
  6. 되돌릴 수 없는 동작에 확인 단계가 있는가
\`\`\`

## 검토를 빠르게 하는 것이 관문 유지의 조건이다

응답이 느리면 우회한다. 검토 요청에 대한 응답 시간을 지표로 관리하고, 하루 안에 1차 회신을 목표로 둔다. 검토가 개발을 돕는다는 경험이 쌓이면 요청이 자발적으로 늘어난다.

## 결정을 기록한다

검토에서 위험을 수용하기로 했다면 그 판단과 근거, 재검토 시점을 남긴다. 기록이 없으면 다음 사람이 같은 논의를 반복하고, 사고 후에는 아무도 그 결정을 설명하지 못한다.

## 바로 확인하기

관문이 실제로 동작하는지 지표로 본다. 요청 수와 응답 시간이 함께 좋아야 한다.

\`\`\`bash
# 검토 요청 수와 1차 회신까지 걸린 시간
gh issue list --label security-review --state all --limit 100 \\
  --json number,createdAt,comments \\
  | python3 -c '
import json,sys
from datetime import datetime
def t(s): return datetime.fromisoformat(s.replace("Z","+00:00"))
rows=json.load(sys.stdin)
lags=[]
for r in rows:
    if not r["comments"]: continue
    lag=(t(r["comments"][0]["createdAt"])-t(r["createdAt"])).total_seconds()/3600
    lags.append(lag)
lags.sort()
print(f"요청 {len(rows)}건, 회신 {len(lags)}건, 중앙값 {lags[len(lags)//2]:.1f}시간" if lags else "회신 없음")
'
\`\`\`

우회 여부는 배포와 검토 기록을 대조해 확인한다.

\`\`\`
점검 항목
  검토 대상 기준에 해당하는 변경 중 검토 기록이 없는 건
  자기 점검 통과 후 발생한 보안 결함 유형 — 점검표 개선 근거
  위험 수용 결정 중 재검토 시점이 지난 건
\`\`\`

## 참고

- NIST SP 800-218, 보안 개발 프레임워크
- OWASP Application Security Verification Standard
- Microsoft Security Development Lifecycle 자료`,
    diagram: {
      type: 'flow',
      caption: '변경 유형에 따른 검토 경로',
      steps: [
        { label: '변경 제안', note: '기준 문서로 자기 판별' },
        { label: '자기 점검', note: '대부분의 변경' },
        { label: '위협 모델링', note: '신뢰 경계 변경 시' },
        { label: '결정 기록', note: '수용 근거와 재검토 시점' },
      ],
    },
  },
  {
    slug: 'vendor-questionnaire',
    title: '협력업체 보안 점검표 만드는 법',
    body: `백 문항짜리 점검표를 보내면 답변은 받지만 판단 근거는 얻지 못한다. 대부분의 항목이 "예"로 돌아오고, 그 답이 사실인지 확인할 방법이 없다. 유효한 점검표는 짧고, 우리가 그 업체에 실제로 맡기는 위험에 맞춰져 있으며, 증적으로 확인할 수 있는 질문으로 구성된다.

## 무엇을 맡기는지에 따라 질문이 달라진다

같은 점검표를 모든 업체에 보내는 것이 가장 큰 낭비다. 위탁 유형별로 물어야 할 것이 다르다.

- 개인정보를 처리하는 위탁 — 접근 통제, 파기, 재위탁, 국외 이전
- 우리 시스템에 접근하는 업체 — 계정 관리, 접속 경로, 이력 보관
- 코드나 산출물을 제공하는 업체 — 개발 보안, 취약점 대응 기한
- 데이터를 보관하는 업체 — 암호화, 백업, 사고 통지

![위탁 유형과 점검 항목의 대응](/img/posts/vendor-questionnaire.svg)

## 짧게 만드는 기준

| 좋은 질문 | 나쁜 질문 |
| --- | --- |
| 최근 3개월 접근권한 검토 기록을 제출할 수 있는가 | 접근 통제를 하고 있는가 |
| 심각 취약점 조치 기한이 며칠인가 | 취약점 관리를 하는가 |
| 사고 발생 시 몇 시간 안에 통지하는가 | 사고 대응 체계가 있는가 |
| 재위탁 업체 목록을 제공할 수 있는가 | 재위탁을 관리하는가 |
| 퇴사자 계정 회수까지 며칠인가 | 계정 관리를 하는가 |

차이는 하나다. 답이 숫자나 문서로 나오는 질문을 쓴다. "예/아니오"로 답할 수 있는 질문은 정보를 주지 않는다.

## 계약에 들어가야 할 항목

점검표는 현황 파악이고, 강제력은 계약에서 나온다. 점검 결과가 계약 조항으로 이어지지 않으면 매년 같은 답변을 받게 된다.

\`\`\`
계약에 넣을 것
  사고 통지 기한 — 시간 단위로 명시
  감사·현장 점검 권리
  재위탁 사전 승인
  계약 종료 시 데이터 파기와 확인서
  접근 계정의 범위와 회수 기한
  위반 시 조치
\`\`\`

## 접근 경로를 좁히는 것이 답변보다 확실하다

업체 답변의 신뢰도를 높이려 노력하는 것보다, 업체가 접근할 수 있는 범위를 좁히는 편이 확실하다. 전용 계정과 지정된 자원만 접근, 작업 기간에만 활성화, 접근 이력 별도 보관으로 구성하면 답변이 부실해도 위험이 제한된다.

## 주기적으로 다시 본다

한 번 통과한 업체를 다시 보지 않으면, 업체의 변화와 위탁 범위의 확대가 반영되지 않는다. 위탁 범위가 커지거나 사고가 있었던 업체는 주기를 짧게 한다.

## 바로 확인하기

우리 쪽에서 확인할 수 있는 것부터 본다. 업체 답변보다 이쪽이 정확하다.

\`\`\`bash
# 업체 계정의 최근 접근 이력 — 사용하지 않는 계정은 회수 대상
awk '$3 ~ /^vendor-/ {print $3, $4}' access.log | sort -u \\
  | awk '{c[$1]=$2} END {for (u in c) print c[u], u}' | sort

# 업체 계정에 부여된 권한 범위 확인
for u in $(cut -d, -f1 vendors.csv); do
  printf '%-24s ' "$u"
  ./bin/iam-effective-permissions --user "$u" --summary
done
\`\`\`

계약과 실제의 차이를 표로 관리한다.

\`\`\`
업체 관리 대장 항목
  위탁 업무와 처리하는 개인정보 항목
  접근 경로와 계정 목록
  계약상 사고 통지 기한
  최근 점검 일자와 결과
  재위탁 여부와 승인 이력
  계약 종료일과 파기 확인 여부
\`\`\`

## 참고

- 개인정보보호법 제26조, 업무 위탁에 따른 제한
- ISO/IEC 27036, 공급자 관계 정보보안
- NIST SP 800-161, 공급망 위험 관리`,
    diagram: {
      type: 'matrix',
      caption: '점검 방식과 실효성',
      x: ['증적 확인', '자기 답변만'],
      y: ['접근 범위 좁음', '접근 범위 넓음'],
      cells: ['위험 통제됨', '답변 신뢰에 의존', '가장 안전', '가장 위험'],
    },
  },
]
