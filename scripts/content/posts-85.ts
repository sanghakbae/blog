import type { SeedPost } from './types'

/** 840~849 — 클라이언트 쪽에서 새는 것 */
export const posts85: SeedPost[] = [
  {
    slug: 'client-side-authorization',
    title: '화면에서만 막힌 권한의 문제',
    body: `버튼을 숨기고 메뉴를 감추면 사용자는 그 기능을 쓸 수 없어 보인다. 그런데 화면을 그리는 코드는 전부 사용자 쪽에서 돌기 때문에, 요청을 직접 보내면 서버는 아무것도 모른 채 처리한다.

## 어디서 뚫리나

| 지점 | 설명 |
| --- | --- |
| 숨긴 버튼 | 요청은 가능 |
| 감춘 메뉴 | 주소 직접 입력 |
| 비활성 입력 | 값 전송 |
| 조건부 화면 | 상태 조작 |
| 응답 필터링 | 이미 전송됨 |

응답에 다 담아 보내고 화면에서 골라 보여 주는 방식이 특히 많다. 개발자 도구를 열면 감춘 값이 그대로 보인다.

![화면 통제가 뚫리는 경로](/img/posts/client-side-authorization.svg)

## 접근 통제를 어디서 판단하나

| 위치 | 역할 |
| --- | --- |
| 화면 | 보여 줄지 |
| 서버 | 허용할지 |
| 데이터 계층 | 범위 제한 |

화면의 판단은 사용자 경험을 위한 것이고 실제 통제는 서버에 있어야 한다. 두 곳에서 같은 규칙을 쓰되 믿는 것은 서버 쪽 하나다.

## 응답을 어떻게 만드나

| 방식 | 노출 |
| --- | --- |
| 전부 보내고 골라 표시 | 전부 노출 |
| 권한만큼 보내기 | 안전 |
| 필드 단위 제어 | 정확 |

권한에 따라 응답 필드를 다르게 구성하면 화면에서 감출 것 자체가 없어진다. 출력용 형식을 권한별로 나누는 방식이 관리하기 쉽다.

![판단 위치와 신뢰](/img/posts/client-side-authorization-2.svg)

## 직접 확인한다

\`\`\`bash
# 화면에서 감춰진 기능을 API 로 직접 호출
curl -s -o /dev/null -w '%{http_code}\\n' -X POST https://example.com/api/admin/users \
  -H "Authorization: Bearer $NORMAL_USER_TOKEN" -d '{}'

# 응답에 권한 밖 필드가 담기는지
curl -s https://example.com/api/orders/1 -H "Authorization: Bearer $NORMAL_USER_TOKEN" | jq 'keys'
\`\`\`

일반 사용자 토큰으로 관리 기능이 호출되면 그 경로는 열려 있는 것이다. 응답 키 목록에 내부용 필드가 있어도 마찬가지다.

## 화면 코드도 정리한다

서버에서 막았더라도 화면에 권한 판단 로직이 흩어져 있으면 유지가 어렵다. 권한 확인을 한 곳에 모으고 그 결과로 화면을 구성하면, 서버 규칙이 바뀔 때 한 곳만 고치면 된다. 두 곳의 규칙이 어긋나면 사용자는 보이는데 눌리지 않는 버튼을 만나게 된다.

## 참고

- OWASP — 접근 통제 취약점
- CWE-602 — 클라이언트 측 보안 강제
- OWASP API 보안 Top 10`,
    diagram: {
      type: 'flow',
      caption: '화면 통제가 뚫리는 경로',
      steps: [
        { label: '버튼 숨김', note: '화면에서' },
        { label: '요청 직접 생성', note: '도구로' },
        { label: '서버 미검증', note: '화면만 믿음' },
        { label: '기능 실행', note: '권한 밖', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '판단 위치와 신뢰',
      x: ['화면', '서버'],
      y: ['보여 주기', '허용하기'],
      cells: ['적합', '불필요', '신뢰 불가', '적합'],
    },
  },
  {
    slug: 'spa-token-handling',
    title: '단일 페이지 앱의 토큰 다루기',
    body: `브라우저에서 도는 앱은 비밀을 지킬 수 없다. 코드도 저장소도 전부 사용자 쪽에 있고, 삽입된 스크립트가 있으면 앱이 접근할 수 있는 모든 것에 접근한다.

## 세션 토큰을 어디에 두나

| 위치 | 스크립트 접근 |
| --- | --- |
| 접근 차단 쿠키 | 불가 |
| 로컬 저장소 | 가능 |
| 메모리 변수 | 가능 |
| 주소 | 기록에 남음 |

스크립트가 읽을 수 없는 쿠키에 두는 방식이 가장 안전하다. 삽입 공격이 있어도 값을 꺼내지 못하고, 요청에는 자동으로 실려 간다.

![토큰 보관 방식의 차이](/img/posts/spa-token-handling.svg)

## 쿠키를 쓸 때의 부담

| 항목 | 내용 |
| --- | --- |
| 요청 위조 | 대응 필요 |
| 교차 도메인 | 설정 복잡 |
| 모바일 앱 | 공유 어려움 |

자동 전송되기 때문에 요청 위조 대응이 따로 필요하다. 교차 사이트 전송 속성과 토큰 검증을 함께 쓰면 정리된다.

## 수명을 짧게 가져간다

| 토큰 | 수명 |
| --- | --- |
| 접근 토큰 | 수 분 |
| 갱신 토큰 | 수 일 |
| 재발급 시 | 회전 |

접근 토큰이 짧으면 유출돼도 곧 무효가 된다. 갱신 토큰은 쿠키에 두고 접근 토큰만 메모리에 두는 구성도 널리 쓰인다.

![토큰 수명 구성](/img/posts/spa-token-handling-2.svg)

## 지금 어디에 있는지 본다

\`\`\`bash
# 코드에서 토큰을 저장소에 넣는 지점
grep -rInE '(localStorage|sessionStorage)\\.setItem' --include='*.ts' --include='*.tsx' src/ 2>/dev/null \
  | grep -iE 'token|jwt|auth' | head

# 응답에서 쿠키 속성 확인
curl -sI https://example.com/api/login | grep -i 'set-cookie'
\`\`\`

토큰이 저장소에 들어가고 있으면 그 값은 삽입된 스크립트에 노출된다. 옮기기 어렵다면 수명을 먼저 줄인다.

## 삽입을 막는 것이 먼저다

토큰을 어디에 두든 스크립트 삽입이 성립하면 사용자가 할 수 있는 모든 일을 공격자도 할 수 있다. 토큰을 못 읽어도 앱을 통해 요청을 보내면 되기 때문이다. 보관 위치를 고민하기 전에 삽입을 막는 층을 갖추는 것이 순서다.

## 참고

- OWASP — 브라우저 저장소 보안
- OAuth 2.0 for Browser-Based Apps
- OWASP — 세션 관리 지침`,
    diagram: {
      type: 'bars',
      caption: '토큰 보관 방식의 차이',
      unit: '점',
      items: [
        { label: '접근 차단 쿠키', value: 90, note: '읽을 수 없음' },
        { label: '메모리 변수', value: 55, note: '새로고침 소실' },
        { label: '로컬 저장소', value: 30, note: '스크립트 접근' },
        { label: '주소에 포함', value: 5, note: '기록에 남음' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '토큰 수명 구성',
      steps: [
        { label: '접근 토큰', note: '수 분' },
        { label: '갱신 토큰', note: '쿠키에' },
        { label: '재발급 회전', note: '재사용 탐지' },
        { label: '이상 시 폐기', note: '전체 종료' },
      ],
    },
  },
  {
    slug: 'browser-extension-risk',
    title: '확장 프로그램이 보는 우리 화면',
    body: `사용자가 설치한 확장 프로그램은 우리 화면의 내용을 읽고 바꿀 수 있다. 우리가 통제할 수 없는 코드가 같은 페이지에서 도는 셈이고, 업무용 서비스라면 이 점을 고려해야 한다.

## 무엇을 할 수 있나

| 동작 | 가능 여부 |
| --- | --- |
| 화면 내용 읽기 | 가능 |
| 입력 값 가로채기 | 가능 |
| 요청 변조 | 권한에 따라 |
| 쿠키 접근 | 권한에 따라 |
| 화면 변경 | 가능 |

권한을 넓게 요구하는 확장이면 사실상 그 사이트의 모든 것에 접근한다. 사용자는 대개 그 권한 목록을 읽지 않는다.

![확장이 접근하는 범위](/img/posts/browser-extension-risk.svg)

## 우리가 할 수 있는 것

| 방법 | 효과 |
| --- | --- |
| 민감 정보 최소 표시 | 노출 축소 |
| 별도 창 사용 | 제한적 |
| 무결성 확인 | 변조 탐지 |
| 안내 | 인식 |

기술적으로 막을 수는 없고 노출을 줄이는 쪽이 현실적이다. 화면에 필요 이상으로 표시하지 않는 것이 가장 효과가 크다.

## 업무 환경에서는

| 방법 | 내용 |
| --- | --- |
| 허용 목록 | 승인된 것만 |
| 설치 차단 | 정책으로 |
| 목록 확인 | 정기 점검 |
| 안내 | 위험 설명 |

관리 도구로 확장 설치를 통제할 수 있다. 업무 계정으로 민감한 시스템에 접근한다면 이 통제가 필요하다.

![대응 방법](/img/posts/browser-extension-risk-2.svg)

## 어떤 것이 설치돼 있나

\`\`\`bash
# 관리 도구가 있다면 정책으로 목록을 확인한다
# macOS 에서 크롬 확장 목록 경로
ls ~/Library/Application\\ Support/Google/Chrome/Default/Extensions 2>/dev/null | head
\`\`\`

목록에 모르는 항목이 많으면 확인이 필요하다. 인기 있던 확장이 다른 곳에 팔려 악성으로 바뀐 사례가 여러 번 있었다.

## 사용자에게 알린다

우리가 막을 수 없는 위험은 알리는 것이 최선일 때가 있다. 민감한 작업을 할 때는 확장이 적은 환경을 쓰라고 안내하거나, 별도 프로필을 권하는 방식이다. 강제할 수 없는 영역이므로 이유를 설명하고 판단은 사용자에게 맡기는 편이 받아들여진다.

## 참고

- OWASP — 브라우저 확장 보안
- 각 브라우저 확장 권한 문서
- CIS Controls — 단말 소프트웨어 통제`,
    diagram: {
      type: 'layers',
      caption: '확장이 접근하는 범위',
      layers: [
        { label: '화면 내용', note: '읽기·변경' },
        { label: '입력 값', note: '가로채기' },
        { label: '요청', note: '권한에 따라' },
        { label: '쿠키', note: '권한에 따라' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '대응 방법',
      x: ['우리 서비스', '업무 환경'],
      y: ['기술적 통제', '안내'],
      cells: ['제한적', '정책 가능', '가능', '가능'],
    },
  },
  {
    slug: 'mixed-content-and-tls',
    title: 'TLS 화면에 평문이 섞일 때',
    body: `암호화된 페이지 안에서 평문으로 자원을 불러오면 그 부분은 가로채거나 바꿀 수 있다. 브라우저가 대부분 막아 주지만, 막히면서 기능이 조용히 동작하지 않는 문제가 생긴다.

## 어디서 생기나

| 자원 | 처리 |
| --- | --- |
| 스크립트 | 차단 |
| 스타일 | 차단 |
| 이미지 | 자동 승격 또는 차단 |
| 프레임 | 차단 |
| 요청 | 차단 |

스크립트가 차단되면 화면의 일부 기능이 동작하지 않는데, 오류 메시지가 눈에 띄지 않아 원인을 찾기 어렵다.

![평문이 섞이는 경로](/img/posts/mixed-content-and-tls.svg)

## 어떻게 찾나

| 방법 | 내용 |
| --- | --- |
| 개발자 도구 | 경고 확인 |
| 보고 수집 | 자동 |
| 코드 검색 | 평문 주소 |
| 자동 승격 | 정책 설정 |

정책으로 평문 요청을 자동 승격하게 하면 대부분 해결되고, 남는 것만 보고로 받을 수 있다.

## 옛 설정이 남는 자리

| 위치 | 설명 |
| --- | --- |
| 데이터베이스 | 저장된 주소 |
| 설정 파일 | 외부 연동 |
| 메일 본문 | 링크 |
| 문서 | 안내 |

본문에 저장된 옛 주소가 가장 자주 남는다. 한 번 일괄 변환하고 새로 들어오는 것을 막는 순서로 처리한다.

![해결 순서](/img/posts/mixed-content-and-tls-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 저장된 본문에 평문 주소가 있는지
psql -At -c "select count(*) from posts where body like '%http://%'" 2>/dev/null

# 응답 헤더에 승격 정책이 있는지
curl -sI https://example.com | grep -i 'content-security-policy' | grep -o 'upgrade-insecure-requests'
\`\`\`

건수가 나오면 일괄 변환 대상이다. 외부 사이트 링크는 그대로 두어도 되지만 우리 자원은 전부 바꾼다.

## 전송 보안 설정도 함께 본다

평문 접속 자체를 막는 설정을 켜면 브라우저가 처음부터 암호화로만 접속한다. 적용 기간을 길게 잡기 전에 모든 하위 도메인이 준비됐는지 확인해야 하고, 한 번 적용하면 되돌리기까지 시간이 걸린다. 짧은 기간으로 시작해 문제가 없는지 보고 늘리는 순서가 안전하다. 사전 등록 목록에 올리는 것은 되돌리기가 특히 어려우니 마지막에 판단한다.

## 참고

- MDN — 혼합 콘텐츠
- NIST SP 800-52 — TLS 구현 지침
- OWASP — 전송 계층 보호`,
    diagram: {
      type: 'flow',
      caption: '평문이 섞이는 경로',
      steps: [
        { label: '암호화 페이지', note: '정상' },
        { label: '평문 자원 참조', note: '옛 주소' },
        { label: '브라우저 차단', note: '조용히' },
        { label: '기능 미동작', note: '원인 불명', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '해결 순서',
      steps: [
        { label: '보고 수집', note: '무엇이 걸리나' },
        { label: '저장된 주소 변환', note: '일괄' },
        { label: '자동 승격', note: '정책' },
        { label: '평문 접속 차단', note: '단계적' },
      ],
    },
  },
  {
    slug: 'form-autofill-risk',
    title: '브라우저 자동 완성이 채우는 값',
    body: `브라우저의 자동 완성은 편리하지만, 화면에 보이지 않는 입력란까지 채우기 때문에 사용자가 의도하지 않은 값이 함께 전송될 수 있다.

## 무엇이 문제인가

| 상황 | 결과 |
| --- | --- |
| 숨은 입력란 | 자동 채움 |
| 삽입된 입력란 | 값 수집 |
| 이름 추측 | 다른 값 |
| 공용 기기 | 잔존 |

공격자가 화면에 보이지 않는 입력란을 넣어 두면 자동 완성이 그것을 채우고 전송된다. 사용자는 아무것도 느끼지 못한다.

![자동 완성이 새는 경로](/img/posts/form-autofill-risk.svg)

## 어떻게 다루나

| 방법 | 내용 |
| --- | --- |
| 속성 지정 | 항목별 |
| 민감 입력 제외 | 자동 완성 끔 |
| 삽입 차단 | 입력란 자체 |
| 공용 기기 안내 | 사용자 |

자동 완성을 무조건 끄면 사용성이 떨어지고 사용자가 더 약한 비밀번호를 쓰게 된다. 비밀번호 관리 도구와의 연동은 오히려 켜 두는 편이 낫다.

## 무엇을 끄나

| 항목 | 판단 |
| --- | --- |
| 비밀번호 | 켜 둠 |
| 일회용 코드 | 끔 |
| 카드 번호 | 상황에 따라 |
| 주민등록번호 | 끔 |

일회용 코드를 저장하게 하면 의미가 없어진다. 한 번 쓰고 버리는 값은 자동 완성 대상에서 빼야 한다.

![자동 완성 판단](/img/posts/form-autofill-risk-2.svg)

## 설정을 확인한다

\`\`\`bash
# 자동 완성 속성이 지정된 입력란
grep -rIoE 'autoComplete="[^"]+"' --include='*.tsx' src/ 2>/dev/null | sort | uniq -c

# 민감 항목에 속성이 빠져 있는지
grep -rInE 'type="password"' --include='*.tsx' src/ 2>/dev/null | head
\`\`\`

속성을 지정하지 않으면 브라우저가 이름으로 추측한다. 항목마다 명시하면 엉뚱한 값이 채워지는 일이 줄어든다.

## 삽입을 막는 것이 근본이다

자동 완성이 악용되려면 공격자가 우리 화면에 입력란을 넣을 수 있어야 한다. 그 자체가 이미 스크립트 삽입 취약점이므로, 자동 완성 설정은 완화 조치이고 삽입을 막는 것이 근본 해결이다. 두 가지를 함께 다루되 우선순위는 분명하다. 입력란 이름을 예측하기 어렵게 두는 것도 작은 보탬이 된다.

## 참고

- MDN — autocomplete 속성
- OWASP — 인증 지침
- CWE-200 — 정보 노출`,
    diagram: {
      type: 'flow',
      caption: '자동 완성이 새는 경로',
      steps: [
        { label: '입력란 삽입', note: '보이지 않게' },
        { label: '자동 완성 채움', note: '브라우저' },
        { label: '함께 전송', note: '사용자 모름' },
        { label: '값 수집', note: '공격자에게', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '자동 완성 판단',
      x: ['재사용 값', '일회성 값'],
      y: ['켜기', '끄기'],
      cells: ['비밀번호', '해당 없음', '해당 없음', '일회용 코드'],
    },
  },
  {
    slug: 'referrer-leak',
    title: '이동할 때 따라가는 주소 정보',
    body: `링크를 눌러 다른 사이트로 가면 어디서 왔는지가 함께 전달된다. 그 주소에 식별자나 검색어가 들어 있으면 그대로 외부에 넘어간다.

## 무엇이 새나

| 내용 | 예 |
| --- | --- |
| 경로 | 화면 구조 |
| 질의 문자열 | 검색어·식별자 |
| 조각 | 전달 안 됨 |
| 내부 주소 | 구조 노출 |

검색 결과 화면에서 외부 링크를 누르면 검색어가 그쪽 기록에 남는다. 검색어가 개인정보인 경우가 있다.

![주소가 전달되는 경로](/img/posts/referrer-leak.svg)

## 보안 헤더로 어떻게 통제하나

| 설정 | 전달 범위 |
| --- | --- |
| 전달 안 함 | 없음 |
| 출처만 | 도메인까지 |
| 같은 출처만 | 내부만 |
| 기본값 | 상황에 따라 |

기본값은 브라우저마다 다르고 시간이 지나며 바뀐다. 명시적으로 지정해 두는 편이 예측 가능하다.

## 어디에 적용하나

| 범위 | 방법 |
| --- | --- |
| 사이트 전체 | 헤더 |
| 특정 페이지 | 메타 태그 |
| 개별 링크 | 속성 |
| 이미지·요청 | 헤더 |

전체에 안전한 기본값을 두고 필요한 곳만 완화하는 순서가 관리하기 쉽다. 반대로 하면 빠지는 곳이 생긴다.

![정책 적용 범위](/img/posts/referrer-leak-2.svg)

## 지금 설정을 본다

\`\`\`bash
# 정책 헤더 확인
curl -sI https://example.com | grep -i 'referrer-policy'

# 주소에 식별자가 들어가는 화면이 있는지
grep -rInE "navigate\\(['\\\"]?/[a-z/]*\\$\\{" --include='*.tsx' src/ 2>/dev/null | head
\`\`\`

헤더가 없으면 브라우저 기본값이 적용된다. 민감한 값이 주소에 들어가는 화면이 있으면 그것부터 바꾼다.

## 주소에 담지 않는 것이 먼저다

정책으로 전달을 막아도 그 주소는 여전히 서버 기록과 브라우저 기록에 남는다. 식별자나 검색어를 주소가 아닌 본문으로 옮기면 여러 경로가 한 번에 정리된다. 정책 설정은 그 뒤에 남는 위험을 줄이는 보조 수단으로 보는 편이 맞다. 외부 도구에 넘기는 주소도 같은 기준으로 확인한다. 광고나 분석 스크립트가 주소를 그대로 보내는 경우가 흔하다.

## 참고

- MDN — Referrer-Policy
- OWASP — 정보 노출 방지
- W3C — Referrer Policy 명세`,
    diagram: {
      type: 'flow',
      caption: '주소가 전달되는 경로',
      steps: [
        { label: '검색 결과 화면', note: '주소에 검색어' },
        { label: '외부 링크 클릭', note: '이동' },
        { label: '출처 헤더 전송', note: '전체 주소' },
        { label: '외부 기록에 남음', note: '검색어 포함', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '정책 적용 범위',
      unit: '점',
      items: [
        { label: '전달 안 함', value: 95, note: '가장 좁음' },
        { label: '같은 출처만', value: 80, note: '내부만' },
        { label: '출처만', value: 55, note: '도메인까지' },
        { label: '지정 없음', value: 25, note: '브라우저 기본' },
      ],
    },
  },
  {
    slug: 'tabnabbing-and-target',
    title: '새 창으로 열 때 넘어가는 권한',
    body: `링크를 새 창으로 열면 열린 쪽에서 원래 창을 조작할 수 있는 경우가 있다. 사용자가 다른 탭을 보는 사이 원래 탭이 가짜 로그인 화면으로 바뀌는 방식의 공격이 성립한다.

## 어떻게 되나

| 단계 | 내용 |
| --- | --- |
| 새 창 열기 | 외부 사이트 |
| 참조 획득 | 원래 창 |
| 주소 변경 | 가짜 화면 |
| 사용자 복귀 | 눈치채지 못함 |

원래 탭이 로그인 화면으로 바뀌어 있으면 사용자는 세션이 끊긴 줄 알고 다시 입력한다.

![새 창이 원래 창을 바꾸는 흐름](/img/posts/tabnabbing-and-target.svg)

## 피싱으로 이어지는 것을 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 관계 끊기 속성 | 링크에 |
| 출처 전달 차단 | 함께 |
| 새 브라우저 기본값 | 대부분 적용 |
| 스크립트로 열 때 | 명시 필요 |

최신 브라우저는 기본으로 관계를 끊지만 옛 버전과 스크립트로 여는 경우는 다르다. 명시하는 편이 안전하다.

## 어디를 확인하나

| 위치 | 내용 |
| --- | --- |
| 사용자 입력 링크 | 가장 위험 |
| 외부 링크 | 확인 |
| 내부 링크 | 문제 없음 |
| 스크립트 열기 | 명시 |

사용자가 올린 내용에 링크가 포함되는 서비스라면 반드시 처리해야 한다. 우리가 만든 링크만 있으면 위험이 낮다.

![확인 대상](/img/posts/tabnabbing-and-target-2.svg)

## 코드를 훑는다

\`\`\`bash
# 새 창 링크에 관계 끊기 속성이 있는지
grep -rIoE '<a[^>]*target="_blank"[^>]*>' --include='*.tsx' src/ 2>/dev/null \
  | grep -vc 'noopener' | head

# 스크립트로 창을 여는 지점
grep -rIn 'window.open' --include='*.ts' --include='*.tsx' src/ 2>/dev/null | head
\`\`\`

속성 없이 새 창으로 여는 링크가 있으면 추가한다. 렌더링 단계에서 자동으로 붙이는 설정을 두면 빠지지 않는다.

## 사용자 콘텐츠는 렌더링에서 처리한다

본문에 포함된 링크를 매번 확인할 수는 없으므로, 마크다운이나 HTML을 화면에 그리는 단계에서 외부 링크에 속성을 자동으로 붙인다. 정화 과정에 이 처리를 넣어 두면 새 글이 올라와도 같은 규칙이 적용된다. 외부 링크임을 표시해 주면 사용자도 이동을 인지하게 된다.

## 참고

- OWASP — Reverse Tabnabbing
- MDN — rel 속성
- CWE-1022 — 안전하지 않은 외부 링크`,
    diagram: {
      type: 'flow',
      caption: '새 창이 원래 창을 바꾸는 흐름',
      steps: [
        { label: '외부 링크 클릭', note: '새 창' },
        { label: '원래 창 참조', note: '획득' },
        { label: '주소 변경', note: '가짜 로그인' },
        { label: '자격 증명 입력', note: '탈취', danger: true },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '확인 대상',
      x: ['우리가 만든 링크', '사용자 콘텐츠'],
      y: ['위험', '조치'],
      cells: ['낮음', '높음', '확인', '렌더링에서 자동'],
    },
  },
  {
    slug: 'postmessage-origin-check',
    title: '창 사이 메시지를 받을 때의 검증',
    body: `창이나 프레임 사이에 메시지를 주고받는 기능은 출처를 확인하지 않으면 누구나 보낼 수 있다. 받은 값을 그대로 처리하면 외부 페이지가 우리 기능을 부르게 된다.

## 크로스 사이트 스크립팅처럼 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 출처 | 보낸 도메인 |
| 형식 | 예상한 구조 |
| 내용 | 허용 목록 |
| 창 참조 | 아는 창인가 |

출처 확인이 빠지면 어떤 페이지든 메시지를 보낼 수 있다. 정확히 일치하는 값과 비교해야 하고 포함 여부로 판단하면 우회된다.

![메시지가 위조되는 경로](/img/posts/postmessage-origin-check.svg)

## 보낼 때도 지정한다

| 방식 | 내용 |
| --- | --- |
| 대상 출처 지정 | 정확히 |
| 모든 출처 허용 | 피함 |
| 민감 정보 | 담지 않음 |

받는 쪽 출처를 지정하지 않고 보내면 그 창이 다른 주소로 이동한 뒤에도 값이 전달된다. 항상 명시한다.

## 무엇을 주고받나

| 내용 | 판단 |
| --- | --- |
| 상태 알림 | 적합 |
| 크기 조정 | 적합 |
| 토큰 | 피함 |
| 실행 명령 | 검증 필수 |

메시지로 동작을 지시하는 구조는 허용 목록으로 제한한다. 임의 함수 이름을 받아 실행하는 구현이 실제로 발견된다.

![안전한 처리 순서](/img/posts/postmessage-origin-check-2.svg)

## 코드를 확인한다

\`\`\`bash
# 메시지 수신 처리에 출처 확인이 있는지
grep -rInA5 "addEventListener\\('message'" --include='*.ts' --include='*.tsx' src/ 2>/dev/null \
  | grep -c 'origin'

# 모든 출처로 보내는 지점
grep -rIn "postMessage(.*'\\*'" --include='*.ts' src/ 2>/dev/null | head
\`\`\`

출처 확인이 없는 수신 처리가 있으면 그 자리가 열려 있다. 모든 출처로 보내는 코드도 함께 정리한다.

## 프레임 구조를 줄인다

여러 프레임이 얽혀 메시지를 주고받는 구조는 이해하기 어렵고 검증할 곳도 많아진다. 꼭 필요한 경우가 아니면 서버를 거쳐 상태를 주고받는 편이 단순하고, 그 과정에서 인증과 권한 확인이 자연스럽게 붙는다. 남길 프레임은 격리 속성을 함께 건다.

## 참고

- MDN — Window.postMessage
- OWASP — HTML5 보안 지침
- CWE-346 — 출처 검증 오류`,
    diagram: {
      type: 'flow',
      caption: '메시지가 위조되는 경로',
      steps: [
        { label: '우리 페이지', note: '수신 대기' },
        { label: '외부 페이지', note: '메시지 발송' },
        { label: '출처 미확인', note: '그대로 수용' },
        { label: '기능 실행', note: '외부 조작', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 처리 순서',
      steps: [
        { label: '출처 확인', note: '정확히 일치' },
        { label: '형식 검증', note: '스키마' },
        { label: '허용 목록', note: '동작 제한' },
        { label: '처리', note: '통과한 것만' },
      ],
    },
  },
  {
    slug: 'service-worker-scope',
    title: '서비스 워커가 가진 캐시 권한',
    body: `서비스 워커는 페이지의 모든 요청을 가로채고 응답을 만들어 줄 수 있다. 오프라인 기능을 위한 장치인데, 그만큼 잘못 쓰면 잘못된 내용이 오래 남는다.

## 무엇을 할 수 있나

| 동작 | 설명 |
| --- | --- |
| 요청 가로채기 | 전부 |
| 응답 생성 | 캐시에서 |
| 백그라운드 실행 | 페이지 없이 |
| 알림 발송 | 권한 시 |

한 번 등록되면 페이지를 닫아도 남아 있다. 잘못된 버전이 배포되면 사용자가 계속 그 코드를 쓰게 된다.

![서비스 워커의 위치](/img/posts/service-worker-scope.svg)

## 캐시 오염을 무엇으로 조심하나

| 항목 | 내용 |
| --- | --- |
| 캐시 범위 | 인증 응답 제외 |
| 갱신 방식 | 새 버전 적용 |
| 복구 경로 | 해제 방법 |
| 범위 | 경로 제한 |

인증이 필요한 응답을 캐시하면 로그아웃 후에도 내용이 남는다. 캐시 대상을 명시적으로 정해야 한다.

## 어떻게 갱신하나

| 방법 | 내용 |
| --- | --- |
| 버전 표시 | 파일 변경 |
| 즉시 적용 | 대기 건너뜀 |
| 안내 후 적용 | 사용자 확인 |
| 강제 해제 | 비상 |

문제가 생겼을 때 해제할 방법을 미리 만들어 두어야 한다. 이것이 없으면 사용자 브라우저에서 옛 코드를 걷어낼 수 없다.

![캐시 대상 판단](/img/posts/service-worker-scope-2.svg)

## 등록 상태를 본다

\`\`\`bash
# 서비스 워커 파일과 캐시 대상 확인
grep -rInE 'caches\\.|addAll\\(|fetch\\(' public/sw.js src/sw.ts 2>/dev/null | head -15
\`\`\`

인증이 필요한 경로가 캐시 목록에 있으면 제외한다. 정적 자원만 캐시하는 구성이 가장 안전하다.

## 비상 해제를 준비한다

잘못된 워커가 배포되면 사용자가 새 코드를 받지 못하는 상태가 된다. 등록을 해제하고 캐시를 비우는 최소한의 워커를 미리 준비해 두면, 그 파일로 교체해 정상 상태로 되돌릴 수 있다. 이 준비가 없으면 사용자에게 브라우저 설정에서 지우라고 안내하는 수밖에 없다. 배포 전에 그 해제 파일이 동작하는지도 확인해 둔다.

## 참고

- MDN — Service Worker API
- OWASP — 클라이언트 저장소 보안
- W3C — Service Workers 명세`,
    diagram: {
      type: 'flow',
      caption: '서비스 워커의 위치',
      steps: [
        { label: '페이지 요청', note: '모든 요청' },
        { label: '워커 가로채기', note: '중간에서' },
        { label: '캐시 또는 네트워크', note: '판단' },
        { label: '응답', note: '워커가 결정' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '캐시 대상 판단',
      x: ['정적 자원', '인증 응답'],
      y: ['캐시', '제외'],
      cells: ['적합', '위험', '불필요', '필수'],
    },
  },
  {
    slug: 'client-error-reporting',
    title: '오류 보고에 담겨 나가는 것',
    body: `화면에서 발생한 오류를 자동으로 수집하면 문제를 빨리 찾을 수 있다. 그런데 그 보고에 화면의 입력값이나 요청 내용이 함께 담겨 외부 서비스로 나가는 경우가 많다.

## 무엇이 담기나

| 항목 | 내용 |
| --- | --- |
| 오류 메시지 | 값 포함 가능 |
| 요청 정보 | 헤더·본문 |
| 화면 상태 | 입력값 |
| 사용자 정보 | 식별자 |
| 화면 기록 | 재생 기능 |

화면을 그대로 재생하는 기능을 켜면 사용자가 입력한 모든 것이 외부로 간다. 편리한 만큼 노출 범위가 크다.

![오류 보고가 나가는 경로](/img/posts/client-error-reporting.svg)

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 필드 가림 | 전송 전 |
| 요청 본문 제외 | 기본 |
| 식별자 치환 | 내부 번호 |
| 화면 재생 제한 | 민감 화면 제외 |

전송 직전에 거르는 처리를 두면 새 화면이 생겨도 적용된다. 각 화면에서 설정하는 방식은 반드시 빠진다.

## 어디를 제외하나

| 화면 | 판단 |
| --- | --- |
| 로그인 | 제외 |
| 결제 | 제외 |
| 개인정보 입력 | 제외 |
| 일반 조회 | 포함 |

민감한 화면을 통째로 제외하는 방식이 단순하고 확실하다. 그 화면의 오류는 다른 방법으로 확인한다.

![가리는 지점](/img/posts/client-error-reporting-2.svg)

## 실제 전송을 본다

\`\`\`bash
# 수집 도구 설정에서 가림 처리가 켜져 있는지
grep -rInE 'maskAll|scrub|beforeSend|denyUrls' --include='*.ts' src/ 2>/dev/null | head

# 실제 전송 내용은 브라우저 네트워크 탭에서 수집 요청을 직접 확인한다
\`\`\`

설정만 보고 안심하지 말고 실제 전송 내용을 한 번 확인한다. 기본값이 생각보다 많이 담는 도구가 있다.

## 보관 기간과 접근 권한도 본다

수집된 자료는 외부 서비스에 쌓이고 팀원 다수가 볼 수 있다. 보관 기간을 짧게 잡고 접근 권한을 필요한 사람으로 제한하면 노출 면적이 줄어든다. 개인정보가 포함될 가능성이 있다면 그 서비스가 위탁 대상인지도 함께 확인해야 한다. 처리방침에 적을 대상인지도 같은 자리에서 판단한다. 수집 범위를 바꿀 때마다 그 판단을 다시 하는 절차를 둔다.

## 참고

- 개인정보보호법 — 제3자 제공
- OWASP — 로깅 지침
- 각 오류 수집 도구 개인정보 설정 문서`,
    diagram: {
      type: 'flow',
      caption: '오류 보고가 나가는 경로',
      steps: [
        { label: '화면 오류', note: '발생' },
        { label: '자동 수집', note: '상태 포함' },
        { label: '외부 전송', note: '수집 서비스' },
        { label: '외부에 축적', note: '넓은 접근', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '가리는 지점',
      layers: [
        { label: '전송 직전', note: '일괄 처리' },
        { label: '요청 본문', note: '기본 제외' },
        { label: '민감 화면', note: '통째로 제외' },
        { label: '보관 기간', note: '짧게' },
      ],
    },
  },
]
