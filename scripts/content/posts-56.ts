import type { SeedPost } from './types'

/** 550~559 — 주소·헤더를 믿는 자리 */
export const posts56: SeedPost[] = [
  {
    slug: 'url-parser-mismatch',
    title: '주소 해석이 갈리면 검증이 뚫린다',
    body: `같은 주소 문자열을 두 라이브러리가 다르게 읽는 일이 있다. 앞단이 읽은 호스트와 뒷단이 실제로 접속하는 호스트가 다르면, 검증은 통과하고 요청은 엉뚱한 곳으로 간다.

## 어디서 갈리나?

규격이 여러 겹이고 구현마다 관대함이 다르다. 관대한 파서와 엄격한 파서를 한 흐름에 섞어 쓰면 그 틈이 생긴다.

| 입력 | 어떤 파서는 | 다른 파서는 |
| --- | --- | --- |
| 사용자 정보가 붙은 주소 | 호스트를 뒤로 봄 | 앞을 호스트로 봄 |
| 백슬래시 섞인 구분자 | 경로로 취급 | 호스트 구분자로 취급 |
| 중복 슬래시 | 정규화 | 그대로 |
| 제어문자 포함 | 거절 | 무시하고 계속 |
| 괄호 표기 주소 | 지원 | 오류 |

같은 문자열을 두 번 읽지 않는 것이 첫 번째 원칙이다. 한 번 읽어 구조를 만들고 그 구조를 넘긴다.

![앞단과 뒷단의 해석 차이](/img/posts/url-parser-mismatch.svg)

## 접근 통제에서 무엇이 무너지나

주소 해석은 대개 접근 통제의 입력이다. 그 입력이 흔들리면 통제 전체가 흔들린다.

| 자리 | 결과 |
| --- | --- |
| 외부 요청 허용 목록 | 내부 주소로 접속 |
| 리다이렉트 검증 | 외부로 전달 |
| 캐시 키 | 다른 응답이 섞임 |
| 접근 로그 | 실제와 다른 값 기록 |

로그에 기록된 주소와 실제 접속한 주소가 다르면 사고 조사 자체가 어긋난다. 이 피해는 나중에야 드러난다.

## 어떻게 맞추나

| 방법 | 효과 |
| --- | --- |
| 한 번만 파싱 | 차이가 생길 자리 제거 |
| 같은 라이브러리 사용 | 해석 통일 |
| 파싱 결과를 넘김 | 문자열 재해석 금지 |
| 엄격 모드 사용 | 이상한 입력 거절 |

![해석을 통일하는 방법](/img/posts/url-parser-mismatch-2.svg)

## 실제로 어떻게 읽는지 본다

쓰는 라이브러리들이 같은 값을 같게 읽는지 확인한다.

\`\`\`bash
python3 - <<'PY'
from urllib.parse import urlsplit
cases = [
  'https://ours.example@evil.example/',
  'https://evil.example\\@ours.example/',
  'https://ours.example:80@evil.example/',
  'http://ours.example\t.evil.example/',
]
for c in cases:
    print(f'{c!r:50} → host={urlsplit(c).hostname!r}')
PY
\`\`\`

같은 문자열을 다른 언어의 파서에도 넣어 비교한다. 결과가 하나라도 다르면 그 조합을 한 흐름에 쓰지 않는다.

## 문자열 비교로 판단하지 않는다

주소를 문자열로 비교하는 검증은 구현 차이와 무관하게 언제나 샌다. 파싱해서 호스트를 꺼내고, 그 값을 허용 목록과 완전히 일치하는지 본다. 접두사 비교나 포함 여부 검사는 이 자리에서 쓸 수단이 아니다.

## 참고

- WHATWG URL Standard — 파싱 규칙
- RFC 3986 — URI 일반 문법
- OWASP — SSRF 방어 Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '앞단과 뒷단의 해석 차이',
      steps: [
        { label: '주소 문자열 수신', note: '사용자 입력' },
        { label: '앞단이 파싱·검증', note: '호스트 A 로 읽음' },
        { label: '문자열을 그대로 전달', note: '다시 파싱', danger: true },
        { label: '뒷단이 호스트 B 로 접속', note: '검증 우회' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '해석을 통일하는 방법',
      steps: [
        { label: '한 번만 파싱', note: '차이 제거' },
        { label: '구조를 넘김', note: '문자열 아님' },
        { label: '같은 라이브러리', note: '흐름 전체' },
        { label: '엄격 모드', note: '이상한 입력 거절' },
      ],
    },
  },
  {
    slug: 'ip-address-parsing',
    title: 'IP 주소 표기를 믿을 때 생기는 일',
    body: `IP 주소는 점 넷으로 적힌 숫자처럼 보이지만 표기 방법이 여러 가지다. 같은 주소를 여러 모양으로 쓸 수 있고, 차단 목록이 그중 한 모양만 알고 있으면 나머지로 지나간다.

## 같은 주소의 다른 모양

내부 대역을 막았다고 믿는 검증이 아래 표기들 앞에서 무너진다.

| 표기 | 실제 |
| --- | --- |
| 십진수 한 덩어리 | 같은 주소 |
| 8진수 표기 | 같은 주소 |
| 16진수 표기 | 같은 주소 |
| 자리 생략 표기 | 같은 주소 |
| IPv6 에 IPv4 를 담은 형태 | 같은 주소 |

문자열로 내부 대역을 막는 검증은 이 목록 전부를 놓친다. 숫자로 바꿔 대역과 비교해야 한다.

![같은 주소의 여러 표기](/img/posts/ip-address-parsing.svg)

## ssrf 에서 이름은 왜 더 위험한가

주소 대신 이름이 오면 문제가 더 커진다. 이름은 해석할 때마다 다른 주소를 돌려줄 수 있다.

| 상황 | 문제 |
| --- | --- |
| 이름이 내부 주소를 가리킴 | 허용 목록 무의미 |
| 검사 후 해석 값이 바뀜 | 검사와 접속이 다름 |
| 여러 주소를 돌려줌 | 하나만 검사 |

검사한 뒤 다시 해석하면 그 사이에 값이 바뀔 수 있다. 검사에 쓴 주소로 직접 접속하는 구조여야 이 틈이 닫힌다.

## 무엇을 막아야 하나

| 대역 | 이유 |
| --- | --- |
| 사설 대역 | 내부망 접근 |
| 루프백 | 로컬 서비스 |
| 링크 로컬 | 클라우드 메타데이터 |
| 특수 목적 대역 | 예기치 않은 동작 |

링크 로컬이 특히 중요하다. 클라우드에서 임시 자격 증명을 내주는 주소가 이 대역에 있다.

![막아야 하는 대역](/img/posts/ip-address-parsing-2.svg)

## 검증이 실제로 되는지 본다

여러 표기를 넣어 같은 결과가 나오는지 확인한다.

\`\`\`bash
python3 - <<'PY'
import ipaddress
forms = ['127.0.0.1', '2130706433', '0177.0.0.1', '0x7f.1', '::ffff:127.0.0.1', '127.1']
for f in forms:
    try:
        ip = ipaddress.ip_address(int(f)) if f.isdigit() else ipaddress.ip_address(f)
        print(f'{f:22} → {ip} · 사설/루프백 {ip.is_private or ip.is_loopback}')
    except ValueError as e:
        print(f'{f:22} → 파싱 실패 ({e})')
PY
\`\`\`

파싱 실패로 나오는 표기도 어떤 라이브러리에서는 통과한다. 우리가 쓰는 라이브러리로 같은 시험을 해 보는 것이 핵심이다.

## 막는 위치를 바꾼다

애플리케이션에서 완벽하게 거르기는 어렵다. 나가는 요청을 전용 프록시로 모으고 그 프록시에서 대역을 막으면, 애플리케이션의 검증이 새더라도 한 겹이 남는다. 검증과 통제를 같은 층에 두지 않는 것이 이 문제에서는 특히 값어치가 있다.

## 참고

- OWASP — SSRF Prevention Cheat Sheet
- RFC 5735 — 특수 목적 IPv4 대역
- CWE-918 — Server-Side Request Forgery`,
    diagram: {
      type: 'layers',
      caption: '같은 주소의 여러 표기',
      layers: [
        { label: '점 넷 표기', note: '흔히 막는 형태' },
        { label: '십진·8진·16진', note: '같은 주소' },
        { label: '자리 생략', note: '같은 주소' },
        { label: 'IPv6 에 담은 IPv4', note: '같은 주소' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '막아야 하는 대역',
      steps: [
        { label: '사설 대역', note: '내부망' },
        { label: '루프백', note: '로컬 서비스' },
        { label: '링크 로컬', note: '메타데이터' },
        { label: '특수 목적', note: '예기치 않은 동작' },
      ],
    },
  },
  {
    slug: 'hostname-validation',
    title: '호스트 이름을 검증하는 올바른 순서',
    body: `허용할 도메인을 정해 두는 것은 좋은 접근 통제다. 문제는 들어온 값이 그 도메인인지 판단하는 방법이다. 대부분의 구현이 문자열을 보고, 그래서 대부분이 샌다.

## 왜 문자열 비교로는 안 되나?

| 방법 | 통과하는 공격 |
| --- | --- |
| 포함 여부 검사 | evil.example/ours.example |
| 접두사 비교 | ours.example.evil.example |
| 접미사 비교 | notours.example |
| 정규식 | 점을 이스케이프하지 않으면 뚫림 |

접미사 비교는 안전해 보이지만 앞에 다른 글자가 붙는 경우를 놓친다. 점까지 포함해 비교해야 하는데 그 한 글자를 빠뜨리는 구현이 많다.

![문자열 비교가 새는 자리](/img/posts/hostname-validation.svg)

## 접근 통제에 쓸 올바른 순서

| 단계 | 내용 |
| --- | --- |
| 1 | 주소를 파싱해 호스트만 꺼낸다 |
| 2 | 소문자로 맞추고 끝점을 제거 |
| 3 | 국제화 도메인을 표준형으로 |
| 4 | 허용 목록과 완전 일치 비교 |
| 5 | 하위 도메인은 점을 포함해 비교 |

3단계를 빠뜨리면 겉보기에 같은 글자가 다른 도메인으로 통과한다. 표기를 통일한 뒤 비교해야 한다.

## 하위 도메인을 허용할 때

하위 도메인 전체를 허용하는 규칙은 신중해야 한다. 누구나 하위 도메인을 만들 수 있는 서비스라면 그 규칙이 곧 전면 개방이다.

| 규칙 | 위험 |
| --- | --- |
| 특정 호스트만 | 가장 안전 |
| 우리 하위 도메인 | 방치된 하위 도메인 주의 |
| 임의 하위 도메인 | 사실상 개방 |

![허용 범위와 위험](/img/posts/hostname-validation-2.svg)

## 검증을 시험한다

허용 목록을 우회하는 흔한 모양을 넣어 본다.

\`\`\`bash
for h in "ours.example" "ours.example.evil.example" "notours.example" \\
         "OURS.EXAMPLE" "ours.example." "evil.example/ours.example"; do
  code=$(curl -s -o /dev/null -w '%{http_code}' -G https://example.com/api/fetch \\
    --data-urlencode "target=https://$h/")
  printf '%-34s %s\\n' "$h" "$code"
done
\`\`\`

두 번째와 세 번째가 통과하면 접두사·접미사 비교를 쓰고 있는 것이다. 네 번째와 다섯 번째가 거절되면 정규화는 되고 있다.

## 방치된 하위 도메인

우리 도메인 전체를 허용했는데 쓰지 않는 하위 도메인이 외부 서비스를 가리킨 채 남아 있으면, 그 서비스를 가져간 사람이 우리 허용 목록 안에 들어온다. 하위 도메인 허용 규칙을 쓰려면 DNS 레코드 정리가 함께 돌아야 한다. 규칙과 목록 관리는 한 쌍이다.

## 참고

- OWASP — SSRF Prevention Cheat Sheet
- RFC 5890 — 국제화 도메인 이름
- CWE-20 — 부적절한 입력 검증`,
    diagram: {
      type: 'matrix',
      caption: '비교 방식과 결과',
      x: ['문자열 비교', '파싱 후 완전 일치'],
      y: ['하위 도메인 허용', '특정 호스트만'],
      cells: ['뚫림', '방치 도메인 주의', '뚫림', '안전'],
    },
    diagram2: {
      type: 'steps',
      caption: '검증 순서',
      steps: [
        { label: '파싱해 호스트 추출', note: '문자열 아님' },
        { label: '표기 통일', note: '소문자·표준형' },
        { label: '완전 일치 비교', note: '허용 목록' },
        { label: '하위는 점 포함', note: '앞글자 차단' },
      ],
    },
  },
  {
    slug: 'punycode-confusion',
    title: '비슷하게 보이는 도메인을 가려내는 법',
    body: `국제화 도메인은 여러 문자 체계를 쓸 수 있게 해 주었고, 그 결과 눈으로는 구분되지 않는 도메인을 만들 수 있게 됐다. 사람이 보기에 같은 글자가 전혀 다른 도메인이 된다.

## 무엇이 문제인가?

라틴 문자와 비슷하게 생긴 글자가 다른 문자 체계에 여럿 있다. 그중 하나만 바꿔도 다른 도메인이 되지만 화면에서는 같아 보인다.

| 상황 | 결과 |
| --- | --- |
| 피싱 메일의 링크 | 진짜 도메인으로 보임 |
| 허용 목록 우회 | 비교는 다르고 눈은 같음 |
| 인증서 발급 | 정상 발급됨 |
| 로그 확인 | 사람이 구분 못 함 |

인증서가 정상 발급된다는 점이 특히 나쁘다. 자물쇠 표시가 보이므로 사용자는 안전하다고 판단한다.

![눈과 코드가 다르게 보는 것](/img/posts/punycode-confusion.svg)

## 브라우저는 어떻게 다루나

브라우저마다 규칙이 다르지만, 한 도메인에 여러 문자 체계가 섞여 있으면 원래 부호화 형태로 보여 주는 방식이 흔하다. 다만 한 체계 안에서 비슷한 글자를 고른 경우는 그대로 표시된다.

| 상황 | 표시 |
| --- | --- |
| 체계가 섞임 | 부호화 형태로 |
| 한 체계 안에서 유사 | 그대로 |
| 알려진 위험 조합 | 부호화 형태로 |

브라우저에 기대는 것은 방어가 아니다. 우리 쪽에서 판단해야 한다.

## 어떻게 가려내나

| 방법 | 효과 |
| --- | --- |
| 부호화 형태로 비교 | 눈속임 제거 |
| 문자 체계 혼합 검사 | 대표적 경우 차단 |
| 유사 글자 대응표 | 정밀하지만 유지 부담 |
| 허용 목록 | 가장 확실 |

우리가 쓰는 도메인은 정해져 있으므로 허용 목록이 가장 값싸다. 목록 밖은 전부 외부로 취급하면 이 문제 자체가 줄어든다.

![가려내는 방법](/img/posts/punycode-confusion-2.svg)

## 직접 확인한다

들어온 도메인이 부호화 형태로 무엇인지 본다.

\`\`\`bash
python3 - <<'PY'
import unicodedata
names = ['аpple.com', 'apple.com', 'gооgle.com']   # 일부 글자가 라틴이 아님
for n in names:
    enc = n.encode('idna', errors='replace') if n.isascii() else n.encode('idna', 'ignore')
    scripts = {unicodedata.name(ch, '?').split()[0] for ch in n if ch.isalpha()}
    print(f'{n} · 문자 체계 {scripts} · 순수 ASCII {n.isascii()}')
PY
\`\`\`

문자 체계가 둘 이상 섞여 있으면 사람에게 보여 줄 때 부호화 형태로 바꿔 주는 편이 안전하다.

## 사용자에게 보여 줄 때

외부 링크를 화면에 표시할 때 도메인을 그대로 쓰지 말고, 우리가 검증한 형태로 바꿔 보여 준다. 메일 본문이나 댓글에 들어온 링크는 특히 그렇다. 무엇을 보여 줄지 우리가 정하면 눈속임이 사용자에게 도달하기 전에 끊긴다.

## 참고

- Unicode Technical Report #36 — 보안 고려사항
- RFC 5890 — 국제화 도메인 이름
- OWASP — 피싱 방어 실무`,
    diagram: {
      type: 'flow',
      caption: '눈과 코드가 다르게 보는 것',
      steps: [
        { label: '유사 글자로 도메인 생성', note: '다른 문자 체계' },
        { label: '인증서 정상 발급', note: '자물쇠 표시' },
        { label: '사용자가 진짜로 인식', note: '구분 불가', danger: true },
        { label: '자격 증명 입력', note: '피싱 성공' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '방법별 효과',
      unit: '점',
      items: [
        { label: '허용 목록', value: 95, note: '가장 확실' },
        { label: '부호화 비교', value: 80, note: '눈속임 제거' },
        { label: '체계 혼합 검사', value: 60, note: '대표 경우' },
        { label: '브라우저에 기대기', value: 20, note: '방어 아님' },
      ],
    },
  },
  {
    slug: 'referer-header-trust',
    title: '출처 헤더를 접근 통제에 쓰면 안 되는 이유',
    body: `어디서 온 요청인지 알려 주는 헤더가 있다. 편리해 보여서 접근 통제에 쓰는 구현이 있는데, 그 값은 요청하는 쪽이 정하고 여러 이유로 아예 오지 않기도 한다.

## 왜 믿을 수 없나?

| 이유 | 설명 |
| --- | --- |
| 보내는 쪽이 정함 | 임의 값 가능 |
| 정책에 따라 생략 | 정상 요청인데 없음 |
| 확장·프록시가 제거 | 환경에 따라 다름 |
| 사생활 설정 | 축약되거나 없음 |

없을 때 통과시키면 헤더를 지우는 것으로 우회되고, 없을 때 막으면 정상 사용자가 걸린다. 어느 쪽으로 정해도 문제가 남는다.

![헤더가 오지 않는 경우들](/img/posts/referer-header-trust.svg)

## 그럼 무엇을 쓰나

목적에 따라 다른 수단이 있다. 접근 통제에는 요청하는 쪽이 정할 수 없는 값을 쓴다.

| 목적 | 올바른 수단 |
| --- | --- |
| 요청 위조 방지 | 토큰 또는 쿠키 속성 |
| 다른 출처 차단 | 출처 헤더 + 서버 판단 |
| 링크 도용 방지 | 서명된 주소 |
| 유입 분석 | 참고용으로만 |

링크 도용을 막을 때는 서명된 주소가 확실하다. 유효 기간과 대상을 서명에 담으면 주소만으로 판단이 끝난다.

## 분석 용도로만 쓸 때의 주의

분석에 쓰는 것은 문제가 없지만, 그 값에 개인정보가 섞여 들어올 수 있다. 다른 사이트의 주소에 검색어나 식별자가 들어 있으면 그것이 우리 로그에 쌓인다.

| 항목 | 처리 |
| --- | --- |
| 질의 문자열 | 잘라내고 저장 |
| 경로 | 필요할 때만 |
| 호스트 | 대개 이것으로 충분 |

![무엇까지 저장할지](/img/posts/referer-header-trust-2.svg)

## 지금 어디에 쓰고 있는지 찾는다

\`\`\`bash
# 코드에서 이 헤더로 판단하는 자리
grep -rniE 'referer|referrer' --include='*.{ts,js,py,go,java}' . \\
  | grep -viE 'referrerpolicy|no-referrer' | grep -v node_modules | head -20

# 헤더를 빼고 요청했을 때 통과하는지
curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/api/internal
curl -s -o /dev/null -w '%{http_code}\\n' -H 'Referer: https://evil.example/' https://example.com/api/internal
\`\`\`

두 요청의 결과가 다르면 그 헤더로 판단하고 있다는 뜻이다. 그 자리는 다른 수단으로 바꿔야 한다.

## 내보낼 때도 정한다

우리 사이트에서 다른 곳으로 나갈 때 무엇을 알려 줄지도 정해야 한다. 기본값은 경로까지 보내는 경우가 많아, 내부 주소 구조가 외부 사이트 로그에 쌓인다. 정책 헤더를 둬서 호스트까지만 보내거나 아예 보내지 않게 하면 그 유출이 닫힌다.

## 참고

- MDN — Referrer-Policy
- OWASP — CSRF Prevention Cheat Sheet
- RFC 9110 — Referer 필드의 정의`,
    diagram: {
      type: 'layers',
      caption: '헤더가 오지 않는 경우',
      layers: [
        { label: '정책으로 생략', note: '정상 요청' },
        { label: '프록시가 제거', note: '환경 차이' },
        { label: '확장이 차단', note: '사용자 설정' },
        { label: '공격자가 위조', note: '임의 값' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '없을 때 어떻게 할 것인가',
      x: ['통과시킴', '차단함'],
      y: ['보안 목적', '분석 목적'],
      cells: ['우회 가능', '정상 사용자 차단', '문제 없음', '데이터 손실'],
    },
  },
  {
    slug: 'origin-header-trust',
    title: '출처 값을 서버가 판단해야 하는 이유',
    body: `다른 출처에서 온 요청인지 알려 주는 값이 있다. 이것은 브라우저가 붙이고 스크립트가 바꿀 수 없어 앞서 다룬 헤더보다 믿을 만하다. 다만 믿을 수 있는 범위가 어디까지인지는 분명히 해야 한다.

## 무엇이 다른가?

| 항목 | 출처 값 | 참조 주소 |
| --- | --- | --- |
| 브라우저가 강제 | 그렇다 | 정책에 따라 |
| 스크립트로 변경 | 불가 | 불가 |
| 생략 가능성 | 일부 요청에서 | 흔함 |
| 담긴 정보 | 출처만 | 전체 경로 |

브라우저에서 온 요청에 한해 믿을 수 있다는 점이 핵심이다. 브라우저가 아닌 클라이언트는 무엇이든 보낼 수 있다.

![어디까지 믿을 수 있나](/img/posts/origin-header-trust.svg)

## 접근 통제에서 쓰는 방법

서버가 목록과 대조해 판단하고, 그 결과를 응답 헤더로 알려 준다. 들어온 값을 그대로 되돌려 주는 구현이 흔한데 그것은 모든 출처를 허용하는 것과 같다.

| 구현 | 결과 |
| --- | --- |
| 목록과 대조 후 반환 | 안전 |
| 들어온 값을 그대로 반환 | 전면 개방 |
| 와일드카드 | 자격 증명 동반 요청에서 거절됨 |
| 여러 값 나열 | 규격 위반, 동작 안 함 |

자격 증명을 함께 보내는 요청에서는 와일드카드가 거절되므로, 실제로는 목록 대조 외에 선택지가 없다.

## 생략되는 경우

같은 출처의 단순한 요청에는 이 값이 붙지 않는다. 없을 때 어떻게 할지 정해 두어야 한다.

| 상황 | 판단 |
| --- | --- |
| 같은 출처 요청 | 값 없음, 허용 |
| 브라우저 외 클라이언트 | 값 없음, 다른 수단으로 |
| 다른 출처 | 값 있음, 대조 |

![판단 흐름](/img/posts/origin-header-trust-2.svg)

## 지금 어떻게 답하는지 본다

임의 출처를 보내고 응답이 그것을 되돌려 주는지 확인한다.

\`\`\`bash
# 들어온 값을 그대로 돌려주면 전면 개방이다
curl -s -D - -o /dev/null https://example.com/api/me \\
  -H 'Origin: https://evil.example' | grep -i 'access-control-allow'

# 자격 증명 허용까지 함께 켜져 있는지
curl -s -D - -o /dev/null https://example.com/api/me \\
  -H 'Origin: https://evil.example' | grep -i 'allow-credentials'
\`\`\`

두 헤더가 함께 나오면 임의 사이트가 로그인한 사용자의 데이터를 읽을 수 있다. 가장 먼저 고쳐야 할 설정이다.

## 목록을 어디에 두나

허용 출처 목록을 코드에 흩어 두면 새 엔드포인트마다 실수가 생긴다. 한 곳에서 정하고 모든 응답이 그것을 거치게 한다. 목록이 한 곳이면 검토도 한 번으로 끝난다.

## 참고

- Fetch Standard — CORS 프로토콜
- OWASP — CORS 설정 오류
- MDN — Access-Control-Allow-Origin`,
    diagram: {
      type: 'matrix',
      caption: '어디까지 믿을 수 있나',
      x: ['브라우저 요청', '그 외 클라이언트'],
      y: ['값 있음', '값 없음'],
      cells: ['대조해 판단', '다른 수단 필요', '같은 출처', '다른 수단 필요'],
    },
    diagram2: {
      type: 'flow',
      caption: '판단 흐름',
      steps: [
        { label: '출처 값 수신', note: '브라우저가 붙임' },
        { label: '허용 목록과 대조', note: '완전 일치' },
        { label: '일치하면 그 값 반환', note: '되돌리기 아님' },
        { label: '아니면 거절', note: '기본은 거절' },
      ],
    },
  },
  {
    slug: 'forwarded-header-spoof',
    title: '전달된 출발지 주소를 믿는 자리',
    body: `프록시 뒤에 있는 서버는 원래 접속한 주소를 헤더로 전달받는다. 그 헤더는 앞단이 붙이는 것이지만, 클라이언트가 직접 보낼 수도 있다. 구분하지 않으면 출발지를 마음대로 바꿀 수 있게 된다.

## 무엇이 뒤집히나

출발지 주소는 여러 판단의 근거로 쓰인다. 그 값이 위조되면 판단도 함께 뒤집힌다.

| 판단 | 위조했을 때 |
| --- | --- |
| 지역 차단 | 다른 나라로 위장 |
| 요청 제한 | 계속 새 주소로 보임 |
| 관리 경로 접근 제한 | 내부 주소로 위장 |
| 접속 기록 | 조사 자체가 어긋남 |

관리 경로를 내부 주소로만 열어 둔 경우가 특히 위험하다. 헤더 한 줄로 그 제한이 사라진다.

![위조된 주소가 통과하는 경로](/img/posts/forwarded-header-spoof.svg)

## 어떻게 구분하나

헤더에는 여러 주소가 쌓인다. 앞쪽은 클라이언트가 넣었을 수 있고 뒤쪽은 우리 프록시가 붙인 것이다. 신뢰할 프록시를 정하고 거기서부터 거꾸로 세는 것이 규칙이다.

| 방법 | 안전성 |
| --- | --- |
| 첫 값 사용 | 위조됨 |
| 마지막 값 사용 | 프록시 주소만 나옴 |
| 신뢰 프록시 수만큼 건너뛰기 | 안전 |
| 앞단에서 덮어쓰기 | 가장 확실 |

가장 확실한 것은 우리 프록시가 들어온 헤더를 버리고 자기가 본 주소로 덮어쓰는 것이다. 그러면 뒷단은 고민할 필요가 없다.

## 신뢰 경계를 어디에 두나

| 구성 | 신뢰할 대상 |
| --- | --- |
| CDN → 우리 프록시 → 앱 | CDN 이 붙인 값 |
| 로드밸런서 → 앱 | 로드밸런서 |
| 직접 노출 | 헤더 전부 무시 |

직접 노출된 서버가 이 헤더를 읽으면 안 된다. 앞에 아무도 없으므로 그 값은 전부 클라이언트가 넣은 것이다.

![신뢰 경계](/img/posts/forwarded-header-spoof-2.svg)

## 위조가 통하는지 시험한다

\`\`\`bash
# 내부 주소로 위장해 관리 경로에 접근되는지
curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/admin \\
  -H 'X-Forwarded-For: 10.0.0.1'

# 응답이나 로그에 그 값이 그대로 반영되는지
curl -s https://example.com/api/whoami -H 'X-Forwarded-For: 1.2.3.4' | head -c 200
\`\`\`

첫 요청이 통과하면 위조가 통하는 것이다. 두 번째에서 우리가 보낸 값이 그대로 보이면 앞단이 덮어쓰지 않고 있다는 뜻이다.

## 기록에 미치는 영향

접속 기록의 주소가 위조 가능하면 사고 조사에서 엉뚱한 곳을 보게 된다. 조사는 기록을 믿고 시작하는 일이라, 이 헤더의 신뢰 경계를 정하는 것은 탐지 규칙을 다듬는 것보다 앞선 작업이다.

## 참고

- RFC 7239 — Forwarded 헤더
- OWASP — 신뢰할 수 없는 입력으로서의 헤더
- CWE-348 — 신뢰할 수 없는 출처 정보 사용`,
    diagram: {
      type: 'flow',
      caption: '위조된 주소가 통과하는 경로',
      steps: [
        { label: '클라이언트가 헤더 삽입', note: '내부 주소로' },
        { label: '프록시가 덮어쓰지 않음', note: '그대로 전달', danger: true },
        { label: '앱이 첫 값을 읽음', note: '내부로 판단' },
        { label: '접근 제한 통과', note: '기록도 위조됨' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '신뢰 경계 정하기',
      steps: [
        { label: '앞단을 정한다', note: 'CDN·LB' },
        { label: '들어온 헤더 폐기', note: '앞단에서' },
        { label: '본 주소로 덮어쓰기', note: '한 값만' },
        { label: '뒷단은 그것만 사용', note: '고민 없음' },
      ],
    },
  },
  {
    slug: 'proxy-trust-boundary',
    title: '프록시 뒤에서 무엇을 믿을 것인가',
    body: `요청이 여러 층을 거쳐 도착하면 각 층이 조금씩 값을 바꾼다. 어느 층까지가 우리 것이고 어디부터가 바깥인지 선을 긋지 않으면, 모든 층의 값을 다 믿거나 다 의심하게 된다.

## 층마다 무엇이 바뀌나

| 층 | 바꾸는 것 |
| --- | --- |
| CDN | 출발지·프로토콜 헤더 |
| 로드밸런서 | 연결 정보 |
| 리버스 프록시 | 경로·호스트 |
| 애플리케이션 서버 | 없음 |

호스트를 바꾸는 층이 있으면 애플리케이션이 만드는 절대 주소가 어긋난다. 비밀번호 재설정 링크가 엉뚱한 도메인으로 나가는 사고가 여기서 나온다.

![층마다 바뀌는 값](/img/posts/proxy-trust-boundary.svg)

## 선을 어디에 긋나

우리가 설정을 통제하는 층까지가 안이다. 그 밖에서 온 값은 전부 입력으로 다룬다.

| 구분 | 다루는 법 |
| --- | --- |
| 우리가 설정한 층 | 값을 믿는다 |
| 외부 CDN | 계약과 설정을 확인한 뒤 |
| 클라이언트 | 전부 의심 |

외부 CDN 은 애매한 자리다. 우리가 설정하지만 그 앞으로 누구나 요청을 보낼 수 있으므로, CDN 을 우회한 직접 접근을 막아야 그 값이 뜻을 갖는다.

## 우회 경로를 막는다

| 방법 | 효과 |
| --- | --- |
| 원본 서버 주소 비공개 | 기본 |
| CDN 대역만 허용 | 확실 |
| 공유 비밀 헤더 확인 | 간단하고 효과적 |
| 상호 인증 | 가장 강함 |

공유 비밀 헤더는 구현이 단순하면서 효과가 크다. CDN 이 붙인 값이 없으면 거절하면 된다.

![우회를 막는 방법](/img/posts/proxy-trust-boundary-2.svg)

## 지금 우회되는지 본다

원본 서버에 직접 접근되는지 확인한다.

\`\`\`bash
# 원본 주소를 알고 있다면 직접 두드려 본다
curl -s -o /dev/null -w '%{http_code}\\n' --resolve example.com:443:ORIGIN_IP https://example.com/

# 호스트를 바꿔 보내면 어떤 응답이 오는지
curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/ -H 'Host: evil.example'
\`\`\`

첫 요청이 200 이면 CDN 을 우회한 접근이 열려 있다. 두 번째가 200 이면서 응답 안의 링크가 바뀐 호스트로 나오면 호스트 값을 그대로 쓰고 있는 것이다.

## 절대 주소를 만들 때

메일 링크나 리다이렉트 주소처럼 절대 주소가 필요한 곳은 요청 헤더가 아니라 설정값에서 도메인을 가져온다. 요청에서 가져오면 그 값을 바꾼 요청 하나로 링크가 바뀐다. 설정에 박아 두면 층이 몇 개든 영향을 받지 않는다.

## 참고

- OWASP — Host 헤더 공격
- RFC 7239 — 프록시 정보 전달
- NIST SP 800-53 — 경계 보호`,
    diagram: {
      type: 'layers',
      caption: '층마다 바뀌는 값',
      layers: [
        { label: 'CDN', note: '출발지·프로토콜' },
        { label: '로드밸런서', note: '연결 정보' },
        { label: '리버스 프록시', note: '경로·호스트' },
        { label: '애플리케이션', note: '그대로 받음' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '우회를 막는 방법',
      unit: '점',
      items: [
        { label: '상호 인증', value: 95, note: '가장 강함' },
        { label: '대역 허용', value: 85, note: '확실' },
        { label: '공유 비밀 헤더', value: 75, note: '간단·효과적' },
        { label: '주소 비공개', value: 30, note: '기본일 뿐' },
      ],
    },
  },
  {
    slug: 'cookie-prefix-usage',
    title: '쿠키 이름 접두사가 막아 주는 것',
    body: `쿠키에는 이름 자체로 제약을 거는 방법이 있다. 정해진 접두사를 붙이면 브라우저가 그 쿠키의 속성을 강제하고, 조건을 만족하지 않는 설정을 아예 거절한다. 설정 실수를 브라우저가 대신 막아 주는 셈이다.

## 무엇을 강제하나

| 접두사 | 강제되는 조건 |
| --- | --- |
| 보안 전용 표시 | 암호화 연결에서만 설정 |
| 호스트 고정 표시 | 암호화 연결 + 경로 전체 + 도메인 지정 금지 |

호스트 고정 표시가 더 강하다. 도메인을 지정할 수 없으므로 하위 도메인이 그 쿠키를 만들거나 덮어쓸 수 없다.

![접두사가 거는 제약](/img/posts/cookie-prefix-usage.svg)

## 어떤 공격을 막나

하위 도메인에서 쿠키를 덮어쓰는 공격이 주된 표적이다. 방치된 하위 도메인이나 사용자 콘텐츠를 올리는 하위 도메인이 있으면 실제로 가능한 일이다.

| 공격 | 막히는가 |
| --- | --- |
| 하위 도메인이 세션 쿠키 설정 | 막힘 |
| 암호화되지 않은 연결에서 설정 | 막힘 |
| 경로를 좁혀 혼란 유발 | 막힘 |
| 값 자체의 변조 | 막히지 않음 |

값의 무결성은 별개다. 접두사는 누가 어디서 설정할 수 있는지를 제한할 뿐이다.

## 세션 쿠키에 적용할 때

| 항목 | 설정 |
| --- | --- |
| 이름 | 호스트 고정 접두사 |
| 도메인 | 지정하지 않음 |
| 경로 | 전체 |
| 전송 | 암호화 연결만 |
| 스크립트 접근 | 금지 |
| 교차 사이트 | 제한 |

하위 도메인에서 세션을 공유해야 하는 구조라면 호스트 고정 접두사를 쓸 수 없다. 그때는 공유가 정말 필요한지부터 다시 본다 — 대개는 하나의 인증 도메인을 두는 편이 낫다.

![세션 쿠키 설정](/img/posts/cookie-prefix-usage-2.svg)

## 지금 어떻게 내려오는지 본다

\`\`\`bash
# 로그인 응답의 쿠키 속성을 전부 본다
curl -s -D - -o /dev/null -X POST https://example.com/login \\
  -d 'id=test&pw=test' | grep -i '^set-cookie'
\`\`\`

이름에 접두사가 없고 도메인이 지정돼 있으면, 하위 도메인이 그 쿠키를 덮어쓸 수 있는 상태다. 하위 도메인을 여럿 운영한다면 그 자체가 위험 항목이다.

## 이름을 바꾸는 일의 비용

접두사를 붙이려면 쿠키 이름이 바뀌므로 기존 세션이 전부 끊긴다. 배포 시점에 모든 사용자가 다시 로그인하게 되는데, 그 불편을 감수할 가치가 있는지는 하위 도메인 구조에 달렸다. 하위 도메인이 없고 앞으로도 없을 계획이면 급하지 않다.

## 참고

- RFC 6265bis — 쿠키 접두사
- OWASP — 세션 관리 Cheat Sheet
- MDN — Set-Cookie 속성`,
    diagram: {
      type: 'steps',
      caption: '접두사가 거는 제약',
      steps: [
        { label: '암호화 연결만', note: '평문에서 설정 불가' },
        { label: '경로 전체', note: '좁힐 수 없음' },
        { label: '도메인 지정 금지', note: '하위 도메인 차단' },
        { label: '조건 위반 시 거절', note: '브라우저가 막음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '언제 쓸 수 있나',
      x: ['하위 도메인 공유 불필요', '필요'],
      y: ['하위 도메인 있음', '없음'],
      cells: ['꼭 쓴다', '구조 재검토', '써도 좋다', '급하지 않음'],
    },
  },
  {
    slug: 'cookie-scope-mistake',
    title: '쿠키 범위를 넓게 잡아 생기는 문제',
    body: `쿠키에 도메인을 지정하면 하위 도메인 전체가 그것을 받는다. 편의를 위해 넓게 잡는 경우가 많은데, 그 순간 하위 도메인 하나하나가 세션을 볼 수 있는 자리가 된다.

## 넓게 잡으면 무엇이 열리나

| 범위 | 쿠키를 보는 곳 |
| --- | --- |
| 호스트 고정 | 그 호스트만 |
| 도메인 지정 | 모든 하위 도메인 |

하위 도메인이 마흔 개라면 세션 쿠키를 볼 수 있는 자리가 마흔 곳이다. 그중 하나에 스크립트 삽입 취약점이 있으면 전체 세션이 위험해진다.

![범위에 따라 열리는 곳](/img/posts/cookie-scope-mistake.svg)

## 어떤 하위 도메인이 위험한가

| 성격 | 위험 |
| --- | --- |
| 사용자 콘텐츠 호스팅 | 매우 높음 |
| 외부 서비스 연동 | 높음 |
| 방치된 옛 서비스 | 높음 |
| 사내 도구 | 중간 |
| 정적 자원 전용 | 낮음 |

방치된 하위 도메인이 특히 나쁘다. 아무도 관리하지 않고, 가리키던 외부 서비스를 다른 사람이 가져가면 그 사람이 우리 쿠키 범위 안에 들어온다.

## 좁히는 방법

| 방법 | 효과 |
| --- | --- |
| 도메인 지정 제거 | 호스트 고정 |
| 인증 도메인 분리 | 세션을 한 곳에 |
| 토큰 교환 방식 | 쿠키 공유 없이 |
| 하위 도메인 정리 | 표면 자체 축소 |

하위 도메인에서 로그인 상태가 필요하다면 쿠키를 공유하는 대신, 인증 도메인에서 짧은 수명의 토큰을 받아 쓰는 구조가 낫다. 세션 자체는 한 곳에만 남는다.

![좁히는 방법](/img/posts/cookie-scope-mistake-2.svg)

## 지금 범위를 확인한다

\`\`\`bash
# 도메인 지정이 붙어 있는지
curl -s -D - -o /dev/null https://example.com/ | grep -i '^set-cookie'

# 하위 도메인이 몇 개나 살아 있는지 — 표면의 크기를 먼저 안다
dig +short example.com NS >/dev/null && echo 'DNS 영역에서 레코드 목록을 확인한다'
\`\`\`

도메인 지정이 있고 하위 도메인이 여럿이면, 그 목록을 만드는 것이 첫 작업이다. 무엇이 있는지 모르는 상태에서는 범위를 좁힐 판단도 할 수 없다.

## 정적 자원 도메인을 나누는 이유

사용자가 올린 파일을 본 도메인에서 서빙하면, 그 파일이 스크립트로 해석될 때 쿠키에 닿는다. 별도 도메인에 두면 쿠키 범위 밖이라 그 경로가 끊긴다. 쿠키 범위를 좁히는 일과 파일 서빙 도메인을 나누는 일은 같은 문제의 양면이다.

## 참고

- RFC 6265bis — 쿠키 도메인 속성
- OWASP — 세션 관리 Cheat Sheet
- CWE-1275 — 부적절한 쿠키 범위 설정`,
    diagram: {
      type: 'bars',
      caption: '범위에 따라 쿠키를 보는 곳',
      unit: '곳',
      items: [
        { label: '호스트 고정', value: 1, note: '그 호스트만' },
        { label: '하위 5개', value: 5, note: '도메인 지정' },
        { label: '하위 20개', value: 20, note: '표면 확대' },
        { label: '하위 40개', value: 40, note: '전부 노출' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '좁히는 방법',
      steps: [
        { label: '도메인 지정 제거', note: '호스트 고정' },
        { label: '인증 도메인 분리', note: '세션 한 곳' },
        { label: '토큰 교환', note: '공유 없이' },
        { label: '하위 도메인 정리', note: '표면 축소' },
      ],
    },
  },
]
