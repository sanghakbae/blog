import type { SeedPost } from './types'

/** 171~180 — 데이터 보호와 암호 운영 */
export const posts18: SeedPost[] = [
  {
    slug: 'hmac-signing',
    title: '웹훅 서명 검증에서 빠지는 항목',
    body: `외부 서비스가 보내는 웹훅을 처리할 때 서명을 확인하지 않으면, 그 엔드포인트는 누구나 호출할 수 있는 상태 변경 API 가 된다. 결제 완료 알림을 위조하면 결제 없이 상품이 나간다. 서명을 확인하더라도 비교 방식, 재전송 처리, 본문 취급 세 가지에서 자주 빠진다.

## 무엇을 확인해야 하는가?

| 항목 | 빠지면 생기는 일 |
| --- | --- |
| 서명 존재 | 누구나 호출 가능 |
| 서명 값 비교 | 시간차로 값이 추론될 수 있다 |
| 타임스탬프 | 옛 요청을 그대로 재전송 |
| 본문 원문 | 파싱 후 재직렬화하면 서명이 어긋난다 |
| 사용한 키 식별 | 회전 중 검증 실패 |
| 멱등 처리 | 같은 이벤트가 두 번 반영 |

![서명 검증이 통과되기까지](/img/posts/hmac-signing.svg)

## 본문은 원문 그대로 써야 한다

서명은 보낸 쪽이 만든 바이트열에 대해 계산된다. 프레임워크가 JSON 을 파싱한 뒤 객체를 다시 문자열로 만들면 공백과 키 순서가 달라져 서명이 맞지 않는다. 웹훅 경로만 원문 바이트를 보존하도록 설정해야 한다.

## 비교는 상수 시간으로

문자열을 앞에서부터 비교하는 함수는 다른 글자가 나오는 순간 멈춘다. 그 시간 차이로 올바른 값을 한 글자씩 좁혀 갈 수 있다. 길이가 같은 바이트열을 시간 차이 없이 비교하는 함수를 쓴다.

![재전송과 중복 처리 방어](/img/posts/hmac-signing-2.svg)

## 재전송과 중복을 나눠 본다

타임스탬프가 허용 범위를 벗어나면 오래된 요청이므로 거부한다. 범위 안이라도 같은 이벤트 식별자가 이미 처리됐다면 성공으로 응답하고 아무것도 하지 않는다. 보내는 쪽은 응답이 늦으면 재전송하므로, 중복은 정상 상황이다.

\`\`\`
타임스탬프 오차   5분 이내만 허용
이벤트 식별자     처리 완료 후 일정 기간 저장
같은 식별자 재도착 200 으로 응답, 처리는 건너뜀
서명 불일치       401, 본문은 로그에 남기지 않는다
\`\`\`

## 키 회전을 준비한다

서명 키를 바꾸는 동안 두 키를 모두 허용해야 끊기지 않는다. 헤더에 키 식별자가 있으면 그것으로 고르고, 없으면 두 키로 각각 검증해 하나라도 맞으면 통과시킨다.

## 바로 확인하기

서명 없이 호출했을 때 처리되는지가 첫 점검이다.

\`\`\`bash
# 서명 헤더 없이 — 401 이어야 한다
curl -s -o /dev/null -w '서명 없음 %{http_code}\\n' -X POST https://app.example.com/webhooks/pay \\
  -H 'Content-Type: application/json' -d '{"event":"paid","orderId":"A-1"}'

# 옛 타임스탬프로 — 거부돼야 한다
TS=$(( $(date +%s) - 3600 ))
BODY='{"event":"paid","orderId":"A-1"}'
SIG=$(printf '%s.%s' "$TS" "$BODY" | openssl dgst -sha256 -hmac "$SECRET" -r | cut -d' ' -f1)
curl -s -o /dev/null -w '옛 요청  %{http_code}\\n' -X POST https://app.example.com/webhooks/pay \\
  -H "X-Signature: t=$TS,v1=$SIG" -H 'Content-Type: application/json' -d "$BODY"
\`\`\`

검증 코드는 원문과 상수 시간 비교를 함께 지킨다.

\`\`\`ts
import { createHmac, timingSafeEqual } from 'node:crypto'

function verify(raw: Buffer, header: string, secret: string): boolean {
  const { t, v1 } = parseHeader(header)
  if (Math.abs(Date.now() / 1000 - Number(t)) > 300) return false   // 재전송 차단
  const expected = createHmac('sha256', secret).update(\`\${t}.\`).update(raw).digest()
  const given = Buffer.from(v1, 'hex')
  return expected.length === given.length && timingSafeEqual(expected, given)
}
\`\`\`

## 실제로 이렇게 터진다

서명은 검증하는데 시각을 확인하지 않은 사례가 있다. 한 번 캡처한 요청을 몇 달 뒤에 다시 보내도 통과했다. 서명은 여전히 유효했기 때문이다.

문자열 비교를 일반 비교 연산자로 한 경우도 있다. 비교 시간 차이로 서명을 한 바이트씩 맞춰 갈 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 서명이 맞으면 안전하다 | 재사용을 막지 못한다 |
| 비교는 어떻게 하든 같다 | 시간 차이가 정보를 준다 |
| 본문만 서명하면 된다 | 시각·경로·메서드가 빠지면 재사용된다 |
| 키는 잘 숨기면 된다 | 회전 경로가 필요하다 |
| 헤더 이름은 상관없다 | 프록시가 바꿀 수 있다 |

## 무엇을 서명에 넣는가

| 항목 | 이유 |
| --- | --- |
| 타임스탬프 | 재사용 방지 |
| 논스 | 같은 시각 내 재사용 방지 |
| 메서드·경로 | 다른 엔드포인트로 전용 방지 |
| 본문 해시 | 내용 변조 방지 |
| 키 식별자 | 회전 지원 |

## 검증 순서

순서가 중요하다. 무거운 연산 전에 싼 검사를 먼저 한다.

\`\`\`ts
// 1) 시각 확인 — 가장 싸다
const ts = Number(req.header('X-Timestamp'))
if (!ts || Math.abs(Date.now() / 1000 - ts) > 300) return reject('시각 오차')

// 2) 논스 재사용 확인
const nonce = req.header('X-Nonce')
if (!nonce || (await seen(nonce))) return reject('재사용된 논스')

// 3) 키 조회 — 식별자로 어떤 키인지 정한다
const key = await keys.get(req.header('X-Key-Id'))
if (!key) return reject('알 수 없는 키')

// 4) 서명 계산과 비교 — 반드시 시간 일정 비교
const payload = [req.method, req.path, ts, nonce, sha256(rawBody)].join('\n')
const expected = hmacSha256(key.secret, payload)
if (!timingSafeEqual(Buffer.from(expected), Buffer.from(req.header('X-Signature') ?? ''))) {
  return reject('서명 불일치')
}

await markNonce(nonce, 600)     // 허용 오차보다 길게 보관
\`\`\`

본문 해시를 쓰는 이유는 큰 본문을 두 번 읽지 않기 위해서다. 스트리밍 처리와도 맞는다.

\`\`\`bash
# 시각 검증이 있는지 — 오래된 타임스탬프로 보내 본다
OLD=$(($(date +%s) - 86400))
curl -s -o /dev/null -w '%{http_code}\n' -X POST https://stg.example.com/webhook \
  -H "X-Timestamp: $OLD" -H "X-Signature: $SIG_FOR_OLD" -d '{}'
# 401 이어야 한다. 200 이면 재사용이 가능하다
\`\`\`

## 참고

- RFC 2104 — HMAC: Keyed-Hashing for Message Authentication
- OWASP Cheat Sheet — Cryptographic Storage
- CWE-208: Observable Timing Discrepancy`,
    diagram: {
      type: 'steps',
      caption: '웹훅 검증 순서',
      steps: [
        { label: '원문 보존', note: '파싱 전 바이트열' },
        { label: '타임스탬프 확인', note: '허용 오차 안인가' },
        { label: '상수 시간 비교', note: '값 추론 차단' },
        { label: '멱등 처리', note: '중복 이벤트 무시' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '서명과 재전송 방어',
      x: ['타임스탬프 확인', '확인 없음'],
      y: ['서명 검증', '검증 없음'],
      cells: ['안전', '옛 요청 재사용 가능', '누구나 호출', '완전 무방비'],
    },
  },
  {
    slug: 'webhook-security',
    title: '웹훅 수신 엔드포인트 보호 설계',
    body: `웹훅을 받는 주소는 인증 없이 외부에서 호출되는 공개 엔드포인트다. 서명 검증은 기본이고, 그 위에 처리 시간, 요청량, 실패 재시도, 부작용 범위를 함께 설계해야 서비스가 흔들리지 않는다. 특히 웹훅 처리 안에서 다른 시스템을 호출하면 그 시스템의 장애가 그대로 전파된다.

## 왜 별도 설계가 필요한가?

보내는 쪽은 우리 사정을 모른다. 응답이 늦으면 재전송하고, 한꺼번에 몰아 보내기도 한다. 처리 시간이 길어지면 재전송이 겹쳐 부하가 배로 늘어난다. 그래서 수신은 짧게 끝내고 실제 처리는 뒤로 미루는 구조가 기본이다.

![수신과 처리를 나눈 구조](/img/posts/webhook-security.svg)

## 설계 항목

| 항목 | 기준 |
| --- | --- |
| 응답 시간 | 수 초 이내, 검증과 적재만 |
| 처리 방식 | 큐에 넣고 별도 작업자가 처리 |
| 요청량 제한 | 발신자별 상한 |
| 본문 크기 | 상한을 두고 초과 시 거부 |
| 실패 응답 | 재전송이 필요한 실패만 5xx |
| 로그 | 서명 값과 민감 항목은 남기지 않는다 |
| 관측 | 지연·실패율·중복률 |

## 검증 실패와 처리 실패를 구분한다

서명이 틀린 요청에 5xx 를 돌려주면 보내는 쪽이 계속 재전송한다. 검증 실패는 4xx 로 확실히 거부하고, 우리 쪽 일시 장애만 5xx 로 응답해 재전송을 유도한다. 이 구분이 없으면 잘못된 요청이 무한히 반복된다.

![응답 코드에 따른 재전송 동작](/img/posts/webhook-security-2.svg)

## 발신자 주소로 한 번 더 좁힌다

대부분의 서비스는 웹훅을 보내는 아이피 대역을 공개한다. 방화벽이나 앞단에서 그 대역만 허용하면 서명 검증 전에 대부분의 잡음이 걸러진다. 다만 대역은 바뀌므로 이것만으로 통제를 삼지 않는다.

## 부작용을 좁힌다

웹훅 처리에서 메일 발송, 정산, 외부 호출처럼 되돌리기 어려운 일을 바로 하지 않는다. 상태만 기록하고 후속 작업은 별도 흐름에서 진행하면, 중복이나 위조가 있어도 되돌릴 수 있다.

## 바로 확인하기

응답 시간과 응답 코드가 설계대로인지 본다.

\`\`\`bash
# 검증 실패 요청에 4xx 가 나오는지 (5xx 면 무한 재전송을 부른다)
curl -s -o /dev/null -w '잘못된 서명 %{http_code} · %{time_total}s\\n' \\
  -X POST https://app.example.com/webhooks/pay \\
  -H 'X-Signature: t=1,v1=deadbeef' -H 'Content-Type: application/json' -d '{}'

# 정상 요청 처리 시간 — 길면 재전송이 겹친다
curl -s -o /dev/null -w '정상 요청   %{http_code} · %{time_total}s\\n' \\
  -X POST https://app.example.com/webhooks/pay \\
  -H "X-Signature: $VALID" -H 'Content-Type: application/json' -d "$BODY"
\`\`\`

수신부는 검증하고 넣는 것까지만 한다.

\`\`\`ts
app.post('/webhooks/pay', rawBody({ limit: '256kb' }), async (req, res) => {
  if (!verify(req.rawBody, req.get('X-Signature') ?? '', SECRET))
    return res.status(401).end()                       // 재전송 유도하지 않음

  const event = JSON.parse(req.rawBody.toString())
  const inserted = await events.insertIfAbsent(event.id, req.rawBody)  // 멱등
  if (inserted) await queue.push({ eventId: event.id })                 // 처리는 뒤에서
  res.status(200).end()                                 // 빠르게 응답
})
\`\`\`

## 실제로 이렇게 터진다

웹훅 수신 엔드포인트가 인증 없이 열려 있던 사례가 있다. 주소를 아는 사람은 누구나 이벤트를 만들어 보낼 수 있었고, 그것으로 주문 상태를 바꿨다.

느린 처리로 중복이 발생한 경우도 있다. 응답이 10초 걸리자 발신 측이 재전송했고, 같은 이벤트가 세 번 처리됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 주소가 복잡하면 안전하다 | 로그와 이력에 남는다 |
| 발신 IP 를 확인하면 된다 | 목록이 자주 바뀐다 |
| 한 번만 온다 | 최소 한 번 전달이 표준이다 |
| 순서대로 온다 | 순서는 보장되지 않는다 |
| 동기로 처리하면 확실하다 | 타임아웃과 재전송을 부른다 |

## 수신 설계

| 항목 | 원칙 |
| --- | --- |
| 인증 | 서명 검증 필수 |
| 응답 | 즉시 200, 처리는 비동기 |
| 멱등 | 이벤트 식별자로 중복 차단 |
| 순서 | 시각·버전으로 판단, 순서 가정 금지 |
| 검증 | 원본 조회로 값 대조 |
| 실패 | 재시도 가능하게, 실패 큐 |

즉시 응답하고 비동기로 처리하는 것이 핵심이다. 처리 중 실패해도 발신 측에는 이미 성공을 알렸으므로, 우리 쪽 재처리 구조가 필요하다.

\`\`\`ts
app.post('/webhook/payment', async (req, res) => {
  if (!verifySignature(req)) return res.status(401).end()

  const id = req.body.eventId
  if (await events.exists(id)) return res.status(200).end()   // 중복은 조용히 성공

  await events.enqueue({ id, raw: req.body, receivedAt: new Date() })
  res.status(200).end()                                        // 즉시 응답
})

// 워커에서 처리 — 값은 원본에서 다시 확인한다
async function handle(event) {
  const real = await provider.getPayment(event.raw.paymentId)
  if (real.status !== 'PAID') return
  await orders.markPaid(event.raw.orderId, real)
}
\`\`\`

## 점검 항목

\`\`\`bash
# 서명 없이 호출했을 때 거부되는지
curl -s -o /dev/null -w '%{http_code}\n' -X POST https://stg.example.com/webhook/payment \
  -H 'Content-Type: application/json' -d '{"eventId":"e1","status":"PAID"}'
# 401 이어야 한다

# 같은 이벤트를 두 번 보냈을 때 한 번만 처리되는지
for i in 1 2; do curl -s -o /dev/null -X POST https://stg.example.com/webhook/payment \
  -H "X-Signature: $SIG" -d "$BODY"; done
\`\`\`

## 참고

- OWASP Cheat Sheet — Denial of Service
- RFC 9110, HTTP 상태 코드 의미
- 각 결제·연동 서비스의 웹훅 재시도 정책 문서`,
    diagram: {
      type: 'flow',
      caption: '수신과 처리의 분리',
      steps: [
        { label: '서명 검증', note: '실패는 4xx' },
        { label: '중복 확인', note: '이벤트 식별자' },
        { label: '큐에 적재', note: '여기까지 수 초 이내' },
        { label: '별도 작업자 처리', note: '재시도와 실패 격리' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '응답 코드와 재전송',
      x: ['4xx 응답', '5xx 응답'],
      y: ['검증 실패', '일시 장애'],
      cells: ['재전송 없음 — 올바름', '무한 재전송', '복구 기회 상실', '재전송으로 복구 — 올바름'],
    },
  },
  {
    slug: 'tokenization',
    title: '토큰화로 민감 데이터 범위 좁히기',
    body: `카드번호나 주민등록번호를 여러 시스템이 함께 다루면, 그 시스템 전부가 보호 대상이 되고 감사 범위도 함께 커진다. 토큰화는 실제 값을 한 곳에만 두고 나머지에는 의미 없는 대체값만 흐르게 하는 방법이다. 암호화와 달리 대체값에서 원본을 계산할 수 없어, 대체값이 유출돼도 그 자체로는 쓸모가 없다.

## 암호화와 무엇이 다른가?

| 항목 | 암호화 | 토큰화 |
| --- | --- | --- |
| 원본 복원 | 키가 있으면 가능 | 금고 조회로만 가능 |
| 유출 시 | 키까지 유출되면 전부 노출 | 대체값만으로는 무의미 |
| 형식 | 길이·형식이 달라진다 | 원본과 같은 형식 유지 가능 |
| 검색 | 어렵다 | 대체값으로 일치 검색 가능 |
| 적용 범위 | 저장 계층 | 데이터 흐름 전체 |

![원본이 흐르는 범위를 좁히는 구조](/img/posts/tokenization.svg)

## 금고를 어디에 둘 것인가

원본을 보관하는 금고는 가장 좁게 통제되는 구간에 둔다. 접근할 수 있는 시스템과 사람을 명시하고, 조회 이력을 전부 남긴다. 금고를 직접 운영하기 어렵다면 결제 대행사처럼 그 역할을 하는 외부 서비스에 맡기고 우리는 대체값만 받는다.

## 형식을 유지할지 정한다

대체값이 원본과 같은 형식이면 기존 시스템을 고치지 않고 넣을 수 있다. 대신 형식만 보고 실제 값으로 오해할 수 있으므로, 마지막 네 자리 같은 표시용 값을 따로 두고 대체값은 화면에 보여주지 않는다.

![대체값 설계 선택](/img/posts/tokenization-2.svg)

## 무엇이 여전히 위험한가

토큰화를 해도 금고 조회 권한을 가진 시스템이 침해되면 원본이 나간다. 그래서 조회 권한을 건 단위로 좁히고, 대량 조회를 탐지 신호로 만든다. 토큰화의 목적은 위험을 없애는 것이 아니라 위험이 있는 범위를 좁히는 것이다.

## 적용 순서

1. 원본이 지나가는 시스템과 저장소를 전부 그린다
2. 그중 원본이 꼭 필요한 곳을 고른다 — 대개 한두 곳뿐이다
3. 나머지 경로를 대체값으로 바꾼다
4. 원본 컬럼을 지우고 접근 권한을 회수한다
5. 금고 조회 이력에 경보를 건다

## 바로 확인하기

원본이 아직 남아 있는 곳을 찾는 것이 시작이다.

\`\`\`sql
-- 카드번호 형태가 남아 있는 컬럼 찾기
SELECT table_name, column_name
  FROM information_schema.columns
 WHERE column_name ~* 'card|pan|ssn|rrn|account_no'
 ORDER BY table_name;

-- 실제 값이 들어 있는지 표본 확인
SELECT count(*) FILTER (WHERE card_no ~ '^[0-9]{13,16}$') AS 원본형태,
       count(*) FILTER (WHERE card_no ~ '^tok_') AS 대체값,
       count(*) AS 전체
  FROM payments;
\`\`\`

금고 조회량은 지표로 만든다.

\`\`\`
정상   건별 조회, 시간당 수십 건
의심   단일 시스템이 시간당 수천 건
경보   업무 시간 외 대량 조회
차단   일일 상한 초과 시 승인 요구
\`\`\`

## 실제로 이렇게 터진다

카드번호를 직접 보관하다 유출된 사례가 있다. 암호화는 했지만 복호 키를 같은 환경에 두었고, 규정상 요구되는 통제 범위가 시스템 전체로 넓어져 감사 부담도 컸다.

토큰화를 도입했지만 원본을 함께 보관한 경우도 있다. 토큰과 원본을 잇는 표가 우리 데이터베이스에 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호화와 같다 | 원본을 우리가 갖지 않는 것이 다르다 |
| 토큰도 민감 정보다 | 단독으로는 가치가 없어야 한다 |
| 통제 범위가 줄지 않는다 | 원본이 없으면 크게 준다 |
| 검색이 불가능하다 | 토큰으로 조회할 수 있다 |
| 도입이 어렵다 | 대행사 기능을 쓰면 단순하다 |

## 무엇이 달라지는가

| 항목 | 암호화 | 토큰화 |
| --- | --- | --- |
| 원본 보관 | 우리가 한다 | 하지 않는다 |
| 복호 가능 | 키가 있으면 | 우리는 불가능 |
| 유출 시 | 키에 따라 위험 | 토큰만으로는 무가치 |
| 통제 범위 | 넓다 | 좁아진다 |
| 형식 유지 | 별도 처리 필요 | 대개 유지 |

## 무엇을 토큰화하는가

| 대상 | 판단 |
| --- | --- |
| 카드번호 | 필수 — 직접 보관하지 않는다 |
| 계좌번호 | 권장 |
| 주민등록번호 | 보관 근거부터 확인 |
| 연락처 | 대개 불필요 |
| 이메일 | 로그인 식별자면 불가 |

## 도입 절차

1. 원본이 정말 필요한 곳을 찾는다 — 대개 거의 없다
2. 토큰만으로 되는 업무를 확인한다 — 조회·환불·정기 결제
3. 대행사 토큰화를 적용하고 원본 저장을 중단한다
4. 기존 데이터를 토큰으로 교체한다
5. 원본 컬럼을 삭제하고 백업에서도 제거한다
6. 통제 범위를 다시 산정한다

5번을 빠뜨리면 효과가 없다. 컬럼만 안 쓰고 남겨 두면 통제 범위가 그대로다.

\`\`\`sql
-- 원본이 남아 있는지 확인 — 형식으로 찾는다
SELECT count(*) FROM payments
WHERE card_number ~ '^[0-9]{13,19}$';
-- 0 이 아니면 아직 원본을 보관하고 있다
\`\`\`

## 참고

- PCI DSS, 카드 데이터 저장 범위 축소
- NIST SP 800-57, 키 관리 일반 권고
- 개인정보의 안전성 확보조치 기준, 고유식별정보 보호`,
    diagram: {
      type: 'flow',
      caption: '원본과 대체값의 경로',
      steps: [
        { label: '입력', note: '원본은 금고로만' },
        { label: '대체값 발급', note: '형식 유지 가능' },
        { label: '업무 시스템', note: '대체값만 흐른다' },
        { label: '필요 시 조회', note: '건 단위·이력 기록' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '대체값 설계',
      x: ['형식 유지', '형식 변경'],
      y: ['화면 노출 금지', '화면 노출'],
      cells: ['기존 시스템 호환', '실제 값으로 오해', '안전하나 개편 필요', '위험'],
    },
  },
  {
    slug: 'data-masking-view',
    title: '조회 화면 마스킹 정책 설계',
    body: `데이터베이스를 암호화해도 상담원이 보는 화면에 전체 값이 그대로 뜨면 유출 경로는 그대로 남는다. 권한 있는 사람의 정당한 조회로는 어떤 암호화도 막지 못하기 때문이다. 화면 마스킹은 그 구간을 다루는 통제이고, 기준은 두 가지다. 업무에 필요한 최소 자릿수만 보여주고, 전체가 필요한 순간을 별도로 다룬다.

## 무엇을 얼마나 가릴 것인가?

| 항목 | 기본 표시 | 전체 필요 시 |
| --- | --- | --- |
| 휴대전화 | 가운데 가림 | 발신 기능으로 대체 |
| 메일 주소 | 앞 두 글자 + 도메인 | 발송 기능으로 대체 |
| 카드번호 | 마지막 네 자리 | 표시하지 않음 |
| 계좌번호 | 마지막 세 자리 | 승인 후 일시 표시 |
| 주소 | 시·구까지 | 배송 조회로 대체 |
| 고유식별정보 | 표시하지 않음 | 법정 사유 확인 후 |

![업무 화면에서 값이 가려지는 지점](/img/posts/data-masking-view.svg)

## 마스킹은 서버에서 한다

화면에서 가리는 방식은 통제가 아니다. 응답에는 전체 값이 들어 있으므로 개발자 도구나 API 직접 호출로 그대로 보인다. 서버가 마스킹된 값을 내려주고, 전체 값이 필요한 경우에만 별도 엔드포인트로 제공해야 한다.

## 전체 값 조회를 별도 동작으로 만든다

가려진 값을 눌러 전체를 보는 기능이 필요하다면, 그것을 별도 동작으로 정의한다. 사유를 남기고, 이력을 기록하고, 건수 상한을 둔다. 그러면 "누가 언제 누구의 무엇을 봤는지"가 남아 사후 확인이 가능해진다.

![전체 값 조회를 다루는 방식](/img/posts/data-masking-view-2.svg)

## 역할에 따라 다르게

모든 사람에게 같은 수준으로 가릴 필요는 없다. 배송 담당은 주소가 필요하고 결제 담당은 카드 뒷자리가 필요하다. 역할별로 필요한 항목을 표로 정의하고 그 표를 그대로 옮긴 판정 함수 하나를 둔다. 판정이 두 곳에 있으면 반드시 갈라진다.

## 목록과 상세를 나눈다

목록 화면은 여러 건이 한 번에 보이므로 유출 규모가 크다. 목록에서는 더 강하게 가리고, 상세 화면에서 필요한 항목만 푼다. 내려받기 기능은 목록보다 더 강한 통제를 둔다.

## 바로 확인하기

화면이 아니라 응답을 본다.

\`\`\`bash
# 응답에 전체 값이 들어 있는지 — 화면에서 가려도 여기 있으면 통제가 아니다
curl -s https://app.example.com/api/customers/12345 -H "Cookie: session=$S" \\
  | python3 -m json.tool | grep -iE 'phone|email|card|account|address'

# 목록 응답도 함께 — 한 번에 나가는 규모가 크다
curl -s 'https://app.example.com/api/customers?limit=100' -H "Cookie: session=$S" \\
  | grep -oE '01[016789]-?[0-9]{3,4}-?[0-9]{4}' | head
\`\`\`

전체 값 조회 이력이 남는지 확인한다.

\`\`\`sql
SELECT at, actor_email, subject_id, field, reason
  FROM sensitive_view_log
 WHERE at > now() - interval '7 days'
 ORDER BY at DESC LIMIT 20;
-- 기록이 없으면 누가 무엇을 봤는지 확인할 방법이 없다
\`\`\`

## 실제로 이렇게 터진다

상담 화면에서 전체 주민등록번호가 보이던 사례가 있다. 업무상 필요한 것은 뒷자리 한 자리뿐이었는데 전체가 나왔다. 화면 캡처가 메신저로 돌아다녔다.

마스킹을 했지만 API 응답에는 원본이 들어 있던 경우도 있다. 화면에서만 가렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 화면에서 가리면 된다 | 응답 본문에 원본이 있다 |
| 업무상 필요하다 | 어느 자리까지 필요한지 확인해야 한다 |
| 관리자는 다 봐야 한다 | 사유와 기록이 붙어야 한다 |
| 마스킹은 표시 방식이다 | 접근 통제의 일부다 |
| 한 규칙이면 충분하다 | 역할·화면별로 다르다 |

## 어디에서 가리는가

| 지점 | 효과 | 한계 |
| --- | --- | --- |
| 화면 | 쉽다 | 응답에 원본이 남는다 |
| API 응답 | 확실 | 역할별 분기 필요 |
| 데이터 계층 | 우회 어려움 | 설정 부담 |
| 저장 | 가장 확실 | 복원 불가 |

API 응답 단계에서 가리는 것이 실무적 기준선이다. 화면에서만 가리면 개발자 도구로 원본이 보인다.

## 역할별 규칙

| 역할 | 표시 |
| --- | --- |
| 일반 상담 | 뒷자리 일부만 |
| 본인 확인 필요 시 | 사유 입력 후 전체, 기록 |
| 정산·회계 | 계좌 뒷자리만 |
| 개발·운영 | 원칙적으로 가림 |
| 감사 | 기록 조회만 |

"사유 입력 후 전체" 경로를 두면 업무가 막히지 않으면서 조회 이력이 남는다.

\`\`\`ts
// 응답 단계에서 역할에 따라 가린다
function maskRRN(value: string, actor: Actor, reason?: string): string {
  if (actor.can('pii:view_full') && reason) {
    void audit.log('pii.view', { field: 'rrn', subject: value.slice(0, 6), reason, actor: actor.id })
    return value
  }
  return value.slice(0, 6) + '-' + value[6] + '******'
}
\`\`\`

\`\`\`bash
# 응답에 원본이 섞여 나가는지 표본 점검
curl -s https://stg.example.com/api/customers/1 -H "Authorization: Bearer $STAFF" |
  grep -oE '[0-9]{6}-[1-4][0-9]{6}' && echo '원본이 응답에 있다'
\`\`\`

## 참고

- 개인정보의 안전성 확보조치 기준, 접근 권한과 접속기록
- OWASP Cheat Sheet — Access Control
- NIST SP 800-53, AC-6 최소 권한`,
    diagram: {
      type: 'layers',
      caption: '조회 통제 계층',
      layers: [
        { label: '서버 마스킹', note: '응답 자체를 가린다' },
        { label: '역할별 범위', note: '필요한 항목만' },
        { label: '전체 조회 분리', note: '사유와 이력' },
        { label: '건수 상한', note: '대량 조회 차단' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '마스킹 위치와 실효성',
      x: ['서버에서 가림', '화면에서 가림'],
      y: ['이력 기록', '기록 없음'],
      cells: ['통제 성립', 'API 로 그대로 노출', '사후 확인 불가', '통제 없음'],
    },
  },
  {
    slug: 'db-encryption-keys',
    title: '데이터베이스 암호화 키 분리 원칙',
    body: `데이터베이스를 암호화했다고 해도 키가 같은 서버, 같은 계정, 같은 백업 안에 함께 있으면 보호 효과가 거의 없다. 데이터를 가져간 쪽이 키도 함께 가져가기 때문이다. 암호화 설계의 절반은 알고리즘이 아니라 키를 데이터와 얼마나 떨어뜨려 놓느냐다.

## 어디까지 떨어뜨려야 하는가?

| 분리 수준 | 막는 위협 |
| --- | --- |
| 같은 서버·같은 디렉터리 | 사실상 없음 |
| 같은 서버·다른 권한 | 애플리케이션 계정 침해 일부 |
| 다른 서버 | 데이터베이스 유출 |
| 키 관리 서비스 | 서버 침해까지 |
| 하드웨어 모듈 | 서비스 제공자 접근까지 |

![데이터와 키가 함께 있을 때와 떨어져 있을 때](/img/posts/db-encryption-keys.svg)

## 백업이 자주 뚫린다

운영 서버의 키는 잘 관리하면서 백업에는 키와 데이터를 함께 넣는 경우가 많다. 복구 편의를 위해 그렇게 하는데, 그 백업 하나가 유출되면 암호화는 없었던 것이 된다. 백업 복구 절차에는 키를 별도 경로로 가져오는 단계가 있어야 한다.

## 권한을 두 축으로 나눈다

키를 쓸 권한과 키 정책을 바꿀 권한은 다르다. 같은 주체가 둘을 모두 가지면 스스로 권한을 넓혀 전부 복호화할 수 있다. 정책 변경은 별도 승인 대상으로 두고 이력을 남긴다.

![권한 분리 구조](/img/posts/db-encryption-keys-2.svg)

## 봉투 암호화로 회전을 가볍게

데이터는 데이터 키로 암호화하고, 데이터 키는 마스터 키로 감싸 데이터 옆에 둔다. 마스터 키를 바꿀 때 데이터를 다시 암호화할 필요가 없어 회전 주기가 실제로 지켜진다.

## 복호화 호출을 지표로 만든다

키 관리 서비스를 쓰면 복호화가 호출로 남는다. 주체별 호출량을 기준선으로 두고 급증을 경보로 만들면, 대량 유출 시도가 그 자체로 탐지 신호가 된다.

## 바로 확인하기

키가 어디에 있는지부터 확인한다.

\`\`\`bash
# 애플리케이션 서버에 키 파일이 있는지
find /srv /opt /home -maxdepth 4 -type f \\( -name '*.key' -o -name '*.pem' -o -name '*enc*key*' \\) 2>/dev/null

# 환경 변수에 키가 그대로 있는지
strings /proc/$(pgrep -f 'node|java|python' | head -1)/environ 2>/dev/null \\
  | grep -iE 'key|secret' | head
\`\`\`

백업에 키가 함께 들어가는지도 본다.

\`\`\`bash
# 백업 아카이브 목록에서 키 파일 확인
tar -tzf backup-latest.tar.gz | grep -iE '\\.key$|\\.pem$|keystore' | head
# 하나라도 나오면 그 백업은 암호화되지 않은 것과 같다
\`\`\`

## 실제로 이렇게 터진다

데이터베이스를 암호화했는데 키를 같은 서버에 둔 사례가 있다. 서버가 침해되자 데이터와 키가 함께 나갔다. 암호화는 매체 도난만 막았고, 실제 위협은 원격 침해였다.

애플리케이션과 데이터베이스가 같은 키를 쓴 경우도 있다. 어느 쪽이 뚫려도 전부 열렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호화하면 안전하다 | 키 위치가 결정한다 |
| 키를 설정 파일에 두면 편하다 | 함께 유출된다 |
| 하나의 키로 관리하면 단순하다 | 하나가 새면 전부 샌다 |
| 데이터베이스 기능이면 충분하다 | 관리자 조회는 막지 못한다 |
| 키 교체는 대공사다 | 봉투 구조면 간단하다 |

## 무엇을 분리하는가

| 분리 축 | 효과 |
| --- | --- |
| 저장 위치 | 데이터와 키를 다른 곳에 |
| 관리 주체 | 데이터베이스 관리자와 키 관리자를 분리 |
| 용도 | 데이터 종류별로 다른 키 |
| 환경 | 운영·개발 키 분리 |
| 계층 | 마스터 키와 데이터 키 |

관리 주체 분리가 특히 중요하다. 데이터베이스 관리자가 키에 접근할 수 있으면 필드 암호화의 의미가 절반이다.

## 어떤 계층에서 암호화하는가

| 계층 | 막는 것 | 못 막는 것 |
| --- | --- | --- |
| 디스크·볼륨 | 매체 도난 | 실행 중 접근 |
| 데이터베이스 전체 | 파일 유출 | 질의를 통한 조회 |
| 테이블·필드 | 다른 서비스 조회 | 권한 있는 애플리케이션 |
| 애플리케이션 | 데이터베이스 관리자 | 애플리케이션 침해 |

애플리케이션 계층에서 암호화하면 데이터베이스는 값을 볼 수 없다. 대신 검색과 정렬에 제약이 생긴다.

## 점검 절차

\`\`\`bash
# 키가 어디에 있는지 — 설정 파일에 있으면 문제다
grep -rniE '(encryption[_-]?key|master[_-]?key|db[_-]?key) *[:=]' \
  /etc/app/ config/ --include='*.yml' --include='*.env' 2>/dev/null | head

# 키 관리 서비스 접근 권한이 데이터베이스 관리자와 겹치는지
aws kms get-key-policy --key-id "$KEY" --policy-name default --output text |
  python3 -m json.tool | grep -A3 '"AWS"'
\`\`\`

## 참고

- NIST SP 800-57 Part 1, 키 관리 권고
- 개인정보의 안전성 확보조치 기준, 암호화와 키 관리
- 클라우드 공급자별 봉투 암호화 문서`,
    diagram: {
      type: 'matrix',
      caption: '데이터와 키의 위치',
      x: ['키 분리', '같은 곳'],
      y: ['백업 분리', '백업 함께'],
      cells: ['보호됨', '백업 유출로 무력화', '운영 유출로 무력화', '암호화 없는 것과 같음'],
    },
    diagram2: {
      type: 'layers',
      caption: '키 권한 분리',
      layers: [
        { label: '키 사용 권한', note: '애플리케이션' },
        { label: '키 정책 권한', note: '별도 승인' },
        { label: '마스터 키', note: '서비스 밖으로 안 나감' },
        { label: '호출 감사', note: '급증 경보' },
      ],
    },
  },
  {
    slug: 'secure-delete',
    title: '삭제와 파기의 차이와 확인 방법',
    body: `화면에서 지웠다고 데이터가 사라진 것은 아니다. 대개는 표시만 감춘 상태이고, 원본은 테이블에 남아 있으며 백업과 로그와 데이터 웨어하우스에도 사본이 있다. 파기는 다시 복원할 수 없게 만드는 것이고, 그것을 증명할 수 있어야 파기다. 규정 대응이든 사고 대비든 이 구분에서 시작한다.

## 어디에 사본이 남는가?

| 위치 | 흔히 놓치는 이유 |
| --- | --- |
| 원본 테이블 | 논리 삭제 플래그만 세움 |
| 파생 테이블 | 통계·집계용 사본 |
| 검색 색인 | 별도 저장소라 함께 안 지워짐 |
| 캐시 | 만료까지 남는다 |
| 로그 | 요청 본문에 값이 그대로 |
| 백업 | 보관 주기만큼 남는다 |
| 데이터 웨어하우스 | 분석용 적재본 |
| 외부 서비스 | 위탁·연동으로 넘어간 사본 |

![지워야 할 사본의 위치](/img/posts/secure-delete.svg)

## 논리 삭제를 언제까지 둘지 정한다

실수 복구를 위해 일정 기간 논리 삭제로 두는 것은 합리적이다. 문제는 그 기간이 정해져 있지 않은 경우다. 유예 기간을 정하고, 기간이 지나면 실제로 지우는 작업이 자동으로 돌아야 한다.

\`\`\`
D+0     논리 삭제 — 화면에서 사라짐, 복구 가능
D+30    물리 삭제 — 원본과 파생 사본 제거
D+30    검색 색인·캐시 제거
백업     보관 주기 경과로 자연 소멸 (주기를 문서화)
로그     마스킹돼 있거나 보관 주기 경과
\`\`\`

![삭제 요구가 처리되는 경로](/img/posts/secure-delete-2.svg)

## 백업은 지우지 않고 주기로 관리한다

백업에서 특정 레코드만 지우는 것은 사실상 불가능하고, 시도하면 백업 무결성이 깨진다. 대신 백업 보관 주기를 명시하고, 그 기간이 지나면 사라진다는 것을 문서로 남긴다. 복구 시 이미 파기된 데이터가 되살아나지 않도록 복구 후 재파기 절차도 함께 정한다.

## 파기했음을 증명한다

파기 작업이 무엇을 몇 건 지웠는지 기록을 남긴다. 이 기록이 감사 대응의 증적이 되고, 지워지지 않는 경로가 어디인지 드러내는 자료가 된다.

## 바로 확인하기

원본을 지운 뒤 파생 저장소에 남아 있는지 대조한다.

\`\`\`sql
-- 논리 삭제만 되고 남아 있는 건수
SELECT count(*) FROM users WHERE deleted_at IS NOT NULL;

-- 유예 기간이 지났는데 물리 삭제되지 않은 건
SELECT count(*) FROM users
 WHERE deleted_at < now() - interval '30 days';

-- 파생 테이블에 남았는지
SELECT 'events' AS t, count(*) FROM events e
  JOIN users u ON u.id = e.user_id AND u.deleted_at IS NOT NULL
UNION ALL
SELECT 'mail_log', count(*) FROM mail_log m
  JOIN users u ON u.id = m.user_id AND u.deleted_at IS NOT NULL;
\`\`\`

검색 색인과 캐시도 확인한다.

\`\`\`bash
curl -s "https://search.internal/customers/_doc/$ID" | head -3
redis-cli --scan --pattern "user:$ID:*" | head
\`\`\`

## 실제로 이렇게 터진다

탈퇴 처리가 플래그 변경뿐이었던 사례가 있다. 데이터는 그대로 있었고, 몇 년 뒤 유출 사고의 피해 규모에 탈퇴 회원이 포함됐다. 처리방침에는 "지체 없이 파기" 라고 적혀 있었다.

삭제는 했는데 사본이 남은 경우도 있다. 검색 색인, 분석 저장소, 백업에 그대로였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 삭제 질의를 돌리면 끝이다 | 사본이 여러 곳에 있다 |
| 백업은 어쩔 수 없다 | 보존 기간을 문서화하면 된다 |
| 논리 삭제도 삭제다 | 데이터는 남아 있다 |
| 지웠으면 확인할 필요 없다 | 확인 절차가 있어야 증명된다 |
| 파기 증명은 형식이다 | 감사와 분쟁에서 요구된다 |

## 삭제와 파기의 차이

| 구분 | 뜻 | 상태 |
| --- | --- | --- |
| 논리 삭제 | 플래그 변경 | 데이터 존재 |
| 물리 삭제 | 행·파일 제거 | 복원 가능성 있음 |
| 파기 | 복원 불가능하게 | 목적 달성 |
| 키 파기 | 복호 키 제거 | 암호문만 남음 |

키 파기가 실무적으로 유용하다. 데이터를 지우지 않아도 읽을 수 없게 되므로, 백업처럼 지우기 어려운 곳에 효과적이다.

## 사본 목록

| 위치 | 처리 |
| --- | --- |
| 운영 데이터베이스 | 물리 삭제 |
| 읽기 복제본 | 자동 반영 확인 |
| 검색 색인 | 별도 삭제 |
| 캐시 | 만료 또는 삭제 |
| 분석·웨어하우스 | 별도 절차 |
| 로그 | 보유 기간으로 처리 |
| 백업 | 기간 경과 소멸, 문서화 |
| 외부 서비스 | 삭제 요청 |

## 확인 방법

지웠다는 것을 증명하려면 확인 절차가 있어야 한다.

\`\`\`sql
-- 파기 대상인데 남아 있는지 — 정기 점검 질의
SELECT 'users' AS src, count(*) FROM users
  WHERE withdrawn_at < now() - interval '30 days'
UNION ALL SELECT 'orders', count(*) FROM orders o
  JOIN users u ON u.id = o.user_id
  WHERE u.withdrawn_at < now() - interval '30 days'
    AND o.created_at < now() - interval '5 years';
-- 0 이 아니면 파기 배치가 동작하지 않는 것이다
\`\`\`

\`\`\`bash
# 검색 색인에도 지워졌는지
curl -s -u "$ES_USER:$ES_PASS" \
  "https://es.example.com:9200/customers/_count?q=user_id:12345" |
  python3 -c 'import sys,json;print("색인 잔여:", json.load(sys.stdin)["count"])'
\`\`\`

## 참고

- 개인정보보호법 제21조, 개인정보의 파기
- 개인정보의 안전성 확보조치 기준
- NIST SP 800-88, 매체 정보 파기 지침`,
    diagram: {
      type: 'layers',
      caption: '사본이 남는 위치',
      layers: [
        { label: '원본 테이블', note: '논리 삭제만 되기 쉽다' },
        { label: '파생·색인·캐시', note: '별도 저장소' },
        { label: '로그', note: '요청 본문에 값 포함' },
        { label: '백업·분석 적재본', note: '보관 주기로 관리' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '삭제 요구 처리',
      steps: [
        { label: '논리 삭제', note: '즉시 화면에서 제거' },
        { label: '유예 기간', note: '복구 가능 구간' },
        { label: '물리 삭제', note: '파생 사본까지' },
        { label: '결과 기록', note: '무엇을 몇 건 지웠는가' },
      ],
    },
  },
  {
    slug: 'data-lineage',
    title: '데이터 흐름도 만들기와 유지 방법',
    body: `개인정보 유출 신고, 정보주체 요구 처리, 위탁 점검, 국외 이전 확인은 모두 같은 질문에서 시작한다. 어떤 데이터가 어디서 들어와 어디에 저장되고 어디로 나가는가. 이 질문에 답할 그림이 없으면 매번 사람을 찾아다니게 된다. 데이터 흐름도는 규정 산출물이 아니라 이 반복을 없애는 도구다.

## 무엇을 그리는가?

| 요소 | 담을 내용 |
| --- | --- |
| 수집 지점 | 화면·API·연동, 수집 항목 |
| 저장소 | 시스템, 테이블, 보유 기간 |
| 처리 | 무엇을 위해 어떤 가공을 하는가 |
| 이동 | 내부 시스템 간, 외부로 |
| 접근 주체 | 어떤 역할이 어디까지 |
| 파기 | 언제 어떻게 지워지는가 |

![데이터가 들어와 나가기까지](/img/posts/data-lineage.svg)

## 시스템이 아니라 데이터를 따라 그린다

시스템 구성도를 그리면 데이터가 어디에 있는지는 여전히 알 수 없다. 항목 단위로 따라가야 한다. "휴대전화번호"가 어디서 들어와 어느 테이블에 앉고 어떤 화면과 배치가 읽는지 한 줄로 이어지면, 그 항목에 대한 모든 질문에 답할 수 있다.

## 문서로 두면 반드시 낡는다

한 번 그린 그림은 몇 달이면 실제와 어긋난다. 유지되는 방법은 두 가지다. 새 저장소나 새 외부 연동이 생길 때 갱신을 필수 항목으로 두거나, 실제 스키마와 설정에서 자동으로 뽑아 대조하는 것이다.

![수동 문서와 자동 대조](/img/posts/data-lineage-2.svg)

## 자동 수집으로 절반을 채운다

컬럼 이름과 주석, 데이터 분류 태그, 외부 호출 목록은 코드와 스키마에서 뽑을 수 있다. 자동으로 채운 초안 위에 사람이 목적과 근거만 적는 방식이면 유지 부담이 크게 줄어든다.

## 쓰이는 곳을 만든다

흐름도가 실제로 참조되지 않으면 갱신되지 않는다. 정보주체 요구 처리, 신규 서비스 검토, 위탁 계약 검토가 이 문서를 출발점으로 삼도록 절차에 넣어야 유지된다.

## 바로 확인하기

스키마에서 개인정보로 보이는 컬럼을 먼저 뽑아 초안을 만든다.

\`\`\`sql
SELECT table_name, column_name, data_type
  FROM information_schema.columns
 WHERE column_name ~* 'name|phone|mobile|email|addr|birth|ssn|rrn|card|account'
   AND table_schema = 'public'
 ORDER BY table_name, ordinal_position;
\`\`\`

외부로 나가는 경로도 코드에서 찾는다.

\`\`\`bash
# 외부 호출 대상 목록 — 위탁·국외 이전 검토 대상이 된다
grep -rhoE 'https://[a-z0-9.-]+' --include='*.ts' --include='*.py' src/ \\
  | sed 's|https://||' | sort -u | grep -v 'example\\.com$'

# 로그로 나가는 항목
grep -rnE 'logger\\.(info|debug).*\\b(phone|email|name|addr)\\b' --include='*.ts' src/ | head
\`\`\`

## 실제로 이렇게 터진다

개인정보가 어디로 흘러가는지 그림이 없던 조직에서, 유출 사고 시 영향 범위를 산정하는 데 일주일이 걸렸다. 데이터가 열 개 시스템을 거쳤고 각 단계를 아는 사람이 달랐다.

영향평가를 할 때도 같은 문제가 생긴다. 흐름을 모르면 어디에 통제를 둘지 정할 수 없다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 그림은 형식 문서다 | 사고 시 가장 먼저 찾는 자료다 |
| 한 번 그리면 된다 | 변경마다 갱신해야 한다 |
| 상세할수록 좋다 | 유지되지 않으면 틀린 그림이 된다 |
| 개발팀만 알면 된다 | 법무·보안이 함께 본다 |
| 도구가 자동으로 그린다 | 의미는 사람이 붙인다 |

## 무엇을 그리는가

| 요소 | 내용 |
| --- | --- |
| 수집 지점 | 어디서 받는가 |
| 저장 위치 | 어느 저장소에 |
| 처리 | 무엇으로 가공되는가 |
| 전송 | 어디로 나가는가 |
| 보관 기간 | 각 위치별로 |
| 접근 주체 | 누가 볼 수 있는가 |
| 경계 | 조직·국경·신뢰 경계 |

경계선을 긋는 것이 핵심이다. 선을 넘는 지점마다 통제와 근거가 필요하다.

## 유지되게 만드는 방법

1. 시스템 단위가 아니라 데이터 항목 단위로 그린다
2. 한 장에 담긴다 — 넘어가면 아무도 안 본다
3. 코드와 함께 저장한다 — 텍스트 형식으로
4. 새 저장소·외부 연동이 생기면 갱신을 필수로 한다
5. 분기마다 실제와 대조한다

\`\`\`
[가입 화면] --이름·연락처--> (회원 DB, 탈퇴+30일)
                              |
                              +--식별자만--> (분석 DB, 2년)
                              +--이름·연락처--> (CRM, 위탁·국내, 계약기간)
                              +--연락처--> [문자 발송사, 위탁·국내, 발송 후 파기]
                              |
                         ==== 국경 ====
                              +--오류 정보--> [오류 추적 서비스, 국외, 90일]
\`\`\`

텍스트로 그리면 변경 이력이 남고 검토가 붙는다.

\`\`\`bash
# 개인정보 컬럼이 있는 테이블 목록 — 그림의 출발점
psql -Atc "
  SELECT table_name, string_agg(column_name, ', ')
  FROM information_schema.columns
  WHERE table_schema='public'
    AND column_name ~* '(email|phone|name|birth|address|rrn|ssn|card)'
  GROUP BY 1 ORDER BY 1"
\`\`\`

## 참고

- 개인정보 처리방침 작성 지침
- ISO/IEC 27701, 개인정보 처리 기록
- 개인정보 영향평가 수행 안내서`,
    diagram: {
      type: 'flow',
      caption: '항목 단위로 따라가는 흐름',
      steps: [
        { label: '수집', note: '화면·API·연동' },
        { label: '저장', note: '시스템·테이블·기간' },
        { label: '이용·이동', note: '내부 처리와 외부 전달' },
        { label: '파기', note: '시점과 방법' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '유지 방식과 정확도',
      x: ['자동 대조', '수동 문서'],
      y: ['절차에 편입', '편입 안 됨'],
      cells: ['실제와 일치', '갱신은 되나 누락', '초안만 정확', '몇 달 뒤 무용'],
    },
  },
  {
    slug: 'synthetic-data',
    title: '합성 데이터로 시험 환경 채우기',
    body: `운영 데이터를 개발 환경으로 내리는 관행은 편하지만, 개발 환경은 접근 권한이 넓고 로그가 부실하고 사본이 노트북까지 내려간다. 그래서 유출 사고의 상당수가 여기서 난다. 대안은 실제처럼 보이지만 실제 사람이 아닌 데이터를 만들어 쓰는 것이다. 관건은 분포와 관계를 얼마나 살리느냐다.

## 왜 그냥 무작위 값으로는 부족한가?

무작위 문자열로 채우면 화면은 뜨지만 시험이 되지 않는다. 이름 길이가 실제와 다르면 레이아웃이 깨지는 것을 못 잡고, 주문과 사용자 관계가 없으면 조회 성능을 볼 수 없다. 쓸모 있는 합성 데이터는 형식, 분포, 관계 세 가지를 닮아야 한다.

![운영 사본과 합성 데이터의 차이](/img/posts/synthetic-data.svg)

## 무엇을 닮게 만드는가?

| 성질 | 왜 필요한가 |
| --- | --- |
| 형식 | 검증 로직이 실제처럼 동작 |
| 길이 분포 | 화면 레이아웃 확인 |
| 값 분포 | 쏠린 데이터에서의 성능 |
| 관계 | 조인과 집계 경로 |
| 경계값 | 빈 값, 최대 길이, 특수문자 |
| 규모 | 인덱스 효과 확인 |

## 만드는 방법 세 가지

- 규칙 기반 생성 — 형식과 분포를 직접 정의한다. 단순하고 안전하다
- 운영 통계 기반 — 실제 분포만 뽑아 그 모양으로 생성한다. 원본은 내려오지 않는다
- 모델 기반 생성 — 정교하지만 학습 데이터에서 원본이 새어 나올 수 있다

첫 두 가지로 대부분 충분하다. 세 번째는 재식별 위험을 따로 검토해야 한다.

![생성 방식과 위험](/img/posts/synthetic-data-2.svg)

## 실존 값과 겹치지 않게 한다

무작위로 만든 전화번호가 실제 사용 중인 번호일 수 있다. 시험 발송이 실제 사람에게 가면 그 자체가 사고다. 시험용으로 예약된 번호 대역과 도메인을 쓰고, 발송 경로는 개발 환경에서 차단한다.

\`\`\`
전화   시험용으로 예약된 대역 사용
메일   example.com 등 예약 도메인
결제   시험용 카드번호만
발송   개발 환경에서는 실제 전송 경로 차단
\`\`\`

## 그래도 운영 데이터가 필요할 때

재현이 안 되는 장애처럼 원본이 꼭 필요한 경우가 있다. 그때는 가명처리 후 반출하고, 반출 승인·기간·대상·삭제 확인을 절차로 남긴다. 예외를 없앨 수 없다면 예외를 기록되게 만든다.

## 바로 확인하기

개발 환경에 운영 데이터가 내려와 있는지부터 본다.

\`\`\`sql
-- 실제 도메인 메일이 섞여 있으면 운영 사본이다
SELECT count(*) FILTER (WHERE email NOT LIKE '%@example.com') AS 실제형태,
       count(*) AS 전체
  FROM users;

-- 이름·전화 분포가 합성인지 확인
SELECT length(name) AS len, count(*) FROM users GROUP BY 1 ORDER BY 1;
\`\`\`

발송 경로가 실제로 막혀 있는지도 확인한다.

\`\`\`bash
grep -rnE 'MAIL_TRANSPORT|SMS_PROVIDER|SEND_ENABLED' .env.development config/ 2>/dev/null
# 개발 환경에서 실제 전송이 켜져 있으면 합성 데이터라도 사고가 난다
\`\`\`

## 실제로 이렇게 터진다

시험 환경에 운영 데이터를 복사한 사례가 반복된다. 개발 환경은 접근 통제가 느슨하고 외주 인력도 접근한다. 개인정보가 통제 밖으로 나가는 가장 흔한 경로다.

합성 데이터를 만들었지만 쓸모없던 경우도 있다. 전부 같은 패턴이라 경계값 시험이 안 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 실제 데이터라야 시험이 된다 | 형태가 같으면 대부분 충분하다 |
| 무작위로 만들면 된다 | 분포와 경계값이 필요하다 |
| 한 번 만들면 끝이다 | 스키마가 바뀌면 갱신한다 |
| 양이 많아야 한다 | 종류가 많아야 한다 |
| 만드는 데 오래 걸린다 | 한 번 만들면 계속 쓴다 |

## 무엇을 담아야 쓸모가 있는가

| 요소 | 이유 |
| --- | --- |
| 경계값 | 최소·최대·0·음수 |
| 특수 문자 | 이름의 따옴표, 이모지 |
| 다국어 | 한글·한자·라틴 확장 |
| 긴 값 | 컬럼 상한 근처 |
| 빈 값·널 | 선택 항목 |
| 관계 | 참조 무결성 |
| 분포 | 실제와 비슷한 비율 |

무작위 문자열만 넣으면 실제 버그를 못 잡는다. 이름에 따옴표가 들어간 사용자가 있어야 인젝션이 드러난다.

## 만드는 방법

1. 스키마에서 제약을 읽어 기본 생성기를 만든다
2. 컬럼 이름으로 형식을 추론한다 — 이메일·전화·주소
3. 경계값 표본을 손으로 추가한다
4. 실제 분포를 통계로만 가져와 반영한다 — 값은 가져오지 않는다
5. 시드를 고정해 재현 가능하게 한다

\`\`\`ts
// 시드 고정 — 같은 데이터가 재현되어야 시험이 안정적이다
import { faker } from '@faker-js/faker'
faker.seed(20260910)

const EDGE_CASES = [
  { name: "O'Brien", email: 'a@b.co' },                       // 따옴표
  { name: '김'.repeat(30), email: 'x'.repeat(60) + '@e.com' }, // 상한 근처
  { name: '田中太郎', email: 'jp@example.jp' },                 // 다국어
  { name: '🙂 이모지', email: 'emoji@example.com' },
]

const rows = [...EDGE_CASES, ...Array.from({ length: 1000 }, () => ({
  name: faker.person.fullName(),
  email: faker.internet.email(),
}))]
\`\`\`

\`\`\`bash
# 시험 환경에 실제 주소가 섞였는지 점검
psql -h dev-db -Atc "
  SELECT count(*) FROM users
  WHERE email !~ '@(example|test)\.(com|org|jp)$'"
# 0 이 아니면 운영 데이터가 들어와 있다
\`\`\`

## 참고

- 개인정보 가명처리 가이드라인
- ISO/IEC 27701, 시험 데이터 관리
- NIST SP 800-122, 개인정보 보호 지침`,
    diagram: {
      type: 'matrix',
      caption: '시험 데이터 선택',
      x: ['합성 데이터', '운영 사본'],
      y: ['분포 반영', '무작위 값'],
      cells: ['쓸모 있고 안전', '유출 위험', '시험이 안 됨', '가장 위험'],
    },
    diagram2: {
      type: 'bars',
      caption: '생성 방식별 재식별 위험',
      unit: '상대값',
      items: [
        { label: '규칙 기반', value: 5, note: '원본과 무관' },
        { label: '통계 기반', value: 20, note: '분포만 사용' },
        { label: '모델 기반', value: 55, note: '검토 필요' },
        { label: '운영 사본', value: 100, note: '원본 그대로' },
      ],
    },
  },
  {
    slug: 'differential-privacy',
    title: '통계 공개와 재식별 위험 관리',
    body: `개별 데이터를 공개하지 않고 집계 값만 내보내면 안전하다고 생각하기 쉽다. 그런데 조건을 조금씩 바꿔 여러 번 질의하면 그 차이에서 개인의 값이 드러난다. 통계 공개에서 지켜야 할 것은 값을 가리는 것이 아니라, 한 사람이 있고 없고가 결과에 거의 영향을 주지 않게 만드는 것이다.

## 집계만 내보내도 왜 위험한가?

조건을 좁힌 두 질의의 결과를 빼면 그 차이에 해당하는 사람의 값이 남는다. "부서 전체 평균"과 "그 사람만 뺀 평균"을 알면 그 사람의 값이 계산된다. 소수 인원 집단에서는 평균 하나만으로도 개인이 특정된다.

![질의 두 번의 차이로 개인이 드러나는 과정](/img/posts/differential-privacy.svg)

## 흔한 대응과 한계

| 대응 | 한계 |
| --- | --- |
| 최소 인원 미만 숨김 | 조건을 바꿔 우회 가능 |
| 반올림 | 반복 질의로 평균 추정 |
| 상위 몇 개만 공개 | 나머지가 역산됨 |
| 항목 제거 | 다른 항목 조합으로 재식별 |
| 잡음 추가 | 잡음 크기와 질의 횟수 관리 필요 |

마지막 항목이 원리적으로 가장 튼튼하지만, 잡음을 얼마나 넣을지와 몇 번까지 답할지를 함께 관리해야 의미가 있다.

## 예산이라는 개념

한 사람에 대해 노출될 수 있는 정보의 총량을 예산으로 보고, 질의를 답할 때마다 그 예산을 소모한다고 생각한다. 예산이 바닥나면 더 답하지 않는다. 이 관리가 없으면 잡음을 넣어도 반복 질의로 평균이 드러나 무력해진다.

![질의 횟수와 노출 누적](/img/posts/differential-privacy-2.svg)

## 실무에서 먼저 할 일

정교한 기법을 도입하기 전에 단순한 것부터 정한다.

- 어떤 집계를 누구에게 공개하는지 목록으로 만든다
- 최소 집단 크기를 정하고 미만은 답하지 않는다
- 같은 사용자의 반복 질의를 추적하고 상한을 둔다
- 자유 조건 질의를 허용하지 않고 정해진 질의만 제공한다

마지막 항목이 가장 효과가 크다. 조건을 임의로 조합할 수 없으면 차이 공격 자체가 어려워진다.

## 내부 분석 환경도 대상이다

외부 공개만 위험한 것이 아니다. 사내 분석 도구에서 누구나 조건을 바꿔가며 집계를 뽑을 수 있으면 같은 문제가 생긴다. 분석 환경의 질의 이력을 남기고, 소규모 집단 질의를 경보로 만든다.

## 바로 확인하기

소규모 집단이 그대로 답해지는지 시험한다.

\`\`\`bash
# 조건을 좁혀 인원이 1~2명이 되는 질의
curl -s "https://analytics.example.com/api/agg?metric=salary_avg&dept=infra&level=7&joined=2026" \\
  -H "Authorization: Bearer $T" | python3 -m json.tool
# count 가 임계값 미만인데 값이 나오면 그대로 개인 값이다
\`\`\`

질의 이력에서 차이 공격 패턴을 찾는다.

\`\`\`sql
-- 짧은 시간에 조건만 조금씩 바꿔 반복한 사용자
SELECT actor, count(*) AS q, count(DISTINCT filters) AS variants
  FROM analytics_query_log
 WHERE at > now() - interval '1 hour'
 GROUP BY actor HAVING count(*) > 50
 ORDER BY q DESC;
\`\`\`

## 실제로 이렇게 터진다

지역별 통계를 공개했는데 인구가 적은 지역에서 개인이 특정된 사례가 있다. "이 동네에서 특정 질환 진단을 받은 30대 남성 1명" 은 통계가 아니라 개인 정보다.

여러 통계를 조합해 복원한 경우도 있다. 각각은 안전해 보였지만 함께 보면 개인이 드러났다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 집계하면 안전하다 | 표본이 작으면 특정된다 |
| 한 번 공개는 괜찮다 | 반복 질의로 복원된다 |
| 이름이 없으면 익명이다 | 조합으로 특정된다 |
| 반올림하면 된다 | 차이 공격에 약하다 |
| 내부 대시보드는 무관하다 | 접근 권한이 넓다 |

## 무엇을 확인하는가

| 항목 | 기준 |
| --- | --- |
| 최소 집단 크기 | 그룹당 최소 건수 |
| 유일값 비율 | 조합이 유일한 행의 비율 |
| 질의 반복 | 같은 조건 반복 제한 |
| 차이 공격 | 조건을 하나씩 바꿔 개인 추출 |
| 조합 | 여러 통계의 결합 |

## 실무적 방법

완전한 수학적 보장까지 가지 않아도 대부분의 위험은 줄일 수 있다.

| 방법 | 효과 | 비용 |
| --- | --- | --- |
| 최소 집단 크기 제한 | 큼 | 낮음 |
| 상위 범주로 묶기 | 큼 | 정확도 하락 |
| 희귀값 제거 | 중간 | 일부 손실 |
| 잡음 추가 | 큼 | 정확도 하락 |
| 질의 예산 | 큼 | 구현 부담 |

최소 집단 크기 제한만으로도 대부분의 사고가 막힌다. 그룹이 기준 미만이면 표시하지 않는다.

\`\`\`sql
-- 표시 전에 집단 크기를 확인한다
SELECT region, age_band, count(*) AS n,
       CASE WHEN count(*) >= 10 THEN round(avg(amount)) ELSE NULL END AS avg_amount
FROM purchases
GROUP BY 1, 2
HAVING count(*) >= 5;      -- 5 미만 그룹은 아예 노출하지 않는다

-- 조합이 유일한 행의 비율 — 재식별 위험 지표
SELECT round(100.0 * count(*) FILTER (WHERE n = 1) / count(*), 1) AS unique_pct
FROM (SELECT region, age_band, gender, count(*) AS n
      FROM purchases GROUP BY 1, 2, 3) g;
\`\`\`

유일 비율이 높으면 범주를 넓히거나 항목을 줄인다.

## 참고

- 개인정보 가명처리 가이드라인, 재식별 위험 관리
- NIST SP 800-188, 비식별화 기술
- ISO/IEC 20889, 비식별화 용어와 기법`,
    diagram: {
      type: 'flow',
      caption: '차이 공격',
      steps: [
        { label: '전체 집계 질의', note: '정상 응답' },
        { label: '조건 좁힌 질의', note: '한 명만 제외' },
        { label: '두 값의 차이', note: '개인 값 산출', danger: true },
        { label: '반복', note: '항목별로 확장', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '질의 횟수와 개인 값 추정 정확도',
      unit: '%',
      items: [
        { label: '1회', value: 12 },
        { label: '10회', value: 38 },
        { label: '100회', value: 71 },
        { label: '1000회', value: 94, note: '상한이 없으면 결국 드러난다' },
      ],
    },
  },
  {
    slug: 'backup-encryption',
    title: '백업 암호화와 복구 키 보관 설계',
    body: `백업은 운영 데이터 전체의 사본이므로, 운영 시스템보다 통제가 느슨하면 그쪽이 공격 대상이 된다. 그런데 백업을 암호화하면 새로운 문제가 생긴다. 복구해야 하는 순간에 키를 못 찾으면 백업이 없는 것과 같다. 설계에서 정할 것은 암호화 방식이 아니라 키를 어디에 어떻게 두고 누가 꺼낼 수 있게 하느냐다.

## 무엇이 잘못될 수 있는가?

| 실패 | 원인 |
| --- | --- |
| 백업 유출 | 암호화 없음, 접근 통제 없음 |
| 복구 불가 | 키 분실, 담당자 부재, 절차 미검증 |

한쪽만 신경 쓰면 다른 쪽에서 사고가 난다. 키를 데이터와 함께 두면 유출에 무력하고, 너무 엄격하게 두면 정작 복구 때 못 꺼낸다.

![백업과 키가 놓이는 위치](/img/posts/backup-encryption.svg)

## 키 보관 원칙

- 백업과 다른 신뢰 경계에 둔다 — 다른 계정, 다른 저장소
- 복구 권한을 가진 사람을 둘 이상 지정한다
- 한 사람만으로 꺼낼 수 없게 하되, 두 사람이면 가능하게 한다
- 담당자 변경 시 인계 절차를 문서로 남긴다
- 키 자체도 백업한다 — 다른 매체, 다른 장소

## 랜섬웨어를 전제로 설계한다

같은 자격 증명으로 지울 수 있는 백업은 랜섬웨어 상황에서 함께 사라진다. 보관 기간 안에는 수정·삭제가 불가능한 저장소를 한 곳 두고, 그 저장소의 권한은 백업을 만드는 계정과 분리한다. 사본 개수보다 이 한 가지가 복구 성공 여부를 정한다.

![복구 훈련에서 확인할 것](/img/posts/backup-encryption-2.svg)

## 복구 훈련이 곧 검증이다

암호화된 백업에서 실제로 복구해 보지 않으면 키가 맞는지, 절차가 도는지 알 수 없다. 분기마다 한 번은 격리된 환경에서 복구까지 해 보고 걸린 시간을 기록한다. 그 시간이 곧 우리가 감당할 수 있는 중단 시간이다.

## 오래된 백업의 키도 유지한다

키를 회전하면 예전 백업은 예전 키로만 풀린다. 보관 기간 동안 옛 키를 함께 유지해야 하고, 어떤 백업이 어떤 키로 만들어졌는지 기록이 있어야 한다. 이 기록이 없으면 백업은 있는데 열 수 없는 상태가 된다.

## 바로 확인하기

백업이 실제로 암호화돼 있는지, 키가 함께 들어 있지 않은지 본다.

\`\`\`bash
# 암호화 여부 — 평문 문자열이 그대로 보이면 암호화되지 않았다
head -c 4096 backup-latest.dump | strings | head -20

# 아카이브에 키가 함께 들어갔는지
tar -tzf backup-latest.tar.gz 2>/dev/null | grep -iE '\\.key$|\\.pem$|keystore|credentials' | head
\`\`\`

삭제 방지가 걸려 있는지도 확인한다.

\`\`\`bash
aws s3api get-object-lock-configuration --bucket backup-vault 2>/dev/null \\
  || echo '객체 잠금 없음 — 같은 권한으로 지울 수 있다'

aws s3api get-bucket-versioning --bucket backup-vault
\`\`\`

## 실제로 이렇게 터진다

백업을 암호화했는데 복구 키를 같은 저장소에 둔 사례가 있다. 저장소 접근 권한이 있으면 백업과 키를 함께 얻을 수 있었다.

반대로 키를 너무 잘 숨겨 복구하지 못한 경우도 있다. 키를 아는 사람이 퇴사했고 문서에 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 저장소가 암호화되면 충분하다 | 저장소 권한자는 그대로 읽는다 |
| 키를 같이 두면 편하다 | 함께 유출된다 |
| 키는 한 사람이 알면 된다 | 그 사람이 없으면 복구 못 한다 |
| 복구 시험은 가끔 하면 된다 | 키 접근까지 시험해야 한다 |
| 오래된 백업은 지우면 된다 | 키도 함께 관리해야 한다 |

## 키를 어디에 두는가

| 방식 | 유출 대비 | 복구 가능성 |
| --- | --- | --- |
| 백업과 같은 곳 | 없음 | 쉬움 |
| 같은 계정의 키 서비스 | 부분 | 쉬움 |
| 별도 계정의 키 서비스 | 좋음 | 절차 필요 |
| 오프라인 보관 | 매우 좋음 | 절차 필요 |
| 분할 보관 | 매우 좋음 | 여러 명 필요 |

별도 계정의 키 서비스가 균형점이다. 백업 저장소가 침해돼도 복호할 수 없고, 복구 절차는 자동화할 수 있다.

## 복구 가능성을 어떻게 보장하는가

1. 키 접근 권한자를 두 명 이상 둔다
2. 절차서에 키 접근 방법을 적는다 — 값이 아니라 방법
3. 분기 복구 시험에 키 접근을 포함한다
4. 담당자 변경 시 인수인계 항목에 넣는다
5. 오프라인 사본을 금고에 보관한다 — 봉인하고 접근을 기록

3번이 핵심이다. 백업 복원만 시험하고 키 접근은 늘 아는 사람이 하면, 그 사람이 없을 때를 시험하지 않은 것이다.

\`\`\`bash
# 백업 암호화와 잠금 상태 확인
aws backup list-backup-vaults \
  --query 'BackupVaultList[].[BackupVaultName,EncryptionKeyArn,Locked]' --output table

# 백업이 쓰는 키가 별도 계정인지 확인
aws kms describe-key --key-id "$BACKUP_KEY" \
  --query 'KeyMetadata.[Arn,KeyManager,MultiRegion]' --output text
\`\`\`

## 참고

- NIST SP 800-34, 비상 계획 수립 지침
- NIST SP 800-57, 키 관리와 보관 기간
- 클라우드 공급자별 객체 잠금 문서`,
    diagram: {
      type: 'matrix',
      caption: '백업과 키의 관계',
      x: ['키 분리 보관', '키 동봉'],
      y: ['삭제 방지 있음', '없음'],
      cells: ['유출·랜섬웨어 모두 대비', '유출 시 무력', '랜섬웨어에 함께 삭제', '백업이 없는 것과 같음'],
    },
    diagram2: {
      type: 'steps',
      caption: '분기 복구 훈련',
      steps: [
        { label: '격리 환경 준비', note: '운영과 분리' },
        { label: '키 인출', note: '두 사람 절차 확인' },
        { label: '복구 수행', note: '소요 시간 기록' },
        { label: '결과 검증', note: '데이터 정합성 확인' },
      ],
    },
  },
]
