import type { SeedPost } from './types'

/** 610~619 — 클라우드 설정에서 새는 자리 */
export const posts62: SeedPost[] = [
  {
    slug: 'cloud-metadata-protection',
    title: '클라우드 메타데이터 주소를 지키는 법',
    body: `클라우드 인스턴스는 자기 자신에 대한 정보를 특정 주소에서 받아 온다. 그 정보에 임시 자격 증명이 들어 있어, 서버가 임의의 주소로 요청을 보내게 만들 수 있으면 그 자격 증명이 밖으로 나간다.

## 어디를 거쳐 닿나

| 경로 | 설명 |
| --- | --- |
| 서버 측 요청 위조 | 주소를 입력받는 기능 |
| 이미지 변환 | 원본 주소를 받아 처리 |
| 웹훅 등록 | 우리가 호출해 주는 주소 |
| 문서 변환 | 외부 자원 참조 |
| 프록시 기능 | 그대로 전달 |

이미지나 문서를 주소로 받아 처리하는 기능이 특히 흔한 입구다. 기능 자체가 서버에서 임의 주소를 부르는 일이라 그렇다.

![메타데이터에 닿는 경로](/img/posts/cloud-metadata-protection.svg)

## 막는 층이 여럿이다

| 층 | 방법 |
| --- | --- |
| 애플리케이션 | 주소 검증, 대역 차단 |
| 프록시 | 나가는 요청을 한 곳으로 |
| 인스턴스 | 세션 토큰 요구 방식 |
| 네트워크 | 메타데이터 주소 차단 |

인스턴스 설정에서 토큰을 요구하는 방식으로 바꾸면, 단순한 요청 위조로는 자격 증명을 꺼낼 수 없게 된다. 설정 한 번으로 큰 부분이 닫힌다.

## 권한을 줄이는 것이 함께 간다

자격 증명이 나가더라도 그 권한이 좁으면 피해가 작다.

| 항목 | 방법 |
| --- | --- |
| 역할 범위 | 그 인스턴스가 하는 일만 |
| 자원 제한 | 특정 버킷·테이블만 |
| 조건 | 출발지·시간 제한 |
| 수명 | 짧게 |

![막는 층](/img/posts/cloud-metadata-protection-2.svg)

## 실제로 닿는지 시험한다

\`\`\`bash
# 주소를 입력받는 기능에 메타데이터 주소를 넣어 본다
curl -s -G https://example.com/api/fetch-image \
  --data-urlencode 'url=http://169.254.169.254/latest/meta-data/' | head -c 200

# 인스턴스에서 직접 — 토큰 요구 방식이면 이 요청은 실패한다
curl -s -m 3 http://169.254.169.254/latest/meta-data/ | head -c 100
\`\`\`

첫 요청에서 목록이 돌아오면 그 기능이 그대로 통로다. 두 번째가 성공하면 인스턴스가 아직 옛 방식으로 설정돼 있다.

## 컨테이너에서는 다르다

컨테이너 환경에서는 각 파드나 작업에 별도 자격 증명이 주어지고, 그 경로도 다르다. 호스트의 메타데이터에 컨테이너가 닿지 못하게 막는 설정이 따로 있다. 막지 않으면 컨테이너 하나가 호스트 전체의 권한을 얻는다.

## 참고

- OWASP — SSRF Prevention Cheat Sheet
- 클라우드 사업자 인스턴스 메타데이터 문서
- CWE-918 — Server-Side Request Forgery`,
    diagram: {
      type: 'flow',
      caption: '메타데이터에 닿는 경로',
      steps: [
        { label: '주소를 입력받는 기능', note: '이미지·웹훅' },
        { label: '검증 없이 호출', note: '서버가 요청', danger: true },
        { label: '메타데이터 응답', note: '임시 자격 증명' },
        { label: '권한만큼 접근', note: '범위가 피해' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '막는 층',
      layers: [
        { label: '인스턴스 설정', note: '토큰 요구' },
        { label: '네트워크', note: '주소 차단' },
        { label: '프록시', note: '나가는 요청 통제' },
        { label: '권한 축소', note: '피해 범위' },
      ],
    },
  },
  {
    slug: 'storage-public-access',
    title: '저장소가 공개로 열리는 경로들',
    body: `클라우드 저장소가 공개로 설정돼 데이터가 노출된 사고는 계속 반복된다. 대부분 의도한 설정이 아니라, 여러 층의 권한이 겹치면서 어느 하나가 열려 있는 상태다.

## 버킷은 어디서 열리나

| 층 | 설정 |
| --- | --- |
| 계정 차원 공개 차단 | 켜져 있으면 아래를 덮음 |
| 버킷 정책 | 조건에 모두 허용 |
| 객체 접근 목록 | 개별 파일 공개 |
| 미리 서명된 주소 | 만료 없이 발급 |
| CDN 설정 | 원본과 다른 접근 |

개별 파일 단위로 열리는 경우가 찾기 어렵다. 버킷은 닫혀 있는데 특정 파일만 공개인 상태다.

![공개로 열리는 층](/img/posts/storage-public-access.svg)

## 계정 차원에서 먼저 막는다

계정 전체에 공개 차단을 켜면 아래 설정과 무관하게 막힌다. 공개가 필요한 저장소가 있다면 그 계정을 분리하는 편이 낫다.

| 구성 | 결과 |
| --- | --- |
| 계정 전체 차단 | 실수해도 안 열림 |
| 저장소마다 판단 | 하나만 틀려도 열림 |
| 공개 계정 분리 | 실수 범위 축소 |

## 미리 서명된 주소의 수명

| 용도 | 수명 |
| --- | --- |
| 화면에 이미지 표시 | 수 분 |
| 파일 내려받기 | 수 분~수 시간 |
| 외부 연동 전달 | 필요 기간만 |
| 무기한 | 공개와 같음 |

만료가 없는 서명 주소는 공개 링크와 다르지 않다. 주소가 어딘가에 남으면 그 파일은 계속 열려 있다.

![서명 주소의 수명](/img/posts/storage-public-access-2.svg)

## 지금 열려 있는지 본다

\`\`\`bash
# 계정 차원 공개 차단 설정
aws s3control get-public-access-block --account-id "$ACCOUNT_ID" 2>/dev/null

# 버킷별 공개 여부
aws s3api list-buckets --query 'Buckets[].Name' --output text | tr '\t' '\n' | while read -r b; do
  p=$(aws s3api get-public-access-block --bucket "$b" 2>/dev/null | grep -c 'true')
  [ "$p" -lt 4 ] && echo "확인 필요: $b"
done
\`\`\`

네 가지 차단 설정이 모두 켜져 있지 않은 버킷이 확인 대상이다. 공개가 의도인 경우도 있으므로 목록을 만들어 하나씩 판단한다.

## 로그가 있어야 확인이 된다

저장소 접근 로그를 켜 두면 공개로 열려 있던 기간에 누가 무엇을 가져갔는지 알 수 있다. 로그가 없으면 노출 사실만 알고 피해 범위는 영영 모른다. 켜는 비용이 작고 사고 때의 값어치가 크다. 접근 로그는 저장소 자신이 아니라 별도 위치에 쌓아야 하고, 그 보관 기간을 신고 의무 기간보다 길게 잡아야 조사에 쓸 수 있다.

## 참고

- CIS Benchmarks — 클라우드 저장소 설정
- NIST SP 800-53 — 접근 통제
- 개인정보보호법 — 안전성 확보조치`,
    diagram: {
      type: 'layers',
      caption: '공개로 열리는 층',
      layers: [
        { label: '계정 차단', note: '켜면 아래를 덮음' },
        { label: '버킷 정책', note: '조건에 모두 허용' },
        { label: '객체 접근 목록', note: '개별 파일' },
        { label: '서명 주소', note: '만료 없음' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '서명 주소 수명',
      unit: '분',
      items: [
        { label: '이미지 표시', value: 5, note: '짧게' },
        { label: '파일 내려받기', value: 60, note: '보통' },
        { label: '외부 전달', value: 1440, note: '필요 기간만' },
        { label: '무기한', value: 10080, note: '공개와 같음' },
      ],
    },
  },
  {
    slug: 'iam-policy-review',
    title: '권한 정책을 읽어 내는 순서',
    body: `클라우드 권한 정책은 여러 개가 겹쳐 최종 권한을 만든다. 하나만 읽어서는 그 사람이 실제로 무엇을 할 수 있는지 알 수 없고, 그래서 넓은 권한이 조용히 남는다.

## 무엇이 겹치나

| 층 | 역할 |
| --- | --- |
| 조직 정책 | 최대 범위를 제한 |
| 역할·그룹 정책 | 부여 |
| 사용자 직접 정책 | 부여 |
| 자원 정책 | 그쪽에서 허용 |
| 세션 정책 | 일시적 축소 |

자원 쪽에서 허용하는 정책이 놓치기 쉽다. 사용자에게 권한이 없어도 자원이 열어 주면 접근된다.

![권한이 겹치는 층](/img/posts/iam-policy-review.svg)

## 넓은 권한의 모양

| 표현 | 의미 |
| --- | --- |
| 모든 동작 허용 | 무엇이든 |
| 모든 자원 대상 | 어디에나 |
| 조건 없음 | 언제 어디서나 |
| 관리 권한 부여 가능 | 스스로 권한 확대 |

마지막이 가장 위험하다. 권한을 부여할 수 있는 권한을 가지면 지금 없는 권한도 스스로 만들 수 있다.

## 실제 권한을 확인한다

정책 문서를 읽는 대신 도구로 실제 권한을 계산한다. 겹침을 사람이 계산하면 틀린다.

| 방법 | 내용 |
| --- | --- |
| 정책 시뮬레이터 | 특정 동작 가능 여부 |
| 마지막 사용 시각 | 쓰지 않는 권한 |
| 접근 분석기 | 외부에 열린 자원 |

![권한 축소 순서](/img/posts/iam-policy-review-2.svg)

## 쓰지 않는 권한을 찾는다

\`\`\`bash
# 역할별 마지막 사용 시각 — 오래 안 쓴 역할
aws iam list-roles --query 'Roles[].RoleName' --output text | tr '\t' '\n' | while read -r r; do
  d=$(aws iam get-role --role-name "$r" \
    --query 'Role.RoleLastUsed.LastUsedDate' --output text 2>/dev/null)
  [ "$d" = "None" ] && echo "미사용: $r"
done | head -20
\`\`\`

한 번도 쓰이지 않은 역할은 삭제 후보다. 다만 연 1회 도는 작업용일 수 있으므로, 담당자를 확인하고 지운다.

## 최소 권한으로 줄이는 순서

한 번에 전부 줄이려 하면 무엇이 깨질지 몰라 시작하지 못한다. 마지막 사용 기록을 근거로 명백히 쓰지 않는 것부터 빼고, 그다음에 범위를 좁힌다. 각 단계 사이에 관찰 기간을 두면 되돌릴 기회가 생긴다. 줄인 내역은 변경 이력으로 남겨 두어야 한다. 무언가 멈췄을 때 어떤 권한을 언제 뺐는지 바로 대조할 수 있어야, 다음 축소를 겁내지 않고 이어 갈 수 있다. 되돌린 기록도 함께 남기면 어떤 권한이 실제로 필요한지가 정리된다.

## 참고

- CIS Benchmarks — 클라우드 권한 관리
- NIST SP 800-53 — 최소 권한
- 클라우드 사업자 권한 분석 도구 문서`,
    diagram: {
      type: 'layers',
      caption: '권한이 겹치는 층',
      layers: [
        { label: '조직 정책', note: '최대 범위' },
        { label: '역할·그룹', note: '부여' },
        { label: '자원 정책', note: '그쪽에서 허용' },
        { label: '세션 정책', note: '일시 축소' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '권한 축소 순서',
      steps: [
        { label: '미사용 제거', note: '기록 근거' },
        { label: '자원 범위 좁힘', note: '특정 대상만' },
        { label: '조건 추가', note: '출발지·시간' },
        { label: '관찰 기간', note: '되돌릴 기회' },
      ],
    },
  },
  {
    slug: 'multi-account-boundary',
    title: '계정을 나눠 사고 범위를 가두기',
    body: `모든 자원을 한 계정에 두면 그 계정의 권한 하나가 전부에 닿는다. 계정을 나누면 권한도 나뉘고, 한쪽에서 생긴 사고가 다른 쪽으로 넘어가지 않는다.

## 무엇을 기준으로 나누나

| 기준 | 효과 |
| --- | --- |
| 환경 | 운영·시험 분리 |
| 서비스 | 팀별 경계 |
| 민감도 | 개인정보 별도 |
| 규제 | 감사 범위 축소 |
| 청구 | 비용 귀속 |

환경을 나누는 것이 가장 기본이다. 시험 환경의 넓은 권한이 운영에 닿지 않게 된다.

![계정을 나눴을 때의 경계](/img/posts/multi-account-boundary.svg)

## 나누면 생기는 부담

| 항목 | 부담 |
| --- | --- |
| 계정 관리 | 수가 늘어남 |
| 권한 연결 | 교차 접근 설정 |
| 로그 수집 | 한곳으로 모아야 |
| 네트워크 | 연결 구성 |
| 비용 관리 | 통합 청구 |

로그를 한곳에 모으는 일이 특히 중요하다. 계정마다 흩어져 있으면 사고 조사에서 여러 곳을 오가야 한다.

## 조직 정책으로 덮는다

| 정책 | 내용 |
| --- | --- |
| 지역 제한 | 쓰지 않는 지역 차단 |
| 로그 비활성화 금지 | 감사 기록 보호 |
| 루트 사용자 제한 | 일상 사용 금지 |
| 특정 서비스 차단 | 쓰지 않는 것 |

조직 차원 정책은 계정 관리자도 넘을 수 없다. 최소한의 방어선을 여기에 둔다.

![조직 정책이 덮는 범위](/img/posts/multi-account-boundary-2.svg)

## 구조를 확인한다

\`\`\`bash
# 조직 안의 계정 목록과 상태
aws organizations list-accounts \
  --query 'Accounts[].[Name,Status,Email]' --output table 2>/dev/null | head -20

# 조직 정책이 붙어 있는지
aws organizations list-policies --filter SERVICE_CONTROL_POLICY \
  --query 'Policies[].Name' --output text 2>/dev/null
\`\`\`

정책이 하나도 없으면 계정만 나뉘고 통제는 없는 상태다. 나누는 것 자체보다 그 위에 무엇을 얹었는지가 실제 효과를 정한다.

## 나중에 나누기는 어렵다

한 계정에 쌓인 자원을 나중에 나누려면 이관 작업이 크다. 처음부터 나눠 두는 편이 훨씬 싸다. 지금 계정이 하나라면, 앞으로 만드는 것부터 새 계정에 두는 방식으로 조금씩 옮기는 것이 현실적이다. 이때 새 계정의 기본 설정을 템플릿으로 만들어 두면 계정이 늘어도 관리 부담이 비례해 늘지 않는다. 감사 로그 전송, 기본 차단 정책, 경보 연결을 처음부터 넣어 둔 계정만 만들어지게 하는 것이 핵심이다.

## 참고

- CIS Benchmarks — 계정 분리
- NIST SP 800-53 — 경계 보호
- 클라우드 사업자 다중 계정 설계 문서`,
    diagram: {
      type: 'matrix',
      caption: '계정을 나눴을 때의 경계',
      x: ['같은 계정', '다른 계정'],
      y: ['사고 발생', '정상'],
      cells: ['전부 영향', '그 계정만', '해당 없음', '해당 없음'],
    },
    diagram2: {
      type: 'steps',
      caption: '조직 정책이 덮는 범위',
      steps: [
        { label: '지역 제한', note: '쓰지 않는 곳' },
        { label: '로그 보호', note: '끄지 못하게' },
        { label: '루트 제한', note: '일상 금지' },
        { label: '서비스 차단', note: '쓰지 않는 것' },
      ],
    },
  },
  {
    slug: 'serverless-permission-scope',
    title: '함수마다 권한을 따로 주는 이유',
    body: `서버리스 함수는 작고 많다. 편의상 여러 함수가 같은 역할을 공유하면, 그 역할은 모든 함수가 필요한 권한의 합집합이 된다. 함수 하나가 뚫리면 그 합집합 전체가 열린다.

## 서버리스에서 역할을 공유했을 때

| 구성 | 한 함수 침해 시 |
| --- | --- |
| 역할 공유 | 전체 권한 |
| 함수별 역할 | 그 함수 권한만 |

함수가 스무 개면 공유 역할은 스무 개 함수의 권한을 전부 담는다. 이미지 처리 함수가 결제 테이블에 쓸 수 있는 상태가 된다.

![역할을 공유했을 때](/img/posts/serverless-permission-scope.svg)

## 왜 공유하게 되나

| 이유 | 대안 |
| --- | --- |
| 만들기 번거로움 | 배포 도구가 자동 생성 |
| 무엇이 필요한지 모름 | 기록으로 확인 |
| 권한 오류가 잦음 | 관찰 모드로 시작 |
| 관리 대상이 늘어남 | 코드로 정의 |

배포 도구가 함수마다 역할을 만들어 주게 하면 번거로움이 사라진다. 사람이 콘솔에서 만드는 방식이 공유를 부른다.

## 무엇이 필요한지 알아내기

| 방법 | 내용 |
| --- | --- |
| 넓게 주고 기록 확인 | 실제 호출을 봄 |
| 좁게 주고 오류 확인 | 하나씩 추가 |
| 도구로 추론 | 코드에서 추출 |

넓게 주고 며칠 기록을 본 뒤 실제 쓰인 것만 남기는 방식이 현실적이다. 다만 그 며칠 동안은 넓은 권한이 열려 있으므로 운영이 아닌 환경에서 한다.

![권한을 좁히는 순서](/img/posts/serverless-permission-scope-2.svg)

## 공유 여부를 확인한다

\`\`\`bash
# 함수별 역할 — 같은 역할을 여러 함수가 쓰는지
aws lambda list-functions \
  --query 'Functions[].[FunctionName,Role]' --output text 2>/dev/null \
  | awk '{print $2}' | sort | uniq -c | sort -rn | head -10
\`\`\`

한 역할에 함수가 여럿 붙어 있으면 그 역할의 권한이 그만큼 넓다는 뜻이다. 가장 많이 붙은 것부터 나눈다.

## 환경 변수도 같은 문제다

함수에 넣은 환경 변수는 그 함수를 볼 수 있는 사람이 전부 읽는다. 여러 함수가 같은 값을 쓰면 그 값의 노출 면적도 함께 넓어진다. 비밀값은 환경 변수 대신 시크릿 저장소에서 실행 시점에 가져오고, 그 저장소 접근 권한도 함수마다 좁게 준다.

## 참고

- OWASP — 서버리스 Top 10
- NIST SP 800-53 — 최소 권한
- 클라우드 사업자 함수 권한 문서`,
    diagram: {
      type: 'matrix',
      caption: '역할을 공유했을 때',
      x: ['역할 공유', '함수별 역할'],
      y: ['함수 침해', '정상'],
      cells: ['전체 권한', '그 함수만', '해당 없음', '해당 없음'],
    },
    diagram2: {
      type: 'steps',
      caption: '권한을 좁히는 순서',
      steps: [
        { label: '넓게 시작', note: '운영 아닌 곳에서' },
        { label: '기록 확인', note: '실제 호출' },
        { label: '쓰인 것만 남김', note: '나머지 제거' },
        { label: '코드로 고정', note: '재발 방지' },
      ],
    },
  },
  {
    slug: 'container-escape-surface',
    title: '컨테이너가 호스트로 넘어가는 자리',
    body: `컨테이너는 격리된 것처럼 보이지만 호스트 커널을 공유한다. 설정 몇 가지가 그 격리를 얇게 만들고, 그 상태에서 컨테이너 하나가 뚫리면 호스트와 다른 컨테이너까지 이어진다.

## 격리를 얇게 만드는 설정

| 설정 | 결과 |
| --- | --- |
| 특권 모드 | 사실상 호스트 권한 |
| 호스트 경로 마운트 | 파일 시스템 접근 |
| 호스트 네트워크 | 네트워크 격리 해제 |
| 도커 소켓 마운트 | 다른 컨테이너 조작 |
| 위험한 기능 추가 | 커널 동작 접근 |

도커 소켓을 마운트하는 구성이 특히 위험하다. 그 소켓으로 새 컨테이너를 특권 모드로 띄울 수 있어 사실상 호스트 장악이다.

![격리가 얇아지는 설정](/img/posts/container-escape-surface.svg)

## 왜 그렇게 설정하나

| 이유 | 대안 |
| --- | --- |
| 빌드 안에서 빌드 | 전용 빌더 사용 |
| 로그 수집 | 표준 출력 사용 |
| 장치 접근 | 필요한 장치만 지정 |
| 편의 | 명시적 설정 |

대부분 편의 때문이고 대안이 있다. 다만 대안이 조금 번거로워 그대로 두는 경우가 많다.

## 줄이는 기본 설정

| 항목 | 값 |
| --- | --- |
| 루트가 아닌 사용자 | 지정 |
| 읽기 전용 파일 시스템 | 켜기 |
| 기능 전부 제거 후 필요한 것만 | 최소화 |
| 권한 상승 금지 | 켜기 |
| 자원 한도 | 설정 |

읽기 전용 파일 시스템은 침입자가 도구를 내려받아 실행하는 것을 어렵게 한다. 쓰기가 필요한 경로만 별도로 붙인다.

![기본 설정으로 줄이기](/img/posts/container-escape-surface-2.svg)

## 지금 설정을 본다

\`\`\`bash
# 특권 모드나 위험한 마운트가 있는 컨테이너
docker ps -q | while read -r c; do
  docker inspect "$c" --format '{{.Name}} priv={{.HostConfig.Privileged}} pid={{.HostConfig.PidMode}} net={{.HostConfig.NetworkMode}}'
done | grep -E 'priv=true|pid=host|net=host'

# 도커 소켓을 마운트한 컨테이너
docker ps -q | while read -r c; do
  docker inspect "$c" --format '{{.Name}} {{range .Mounts}}{{.Source}} {{end}}' | grep docker.sock
done
\`\`\`

여기서 나오는 컨테이너가 우선 검토 대상이다. 필요해서 그런 것인지, 예전에 그렇게 만들고 그대로 둔 것인지 구분한다.

## 정책으로 강제한다

설정을 사람이 지키게 하면 새 워크로드에서 빠진다. 배포 시점에 검사해 위반을 거절하는 정책을 두면 그 자리가 닫힌다. 처음에는 경고만 하다가 목록이 정리된 뒤 차단으로 바꾸는 순서가 무리가 없다. 예외가 필요한 워크로드는 명시적으로 등록하고 담당자와 사유를 적어 둔다. 사유가 적히지 않은 예외는 시간이 지나면 아무도 손대지 못하는 자리가 된다.

## 참고

- NIST SP 800-190 — 컨테이너 보안 지침
- CIS Docker Benchmark
- OWASP — 컨테이너 보안`,
    diagram: {
      type: 'layers',
      caption: '격리가 얇아지는 설정',
      layers: [
        { label: '특권 모드', note: '사실상 호스트' },
        { label: '도커 소켓', note: '다른 컨테이너 조작' },
        { label: '호스트 경로', note: '파일 접근' },
        { label: '호스트 네트워크', note: '격리 해제' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '기본 설정으로 줄이기',
      steps: [
        { label: '루트 아닌 사용자', note: '기본' },
        { label: '읽기 전용', note: '도구 실행 차단' },
        { label: '기능 최소화', note: '필요한 것만' },
        { label: '권한 상승 금지', note: '켜 둔다' },
      ],
    },
  },
  {
    slug: 'k8s-secret-handling',
    title: '쿠버네티스에서 비밀값이 새는 자리',
    body: `쿠버네티스의 비밀값 객체는 이름만 비밀이다. 기본 설정에서는 인코딩만 돼 있고 암호화되지 않으며, 볼 수 있는 사람이 생각보다 많다.

## 누가 볼 수 있나

| 경로 | 조건 |
| --- | --- |
| 해당 네임스페이스 조회 권한 | 목록·내용 |
| 파드에 마운트 | 그 파드 안에서 |
| 노드 접근 | 그 노드의 것 |
| etcd 접근 | 전부 |
| 백업 | 저장된 형태 그대로 |

백업이 자주 잊힌다. 클러스터 백업에 비밀값이 평문에 가까운 형태로 들어가고, 그 백업의 접근 통제는 대개 느슨하다.

![비밀값이 보이는 경로](/img/posts/k8s-secret-handling.svg)

## 기본 설정에서 해야 할 것

| 항목 | 내용 |
| --- | --- |
| 저장 시 암호화 | 켜기 |
| 권한 분리 | 네임스페이스별 |
| 목록 조회 제한 | 필요한 것만 |
| 환경 변수 대신 파일 | 노출 면적 축소 |

환경 변수로 주입하면 프로세스 목록이나 오류 화면에 나올 수 있다. 파일로 마운트하면 그 경로를 읽는 경우로 좁혀진다.

## 외부 저장소를 쓸 때

| 방식 | 장점 |
| --- | --- |
| 외부 시크릿 저장소 연동 | 회전·감사 일원화 |
| 실행 시점 주입 | 클러스터에 남지 않음 |
| 짧은 수명 자격 증명 | 유출 창 축소 |

외부 저장소를 쓰면 회전과 접근 기록이 한곳에 모인다. 클러스터 안에 값이 남지 않는 구성이 가장 낫다.

![비밀값을 다루는 방식](/img/posts/k8s-secret-handling-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 저장 시 암호화가 켜져 있는지 (관리형이면 사업자 문서 확인)
kubectl get secrets -A --no-headers 2>/dev/null | wc -l

# 비밀값을 환경 변수로 주입하는 워크로드
kubectl get pods -A -o json 2>/dev/null \
  | grep -o '"secretKeyRef"' | wc -l

# 누가 비밀값을 읽을 수 있는지
kubectl auth can-i get secrets --all-namespaces --as=system:serviceaccount:default:default
\`\`\`

기본 서비스 계정이 비밀값을 읽을 수 있으면 그 네임스페이스의 모든 파드가 읽을 수 있다는 뜻이다. 그 설정부터 좁힌다.

## 저장소에 들어간 것은 별개다

매니페스트 파일에 비밀값을 적어 저장소에 올리면 클러스터 설정과 무관하게 이미 노출된 것이다. 암호화해서 커밋하는 방식이나 참조만 두는 방식으로 바꾸고, 이미 올라간 값은 폐기한다. 저장소 이력에서 지우는 것만으로는 부족하다. 복제본과 병합 요청, 빌드 기록에 그 값이 남아 있을 수 있으므로 값 자체를 회전하는 것이 유일하게 확실한 조치다.

## 참고

- 쿠버네티스 문서 — Secret 보호
- NIST SP 800-190 — 컨테이너 오케스트레이션 보안
- CIS Kubernetes Benchmark`,
    diagram: {
      type: 'layers',
      caption: '비밀값이 보이는 경로',
      layers: [
        { label: '조회 권한', note: '네임스페이스' },
        { label: '마운트된 파드', note: '그 안에서' },
        { label: '노드', note: '그 노드의 것' },
        { label: '백업', note: '자주 잊힘' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '방식별 노출 면적',
      unit: '점',
      items: [
        { label: '외부 저장소 주입', value: 15, note: '남지 않음' },
        { label: '파일 마운트', value: 40, note: '경로만' },
        { label: '환경 변수', value: 65, note: '목록·오류에' },
        { label: '매니페스트에 직접', value: 100, note: '이미 노출' },
      ],
    },
  },
  {
    slug: 'k8s-admission-policy',
    title: '배포 시점에 거절하는 규칙 만들기',
    body: `클러스터에 들어오는 워크로드를 배포 시점에 검사해 기준에 맞지 않으면 거절할 수 있다. 사후에 찾아 고치는 것보다 애초에 못 들어오게 하는 편이 비용이 적다.

## 쿠버네티스에서 무엇을 거절하나

| 항목 | 이유 |
| --- | --- |
| 특권 모드 | 격리 해제 |
| 루트 실행 | 권한 과다 |
| 태그가 latest | 무엇이 도는지 불명 |
| 자원 한도 없음 | 다른 워크로드 영향 |
| 서명 없는 이미지 | 출처 불명 |

태그가 고정되지 않은 이미지는 지금 도는 것이 무엇인지 알 수 없게 만든다. 사고 조사에서 버전을 특정할 수 없다.

![배포 시점의 검사](/img/posts/k8s-admission-policy.svg)

## 어떻게 시작하나

처음부터 거절하면 기존 워크로드가 배포되지 않아 반발이 크다. 단계를 둔다.

| 단계 | 처리 |
| --- | --- |
| 1 | 위반을 기록만 |
| 2 | 경고 표시 |
| 3 | 새 워크로드만 거절 |
| 4 | 전체 거절 |

1단계에서 나온 목록이 곧 정리 대상이다. 그 목록을 줄여 가며 단계를 올린다.

## 네임스페이스별로 다르게

| 환경 | 수준 |
| --- | --- |
| 운영 | 엄격 |
| 시험 | 경고 |
| 개발 | 기록만 |
| 시스템 | 예외 목록 |

시스템 구성 요소는 특권이 필요한 경우가 있다. 예외를 두되 그 목록을 짧게 유지하고 주기적으로 확인한다.

![단계적으로 올리기](/img/posts/k8s-admission-policy-2.svg)

## 위반 현황을 본다

\`\`\`bash
# 특권 모드로 도는 파드
kubectl get pods -A -o json 2>/dev/null \
  | grep -c '"privileged":true'

# 자원 한도가 없는 파드 — 다른 워크로드에 영향을 준다
kubectl get pods -A -o json 2>/dev/null \
  | python3 -c 'import json,sys; d=json.load(sys.stdin);
print(sum(1 for p in d["items"] for c in p["spec"]["containers"] if not c.get("resources",{}).get("limits")))'
\`\`\`

숫자가 크면 한 번에 거절로 바꿀 수 없다. 기록 단계에서 목록을 만들고 팀별로 나눠 처리하는 편이 실제로 줄어든다.

## 규칙을 코드로 둔다

정책을 클러스터 안에만 두면 어떤 규칙이 있는지 알기 어렵고 변경 이력도 남지 않는다. 코드로 정의해 저장소에 두면 리뷰를 거치고, 새 클러스터에도 같은 규칙이 적용된다. 규칙 자체가 변경 통제의 대상이 되어야 한다.

## 참고

- 쿠버네티스 문서 — 어드미션 컨트롤
- CIS Kubernetes Benchmark
- NIST SP 800-190 — 오케스트레이션 보안`,
    diagram: {
      type: 'flow',
      caption: '배포 시점의 검사',
      steps: [
        { label: '배포 요청', note: '매니페스트' },
        { label: '규칙 검사', note: '기준 대조' },
        { label: '위반이면 거절', note: '들어오지 못함' },
        { label: '통과만 실행', note: '사후 정리 불필요' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '단계적으로 올리기',
      steps: [
        { label: '기록만', note: '목록 작성' },
        { label: '경고', note: '인지시키기' },
        { label: '새 것만 거절', note: '기존 유예' },
        { label: '전체 거절', note: '정리 후' },
      ],
    },
  },
  {
    slug: 'cloud-logging-gaps',
    title: '클라우드 감사 로그에 비는 구간',
    body: `클라우드 사업자가 제공하는 감사 로그는 관리 동작을 기록한다. 그런데 기본 설정으로는 기록되지 않는 것이 여럿 있고, 그 공백이 사고 조사에서 드러난다.

## 무엇이 기본으로 안 남나

| 대상 | 기본 |
| --- | --- |
| 관리 동작 | 남음 |
| 데이터 읽기 | 대개 안 남음 |
| 데이터 쓰기 | 설정에 따라 |
| 특정 지역 | 설정한 곳만 |
| 다른 계정의 동작 | 그 계정에 |

데이터 읽기가 안 남는 것이 가장 큰 공백이다. 누가 어떤 파일을 가져갔는지가 조사의 핵심인데 그 기록이 없다.

![기본 설정의 공백](/img/posts/cloud-logging-gaps.svg)

## 켜면 비용이 는다

| 대상 | 양 |
| --- | --- |
| 관리 동작 | 적음 |
| 데이터 읽기 | 매우 많음 |
| 네트워크 흐름 | 많음 |

전부 켜면 비용이 감당되지 않는다. 민감한 저장소만 데이터 접근 기록을 켜는 식으로 범위를 정한다.

## 어디에 모으나

| 항목 | 내용 |
| --- | --- |
| 별도 계정 | 침해 계정에서 지울 수 없게 |
| 쓰기 전용 | 수정 불가 설정 |
| 보관 기간 | 조사에 필요한 만큼 |
| 접근 제한 | 조회도 승인 |

로그를 같은 계정에 두면 그 계정을 장악한 사람이 지울 수 있다. 별도 계정으로 내보내는 구성이 기본이다.

![로그를 모으는 구조](/img/posts/cloud-logging-gaps-2.svg)

## 지금 무엇이 켜져 있는지 본다

\`\`\`bash
# 감사 로그 설정 — 모든 지역을 덮는지, 데이터 동작을 포함하는지
aws cloudtrail describe-trails \
  --query 'trailList[].[Name,IsMultiRegionTrail,IncludeGlobalServiceEvents]' \
  --output table 2>/dev/null

# 로그 파일 무결성 검증이 켜져 있는지
aws cloudtrail describe-trails \
  --query 'trailList[].[Name,LogFileValidationEnabled]' --output text 2>/dev/null
\`\`\`

다중 지역이 꺼져 있으면 쓰지 않는 지역에서 일어난 일이 기록되지 않는다. 공격자는 그런 지역을 골라 쓴다.

## 없는 기록은 만들 수 없다

사고가 난 뒤에 로그를 켜도 그 이전은 알 수 없다. 조사에 필요한 것이 무엇인지 미리 정하고 그것만이라도 켜 두는 편이, 전부 켰다가 비용 때문에 다시 끄는 것보다 낫다. 무엇을 포기했는지 기록해 두면 조사 때 헤매지 않는다. 무엇을 켜 둘지는 데이터 등급을 기준으로 정하는 것이 설득하기 쉽다. 개인정보가 들어 있는 저장소와 결제에 관련된 자원부터 데이터 접근 기록을 켜면, 비용과 필요 사이에서 설명 가능한 선이 생긴다.

## 참고

- CIS Benchmarks — 감사 로그 설정
- NIST SP 800-92 — 로그 관리
- 클라우드 사업자 감사 로그 문서`,
    diagram: {
      type: 'layers',
      caption: '기본 설정의 공백',
      layers: [
        { label: '관리 동작', note: '남음' },
        { label: '데이터 쓰기', note: '설정에 따라' },
        { label: '데이터 읽기', note: '대개 안 남음' },
        { label: '미사용 지역', note: '설정 안 하면 없음' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '로그를 모으는 구조',
      steps: [
        { label: '별도 계정', note: '지울 수 없게' },
        { label: '쓰기 전용', note: '수정 불가' },
        { label: '무결성 검증', note: '변조 탐지' },
        { label: '접근 승인', note: '조회도 통제' },
      ],
    },
  },
  {
    slug: 'cost-anomaly-signal',
    title: '비용 급증이 알려 주는 보안 신호',
    body: `클라우드 비용이 갑자기 오르면 대개 설정 실수나 트래픽 증가를 의심한다. 그런데 자격 증명이 유출돼 남이 우리 계정에서 자원을 쓰는 경우도 같은 모양으로 나타난다.

## 어떤 모양으로 나타나나

| 상황 | 비용 패턴 |
| --- | --- |
| 채굴용 인스턴스 | 계산 자원 급증 |
| 대량 데이터 반출 | 전송 비용 급증 |
| 스팸 발송 | 메일·메시지 비용 |
| 쓰지 않던 지역 | 그 지역에 청구 |
| 쓰지 않던 서비스 | 새 항목 등장 |

쓰지 않던 지역에 청구가 생기는 것이 가장 분명한 신호다. 정상 운영에서는 지역이 갑자기 늘지 않는다.

![비용으로 드러나는 침해](/img/posts/cost-anomaly-signal.svg)

## 왜 비용이 먼저 보이나

| 이유 | 설명 |
| --- | --- |
| 탐지 규칙보다 단순 | 숫자 하나 |
| 지역·서비스 무관 | 전부 합산됨 |
| 숨기기 어려움 | 청구는 사업자가 함 |
| 알림이 이미 있음 | 예산 경보 |

공격자가 로그를 지워도 청구 기록은 사업자 쪽에 남는다. 그래서 다른 흔적이 지워진 상황에서도 이 신호는 살아 있다.

## 경보를 어떻게 거나

| 기준 | 내용 |
| --- | --- |
| 절대 금액 | 예산 초과 |
| 증가율 | 평소 대비 배수 |
| 새 지역 | 처음 청구되는 곳 |
| 새 서비스 | 처음 쓰는 항목 |

절대 금액만 보면 작은 계정에서는 늦게 걸린다. 증가율과 새 항목 등장을 함께 보는 편이 빠르다.

![비용 경보 기준](/img/posts/cost-anomaly-signal-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 지역별 비용 — 쓰지 않는 지역에 청구가 있는지
aws ce get-cost-and-usage --time-period Start=$(date -v-30d +%F),End=$(date +%F) \
  --granularity MONTHLY --metrics UnblendedCost \
  --group-by Type=DIMENSION,Key=REGION \
  --query 'ResultsByTime[].Groups[].[Keys[0],Metrics.UnblendedCost.Amount]' \
  --output text 2>/dev/null | sort -k2 -rn | head -10
\`\`\`

목록에 낯선 지역이 있으면 그 지역의 자원을 확인한다. 쓰지 않기로 한 지역이라면 조직 정책으로 아예 막는 것이 다음 단계다.

## 경보를 누가 받나

비용 경보가 재무 담당에게만 가면 보안 관점의 해석이 늦어진다. 급증 경보를 보안 쪽에도 함께 보내고, 원인이 설정 실수인지 침해인지 확인하는 절차를 짧게 만들어 둔다. 대부분은 실수지만, 그 확인 한 번이 침해를 며칠 앞당겨 발견하게 한다.

## 참고

- CIS Benchmarks — 비용 모니터링
- NIST SP 800-137 — 지속적 모니터링
- 클라우드 사업자 비용 이상 탐지 문서`,
    diagram: {
      type: 'flow',
      caption: '비용으로 드러나는 침해',
      steps: [
        { label: '자격 증명 유출', note: '탐지 안 됨' },
        { label: '자원 생성', note: '낯선 지역' },
        { label: '비용 급증', note: '숨기기 어려움' },
        { label: '경보', note: '다른 흔적이 없어도' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '비용 경보 기준',
      steps: [
        { label: '증가율', value: undefined, note: '평소 대비' },
        { label: '새 지역', note: '처음 청구' },
        { label: '새 서비스', note: '처음 쓰는 항목' },
        { label: '절대 금액', note: '예산 초과' },
      ],
    },
  },
]
