import type { SeedPost } from './types'

/** 660~669 — 브라우저에서 벌어지는 일 */
export const posts67: SeedPost[] = [
  {
    slug: 'csp-rollout-steps',
    title: '콘텐츠 보안 정책을 켜는 순서',
    body: `보안 헤더 중에서 효과가 크지만 켜기 어려운 것이 콘텐츠 보안 정책이다. 잘못 켜면 화면이 깨지고, 느슨하게 켜면 아무것도 막지 못한다.

## 무엇을 막나

| 위협 | 차단 |
| --- | --- |
| 삽입된 스크립트 | 실행 차단 |
| 외부 자원 로드 | 출처 제한 |
| 인라인 코드 | 기본 금지 |
| 프레임 삽입 | 조상 제한 |
| 폼 전송 대상 | 목적지 제한 |

크로스 사이트 스크립팅이 성공해도 실행을 막는 마지막 층이다. 입력 검증이 뚫린 뒤에도 작동한다.

![정책이 막는 지점](/img/posts/csp-rollout-steps.svg)

## 어떤 순서로 켜나

| 단계 | 내용 |
| --- | --- |
| 보고 전용 | 위반만 수집 |
| 위반 분석 | 무엇이 걸리나 |
| 정책 조정 | 필요한 출처 |
| 차단 전환 | 적용 |
| 조임 | 점차 좁게 |

보고 전용 모드로 며칠 돌리면 무엇이 걸릴지 전부 보인다. 이 단계를 건너뛰면 반드시 화면이 깨진다.

## 인라인 코드를 어떻게 다루나

| 방법 | 내용 |
| --- | --- |
| 임의값 부여 | 요청마다 다른 값 |
| 해시 지정 | 고정 스크립트 |
| 외부 파일로 | 인라인 제거 |
| 전부 허용 | 효과 없음 |

전부 허용하면 정책이 있으나 마나다. 요청마다 다른 값을 부여하는 방식이 가장 견고하다.

![인라인 처리 방식](/img/posts/csp-rollout-steps-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 현재 헤더 확인
curl -sI https://example.com | grep -i 'content-security-policy'

# 위반 보고가 들어오는지 — 수집 지점 로그
tail -20 /var/log/csp-report.log 2>/dev/null | head
\`\`\`

정책에 전부 허용하는 표현이 들어 있으면 그 지시어는 사실상 꺼진 것이다. 그런 항목부터 하나씩 좁힌다.

## 보고 수집을 준비한다

위반 보고를 받을 곳이 없으면 무엇이 막히는지 알 수 없다. 간단한 수집 지점을 먼저 만들고, 들어오는 양을 제한한다. 외부에서 임의로 보고를 보낼 수 있으므로 그대로 저장하면 저장소가 금세 찬다. 출처와 지시어 조합으로 집계하면 양이 크게 줄고 읽기도 쉬워진다.

## 참고

- OWASP — Content Security Policy Cheat Sheet
- MDN — Content-Security-Policy
- W3C — CSP Level 3`,
    diagram: {
      type: 'layers',
      caption: '정책이 막는 지점',
      layers: [
        { label: '입력 검증', note: '첫 층' },
        { label: '출력 처리', note: '둘째 층' },
        { label: '콘텐츠 정책', note: '실행 차단' },
        { label: '전송 대상 제한', note: '반출 차단' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '인라인 처리 방식',
      unit: '점',
      items: [
        { label: '요청별 임의값', value: 95, note: '가장 견고' },
        { label: '해시 지정', value: 80, note: '고정 코드' },
        { label: '외부 파일화', value: 85, note: '구조 개선' },
        { label: '전부 허용', value: 5, note: '효과 없음' },
      ],
    },
  },
  {
    slug: 'cookie-attribute-scope',
    title: '쿠키 속성이 정하는 노출 범위',
    body: `세션 쿠키 하나에 붙는 속성 몇 개가 그 쿠키가 언제 어디로 전송되는지를 정한다. 기본값으로 두면 필요 이상으로 넓게 전송된다.

## 세션 쿠키에 어떤 속성이 있나

| 속성 | 효과 |
| --- | --- |
| 스크립트 접근 차단 | 코드로 못 읽음 |
| 암호화 연결만 | 평문 전송 방지 |
| 교차 사이트 전송 | 요청 위조 완화 |
| 경로 제한 | 특정 경로만 |
| 도메인 범위 | 하위 포함 여부 |

스크립트 접근을 막는 속성이 가장 기본이다. 삽입된 스크립트가 세션을 읽어 가는 경로가 닫힌다.

![속성이 정하는 범위](/img/posts/cookie-attribute-scope.svg)

## 교차 사이트 전송 값

| 값 | 동작 |
| --- | --- |
| 엄격 | 외부에서 온 요청에 미전송 |
| 완화 | 안전한 이동에만 |
| 없음 | 항상 전송 |
| 미지정 | 브라우저 기본 |

엄격으로 두면 외부 링크로 들어온 사용자가 로그아웃 상태로 보인다. 그래서 완화를 기본으로 하고 민감한 동작에 별도 확인을 둔다.

## 도메인 범위를 어디까지 두나

| 설정 | 결과 |
| --- | --- |
| 정확한 호스트 | 그 호스트만 |
| 상위 도메인 | 모든 하위 포함 |

상위 도메인으로 설정하면 하위 도메인 하나가 뚫릴 때 쿠키가 함께 넘어간다. 공유가 꼭 필요한 경우가 아니면 넓히지 않는다.

![도메인 범위의 차이](/img/posts/cookie-attribute-scope-2.svg)

## 지금 설정을 본다

\`\`\`bash
# 응답에서 설정되는 쿠키 속성 확인
curl -sI https://example.com/login | grep -i 'set-cookie'

# 속성이 빠진 쿠키 찾기
curl -sI https://example.com/login | grep -i 'set-cookie' \
  | grep -vi 'httponly' | grep -vi 'secure'
\`\`\`

두 속성이 빠진 쿠키가 나오면 우선 처리 대상이다. 서버 설정이나 프레임워크 기본값에서 한 줄로 바뀌는 경우가 많다.

## 이름에도 규칙이 있다

특정 접두사를 붙이면 브라우저가 속성 조건을 강제한다. 암호화 연결에서만, 경로 제한과 함께 설정된 경우에만 받아들인다. 이름을 바꾸는 것만으로 설정 실수를 브라우저가 막아 주는 셈이라 비용 대비 효과가 좋다. 다만 이름을 바꾸면 기존에 발급된 값이 인식되지 않아 전원이 다시 로그인하게 된다. 한동안 두 이름을 함께 읽다가 새 이름만 발급하는 방식으로 넘기면 갑작스러운 로그아웃을 피할 수 있다.

## 참고

- OWASP — 세션 관리 지침
- MDN — Set-Cookie
- RFC 6265bis — 쿠키 사양`,
    diagram: {
      type: 'layers',
      caption: '속성이 정하는 범위',
      layers: [
        { label: '스크립트 차단', note: '코드로 못 읽음' },
        { label: '암호화 연결만', note: '평문 방지' },
        { label: '교차 전송 제한', note: '요청 위조' },
        { label: '경로·도메인', note: '범위' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '도메인 범위의 차이',
      x: ['정확한 호스트', '상위 도메인'],
      y: ['하위 침해', '정상'],
      cells: ['영향 없음', '쿠키 전달', '해당 없음', '해당 없음'],
    },
  },
  {
    slug: 'cors-misconfiguration',
    title: '교차 출처 설정에서 흔한 실수',
    body: `다른 출처에서 우리 API를 부르게 하려면 허용 설정이 필요하다. 그 설정을 넓게 열면 남의 사이트가 로그인한 사용자의 데이터를 읽어 간다.

## 어떤 실수가 있나

| 실수 | 결과 |
| --- | --- |
| 요청 출처 그대로 반사 | 아무나 허용 |
| 자격 증명 포함 허용 | 세션까지 |
| 모든 출처 허용 | 공개와 같음 |
| 부분 문자열 검사 | 우회 가능 |
| 널 출처 허용 | 샌드박스에서 접근 |

요청에 담긴 출처를 그대로 응답에 넣는 구현이 가장 흔하다. 검증 없이 반사하면 모든 출처를 허용한 것과 같다.

![잘못된 허용의 결과](/img/posts/cors-misconfiguration.svg)

## 부분 문자열 검사의 함정

| 검사 | 우회 |
| --- | --- |
| 끝부분 포함 | 공격자 도메인 뒤에 붙임 |
| 앞부분 포함 | 하위 도메인 사칭 |
| 포함 여부 | 중간에 삽입 |
| 정확히 일치 | 안전 |

허용 목록과 정확히 일치하는지 비교해야 한다. 포함 여부로 판단하면 비슷한 이름을 만들어 통과한다.

## 자격 증명이 함께 갈 때

| 조합 | 위험 |
| --- | --- |
| 모든 출처 + 자격 증명 | 브라우저가 거부 |
| 반사 + 자격 증명 | 실질적으로 전면 개방 |
| 목록 + 자격 증명 | 정상 |

브라우저가 일부 조합을 막아 주지만 반사 방식은 막지 못한다. 서버에서 제대로 검증해야 한다.

![검사 방식의 차이](/img/posts/cors-misconfiguration-2.svg)

## 지금 동작을 본다

\`\`\`bash
# 임의 출처를 보냈을 때 그대로 반사되는지
curl -sI https://api.example.com/me -H 'Origin: https://evil.example' \
  | grep -i 'access-control-allow'

# 자격 증명 허용 여부
curl -sI https://api.example.com/me -H 'Origin: https://evil.example' \
  | grep -i 'allow-credentials'
\`\`\`

보낸 출처가 그대로 돌아오고 자격 증명 허용이 함께 있으면 심각한 상태다. 허용 목록 방식으로 즉시 바꾼다.

## 목록을 어디에 두나

허용 목록을 코드에 흩어 두면 어디가 열려 있는지 파악이 안 된다. 한곳에 모아 설정으로 관리하고, 추가할 때 리뷰를 거치게 한다. 목록이 길어지면 그 자체가 재검토 신호다. 개발 환경에서만 넓게 열어 두는 구성도 위험하다. 설정이 뒤섞여 운영에 그대로 나가는 사고가 반복된다. 환경별 설정을 분리하고, 운영 설정에 넓은 허용이 들어가면 배포가 실패하도록 검사를 붙이는 편이 확실하다.

## 참고

- OWASP — CORS 보안
- MDN — 교차 출처 자원 공유
- PortSwigger — CORS 취약점`,
    diagram: {
      type: 'flow',
      caption: '잘못된 허용의 결과',
      steps: [
        { label: '사용자 로그인', note: '세션 보유' },
        { label: '악성 사이트 방문', note: '스크립트 실행' },
        { label: '우리 API 호출', note: '출처 반사' },
        { label: '데이터 읽힘', note: '세션 포함', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검사 방식의 안전도',
      unit: '점',
      items: [
        { label: '정확히 일치', value: 95, note: '안전' },
        { label: '정규식 검사', value: 60, note: '실수 여지' },
        { label: '부분 문자열', value: 20, note: '우회 가능' },
        { label: '그대로 반사', value: 0, note: '전면 개방' },
      ],
    },
  },
  {
    slug: 'clickjacking-frame-guard',
    title: '화면을 덧씌우는 클릭재킹 막기',
    body: `우리 화면을 투명한 틀에 넣고 그 위에 다른 화면을 겹치면, 사용자는 다른 것을 누른다고 생각하며 우리 화면의 버튼을 누른다.

## 어떤 동작이 위험한가

| 동작 | 결과 |
| --- | --- |
| 권한 부여 | 연동 승인 |
| 결제 확인 | 금전 피해 |
| 설정 변경 | 보안 약화 |
| 삭제 | 데이터 손실 |
| 좋아요·구독 | 평판 조작 |

한 번의 클릭으로 끝나는 동작이 표적이 된다. 여러 단계를 거치는 동작은 상대적으로 어렵다.

![덧씌우기 공격의 구조](/img/posts/clickjacking-frame-guard.svg)

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 조상 제한 헤더 | 틀에 못 넣게 |
| 옛 헤더 병행 | 구형 브라우저 |
| 확인 단계 | 한 번 더 |
| 재인증 | 중요한 동작 |

조상 제한을 지정하는 헤더 한 줄이면 대부분 막힌다. 넣어야 할 화면이 있으면 그 출처만 허용한다.

## 어디에 적용하나

| 대상 | 설정 |
| --- | --- |
| 전체 기본 | 금지 |
| 삽입 허용 화면 | 특정 출처 |
| 위젯 제공 | 명시적 허용 |
| 관리 화면 | 반드시 금지 |

기본을 금지로 두고 필요한 화면만 여는 것이 안전하다. 반대로 하면 새 화면이 생길 때마다 빠진다.

![적용 범위](/img/posts/clickjacking-frame-guard-2.svg)

## 지금 헤더를 본다

\`\`\`bash
# 주요 화면의 삽입 방지 헤더
for p in / /settings /admin /payment; do
  printf '%s ' "$p"
  curl -sI "https://example.com$p" \
    | grep -ciE 'frame-ancestors|x-frame-options' || echo 0
done
\`\`\`

0이 나오는 화면은 틀에 넣을 수 있다. 관리 화면이나 설정 화면이 여기 나오면 우선 처리한다.

## 중요한 동작에는 확인을 둔다

헤더로 막는 것이 기본이지만, 정말 되돌릴 수 없는 동작에는 확인 단계를 둔다. 비밀번호를 다시 묻거나 입력을 요구하면 겹쳐 놓은 화면으로는 통과할 수 없다. 사용자의 수고와 위험의 크기를 견줘 어디에 둘지 정한다. 연동 승인 화면이 대표적인 대상이다. 한 번의 클릭으로 외부 앱에 권한이 넘어가고, 그 뒤로는 사용자가 인지하지 못한 채 접근이 계속된다. 이런 화면은 삽입 금지와 확인 단계를 모두 두는 편이 맞다.

## 참고

- OWASP — Clickjacking Defense Cheat Sheet
- MDN — X-Frame-Options
- W3C — CSP frame-ancestors`,
    diagram: {
      type: 'flow',
      caption: '덧씌우기 공격의 구조',
      steps: [
        { label: '악성 페이지', note: '유인' },
        { label: '우리 화면 삽입', note: '투명 틀' },
        { label: '겹친 버튼', note: '다른 것으로 보임' },
        { label: '의도치 않은 클릭', note: '실행됨', danger: true },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '적용 범위',
      layers: [
        { label: '기본 금지', note: '전체' },
        { label: '관리 화면', note: '반드시' },
        { label: '결제·설정', note: '반드시' },
        { label: '위젯', note: '출처 지정' },
      ],
    },
  },
  {
    slug: 'third-party-script-risk',
    title: '외부 스크립트가 실제로 가진 권한',
    body: `우리 화면에 넣은 외부 스크립트는 우리 코드와 같은 권한으로 돈다. 화면의 모든 입력과 쿠키에 접근할 수 있고, 그쪽이 바뀌면 우리 화면이 바뀐다.

## 무엇을 할 수 있나

| 동작 | 가능 여부 |
| --- | --- |
| 입력 값 읽기 | 가능 |
| 쿠키 접근 | 속성에 따라 |
| 화면 변경 | 가능 |
| 요청 전송 | 가능 |
| 다른 스크립트 로드 | 가능 |

다른 스크립트를 불러올 수 있다는 점이 특히 문제다. 우리가 검토한 것과 다른 코드가 나중에 들어온다.

![외부 스크립트의 권한](/img/posts/third-party-script-risk.svg)

## 어디에 넣느냐가 다르다

| 위치 | 위험 |
| --- | --- |
| 결제 화면 | 가장 높음 |
| 로그인 화면 | 자격 증명 |
| 일반 화면 | 보통 |
| 별도 틀 안 | 격리됨 |

결제나 로그인 화면에는 외부 스크립트를 넣지 않는 것이 원칙이다. 필요하면 격리된 틀 안에서 돌린다.

## 어떻게 줄이나

| 방법 | 내용 |
| --- | --- |
| 무결성 검증 | 해시 고정 |
| 자체 호스팅 | 내용 고정 |
| 정책으로 제한 | 출처 지정 |
| 격리 실행 | 별도 틀 |
| 최소화 | 꼭 필요한 것만 |

해시를 고정하면 그 파일이 바뀌었을 때 로드되지 않는다. 다만 자주 갱신되는 스크립트에는 쓰기 어렵다.

![줄이는 방법](/img/posts/third-party-script-risk-2.svg)

## 지금 무엇이 들어 있는지 본다

\`\`\`bash
# 화면에 로드되는 외부 스크립트 출처
curl -s https://example.com | grep -oE '<script[^>]+src="[^"]+"' \
  | grep -oE 'https?://[^/]+' | sort | uniq -c | sort -rn

# 무결성 속성이 붙어 있는지
curl -s https://example.com | grep -c 'integrity='
\`\`\`

목록에 기억나지 않는 도메인이 있으면 누가 왜 넣었는지 확인한다. 마케팅 도구가 다른 도구를 부르면서 목록이 늘어난 경우가 많다.

## 목록을 정기적으로 정리한다

한 번 넣은 스크립트는 잘 빠지지 않는다. 캠페인이 끝났는데 추적 스크립트가 남아 있거나, 쓰지 않는 도구가 계속 로드되는 상태가 흔하다. 반기마다 목록을 보고 쓰임을 확인하면 화면도 빨라지고 노출 면적도 줄어든다. 추가하는 절차도 정해 둔다. 누가 승인했고 어떤 목적인지 남겨 두면 나중에 제거를 판단할 수 있다. 기록이 없으면 지워도 되는지 아무도 몰라 그대로 남는다.

## 참고

- OWASP — 제3자 자바스크립트 관리
- MDN — 하위 자원 무결성
- PCI DSS — 결제 화면 스크립트 통제`,
    diagram: {
      type: 'layers',
      caption: '외부 스크립트의 권한',
      layers: [
        { label: '입력 값', note: '읽기 가능' },
        { label: '쿠키', note: '속성에 따라' },
        { label: '화면', note: '변경 가능' },
        { label: '추가 로드', note: '검토 밖 코드' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '줄이는 방법의 효과',
      unit: '점',
      items: [
        { label: '제거', value: 100, note: '가장 확실' },
        { label: '격리 실행', value: 80, note: '별도 틀' },
        { label: '해시 고정', value: 70, note: '변경 차단' },
        { label: '정책 제한', value: 50, note: '출처만' },
      ],
    },
  },
  {
    slug: 'browser-storage-choice',
    title: '브라우저 저장소를 고르는 기준',
    body: `토큰이나 상태를 브라우저 어디에 둘지는 보안에 직접 영향을 준다. 편의로 고르면 삽입된 스크립트에 그대로 읽힌다.

## 선택지의 차이

| 저장소 | 스크립트 접근 | 자동 전송 |
| --- | --- | --- |
| 스크립트 차단 쿠키 | 불가 | 자동 |
| 일반 쿠키 | 가능 | 자동 |
| 로컬 저장소 | 가능 | 수동 |
| 세션 저장소 | 가능 | 수동 |
| 메모리 | 가능 | 수동 |

스크립트가 읽을 수 없는 쿠키에 두는 것이 가장 안전하다. 삽입된 스크립트가 있어도 값을 꺼내지 못한다.

![저장소별 접근 가능성](/img/posts/browser-storage-choice.svg)

## 무엇을 어디에 두나

| 데이터 | 위치 |
| --- | --- |
| 세션 토큰 | 접근 차단 쿠키 |
| 갱신 토큰 | 접근 차단 쿠키 |
| 화면 설정 | 로컬 저장소 |
| 임시 상태 | 세션 저장소 |
| 민감 정보 | 저장하지 않음 |

민감한 값은 아예 브라우저에 두지 않는 선택이 가장 낫다. 필요할 때 서버에서 가져오면 된다.

## 남는 문제

| 항목 | 내용 |
| --- | --- |
| 요청 위조 | 자동 전송의 이면 |
| 다중 탭 | 상태 공유 |
| 로그아웃 | 확실한 제거 |
| 공용 기기 | 잔존 |

쿠키는 자동 전송되므로 요청 위조 대응이 별도로 필요하다. 교차 사이트 전송 속성과 토큰 검증을 함께 쓴다.

![데이터별 배치](/img/posts/browser-storage-choice-2.svg)

## 지금 무엇이 들어 있는지 본다

\`\`\`bash
# 코드에서 브라우저 저장소를 쓰는 지점
grep -rInE 'localStorage|sessionStorage' --include='*.ts' --include='*.tsx' src/ 2>/dev/null \
  | grep -iE 'token|auth|key|secret' | head -20
\`\`\`

토큰을 저장소에 넣는 코드가 나오면 검토 대상이다. 옮기는 작업이 크면 우선 만료를 짧게 하는 것으로 위험을 줄일 수 있다.

## 로그아웃에서 확실히 지운다

로그아웃할 때 저장소를 비우지 않으면 다음 사용자가 그 값을 본다. 공용 기기에서 특히 문제가 된다. 지우는 코드를 한곳에 모아 두고 로그아웃 경로가 여럿이어도 같은 함수를 부르게 하면 빠지지 않는다. 서버 쪽 세션도 함께 끝내야 한다. 브라우저에서만 지우면 그 토큰은 여전히 유효해서, 이미 새어 나간 값으로 접근이 계속된다. 로그아웃은 양쪽에서 동시에 일어나야 완결된다.

## 참고

- OWASP — 브라우저 저장소 보안
- MDN — Web Storage API
- OWASP — 세션 관리 지침`,
    diagram: {
      type: 'matrix',
      caption: '저장소별 접근 가능성',
      x: ['스크립트 접근 가능', '불가'],
      y: ['자동 전송', '수동 전송'],
      cells: ['일반 쿠키', '접근 차단 쿠키', '로컬·세션 저장소', '해당 없음'],
    },
    diagram2: {
      type: 'layers',
      caption: '데이터별 배치',
      layers: [
        { label: '세션 토큰', note: '접근 차단 쿠키' },
        { label: '갱신 토큰', note: '접근 차단 쿠키' },
        { label: '화면 설정', note: '로컬 저장소' },
        { label: '민감 정보', note: '저장 안 함' },
      ],
    },
  },
  {
    slug: 'dom-based-xss',
    title: '서버를 거치지 않는 스크립트 삽입',
    body: `입력이 서버로 가지 않고 브라우저 안에서만 처리되는 경우가 있다. 서버 쪽 검증을 아무리 해도 이 경로는 막히지 않는다.

## 어디서 생기나

| 입력원 | 설명 |
| --- | --- |
| 주소의 조각 부분 | 서버로 안 감 |
| 질의 문자열 | 스크립트가 읽음 |
| 메시지 수신 | 다른 창에서 |
| 브라우저 저장소 | 이전에 저장된 값 |
| 참조 주소 | 이전 화면 |

주소의 조각 부분은 서버 로그에도 남지 않는다. 그래서 공격 흔적을 찾기도 어렵다.

![삽입이 생기는 경로](/img/posts/dom-based-xss.svg)

## 어떤 스크립팅 코드가 위험한가

| 처리 | 위험 |
| --- | --- |
| 원시 HTML 삽입 | 높음 |
| 문자열 실행 | 높음 |
| 이벤트 속성 설정 | 높음 |
| 텍스트로 삽입 | 안전 |
| 속성 값 설정 | 대개 안전 |

원시 HTML로 넣는 코드가 대부분의 원인이다. 텍스트로 넣는 방식으로 바꾸면 그 자리는 닫힌다.

## 어떻게 찾나

| 방법 | 내용 |
| --- | --- |
| 위험 함수 검색 | 코드 훑기 |
| 흐름 추적 | 입력원에서 출력까지 |
| 동적 검사 | 실제 실행 |
| 정책 병행 | 실행 차단 |

입력원에서 출력까지 이어지는지 확인해야 실제 위험인지 알 수 있다. 위험 함수를 쓴다고 전부 문제는 아니다.

![안전한 처리로 바꾸기](/img/posts/dom-based-xss-2.svg)

## 코드를 훑는다

\`\`\`bash
# 원시 삽입과 문자열 실행 지점
grep -rInE 'innerHTML|outerHTML|insertAdjacentHTML|document\\.write' \
  --include='*.ts' --include='*.tsx' --include='*.js' src/ 2>/dev/null | head -20

# 주소에서 값을 읽는 지점
grep -rInE 'location\\.(hash|search)|URLSearchParams' --include='*.ts' src/ 2>/dev/null | head
\`\`\`

두 목록이 같은 파일에서 만나면 우선 확인 대상이다. 읽은 값이 그대로 삽입되는지 흐름을 따라간다.

## 프레임워크를 믿되 예외를 안다

요즘 프레임워크는 기본적으로 값을 텍스트로 처리해 안전하다. 다만 원시 HTML을 넣는 전용 방법이 따로 있고, 그 부분만 위험하다. 이름이 분명해서 검색으로 찾기 쉬우므로 그 목록을 유지하고 쓸 때마다 검토하면 대부분이 관리된다. 정리 라이브러리를 쓸 때도 설정을 확인한다. 기본 설정이 허용하는 태그와 속성이 생각보다 넓은 경우가 있고, 그 안에 실행으로 이어지는 조합이 남아 있을 수 있다. 우리가 필요한 최소 집합으로 좁혀 두는 편이 안전하다.

## 참고

- OWASP — DOM based XSS Prevention
- PortSwigger — DOM XSS
- MDN — 신뢰할 수 있는 타입`,
    diagram: {
      type: 'flow',
      caption: '삽입이 생기는 경로',
      steps: [
        { label: '주소에 값', note: '서버로 안 감' },
        { label: '스크립트가 읽음', note: '브라우저 안' },
        { label: '원시 삽입', note: 'HTML로 해석' },
        { label: '실행', note: '서버 검증 무관', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 처리로 바꾸기',
      steps: [
        { label: '입력원 파악', note: '어디서 오나' },
        { label: '흐름 추적', note: '어디로 가나' },
        { label: '텍스트 삽입', note: '해석 안 함' },
        { label: '정책 병행', note: '마지막 층' },
      ],
    },
  },
  {
    slug: 'iframe-sandbox-use',
    title: '외부 콘텐츠를 격리해 띄우기',
    body: `사용자가 올린 HTML이나 외부 위젯을 그대로 화면에 넣으면 우리 코드와 같은 권한을 준다. 격리 설정을 붙인 틀 안에서 돌리면 그 권한이 끊긴다.

## 무엇을 끊나

| 제한 | 효과 |
| --- | --- |
| 같은 출처 취급 해제 | 쿠키·저장소 차단 |
| 스크립트 금지 | 실행 차단 |
| 폼 전송 금지 | 데이터 전송 |
| 상위 이동 금지 | 화면 탈취 방지 |
| 팝업 금지 | 창 열기 |

같은 출처 취급을 해제하면 그 안의 코드가 우리 쿠키나 저장소에 닿지 못한다. 가장 중요한 제한이다.

![격리가 끊는 연결](/img/posts/iframe-sandbox-use.svg)

## 필요한 것만 다시 연다

| 허용 | 용도 |
| --- | --- |
| 스크립트 실행 | 동작하는 위젯 |
| 폼 전송 | 입력 처리 |
| 팝업 | 인증 창 |
| 상위 이동 | 사용자 조작 시만 |

전부 막으면 동작하지 않는 콘텐츠가 많다. 필요한 것만 하나씩 허용하되, 같은 출처 취급과 스크립트를 함께 허용하면 격리가 사실상 풀린다.

## 별도 도메인에 둔다

| 구성 | 효과 |
| --- | --- |
| 같은 도메인 | 쿠키 범위 공유 |
| 별도 도메인 | 완전 분리 |
| 하위 도메인 | 부분 분리 |

사용자 콘텐츠를 아예 다른 도메인에서 서비스하면 브라우저의 출처 구분이 경계가 된다. 격리 설정보다 확실하다.

![격리 구성의 강도](/img/posts/iframe-sandbox-use-2.svg)

## 설정을 확인한다

\`\`\`bash
# 화면의 틀 요소와 격리 속성
curl -s https://example.com/preview | grep -oE '<iframe[^>]*>' | head -10

# 격리 속성 없이 삽입되는 곳
curl -s https://example.com/preview | grep -oE '<iframe[^>]*>' | grep -vc 'sandbox'
\`\`\`

격리 속성이 없는 틀이 있으면 그 안의 콘텐츠가 어디서 오는지 확인한다. 우리가 만든 내용만 들어간다면 문제가 없지만, 사용자 입력이 섞이면 처리가 필요하다.

## 내용 자체도 정리한다

격리는 실행 권한을 줄이는 장치이고, 내용에 남은 위험한 요소를 지우는 일은 별개다. 사용자 HTML을 저장하기 전에 허용 목록 기반으로 정리하고, 표시할 때 격리 틀을 쓰는 두 겹 구성이 안전하다. 한쪽만으로는 부족한 경우가 생긴다.

## 참고

- MDN — iframe sandbox
- OWASP — HTML 정화 지침
- W3C — HTML 명세 sandbox 속성`,
    diagram: {
      type: 'layers',
      caption: '격리가 끊는 연결',
      layers: [
        { label: '출처 취급 해제', note: '쿠키·저장소' },
        { label: '스크립트 금지', note: '실행' },
        { label: '폼 전송 금지', note: '데이터' },
        { label: '상위 이동 금지', note: '화면 탈취' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '격리 구성의 강도',
      unit: '점',
      items: [
        { label: '별도 도메인 + 격리', value: 95, note: '가장 강함' },
        { label: '격리만', value: 70, note: '설정 의존' },
        { label: '하위 도메인', value: 50, note: '부분 분리' },
        { label: '같은 도메인 직접', value: 5, note: '격리 없음' },
      ],
    },
  },
  {
    slug: 'open-redirect-abuse',
    title: '주소를 넘겨받아 이동시키는 기능',
    body: `로그인 후 원래 화면으로 돌려보내는 기능은 대개 이동할 주소를 값으로 받는다. 그 값을 검증하지 않으면 우리 도메인을 거쳐 아무 데나 보낼 수 있다.

## 왜 문제가 되나

| 용도 | 악용 |
| --- | --- |
| 피싱 링크 | 우리 도메인으로 시작 |
| 필터 우회 | 신뢰 도메인 통과 |
| 토큰 탈취 | 인가 흐름에서 |
| 통계 오염 | 유입 조작 |

링크가 우리 도메인으로 시작하므로 사용자도 필터도 안전하다고 판단한다. 도메인의 신뢰를 빌려 쓰는 셈이다.

![주소 전달의 악용](/img/posts/open-redirect-abuse.svg)

## 어떻게 검증하나

| 방법 | 안전도 |
| --- | --- |
| 허용 목록 | 높음 |
| 상대 경로만 | 높음 |
| 호스트 비교 | 구현 주의 |
| 앞부분 검사 | 우회 가능 |
| 검증 없음 | 없음 |

상대 경로만 허용하는 방식이 가장 단순하고 안전하다. 외부로 보낼 일이 없다면 이것으로 끝난다.

## 우회되는 표현

| 입력 | 결과 |
| --- | --- |
| 슬래시 두 개로 시작 | 외부 주소 |
| 역슬래시 혼용 | 브라우저 해석 |
| 인코딩 | 검사 통과 |
| 사용자 정보 포함 | 다른 호스트 |

문자열 검사로 막으려 하면 이런 표현에 걸린다. 주소 분석 함수로 파싱한 뒤 호스트를 비교하는 것이 확실하다.

![검증 방식의 안전도](/img/posts/open-redirect-abuse-2.svg)

## 지금 동작을 본다

\`\`\`bash
# 외부 주소로 이동하는지 확인
curl -sI "https://example.com/login?next=https://evil.example" \
  | grep -i '^location:'

# 우회 표현도 함께
curl -sI "https://example.com/login?next=//evil.example" | grep -i '^location:'
\`\`\`

외부 주소가 응답에 나오면 열려 있는 상태다. 두 번째 표현이 통과하는 경우가 특히 많다.

## 인가 흐름에서는 더 위험하다

외부 로그인 연동에서 돌아올 주소가 검증되지 않으면 인가 코드나 토큰이 공격자에게 전달된다. 이 경우 단순한 피싱을 넘어 계정 탈취로 이어진다. 연동 설정에서 돌아올 주소를 정확히 일치하는 값으로 등록하고, 우리 쪽에서도 다시 확인한다.

## 참고

- OWASP — Unvalidated Redirects and Forwards
- OAuth 2.0 Security Best Current Practice
- PortSwigger — Open redirection`,
    diagram: {
      type: 'flow',
      caption: '주소 전달의 악용',
      steps: [
        { label: '우리 도메인 링크', note: '신뢰 획득' },
        { label: '이동 값에 외부 주소', note: '검증 없음' },
        { label: '외부로 이동', note: '사용자 인지 못함' },
        { label: '피싱·토큰 탈취', note: '결과', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '검증 방식의 안전도',
      unit: '점',
      items: [
        { label: '상대 경로만', value: 95, note: '단순·확실' },
        { label: '허용 목록', value: 90, note: '외부 필요 시' },
        { label: '호스트 비교', value: 70, note: '파싱 필요' },
        { label: '앞부분 검사', value: 15, note: '우회 가능' },
      ],
    },
  },
  {
    slug: 'file-upload-rendering',
    title: '올린 파일을 보여 줄 때의 위험',
    body: `사용자가 올린 파일을 그대로 서비스하면, 그 파일이 우리 도메인에서 실행되는 코드가 될 수 있다. 저장하는 것보다 보여 주는 쪽이 더 위험하다.

## 어떤 파일이 문제인가

| 형식 | 위험 |
| --- | --- |
| HTML | 스크립트 실행 |
| SVG | 스크립트 포함 가능 |
| 문서 | 내장 코드 |
| 이미지 위장 | 확장자 조작 |
| 압축 파일 | 경로 조작 |

SVG가 이미지로 분류되어 검사를 통과하는 경우가 많다. 그 안에 스크립트를 넣을 수 있다.

![올린 파일이 실행되는 경로](/img/posts/file-upload-rendering.svg)

## 어떻게 서비스하나

| 방법 | 효과 |
| --- | --- |
| 별도 도메인 | 출처 분리 |
| 내려받기 강제 | 실행 안 됨 |
| 형식 고정 응답 | 해석 제한 |
| 이미지 재생성 | 내용 제거 |
| 스니핑 차단 | 추측 방지 |

별도 도메인에서 서비스하는 것이 가장 확실하다. 실행되더라도 우리 쿠키와 저장소에 닿지 못한다.

## 검사는 무엇을 보나

| 검사 | 한계 |
| --- | --- |
| 확장자 | 쉽게 조작 |
| 선언된 형식 | 클라이언트 값 |
| 내용 서명 | 위장 가능 |
| 재생성 | 가장 확실 |

이미지를 다시 만들어 저장하면 원본에 있던 것이 사라진다. 품질 손실이 있지만 위험한 내용은 남지 않는다.

![처리 방식의 강도](/img/posts/file-upload-rendering-2.svg)

## 지금 응답을 본다

\`\`\`bash
# 올린 파일의 응답 헤더 — 형식과 처리 방식
curl -sI https://example.com/uploads/test.svg \
  | grep -iE 'content-type|content-disposition|x-content-type-options'

# 같은 도메인에서 서비스되는지
curl -s https://example.com/files | grep -oE 'src="[^"]+"' | head
\`\`\`

형식이 SVG 그대로이고 내려받기 지정이 없으면 브라우저가 해석해 실행한다. 별도 도메인이 아니라면 우선 처리 대상이다.

## 이름과 경로도 정리한다

원래 파일 이름을 그대로 쓰면 경로 조작이나 덮어쓰기 문제가 생긴다. 서버에서 새 이름을 부여하고 원래 이름은 표시용으로만 저장하는 방식이 단순하다. 이렇게 하면 같은 이름의 파일이 서로를 덮어쓰는 일도 함께 해결된다. 크기와 개수 제한도 함께 둔다. 제한이 없으면 저장 공간이 차거나 처리 비용이 급증하는 방식으로 서비스가 멈출 수 있다. 업로드는 인증된 사용자만 하게 하고 속도 제한을 거는 것이 기본이다.

## 참고

- OWASP — File Upload Cheat Sheet
- MDN — X-Content-Type-Options
- CWE-434 — 위험한 형식의 파일 업로드`,
    diagram: {
      type: 'flow',
      caption: '올린 파일이 실행되는 경로',
      steps: [
        { label: '파일 업로드', note: '검사 통과' },
        { label: '같은 도메인 서비스', note: '출처 공유' },
        { label: '브라우저 해석', note: '형식에 따라' },
        { label: '스크립트 실행', note: '우리 권한', danger: true },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '처리 방식의 강도',
      unit: '점',
      items: [
        { label: '별도 도메인', value: 95, note: '출처 분리' },
        { label: '이미지 재생성', value: 85, note: '내용 제거' },
        { label: '내려받기 강제', value: 70, note: '실행 안 함' },
        { label: '확장자 검사만', value: 15, note: '조작 쉬움' },
      ],
    },
  },
]
