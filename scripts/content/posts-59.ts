import type { SeedPost } from './types'

/** 580~589 — 암호와 키를 다루는 실무 */
export const posts59: SeedPost[] = [
  {
    slug: 'certificate-expiry-watch',
    title: '인증서 만료로 서비스가 멈추지 않게',
    body: `인증서 만료는 예고된 장애다. 날짜가 정해져 있고 누구나 볼 수 있는데도 가장 자주 반복되는 사고 중 하나다. 원인은 기술이 아니라 누가 언제 보는지가 정해져 있지 않다는 데 있다.

## 어디에 있는지부터 모른다

| 위치 | 잊히는 이유 |
| --- | --- |
| 로드밸런서 | 인프라 담당만 앎 |
| 내부 서비스 간 | 자동화에 묻힘 |
| 외부 연동 상대 | 우리 것이 아님 |
| 코드에 박힌 고정 값 | 배포해야 바뀜 |
| 옛 도메인 | 쓰는지도 모름 |

외부 연동 상대의 인증서가 만료되면 우리 쪽 호출이 실패한다. 우리 것이 아니라서 목록에 없고, 그래서 아무도 안 본다.

![인증서가 흩어져 있는 곳](/img/posts/certificate-expiry-watch.svg)

## 목록을 어떻게 만드나

| 방법 | 범위 |
| --- | --- |
| 발급 기록 | 우리가 발급한 것 |
| 외부 스캔 | 열려 있는 것 |
| 투명성 로그 조회 | 우리 도메인 전부 |
| 연동 문서 | 상대 것 |

투명성 로그를 조회하면 우리 도메인으로 발급된 인증서가 전부 나온다. 우리가 발급한 기억이 없는 것이 나오면 그것 자체가 확인할 이유가 된다.

## 언제 알릴 것인가

| 시점 | 용도 |
| --- | --- |
| 60일 전 | 계획 |
| 30일 전 | 실행 |
| 7일 전 | 경고 |
| 1일 전 | 비상 |

한 번만 알리면 그날 휴가인 사람이 있다. 여러 번 알리되 마지막 단계는 다른 경로로 보낸다.

![알림 단계](/img/posts/certificate-expiry-watch-2.svg)

## 직접 훑어 본다

\`\`\`bash
# 도메인 목록의 만료일을 한 번에 본다
for h in example.com api.example.com internal.example.com; do
  d=$(echo | openssl s_client -servername "$h" -connect "$h":443 2>/dev/null \
    | openssl x509 -noout -enddate 2>/dev/null | cut -d= -f2)
  printf '%-28s %s\n' "$h" "\${d:-확인 실패}"
done
\`\`\`

확인 실패로 나오는 항목이 더 중요하다. 이미 만료됐거나, 접속이 안 되거나, 우리가 모르는 상태다.

## 자동 갱신이 답이지만

갱신을 자동화하면 이 문제 대부분이 사라진다. 다만 자동화 자체가 조용히 멈출 수 있어, 갱신이 돌고 있는지 확인하는 장치가 여전히 필요하다. 만료일 감시는 자동화가 있어도 끄지 않는다 — 감시가 자동화의 고장을 알아차리는 유일한 수단이다.

## 참고

- NIST SP 800-52 — TLS 인증서 관리
- CIS Controls — 자산 목록과 만료 관리
- RFC 6962 — 인증서 투명성`,
    diagram: {
      type: 'layers',
      caption: '인증서가 흩어져 있는 곳',
      layers: [
        { label: '외부 진입점', note: '눈에 띔' },
        { label: '내부 서비스 간', note: '자동화에 묻힘' },
        { label: '코드에 박힌 값', note: '배포해야 바뀜' },
        { label: '연동 상대', note: '우리 것이 아님' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '알림 단계',
      steps: [
        { label: '60일 전', note: '계획' },
        { label: '30일 전', note: '실행' },
        { label: '7일 전', note: '경고' },
        { label: '1일 전', note: '다른 경로로' },
      ],
    },
  },
  {
    slug: 'private-ca-operation',
    title: '사설 인증 기관을 운영할 때의 무게',
    body: `내부 통신에 인증서를 쓰려면 발급해 줄 기관이 필요하다. 직접 세우면 비용 없이 원하는 만큼 발급할 수 있지만, 그 기관의 키가 곧 내부망 전체를 위조할 수 있는 열쇠가 된다.

## 무엇을 쥐게 되나

| 대상 | 가능한 일 |
| --- | --- |
| 루트 키 | 어떤 내부 도메인도 위조 |
| 중간 키 | 그 범위 안에서 위조 |
| 발급 시스템 | 임의 인증서 발급 |
| 폐기 목록 | 폐기를 막을 수 있음 |

루트 키가 유출되면 내부 서비스 어느 것으로도 위장할 수 있다. 신뢰의 출발점이라 그 위를 확인해 줄 것이 없다.

![신뢰가 쌓이는 구조](/img/posts/private-ca-operation.svg)

## 루트를 어떻게 두나

| 방식 | 안전성 | 운영 |
| --- | --- | --- |
| 온라인 루트 | 낮음 | 쉬움 |
| 오프라인 루트 + 중간 | 높음 | 중간 |
| 하드웨어 보관 | 매우 높음 | 준비 필요 |

루트를 꺼 두고 중간 인증서로 발급하는 구성이 표준적이다. 루트는 중간을 만들 때만 잠깐 쓰고 다시 꺼 둔다.

## 키 관리에서 정할 것

| 항목 | 내용 |
| --- | --- |
| 생성 장소 | 키가 밖으로 나오지 않는 곳 |
| 접근 승인 | 두 사람 이상 |
| 사용 기록 | 발급 전부 |
| 백업 | 분산 보관 |
| 폐기 절차 | 미리 연습 |

두 사람 이상의 승인은 형식이 아니다. 한 사람이 혼자 발급할 수 있으면 그 한 사람이 곧 위험이 된다.

![루트 키를 두는 방식](/img/posts/private-ca-operation-2.svg)

## 발급 기록을 확인한다

\`\`\`bash
# 발급 목록 — 우리가 모르는 이름이 있는지
ls -1 /etc/pki/ca/newcerts/*.pem 2>/dev/null | while read -r f; do
  openssl x509 -in "$f" -noout -subject -enddate 2>/dev/null
done | head -20

# 중간 인증서의 남은 수명 — 이것이 만료되면 전부 멈춘다
openssl x509 -in intermediate.pem -noout -enddate
\`\`\`

중간 인증서의 만료를 놓치면 그 아래 모든 인증서가 한꺼번에 무효가 된다. 개별 서버 인증서보다 먼저 챙길 항목이다.

## 세우지 않는 선택지

관리형 서비스가 같은 일을 해 준다. 키를 우리가 만지지 않으므로 유출 위험이 줄고, 발급·갱신·폐기가 이미 자동화돼 있다. 직접 세우는 것은 그 서비스를 쓸 수 없는 사정이 있을 때다. "우리가 통제한다"는 느낌이 실제 안전을 뜻하지는 않는다.

## 참고

- NIST SP 800-57 — 키 관리 권고
- RFC 5280 — 인증서 경로 검증
- CA/Browser Forum — 기관 운영 요건`,
    diagram: {
      type: 'layers',
      caption: '신뢰가 쌓이는 구조',
      layers: [
        { label: '루트 키', note: '전부를 위조 가능' },
        { label: '중간 키', note: '범위 안에서' },
        { label: '서버 인증서', note: '개별 서비스' },
        { label: '검증하는 쪽', note: '루트를 믿음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '루트 키를 두는 방식',
      x: ['오프라인', '온라인'],
      y: ['하드웨어 보관', '파일'],
      cells: ['가장 안전', '위험', '보통', '가장 위험'],
    },
  },
  {
    slug: 'hsm-key-custody',
    title: '키를 꺼낼 수 없게 보관한다는 것',
    body: `암호화 키를 파일로 두면 그 파일을 읽을 수 있는 누구나 키를 가진 것이 된다. 키가 밖으로 나오지 않는 장치에 두면, 키를 쓰는 것과 키를 갖는 것이 분리된다.

## 무엇이 달라지나

| 보관 | 유출 시 |
| --- | --- |
| 파일 | 키 자체가 나감 |
| 암호화된 파일 | 암호를 함께 얻으면 나감 |
| 전용 장치 | 나가지 않음, 쓸 수만 있음 |

전용 장치에서는 침입자가 "지금 그 서버에서" 키를 쓸 수는 있어도 키를 들고 나갈 수는 없다. 침입을 막는 것이 아니라 침입 이후의 범위를 줄이는 장치다.

![키가 나가는 경로](/img/posts/hsm-key-custody.svg)

## 무엇을 넣을 것인가

| 키 | 넣을 가치 |
| --- | --- |
| 인증 기관 루트 | 매우 높음 |
| 서명 키 | 높음 |
| 데이터 암호화 마스터 키 | 높음 |
| 세션 키 | 낮음, 성능 부담 |

모든 키를 넣을 필요는 없다. 마스터 키만 장치에 두고 실제 데이터는 그 아래 키로 암호화하는 구성이 흔하다.

## 운영에서 부딪히는 것

| 항목 | 내용 |
| --- | --- |
| 성능 | 호출마다 왕복 |
| 가용성 | 장치가 멈추면 전부 멈춤 |
| 백업 | 키를 복제할 수 있는가 |
| 비용 | 장치·서비스 요금 |

가용성이 가장 자주 문제가 된다. 키를 꺼낼 수 없다는 성질 때문에, 장치를 잃으면 데이터도 함께 잃는다.

![넣을 키와 두는 키](/img/posts/hsm-key-custody-2.svg)

## 접근 기록을 본다

\`\`\`bash
# 클라우드 키 관리 서비스의 사용 기록
aws cloudtrail lookup-events \
  --lookup-attributes AttributeKey=EventName,AttributeValue=Decrypt \
  --max-results 20 --query 'Events[].[EventTime,Username]' --output text

# 어떤 키가 얼마나 쓰이는지 — 쓰지 않는 키는 정리 대상이다
aws kms list-keys --query 'Keys[].KeyId' --output text | tr '\t' '\n' | head -10
\`\`\`

복호화 호출이 평소보다 급증하면 대량 반출의 신호일 수 있다. 키를 꺼낼 수 없어도 대량으로 복호화하면 데이터는 나간다.

## 꺼낼 수 없다는 것의 다른 면

키를 꺼낼 수 없으면 백업도 만들 수 없다. 장치가 고장 나거나 서비스 계정이 잠기면 데이터를 영영 읽지 못한다. 복제를 허용하는 구성이나 다중 지역 키를 쓰는 선택지를 처음에 정해야 하고, 이 결정은 나중에 바꾸기 어렵다.

## 참고

- NIST SP 800-57 — 키 관리 수명 주기
- FIPS 140-3 — 암호 모듈 요건
- 클라우드 사업자 키 관리 서비스 문서`,
    diagram: {
      type: 'flow',
      caption: '키가 나가는 경로',
      steps: [
        { label: '키를 파일로 보관', note: '서버 안' },
        { label: '서버 침해', note: '파일 읽기' },
        { label: '키 반출', note: '어디서나 사용', danger: true },
        { label: '발견해도 무력', note: '이미 나감' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '넣을 키와 두는 키',
      x: ['사용 빈도 낮음', '높음'],
      y: ['영향 큼', '작음'],
      cells: ['장치에 보관', '마스터만 보관', '파일로 충분', '파일로 충분'],
    },
  },
  {
    slug: 'encryption-in-transit-internal',
    title: '내부 통신도 암호화해야 하는 이유',
    body: `내부망은 안전하다는 전제로 서비스 사이 통신을 평문으로 두는 경우가 많다. 그 전제는 네트워크에 발을 들인 공격자, 잘못 연결된 장비, 클라우드의 공유 구간 앞에서 성립하지 않는다.

## 평문일 때 보이는 것

| 통신 | 흘러가는 내용 |
| --- | --- |
| 서비스 간 API | 토큰·개인정보 |
| 데이터베이스 연결 | 질의와 결과 전부 |
| 캐시 | 세션 값 |
| 메시지 큐 | 이벤트 본문 |
| 로그 전송 | 수집되는 모든 것 |

로그 전송이 특히 크다. 모든 서비스의 기록이 한 통로로 흐르므로 그 구간 하나가 전체를 들여다보는 창이 된다.

![평문 구간이 보여 주는 것](/img/posts/encryption-in-transit-internal.svg)

## 어디부터 덮나

| 우선순위 | 구간 |
| --- | --- |
| 1 | 가용 영역·지역을 넘는 구간 |
| 2 | 자격 증명이 흐르는 구간 |
| 3 | 개인정보가 흐르는 구간 |
| 4 | 같은 호스트 안 |

같은 호스트 안의 통신은 우선순위가 낮다. 그 지점까지 공격자가 왔다면 이미 다른 문제가 더 크다.

## 부담을 줄이는 방법

| 방법 | 부담 |
| --- | --- |
| 애플리케이션마다 설정 | 큼 |
| 사이드카가 대신 처리 | 중간 |
| 네트워크 계층 암호화 | 작음, 범위 넓음 |

애플리케이션마다 설정하면 새 서비스가 생길 때마다 빠뜨릴 기회가 생긴다. 한 층 아래에서 일괄로 처리하면 개별 서비스가 신경 쓸 일이 없어진다.

![어디서 처리할 것인가](/img/posts/encryption-in-transit-internal-2.svg)

## 지금 평문인 곳을 찾는다

\`\`\`bash
# 열린 포트 중 암호화되지 않은 것
ss -tlnp | awk 'NR>1 {print $4}' | grep -oE '[0-9]+$' | sort -un | while read -r p; do
  case "$p" in 443|8443|6443) ;; *) echo "평문 가능 포트 $p" ;; esac
done | head -20

# 데이터베이스 연결 문자열에 암호화 요구가 있는지
grep -rniE 'sslmode|require_secure_transport|tls=true' --include='*.{yml,yaml,env,conf}' . | head
\`\`\`

연결 문자열에 암호화 요구가 없으면 서버가 지원해도 평문으로 붙는다. 서버 설정과 클라이언트 설정을 둘 다 봐야 한다.

## 인증까지 함께 볼 것

암호화만 켜면 도청은 막히지만 상대가 누구인지는 여전히 모른다. 중간에서 가로채는 상대가 자기 인증서를 내밀어도 검증하지 않으면 통한다. 암호화를 켜는 작업에 인증서 검증을 함께 켜지 않으면 절반만 한 셈이다.

## 참고

- NIST SP 800-52 — 전송 구간 보호
- CIS Controls — 데이터 전송 보호
- 개인정보보호법 — 안전성 확보조치 기준`,
    diagram: {
      type: 'layers',
      caption: '평문 구간이 보여 주는 것',
      layers: [
        { label: '로그 전송', note: '전체 기록' },
        { label: '서비스 간 호출', note: '토큰' },
        { label: '데이터베이스', note: '질의와 결과' },
        { label: '캐시·큐', note: '세션·이벤트' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '처리 위치별 누락 위험',
      unit: '점',
      items: [
        { label: '네트워크 계층', value: 10, note: '일괄 적용' },
        { label: '사이드카', value: 25, note: '주입 누락' },
        { label: '앱마다 설정', value: 70, note: '빠뜨리기 쉬움' },
        { label: '안 함', value: 100, note: '전부 평문' },
      ],
    },
  },
  {
    slug: 'database-encryption-choice',
    title: '데이터베이스 암호화를 어디에 걸 것인가',
    body: `데이터를 암호화한다는 말은 여러 층을 가리킬 수 있다. 디스크 전체를 덮는 것과 특정 칸만 덮는 것은 막아 주는 상황이 전혀 다르다. 어떤 위협을 막으려는지 정하지 않으면 켜 놓고도 아무것도 막지 못한다.

## 층마다 막는 것이 다르다

| 층 | 막는 상황 |
| --- | --- |
| 디스크 암호화 | 장비·매체 도난 |
| 데이터베이스 전체 | 데이터 파일 유출 |
| 칸 단위 | 데이터베이스 접근자 |
| 애플리케이션에서 | 데이터베이스 관리자까지 |

디스크 암호화는 서버가 켜져 있는 동안에는 아무것도 막지 못한다. 침입자가 데이터베이스에 접속하면 평문으로 읽힌다. 그런데 "암호화했다"는 답으로 이것을 드는 경우가 많다.

![층마다 막는 상황](/img/posts/database-encryption-choice.svg)

## 칸 단위로 걸 때의 대가

| 항목 | 영향 |
| --- | --- |
| 정확 일치 검색 | 결정적 암호화로 가능 |
| 범위 검색 | 어려움 |
| 부분 일치 | 사실상 불가 |
| 정렬 | 불가 |
| 색인 효율 | 떨어짐 |

주민등록번호처럼 정확히 일치로만 찾는 값은 칸 단위 암호화가 잘 맞는다. 이름처럼 부분 검색이 필요한 값은 다른 방법을 찾아야 한다.

## 키를 어디에 두느냐가 핵심

| 위치 | 막는 범위 |
| --- | --- |
| 같은 데이터베이스 | 거의 없음 |
| 애플리케이션 서버 | 데이터베이스 유출 |
| 키 관리 서비스 | 서버 유출까지 일부 |
| 전용 장치 | 가장 넓음 |

키를 데이터와 같은 곳에 두면 암호화하지 않은 것과 크게 다르지 않다. 이 실수가 생각보다 흔하다.

![키 위치와 막는 범위](/img/posts/database-encryption-choice-2.svg)

## 무엇이 암호화됐는지 확인한다

\`\`\`sql
-- 어떤 칸이 암호화 대상인지 목록으로 관리한다
SELECT table_name, column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public'
  AND column_name ~ '(ssn|rrn|card|account|phone|email|birth)'
ORDER BY table_name;
\`\`\`

이름으로 찾은 목록이 실제 암호화 대상 목록과 맞는지 대조한다. 나중에 추가된 칸이 빠져 있는 경우가 대부분이다.

## 백업과 복제본까지 본다

운영 데이터베이스만 암호화하고 백업이나 읽기 복제본을 평문으로 두면 그쪽이 더 쉬운 표적이 된다. 백업은 보관 기간이 길고 접근 통제가 느슨한 경우가 많다. 암호화 범위를 정할 때 데이터가 복제되는 모든 곳을 함께 적어야 한다.

## 참고

- 개인정보보호법 — 개인정보의 암호화
- NIST SP 800-111 — 저장 데이터 암호화
- CIS Controls — 데이터 보호`,
    diagram: {
      type: 'layers',
      caption: '층마다 막는 상황',
      layers: [
        { label: '디스크', note: '매체 도난만' },
        { label: '데이터베이스 전체', note: '파일 유출' },
        { label: '칸 단위', note: 'DB 접근자' },
        { label: '애플리케이션', note: '관리자까지' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '키 위치와 막는 범위',
      unit: '점',
      items: [
        { label: '전용 장치', value: 90, note: '가장 넓음' },
        { label: '키 관리 서비스', value: 75, note: '서버 유출 일부' },
        { label: '앱 서버', value: 50, note: 'DB 유출' },
        { label: '같은 DB', value: 10, note: '거의 없음' },
      ],
    },
  },
  {
    slug: 'tokenization-vs-encryption',
    title: '토큰화와 암호화 중 무엇을 고를 것인가',
    body: `민감한 값을 그대로 두지 않는 방법에는 암호화와 토큰화가 있다. 암호화는 값을 되돌릴 수 있는 형태로 바꾸고, 토큰화는 값을 별도 저장소에 두고 대체 값만 남긴다. 어느 쪽이 맞는지는 그 값을 어떻게 쓰는지에 달렸다.

## 무엇이 다른가

| 항목 | 암호화 | 토큰화 |
| --- | --- | --- |
| 원본 위치 | 그 자리에 | 별도 저장소 |
| 되돌리기 | 키가 있으면 | 저장소 조회 |
| 유출 시 | 키가 함께 나가면 노출 | 대체 값만 나감 |
| 형식 유지 | 어려움 | 쉬움 |
| 감사 범위 | 키를 쓰는 모든 곳 | 저장소로 축소 |

감사 범위 축소가 토큰화의 주된 값어치다. 결제 규정에서 카드 번호를 다루는 시스템의 범위를 줄이려고 쓰는 경우가 대표적이다.

![두 방식의 구조](/img/posts/tokenization-vs-encryption.svg)

## 형식을 유지해야 할 때

기존 시스템이 특정 자릿수를 기대하면 암호문을 넣을 수 없다. 토큰은 같은 형식으로 만들 수 있어 주변을 고치지 않아도 된다.

| 상황 | 적합 |
| --- | --- |
| 자릿수 고정 필드 | 토큰화 |
| 대량 데이터 | 암호화 |
| 자주 되돌림 | 암호화 |
| 드물게 되돌림 | 토큰화 |

대량 데이터에 토큰화를 쓰면 저장소 조회가 병목이 된다. 한 건씩 되돌리는 흐름에 맞는 방식이다.

## 토큰 저장소가 새 위험이 된다

| 항목 | 내용 |
| --- | --- |
| 한 곳에 원본 집중 | 뚫리면 전부 |
| 가용성 | 멈추면 되돌릴 수 없음 |
| 접근 통제 | 가장 엄격하게 |
| 기록 | 조회 전부 남김 |

토큰화는 위험을 없애지 않고 한 곳으로 모은다. 그 한 곳을 지킬 수 있을 때 뜻이 있다.

![무엇을 고를 것인가](/img/posts/tokenization-vs-encryption-2.svg)

## 어디에 무엇이 있는지 센다

\`\`\`sql
-- 카드 번호 형식이 남아 있는 칸을 찾는다
SELECT table_name, column_name
FROM information_schema.columns
WHERE table_schema = 'public' AND data_type IN ('text','character varying');

-- 실제 값 표본에서 형식 확인 (한 테이블씩)
SELECT count(*) FROM payments WHERE card_no ~ '^[0-9]{13,19}$';
\`\`\`

0 이 아니면 그 칸에 원본이 남아 있다. 토큰화를 도입한 뒤에도 옛 데이터가 남아 있는 경우가 많아, 전환과 함께 과거 데이터 처리 계획이 필요하다.

## 결제 영역 밖에서도

주민등록번호, 계좌번호, 연락처처럼 정확히 일치로만 쓰는 값은 같은 논리가 적용된다. 검색과 통계에는 대체 값을 쓰고, 원본이 필요한 순간에만 저장소를 부른다. 그러면 원본에 닿는 코드가 몇 군데로 줄고, 그 몇 군데만 엄격하게 보면 된다.

## 참고

- PCI DSS — 토큰화 지침
- NIST SP 800-38G — 형식 보존 암호화
- 개인정보보호법 — 고유식별정보의 처리`,
    diagram: {
      type: 'matrix',
      caption: '두 방식의 구조',
      x: ['원본을 그 자리에', '별도 저장소'],
      y: ['자주 되돌림', '드물게'],
      cells: ['암호화', '조회 병목', '암호화', '토큰화'],
    },
    diagram2: {
      type: 'steps',
      caption: '토큰 저장소를 둘 때',
      steps: [
        { label: '접근 통제 최고 수준', note: '원본 집중' },
        { label: '조회 전부 기록', note: '누가 되돌렸나' },
        { label: '가용성 확보', note: '멈추면 못 읽음' },
        { label: '범위를 좁게', note: '부르는 코드 최소화' },
      ],
    },
  },
  {
    slug: 'searchable-encryption-limits',
    title: '암호화한 데이터를 검색하려 할 때',
    body: `데이터를 암호화하면 검색이 어려워진다. 이 문제를 풀려는 방법이 여럿 있는데, 각각 무엇을 포기하는지 알지 못하면 암호화했다는 느낌만 남고 실제 보호는 사라진다.

## 방법마다 새는 것이 다르다

| 방법 | 가능한 검색 | 새는 정보 |
| --- | --- | --- |
| 결정적 암호화 | 정확 일치 | 같은 값의 반복 |
| 순서 보존 | 범위·정렬 | 값의 순서 |
| 색인용 해시 | 정확 일치 | 같은 값의 반복 |
| 전부 복호화 후 검색 | 전부 | 성능 문제 |

같은 값이 반복된다는 사실만으로도 많은 것이 드러난다. 성별이나 등급처럼 값의 종류가 적은 칸은 빈도만 세어도 추측된다.

![방법별로 새는 정보](/img/posts/searchable-encryption-limits.svg)

## 빈도가 드러나는 문제

| 칸 | 값의 종류 | 추측 난이도 |
| --- | --- | --- |
| 성별 | 2~3개 | 매우 쉬움 |
| 등급 | 5~10개 | 쉬움 |
| 지역 | 수십 개 | 보통 |
| 연락처 | 매우 많음 | 어려움 |

종류가 적은 칸은 결정적 암호화를 쓰지 않는 편이 낫다. 암호화의 뜻이 거의 없으면서 성능만 잃는다.

## 실무적인 절충

| 방법 | 내용 |
| --- | --- |
| 검색용 별도 칸 | 해시를 색인, 원본은 암호화 |
| 앞 몇 글자만 | 부분 검색 지원, 노출 최소 |
| 검색 범위 축소 | 목록에서 빼고 상세에서만 |
| 애초에 저장하지 않기 | 가장 확실 |

저장하지 않는 선택지를 먼저 본다. 검색해야 하는 이유가 업무 요구인지 관성인지 물으면 상당수가 관성이다.

![절충안 고르기](/img/posts/searchable-encryption-limits-2.svg)

## 지금 검색이 무엇을 드러내는지 본다

\`\`\`sql
-- 암호화된 칸의 값 분포 — 종류가 적으면 빈도로 추측된다
SELECT enc_grade, count(*) AS 건수
FROM users GROUP BY enc_grade ORDER BY 건수 DESC LIMIT 10;

-- 상위 몇 개가 전체의 대부분이면 추측이 쉽다
SELECT count(DISTINCT enc_grade) AS 값_종류 FROM users;
\`\`\`

값 종류가 열 개 미만이면 그 칸은 사실상 공개된 것으로 본다. 그런 칸은 암호화 대신 접근 통제로 다루는 편이 맞다.

## 검색을 어디서 할 것인가

검색 자체를 애플리케이션 쪽으로 옮기는 방법도 있다. 필요한 범위만 가져와 복호화한 뒤 거르면 데이터베이스는 아무것도 모른 채로 남는다. 전체를 훑어야 하는 검색에는 못 쓰지만, 한 사용자의 데이터 안에서 찾는 경우에는 충분하다. 대부분의 검색이 그 범위 안에 있다.

## 참고

- NIST SP 800-38G — 형식 보존 암호화의 한계
- 개인정보보호법 — 암호화 조치 기준
- CWE-327 — 취약한 암호 알고리즘 사용`,
    diagram: {
      type: 'matrix',
      caption: '방법별로 새는 정보',
      x: ['값 종류 많음', '적음'],
      y: ['결정적 암호화', '무작위 암호화'],
      cells: ['쓸 만함', '빈도로 추측', '검색 불가', '가장 안전'],
    },
    diagram2: {
      type: 'steps',
      caption: '절충안 고르기',
      steps: [
        { label: '저장하지 않기', note: '가장 확실' },
        { label: '검색 범위 축소', note: '상세에서만' },
        { label: '앞 글자만 색인', note: '노출 최소' },
        { label: '앱에서 거르기', note: 'DB 는 모름' },
      ],
    },
  },
  {
    slug: 'random-value-generation',
    title: '무작위 값을 만들 때 쓰면 안 되는 것',
    body: `토큰, 세션 식별자, 비밀번호 재설정 값은 전부 추측되지 않아야 한다. 그런데 일반적인 난수 함수는 추측되지 않도록 만들어진 것이 아니다. 빠르고 고르게 퍼지도록 만들어진 것이라 목적이 다르다.

## 무엇이 다른가

| 종류 | 목적 | 예측 |
| --- | --- | --- |
| 일반 난수 | 통계적 균등 | 몇 개를 보면 다음을 계산 가능 |
| 암호학적 난수 | 예측 불가 | 불가능 |

일반 난수는 내부 상태에서 값을 만들어 내는데, 출력 몇 개를 보면 그 상태를 복원할 수 있다. 그러면 이전과 이후의 모든 값이 계산된다.

![두 종류의 차이](/img/posts/random-value-generation.svg)

## 어디에 써야 하나

| 용도 | 필요한 것 |
| --- | --- |
| 세션 식별자 | 암호학적 |
| 재설정 토큰 | 암호학적 |
| 초기 벡터·솔트 | 암호학적 |
| 화면 애니메이션 | 일반 |
| 표본 추출 | 일반 |

보안과 무관한 곳에는 일반 난수를 써도 된다. 문제는 한 곳에서 편하다고 쓴 함수가 다른 곳으로 복사되는 것이다.

## 길이도 함께 정한다

| 길이 | 경우의 수 |
| --- | --- |
| 32비트 | 40억 — 부족 |
| 64비트 | 대량 시도에 취약 |
| 128비트 | 충분 |
| 256비트 | 넉넉 |

128비트 이상을 권한다. 바이트로 16개, 16진수로 32글자다.

![길이와 안전 여유](/img/posts/random-value-generation-2.svg)

## 지금 무엇을 쓰는지 찾는다

\`\`\`bash
# 보안 값에 일반 난수를 쓴 자리
grep -rniE 'Math\.random|new Random\(|rand\(\)|mt_rand' \
  --include='*.{ts,js,java,php,py,go}' . | grep -v node_modules | head -20

# 시각을 값으로 쓴 자리 — 이것도 추측된다
grep -rniE '(token|secret|otp).{0,20}(Date\.now|time\(\)|currentTimeMillis)' \
  --include='*.{ts,js,java,php,py,go}' . | grep -v node_modules | head -10
\`\`\`

시각을 섞어 만든 값이 특히 위험하다. 언제 만들어졌는지 짐작할 수 있으면 후보가 크게 줄어든다.

## 만든 뒤에 확인할 것

값이 충분히 무작위인지는 눈으로 판단할 수 없다. 대신 만들어진 값들이 겹치는지는 확인할 수 있다. 데이터베이스에 유일 제약을 걸어 두면 충돌이 나는 순간 드러난다. 충돌이 실제로 발생한다면 길이가 짧거나 생성기가 잘못된 것이고, 둘 다 즉시 고쳐야 할 신호다.

## 참고

- NIST SP 800-90A — 난수 생성기
- OWASP — 암호학적 저장 Cheat Sheet
- CWE-338 — 취약한 난수 생성기 사용`,
    diagram: {
      type: 'flow',
      caption: '일반 난수가 예측되는 경로',
      steps: [
        { label: '값 몇 개 수집', note: '공개된 토큰' },
        { label: '내부 상태 복원', note: '계산 가능', danger: true },
        { label: '이후 값 계산', note: '전부' },
        { label: '남의 토큰 획득', note: '추측이 아님' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '길이와 안전 여유',
      unit: '비트',
      items: [
        { label: '32비트', value: 32, note: '부족' },
        { label: '64비트', value: 64, note: '취약' },
        { label: '128비트', value: 128, note: '충분' },
        { label: '256비트', value: 256, note: '넉넉' },
      ],
    },
  },
  {
    slug: 'password-hashing-migration',
    title: '비밀번호 해시를 옮기는 현실적인 방법',
    body: `옛 방식으로 저장된 비밀번호를 새 방식으로 바꿔야 할 때가 온다. 문제는 해시가 되돌릴 수 없어 일괄 변환이 안 된다는 점이다. 사용자가 로그인해야 원본을 알 수 있다.

## 되돌릴 수 없는 값을 어떻게 옮기나?

| 방법 | 내용 | 대가 |
| --- | --- | --- |
| 로그인 시 전환 | 맞으면 새 방식으로 다시 저장 | 미접속자는 그대로 |
| 전체 재설정 요구 | 모두에게 새 비밀번호 | 이탈 발생 |
| 겹쳐 씌우기 | 옛 해시를 새 방식으로 한 번 더 | 즉시 적용 |

겹쳐 씌우기가 즉시 효과를 낸다. 옛 해시 값을 입력으로 삼아 새 방식을 한 번 더 적용하면, 로그인을 기다리지 않고 전체가 강화된다.

![전환 방식의 차이](/img/posts/password-hashing-migration.svg)

## 겹쳐 씌울 때의 검증 순서

로그인할 때는 반대 순서로 확인한다. 입력받은 비밀번호에 옛 방식을 적용하고, 그 결과에 새 방식을 적용해 저장된 값과 비교한다.

| 단계 | 처리 |
| --- | --- |
| 1 | 입력에 옛 방식 적용 |
| 2 | 결과에 새 방식 적용 |
| 3 | 저장 값과 비교 |
| 4 | 맞으면 순수 새 방식으로 재저장 |

4단계를 함께 두면 시간이 지나며 겹친 값이 줄어든다. 두 방식을 병행하는 기간이 있지만 끝이 보인다.

## 어떤 방식으로 옮기나

| 항목 | 기준 |
| --- | --- |
| 알고리즘 | 메모리를 쓰는 방식 |
| 작업 계수 | 장비에서 재서 정함 |
| 솔트 | 계정마다 다르게 |
| 저장 형식 | 방식과 계수를 함께 |

방식과 계수를 해시 문자열에 함께 담아 두면 나중에 또 옮길 때 판단이 쉬워진다. 저장된 값만 보고 어떤 방식인지 알 수 있어야 한다.

![전환 중 저장 상태](/img/posts/password-hashing-migration-2.svg)

## 진행 상황을 본다

\`\`\`sql
-- 저장 형식별 분포 — 전환이 얼마나 진행됐는지
SELECT
  CASE
    WHEN password_hash LIKE '$argon2%' THEN '새 방식'
    WHEN password_hash LIKE '$2%'      THEN '중간 방식'
    ELSE '옛 방식'
  END AS 방식,
  count(*) AS 계정
FROM users GROUP BY 1 ORDER BY 2 DESC;
\`\`\`

옛 방식이 몇 달째 줄지 않으면 그 계정들은 접속하지 않는 계정이다. 전환을 기다리기보다 휴면 처리 대상으로 다루는 편이 낫다.

## 계수를 정하는 기준

작업 계수는 높을수록 안전하지만 로그인이 느려진다. 실제 장비에서 재서 로그인 하나가 수백 밀리초 정도 걸리는 값을 고른다. 다른 곳의 권장값을 그대로 가져오면 우리 장비에서는 너무 느리거나 너무 빠르다. 장비를 바꿀 때 다시 재는 것도 함께 정해 둔다.

## 참고

- OWASP — 비밀번호 저장 Cheat Sheet
- NIST SP 800-63B — 기억된 비밀 저장
- RFC 9106 — Argon2 규격`,
    diagram: {
      type: 'matrix',
      caption: '전환 방식의 차이',
      x: ['즉시 적용', '로그인 기다림'],
      y: ['사용자 영향 없음', '있음'],
      cells: ['겹쳐 씌우기', '로그인 시 전환', '해당 없음', '전체 재설정'],
    },
    diagram2: {
      type: 'steps',
      caption: '겹쳐 씌운 값의 검증',
      steps: [
        { label: '입력에 옛 방식', note: '1단계' },
        { label: '결과에 새 방식', note: '2단계' },
        { label: '저장 값과 비교', note: '3단계' },
        { label: '순수 새 방식으로', note: '재저장' },
      ],
    },
  },
  {
    slug: 'key-escrow-decision',
    title: '키를 맡겨 둘 것인가 말 것인가',
    body: `암호화 키의 사본을 따로 보관해 두면 담당자가 없어도 데이터를 복구할 수 있다. 동시에 그 사본을 쥔 사람은 모든 데이터를 읽을 수 있게 된다. 이 결정은 기술이 아니라 조직이 무엇을 더 두려워하는지의 문제다.

## 두 가지 실패

| 선택 | 실패 모습 |
| --- | --- |
| 사본 없음 | 키를 잃으면 데이터도 잃음 |
| 사본 있음 | 사본이 새면 전부 노출 |

어느 쪽도 공짜가 아니다. 데이터를 영영 못 읽는 것과 남이 읽을 수 있게 되는 것 중 어느 쪽이 더 큰 손해인지 먼저 정해야 한다.

![두 가지 실패](/img/posts/key-escrow-decision.svg)

## 사본을 둔다면 어떻게

| 항목 | 방법 |
| --- | --- |
| 분할 보관 | 여러 조각으로 나눠 각자 보관 |
| 복원 조건 | 정해진 인원 이상 모여야 |
| 보관 장소 | 물리적으로 분리 |
| 사용 기록 | 복원 시도 전부 |
| 정기 점검 | 실제로 복원되는지 |

조각으로 나누고 여러 사람이 모여야 복원되게 하면, 한 사람의 배신이나 실수로 전부가 나가지 않는다.

## 어떤 데이터에 적용하나

| 데이터 | 사본 필요성 |
| --- | --- |
| 장기 보관 기록 | 높음 |
| 법정 보존 대상 | 높음 |
| 세션·임시 데이터 | 없음 |
| 사용자 개인 암호화 | 신중 |

사용자가 스스로 키를 관리하는 구조에서 사본을 두면, 그 기능의 약속 자체가 달라진다. 그런 경우에는 사본을 두지 않는다는 사실 자체가 제품의 성질이 된다.

![복원 조건 설계](/img/posts/key-escrow-decision-2.svg)

## 복원이 되는지 확인한다

\`\`\`bash
# 조각을 모아 복원하는 절차를 실제로 돌려 본다 (시험 데이터로)
# 예: 3조각 중 2개로 복원되는 구성
for i in 1 2; do echo "조각 $i 보유자 확인"; done
echo "복원 시도 → 시험 데이터 복호화 확인"

# 보관된 조각의 무결성 — 파일이 손상되지 않았는지
sha256sum /secure/escrow/share-*.bin 2>/dev/null
\`\`\`

한 번도 복원해 보지 않은 절차는 필요한 순간에 작동하지 않는다. 연 1회라도 시험 데이터로 전 과정을 돌려 본다.

## 결정을 기록으로 남긴다

사본을 두지 않기로 했다면 그 결정과 이유를 문서로 남긴다. 몇 해 뒤 데이터를 못 읽는 상황이 왔을 때, 누가 왜 그렇게 정했는지가 없으면 그것이 사고로 취급된다. 위험을 알고 받아들인 결정과 모르고 방치한 상태는 결과가 같아도 다른 일이다.

## 참고

- NIST SP 800-57 — 키 복구 고려사항
- ISO/IEC 27002 — 암호 키 관리
- 개인정보보호법 — 안전성 확보조치`,
    diagram: {
      type: 'matrix',
      caption: '두 가지 실패',
      x: ['사본 있음', '없음'],
      y: ['사본 유출', '키 상실'],
      cells: ['전부 노출', '해당 없음', '피해 없음', '데이터 상실'],
    },
    diagram2: {
      type: 'steps',
      caption: '복원 조건 설계',
      steps: [
        { label: '조각으로 분할', note: '한 사람이 못 씀' },
        { label: '정족수 지정', note: '모여야 복원' },
        { label: '물리적 분리', note: '한 사고로 못 잃게' },
        { label: '정기 복원 시험', note: '작동 확인' },
      ],
    },
  },
]
