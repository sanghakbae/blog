import type { SeedPost } from './types'

/** 111~120 — 인증·계정과 암호 운영 */
export const posts12: SeedPost[] = [
  {
    slug: 'passkey-migration',
    title: '패스키 도입 순서와 비밀번호 폐지',
    body: `패스키는 비밀번호를 대체하는 수단이지 다단계 인증의 한 종류가 아니다. 개인키가 기기를 벗어나지 않고 출처가 서명에 묶이므로 피싱과 자격 증명 대입이 원리적으로 성립하지 않는다. 다만 기존 계정 체계를 하루아침에 바꿀 수는 없어서, 추가 수단으로 시작해 기본 수단으로 옮기고 마지막에 비밀번호를 끊는 세 단계가 현실적이다.

## 왜 피싱에 강한가?

패스키는 서명할 때 요청이 어느 출처에서 왔는지를 함께 포함한다. 복제한 화면에서 로그인을 유도해도 출처가 다르므로 서버 검증에서 걸린다. 사용자가 속아도 자격 증명 자체가 넘어가지 않는다는 점이 문자 인증이나 일회용 번호와 근본적으로 다르다.

![비밀번호와 패스키의 자격 증명 흐름](/img/posts/passkey-migration.svg)

## 인증 수단 비교

| 수단 | 피싱 저항 | 서버 보관물 | 분실 대응 |
| --- | --- | --- | --- |
| 비밀번호 | 없음 | 해시 | 재설정 메일 |
| 문자 일회용 번호 | 없음 | 없음 | 통신사 절차 |
| 앱 일회용 번호 | 없음 | 공유 비밀 | 복구 코드 |
| 푸시 승인 | 낮음 | 없음 | 기기 재등록 |
| 패스키 | 높음 | 공개키 | 다른 기기 또는 복구 수단 |
| 하드웨어 키 | 높음 | 공개키 | 예비 키 등록 |

서버에 공개키만 남는다는 점이 중요하다. 데이터베이스가 유출돼도 인증에 쓸 수 있는 값이 없다.

## 단계별 도입

\`\`\`
1단계  추가 수단  기존 로그인 유지, 패스키 등록 유도
2단계  기본 수단  패스키가 있으면 그것을 먼저 제시
3단계  단독 수단  비밀번호 삭제, 복구 경로만 남긴다
\`\`\`

2단계를 건너뛰면 등록률이 오르지 않는다. 로그인 화면에서 패스키를 첫 번째 선택지로 보여주는 것만으로 사용률이 크게 달라진다.

## 계정 복구가 진짜 난관이다

패스키를 도입해도 복구 경로가 메일 링크 한 번이면 전체 강도는 그 메일 계정의 강도로 내려앉는다. 기기를 모두 잃은 사용자를 어떻게 되돌릴지 미리 정하고, 그 경로에도 최소한 두 가지 확인을 두어야 한다.

- 여러 기기에 패스키를 등록하도록 유도
- 계정 단위 복구 코드를 발급하고 사용 시 알림
- 고위험 계정은 신원 확인 절차를 별도로 둔다

## 바로 확인하기

등록·인증 시 서버가 출처와 사용자 검증 플래그를 실제로 확인하는지가 핵심이다. 라이브러리에 맡기더라도 설정값은 직접 봐야 한다.

\`\`\`ts
const verification = await verifyAuthenticationResponse({
  response: body,
  expectedChallenge: session.challenge,   // 서버가 발급한 값과 일치
  expectedOrigin: 'https://app.example.com', // 출처 고정 — 피싱 저항의 핵심
  expectedRPID: 'example.com',
  requireUserVerification: true,          // 생체·PIN 확인 요구
})
if (!verification.verified) throw new Error('unverified')
await credentials.updateCounter(body.id, verification.authenticationInfo.newCounter)
\`\`\`

전환이 실제로 진행되는지는 지표로 본다.

\`\`\`
등록률   패스키를 1개 이상 가진 활성 사용자 비율
사용률   전체 로그인 중 패스키 로그인 비율
잔존     비밀번호 로그인만 쓰는 계정 수
복구율   복구 경로를 탄 비율 — 급증은 등록 안내 문제
\`\`\`

## 실제로 이렇게 터진다

패스키를 도입했지만 등록률이 5% 에서 멈춘 사례가 있다. 설정 화면 깊은 곳에 등록 메뉴를 두었고, 왜 필요한지 설명이 없었다. 비밀번호 로그인이 그대로 남아 있으니 바꿀 이유도 없었다.

복구 경로를 그대로 둔 경우도 있다. 패스키를 등록해도 "기기를 잃어버렸어요" 창구가 문자 인증이면 전체 수준이 문자 인증이다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 도입하면 비밀번호가 사라진다 | 병행 기간이 길다 |
| 기기 하나면 충분하다 | 여러 개 등록을 유도해야 한다 |
| 등록만 하면 안전하다 | 복구 경로가 약하면 소용없다 |
| 기업용이라 어렵다 | 개인 서비스에서 더 효과가 크다 |
| 생체 정보가 서버로 간다 | 기기 안에만 있다 |

## 왜 피싱에 강한가

| 방식 | 중계 공격 |
| --- | --- |
| 비밀번호 | 그대로 전달됨 |
| 문자 인증번호 | 실시간 중계 가능 |
| 앱 일회용 번호 | 실시간 중계 가능 |
| 패스키 | 출처가 다르면 서명하지 않음 |

패스키는 인증 요청에 사이트 출처가 묶여 있어, 가짜 사이트에서는 브라우저가 서명을 만들지 않는다. 사용자가 속아도 성립하지 않는다.

## 도입 순서

1. 관리자·개발자부터 등록을 필수로 한다 — 표본이자 지지자가 된다
2. 복구 절차를 먼저 강화한다 — 여기가 가장 약한 고리다
3. 로그인 후 배너로 등록을 권한다 — 설정 메뉴에 숨기지 않는다
4. 두 개 이상 등록을 유도한다 — 기기 분실 대비
5. 등록자에게는 비밀번호 로그인을 숨긴다
6. 등록률이 충분해지면 비밀번호를 선택 사항으로 내린다

\`\`\`sql
-- 등록률과 기기 수 분포 — 하나만 등록한 사용자가 위험군이다
SELECT count(*) FILTER (WHERE n = 0) AS none,
       count(*) FILTER (WHERE n = 1) AS one,
       count(*) FILTER (WHERE n >= 2) AS two_plus
FROM (SELECT u.id, count(c.id) AS n
      FROM users u LEFT JOIN webauthn_credentials c ON c.user_id = u.id
      GROUP BY u.id) t;
\`\`\`

## 참고

- W3C Web Authentication (WebAuthn) Level 3
- FIDO Alliance — Passkeys 배포 지침
- NIST SP 800-63B, 인증 보증 등급`,
    diagram: {
      type: 'steps',
      caption: '패스키 전환 단계',
      steps: [
        { label: '추가 수단으로 등록', note: '기존 로그인 유지' },
        { label: '기본 수단으로 승격', note: '로그인 화면 첫 선택지' },
        { label: '복구 경로 정비', note: '메일 링크 단독 금지' },
        { label: '비밀번호 폐지', note: '잔존 계정 개별 전환' },
      ],
    },
  },
  {
    slug: 'credential-stuffing',
    title: '크리덴셜 스터핑 탐지와 단계별 차단',
    body: `크리덴셜 스터핑은 다른 곳에서 유출된 아이디와 비밀번호 목록을 우리 로그인에 그대로 넣어보는 공격이다. 비밀번호가 실제로 맞기 때문에 인증 로직에는 아무 결함이 없어도 계정이 넘어간다. 막는 방법은 시도 자체를 비싸게 만들고, 성공한 로그인 중 수상한 것을 골라내는 두 축이다.

## 어떤 신호로 알아보는가?

정상 로그인과 다른 점은 개별 요청이 아니라 분포에 있다. 하나씩 보면 평범하고, 묶어서 보면 확연히 다르다.

- 실패율이 평소의 몇 배로 뛴다
- 한 아이피에서 서로 다른 계정 수십 개를 시도한다
- 계정마다 시도가 정확히 한 번씩만 있다
- 성공률이 0.1% 근처로 일정하다
- 사용자 에이전트와 요청 간격이 지나치게 균일하다

![시도 분포로 드러나는 자동화 로그인](/img/posts/credential-stuffing.svg)

## 대응 단계

| 단계 | 조치 | 사용자 영향 |
| --- | --- | --- |
| 상시 | 유출 비밀번호 목록 대조 | 없음 |
| 상시 | 계정·아이피별 요청 수 제한 | 없음 |
| 의심 | 응답 지연 증가 | 미미 |
| 의심 | 추가 인증 요구 | 있음 |
| 확정 | 해당 대역 차단 | 있음 |
| 사후 | 세션 폐기와 비밀번호 초기화 | 큼 |

## 가장 값싼 대책이 가장 효과적이다

가입과 비밀번호 변경 시점에 알려진 유출 비밀번호인지 대조하는 것만으로 성공 가능한 계정 수가 크게 줄어든다. 해시 접두어만 보내 확인하는 방식이 있으므로 비밀번호를 외부로 보내지 않고도 구현할 수 있다.

## 성공한 로그인도 검사한다

차단을 아무리 해도 일부는 성공한다. 로그인 성공 시 기기·지역·시간대가 평소와 다르면 알림을 보내고, 민감한 동작 전에 재인증을 요구한다. 계정 탈취 피해는 로그인 자체가 아니라 그 뒤의 정보 변경과 출금에서 발생한다.

## 바로 확인하기

로그에서 아이피당 계정 수와 계정당 아이피 수를 세는 것이 출발점이다. 두 값이 함께 커지면 자동화 트래픽이다.

\`\`\`bash
# 아이피별 시도 계정 수 — 상위 20개
awk '$0 ~ /login/ {print $1, $7}' access.log \\
  | sort -u | awk '{c[$1]++} END {for (i in c) print c[i], i}' \\
  | sort -rn | head -20

# 시간대별 로그인 실패 건수로 급증 구간 찾기
grep 'login.*401' access.log | awk '{print substr($4,2,14)}' | uniq -c | sort -rn | head
\`\`\`

성공 후 위험 동작에는 재인증을 넣는다. 이것이 실질적인 피해 차단선이다.

\`\`\`ts
const SENSITIVE = ['/me/email', '/me/password', '/withdrawals', '/api-keys']

app.use((req, res, next) => {
  if (!SENSITIVE.some((p) => req.path.startsWith(p))) return next()
  const age = Date.now() - req.session.authAt
  if (age > 5 * 60 * 1000) return res.status(401).json({ error: 'reauth_required' })
  next()
})
\`\`\`

## 실제로 이렇게 터진다

로그인 실패가 평소의 세 배가 됐지만 경보가 없던 사례가 있다. 임계값이 계정별로만 걸려 있었고, 공격자는 계정마다 두세 번씩만 시도했다. 전체 시도량은 수십만 건이었지만 계정별로는 정상 범위였다.

성공률이 낮다고 안심한 경우도 있다. 0.2% 라도 시도가 100만 건이면 2천 개 계정이 넘어간다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 비밀번호가 강하면 안전하다 | 다른 곳에서 유출된 것을 쓴다 |
| 계정별 잠금이면 막힌다 | 계정을 바꿔 가며 시도한다 |
| 성공률이 낮으면 무해하다 | 규모가 크면 절대 수가 크다 |
| 실패만 세면 된다 | 성공 패턴이 더 중요하다 |
| 다단계가 있으면 무관하다 | 등록률이 100% 가 아니다 |

## 무엇을 신호로 삼는가

| 신호 | 의미 |
| --- | --- |
| 전체 실패율 급증 | 대량 시도 |
| 실패 후 성공 비율 상승 | 유효 자격 증명 확인 |
| 한 주소에서 여러 계정 | 자동화 |
| 신규 기기 로그인 급증 | 탈취 후 접근 |
| 로그인 직후 정보 변경 | 계정 장악 |

마지막 항목이 실질적인 피해 신호다. 로그인 성공 자체는 정상으로 보이므로, 그다음 행동을 봐야 한다.

## 단계별 차단

1. 유출 목록과 대조해 이미 털린 비밀번호를 거부한다 — 가입·변경 시점
2. 전역 실패율 기준 경보를 건다 — 계정별과 별도로
3. 의심 점수에 따라 추가 인증을 요구한다 — 즉시 차단 대신
4. 성공 후 민감 동작에 재인증을 건다
5. 탈취가 확인되면 그 계정의 세션과 토큰을 전부 폐기한다

\`\`\`sql
-- 실패 후 성공 패턴 — 자격 증명 확인 시도의 흔적
SELECT src_ip, count(DISTINCT account) AS accounts,
       count(*) FILTER (WHERE ok) AS ok,
       count(*) FILTER (WHERE NOT ok) AS fail
FROM login_attempts
WHERE at > now() - interval '1 hour'
GROUP BY 1 HAVING count(DISTINCT account) > 20
ORDER BY accounts DESC LIMIT 20;
\`\`\`

## 참고

- OWASP Cheat Sheet — Credential Stuffing Prevention
- NIST SP 800-63B, 유출 비밀번호 대조 권고
- OWASP Automated Threats — OAT-008 Credential Stuffing`,
    diagram: {
      type: 'bars',
      caption: '대책별 시도 성공 가능 계정 감소 효과',
      unit: '상대값',
      items: [
        { label: '조치 없음', value: 100, note: '기준' },
        { label: '요청 수 제한', value: 70, note: '분산에 약함' },
        { label: '유출 목록 대조', value: 25, note: '가장 효과적' },
        { label: '다단계 인증', value: 5, note: '피싱 저항 수단일 때' },
      ],
    },
  },
  {
    slug: 'account-recovery',
    title: '계정 복구 절차의 인증 우회 지점',
    body: `계정 보안의 실제 강도는 가장 약한 진입 경로가 결정한다. 로그인에 다단계 인증을 붙여도 복구 절차가 메일 링크 하나면 전체 강도는 그 메일 계정의 강도와 같아진다. 복구는 예외 흐름이라 설계와 점검에서 자주 빠지지만, 공격자가 가장 먼저 두드리는 문이다.

## 어디가 뚫리는가?

- 재설정 토큰이 추측 가능하거나 만료되지 않는다
- 토큰을 써도 기존 세션이 유지된다
- 재설정 시 다단계 인증이 함께 해제돼 로그인 강도가 내려간다
- 응답 차이로 가입 여부가 드러난다
- 헬프데스크가 통화만으로 초기화해 준다
- 복구용 메일 주소를 본인 확인 없이 바꿀 수 있다

![복구 경로가 만드는 우회 통로](/img/posts/account-recovery.svg)

## 비밀번호 재설정 토큰의 요건

| 항목 | 기준 |
| --- | --- |
| 생성 | 암호학적 난수 32바이트 이상 |
| 저장 | 해시로 저장. 원문 보관 금지 |
| 만료 | 15~30분 |
| 사용 | 1회. 사용 즉시 폐기 |
| 범위 | 계정 하나, 목적 하나 |
| 통지 | 발급과 사용 모두 사용자에게 알림 |

## 복구 후 처리가 절반이다

비밀번호를 바꿨는데 공격자의 기존 세션이 살아 있으면 아무것도 해결되지 않는다. 재설정 성공 시 모든 세션과 갱신 토큰을 폐기하고, 등록된 기기 목록을 사용자에게 보여줘야 한다.

\`\`\`
재설정 성공 시
  1. 다른 모든 세션 폐기
  2. 갱신 토큰·API 키 무효화
  3. 등록 기기 목록 통지
  4. 다단계 인증 설정은 유지 — 함께 해제하지 않는다
  5. 감사 로그에 기록
\`\`\`

## 사람이 개입하는 경로

비밀번호 초기화를 사람이 대신 해주는 헬프데스크 경로는 가장 자주 악용되는 통로다. 요청자가 본인인지 확인하는 기준을 문서로 정하고, 자격 증명을 대신 만들어 주기 전에 확인 수단을 두 가지 이상 요구하며, 처리 내역을 감사 로그에 남긴다. 급하다는 압박이 있을 때 절차를 건너뛰는 것이 바로 소셜 엔지니어링의 목표다.

## 바로 확인하기

가입 여부가 응답으로 드러나는지, 토큰이 재사용되는지 두 가지를 먼저 본다.

\`\`\`bash
# 존재하는 계정과 없는 계정의 응답이 같아야 한다
for e in real@example.com nobody@example.com; do
  printf '%-22s ' "$e"
  curl -s -o /dev/null -w '%{http_code} %{time_total}s\n' -X POST \\
    https://app.example.com/password/reset \\
    -H 'Content-Type: application/json' -d "{\\"email\\":\\"$e\\"}"
done

# 같은 토큰을 두 번 쓰면 두 번째는 실패해야 한다
curl -s -o /dev/null -w '1차 %{http_code}\\n' -X POST https://app.example.com/password/confirm \\
  -H 'Content-Type: application/json' -d '{"token":"'$T'","password":"NewPass!23"}'
curl -s -o /dev/null -w '2차 %{http_code}\\n' -X POST https://app.example.com/password/confirm \\
  -H 'Content-Type: application/json' -d '{"token":"'$T'","password":"NewPass!45"}'
\`\`\`

## 실제로 이렇게 터진다

계정 복구가 인증보다 약해서 그쪽으로 뚫린 사례가 흔하다. 로그인에는 다단계를 걸어 두고, 복구는 메일로 보낸 링크 하나면 됐다. 메일 계정이 먼저 털리면 다단계는 무의미했다.

상담원 경로가 약한 경우도 있다. 사번과 생년월일로 본인 확인을 했는데 둘 다 이전 유출에서 구할 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 복구는 예외 경로다 | 공격자에게는 주 경로다 |
| 메일 인증이면 충분하다 | 메일이 가장 흔한 진입점이다 |
| 질문·답변은 보조 수단이다 | 대부분 검색으로 알 수 있다 |
| 상담원이 판단한다 | 판단 기준이 없으면 사회공학에 뚫린다 |
| 복구는 빨라야 한다 | 지연이 방어가 된다 |

## 복구 강도를 인증과 맞춘다

| 인증 수준 | 복구 요건 |
| --- | --- |
| 비밀번호만 | 메일 확인 |
| 비밀번호 + 문자 | 메일 + 대기 시간 |
| 패스키·하드웨어 키 | 두 번째 키 또는 신원 확인 |
| 특권 계정 | 담당자 승인 + 별도 확인 |

핵심 원칙은 복구가 인증보다 약하면 안 된다는 것이다. 강한 인증을 도입할 때 복구를 같이 올리지 않으면 실질 수준은 그대로다.

## 지연과 알림

즉시 처리하지 않고 대기 시간을 두면 정당한 소유자가 알아챌 기회가 생긴다.

1. 복구 요청 시 등록된 모든 경로로 알린다
2. 대기 시간을 둔다 — 민감 계정은 24시간
3. 대기 중 취소 링크를 제공한다
4. 완료 후에도 알리고, 되돌리기 경로를 남긴다
5. 복구 직후에는 민감 동작을 제한한다 — 출금·주소 변경

\`\`\`sql
-- 복구 후 곧바로 민감 정보를 바꾼 계정 — 탈취 신호
SELECT r.user_id, r.completed_at, c.changed_field, c.at
FROM account_recoveries r
JOIN sensitive_changes c ON c.user_id = r.user_id
WHERE c.at BETWEEN r.completed_at AND r.completed_at + interval '1 hour'
  AND r.completed_at > now() - interval '30 days'
ORDER BY r.completed_at DESC;
\`\`\`

## 참고

- OWASP Cheat Sheet — Forgot Password
- NIST SP 800-63B, 계정 복구와 재바인딩 요건
- CWE-640: Weak Password Recovery Mechanism`,
    diagram: {
      type: 'flow',
      caption: '복구 요청부터 세션 정리까지',
      steps: [
        { label: '복구 요청', note: '가입 여부를 노출하지 않는 응답' },
        { label: '토큰 발급', note: '해시 저장·짧은 만료' },
        { label: '본인 확인', note: '수단 두 가지 이상' },
        { label: '세션 전면 폐기', note: '공격자 세션 제거' },
      ],
    },
  },
  {
    slug: 'sso-scim',
    title: 'SSO 와 SCIM 으로 계정 관리 자동화',
    body: `통합 인증만 붙이면 계정 관리가 끝난다고 생각하기 쉽지만, 로그인 경로가 하나로 모여도 계정 자체는 각 서비스에 남는다. 퇴사자가 사내 계정을 잃어도 서비스에 직접 만든 계정으로 계속 접속할 수 있다면 오프보딩은 실패한 것이다. 인증은 SSO 로, 계정의 생성과 회수는 SCIM 으로 나눠 봐야 한다.

## 무엇이 어디까지 해결되는가?

| 영역 | SSO | SCIM |
| --- | --- | --- |
| 로그인 | 중앙 인증 | 관여 없음 |
| 계정 생성 | 첫 로그인 시 자동 생성 가능 | 입사 시 즉시 생성 |
| 권한 부여 | 속성 전달 수준 | 그룹 동기화 |
| 계정 회수 | 로그인만 차단 | 계정 비활성화 |
| 감사 | 인증 이력 | 계정 변경 이력 |

핵심 차이는 회수다. SSO 만 있으면 인증 경로만 막히고 서비스에 남은 계정과 개인 비밀번호, API 키는 그대로 살아 있다.

![인증 경로와 계정 수명 주기의 분리](/img/posts/sso-scim.svg)

## 우회 경로를 먼저 닫는다

- 서비스별 로컬 로그인 비활성화
- 초대 링크로 계정을 만드는 기능 제한
- 개인 API 키·개인 접근 토큰 정책 수립
- 도메인 소유 확인으로 조직 외 계정 유입 차단
- 서비스 계정은 별도 목록으로 관리

이 항목이 열려 있으면 SSO 를 붙여도 실질적인 통제가 생기지 않는다.

## 그룹을 권한의 단일 원천으로

권한을 서비스마다 개별 부여하면 재검토가 불가능해진다. 인사 시스템의 소속과 직무를 기준으로 그룹을 만들고, 서비스 권한은 그룹에만 연결한다. 사람의 이동은 그룹 이동으로만 표현되므로 권한 회수가 자동으로 따라온다.

\`\`\`
인사 시스템  →  디렉터리 그룹  →  서비스 역할
  입사·이동·퇴사     단일 원천        직접 부여 금지
\`\`\`

## 동기화 실패를 감시한다

SCIM 연동은 조용히 멈춘다. 토큰 만료, 속성 형식 변경, 대상 서비스의 정책 변경으로 중단되면 그 시점 이후의 퇴사자가 남는다. 동기화 지연과 실패를 지표로 만들어 경보를 걸어야 한다.

## 바로 확인하기

퇴사자 목록과 각 서비스의 활성 계정 목록을 대조하는 것이 가장 확실한 점검이다.

\`\`\`bash
# 인사 시스템의 퇴사자와 서비스 활성 계정 대조
comm -12 <(sort leavers.txt) <(sort service-active-users.txt) \\
  | tee remaining.txt | wc -l

# SCIM 으로 비활성화 처리
while read -r id; do
  curl -s -X PATCH "https://api.saas.example.com/scim/v2/Users/$id" \\
    -H "Authorization: Bearer $SCIM_TOKEN" \\
    -H 'Content-Type: application/scim+json' \\
    -d '{"schemas":["urn:ietf:params:scim:api:messages:2.0:PatchOp"],
         "Operations":[{"op":"replace","path":"active","value":false}]}'
done < remaining.txt
\`\`\`

## 실제로 이렇게 터진다

통합 인증은 붙였는데 계정 해제가 자동이 아니었던 사례가 있다. 퇴사자가 신원 제공자에서는 비활성화됐지만, 각 서비스에는 계정이 그대로 남아 로컬 로그인으로 접속됐다.

반대로 자동 프로비저닝이 과하게 동작한 경우도 있다. 그룹 하나에 사람을 추가하자 열 개 서비스에 계정이 생겼고, 그중 몇 개는 라이선스 비용이 붙었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 통합 인증이면 계정 관리가 된다 | 인증과 계정 수명은 별개다 |
| 비활성화하면 접근이 끊긴다 | 이미 발급된 세션이 남는다 |
| 로컬 로그인은 안 쓴다 | 비상용으로 열려 있는 경우가 많다 |
| 자동 생성은 편리하다 | 비용과 권한이 함께 늘어난다 |
| 표준을 쓰면 다 된다 | 서비스마다 지원 범위가 다르다 |

## 무엇을 자동화하는가

| 항목 | 효과 |
| --- | --- |
| 계정 생성 | 입사 시 즉시 접근 |
| 속성 동기화 | 부서·직책 변경 반영 |
| 그룹 기반 권한 | 역할 변경이 자동 반영 |
| 계정 비활성화 | 퇴사 시 즉시 차단 |
| 계정 삭제 | 보존 정책에 따라 |

비활성화가 가장 중요하다. 생성은 늦어도 사람이 요청하지만, 회수는 아무도 요청하지 않는다.

## 도입 순서

1. 신원 제공자를 하나로 정한다
2. 로컬 로그인 경로를 목록화하고 닫는다 — 비상 계정만 남긴다
3. 사용자 수가 많은 서비스부터 연동한다
4. 계정 생성보다 비활성화를 먼저 검증한다
5. 그룹으로 권한을 관리하고 개인 부여를 줄인다
6. 연동 밖 서비스를 주기적으로 찾는다

\`\`\`bash
# 신원 제공자에서 비활성인데 서비스에는 살아 있는 계정
comm -12 <(cut -d, -f1 idp-disabled.csv | sort -u) \
         <(cut -d, -f1 service-active-users.csv | sort -u)
# 출력이 있으면 회수가 안 된 것이다
\`\`\`

## 참고

- RFC 7644 — SCIM Protocol
- SAML 2.0 및 OpenID Connect 규격
- NIST SP 800-53, AC-2 계정 관리`,
    diagram: {
      type: 'layers',
      caption: '계정 통제의 구성 요소',
      layers: [
        { label: '인사 시스템', note: '입사·이동·퇴사의 단일 원천' },
        { label: '디렉터리 그룹', note: '권한의 유일한 부여 단위' },
        { label: 'SCIM 동기화', note: '계정 생성과 비활성화' },
        { label: 'SSO 인증', note: '로그인 경로 통합' },
      ],
    },
  },
  {
    slug: 'service-account',
    title: '서비스 계정 자격 증명 관리 원칙',
    body: `사람 계정은 퇴사와 권한 재검토로 정리되지만, 서비스 계정은 만든 사람도 잊고 아무도 회수하지 않는다. 그래서 권한은 가장 넓고 자격 증명은 가장 오래된 계정이 서비스 계정인 경우가 많다. 원칙은 세 가지다. 소유자를 반드시 둔다, 자격 증명에 수명을 둔다, 사람이 그 자격 증명으로 로그인할 수 없게 만든다.

## 왜 서비스 계정이 가장 위험해지는가?

배포가 급할 때 만들어지고, 권한 오류를 피하려 넓게 부여되며, 회전하면 장애가 날까 두려워 그대로 방치된다. 자격 증명이 저장소와 위키, 로컬 파일로 복제되면서 어디에 있는지도 알 수 없게 된다.

![사람 계정과 서비스 계정의 수명 주기 차이](/img/posts/service-account.svg)

## 자격 증명 유형별 선택

| 유형 | 수명 | 권장 여부 |
| --- | --- | --- |
| 워크로드 아이덴티티 | 요청 단위 | 최우선. 저장할 값이 없다 |
| 임시 토큰 발급 | 수십 분 | 권장 |
| 정적 키 + 자동 회전 | 30~90일 | 차선 |
| 정적 키 + 수동 회전 | 사실상 무한 | 피할 것 |
| 개인 계정 재사용 | 사람에 종속 | 금지 |

가장 좋은 비밀값은 존재하지 않는 비밀값이다. 클라우드와 컨테이너 환경에서는 실행 주체 자체를 신원으로 쓰는 방식이 있으므로 키를 만들 필요가 없는 경우가 많다.

## 등록 항목을 정한다

서비스 계정을 만들 때 다음을 기록하지 않으면 나중에 회수할 수 없다.

\`\`\`
이름     용도가 드러나는 규칙 (svc-<시스템>-<용도>)
소유팀   개인이 아니라 팀
용도     어떤 작업을 위해 어떤 자원에 접근하는가
권한     부여된 범위와 근거
회전     주기와 마지막 회전 시각
만료     이 계정이 필요 없어지는 시점 또는 재검토일
\`\`\`

## 회전을 전제로 설계한다

키가 하나면 회전할 때 반드시 끊긴다. 두 개를 동시에 유효하게 두고 새 키를 배포한 뒤 옛 키를 폐기하는 순서를 만들면 무중단 회전이 가능해진다. 이 구조가 없으면 회전 주기는 문서에만 남는다.

## 사람이 쓰지 못하게 한다

서비스 계정으로 대화형 로그인이 가능하면 감사 추적이 끊긴다. 대화형 로그인을 차단하고, 사람은 자기 계정으로 인증한 뒤 권한을 위임받아 작업하도록 만든다.

## 바로 확인하기

오래된 키와 쓰이지 않는 계정을 뽑는 것이 정리의 시작이다.

\`\`\`bash
# 90일 넘은 액세스 키
aws iam list-users --query 'Users[].UserName' --output text | tr '\\t' '\\n' \\
| while read -r u; do
    aws iam list-access-keys --user-name "$u" \\
      --query "AccessKeyMetadata[?CreateDate<='$(date -u -v-90d +%Y-%m-%d)'].[UserName,AccessKeyId,CreateDate]" \\
      --output text
  done

# 최근 사용 이력이 없는 키 — 회수 후보
aws iam get-access-key-last-used --access-key-id "$KEY" \\
  --query 'AccessKeyLastUsed.LastUsedDate'
\`\`\`

## 실제로 이렇게 터진다

서비스 계정의 자격 증명이 몇 년째 그대로였던 사례가 있다. 만든 사람은 퇴사했고, 어디에 쓰이는지 아무도 몰라 바꾸지 못했다. 그 값은 설정 파일과 위키와 채팅 이력에 흩어져 있었다.

사람 계정을 서비스용으로 쓴 경우도 흔하다. 퇴사 처리로 계정이 잠기자 야간 배치가 전부 멈췄다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 서비스 계정은 사람이 안 쓴다 | 실제로는 사람도 쓴다 |
| 회전하면 서비스가 멈춘다 | 두 값 병행이면 안 멈춘다 |
| 권한을 넓게 줘야 편하다 | 침해 시 그 범위가 피해다 |
| 사람 계정을 쓰면 간단하다 | 퇴사·휴가에 서비스가 멈춘다 |
| 이름만 보면 용도를 안다 | 몇 년 뒤에는 아무도 모른다 |

## 무엇을 기록하는가

| 항목 | 없으면 |
| --- | --- |
| 소유 팀·담당자 | 물어볼 사람이 없다 |
| 용도 | 지워도 되는지 모른다 |
| 사용 위치 | 회전 시 무엇이 깨질지 모른다 |
| 권한 근거 | 왜 이 권한인지 모른다 |
| 회전 주기·최종 회전일 | 방치를 못 잡는다 |
| 만료일 | 임시 계정이 영구가 된다 |

## 관리 원칙

1. 사람 계정을 서비스에 쓰지 않는다
2. 서비스마다 계정을 분리한다 — 공유 금지
3. 가능한 곳은 자격 증명 자체를 없앤다 — 역할·신원 연동
4. 남는 것은 회전 가능한 구조로 만든다 — 두 값 병행
5. 대화형 로그인을 차단한다 — 서비스 계정은 화면에 로그인하지 않는다
6. 사용 기록을 기준으로 권한을 좁힌다

\`\`\`bash
# 오래 회전되지 않은 서비스 계정
aws iam list-users --query 'Users[?starts_with(UserName, \`svc-\`)].UserName' --output text |
  tr '\t' '\n' | while read -r u; do
    aws iam list-access-keys --user-name "$u" \
      --query 'AccessKeyMetadata[].[UserName,AccessKeyId,CreateDate,Status]' --output text
  done | awk '{print}' | sort -k3

# 서비스 계정이 대화형 로그인을 한 적 있는지 (윈도)
# 4624 로그온 유형 2·10 에 svc- 계정이 나오면 사람이 쓰고 있다는 뜻이다
\`\`\`

## 참고

- NIST SP 800-53, IA-5 인증자 관리
- OWASP Cheat Sheet — Secrets Management
- 클라우드 공급자별 워크로드 아이덴티티 문서`,
    diagram: {
      type: 'matrix',
      caption: '소유자 지정과 자격 증명 수명',
      x: ['수명 있음', '수명 없음'],
      y: ['소유팀 지정', '소유자 불명'],
      cells: ['관리 가능', '회전 누락 위험', '사고 시 추적 불가', '가장 위험한 상태'],
    },
  },
  {
    slug: 'token-revocation',
    title: '토큰 폐기 설계와 즉시 차단 경로',
    body: `상태 없는 토큰의 장점은 검증에 저장소가 필요 없다는 것이고, 단점은 그래서 취소가 안 된다는 것이다. 계정이 탈취됐을 때 "지금 즉시 끊는" 경로가 없으면 만료를 기다려야 한다. 해결은 접근 토큰을 짧게, 갱신 토큰을 취소 가능하게 만들고, 즉시 차단이 필요한 사건 목록을 미리 정해두는 것이다.

## 무엇을 즉시 끊어야 하는가?

- 비밀번호 변경과 계정 복구
- 다단계 인증 수단 변경
- 권한 회수와 역할 변경
- 퇴사와 계약 종료
- 침해 의심 신고
- 기기 분실 신고

이 목록이 없으면 폐기 기능을 만들어도 호출하는 곳이 없다.

![접근 토큰과 갱신 토큰의 수명 분리](/img/posts/token-revocation.svg)

## 폐기 방식 비교

| 방식 | 즉시성 | 비용 |
| --- | --- | --- |
| 만료만 기다림 | 없음 | 없음 |
| 접근 토큰 수명 단축 | 수 분 | 갱신 요청 증가 |
| 폐기 목록 조회 | 즉시 | 검증마다 조회 |
| 토큰 버전 필드 | 즉시 | 사용자 레코드 조회 |
| 세션 저장소 사용 | 즉시 | 저장소 의존 |

## 실무에서 잘 맞는 조합

접근 토큰은 5~15분으로 짧게 두고, 갱신 시점에 사용자 상태를 확인한다. 사용자 레코드에 토큰 버전을 두고 사건이 발생하면 값을 올린다. 그러면 다음 갱신에서 모든 기존 토큰이 무효가 된다. 즉시성이 더 필요한 경로는 검증 단계에서 폐기 목록을 조회한다.

\`\`\`
접근 토큰   5~15분, 서명 검증만
갱신 토큰   회전형, 저장소에 보관, 재사용 감지
사건 발생   tokenVersion += 1  →  기존 토큰 전부 무효
고위험 경로 검증 시 폐기 목록 조회 추가
\`\`\`

## 갱신 토큰 재사용 감지

갱신 토큰은 한 번 쓰면 새 것으로 바꾼다. 이미 쓰인 토큰이 다시 들어오면 복제된 것으로 보고 해당 계정의 토큰 계열 전체를 폐기한다. 탈취를 사후에라도 잡아내는 유일한 수단이다.

## 로그아웃은 어디까지인가

사용자가 기대하는 로그아웃은 "이 기기에서 나가기"이고, 보안이 필요한 것은 "모든 기기에서 나가기"다. 두 기능을 모두 제공하고, 활성 세션 목록을 사용자에게 보여줘야 한다.

## 바로 확인하기

비밀번호를 바꾼 뒤 옛 토큰이 실제로 막히는지 확인한다. 막히지 않으면 폐기 경로가 없는 것이다.

\`\`\`bash
OLD=$ACCESS_TOKEN
curl -s -X POST https://api.example.com/me/password \\
  -H "Authorization: Bearer $OLD" -H 'Content-Type: application/json' \\
  -d '{"current":"Old!2345","next":"New!6789"}'

# 옛 접근 토큰 — 수명 내라면 잠시 유효할 수 있다
curl -s -o /dev/null -w '접근 토큰 %{http_code}\\n' -H "Authorization: Bearer $OLD" \\
  https://api.example.com/me

# 옛 갱신 토큰 — 반드시 401 이어야 한다
curl -s -o /dev/null -w '갱신 토큰 %{http_code}\\n' -X POST https://api.example.com/token/refresh \\
  -H 'Content-Type: application/json' -d "{\\"refresh_token\\":\\"$OLD_REFRESH\\"}"
\`\`\`

## 실제로 이렇게 터진다

유출된 토큰을 무효화할 방법이 없어 만료를 기다린 사례가 있다. 수명이 30일이었고, 그동안 공격자가 API 를 계속 호출했다. 계정을 잠가도 토큰은 살아 있었다.

폐기 기능은 있었는데 캐시 때문에 늦은 경우도 있다. 각 서비스가 폐기 목록을 5분마다 갱신해서, 그 사이 요청은 통과했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 로그아웃하면 폐기된다 | 클라이언트에서 지울 뿐이다 |
| 짧은 수명이면 폐기가 필요 없다 | 그 수명 동안은 유효하다 |
| 계정을 잠그면 끝이다 | 발급된 토큰과 무관하다 |
| 폐기 목록은 비싸다 | 짧은 수명과 함께 쓰면 작다 |
| 갱신 토큰만 관리하면 된다 | 접근 토큰 수명만큼 지연된다 |

## 즉시 차단이 필요한 상황

| 상황 | 폐기 대상 |
| --- | --- |
| 자격 증명 유출 | 그 사용자 전체 |
| 퇴사·계정 정지 | 그 사용자 전체 |
| 권한 변경 | 해당 범위 |
| 기기 분실 | 그 기기 세션 |
| 의심 활동 | 그 세션 |

## 어떻게 구현하는가

수명과 폐기 목록을 조합한다. 접근 토큰을 짧게 두면 목록이 작아진다.

| 요소 | 권장 |
| --- | --- |
| 접근 토큰 수명 | 5~15분 |
| 갱신 토큰 | 서버 저장, 회전 |
| 폐기 목록 | 접근 토큰 수명만큼만 보관 |
| 전파 | 목록 조회를 캐시하되 수명을 짧게 |
| 일괄 폐기 | 사용자별 발급 기준 시각으로 |

일괄 폐기는 목록이 아니라 기준 시각으로 하면 저장이 거의 필요 없다. 사용자 레코드에 "이 시각 이전 발급 토큰은 무효" 를 두고 검증 시 비교한다.

\`\`\`ts
// 개별 폐기는 목록으로, 일괄 폐기는 기준 시각으로
const user = await users.get(claims.sub)
if (claims.iat < user.tokensInvalidBefore) return reject('폐기된 토큰')
if (await revoked.has(claims.jti)) return reject('폐기된 토큰')
\`\`\`

## 점검 절차

\`\`\`bash
# 폐기가 실제로 즉시 반영되는지
TOKEN=$(login)
curl -s -o /dev/null -w '폐기 전 %{http_code}\n' https://stg.example.com/api/me -H "Authorization: Bearer $TOKEN"
curl -s -X POST https://stg.example.com/api/sessions/revoke-all -H "Authorization: Bearer $TOKEN"
sleep 1
curl -s -o /dev/null -w '폐기 후 %{http_code}\n' https://stg.example.com/api/me -H "Authorization: Bearer $TOKEN"
# 폐기 후 401 이어야 한다
\`\`\`

## 참고

- RFC 7009 — OAuth 2.0 Token Revocation
- OAuth 2.0 Security Best Current Practice, 갱신 토큰 회전
- OWASP Cheat Sheet — Session Management`,
    diagram: {
      type: 'flow',
      caption: '사건 발생 시 토큰 무효화 경로',
      steps: [
        { label: '보안 사건', note: '비밀번호 변경·권한 회수' },
        { label: '토큰 버전 증가', note: '사용자 레코드 갱신' },
        { label: '갱신 거부', note: '기존 계열 전체 무효' },
        { label: '재로그인', note: '새 자격 증명으로만' },
      ],
    },
  },
  {
    slug: 'field-level-encryption',
    title: '필드 단위 암호화와 검색 가능성',
    body: `디스크 암호화는 디스크를 훔쳐 갔을 때만 도움이 된다. 애플리케이션 계정이 유출되거나 조회 권한이 오용되는 상황에서는 아무 효과가 없다. 그래서 주민등록번호나 계좌번호 같은 값은 필드 단위로 암호화한다. 문제는 암호화한 값으로는 검색과 정렬, 부분 일치가 되지 않는다는 점이고, 설계의 대부분은 이 제약을 다루는 일이다.

## 어떤 위협에 효과가 있는가?

| 위협 | 디스크 암호화 | 필드 암호화 |
| --- | --- | --- |
| 디스크·백업 매체 탈취 | 효과 있음 | 효과 있음 |
| 데이터베이스 파일 유출 | 효과 있음 | 효과 있음 |
| 애플리케이션 계정 유출 | 없음 | 키 접근이 없으면 효과 |
| 운영자 직접 조회 | 없음 | 효과 있음 |
| 인젝션으로 인한 대량 조회 | 없음 | 효과 있음 |
| 권한 있는 사용자의 오용 | 없음 | 없음 |

마지막 줄이 중요하다. 정당한 권한으로 화면에서 조회하는 오용은 암호화로 막히지 않는다. 마스킹과 조회 상한, 조회 이력이 필요한 영역이다.

![평문 조회 경로와 복호화 경계](/img/posts/field-level-encryption.svg)

## 검색을 살리는 방법

- 완전 일치 검색 — 결정적 암호화 또는 별도 해시 색인 컬럼
- 부분 일치 — 검색 전용 토큰을 미리 만들어 저장
- 정렬·범위 — 암호화 필드로는 불가. 비민감 파생값으로 대체
- 중복 확인 — 키 있는 해시(HMAC) 컬럼에 유니크 제약

결정적 암호화는 같은 값이 같은 암호문이 되므로 빈도 분석에 노출된다. 성별이나 지역처럼 값의 종류가 적은 필드에는 쓰지 않는다.

## 키 접근을 애플리케이션 밖에 둔다

키가 애플리케이션 메모리에 상주하면 애플리케이션 침해로 전부 복호화된다. 복호화를 키 관리 서비스에 위임하고 호출 이력을 남기면, 대량 복호화 자체가 탐지 신호가 된다.

\`\`\`
저장   데이터 키로 암호화 → 데이터 키는 마스터 키로 봉투 암호화
조회   필요한 건에 대해서만 복호화 요청
감사   복호화 호출 건수와 주체를 기록 — 급증 시 경보
회전   마스터 키 회전은 데이터 재암호화 없이 가능하게 설계
\`\`\`

## 바로 확인하기

암호화가 실제로 적용됐는지는 저장된 값을 직접 보는 것으로 확인한다.

\`\`\`sql
-- 평문이 남아 있는지. 암호문이면 형식이 일정하다.
SELECT id, ssn_enc, length(ssn_enc) FROM users LIMIT 5;

-- 중복 확인용 HMAC 컬럼에 유니크 제약이 있는지
SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'users';
\`\`\`

복호화 호출을 지표로 만들면 오용 탐지가 가능해진다.

\`\`\`
정상   건별 조회, 시간당 수십 건
의심   단일 주체가 시간당 수천 건 복호화
경보   업무 시간 외 대량 복호화
차단   일일 복호화 상한 초과 시 승인 요구
\`\`\`

## 실제로 이렇게 터진다

주민등록번호를 암호화했는데 검색이 필요해져 평문 컬럼을 하나 더 만든 사례가 있다. 암호화 컬럼은 그대로 두고 평문으로 조회했으니 암호화의 의미가 사라졌다.

키를 애플리케이션 설정에 둔 경우도 있다. 데이터베이스와 애플리케이션이 함께 침해되면 한 번에 풀린다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호화하면 검색을 못 한다 | 방식에 따라 가능하다 |
| 데이터베이스 암호화면 충분하다 | 관리자 조회는 막지 못한다 |
| 키를 설정에 두면 편하다 | 함께 유출되면 무의미하다 |
| 전 필드를 암호화하면 안전하다 | 성능과 운영 부담이 크다 |
| 한 번 정하면 바꾸기 어렵다 | 키 버전을 남기면 교체 가능하다 |

## 검색 요구별 방법

| 요구 | 방법 | 한계 |
| --- | --- | --- |
| 완전 일치 | 결정적 암호화 또는 키 있는 해시 | 빈도 분석 가능 |
| 부분 일치 | 별도 색인 필요 | 설계 복잡 |
| 범위 검색 | 사실상 어려움 | 평문 구간값 병행 |
| 정렬 | 어려움 | 정렬용 컬럼 분리 |
| 조회 없음 | 무작위 암호화 | 가장 안전 |

완전 일치만 필요하면 키 있는 해시를 별도 컬럼에 두는 방식이 실용적이다. 원문은 무작위 암호화로 저장하고, 검색은 해시로 한다.

\`\`\`sql
-- 원문은 무작위 암호화, 검색은 키 있는 해시로
CREATE TABLE persons (
  id          bigserial PRIMARY KEY,
  rrn_enc     bytea NOT NULL,      -- 무작위 암호화, 조회 불가
  rrn_hmac    bytea NOT NULL,      -- 키 있는 해시, 완전 일치 검색용
  key_version int   NOT NULL       -- 키 교체를 위해 버전을 남긴다
);
CREATE INDEX ON persons (rrn_hmac);
\`\`\`

키 버전 컬럼이 중요하다. 키를 바꿀 때 옛 버전으로 복호하면서 점진적으로 옮길 수 있다.

## 무엇을 암호화하는가

| 항목 | 권장 |
| --- | --- |
| 주민등록번호·여권번호 | 필드 암호화 필수 |
| 계좌·카드 | 토큰화 우선 |
| 연락처·주소 | 등급에 따라 |
| 이름 | 대개 불필요 |
| 비밀번호 | 암호화가 아니라 단방향 해시 |

마지막 항목을 혼동하면 안 된다. 비밀번호는 복호할 이유가 없다.

## 참고

- NIST SP 800-57, 키 관리 권고
- OWASP Cheat Sheet — Cryptographic Storage
- 개인정보의 안전성 확보조치 기준, 암호화 대상`,
    diagram: {
      type: 'layers',
      caption: '데이터 보호 계층과 효과 범위',
      layers: [
        { label: '디스크 암호화', note: '매체 탈취에만 효과' },
        { label: '필드 단위 암호화', note: '데이터 유출 시 효과' },
        { label: '복호화 권한 분리', note: '키 접근을 별도 통제' },
        { label: '조회 이력·상한', note: '권한 오용 탐지' },
      ],
    },
  },
  {
    slug: 'kms-hsm-choice',
    title: 'KMS 와 HSM 선택 기준과 키 관리',
    body: `키를 어디에 둘지는 규정과 위협 모델, 그리고 운영 능력으로 결정한다. 관리형 키 관리 서비스는 대부분의 경우에 충분하고 운영 부담이 가장 낮다. 전용 하드웨어 모듈은 키가 물리적으로 분리되고 규정 요건을 충족하지만 비용과 가용성 설계 부담이 크다. 기준 없이 "더 안전한 쪽"을 고르면 관리 실패로 더 위험해진다.

## 무엇을 기준으로 나누는가?

| 기준 | 관리형 KMS | 전용 HSM |
| --- | --- | --- |
| 키 노출 경계 | 서비스 경계 안 | 하드웨어 밖으로 나오지 않음 |
| 규정 대응 | 다수 인증 보유 | 특정 등급 요건 충족 |
| 운영 부담 | 낮음 | 높음. 정족수 관리 필요 |
| 비용 | 사용량 과금 | 고정비 큼 |
| 가용성 | 서비스 보장 | 직접 이중화 |
| 성능 | 호출 단위 제한 | 장비 처리량 한계 |

![봉투 암호화에서 키의 계층](/img/posts/kms-hsm-choice.svg)

## 봉투 암호화가 기본 형태다

데이터는 데이터 키로 암호화하고, 데이터 키는 마스터 키로 암호화해 데이터 옆에 저장한다. 마스터 키는 키 관리 서비스를 벗어나지 않고, 대량 데이터 암복호는 애플리케이션에서 수행하므로 호출 한도와 성능 문제를 함께 해결한다.

\`\`\`
평문  --(데이터 키)-->  암호문        저장
데이터 키  --(마스터 키)-->  암호화된 데이터 키   같이 저장
마스터 키  →  KMS/HSM 밖으로 나오지 않는다
\`\`\`

## 회전은 두 층으로 생각한다

마스터 키 회전은 새 버전을 추가하는 것으로 끝난다. 기존 데이터는 예전 버전으로 복호화되므로 재암호화가 필요 없다. 데이터 키 회전이 실제 재암호화를 요구하므로, 재암호화가 필요한 범위를 미리 설계해 둬야 회전 주기가 지켜진다.

## 접근 권한을 두 축으로 나눈다

키를 쓸 권한과 키 정책을 바꿀 권한을 분리한다. 같은 주체가 둘을 모두 가지면 스스로 권한을 넓혀 전체를 복호화할 수 있다. 키 정책 변경은 감사 대상으로 두고 별도 승인을 요구한다.

## 언제 HSM 이 필요한가

- 규정이 특정 등급의 하드웨어 모듈을 명시할 때
- 서비스 제공자가 키에 접근할 수 없어야 한다는 요건이 있을 때
- 인증서 발급 기관처럼 키 하나가 전체 신뢰의 근원일 때

그 외에는 관리형 서비스가 실수 여지를 줄인다.

## 바로 확인하기

키 정책과 회전 설정, 사용 이력을 확인하는 것이 점검의 핵심이다.

\`\`\`bash
# 자동 회전 설정 여부
aws kms get-key-rotation-status --key-id "$KEY_ID"

# 키 정책에 과도한 주체가 없는지
aws kms get-key-policy --key-id "$KEY_ID" --policy-name default \\
  --output text | python3 -m json.tool | grep -A3 Principal

# 복호화 호출 주체별 집계 — 급증 탐지의 기준선
aws cloudtrail lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=Decrypt \\
  --max-results 50 --query 'Events[].Username' --output text | tr '\\t' '\\n' | sort | uniq -c
\`\`\`

## 실제로 이렇게 터진다

규정을 이유로 전용 장비를 도입했는데 운영 인력이 없어 방치된 사례가 있다. 펌웨어 갱신도, 백업도, 이중화 시험도 하지 않았다. 장애가 났을 때 복구에 며칠이 걸렸다.

반대로 관리형 서비스로 충분한데 과하게 투자한 경우도 있다. 요구사항을 확인하지 않고 "가장 안전한 것" 을 골랐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 전용 장비가 항상 낫다 | 운영 부담이 위험을 만든다 |
| 관리형은 공급자가 키를 본다 | 설계상 접근할 수 없는 구성이 있다 |
| 규정이 전용 장비를 요구한다 | 인증 수준을 요구하는 경우가 많다 |
| 한 번 고르면 못 바꾼다 | 봉투 암호화면 교체 가능하다 |
| 비용은 라이선스뿐이다 | 이중화·운영·시험이 더 크다 |

## 선택 기준

| 요구 | 관리형 키 서비스 | 관리형 전용 장비 | 자체 장비 |
| --- | --- | --- | --- |
| 단독 제어 요구 | 부분 | 높음 | 완전 |
| 인증 수준 | 대개 충족 | 높음 | 구성에 따라 |
| 운영 부담 | 낮음 | 중간 | 높음 |
| 비용 | 요청 수 기준 | 시간당 | 장비 + 인력 |
| 이중화 | 자동 | 구성 필요 | 직접 구축 |
| 적합 | 대부분 | 규정 요구 시 | 특수 요구 |

대부분의 조직은 첫 번째 열로 충분하다. 두 번째 이상은 규정이나 계약이 명시적으로 요구할 때 고른다.

## 무엇을 확인하고 고르는가

1. 규정·계약이 요구하는 인증 수준을 문서에서 확인한다 — 추정하지 않는다
2. 키를 우리만 제어해야 하는지, 공급자 접근 불가만 필요한지 구분한다
3. 초당 요청 수를 추정한다 — 봉투 암호화면 대개 작다
4. 이중화와 복구 절차를 누가 운영할지 정한다
5. 비용을 라이선스가 아니라 운영 포함으로 계산한다

\`\`\`bash
# 지금 키 사용량 — 관리형으로 충분한지 판단 근거
aws cloudwatch get-metric-statistics --namespace AWS/KMS \
  --metric-name NumberOfOperations --start-time "$(date -u -v-7d +%FT%TZ)" \
  --end-time "$(date -u +%FT%TZ)" --period 86400 --statistics Sum \
  --query 'Datapoints[].[Timestamp,Sum]' --output text | sort
\`\`\`

## 참고

- NIST SP 800-57 Part 1, 키 관리 일반 권고
- FIPS 140-3 보안 요건 등급
- 클라우드 공급자별 봉투 암호화 문서`,
    diagram: {
      type: 'layers',
      caption: '키 계층과 보호 경계',
      layers: [
        { label: '마스터 키', note: 'KMS/HSM 밖으로 나오지 않음' },
        { label: '데이터 키', note: '암호화된 상태로 데이터 옆에' },
        { label: '데이터', note: '데이터 키로 암복호' },
        { label: '감사 이력', note: '복호화 호출 주체 기록' },
      ],
    },
  },
  {
    slug: 'certificate-automation',
    title: '인증서 자동 갱신과 만료 감시',
    body: `인증서 만료로 인한 장애는 원인이 명확하고 예고까지 되는데도 반복된다. 갱신 담당자가 바뀌고, 자동화가 일부 자산만 덮고, 내부 통신용 인증서는 아무도 목록에 넣지 않기 때문이다. 해결은 발급을 자동화하는 것과, 자동화가 닿지 않는 자산을 외부에서 감시하는 것 두 가지를 함께 두는 것이다.

## 어디가 빠지는가?

- 내부 서비스 간 통신에 쓰는 사설 인증서
- 로드 밸런서가 아닌 개별 인스턴스에 설치된 인증서
- 클라이언트 인증용 인증서
- 모바일 앱에 설정한 인증서 고정 값
- 코드 서명 인증서
- 만료가 몇 년 뒤라 잊힌 루트·중간 인증서

![발급 자동화와 외부 만료 감시의 이중 구조](/img/posts/certificate-automation.svg)

## 자산별 접근

| 자산 | 갱신 방식 | 감시 |
| --- | --- | --- |
| 공개 웹 | ACME 자동 발급 | 외부에서 남은 일수 확인 |
| 내부 서비스 | 사설 CA + 짧은 수명 | 발급 기록 기준 |
| 클라이언트 인증 | 사설 CA + 배포 절차 | 사용 실패율 |
| 앱 인증서 고정 | 앱 배포 주기 고려 | 예비 핀 필수 |
| 코드 서명 | 수동 + 하드웨어 보관 | 만료 캘린더 |

## 수명을 짧게 하면 자동화가 강제된다

90일 수명은 수동 갱신을 사실상 불가능하게 만들어 자동화를 강제한다. 자동 갱신이 도입되면 만료 사고는 사라진다. 이것이 짧은 수명이 오히려 안정적인 이유다.

## 갱신 실패를 미리 안다

만료 당일에 아는 것과 갱신 시도 실패를 아는 것은 다르다. 갱신은 만료 30일 전부터 시도하고, 시도 실패를 즉시 경보로 만든다. 만료 임박 경보는 자동화가 이미 실패한 뒤의 마지막 안전망이다.

\`\`\`
D-30  자동 갱신 시도 시작
D-30  시도 실패 시 즉시 경보 — 조치 여유 확보
D-14  남은 일수 경보 2단계
D-7   담당자 호출 수준으로 상향
D-0   장애
\`\`\`

## 인증서 고정은 예비 핀 없이 하지 않는다

모바일 앱에 인증서를 고정했는데 예비 핀이 없으면, 인증서 교체 시점에 앱을 업데이트하지 않은 사용자가 전부 접속 불가가 된다. 갱신 계획이 앱 배포 주기보다 짧을 수 없다는 제약을 처음부터 고려해야 한다.

## 바로 확인하기

목록에 있는 모든 이름의 남은 일수를 한 번에 뽑는다. 자산 목록 밖의 이름은 인증서 투명성 로그로 찾는다.

\`\`\`bash
while read -r host; do
  end=$(echo | openssl s_client -connect "$host:443" -servername "$host" 2>/dev/null \\
        | openssl x509 -noout -enddate | cut -d= -f2)
  [ -z "$end" ] && { echo "$host 확인 실패"; continue; }
  left=$(( ( $(date -j -f '%b %d %T %Y %Z' "$end" +%s) - $(date +%s) ) / 86400 ))
  printf '%-32s %4d일 남음\\n' "$host" "$left"
done < hosts.txt | sort -k2 -n | head -20
\`\`\`

내부 인증서는 발급 기록에서 뽑는 편이 정확하다. 접속이 안 되는 자산도 목록에 남아야 한다.

## 실제로 이렇게 터진다

자동 갱신을 걸었는데 서비스가 멈춘 사례가 있다. 파일은 새것으로 바뀌었지만 프로세스가 재적재하지 않아 옛 인증서를 계속 들고 있었다. 만료 당일까지 아무도 몰랐다.

내부 서비스에서 더 자주 생긴다. 외부 도메인은 감시 도구가 있는데, 내부 상호 인증용 인증서는 목록에도 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 자동 갱신이면 안심이다 | 재적재까지 확인해야 한다 |
| 만료일만 보면 된다 | 중간 인증서도 만료된다 |
| 외부 도메인만 관리하면 된다 | 내부 인증서가 더 많다 |
| 발급되면 서비스된다 | 배포와 적재가 남는다 |
| 감시 도구가 잡아 준다 | 목록에 없으면 못 잡는다 |

## 무엇을 감시하는가

| 항목 | 확인 방법 |
| --- | --- |
| 남은 기간 | 소켓에서 직접 조회 |
| 실제 제공 인증서 | 파일이 아니라 응답 |
| 체인 완전성 | 중간 인증서 포함 |
| 갱신 후 지문 변화 | 재적재 확인 |
| 대상 목록 | 인증서 로그로 보완 |

핵심은 파일이 아니라 소켓에서 확인하는 것이다. 갱신 성공을 성공 조건으로 삼으면 재적재 누락을 놓친다.

## 자동화에 무엇을 넣는가

1. 발급 — 자동
2. 배포 — 대상 서버·로드밸런서에 반영
3. 재적재 — 프로세스에 신호 전달
4. 검증 — 소켓에서 새 지문 확인
5. 실패 시 경보 — 갱신 실패와 적재 실패를 구분
6. 만료 임박 경보 — 30·14·7일 단계

4번이 있어야 자동화가 완성된다.

\`\`\`bash
# 실제 서비스 중인 인증서의 만료와 지문 — 목록 전체를 훑는다
while read -r h; do
  info=$(echo | timeout 5 openssl s_client -connect "$h:443" -servername "$h" 2>/dev/null |
         openssl x509 -noout -enddate -fingerprint -sha256 2>/dev/null)
  [ -z "$info" ] && { printf '%-32s 연결 실패\n' "$h"; continue; }
  printf '%-32s %s\n' "$h" "$(echo "$info" | tr '\n' ' ')"
done < hosts.txt
\`\`\`

## 참고

- RFC 8555 — Automatic Certificate Management Environment (ACME)
- CA/Browser Forum Baseline Requirements, 인증서 수명
- OWASP Cheat Sheet — Transport Layer Security`,
    diagram: {
      type: 'steps',
      caption: '만료 사고를 없애는 절차',
      steps: [
        { label: '인증서 목록화', note: '외부·내부·클라이언트 모두' },
        { label: '발급 자동화', note: '짧은 수명으로 강제' },
        { label: '갱신 실패 경보', note: '만료 경보보다 앞선 신호' },
        { label: '외부 감시', note: '자동화 밖 자산 확인' },
      ],
    },
  },
  {
    slug: 'crypto-agility',
    title: '암호 알고리즘 교체를 전제한 설계',
    body: `암호 알고리즘은 언젠가 바뀐다. 취약점이 발견되거나 규정이 바뀌거나 양자 컴퓨팅 대비 전환이 필요해진다. 문제는 바꿔야 할 때가 되면 어디에 무엇이 쓰였는지 아무도 모른다는 것이다. 교체를 전제한 설계란 알고리즘 이름을 데이터에 함께 저장하고, 여러 버전을 동시에 지원하며, 목록으로 사용 현황을 파악할 수 있게 만드는 것이다.

## 왜 교체가 어려운가?

암호문에 어떤 알고리즘과 어떤 키로 만들어졌는지 표시가 없으면, 교체하려면 모든 데이터를 다시 만들어야 한다. 저장된 값의 형식이 코드에 암묵적으로 박혀 있으면 새 형식을 도입하는 순간 기존 데이터를 읽을 수 없게 된다.

![버전 표시가 있는 암호문과 없는 암호문](/img/posts/crypto-agility.svg)

## 저장 형식에 버전을 넣는다

암호문 앞에 짧은 접두어로 버전과 키 식별자를 붙인다. 그러면 읽을 때 어떤 방식으로 복호화할지 스스로 알 수 있고, 새 방식은 새 접두어로 추가만 하면 된다.

\`\`\`
v1:kid-2024a:<nonce>:<암호문>     예전 방식 — 읽기만
v2:kid-2026b:<nonce>:<암호문>     현재 방식 — 읽기와 쓰기
\`\`\`

읽기는 모든 버전을 지원하고 쓰기는 최신 버전만 쓴다. 접근할 때마다 최신 버전으로 다시 쓰면, 재암호화 배치 없이도 시간이 지나면서 자연스럽게 이전이 끝난다.

## 교체 대상 목록

| 영역 | 확인 항목 |
| --- | --- |
| 저장 암호화 | 알고리즘, 키 길이, 모드 |
| 비밀번호 해시 | 알고리즘과 작업 계수 |
| 전송 구간 | TLS 버전과 암호 스위트 |
| 서명 | 서명 알고리즘, 인증서 키 유형 |
| 토큰 | JWT 서명 알고리즘 |
| 난수 | 생성기 종류 |
| 무결성 | 해시 함수 |

## 비밀번호 해시는 로그인 시점에 올린다

작업 계수를 올리거나 알고리즘을 바꿀 때, 로그인 성공 시점에 입력받은 평문으로 새 해시를 만들어 교체한다. 사용자는 아무것도 느끼지 않고, 활성 사용자부터 순차적으로 이전된다.

## 양자 대비 전환은 목록 작업부터

전환 계획의 첫 단계는 새 알고리즘을 고르는 것이 아니라 우리가 어디서 어떤 공개키 암호를 쓰는지 목록을 만드는 것이다. 오래 보관되는 데이터와 장기 유효 서명이 우선순위가 높다.

## 바로 확인하기

저장된 값과 설정에서 현재 쓰이는 알고리즘 분포를 뽑는다. 분포를 알면 교체 계획이 나온다.

\`\`\`sql
-- 비밀번호 해시 알고리즘 분포
SELECT split_part(password_hash, '$', 2) AS algo, count(*)
  FROM users GROUP BY 1 ORDER BY 2 DESC;

-- 암호문 버전 분포 — 남은 이전 대상 규모
SELECT split_part(ssn_enc, ':', 1) AS ver, count(*)
  FROM users GROUP BY 1 ORDER BY 1;
\`\`\`

전송 구간은 외부에서 본다.

\`\`\`bash
nmap --script ssl-enum-ciphers -p 443 app.example.com | grep -E 'TLSv|ciphers|least strength'
\`\`\`

## 실제로 이렇게 터진다

알고리즘을 바꿔야 하는 상황이 왔는데 코드 200곳을 고쳐야 했던 사례가 있다. 암호화 호출이 여기저기 흩어져 있었고, 저장된 데이터에 어떤 알고리즘을 썼는지 표시가 없었다. 결국 전부 다시 암호화하는 대규모 작업이 됐다.

라이브러리 기본값에 의존한 경우도 있다. 라이브러리를 올렸더니 기본 알고리즘이 바뀌어 옛 데이터를 못 읽게 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 지금 알고리즘이면 충분하다 | 언젠가 반드시 바뀐다 |
| 바꿀 때 생각하면 된다 | 그때는 시간이 없다 |
| 라이브러리가 알아서 한다 | 기본값이 바뀌면 깨진다 |
| 데이터를 전부 다시 암호화한다 | 봉투 구조면 필요 없다 |
| 표준이 나오면 그때 옮긴다 | 준비가 안 되면 몇 년 걸린다 |

## 무엇을 준비하는가

| 항목 | 없으면 |
| --- | --- |
| 알고리즘 식별자 저장 | 무엇으로 암호화했는지 모른다 |
| 키 버전 저장 | 어떤 키인지 모른다 |
| 암호화 호출 집중 | 고칠 곳이 흩어진다 |
| 복호는 여러 버전 지원 | 전환 중 읽기가 깨진다 |
| 재암호화 배치 | 옮길 수단이 없다 |
| 사용 현황 지표 | 진행률을 모른다 |

## 저장 형식을 어떻게 잡는가

값 안에 메타데이터를 함께 넣으면 나중에 판단할 수 있다.

\`\`\`
v1:aes-256-gcm:k3:<nonce>:<ciphertext>
 |     |        |
 |     |        +-- 키 버전
 |     +----------- 알고리즘
 +----------------- 형식 버전
\`\`\`

복호는 접두사를 보고 분기하고, 암호화는 항상 최신 형식으로 한다. 그러면 읽기는 옛것을 지원하면서 새로 쓰는 것은 자동으로 옮겨 간다.

## 옮겨 가는 절차

1. 새 알고리즘으로 쓸 수 있게 하고, 옛것은 읽기만 지원한다
2. 접근이 잦은 데이터는 읽을 때 다시 써서 자동으로 옮긴다
3. 남은 것은 배치로 옮긴다 — 부하가 적은 시간에
4. 옛 형식 비율을 지표로 본다
5. 0이 되면 옛 복호 코드를 제거한다

\`\`\`sql
-- 형식별 분포 — 이관 진행률
SELECT split_part(encode(payload, 'escape'), ':', 1) AS ver, count(*)
FROM encrypted_blobs GROUP BY 1 ORDER BY 2 DESC;
\`\`\`

## 참고

- NIST SP 800-131A, 암호 알고리즘 전환 권고
- NIST Post-Quantum Cryptography 표준 문서
- OWASP Cheat Sheet — Cryptographic Storage, 알고리즘 선택`,
    diagram: {
      type: 'flow',
      caption: '버전 표시를 이용한 무중단 교체',
      steps: [
        { label: '버전 접두어 도입', note: '기존 값은 v1 로 간주' },
        { label: '새 방식 추가', note: '쓰기는 v2, 읽기는 둘 다' },
        { label: '접근 시 재작성', note: '자연 이전' },
        { label: '잔여 배치 처리', note: '접근 없는 데이터만' },
      ],
    },
  },
]
