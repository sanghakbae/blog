import type { SeedPost } from './types'

/** 670~679 — 데이터베이스 다루기 */
export const posts68: SeedPost[] = [
  {
    slug: 'db-account-separation',
    title: '데이터베이스 계정을 나누는 기준',
    body: `애플리케이션 하나에 계정 하나를 쓰고 그 계정이 모든 권한을 갖는 구성이 흔하다. 코드 어디에서든 무엇이든 할 수 있다는 뜻이고, 삽입 취약점 하나의 영향이 그만큼 커진다.

## 어떻게 나누나

| 용도 | 권한 |
| --- | --- |
| 일반 조회·수정 | 읽기·쓰기 |
| 통계·보고 | 읽기 전용 |
| 배치 작업 | 해당 테이블만 |
| 스키마 변경 | 배포 시에만 |
| 관리 | 사람이 직접 |

읽기 전용 계정을 따로 두는 것만으로 많은 부분이 닫힌다. 보고서나 분석 코드에서 실수로 쓰는 일이 사라진다.

![계정 분리의 효과](/img/posts/db-account-separation.svg)

## 스키마 변경 권한

| 구성 | 위험 |
| --- | --- |
| 운영 계정이 보유 | 삽입 시 테이블 삭제 |
| 배포 전용 계정 | 배포 중에만 |
| 사람이 수행 | 기록 남음 |

운영 계정에 테이블을 지울 권한이 있으면 삽입 취약점의 피해가 데이터 삭제까지 간다. 배포용 계정을 따로 두면 그 자리가 닫힌다.

## 조회 상한을 둔다

| 항목 | 설정 |
| --- | --- |
| 결과 행 수 | 상한 |
| 실행 시간 | 제한 |
| 동시 연결 | 제한 |
| 대량 조회 | 기록 |

한 번에 수백만 행을 가져가는 조회는 정상 기능에서는 드물다. 상한을 두면 대량 반출이 어려워지고 실수로 인한 부하도 줄어든다.

![권한별 가능한 동작](/img/posts/db-account-separation-2.svg)

## 지금 권한을 본다

\`\`\`bash
# 계정별 권한 — 과도한 것이 있는지
psql -At -c "select grantee, privilege_type, count(*)
  from information_schema.role_table_grants
  group by 1,2 order by 3 desc" 2>/dev/null | head -20

# 상위 권한을 가진 역할
psql -At -c "select rolname from pg_roles where rolsuper or rolcreaterole" 2>/dev/null
\`\`\`

애플리케이션 계정이 상위 권한 목록에 나오면 즉시 검토 대상이다. 대개 초기 구축 때 편의로 준 뒤 그대로 남은 것이다.

## 나누는 순서

한 번에 나누면 무엇이 깨질지 몰라 시작하지 못한다. 읽기 전용 계정을 먼저 만들어 보고용 코드부터 옮기고, 그다음 배치 작업을 분리한다. 각 단계에서 권한 오류가 나오는지 관찰하면 실제로 필요한 권한이 드러난다. 운영 계정의 권한을 줄이는 것은 마지막이다.

## 참고

- OWASP — 데이터베이스 보안 지침
- CIS Benchmarks — 데이터베이스 설정
- NIST SP 800-53 — 최소 권한`,
    diagram: {
      type: 'matrix',
      caption: '계정 분리의 효과',
      x: ['계정 하나', '용도별 분리'],
      y: ['삽입 취약점', '정상'],
      cells: ['전체 권한', '해당 권한만', '해당 없음', '해당 없음'],
    },
    diagram2: {
      type: 'layers',
      caption: '권한별 가능한 동작',
      layers: [
        { label: '관리', note: '사람이 직접' },
        { label: '스키마 변경', note: '배포 시만' },
        { label: '읽기·쓰기', note: '일반 기능' },
        { label: '읽기 전용', note: '보고·분석' },
      ],
    },
  },
  {
    slug: 'query-parameterization',
    title: '질의를 문자열로 만들지 않는 법',
    body: `SQL 인젝션은 오래된 문제인데도 계속 나온다. 원인은 대부분 값을 문자열로 이어 붙여 질의를 만든 자리 하나다.

## SQL 인젝션은 어디서 생기나

| 상황 | 이유 |
| --- | --- |
| 동적 조건 | 조건 개수가 변함 |
| 정렬 컬럼 | 값이 아닌 식별자 |
| 테이블명 변수 | 구조가 동적 |
| 목록 조건 | 개수가 가변 |
| 급한 수정 | 임시 코드 |

정렬 컬럼이나 테이블명은 매개변수로 넘길 수 없어서 이어 붙이게 된다. 여기서 문제가 생긴다.

![문자열 결합이 남는 자리](/img/posts/query-parameterization.svg)

## 식별자를 다루는 법

| 방법 | 내용 |
| --- | --- |
| 허용 목록 대조 | 정해진 값만 |
| 매핑 테이블 | 입력을 이름으로 |
| 인용 함수 | 라이브러리 제공 |
| 그대로 삽입 | 위험 |

정렬 컬럼은 허용 목록으로 처리한다. 사용자가 보낸 값을 그대로 쓰는 대신 미리 정한 목록에서 고르게 한다.

## 목록 조건 처리

| 방법 | 내용 |
| --- | --- |
| 배열 매개변수 | 드라이버 지원 |
| 자리표시자 생성 | 개수만큼 |
| 임시 테이블 | 대량인 경우 |
| 문자열 결합 | 피함 |

자리표시자를 개수만큼 만들어 넘기는 방식이 일반적이다. 개수에 상한을 두지 않으면 매우 긴 질의가 만들어질 수 있다.

![안전한 처리 순서](/img/posts/query-parameterization-2.svg)

## 결합 지점을 찾는다

\`\`\`bash
# 질의 문자열 안에 변수가 들어가는 지점
grep -rInE "(SELECT|INSERT|UPDATE|DELETE).*(\\+|\\\$\\{|%s|format\\()" \
  --include='*.ts' --include='*.py' --include='*.java' src/ 2>/dev/null | head -20
\`\`\`

나온 목록을 하나씩 보면 대부분은 상수 결합이라 문제가 없다. 사용자 입력이 닿는 몇 개만 골라 고치면 된다.

## 도구를 함께 쓴다

정적 분석 도구가 이런 자리를 잘 찾는다. 완전하지는 않지만 사람이 놓치는 것을 보완하고, 새로 들어오는 코드에서 같은 패턴이 생기는 것을 막는다. 검사 결과를 리뷰 단계에 붙여 두면 고치는 비용이 가장 작은 시점에 걸린다. 데이터베이스 쪽에서도 방어를 둘 수 있다. 오류 메시지를 밖으로 그대로 내보내지 않으면 공격자가 구조를 알아내는 속도가 크게 느려진다. 막는 것은 아니지만 시도를 어렵게 만든다.

## 참고

- OWASP — SQL Injection Prevention Cheat Sheet
- CWE-89 — SQL 명령 삽입
- 각 데이터베이스 드라이버 문서`,
    diagram: {
      type: 'flow',
      caption: '문자열 결합이 남는 자리',
      steps: [
        { label: '값 매개변수화', note: '대부분 처리됨' },
        { label: '식별자 결합', note: '정렬·테이블명' },
        { label: '목록 조건', note: '개수 가변' },
        { label: '급한 수정', note: '검토 없이', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 처리 순서',
      steps: [
        { label: '값은 매개변수', note: '기본' },
        { label: '식별자는 목록', note: '대조' },
        { label: '목록은 자리표시자', note: '개수만큼' },
        { label: '상한 설정', note: '길이 제한' },
      ],
    },
  },
  {
    slug: 'db-encryption-layers',
    title: '데이터베이스 암호화의 층 나누기',
    body: `데이터베이스 암호화라고 하면 하나를 떠올리지만 실제로는 여러 층이 있고 각각 막는 것이 다르다. 무엇을 막으려는지 정하지 않으면 엉뚱한 것을 적용하게 된다.

## 어떤 층이 있나

| 층 | 막는 것 |
| --- | --- |
| 저장 장치 암호화 | 물리적 탈취 |
| 데이터 파일 암호화 | 파일 복사 |
| 컬럼 암호화 | 권한 있는 조회 |
| 애플리케이션 암호화 | 데이터베이스 관리자 |
| 전송 암호화 | 도청 |

저장 장치 암호화는 서버가 켜져 있는 동안에는 아무것도 막지 않는다. 디스크를 뽑아 가는 경우에만 효과가 있다.

![암호화 층과 효과 범위](/img/posts/db-encryption-layers.svg)

## 무엇을 막고 싶은가

| 위협 | 필요한 층 |
| --- | --- |
| 장비 분실 | 저장 장치 |
| 백업 유출 | 파일·백업 |
| 내부 조회 | 컬럼 이상 |
| 관리자 접근 | 애플리케이션 |
| 네트워크 도청 | 전송 |

내부자의 조회를 막으려면 컬럼 이상의 층이 필요하다. 저장 장치 암호화로는 조회 화면에 그대로 나온다.

## 컬럼 암호화의 대가

| 항목 | 영향 |
| --- | --- |
| 검색 | 어려워짐 |
| 정렬 | 불가 |
| 색인 | 제한 |
| 성능 | 저하 |
| 키 관리 | 부담 |

암호화한 컬럼은 부분 검색이 되지 않는다. 검색이 필요하면 별도 색인 값을 함께 두는 설계가 필요하다.

![층별 대가와 효과](/img/posts/db-encryption-layers-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 전송 암호화 여부
psql -At -c "select ssl, version from pg_stat_ssl
  join pg_stat_activity using (pid) limit 5" 2>/dev/null

# 저장 암호화 설정 — 클라우드 관리형인 경우
aws rds describe-db-instances \
  --query 'DBInstances[].[DBInstanceIdentifier,StorageEncrypted]' --output text 2>/dev/null
\`\`\`

전송 암호화가 꺼져 있으면 내부망이라도 켜는 것이 좋다. 설정 한 줄이고 성능 영향이 크지 않다.

## 키를 어디에 두나

암호화를 적용해도 키가 같은 서버에 평문으로 있으면 효과가 크게 줄어든다. 별도 키 관리 서비스에 두고 필요할 때 가져오는 구성이 기본이다. 키 회전 절차와 과거 데이터 재암호화 방법까지 정해 두지 않으면 몇 년 뒤에 손대지 못하는 상태가 된다. 규제에서 요구하는 수준도 확인한다. 어떤 항목을 어떤 방식으로 암호화해야 하는지 기준이 있는 경우가 있고, 그 기준을 모르고 자체 판단으로 적용하면 나중에 다시 해야 한다.

## 참고

- NIST SP 800-111 — 저장 데이터 암호화
- 개인정보보호법 — 암호화 조치 기준
- 각 데이터베이스 암호화 문서`,
    diagram: {
      type: 'layers',
      caption: '암호화 층과 효과 범위',
      layers: [
        { label: '애플리케이션', note: '관리자도 못 봄' },
        { label: '컬럼', note: '권한 조회 차단' },
        { label: '데이터 파일', note: '파일 복사' },
        { label: '저장 장치', note: '물리 탈취만' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '층별 운영 부담',
      unit: '점',
      items: [
        { label: '저장 장치', value: 10, note: '거의 없음' },
        { label: '데이터 파일', value: 25, note: '설정' },
        { label: '컬럼', value: 70, note: '검색 제약' },
        { label: '애플리케이션', value: 90, note: '설계 변경' },
      ],
    },
  },
  {
    slug: 'db-access-audit',
    title: '누가 데이터를 조회했는지 남기기',
    body: `데이터 유출 사고에서 가장 자주 나오는 답은 모른다는 것이다. 조회 기록이 없으면 누가 무엇을 봤는지 영영 알 수 없다.

## 무엇을 남기나

| 항목 | 내용 |
| --- | --- |
| 계정 | 실제 사람 |
| 시각 | 조회 시점 |
| 질의 | 무엇을 |
| 결과 건수 | 얼마나 |
| 출발지 | 어디서 |

결과 건수가 빠지면 범위를 알 수 없다. 같은 질의라도 조건에 따라 한 건일 수도 백만 건일 수도 있다.

![조회 기록의 구성](/img/posts/db-access-audit.svg)

## 조회 이력과 공유 계정 문제

| 구성 | 추적 |
| --- | --- |
| 개인 계정 | 가능 |
| 공유 계정 | 불가 |
| 애플리케이션 경유 | 앱 로그 필요 |
| 직접 접속 | 데이터베이스 로그 |

애플리케이션을 통한 조회는 데이터베이스에서 모두 같은 계정으로 보인다. 앱 쪽에서 실제 사용자를 함께 남겨야 연결된다.

## 어디까지 남기나

| 대상 | 수준 |
| --- | --- |
| 개인정보 테이블 | 전부 |
| 일반 테이블 | 변경만 |
| 관리자 접속 | 전부 |
| 배치 작업 | 요약 |

전부 남기면 양이 감당되지 않는다. 민감한 테이블과 사람의 직접 접속에 집중하는 것이 현실적이다.

![기록 대상의 우선순위](/img/posts/db-access-audit-2.svg)

## 지금 설정을 본다

\`\`\`bash
# 로깅 설정 — 무엇이 기록되는지
psql -At -c "select name, setting from pg_settings
  where name in ('log_statement','log_min_duration_statement','log_connections')" 2>/dev/null

# 직접 접속한 사람 계정
psql -At -c "select usename, client_addr, count(*) from pg_stat_activity
  group by 1,2 order by 3 desc" 2>/dev/null | head
\`\`\`

사람 계정으로 직접 접속한 기록이 많으면 그 경로를 먼저 정리한다. 중계 도구를 거치게 하면 기록과 통제가 함께 붙는다.

## 기록을 실제로 본다

기록을 쌓기만 하고 보지 않으면 사고 뒤에만 쓰인다. 대량 조회나 평소와 다른 시간대 접속처럼 몇 가지 조건에 경보를 걸어 두면 사전 탐지로 쓸 수 있다. 처음에는 오탐이 많지만 몇 주 조정하면 볼 만한 수준으로 줄어든다. 기록의 보관 기간도 정한다. 법에서 요구하는 기간이 있는 경우 그보다 짧으면 안 되고, 너무 길면 비용과 관리 부담이 커진다. 요구 기간에 여유를 조금 더한 값이 보통 적절하다.

## 참고

- 개인정보보호법 — 접속기록 보관
- 개인정보보호위원회 — 안전성 확보조치 기준
- NIST SP 800-92 — 로그 관리`,
    diagram: {
      type: 'layers',
      caption: '조회 기록의 구성',
      layers: [
        { label: '실제 사용자', note: '앱에서 전달' },
        { label: '질의 내용', note: '무엇을' },
        { label: '결과 건수', note: '범위' },
        { label: '출발지·시각', note: '맥락' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '기록 대상의 우선순위',
      unit: '점',
      items: [
        { label: '개인정보 조회', value: 95, note: '전부' },
        { label: '관리자 접속', value: 90, note: '전부' },
        { label: '일반 변경', value: 60, note: '변경만' },
        { label: '일반 조회', value: 20, note: '양 과다' },
      ],
    },
  },
  {
    slug: 'db-connection-security',
    title: '애플리케이션과 데이터베이스 사이',
    body: `연결 설정은 한 번 만들고 잊는 부분이지만, 여기에 비밀번호가 평문으로 남거나 암호화 없이 연결되는 경우가 많다.

## 무엇을 확인하나

| 항목 | 내용 |
| --- | --- |
| 전송 암호화 | 켜짐 여부 |
| 인증서 검증 | 서버 확인 |
| 자격 증명 위치 | 코드 밖 |
| 연결 수 제한 | 자원 보호 |
| 유휴 종료 | 방치 연결 |

인증서 검증을 끄고 암호화만 켜면 중간자에 그대로 노출된다. 암호화와 검증은 함께 가야 한다.

![연결 구간의 확인 항목](/img/posts/db-connection-security.svg)

## 자격 증명을 어디에 두나

| 위치 | 평가 |
| --- | --- |
| 코드 안 | 최악 |
| 설정 파일 | 저장소에 올라감 |
| 환경 변수 | 프로세스 목록 노출 |
| 시크릿 저장소 | 권장 |
| 임시 자격 증명 | 가장 나음 |

클라우드에서는 데이터베이스가 역할 기반 인증을 지원하는 경우가 있다. 비밀번호 자체가 없어지므로 유출될 것도 없다.

## 연결 풀 설정

| 항목 | 영향 |
| --- | --- |
| 최대 연결 수 | 데이터베이스 보호 |
| 유휴 시간 | 자원 회수 |
| 검증 질의 | 끊긴 연결 |
| 대기 시간 | 장애 전파 방지 |

연결 수 상한이 없으면 트래픽이 몰릴 때 데이터베이스가 먼저 쓰러진다. 애플리케이션이 느려지는 편이 전체 장애보다 낫다.

![자격 증명 보관 방식](/img/posts/db-connection-security-2.svg)

## 지금 연결을 본다

\`\`\`bash
# 암호화되지 않은 연결이 있는지
psql -At -c "select count(*) from pg_stat_ssl where ssl = false" 2>/dev/null

# 연결 문자열이 코드나 설정에 평문으로 있는지
grep -rInE '(postgres|mysql)://[^:]+:[^@]+@' --include='*.ts' --include='*.yml' . 2>/dev/null \
  | grep -v node_modules | head
\`\`\`

두 번째 명령에서 무언가 나오면 그 값은 이미 노출된 것으로 보고 회전한다. 저장소 이력에도 남아 있으므로 지우는 것만으로는 부족하다.

## 접속 경로를 좁힌다

데이터베이스가 애플리케이션 서버에서만 접근되게 하면 다른 경로가 닫힌다. 방화벽 규칙과 네트워크 구간을 확인하고, 관리 목적의 접속은 중계 서버를 거치게 한다. 개발자 노트북에서 운영 데이터베이스에 직접 붙는 경로가 열려 있는 경우가 흔하고, 그 경로가 가장 통제하기 어렵다.

## 참고

- OWASP — 데이터베이스 보안
- CIS Benchmarks — 데이터베이스 설정
- 각 데이터베이스 연결 보안 문서`,
    diagram: {
      type: 'layers',
      caption: '연결 구간의 확인 항목',
      layers: [
        { label: '전송 암호화', note: '켜짐' },
        { label: '인증서 검증', note: '함께 필요' },
        { label: '자격 증명', note: '코드 밖' },
        { label: '연결 제한', note: '자원 보호' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '자격 증명 보관 방식',
      unit: '점',
      items: [
        { label: '임시 자격 증명', value: 95, note: '비밀번호 없음' },
        { label: '시크릿 저장소', value: 85, note: '권장' },
        { label: '환경 변수', value: 50, note: '목록 노출' },
        { label: '코드 안', value: 5, note: '최악' },
      ],
    },
  },
  {
    slug: 'nosql-injection',
    title: '문서형 데이터베이스에서의 삽입',
    body: `SQL을 쓰지 않으면 삽입 문제가 없다고 생각하기 쉽다. 질의를 객체로 구성하는 데이터베이스에도 같은 종류의 문제가 있다.

## 어떻게 생기나

| 원인 | 설명 |
| --- | --- |
| 객체를 그대로 전달 | 연산자 삽입 |
| 문자열 질의 | 코드 실행 |
| 형 변환 부재 | 배열·객체 통과 |
| 조건 결합 | 논리 조작 |

사용자가 보낸 값이 문자열일 것이라 가정하고 그대로 조건에 넣으면, 객체가 들어와 연산자로 해석된다.

![객체 삽입이 생기는 흐름](/img/posts/nosql-injection.svg)

## 인젝션으로 무슨 일이 일어나나

| 결과 | 설명 |
| --- | --- |
| 인증 우회 | 항상 참인 조건 |
| 조건 무력화 | 필터 제거 |
| 전체 조회 | 범위 확장 |
| 코드 실행 | 서버 측 스크립트 |

로그인에서 비밀번호 비교가 항상 참이 되는 형태가 대표적이다. 비밀번호를 몰라도 통과한다.

## 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 형 검사 | 문자열인지 확인 |
| 스키마 검증 | 구조 고정 |
| 연산자 차단 | 특정 접두사 |
| 서버 측 스크립트 금지 | 설정 |

입력 스키마를 정의하고 그에 맞는지 검사하면 대부분 막힌다. 형이 다르면 거절하는 것이 기본이다.

![막는 방법](/img/posts/nosql-injection-2.svg)

## 지금 코드를 본다

\`\`\`bash
# 요청 본문을 그대로 질의 조건에 넣는 지점
grep -rInE 'find\\(\\s*(req\\.(body|query|params))' \
  --include='*.ts' --include='*.js' src/ 2>/dev/null | head -20

# 서버 측 스크립트 실행 설정
grep -rIn 'javascriptEnabled\\|\\$where' --include='*.ts' src/ 2>/dev/null | head
\`\`\`

요청 객체가 질의에 그대로 들어가는 자리가 있으면 우선 확인한다. 스키마 검증 계층을 앞에 두는 것으로 대부분 해결된다.

## 검증을 한곳에 모은다

각 처리 함수마다 검사를 넣으면 새로 생기는 곳에서 빠진다. 요청이 들어오는 지점에서 스키마 검증을 일괄 적용하고, 검증을 통과한 값만 아래로 내려보내는 구조가 유지하기 쉽다. 이 구조는 삽입만이 아니라 잘못된 입력 전반을 걸러 준다. 집계 파이프라인처럼 복잡한 질의를 쓰는 곳도 확인한다. 단계마다 사용자 입력이 들어갈 수 있고, 그중 하나만 검증이 빠져도 전체가 뚫린다. 입력이 닿는 단계를 목록으로 만들어 두는 편이 낫다.

## 참고

- OWASP — NoSQL Injection
- CWE-943 — 데이터 질의 논리의 부적절한 중화
- 각 데이터베이스 보안 문서`,
    diagram: {
      type: 'flow',
      caption: '객체 삽입이 생기는 흐름',
      steps: [
        { label: '요청 본문', note: '객체 가능' },
        { label: '형 검사 없음', note: '가정만' },
        { label: '질의 조건에 삽입', note: '연산자 해석' },
        { label: '인증 우회', note: '항상 참', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '막는 방법',
      steps: [
        { label: '스키마 정의', note: '구조 고정' },
        { label: '입구에서 검증', note: '일괄' },
        { label: '형 강제', note: '문자열만' },
        { label: '스크립트 금지', note: '설정' },
      ],
    },
  },
  {
    slug: 'db-migration-safety',
    title: '스키마 변경을 안전하게 배포하기',
    body: `테이블 구조를 바꾸는 배포는 되돌리기 어렵고 서비스가 멈출 수 있다. 순서와 방식에 규칙을 두면 대부분의 사고를 피할 수 있다.

## 무엇이 위험한가

| 변경 | 위험 |
| --- | --- |
| 컬럼 삭제 | 옛 코드가 참조 |
| 이름 변경 | 삭제와 추가 |
| 형 변경 | 잠금·데이터 손실 |
| 색인 추가 | 잠금 |
| 제약 추가 | 기존 데이터 위반 |

컬럼 삭제가 가장 흔한 사고 원인이다. 배포 중에는 옛 코드와 새 코드가 함께 돌기 때문이다.

![변경과 배포의 겹침](/img/posts/db-migration-safety.svg)

## 파이프라인 단계를 나눈다

| 단계 | 내용 |
| --- | --- |
| 추가 | 새 컬럼 |
| 양쪽 쓰기 | 두 곳 모두 |
| 읽기 전환 | 새 것으로 |
| 옛 것 제거 | 다음 배포 |

이름을 바꾸는 작업도 이 순서로 나누면 무중단으로 가능하다. 한 번에 하려 하면 반드시 겹치는 구간이 생긴다.

## 잠금을 피한다

| 항목 | 방법 |
| --- | --- |
| 색인 생성 | 동시 생성 옵션 |
| 제약 추가 | 검증 분리 |
| 대량 갱신 | 나눠서 |
| 긴 트랜잭션 | 피함 |

대량 갱신을 한 번에 돌리면 그동안 다른 작업이 대기한다. 나눠서 처리하고 사이에 여유를 두면 서비스가 멈추지 않는다.

![안전한 변경 순서](/img/posts/db-migration-safety-2.svg)

## 미리 확인한다

\`\`\`bash
# 적용 전 실행 계획과 예상 시간 — 운영 복제본에서
psql -At -c "explain (analyze false)
  alter table users add column email_verified boolean default false" 2>/dev/null

# 현재 긴 트랜잭션이 있는지
psql -At -c "select pid, now()-xact_start, query from pg_stat_activity
  where xact_start < now() - interval '1 minute'" 2>/dev/null
\`\`\`

긴 트랜잭션이 돌고 있으면 스키마 변경이 대기하고, 그 뒤의 모든 질의도 함께 대기한다. 배포 전에 확인하는 습관이 사고를 막는다.

## 되돌릴 방법을 준비한다

각 변경마다 되돌리는 절차를 함께 적어 둔다. 데이터가 사라지는 변경은 되돌릴 수 없으므로, 그런 변경은 앞뒤로 백업 시점을 명확히 한다. 되돌릴 수 없는 변경과 되돌릴 수 있는 변경을 구분해 두는 것만으로 배포 시점의 판단이 빨라진다. 변경 스크립트도 리뷰를 거치게 한다. 코드는 리뷰하면서 스키마 변경은 그냥 넘어가는 경우가 많은데, 영향 범위는 오히려 이쪽이 크다. 잠금 시간과 되돌리기 방법을 리뷰 항목으로 넣어 둔다.

## 참고

- 각 데이터베이스 무중단 변경 문서
- Google SRE — 안전한 배포
- OWASP — 변경 관리`,
    diagram: {
      type: 'flow',
      caption: '변경과 배포의 겹침',
      steps: [
        { label: '스키마 변경', note: '먼저 적용' },
        { label: '옛 코드 실행 중', note: '아직 남음' },
        { label: '참조 실패', note: '오류 발생', danger: true },
        { label: '새 코드 배포', note: '뒤늦게' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 변경 순서',
      steps: [
        { label: '추가', note: '새 컬럼' },
        { label: '양쪽 쓰기', note: '겹침 구간' },
        { label: '읽기 전환', note: '새 것으로' },
        { label: '옛 것 제거', note: '다음 배포' },
      ],
    },
  },
  {
    slug: 'db-backup-restore-test',
    title: '복구가 되는지 확인해 봤는가',
    body: `백업이 돌고 있다는 사실과 복구가 실제로 된다는 사실은 전혀 다르다. 한 번도 복구해 보지 않은 백업은 있다고 믿는 것일 뿐이고, 정말 필요한 날에야 그 사실을 알게 된다. 확인은 어렵지 않고 미루는 이유만 많다.

## 무엇이 실패하나

| 원인 | 설명 |
| --- | --- |
| 백업 자체 실패 | 경보 없음 |
| 일부 누락 | 새 테이블 |
| 손상 | 검증 없음 |
| 복구 절차 부재 | 방법 모름 |
| 시간 초과 | 너무 오래 걸림 |

백업이 실패하고 있는데 아무도 모르는 경우가 가장 흔하다. 성공만 알리고 실패는 조용한 설정이 원인이다.

![백업이 실패하는 지점](/img/posts/db-backup-restore-test.svg)

## 무엇을 재는가

| 지표 | 내용 |
| --- | --- |
| 복구 소요 시간 | 얼마나 걸리나 |
| 손실 범위 | 어디까지 되살아나나 |
| 절차 명확성 | 누구나 가능한가 |
| 의존성 | 다른 것이 필요한가 |

복구에 여덟 시간이 걸린다면 그것이 실제 목표가 된다. 희망하는 시간이 아니라 측정한 시간을 기준으로 삼는다.

## 어떻게 시험하나

| 방법 | 부담 |
| --- | --- |
| 전체 복구 | 높음 |
| 표본 테이블 | 낮음 |
| 별도 환경 | 보통 |
| 자동 검증 | 초기 구축 |

작은 규모라도 정기적으로 하는 편이 낫다. 일 년에 한 번 전체를 하는 것보다 분기마다 표본을 복구하는 쪽이 문제를 빨리 찾는다.

![시험 방식의 부담과 효과](/img/posts/db-backup-restore-test-2.svg)

## 실제로 해 본다

\`\`\`bash
# 최근 백업 파일 확인과 복구 시험 — 별도 환경에서
ls -lh /backup/ | tail -3
time pg_restore -d testdb --clean --if-exists /backup/latest.dump 2>&1 | tail -5

# 복구 후 건수 대조
psql -d testdb -At -c "select count(*) from users" 2>/dev/null
\`\`\`

복구 시간을 기록해 두면 다음 시험과 비교할 수 있다. 데이터가 늘면서 시간도 늘어나므로 추세를 보는 것이 중요하다.

## 결과를 문서로 남긴다

시험 날짜, 소요 시간, 발견한 문제, 조치 내용을 한 장으로 남긴다. 심사나 점검에서 그대로 증적이 되고, 다음 시험 때 지난번 문제가 해결됐는지 확인하는 기준이 된다. 기록이 없으면 매번 처음부터 시작하게 된다. 복구 절차서는 그 자리에 없는 사람도 따라 할 수 있게 쓴다. 사고는 담당자가 휴가일 때 나고, 평소 익숙한 사람의 머릿속에만 있는 절차는 그때 쓸 수 없다.

## 참고

- NIST SP 800-34 — 비상 계획 지침
- ISO/IEC 27002 — 백업 관리
- 각 데이터베이스 복구 문서`,
    diagram: {
      type: 'layers',
      caption: '백업이 실패하는 지점',
      layers: [
        { label: '백업 실행', note: '조용한 실패' },
        { label: '범위', note: '새 테이블 누락' },
        { label: '무결성', note: '손상 미검증' },
        { label: '복구 절차', note: '방법 모름' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '시험 방식의 부담과 효과',
      x: ['부담 낮음', '부담 높음'],
      y: ['자주', '드물게'],
      cells: ['표본 복구', '전체 복구', '효과 적음', '연 1회'],
    },
  },
  {
    slug: 'read-replica-exposure',
    title: '읽기 복제본에서 데이터가 새는 경로',
    body: `읽기 부하를 나누려고 복제본을 두면 데이터가 한 벌 더 생긴다. 운영 데이터베이스에 건 통제가 복제본에도 걸려 있는지는 별개의 문제다.

## 데이터베이스 접근 통제가 어디가 다른가

| 항목 | 차이 |
| --- | --- |
| 접근 권한 | 별도 설정 |
| 네트워크 | 다른 구간일 수 있음 |
| 감사 로그 | 켜지 않은 경우 |
| 암호화 | 별도 확인 |
| 백업 | 또 다른 사본 |

복제본에 감사 로그를 켜지 않으면 그쪽 조회는 기록되지 않는다. 조회 목적으로 만든 것이니 대부분의 조회가 그쪽에서 일어난다.

![복제본이 만드는 사본](/img/posts/read-replica-exposure.svg)

## 왜 통제가 느슨해지나

| 이유 | 설명 |
| --- | --- |
| 읽기 전용이라 안심 | 유출은 읽기로 |
| 나중에 추가 | 초기 설정 미적용 |
| 분석팀 전용 | 권한 넓게 |
| 다른 계정 | 조직 정책 밖 |

읽기 전용이라 안전하다는 생각이 가장 큰 문제다. 데이터 유출은 쓰기가 아니라 읽기로 일어난다.

## 무엇을 맞추나

| 항목 | 내용 |
| --- | --- |
| 계정과 권한 | 동일 기준 |
| 감사 로그 | 켜기 |
| 네트워크 | 같은 제한 |
| 마스킹 | 필요 시 적용 |
| 보관 | 같은 기준 |

분석용이라면 아예 마스킹된 복제본을 따로 만드는 방법도 있다. 원본이 필요 없는 분석이 대부분이다.

![통제를 맞추는 항목](/img/posts/read-replica-exposure-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 복제본 목록과 설정
aws rds describe-db-instances \
  --query 'DBInstances[?ReadReplicaSourceDBInstanceIdentifier!=null].[DBInstanceIdentifier,PubliclyAccessible,StorageEncrypted]' \
  --output text 2>/dev/null

# 복제본에 접속하는 계정
psql -h replica -At -c "select usename, client_addr, count(*)
  from pg_stat_activity group by 1,2" 2>/dev/null | head
\`\`\`

공개 접근이 켜져 있는 복제본이 나오면 즉시 처리한다. 원본은 막혀 있는데 복제본이 열려 있는 구성이 실제로 발견된다.

## 목록을 관리한다

복제본은 필요할 때 만들고 잊기 쉽다. 어떤 복제본이 왜 있는지 목록으로 관리하고, 쓰이지 않는 것은 정리한다. 데이터 사본의 개수를 줄이는 것이 가장 단순한 통제이고, 복제본 하나를 지우는 것은 통제 하나를 추가하는 것보다 쉽다. 복제 지연도 확인해 둔다. 지연이 크면 복제본에서 읽은 값이 오래된 것일 수 있고, 권한 변경이나 계정 정지가 아직 반영되지 않았을 수 있다. 보안 관련 판단을 복제본에서 하지 않는 것이 원칙이다.

## 참고

- CIS Benchmarks — 데이터베이스 설정
- 개인정보보호법 — 안전성 확보조치
- 클라우드 사업자 복제본 문서`,
    diagram: {
      type: 'flow',
      caption: '복제본이 만드는 사본',
      steps: [
        { label: '운영 데이터베이스', note: '통제 적용' },
        { label: '복제본 생성', note: '별도 설정' },
        { label: '통제 미적용', note: '기록·권한' },
        { label: '조회가 그쪽에서', note: '기록 없음', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '통제를 맞추는 항목',
      steps: [
        { label: '권한 동일', note: '같은 기준' },
        { label: '감사 로그', note: '켜기' },
        { label: '네트워크', note: '같은 제한' },
        { label: '마스킹 사본', note: '분석용' },
      ],
    },
  },
  {
    slug: 'orm-security-pitfalls',
    title: '객체 매핑 도구를 쓸 때의 함정',
    body: `객체 매핑 도구는 질의를 대신 만들어 주므로 삽입 걱정이 줄어든다. 그런데 다른 종류의 문제가 생기고, 그쪽이 더 조용히 지나간다.

## 어떤 문제가 있나

| 항목 | 설명 |
| --- | --- |
| 대량 할당 | 요청 필드가 그대로 |
| 과다 조회 | 필요 없는 컬럼 |
| 원시 질의 | 결국 문자열 |
| 조건 누락 | 소유자 확인 |
| 지연 로딩 | 예상 밖 질의 |

요청 본문을 그대로 객체에 넣으면 권한 필드까지 바뀐다. 이것이 가장 흔한 실수다.

![대량 할당이 생기는 흐름](/img/posts/orm-security-pitfalls.svg)

## 대량 할당을 어떻게 막나

| 방법 | 내용 |
| --- | --- |
| 허용 필드 지정 | 명시적 |
| 전용 입력 형식 | 분리된 타입 |
| 읽기 전용 표시 | 변경 금지 |
| 요청 객체 직접 전달 금지 | 원칙 |

입력용 타입을 따로 두고 거기서 도메인 객체로 옮기는 방식이 확실하다. 필드가 늘어도 자동으로 열리지 않는다.

## 접근 통제 조건 누락을 막는다

| 방법 | 내용 |
| --- | --- |
| 기본 범위 적용 | 소유자 조건 |
| 저장소 계층 | 한곳에서 |
| 검사 헬퍼 | 공통 함수 |
| 시험 | 남의 자원 접근 |

식별자로만 조회하면 다른 사람의 자원이 나온다. 소유자 조건을 기본으로 붙이는 구조가 안전하다.

![안전한 구조](/img/posts/orm-security-pitfalls-2.svg)

## 코드를 훑는다

\`\`\`bash
# 요청 본문을 객체에 그대로 넣는 지점
grep -rInE '(update|create)\\(\\s*(req\\.body|\\.\\.\\.req\\.body)' \
  --include='*.ts' src/ 2>/dev/null | head -20

# 원시 질의 사용 지점
grep -rInE 'raw\\(|query\\(|createQueryBuilder' --include='*.ts' src/ 2>/dev/null | head
\`\`\`

원시 질의를 쓰는 곳은 도구의 보호를 받지 못한다. 그 목록을 따로 관리하고 매개변수를 쓰는지 확인한다.

## 생성되는 질의를 본다

도구가 어떤 질의를 만드는지 개발 중에 출력해 보면 예상과 다른 경우가 자주 발견된다. 연관 객체를 가져오면서 질의가 수백 번 도는 문제나, 필요 없는 컬럼을 전부 가져오는 문제가 여기서 드러난다. 성능 문제로 보이지만 민감한 컬럼이 불필요하게 오가는 보안 문제이기도 하다. 응답으로 나가는 필드도 함께 정리한다. 객체를 그대로 직렬화하면 화면에서 쓰지 않는 필드까지 나가고, 그 안에 내부 식별자나 처리 상태 같은 것이 섞인다. 출력용 형식을 따로 두면 이 부분이 닫힌다.

## 참고

- OWASP — 대량 할당 취약점
- CWE-915 — 동적으로 결정되는 속성의 부적절한 수정
- 각 매핑 도구 보안 문서`,
    diagram: {
      type: 'flow',
      caption: '대량 할당이 생기는 흐름',
      steps: [
        { label: '요청 본문', note: '임의 필드 포함' },
        { label: '객체에 그대로', note: '필터 없음' },
        { label: '저장', note: '권한 필드 포함' },
        { label: '권한 상승', note: '조용히', danger: true },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '안전한 구조',
      steps: [
        { label: '입력 타입 분리', note: '허용 필드만' },
        { label: '변환 계층', note: '명시적 대입' },
        { label: '소유자 조건', note: '기본 적용' },
        { label: '질의 확인', note: '생성 결과' },
      ],
    },
  },
]
