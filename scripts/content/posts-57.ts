import type { SeedPost } from './types'

/** 560~569 — 토큰과 세션의 수명 */
export const posts57: SeedPost[] = [
  {
    slug: 'jwt-key-confusion',
    title: '토큰 서명 검증에서 키가 뒤바뀌는 경우',
    body: `서명된 토큰을 검증할 때 어떤 키로 검증할지는 토큰 자체가 알려 준다. 그 말을 그대로 믿으면, 공격자가 지정한 키로 검증하게 되어 서명이 아무것도 보장하지 못한다.

## 어떻게 뒤바뀌나?

토큰 머리말에는 알고리즘과 키 식별자가 들어 있다. 둘 다 서명 대상이 아니거나, 서명 대상이어도 검증 전에 읽어야 하므로 그 시점에는 믿을 수 없다.

| 조작 | 결과 |
| --- | --- |
| 알고리즘을 없음으로 | 서명 검사 생략 |
| 비대칭을 대칭으로 | 공개 키가 비밀 키가 됨 |
| 키 식별자를 바꿈 | 다른 키로 검증 |
| 키 주소를 바꿈 | 공격자 서버의 키 사용 |

비대칭을 대칭으로 바꾸는 수법이 고전적이다. 공개 키는 누구나 아는 값이라, 그것을 대칭 키로 쓰면 공격자도 서명을 만들 수 있다.

![토큰이 검증 방법을 정하는 구조](/img/posts/jwt-key-confusion.svg)

## 어떻게 막나

검증 방법을 토큰이 아니라 우리가 정한다. 이 한 문장이 방어의 전부다.

| 항목 | 규칙 |
| --- | --- |
| 알고리즘 | 우리가 지정, 토큰 값 무시 |
| 키 | 발급자별로 미리 정함 |
| 키 식별자 | 허용 목록 안에서만 |
| 키 주소 | 고정, 토큰에서 읽지 않음 |

라이브러리에 따라 알고리즘을 지정하지 않으면 토큰 값을 따르는 것이 기본인 경우가 있다. 명시하지 않으면 안전하지 않다고 보는 편이 맞다.

## 키를 어떻게 가져오나

| 방식 | 주의 |
| --- | --- |
| 설정에 고정 | 회전이 불편 |
| 발급자 공개 목록 조회 | 주소를 고정할 것 |
| 캐시 | 만료와 갱신 실패 처리 |

공개 목록을 조회하는 방식이 흔한데, 그 주소를 토큰에서 읽으면 처음 문제로 돌아간다. 발급자별 주소를 설정에 두고 그것만 쓴다.

![검증 방법을 우리가 정한다](/img/posts/jwt-key-confusion-2.svg)

## 지금 안전한지 시험한다

알고리즘을 없음으로 바꾼 토큰을 만들어 넣어 본다.

\`\`\`bash
# 머리말을 alg=none 으로 바꾸고 서명을 비운 토큰
python3 - <<'PY'
import base64, json
def b64(d): return base64.urlsafe_b64encode(json.dumps(d).encode()).rstrip(b'=').decode()
print(b64({'alg':'none','typ':'JWT'}) + '.' + b64({'sub':'admin','exp':9999999999}) + '.')
PY
# 이 토큰으로 200 이 오면 검증이 토큰 말을 따르고 있다
\`\`\`

200 이 오면 즉시 조치 대상이다. 401 이 와도 알고리즘을 대칭으로 바꾼 토큰으로 한 번 더 시험한다 — 막는 지점이 다르다.

## 검증과 인가는 다른 일이다

서명이 맞다는 것은 그 토큰을 우리가 발급했다는 뜻일 뿐, 그 안의 주장이 지금도 유효하다는 뜻은 아니다. 권한이 회수됐거나 계정이 정지됐을 수 있다. 서명 검증을 통과한 뒤에 무엇을 더 확인할지는 별도로 정해야 한다.

## 참고

- RFC 8725 — JWT 사용 시 모범 사례
- OWASP — JWT 보안 Cheat Sheet
- CWE-347 — 서명 검증 부적절`,
    diagram: {
      type: 'flow',
      caption: '토큰이 검증 방법을 정하는 구조',
      steps: [
        { label: '토큰 수신', note: '머리말 포함' },
        { label: '머리말에서 알고리즘 읽음', note: '아직 미검증', danger: true },
        { label: '그 방법으로 검증', note: '공격자가 정함' },
        { label: '통과', note: '서명이 무의미' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '검증 방법을 우리가 정한다',
      steps: [
        { label: '알고리즘 고정', note: '토큰 값 무시' },
        { label: '발급자별 키', note: '미리 지정' },
        { label: '키 식별자 허용 목록', note: '범위 제한' },
        { label: '키 주소 고정', note: '토큰에서 읽지 않음' },
      ],
    },
  },
  {
    slug: 'jwt-claim-validation',
    title: '서명이 맞아도 확인해야 하는 주장들',
    body: `토큰의 서명이 맞다는 것은 그것을 우리가 만들었다는 뜻이다. 그 안에 적힌 내용이 지금 이 요청에 맞는지는 별개의 확인이고, 이 확인을 빠뜨린 구현이 많다.

## 무엇을 확인하나?

| 주장 | 확인 |
| --- | --- |
| 수신자 | 우리 서비스를 가리키는가 |
| 발급자 | 우리가 아는 발급자인가 |
| 만료 | 아직 유효한가 |
| 발급 시각 | 미래가 아닌가 |
| 사용 시작 | 이미 시작됐는가 |
| 주체 | 요청 대상과 맞는가 |

수신자 확인을 빠뜨리는 경우가 가장 흔하다. 같은 발급자가 여러 서비스에 토큰을 내주는 구조라면, 다른 서비스용 토큰이 우리에게도 통한다.

![서명 통과 뒤에 남는 확인](/img/posts/jwt-claim-validation.svg)

## 인가는 그다음 일이다

주장이 형식적으로 맞더라도 지금 그 일을 할 수 있는지는 또 다른 문제다.

| 단계 | 묻는 것 |
| --- | --- |
| 서명 | 우리가 발급했나 |
| 주장 | 이 요청에 맞는 토큰인가 |
| 접근 통제 | 이 사람이 이 자원에 권한이 있나 |

토큰 안에 권한 목록을 담아 두고 그것만 보는 구현은, 권한을 회수해도 토큰이 만료될 때까지 계속 통한다. 되돌릴 수 없는 작업에는 그때그때 확인하는 편이 맞다.

## 수명을 짧게 잡는 이유

| 수명 | 회수까지 걸리는 시간 |
| --- | --- |
| 5분 | 최대 5분 |
| 1시간 | 최대 1시간 |
| 하루 | 최대 하루 |
| 없음 | 영원히 |

짧게 잡으면 갱신이 잦아 불편하다. 그래서 짧은 접근 토큰과 긴 갱신 토큰을 나누는 구조가 흔하다.

![수명과 회수 지연](/img/posts/jwt-claim-validation-2.svg)

## 지금 무엇을 확인하는지 본다

다른 수신자용 토큰을 만들어 넣어 본다.

\`\`\`bash
# 만료된 토큰과 수신자가 다른 토큰이 각각 어떻게 처리되는지
for t in "$EXPIRED_TOKEN" "$OTHER_AUDIENCE_TOKEN"; do
  curl -s -o /dev/null -w '%{http_code}\\n' https://example.com/api/me \\
    -H "Authorization: Bearer $t"
done
\`\`\`

만료 토큰이 401 인데 수신자가 다른 토큰이 200 이면, 수신자 확인이 빠진 것이다. 발급자가 하나뿐이라 안전해 보여도, 발급자가 서비스를 늘리는 순간 열린다.

## 시계가 맞아야 성립한다

만료 확인은 서버 시계에 기댄다. 시계가 밀리면 유효한 토큰이 거절되거나 만료된 토큰이 통과한다. 토큰 검증을 다듬기 전에 시각 동기화가 되고 있는지 먼저 보는 편이 순서에 맞다.

## 참고

- RFC 7519 — JWT 등록된 주장
- RFC 8725 — JWT 모범 사례
- OWASP — 인증 Cheat Sheet`,
    diagram: {
      type: 'steps',
      caption: '서명 통과 뒤에 남는 확인',
      steps: [
        { label: '수신자', note: '우리 서비스인가' },
        { label: '발급자', note: '아는 곳인가' },
        { label: '유효 기간', note: '만료·시작 시각' },
        { label: '접근 통제', note: '지금 권한이 있나' },
      ],
    },
    diagram2: {
      type: 'bars',
      caption: '수명과 회수 지연',
      unit: '분',
      items: [
        { label: '5분', value: 5, note: '갱신 잦음' },
        { label: '1시간', value: 60, note: '균형' },
        { label: '하루', value: 1440, note: '회수 느림' },
        { label: '일주일', value: 10080, note: '사실상 방치' },
      ],
    },
  },
  {
    slug: 'oauth-state-parameter',
    title: '인가 흐름에서 상태값이 하는 일',
    body: `외부 로그인 흐름은 사용자를 다른 사이트로 보냈다가 다시 받는다. 돌아온 요청이 정말 우리가 보낸 그 흐름의 응답인지 확인하지 않으면, 공격자가 자기 계정으로 시작한 흐름을 피해자에게 이어 붙일 수 있다.

## 무엇이 가능해지나

공격자가 자기 계정의 인가 코드를 피해자 브라우저에서 처리하게 만들면, 피해자는 자기도 모르게 공격자 계정에 로그인된다. 그 상태에서 입력한 것이 공격자에게 남는다.

| 상황 | 결과 |
| --- | --- |
| 연동 계정 추가 | 공격자 계정에 피해자 데이터 연결 |
| 결제 수단 등록 | 공격자 계정에 등록 |
| 문서 작성 | 공격자가 열람 |

로그인 자체보다 연동을 추가하는 흐름에서 피해가 크다. 이미 로그인한 사용자가 외부 계정을 붙이는 자리다.

![흐름이 바꿔치기되는 경로](/img/posts/oauth-state-parameter.svg)

## 상태값을 어떻게 만드나

| 요건 | 방법 |
| --- | --- |
| 추측 불가 | 충분한 길이의 무작위 값 |
| 세션에 묶임 | 서버 세션이나 서명된 쿠키에 보관 |
| 한 번만 사용 | 검증 후 즉시 폐기 |
| 짧은 수명 | 몇 분 |

값을 만들기만 하고 세션에 묶지 않으면 아무 값이나 돌아와도 통과한다. 세션에 저장한 값과 돌아온 값이 같은지 보는 것이 핵심이다.

## 함께 쓰는 다른 장치

| 장치 | 막는 것 |
| --- | --- |
| 상태값 | 흐름 바꿔치기 |
| 코드 검증자 | 코드 가로채기 |
| 정확한 리다이렉트 주소 | 코드 유출 |
| 한 번 쓰는 코드 | 재사용 |

공개 클라이언트에서는 코드 검증자가 필수에 가깝다. 코드가 가로채여도 검증자를 모르면 교환할 수 없다.

![인가 흐름의 방어 장치](/img/posts/oauth-state-parameter-2.svg)

## 검증이 되는지 시험한다

\`\`\`bash
# 상태값을 임의 값으로 바꿔 콜백을 호출한다
curl -s -o /dev/null -w '%{http_code}\\n' -b "$SESSION_COOKIE" \\
  "https://example.com/auth/callback?code=dummy&state=tampered"

# 상태값을 아예 빼고 호출한다
curl -s -o /dev/null -w '%{http_code}\\n' -b "$SESSION_COOKIE" \\
  "https://example.com/auth/callback?code=dummy"
\`\`\`

둘 다 400 대 응답이어야 한다. 500 이 오면 검증은 하지만 예외 처리가 없는 것이고, 200 이 오면 검증 자체가 없다.

## 돌아올 곳을 상태값에 담을 때

로그인 후 돌아갈 주소를 상태값에 실어 보내는 구현이 있다. 편리하지만 그 주소가 검증되지 않으면 열린 전달이 된다. 주소 자체를 담지 말고 서버가 보관한 흐름 식별자만 담아, 돌아갈 곳은 서버가 정하는 편이 안전하다.

## 참고

- RFC 6749 §10.12 — 요청 위조 방어
- OAuth 2.0 Security Best Current Practice
- RFC 7636 — 코드 검증자(PKCE)`,
    diagram: {
      type: 'flow',
      caption: '흐름이 바꿔치기되는 경로',
      steps: [
        { label: '공격자가 흐름 시작', note: '자기 계정' },
        { label: '인가 코드 획득', note: '사용하지 않음' },
        { label: '피해자에게 콜백 링크', note: '상태값 미검증', danger: true },
        { label: '피해자가 공격자 계정에 로그인', note: '데이터가 넘어감' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '인가 흐름의 방어 장치',
      layers: [
        { label: '상태값', note: '흐름 바꿔치기' },
        { label: '코드 검증자', note: '코드 가로채기' },
        { label: '정확한 주소', note: '코드 유출' },
        { label: '일회용 코드', note: '재사용' },
      ],
    },
  },
  {
    slug: 'oauth-scope-creep',
    title: '연동 권한이 계속 넓어지는 문제',
    body: `외부 서비스와 연동할 때 요청하는 권한은 한 번 정하면 잘 줄지 않는다. 기능을 더할 때마다 하나씩 붙고, 그 기능이 사라져도 권한은 남는다. 몇 해가 지나면 처음 필요했던 것보다 훨씬 넓은 접근 권한을 들고 있게 된다.

## 왜 줄지 않나?

| 이유 | 설명 |
| --- | --- |
| 줄이면 재동의 필요 | 사용자가 다시 눌러야 함 |
| 어디에 쓰는지 모름 | 지워도 되는지 판단 불가 |
| 넓게 받아 두면 편함 | 나중을 대비 |
| 담당이 바뀜 | 이유를 아는 사람이 없음 |

재동의가 필요하다는 점이 가장 큰 제동이다. 권한을 줄이면 기존 사용자에게 다시 물어야 하고, 그 과정에서 이탈이 생긴다.

![권한이 쌓이는 경로](/img/posts/oauth-scope-creep.svg)

## 무엇이 위험해지나

우리가 뚫리면 그 권한만큼 남의 서비스에 접근된다. 우리 데이터베이스만의 문제가 아니게 된다.

| 권한 | 뚫렸을 때 |
| --- | --- |
| 읽기 전용 | 데이터 열람 |
| 쓰기 | 남의 계정에서 글 작성 |
| 오프라인 접근 | 사용자가 없어도 계속 |
| 관리 권한 | 조직 전체 |

오프라인 접근이 특히 무겁다. 사용자가 접속하지 않아도 계속 쓸 수 있는 토큰이라, 유출되면 발견이 늦다.

## 최소 권한으로 되돌리는 방법

| 방법 | 내용 |
| --- | --- |
| 기능별 최소 권한 | 필요할 때 추가 요청 |
| 단계적 요청 | 처음엔 최소, 쓸 때 더 |
| 주기적 검토 | 쓰지 않는 것 제거 |
| 사용 기록 | 무엇을 실제로 부르는지 |

사용 기록이 검토의 근거가 된다. 지난 반년 동안 한 번도 부르지 않은 권한은 뺄 후보다.

![권한 검토 근거](/img/posts/oauth-scope-creep-2.svg)

## 지금 무엇을 요청하는지 본다

\`\`\`bash
# 코드에서 요청하는 권한 목록
grep -rniE "scope['\\\"]?\\s*[:=]" --include='*.{ts,js,py,go}' . \\
  | grep -v node_modules | head -20

# 실제로 부르는 엔드포인트 — 권한과 대조한다
grep -rniE 'googleapis|graph.microsoft|api.github' --include='*.{ts,js,py,go}' . \\
  | grep -v node_modules | sed 's/.*\\(https[^"'\\'']*\\).*/\\1/' | sort -u | head -20
\`\`\`

요청하는 권한 목록과 실제로 부르는 주소 목록을 나란히 놓으면 남는 권한이 드러난다. 대개 몇 개가 나온다.

## 받은 토큰을 어떻게 두나

넓은 권한을 가진 토큰일수록 보관이 중요해진다. 데이터베이스에 평문으로 두면 데이터베이스 유출이 곧 남의 서비스 접근으로 이어진다. 별도 키로 암호화해 두고, 그 키는 데이터베이스와 다른 곳에 둔다. 권한을 줄이는 일과 토큰을 보호하는 일은 함께 가야 한다.

## 참고

- OAuth 2.0 Security Best Current Practice
- RFC 6749 §3.3 — 접근 범위
- OWASP — 제3자 연동 보안`,
    diagram: {
      type: 'flow',
      caption: '권한이 쌓이는 경로',
      steps: [
        { label: '기능 추가', note: '권한 하나 더' },
        { label: '기능 폐기', note: '권한은 남음', danger: true },
        { label: '담당자 교체', note: '이유를 모름' },
        { label: '넓은 권한 보유', note: '줄일 근거 없음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '권한 검토 근거',
      x: ['최근 사용함', '사용 없음'],
      y: ['쓰기 권한', '읽기 전용'],
      cells: ['유지', '즉시 제거', '유지', '제거 검토'],
    },
  },
  {
    slug: 'saml-assertion-check',
    title: '기업 로그인 응답에서 확인할 것',
    body: `기업 환경의 통합 인증은 인증 결과를 서명된 문서로 받는다. 그 문서를 받아 사용자를 로그인시키는 과정에 확인할 것이 여럿 있고, 하나만 빠져도 남의 계정으로 들어올 수 있다.

## 무엇을 확인하나?

| 항목 | 확인 |
| --- | --- |
| 서명 | 우리가 아는 발급자의 것인가 |
| 서명 범위 | 문서 전체인가 일부인가 |
| 수신자 | 우리 서비스를 가리키는가 |
| 유효 시각 | 지금 유효한가 |
| 사용 이력 | 처음 쓰는 문서인가 |
| 요청 대응 | 우리가 보낸 요청의 답인가 |

서명 범위 확인이 놓치기 쉽다. 일부만 서명된 문서에서 서명되지 않은 부분을 바꾸면, 서명은 통과하면서 내용은 달라진다.

![서명 범위와 변조 지점](/img/posts/saml-assertion-check.svg)

## 문서 구조가 주는 어려움

인증 문서는 XML 이다. 같은 이름의 요소를 여러 개 넣거나 주석을 끼워 파서마다 다르게 읽히게 만드는 수법이 알려져 있다.

| 수법 | 원리 |
| --- | --- |
| 요소 중복 | 검증과 사용이 다른 것을 봄 |
| 주석 삽입 | 값이 잘려 읽힘 |
| 네임스페이스 조작 | 해석이 갈림 |

검증한 문서 조각을 그대로 들고 값을 꺼내는 구조여야 한다. 검증 후 다시 파싱하면 다른 것을 읽을 수 있다.

## 한 번만 쓰이게 한다

같은 문서를 여러 번 제출하면 세션이 계속 만들어진다. 문서 식별자를 기록해 두고 재사용을 막는다.

| 항목 | 방법 |
| --- | --- |
| 식별자 보관 | 유효 기간만큼 |
| 중복 시 | 거절하고 기록 |
| 저장소 | 여러 서버가 공유 |

서버가 여럿이면 각자 기억해서는 막지 못한다. 공유 저장소를 써야 한다.

![재사용을 막는 구조](/img/posts/saml-assertion-check-2.svg)

## 연동 설정을 점검한다

\`\`\`bash
# 발급자 인증서의 만료를 본다 — 만료되면 로그인 전체가 멈춘다
openssl x509 -in idp-cert.pem -noout -subject -dates

# 설정에서 확인 항목이 켜져 있는지
grep -niE 'wantAssertionsSigned|validateInResponseTo|audience|clockSkew' saml-config.* 2>/dev/null
\`\`\`

인증서 만료는 예고 없이 전 직원 로그인을 막는다. 만료일을 달력에 넣고 한 달 전에 알림이 오게 하는 편이 낫다.

## 계정을 어떻게 잇나

인증 문서가 알려 주는 식별자로 우리 계정을 찾는데, 이때 이메일을 기준으로 삼으면 문제가 생길 수 있다. 이메일은 바뀌고 재사용되기도 한다. 발급자가 주는 변하지 않는 식별자를 기준으로 삼고 이메일은 표시용으로만 쓰는 편이 안전하다.

## 참고

- OASIS SAML 2.0 Core — 검증 규칙
- OWASP — SAML 보안 Cheat Sheet
- CWE-347 — 서명 검증 부적절`,
    diagram: {
      type: 'flow',
      caption: '서명 범위와 변조 지점',
      steps: [
        { label: '문서 수신', note: '서명 포함' },
        { label: '서명 검증 통과', note: '일부만 서명됨', danger: true },
        { label: '서명 밖 값 변조', note: '검증과 무관' },
        { label: '다른 계정으로 로그인', note: '통과' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '재사용을 막는 구조',
      steps: [
        { label: '식별자 추출', note: '문서마다 고유' },
        { label: '공유 저장소 확인', note: '서버 여럿' },
        { label: '처음이면 기록', note: '유효 기간만큼' },
        { label: '중복이면 거절', note: '기록 남김' },
      ],
    },
  },
  {
    slug: 'scim-provisioning',
    title: '계정 자동 생성·회수 연동에서 새는 곳',
    body: `인사 시스템과 서비스 계정을 잇는 자동화는 입사·퇴사 처리를 빠르게 만든다. 그런데 그 연결이 조용히 끊기면, 퇴사자가 계속 접근할 수 있는 상태가 아무도 모르게 이어진다.

## 어디서 끊기나?

| 지점 | 증상 |
| --- | --- |
| 토큰 만료 | 동기화 전체 중단 |
| 필드 이름 변경 | 일부만 반영 |
| 대상 필터 | 특정 부서만 빠짐 |
| 오류 무시 | 실패가 성공으로 보임 |
| 속도 제한 | 일부 건 누락 |

오류를 무시하는 설정이 가장 나쁘다. 실패해도 성공으로 보고되므로 아무도 알아차리지 못한다.

![동기화가 끊기는 자리](/img/posts/scim-provisioning.svg)

## 계정 회수가 늦어지면

퇴사자 접근이 남아 있는 기간이 그대로 위험 구간이 된다. 이 구간은 자동화가 있는 조직에서 오히려 길어지기도 한다 — 자동으로 된다고 믿고 확인하지 않기 때문이다.

| 처리 방식 | 지연 |
| --- | --- |
| 수동 처리 | 사람이 잊으면 무한 |
| 자동화 정상 | 수 분 |
| 자동화 고장 | 발견할 때까지 |

## 어떻게 확인하나

| 방법 | 확인 내용 |
| --- | --- |
| 양쪽 계정 수 대조 | 총량이 맞는가 |
| 퇴사자 표본 확인 | 실제로 막혔는가 |
| 동기화 로그 | 마지막 성공 시각 |
| 실패 경보 | 끊긴 즉시 알림 |

총량 대조가 값싸고 효과적이다. 인사 시스템의 재직자 수와 서비스의 활성 계정 수가 크게 벌어지면 무언가 끊긴 것이다.

![확인 방법](/img/posts/scim-provisioning-2.svg)

## 대조를 자동으로 돌린다

\`\`\`bash
# 양쪽 계정 수를 세어 차이를 본다
hr=$(curl -s -H "Authorization: Bearer $HR_TOKEN" \\
  'https://hr.example.com/api/employees?status=active' | python3 -c 'import json,sys;print(len(json.load(sys.stdin)["items"]))')
svc=$(curl -s -H "Authorization: Bearer $SVC_TOKEN" \\
  'https://example.com/scim/v2/Users?filter=active eq true' | python3 -c 'import json,sys;print(json.load(sys.stdin)["totalResults"])')
echo "재직 $hr · 활성 계정 $svc · 차이 $((svc-hr))"
\`\`\`

차이가 0 일 필요는 없다. 서비스 계정이나 협력사 계정이 섞이기 때문이다. 다만 그 차이가 평소와 달라지면 확인할 이유가 된다.

## 자동화가 있어도 남는 일

자동화는 시스템에 등록된 계정만 다룬다. 개인이 따로 만든 외부 서비스 계정, 공유 계정, API 키는 이 흐름 밖에 있다. 퇴사 처리 절차에 그 목록을 확인하는 단계를 따로 두지 않으면 자동화가 있어도 구멍이 남는다.

## 참고

- RFC 7644 — SCIM 프로토콜
- NIST SP 800-53 — 계정 관리
- CIS Controls — 계정 회수`,
    diagram: {
      type: 'flow',
      caption: '동기화가 끊기는 자리',
      steps: [
        { label: '토큰 만료', note: '연결 중단' },
        { label: '오류를 무시', note: '성공으로 보고', danger: true },
        { label: '아무도 모름', note: '경보 없음' },
        { label: '퇴사자 접근 유지', note: '기간 무제한' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '확인 방법',
      steps: [
        { label: '총량 대조', note: '값싸고 효과적' },
        { label: '표본 확인', note: '실제로 막혔나' },
        { label: '마지막 성공 시각', note: '끊긴 시점' },
        { label: '실패 경보', note: '즉시 알림' },
      ],
    },
  },
  {
    slug: 'api-key-token-split',
    title: 'API 키와 토큰 중 무엇을 쓸 것인가',
    body: `외부에 접근 수단을 줄 때 긴 문자열 하나를 주는 방식과, 짧은 수명의 토큰을 발급받게 하는 방식이 있다. 둘은 운영 부담과 위험의 모양이 다르고, 상황에 따라 맞는 쪽이 갈린다.

## 무엇이 다른가?

| 항목 | 긴 키 | 짧은 토큰 |
| --- | --- | --- |
| 수명 | 사실상 무제한 | 수 분~수 시간 |
| 유출 시 | 폐기할 때까지 유효 | 곧 만료 |
| 구현 | 단순 | 발급 흐름 필요 |
| 회수 | 목록에서 제거 | 기다리면 만료 |
| 권한 표현 | 대개 단순 | 세밀하게 가능 |

긴 키는 만들기 쉬운 대신 유출을 발견하기 전까지 계속 유효하다. 짧은 토큰은 그 창을 줄이지만 발급 구조를 만들어야 한다.

![두 방식의 위험 구간](/img/posts/api-key-token-split.svg)

## 어느 쪽을 고르나

| 상황 | 권장 |
| --- | --- |
| 서버 간 고정 연동 | 짧은 토큰 |
| 고객이 직접 넣는 연동 | 긴 키 |
| 브라우저에서 사용 | 짧은 토큰 |
| 일회성 스크립트 | 짧은 수명의 긴 키 |

브라우저에서 쓰는 값은 반드시 짧아야 한다. 화면에 노출되는 값은 언젠가 새어 나간다고 보는 편이 맞다.

## 긴 키를 쓸 때 줄여야 할 것

| 항목 | 방법 |
| --- | --- |
| 권한 범위 | 키마다 다르게 |
| 출발지 제한 | 허용 대역 지정 |
| 사용 기록 | 마지막 사용 시각 |
| 회전 | 두 키 공존 구간 |
| 저장 | 해시로 보관 |

키를 해시로 보관하면 우리 데이터베이스가 유출돼도 키 자체는 나가지 않는다. 대신 사용자에게 보여 줄 수 없으므로 발급 시점에 한 번만 보여 준다.

![긴 키를 안전하게 두는 법](/img/posts/api-key-token-split-2.svg)

## 어디서 새는지 찾는다

\`\`\`bash
# 저장소에 박힌 키 형태 문자열
grep -rniE '(api[_-]?key|secret)["'\\'']?\\s*[:=]\\s*["'\\''][A-Za-z0-9_\\-]{20,}' \\
  --include='*.{ts,js,py,go,yml,yaml,json}' . | grep -v node_modules | head -20

# 클라이언트 번들에 들어갔는지 — 들어갔다면 이미 공개된 값이다
grep -rE '[A-Za-z0-9_\\-]{32,}' dist/assets/*.js 2>/dev/null | head -5
\`\`\`

클라이언트 번들에서 나온 값은 폐기 대상이다. 그것은 유출 가능성이 아니라 이미 공개된 값이다.

## 사용 기록이 회수를 가능하게 한다

키마다 마지막 사용 시각을 남기면, 반년 동안 쓰지 않은 키를 골라낼 수 있다. 목록만 있고 사용 기록이 없으면 지워도 되는지 판단할 근거가 없어 아무것도 못 지운다. 발급할 때 기록 구조를 함께 만들어 두는 것이 나중의 정리를 가능하게 한다.

## 참고

- OWASP API Security Top 10 — 인증
- NIST SP 800-63B — 인증자 관리
- RFC 6750 — Bearer 토큰 사용`,
    diagram: {
      type: 'bars',
      caption: '유출부터 무효화까지',
      unit: '시간',
      items: [
        { label: '짧은 토큰', value: 1, note: '만료로 해결' },
        { label: '하루 토큰', value: 24, note: '중간' },
        { label: '긴 키(발견함)', value: 72, note: '폐기까지' },
        { label: '긴 키(모름)', value: 8760, note: '계속 유효' },
      ],
    },
    diagram2: {
      type: 'layers',
      caption: '긴 키를 안전하게 두는 법',
      layers: [
        { label: '해시로 보관', note: 'DB 유출 대비' },
        { label: '권한 범위 분리', note: '키마다 다르게' },
        { label: '출발지 제한', note: '허용 대역' },
        { label: '사용 기록', note: '회수 근거' },
      ],
    },
  },
  {
    slug: 'refresh-token-rotation',
    title: '갱신 토큰을 돌려쓰며 탈취를 알아내기',
    body: `짧은 접근 토큰과 긴 갱신 토큰을 나누면 유출 창이 줄어든다. 다만 갱신 토큰 자체가 유출되면 오래 쓰인다. 갱신할 때마다 새것으로 바꾸고 옛것을 무효화하면, 유출을 탐지하는 장치가 생긴다.

## 어떻게 탐지가 되나?

이미 쓴 갱신 토큰이 다시 오면 둘 중 하나다. 우리가 발급한 새 토큰을 받지 못한 정상 사용자거나, 옛 토큰을 들고 있는 공격자다. 어느 쪽이든 그 계정의 토큰 전체를 무효화하는 것이 안전하다.

| 상황 | 해석 |
| --- | --- |
| 새 토큰으로 갱신 | 정상 |
| 이미 쓴 토큰으로 갱신 | 유출 의심 |
| 두 기기가 번갈아 | 전부 무효화 후 재로그인 |

![옛 토큰이 다시 오는 순간](/img/posts/refresh-token-rotation.svg)

## 정상 사용자가 걸리는 경우

네트워크가 끊겨 새 토큰을 받지 못하면 정상 사용자도 옛 토큰으로 다시 시도한다. 이것을 유출로 판단하면 로그아웃이 잦아진다.

| 완화 | 내용 |
| --- | --- |
| 짧은 유예 구간 | 직전 토큰을 잠시 함께 허용 |
| 응답 재전송 | 같은 요청에 같은 답 |
| 기기 구분 | 기기별로 토큰 관리 |

유예 구간을 너무 길게 잡으면 탐지 자체가 무뎌진다. 수십 초 정도가 균형점이다.

## 세션을 어떻게 끊나

유출이 의심되면 그 계정의 모든 토큰을 무효화한다. 접근 토큰은 서명만으로 검증되므로 만료될 때까지 남는데, 그 짧은 수명이 여기서 값을 한다.

| 대상 | 무효화 방법 |
| --- | --- |
| 갱신 토큰 | 저장소에서 제거 |
| 접근 토큰 | 만료를 기다림 |
| 즉시 끊어야 할 때 | 세션 버전을 올려 전부 거절 |

계정마다 버전 번호를 두고 토큰에 담아 두면, 번호를 올리는 것만으로 기존 토큰이 전부 무효가 된다.

![무효화 수단](/img/posts/refresh-token-rotation-2.svg)

## 동작을 확인한다

\`\`\`bash
# 갱신을 한 번 하고, 같은 토큰으로 다시 갱신해 본다
new=$(curl -s -X POST https://example.com/auth/refresh \\
  -d "refresh_token=$OLD" | python3 -c 'import json,sys;print(json.load(sys.stdin)["refresh_token"])')
curl -s -o /dev/null -w '재사용 %{http_code}\\n' -X POST https://example.com/auth/refresh \\
  -d "refresh_token=$OLD"
\`\`\`

재사용이 200 이면 회전이 동작하지 않는 것이다. 401 이 오면 회전은 되는데, 그때 계정 전체를 무효화하는지까지 확인한다.

## 저장 위치가 결정한다

갱신 토큰을 브라우저 저장소에 두면 스크립트 삽입 취약점 하나로 새어 나간다. 스크립트가 읽을 수 없는 쿠키에 두면 그 경로가 막힌다. 회전 장치를 붙이기 전에 어디에 두고 있는지부터 보는 편이 순서에 맞다.

## 참고

- OAuth 2.0 Security Best Current Practice
- RFC 6749 §10.4 — 갱신 토큰 보호
- OWASP — 세션 관리 Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '옛 토큰이 다시 오는 순간',
      steps: [
        { label: '갱신 요청', note: '새 토큰 발급' },
        { label: '옛 토큰 무효화', note: '한 번만 유효' },
        { label: '옛 토큰 재등장', note: '유출 의심', danger: true },
        { label: '계정 전체 무효화', note: '재로그인 요구' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '무효화 수단',
      steps: [
        { label: '갱신 토큰 제거', note: '저장소에서' },
        { label: '접근 토큰 만료 대기', note: '짧은 수명' },
        { label: '세션 버전 증가', note: '즉시 전부 거절' },
        { label: '사용자 통지', note: '재로그인 안내' },
      ],
    },
  },
  {
    slug: 'mutual-tls-setup',
    title: '서버끼리 서로를 확인하게 만들기',
    body: `내부 서비스 사이의 통신은 네트워크가 분리돼 있다는 이유로 인증 없이 열려 있는 경우가 많다. 그 전제는 네트워크에 들어온 공격자 앞에서 무너진다. 양쪽이 인증서로 서로를 확인하면 그 전제가 필요 없어진다.

## 무엇이 달라지나

| 방식 | 확인되는 것 |
| --- | --- |
| 암호화만 | 도청 방지 |
| 서버 인증서만 | 서버가 진짜인지 |
| 양방향 인증 | 클라이언트도 누구인지 |

양방향이면 네트워크 안에 있다는 것만으로는 부족해진다. 인증서가 없으면 연결 자체가 맺어지지 않는다.

![무엇을 확인하는가](/img/posts/mutual-tls-setup.svg)

## 어디서 부담이 생기나

| 항목 | 부담 |
| --- | --- |
| 인증서 발급 | 서비스마다 필요 |
| 만료 관리 | 놓치면 통신 중단 |
| 폐기 처리 | 목록 배포 |
| 수명 | 짧을수록 안전, 잦은 갱신 |

만료 관리가 가장 자주 사고를 만든다. 인증서 하나가 만료되면 그 서비스로 가는 모든 호출이 한꺼번에 실패한다.

## 자동화가 전제다

인증서를 사람이 관리하면 수명을 길게 잡게 되고, 길면 폐기가 어려워진다. 발급과 갱신이 자동으로 돌아야 짧은 수명을 쓸 수 있다.

| 요소 | 자동화 |
| --- | --- |
| 발급 | 서비스 시작 시 |
| 갱신 | 만료 전 자동 |
| 배포 | 파일이 아니라 주입 |
| 폐기 | 수명으로 대체 |

수명이 짧으면 폐기 목록 자체가 거의 필요 없어진다. 몇 시간짜리 인증서는 폐기할 틈도 없이 만료된다.

![자동화가 만드는 차이](/img/posts/mutual-tls-setup-2.svg)

## 지금 상태를 본다

\`\`\`bash
# 서비스가 클라이언트 인증서를 요구하는지
openssl s_client -connect internal.example.com:443 </dev/null 2>&1 \\
  | grep -iE 'acceptable client certificate|verify'

# 인증서 만료일 — 가까운 것부터 챙긴다
for h in svc-a svc-b svc-c; do
  d=$(openssl s_client -connect $h:443 </dev/null 2>/dev/null \\
    | openssl x509 -noout -enddate 2>/dev/null | cut -d= -f2)
  printf '%-8s %s\\n' "$h" "\${d:-확인 실패}"
done
\`\`\`

클라이언트 인증서를 요구하지 않는다면 그 서비스는 네트워크 위치만으로 접근을 허용하고 있다. 그것이 의도인지 방치인지 구분하는 것이 첫 작업이다.

## 인증과 인가는 여전히 다르다

양방향 인증은 상대가 누구인지 확인해 줄 뿐, 그 상대가 이 요청을 해도 되는지는 답하지 않는다. 인증서에 담긴 신원을 접근 통제의 입력으로 쓰고, 어떤 서비스가 어떤 엔드포인트를 부를 수 있는지는 따로 정해야 한다. 그러지 않으면 인증서를 가진 모든 서비스가 모든 것을 부를 수 있다.

## 참고

- RFC 8446 — TLS 1.3 클라이언트 인증
- NIST SP 800-52 — TLS 구현 지침
- CIS Controls — 서비스 간 인증`,
    diagram: {
      type: 'layers',
      caption: '무엇을 확인하는가',
      layers: [
        { label: '암호화', note: '도청 방지' },
        { label: '서버 확인', note: '서버가 진짜인가' },
        { label: '클라이언트 확인', note: '누가 부르는가' },
        { label: '접근 통제', note: '불러도 되는가' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '자동화가 만드는 차이',
      x: ['자동 갱신', '수동'],
      y: ['짧은 수명', '긴 수명'],
      cells: ['이상적', '운영 불가', '폐기 필요', '위험 누적'],
    },
  },
  {
    slug: 'certificate-pinning-lifecycle',
    title: '인증서 고정을 운영할 때의 대가',
    body: `인증서를 고정하면 중간에서 가로채는 공격이 어려워진다. 다만 고정한 값이 바뀌는 순간 통신이 전부 끊기고, 그 복구가 앱 배포 주기에 묶인다. 얻는 것과 잃는 것을 함께 봐야 한다.

## 무엇을 고정하나

| 대상 | 수명 | 갱신 영향 |
| --- | --- | --- |
| 서버 인증서 | 짧음 | 갱신마다 끊김 |
| 중간 인증서 | 중간 | 교체 시 끊김 |
| 공개 키 | 키를 유지하면 김 | 키 교체 때만 |

공개 키를 고정하는 편이 운영에 낫다. 인증서를 갱신해도 키를 유지하면 고정 값이 바뀌지 않는다.

![무엇을 고정하느냐에 따른 차이](/img/posts/certificate-pinning-lifecycle.svg)

## 끊겼을 때 무엇이 남나

고정이 어긋나면 그 앱은 서버에 접속하지 못한다. 서버를 고쳐도 소용이 없고, 앱을 새로 배포해 사용자가 갱신할 때까지 기다려야 한다.

| 경로 | 복구 시간 |
| --- | --- |
| 서버 설정 변경 | 즉시 — 단 고정과 무관 |
| 앱 배포 | 심사 + 갱신 확산 |
| 원격 설정으로 해제 | 그 경로가 살아 있다면 |

원격으로 고정을 끌 수 있는 길을 남겨 두는 것이 실무적이다. 다만 그 경로 자체가 공격 대상이 되므로 서명된 설정이어야 한다.

## 백업 값을 함께 넣는다

지금 쓰는 키와 다음에 쓸 키를 함께 고정해 두면 교체가 끊김 없이 된다.

| 항목 | 내용 |
| --- | --- |
| 현재 키 | 운영 중 |
| 예비 키 | 보관만, 아직 미사용 |
| 교체 절차 | 예비를 현재로, 새 예비 생성 |

예비 키는 다른 곳에 보관해야 의미가 있다. 같은 서버에 두면 그 서버가 뚫릴 때 둘 다 나간다.

![끊김 없이 교체하기](/img/posts/certificate-pinning-lifecycle-2.svg)

## 값을 뽑아 확인한다

\`\`\`bash
# 현재 서버의 공개 키 지문 — 고정할 값
openssl s_client -connect example.com:443 </dev/null 2>/dev/null \\
  | openssl x509 -pubkey -noout \\
  | openssl pkey -pubin -outform der \\
  | openssl dgst -sha256 -binary | base64

# 만료일도 함께 본다
openssl s_client -connect example.com:443 </dev/null 2>/dev/null \\
  | openssl x509 -noout -enddate
\`\`\`

이 값을 앱에 넣기 전에, 같은 명령을 예비 키에도 돌려 두 값을 함께 준비한다. 하나만 넣는 것이 사고의 시작이다.

## 어디에 쓸 만한가

일반 웹 서비스에는 권하기 어렵다. 브라우저는 이미 인증서 검증을 하고, 고정은 운영 위험만 더한다. 모바일 앱에서 특정 서버와만 통신하고 그 통신이 특히 민감할 때가 쓸 자리다. 얻는 것이 분명하지 않으면 넣지 않는 편이 낫다.

## 참고

- OWASP — 인증서 고정 Cheat Sheet
- RFC 7469 — 공개 키 고정(웹에서는 폐기됨)
- NIST SP 800-52 — TLS 운영 지침`,
    diagram: {
      type: 'matrix',
      caption: '고정 대상과 운영 부담',
      x: ['공개 키 고정', '인증서 고정'],
      y: ['예비 값 있음', '없음'],
      cells: ['운영 가능', '교체 시 끊김', '갱신마다 위험', '사고 예약'],
    },
    diagram2: {
      type: 'steps',
      caption: '끊김 없이 교체하기',
      steps: [
        { label: '예비 키 생성', note: '다른 곳에 보관' },
        { label: '두 값 함께 고정', note: '현재 + 예비' },
        { label: '서버를 예비로 교체', note: '앱은 그대로' },
        { label: '새 예비 준비', note: '다음 교체 대비' },
      ],
    },
  },
]
