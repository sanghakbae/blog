import type { SeedPost } from './types'

/** 570~579 — 계정 흐름의 빈틈 */
export const posts58: SeedPost[] = [
  {
    slug: 'password-reset-token',
    title: '비밀번호 재설정 링크를 안전하게 만들기',
    body: `재설정 링크는 비밀번호를 모르는 사람에게 비밀번호를 바꿀 권한을 주는 장치다. 인증을 우회하는 통로를 스스로 만드는 셈이라, 만드는 방법이 조금만 허술해도 그 자체가 침입 경로가 된다.

## 어디가 약한가?

| 지점 | 문제 |
| --- | --- |
| 값 생성 | 추측 가능한 값 |
| 수명 | 너무 길어 창이 넓음 |
| 재사용 | 한 번 쓴 뒤에도 유효 |
| 전달 | 주소에 그대로 노출 |
| 결과 안내 | 계정 존재 여부 노출 |

값 생성이 가장 근본이다. 시각이나 사용자 식별자를 섞어 만든 값은 다른 사람의 것을 계산해 낼 수 있다. 암호학적으로 안전한 난수만 쓴다.

![재설정 링크가 새는 자리](/img/posts/password-reset-token.svg)

## 어떻게 만들고 보관하나

| 항목 | 방법 |
| --- | --- |
| 생성 | 안전한 난수, 충분한 길이 |
| 보관 | 원본이 아니라 해시 |
| 수명 | 15~30분 |
| 사용 | 한 번, 즉시 폐기 |
| 범위 | 요청한 계정에만 |

해시로 보관하는 이유는 세션 토큰과 같다. 데이터베이스가 유출돼도 그 값으로 남의 비밀번호를 바꿀 수는 없어야 한다.

## 계정이 있는지 알려 주지 않는다

재설정을 요청했을 때 "그런 계정이 없습니다"라고 답하면, 그 화면이 계정 존재 여부를 확인하는 도구가 된다.

| 상황 | 응답 |
| --- | --- |
| 계정 있음 | 메일을 보냈습니다 |
| 계정 없음 | 메일을 보냈습니다 |
| 응답 시간 | 두 경우를 비슷하게 |

응답 시간도 맞춰야 한다. 계정이 있을 때만 메일을 보내느라 느려지면 시간 차이로 구분된다.

![존재 여부를 감추는 응답](/img/posts/password-reset-token-2.svg)

## 바꾼 뒤에 할 일

비밀번호가 바뀌면 기존 세션을 전부 끊는다. 공격자가 이미 들어와 있었다면 그 세션이 살아남기 때문이다.

\`\`\`sql
-- 재설정 토큰은 해시로 두고, 쓴 즉시 지운다
CREATE TABLE password_reset (
  token_hash  BYTEA PRIMARY KEY,
  user_id     BIGINT NOT NULL,
  expires_at  TIMESTAMPTZ NOT NULL,
  used_at     TIMESTAMPTZ
);

-- 사용 처리와 세션 무효화를 한 트랜잭션에서
UPDATE password_reset SET used_at = now()
WHERE token_hash = $1 AND used_at IS NULL AND expires_at > now()
RETURNING user_id;
\`\`\`

반환된 행이 없으면 이미 쓴 토큰이거나 만료된 것이다. 조회 후 갱신하는 방식은 동시에 두 번 오면 둘 다 통과한다.

## 알림도 방어다

비밀번호가 바뀌었다는 사실을 등록된 연락처로 알린다. 본인이 한 것이 아니라면 그 메일이 유일한 경고가 된다. 바뀐 주소로 보내면 뜻이 없으므로, 바뀌기 전 주소로도 함께 보낸다.

## 참고

- OWASP — Forgot Password Cheat Sheet
- NIST SP 800-63B — 인증자 재설정
- CWE-640 — 취약한 비밀번호 복구 절차`,
    diagram: {
      type: 'flow',
      caption: '재설정 링크가 새는 자리',
      steps: [
        { label: '추측 가능한 값 생성', note: '시각·식별자 조합', danger: true },
        { label: '긴 수명', note: '창이 넓음' },
        { label: '재사용 허용', note: '한 번으로 안 끝남' },
        { label: '남의 계정 탈취', note: '인증 우회' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '존재 여부를 감추는 응답',
      steps: [
        { label: '같은 문구', note: '있든 없든' },
        { label: '같은 응답 시간', note: '시간 차 제거' },
        { label: '같은 상태 코드', note: '구분 불가' },
        { label: '요청 제한', note: '대량 확인 차단' },
      ],
    },
  },
  {
    slug: 'email-change-flow',
    title: '이메일 변경에서 계정을 잃는 경로',
    body: `이메일 주소는 대개 계정 복구의 출발점이다. 그 주소를 바꾸는 절차가 허술하면, 공격자가 주소를 자기 것으로 바꾼 뒤 재설정을 요청해 계정을 통째로 가져간다.

## 무엇을 확인해야 하나

| 확인 | 이유 |
| --- | --- |
| 현재 비밀번호 | 세션 탈취만으로는 못 바꾸게 |
| 새 주소 소유 | 아무 주소나 지정 못 하게 |
| 이전 주소 통지 | 본인이 알아차리게 |
| 변경 후 대기 | 되돌릴 시간 |

현재 비밀번호를 묻지 않으면, 잠깐 자리를 비운 사이 열린 화면 하나로 계정이 넘어간다.

![주소 변경으로 계정을 가져가는 경로](/img/posts/email-change-flow.svg)

## 언제 바꿀 것인가

확인 메일을 보내고 새 주소에서 눌렀을 때 바꾸는 것이 기본이다. 요청 즉시 바꾸면 오타 하나로 계정에 닿을 수 없게 된다.

| 시점 | 위험 |
| --- | --- |
| 요청 즉시 | 오타로 계정 상실 |
| 새 주소 확인 후 | 안전 |
| 양쪽 확인 후 | 가장 안전, 번거로움 |

양쪽 확인은 이전 주소에서도 승인하게 하는 방식이다. 이전 주소에 접근할 수 없게 된 사용자를 막으므로, 그 경우의 지원 절차를 함께 준비해야 한다.

## 이전 주소에 보내는 통지

| 담을 것 | 담지 말 것 |
| --- | --- |
| 변경 시각 | 새 주소 전체 |
| 되돌리는 링크 | 비밀번호 관련 값 |
| 문의 경로 | 세션 정보 |

새 주소를 그대로 적으면, 계정을 가져간 사람이 그 메일을 통해 피해자에게 자기 주소를 알리는 셈이 된다. 일부만 가려 보여 주는 편이 낫다.

![통지에 담을 것](/img/posts/email-change-flow-2.svg)

## 되돌리는 길을 남긴다

이전 주소로 보낸 통지에 되돌리기 링크를 넣으면, 본인이 하지 않은 변경을 스스로 취소할 수 있다.

\`\`\`sql
-- 변경 이력을 남겨 되돌릴 수 있게 한다
CREATE TABLE email_change (
  id          UUID PRIMARY KEY,
  user_id     BIGINT NOT NULL,
  old_email   TEXT NOT NULL,
  new_email   TEXT NOT NULL,
  revert_hash BYTEA NOT NULL,
  expires_at  TIMESTAMPTZ NOT NULL,
  reverted_at TIMESTAMPTZ
);
\`\`\`

되돌리기 링크의 수명은 넉넉히 잡는다. 메일을 며칠 뒤에 보는 사람이 있고, 그 사이에 계정을 잃은 채로 두는 것이 더 나쁘다.

## 다른 인증 수단과 함께 본다

이메일만이 복구 경로인 구조에서는 이 절차가 곧 계정의 마지막 방어선이다. 두 번째 수단이 등록돼 있으면 부담이 나뉜다. 주소 변경 절차를 다듬는 일과 두 번째 수단을 권하는 일은 같은 목적을 향한다.

## 참고

- OWASP — 계정 관리 Cheat Sheet
- NIST SP 800-63B — 계정 복구
- CWE-640 — 취약한 복구 절차`,
    diagram: {
      type: 'flow',
      caption: '주소 변경으로 계정을 가져가는 경로',
      steps: [
        { label: '세션 탈취', note: '비밀번호는 모름' },
        { label: '주소를 자기 것으로', note: '확인 없음', danger: true },
        { label: '재설정 요청', note: '새 주소로 도착' },
        { label: '비밀번호 변경', note: '계정 상실' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '통지에 담을 것',
      steps: [
        { label: '변경 시각', note: '언제' },
        { label: '새 주소 일부', note: '전체는 아님' },
        { label: '되돌리기 링크', note: '넉넉한 수명' },
        { label: '문의 경로', note: '막혔을 때' },
      ],
    },
  },
  {
    slug: 'phone-change-flow',
    title: '휴대폰 번호를 인증 수단으로 쓸 때',
    body: `휴대폰 번호는 본인 확인에 널리 쓰이지만 계정과 영구히 묶이는 값이 아니다. 번호는 해지되고 재배정되며, 통신사 절차만 통과하면 남이 가져갈 수도 있다.

## 번호가 옮겨 다닌다

| 상황 | 결과 |
| --- | --- |
| 해지 후 재배정 | 남이 그 번호를 씀 |
| 번호 이동 | 통신사만 바뀜 |
| 기기 분실 | 번호는 유지 |
| 통신사 사칭 | 번호를 탈취 |

재배정이 특히 조용한 위험이다. 몇 달 뒤 그 번호를 받은 사람이 인증 문자를 받게 되는데, 우리 시스템은 아무 변화도 느끼지 못한다.

![번호가 옮겨 가는 경로](/img/posts/phone-change-flow.svg)

## 어디에 쓰고 어디에 쓰지 않나

| 용도 | 적합성 |
| --- | --- |
| 이상 로그인 알림 | 적합 |
| 두 번째 확인 수단 | 조건부 |
| 유일한 복구 수단 | 부적합 |
| 유일한 인증 수단 | 부적합 |

유일한 복구 수단으로 두면, 번호를 잃은 사용자가 계정도 함께 잃는다. 다른 수단을 함께 등록하게 하는 것이 사용자를 위한 일이기도 하다.

## 변경 절차에서 확인할 것

| 확인 | 내용 |
| --- | --- |
| 현재 비밀번호 | 세션만으로 불가 |
| 새 번호 소유 | 인증 문자 |
| 이전 연락처 통지 | 메일로도 |
| 변경 후 유예 | 민감 작업 잠시 제한 |

변경 직후 출금이나 대량 조회 같은 작업을 잠시 막으면, 탈취당한 경우에 피해가 커지기 전에 본인이 알아차릴 시간이 생긴다.

![변경 후 유예 구간](/img/posts/phone-change-flow-2.svg)

## 오래된 번호를 찾아낸다

\`\`\`sql
-- 오래 확인하지 않은 번호 — 재배정 가능성이 쌓인 구간
SELECT count(*) AS 미확인_1년이상
FROM users
WHERE phone_verified_at < now() - interval '1 year';

-- 같은 번호가 여러 계정에 묶여 있는지 — 재배정의 흔적일 수 있다
SELECT phone, count(*) AS 계정수
FROM users WHERE phone IS NOT NULL
GROUP BY phone HAVING count(*) > 1;
\`\`\`

같은 번호에 여러 계정이 붙어 있으면 재배정이 일어났을 가능성이 있다. 그 계정들을 한 번에 처리하기보다 개별 확인이 필요한 목록으로 다룬다.

## 문자 대신 쓸 수 있는 것

앱 기반 코드나 하드웨어 키는 번호에 기대지 않는다. 문자보다 안전하고 통신 상태에도 덜 영향받는다. 문자를 완전히 없애기는 어렵더라도, 더 강한 수단을 등록한 사용자에게는 문자를 복구 경로에서 빼는 선택지를 주는 것이 합리적이다.

## 참고

- NIST SP 800-63B — 문자 기반 인증의 한계
- OWASP — 다단계 인증 Cheat Sheet
- CWE-304 — 인증 단계 누락`,
    diagram: {
      type: 'flow',
      caption: '번호가 옮겨 가는 경로',
      steps: [
        { label: '사용자가 번호 해지', note: '계정은 그대로' },
        { label: '통신사가 재배정', note: '몇 달 뒤' },
        { label: '새 주인이 문자 수신', note: '우리는 모름', danger: true },
        { label: '계정 복구 가능', note: '남이' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '변경 후 유예 구간',
      steps: [
        { label: '번호 변경', note: '확인 완료' },
        { label: '민감 작업 제한', note: '출금·대량 조회' },
        { label: '이전 연락처 통지', note: '메일로도' },
        { label: '유예 후 해제', note: '이상 없으면' },
      ],
    },
  },
  {
    slug: 'magic-link-risk',
    title: '메일 링크 로그인이 옮겨 놓는 위험',
    body: `비밀번호 없이 메일로 온 링크를 눌러 로그인하는 방식은 편리하고, 비밀번호 재사용 문제를 없앤다. 다만 계정의 안전이 메일함 하나에 전부 얹히고, 링크가 지나는 모든 길이 새 공격면이 된다.

## 어디에 기대게 되나

| 의존 | 결과 |
| --- | --- |
| 메일함 보안 | 메일함이 뚫리면 전부 |
| 메일 전달 경로 | 중간에서 열람 |
| 링크 미리보기 | 자동으로 눌러짐 |
| 기기 간 이동 | 다른 기기에서 열림 |

링크 미리보기가 실무에서 자주 문제가 된다. 보안 제품이나 메신저가 링크를 미리 열어 보면, 사용자가 누르기 전에 토큰이 소모된다.

![링크가 지나는 길](/img/posts/magic-link-risk.svg)

## 미리보기에 소모되지 않게

| 방법 | 내용 |
| --- | --- |
| 첫 접근은 확인 화면 | 누르면 버튼이 있는 페이지 |
| 실제 로그인은 POST | 미리보기는 GET 만 함 |
| 같은 기기 제한 | 요청한 브라우저에서만 |
| 짧은 코드 병행 | 화면에 숫자를 함께 표시 |

같은 기기 제한이 효과가 크다. 로그인을 요청한 브라우저에 표식을 남기고, 링크를 그 브라우저에서 열었을 때만 완료시킨다.

## 수명과 재사용

| 항목 | 값 |
| --- | --- |
| 수명 | 5~15분 |
| 사용 횟수 | 한 번 |
| 동시 발급 | 최신 것만 유효 |
| 실패 제한 | 요청 횟수 제한 |

여러 번 요청해 여러 링크가 살아 있는 상태는 위험하다. 새로 발급하면 이전 것을 무효화한다.

![발급과 소모 규칙](/img/posts/magic-link-risk-2.svg)

## 요청 화면이 알려 주는 것

\`\`\`bash
# 존재하지 않는 주소로 요청했을 때 응답이 다른지
for a in "real@example.com" "nobody-$RANDOM@example.com"; do
  curl -s -o /dev/null -w "%{http_code} %{time_total}s  $a\\n" \\
    -X POST https://example.com/auth/link -d "email=$a"
done
\`\`\`

상태 코드나 응답 시간이 다르면 계정 존재 여부가 드러난다. 메일을 보내는 작업을 비동기로 돌려 두 경우의 응답을 같게 만든다.

## 비밀번호를 없앤 것이 아니다

메일 링크만 쓰는 구조에서는 메일 계정의 비밀번호가 사실상 우리 서비스의 비밀번호다. 사용자가 그쪽에 두 번째 인증 수단을 걸어 두었는지는 우리가 알 수 없다. 편의를 위해 선택하는 것은 괜찮지만, 위험을 없앴다기보다 옮겨 놓았다는 것을 전제로 다른 방어를 설계해야 한다.

## 참고

- NIST SP 800-63B — 대역 외 인증자
- OWASP — 인증 Cheat Sheet
- CWE-640 — 취약한 복구 절차`,
    diagram: {
      type: 'layers',
      caption: '링크가 지나는 길',
      layers: [
        { label: '메일 서버', note: '전달 경로' },
        { label: '보안 제품', note: '링크 미리 열람' },
        { label: '메일함', note: '장기 보관' },
        { label: '사용자 기기', note: '최종 도달' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '발급과 소모 규칙',
      steps: [
        { label: '짧은 수명', note: '5~15분' },
        { label: '한 번만 사용', note: '즉시 폐기' },
        { label: '최신 것만 유효', note: '이전 무효화' },
        { label: '같은 기기에서만', note: '미리보기 방어' },
      ],
    },
  },
  {
    slug: 'logout-completeness',
    title: '로그아웃이 실제로 끊는 것과 남기는 것',
    body: `로그아웃 버튼을 누르면 화면은 로그인 전으로 돌아간다. 그런데 뒤에서 무엇이 실제로 무효화됐는지는 구현마다 다르다. 남아 있는 것이 있으면 로그아웃은 화면 연출에 그친다.

## 무엇이 남을 수 있나

| 대상 | 흔한 상태 |
| --- | --- |
| 세션 쿠키 | 지워짐 |
| 서버 세션 | 지워지는 경우와 아닌 경우 |
| 접근 토큰 | 만료까지 유효 |
| 갱신 토큰 | 지워지는 경우와 아닌 경우 |
| 다른 기기 세션 | 대개 남음 |
| 캐시된 화면 | 뒤로 가기로 보임 |

쿠키만 지우고 서버 상태를 그대로 두면, 그 쿠키 값을 미리 복사해 둔 사람은 계속 들어올 수 있다.

![무엇이 남는가](/img/posts/logout-completeness.svg)

## 어디까지 끊을지 정한다

| 범위 | 언제 |
| --- | --- |
| 이 기기만 | 일반 로그아웃 |
| 모든 기기 | 비밀번호 변경·유출 의심 |
| 특정 기기 | 사용자가 목록에서 선택 |

기기 목록을 보여 주고 개별로 끊게 하면, 사용자가 스스로 이상을 발견하고 조치할 수 있다. 목록에 마지막 접속 시각과 대략적인 위치를 함께 보여 주면 판단이 쉬워진다.

## 뒤로 가기로 보이는 화면

로그아웃 후 뒤로 가기를 누르면 이전 화면이 보이는 경우가 있다. 브라우저가 캐시한 것이라 서버는 관여하지 않는다.

| 대상 | 방법 |
| --- | --- |
| 개인정보가 있는 화면 | 캐시 금지 헤더 |
| 정적 자원 | 캐시해도 됨 |
| 목록·조회 결과 | 캐시 금지 |

![로그아웃이 끊는 범위](/img/posts/logout-completeness-2.svg)

## 실제로 끊기는지 확인한다

\`\`\`bash
# 로그아웃 전 쿠키를 복사해 두고, 로그아웃 뒤 그 값으로 접근한다
curl -s -c /tmp/c.txt -X POST https://example.com/login -d 'id=u&pw=p' >/dev/null
cp /tmp/c.txt /tmp/before.txt
curl -s -b /tmp/c.txt -X POST https://example.com/logout >/dev/null
curl -s -o /dev/null -w '옛 쿠키로 접근 %{http_code}\\n' -b /tmp/before.txt https://example.com/api/me
\`\`\`

200 이 오면 서버 세션이 남아 있는 것이다. 401 이 와야 실제로 끊긴 것이다.

## 외부 로그인과 함께 쓸 때

외부 계정으로 로그인한 경우, 우리 세션을 끊어도 그쪽 세션은 남는다. 다시 로그인 버튼을 누르면 아무것도 묻지 않고 바로 들어온다. 공용 컴퓨터에서는 이것이 로그아웃되지 않은 것처럼 보인다. 그쪽 세션까지 끊을지, 아니면 그 사실을 화면에 안내할지 정해 두어야 한다.

## 참고

- OWASP — 세션 관리 Cheat Sheet
- NIST SP 800-63B — 세션 종료
- CWE-613 — 부적절한 세션 만료`,
    diagram: {
      type: 'layers',
      caption: '무엇이 남는가',
      layers: [
        { label: '세션 쿠키', note: '대개 지워짐' },
        { label: '서버 세션', note: '남는 경우 있음' },
        { label: '발급된 토큰', note: '만료까지' },
        { label: '다른 기기', note: '대개 남음' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '로그아웃이 끊는 범위',
      x: ['이 기기', '모든 기기'],
      y: ['일반 상황', '유출 의심'],
      cells: ['충분', '과함', '부족', '적절'],
    },
  },
  {
    slug: 'concurrent-session-limit',
    title: '동시 접속을 제한할 때 생각할 것',
    body: `한 계정으로 여러 곳에서 동시에 접속하는 것을 막는 규칙은 계정 공유를 억제하고 탈취를 눈에 띄게 한다. 다만 정상 사용자도 기기를 여럿 쓰므로, 규칙을 잘못 잡으면 불편만 남는다.

## 무엇을 얻고 무엇을 잃나

| 얻는 것 | 잃는 것 |
| --- | --- |
| 계정 공유 억제 | 다기기 사용 불편 |
| 탈취 시 충돌로 인지 | 잦은 재로그인 |
| 라이선스 정책 강제 | 지원 문의 증가 |

탈취를 인지하게 되는 효과가 보안 쪽 값어치다. 누군가 내 계정으로 들어오면 내가 끊기므로 이상을 알아차린다.

![제한이 만드는 효과와 마찰](/img/posts/concurrent-session-limit.svg)

## 무엇을 하나로 셀 것인가

| 단위 | 결과 |
| --- | --- |
| 세션마다 | 브라우저 탭·앱마다 셈 |
| 기기마다 | 자연스러움 |
| IP 마다 | 회선 공유에서 오작동 |
| 로그인마다 | 재로그인이 쌓임 |

기기 단위가 사용자의 인식과 가장 가깝다. 기기를 구분하려면 표식을 남겨야 하고, 그 표식이 지워지면 새 기기로 보인다는 점을 감안한다.

## 넘쳤을 때의 처리

| 방식 | 사용자 경험 |
| --- | --- |
| 가장 오래된 것 끊기 | 조용함, 인지 못 함 |
| 새 로그인 거절 | 이유가 분명 |
| 무엇을 끊을지 선택 | 통제감 |

무엇을 끊을지 사용자가 고르게 하는 방식이 가장 낫다. 목록에서 낯선 기기를 발견하는 것 자체가 방어다.

![넘쳤을 때의 선택](/img/posts/concurrent-session-limit-2.svg)

## 기기 목록을 만든다

\`\`\`sql
-- 세션마다 기기 표식과 마지막 사용 시각을 둔다
CREATE TABLE sessions (
  id           UUID PRIMARY KEY,
  user_id      BIGINT NOT NULL,
  device_id    TEXT NOT NULL,
  user_agent   TEXT,
  last_seen_at TIMESTAMPTZ NOT NULL,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 한 계정의 활성 기기 수
SELECT user_id, count(DISTINCT device_id) AS 기기수
FROM sessions WHERE last_seen_at > now() - interval '30 days'
GROUP BY user_id ORDER BY 기기수 DESC LIMIT 20;
\`\`\`

상위 계정의 기기 수가 유난히 많으면 공유이거나 탈취다. 제한을 걸기 전에 이 분포를 먼저 보는 것이 순서다 — 분포를 모르고 숫자를 정하면 대부분의 정상 사용자가 걸린다.

## 제한보다 알림이 나을 때

동시 접속을 막는 대신 새 기기에서 로그인했을 때 알림을 보내는 방법이 있다. 불편을 만들지 않으면서 탈취 인지라는 목적은 달성한다. 계정 공유를 막는 것이 목적이 아니라면 이쪽이 균형이 낫다.

## 참고

- NIST SP 800-63B — 세션 관리
- OWASP — 세션 관리 Cheat Sheet
- CWE-613 — 세션 만료 처리`,
    diagram: {
      type: 'matrix',
      caption: '제한이 만드는 효과와 마찰',
      x: ['제한 있음', '없음'],
      y: ['탈취 상황', '정상 다기기'],
      cells: ['충돌로 인지', '모름', '불편', '편함'],
    },
    diagram2: {
      type: 'steps',
      caption: '넘쳤을 때의 선택',
      steps: [
        { label: '기기 목록 표시', note: '마지막 사용 시각' },
        { label: '사용자가 선택', note: '무엇을 끊을지' },
        { label: '낯선 기기 발견', note: '그 자체가 방어' },
        { label: '끊고 계속', note: '통제감' },
      ],
    },
  },
  {
    slug: 'device-binding',
    title: '기기를 기억해 두는 것의 값과 한계',
    body: `자주 쓰는 기기를 기억해 두면 매번 두 번째 인증을 요구하지 않아도 된다. 편의가 크게 오르는 대신, 그 기억이 곧 인증 수단이 되므로 기억하는 방식이 허술하면 그것이 우회 경로가 된다.

## 무엇으로 기억하나

| 방법 | 안정성 | 위조 난이도 |
| --- | --- | --- |
| 쿠키에 표식 | 지워지면 사라짐 | 훔치면 가능 |
| 기기 지문 | 브라우저 갱신에 취약 | 흉내 가능 |
| 플랫폼 인증자 | 안정적 | 매우 어려움 |
| 인증서 | 안정적 | 어려움 |

쿠키 표식은 구현이 쉬운 대신 그 값을 훔치면 그대로 통과한다. 표식 자체를 두 번째 인증 수단으로 취급해 보호해야 한다.

![기억 방식과 위조 난이도](/img/posts/device-binding.svg)

## 얼마나 오래 기억하나

| 기간 | 균형 |
| --- | --- |
| 며칠 | 불편이 큼 |
| 30일 | 흔한 선택 |
| 무기한 | 사실상 해제 |

무기한 기억은 두 번째 인증을 껐다고 보는 편이 맞다. 기간을 정하고, 위험 신호가 보이면 그 전에라도 다시 묻는다.

## 언제 다시 물을 것인가

| 신호 | 판단 |
| --- | --- |
| 새 지역에서 접속 | 다시 묻기 |
| 비밀번호 변경 후 | 전부 초기화 |
| 오랜 미접속 | 다시 묻기 |
| 민감 작업 | 기억과 무관하게 묻기 |

민감 작업은 기억 여부와 별개로 다룬다. 출금이나 권한 변경처럼 되돌리기 어려운 일은 그 시점에 다시 확인하는 것이 맞다.

![다시 물어야 하는 신호](/img/posts/device-binding-2.svg)

## 기억을 관리할 수 있게 한다

\`\`\`sql
-- 기억된 기기 목록. 사용자가 직접 지울 수 있어야 한다.
CREATE TABLE trusted_device (
  id           UUID PRIMARY KEY,
  user_id      BIGINT NOT NULL,
  token_hash   BYTEA NOT NULL,
  label        TEXT,
  last_used_at TIMESTAMPTZ,
  expires_at   TIMESTAMPTZ NOT NULL
);

-- 오래 쓰지 않은 기억은 스스로 정리한다
DELETE FROM trusted_device WHERE expires_at < now();
\`\`\`

표식을 해시로 두는 것은 세션 토큰과 같은 이유다. 데이터베이스가 유출돼도 그 값으로 두 번째 인증을 넘을 수는 없어야 한다.

## 공용 기기에서의 선택

"이 기기 기억하기"를 기본으로 켜 두면 공용 컴퓨터에서 켜진 채로 남는다. 기본을 꺼 두고 사용자가 고르게 하되, 무엇을 뜻하는지 한 줄로 설명한다. 설명 없는 체크박스는 아무도 읽지 않고 켜 둔다.

## 참고

- NIST SP 800-63B — 기억된 기기
- OWASP — 다단계 인증 Cheat Sheet
- W3C WebAuthn — 플랫폼 인증자`,
    diagram: {
      type: 'bars',
      caption: '기억 방식과 위조 난이도',
      unit: '점',
      items: [
        { label: '플랫폼 인증자', value: 95, note: '매우 어려움' },
        { label: '인증서', value: 85, note: '어려움' },
        { label: '쿠키 표식', value: 45, note: '훔치면 가능' },
        { label: '기기 지문', value: 25, note: '흉내 가능' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '다시 물어야 하는 신호',
      steps: [
        { label: '새 지역', note: '평소와 다름' },
        { label: '비밀번호 변경', note: '전부 초기화' },
        { label: '오랜 미접속', note: '기억 만료' },
        { label: '민감 작업', note: '기억과 무관' },
      ],
    },
  },
  {
    slug: 'account-linking-safety',
    title: '외부 계정을 이어 붙일 때의 함정',
    body: `기존 계정에 외부 로그인을 붙이거나, 외부 로그인으로 처음 들어온 사람을 기존 계정과 잇는 흐름이 있다. 잇는 기준을 이메일로만 잡으면 남의 계정에 올라탈 수 있다.

## 이메일로만 이으면 무엇이 뚫리나?

외부 제공자가 알려 준 이메일이 우리 계정의 이메일과 같다고 해서 같은 사람이라는 보장은 없다. 제공자가 그 주소의 소유를 확인했는지가 관건이다.

| 상황 | 위험 |
| --- | --- |
| 제공자가 미확인 주소 제공 | 남의 주소로 가입해 잇기 |
| 주소만 같고 사람은 다름 | 계정 병합 사고 |
| 주소 변경 후 재사용 | 옛 주인이 이어 붙음 |

제공자가 주소 확인 여부를 함께 알려 주는 경우가 많다. 그 값이 참이 아니면 자동으로 잇지 않는다.

![이메일만으로 잇는 위험](/img/posts/account-linking-safety.svg)

## 안전하게 잇는 순서

| 단계 | 내용 |
| --- | --- |
| 1 | 기존 계정으로 먼저 로그인 |
| 2 | 로그인한 상태에서 외부 계정 연결 |
| 3 | 연결 시 현재 비밀번호 확인 |
| 4 | 이전 연락처에 통지 |

로그인한 상태에서만 잇게 하면 이 문제 대부분이 사라진다. 외부 로그인으로 처음 들어온 사람에게 "기존 계정이 있나요"라고 묻고 그쪽으로 로그인하게 하는 흐름이다.

## 끊는 것도 함께 설계한다

| 상황 | 처리 |
| --- | --- |
| 유일한 로그인 수단 | 끊기 전 다른 수단 요구 |
| 여러 수단 보유 | 자유롭게 끊기 |
| 끊은 뒤 재연결 | 같은 절차 다시 |

유일한 수단을 끊게 두면 사용자가 계정에 들어올 길을 스스로 없앤다. 끊기 전에 비밀번호를 설정하게 하거나 다른 제공자를 먼저 붙이게 한다.

![연결과 해제](/img/posts/account-linking-safety-2.svg)

## 현재 상태를 본다

\`\`\`sql
-- 로그인 수단이 하나뿐인 계정 — 끊기 제한이 필요한 대상
SELECT count(*) AS 단일수단_계정
FROM users u
WHERE (SELECT count(*) FROM identities i WHERE i.user_id = u.id) = 1
  AND u.password_hash IS NULL;

-- 같은 이메일에 여러 계정 — 병합 사고의 흔적일 수 있다
SELECT email, count(*) FROM users GROUP BY email HAVING count(*) > 1;
\`\`\`

단일 수단 계정이 많다면 해제 제한을 먼저 넣는 편이 낫다. 지원 문의 중 상당수가 여기서 나온다.

## 제공자가 주는 식별자를 기준으로

이메일이 아니라 제공자가 주는 변하지 않는 식별자를 저장해 두고 그것으로 잇는다. 이메일은 표시용이다. 사용자가 제공자 쪽에서 주소를 바꿔도 연결이 유지되고, 주소가 재사용돼도 남이 올라타지 않는다.

## 참고

- OAuth 2.0 Security Best Current Practice
- OpenID Connect Core — 주소 확인 여부
- OWASP — 계정 관리 Cheat Sheet`,
    diagram: {
      type: 'flow',
      caption: '이메일만으로 잇는 위험',
      steps: [
        { label: '외부 계정 생성', note: '남의 주소로' },
        { label: '주소 확인 없음', note: '제공자에 따라' },
        { label: '우리가 이메일로 매칭', note: '같은 사람으로 판단', danger: true },
        { label: '기존 계정 접근', note: '탈취 성립' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '연결과 해제',
      x: ['수단 여럿', '하나뿐'],
      y: ['연결', '해제'],
      cells: ['자유', '로그인 후에만', '자유', '다른 수단 먼저'],
    },
  },
  {
    slug: 'impersonation-controls',
    title: '지원 담당이 사용자 계정으로 들어갈 때',
    body: `고객 문의를 해결하려면 그 사용자가 보는 화면을 봐야 할 때가 있다. 담당자가 사용자 계정으로 들어가는 기능은 그래서 만들어지는데, 통제 없이 두면 가장 조용한 내부 접근 경로가 된다.

## 접근 통제에서 무엇을 정하나

| 항목 | 내용 |
| --- | --- |
| 누가 | 지정된 역할만 |
| 언제 | 문의가 열려 있는 동안 |
| 누구에게 | 그 문의의 당사자만 |
| 무엇을 | 읽기 위주, 쓰기는 제한 |
| 얼마나 | 짧은 제한 시간 |

"그 문의의 당사자만"이 핵심이다. 대상 제한이 없으면 담당자가 아무 계정에나 들어갈 수 있다.

![통제 없는 대리 접근](/img/posts/impersonation-controls.svg)

## 기록을 어떻게 남기나

대리 접근 중에 일어난 일은 사용자가 한 것으로 기록되면 안 된다. 누가 대신 했는지가 함께 남아야 한다.

| 기록 항목 | 내용 |
| --- | --- |
| 실제 행위자 | 담당자 계정 |
| 대상 계정 | 사용자 |
| 사유 | 문의 번호 |
| 시작·종료 시각 | 구간 |
| 수행한 작업 | 전부 |

감사 로그에 사용자 이름만 남으면, 나중에 그 사용자가 하지 않았다고 할 때 확인할 방법이 없다.

## 사용자에게 알린다

| 방식 | 효과 |
| --- | --- |
| 사전 동의 | 가장 투명, 번거로움 |
| 사후 통지 | 균형 |
| 화면에 표시 | 접속 중임을 알림 |
| 기록만 | 최소 |

사후 통지가 실무적 균형이다. "문의 처리를 위해 담당자가 계정에 접근했습니다"라는 메일 한 통이 신뢰를 지킨다.

![대리 접근 기록](/img/posts/impersonation-controls-2.svg)

## 화면에 표시한다

\`\`\`sql
-- 대리 접근 세션은 별도로 표시해 둔다
CREATE TABLE impersonation (
  id         UUID PRIMARY KEY,
  actor_id   BIGINT NOT NULL,
  target_id  BIGINT NOT NULL,
  ticket     TEXT NOT NULL,
  started_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  ended_at   TIMESTAMPTZ,
  CHECK (actor_id <> target_id)
);

-- 아직 끝나지 않은 접근 — 방치된 것이 있는지
SELECT * FROM impersonation WHERE ended_at IS NULL
  AND started_at < now() - interval '1 hour';
\`\`\`

한 시간 넘게 끝나지 않은 대리 접근은 닫는 것을 잊은 것이다. 제한 시간을 두고 자동으로 끊는 편이 낫다.

## 할 수 없는 일을 정한다

대리 접근 중에는 비밀번호 변경, 이메일 변경, 출금, 탈퇴처럼 되돌리기 어려운 작업을 막는다. 문의 해결에 필요한 것은 대개 조회이고, 변경이 필요하면 담당자 권한으로 별도 절차를 거치게 한다. 이 구분이 없으면 대리 접근이 만능 열쇠가 된다.

## 참고

- NIST SP 800-53 — 감사와 책임 추적성
- OWASP — 접근 통제 Cheat Sheet
- 개인정보보호법 — 접근 권한의 차등 부여`,
    diagram: {
      type: 'flow',
      caption: '통제 없는 대리 접근',
      steps: [
        { label: '담당자가 기능 사용', note: '대상 제한 없음' },
        { label: '아무 계정에나 진입', note: '사유 기록 없음', danger: true },
        { label: '사용자 이름으로 기록', note: '누가 했는지 모름' },
        { label: '사용자는 모름', note: '통지 없음' },
      ],
    },
    diagram2: {
      type: 'steps',
      caption: '대리 접근 기록',
      steps: [
        { label: '실제 행위자', note: '담당자 계정' },
        { label: '사유', note: '문의 번호' },
        { label: '구간', note: '시작·종료' },
        { label: '사후 통지', note: '사용자에게' },
      ],
    },
  },
  {
    slug: 'signup-abuse-control',
    title: '가입 단계에서 걸러야 하는 것',
    body: `가입 화면은 아무나 접근할 수 있는 자리라 자동화된 시도가 가장 먼저 닿는다. 걸러 내지 않으면 가짜 계정이 쌓이고, 그 계정들이 다른 기능의 남용 통로가 된다.

## 어떤 남용이 들어오나

| 유형 | 목적 |
| --- | --- |
| 대량 자동 가입 | 이벤트·쿠폰 수령 |
| 일회용 주소 가입 | 추적 회피 |
| 기존 계정 확인 | 주소 존재 여부 탐색 |
| 스팸 발송 준비 | 프로필·댓글 도배 |

가입 화면이 계정 존재 여부를 알려 주는 도구로 쓰이는 경우가 특히 흔하다. "이미 가입된 주소입니다"라는 안내가 그 역할을 한다.

![가입 화면으로 들어오는 것](/img/posts/signup-abuse-control.svg)

## 존재 여부를 감추면서 안내하기

| 상황 | 처리 |
| --- | --- |
| 새 주소 | 확인 메일 발송 |
| 이미 가입된 주소 | 같은 문구, 다른 내용의 메일 |
| 화면 안내 | 두 경우 동일 |

이미 가입된 주소에는 "이 주소로 가입 시도가 있었습니다"라는 메일을 보낸다. 화면은 같게 답하면서 본인에게는 알리는 방법이다.

## 레이트 리밋 등 자동화를 줄이는 수단

| 수단 | 마찰 | 효과 |
| --- | --- | --- |
| 요청 제한 | 낮음 | 중간 |
| 메일 확인 필수 | 낮음 | 높음 |
| 일회용 주소 차단 | 중간 | 중간 |
| 자동 판별 도구 | 중간 | 높음 |
| 전화 확인 | 높음 | 매우 높음 |

메일 확인을 필수로 두는 것이 값싸고 효과가 크다. 확인 전에는 아무 기능도 쓸 수 없게 하면 가짜 계정이 쌓여도 해가 적다.

![수단별 마찰과 효과](/img/posts/signup-abuse-control-2.svg)

## 지금 얼마나 들어오는지 본다

\`\`\`sql
-- 시간대별 가입 수 — 자동화는 사람과 다른 모양을 그린다
SELECT date_trunc('hour', created_at) AS 시각, count(*) AS 가입
FROM users WHERE created_at > now() - interval '7 days'
GROUP BY 1 ORDER BY 2 DESC LIMIT 10;

-- 확인하지 않은 계정 비율
SELECT count(*) FILTER (WHERE email_verified_at IS NULL) * 100.0 / count(*) AS 미확인_비율
FROM users WHERE created_at > now() - interval '30 days';
\`\`\`

새벽 시간대에 몰린 봉우리가 있으면 자동화다. 미확인 비율이 높으면 가입만 하고 버려진 계정이 쌓이는 중이다.

## 쌓인 가짜 계정을 어떻게 하나

확인하지 않은 계정을 일정 기간 뒤에 정리하면 목록이 깨끗해진다. 다만 지우기 전에 그 주소로 알림을 한 번 보낸다 — 진짜 사용자가 확인 메일을 놓쳤을 수 있다. 정리 자체보다, 정리할 수 있는 상태를 유지하는 것이 목적이다.

## 참고

- OWASP — 인증 Cheat Sheet
- NIST SP 800-63A — 신원 확인 수준
- CWE-799 — 상호작용 빈도 제어 실패`,
    diagram: {
      type: 'layers',
      caption: '가입 화면으로 들어오는 것',
      layers: [
        { label: '정상 가입', note: '대부분' },
        { label: '대량 자동 가입', note: '혜택 수령' },
        { label: '존재 여부 탐색', note: '안내 문구 악용' },
        { label: '도배 준비', note: '이후 남용' },
      ],
    },
    diagram2: {
      type: 'matrix',
      caption: '수단별 선택',
      x: ['마찰 낮음', '높음'],
      y: ['효과 큼', '작음'],
      cells: ['먼저 적용', '필요할 때', '보조', '쓰지 않음'],
    },
  },
]
