const R = String.raw
export default {

'detection-rule-lifecycle': R`## 규칙을 코드로 관리하기

콘솔에서 직접 만드는 방식은 이력이 남지 않고 시험할 수 없다. 규칙을 코드로 옮기면 수명 주기가 자연히 붙는다.

| 항목 | 콘솔 | 코드 |
| --- | --- | --- |
| 변경 이력 | 없음 | 있음 |
| 리뷰 | 없음 | 가능 |
| 시험 | 어려움 | 자동화 |
| 되돌리기 | 수동 | 즉시 |
| 문서화 | 별도 | 함께 |

규칙 파일에 목적과 대응 절차를 주석으로 함께 두면 문서가 흩어지지 않는다.

## 규칙 품질 기준

새 규칙을 운영으로 올릴 때 확인할 항목을 정해 둔다.

| 항목 | 기준 |
| --- | --- |
| 목적 기술 | 한 줄로 |
| 대응 절차 | 있어야 함 |
| 시험 기간 오탐률 | 20% 이하 |
| 매칭 확인 | 재현으로 검증 |
| 검토 주기 | 지정 |

대응 절차가 없는 규칙은 경보만 만든다. 탐지되면 무엇을 할지 정하지 않았다면 아직 운영할 준비가 안 된 것이다.`,

'alert-triage-workflow': R`## 자동 종결 기준

무엇을 자동으로 닫을지가 처리량을 정한다. 근거 없이 닫으면 위험하고, 닫지 않으면 감당이 안 된다.

| 조건 | 자동 종결 |
| --- | --- |
| 알려진 정상 작업과 일치 | 가능 |
| 점검 도구 출발지 | 가능 |
| 과거 동일 건 오탐 판정 | 가능 — 기간 제한 |
| 저위험 + 자산 중요도 낮음 | 검토 후 |
| 새로운 패턴 | 불가 |

세 번째는 기간을 정한다. 한 달 전 오탐이 지금도 오탐이라는 보장은 없다.

## 인수인계

교대 시점에 정보가 끊기면 처음부터 다시 본다.

| 항목 | 전달 |
| --- | --- |
| 진행 중 건 | 현재 판단과 다음 단계 |
| 감시 중 항목 | 왜 보고 있나 |
| 예정 작업 | 오탐 예상 |
| 미해결 문의 | 누구에게 무엇을 |

전달 양식을 고정하면 빠뜨리지 않는다. 구두 인수인계는 시간이 지나며 내용이 줄어든다.`,

'log-normalization': R`## 어디까지 정규화할지 정하기

전부 통일하려면 비용이 크고, 그대로 두면 조사가 어렵다. 조사에 필요한 것만 맞춘다.

| 필드 | 통일 | 이유 |
| --- | --- | --- |
| 시각 | 필수 | 순서 |
| 주체 | 필수 | 추적 |
| 대상 | 필수 | 범위 |
| 동작 | 필수 | 무엇을 |
| 결과 | 필수 | 성공·실패 |
| 출발지 | 필수 | 어디서 |
| 나머지 | 원본 유지 | 필요 시 추출 |

여섯 개만 맞추면 교차 조회가 된다. 나머지를 통일하려다 프로젝트가 커진다.

## 파싱 규칙 관리

규칙도 코드처럼 관리한다. 시험 데이터를 두고 변경 시 확인한다.

| 항목 | 방법 |
| --- | --- |
| 규칙 저장 | 저장소 |
| 시험 데이터 | 실제 로그 표본 |
| 변경 검증 | 자동 시험 |
| 실패율 감시 | 지표 |
| 원본 보관 | 재추출 대비 |

원본을 버리지 않는 것이 중요하다. 나중에 다른 필드가 필요해지면 다시 추출할 수 있다.`,

'security-metrics-that-matter': R`## 지표를 쓰는 자리

같은 지표라도 누가 보느냐에 따라 다르게 구성한다.

| 대상 | 지표 수 | 성격 |
| --- | --- | --- |
| 경영진 | 3~5개 | 위험·추이 |
| 보안팀 | 10개 이내 | 운영 상태 |
| 개발팀 | 팀별 2~3개 | 자기 영역 |
| 감사 | 통제별 | 준수 여부 |

개발팀에는 자기 영역 지표만 보여 준다. 전사 지표를 보여 주면 자기 일이 아니라고 느낀다.

## 목표를 정하는 방법

절대값 목표는 조직마다 의미가 다르다. 개선 방향으로 정한다.

| 방식 | 예 |
| --- | --- |
| 절대값 | 미조치 고위험 0건 — 비현실적 |
| 개선률 | 분기 대비 30% 감소 |
| 상한 | 30일 초과 건 0 |
| 추이 | 3분기 연속 감소 |

상한 방식이 실용적이다. 총량보다 기한을 넘긴 것이 없게 하는 편이 관리 가능하다.`,

'threat-hunting-hypothesis': R`## 가설을 어디서 얻나

매번 새로 생각하기 어렵다. 정기적으로 가설이 나오는 경로를 만든다.

| 경로 | 주기 |
| --- | --- |
| 탐지 규칙이 없는 기법 목록 | 분기 검토 |
| 최근 위협 정보 | 월 |
| 사고 회고의 유사 경로 | 사고 후 |
| 새로 추가된 자산 | 배포 후 |
| 가시성 공백 목록 | 분기 |

첫 줄이 가장 안정적이다. 공격 기법 목록과 우리 탐지 규칙을 대조하면 빈 곳이 그대로 가설이 된다.

## 시간을 확보하는 방법

헌팅은 늘 급한 일에 밀린다. 일정에 고정하지 않으면 하지 않게 된다.

| 방법 | 효과 |
| --- | --- |
| 격주 반나절 고정 | 유지됨 |
| 2인 1조 | 관점 보완 |
| 산출물 의무화 | 결과 축적 |
| 결과 공유 | 가치 인식 |

혼자 하면 놓치는 것이 많다. 둘이 하면 서로 다른 각도에서 보게 되고 논의 자체가 가설을 만든다.`,

'siem-tuning': R`## 제외 조건의 위험

제외는 편하지만 탐지 공백을 만든다. 무엇을 포기하는지 알고 결정한다.

| 제외 방식 | 공백 |
| --- | --- |
| 출발지 주소 | 그 주소에서 오는 실제 공격 |
| 계정 | 그 계정이 침해된 경우 |
| 시간대 | 그 시간의 공격 |
| 규칙 전체 | 해당 기법 전부 |

계정 단위 제외가 특히 위험하다. 점검 도구 계정을 제외하면 그 계정이 탈취됐을 때 아무것도 보이지 않는다.

## 대체 탐지 확보

제외한 영역을 다른 방법으로 볼 수 있는지 확인한다.

| 제외 | 대체 |
| --- | --- |
| 점검 도구 활동 | 도구 자체의 실행 기록 |
| 배포 파이프라인 | 배포 이력과 대조 |
| 모니터링 에이전트 | 에이전트 상태 감시 |
| 정기 배치 | 실행 일정과 대조 |

대체 수단이 없으면 제외하지 않고 등급만 낮춘다. 기록은 남으므로 사후 조사가 가능하다.`,

'canary-token-usage': R`## 어디에 심을지 정하기

공격자가 반드시 지나는 곳에 심어야 효과가 있다. 침해 단계별로 배치한다.

| 침해 단계 | 심을 곳 |
| --- | --- |
| 초기 정찰 | 공개 저장소의 가짜 설정 |
| 자격 증명 탐색 | 서버 설정 파일 |
| 내부 이동 | 공유 폴더의 가짜 문서 |
| 데이터 탐색 | 데이터베이스의 가짜 레코드 |
| 반출 | 추적 링크가 든 문서 |

설정 파일의 가짜 자격 증명이 가장 확실하다. 침해자가 반드시 뒤지는 곳이고, 발견하면 반드시 시도한다.

## 운영 시 확인

심어 두고 방치하면 나중에 진짜와 헷갈린다.

| 항목 | 주기 |
| --- | --- |
| 배치 목록 확인 | 분기 |
| 경보 도달 시험 | 분기 |
| 정상 업무 사용 여부 | 초기 1주 |
| 그럴듯함 점검 | 반기 |

경보가 실제로 도달하는지 시험한다. 심어 두었는데 알림 경로가 끊겨 있으면 아무 의미가 없다.`,

'baseline-establishment': R`## 기준선 수집 중 발견되는 것

기준선을 만드는 과정 자체가 정리 작업이 된다. 예상 밖의 것이 나온다.

| 발견 | 빈도 |
| --- | --- |
| 아무도 모르던 정기 작업 | 높음 |
| 미사용 계정의 주기적 접속 | 높음 |
| 예상보다 큰 외부 전송 | 중간 |
| 퇴사자 계정 활동 | 중간 |
| 잘못된 시각 설정 | 중간 |

탐지를 시작하기 전에 이미 정리할 것이 나온다. 이 단계에서 발견한 것을 먼저 처리하면 기준선도 깨끗해진다.

## 갱신 시점 판단

기준선이 낡으면 오탐이 늘어난다. 무엇이 바뀌면 다시 만들지 정해 둔다.

| 변화 | 갱신 |
| --- | --- |
| 조직 개편 | 즉시 |
| 새 서비스 출시 | 해당 영역 |
| 근무 형태 변경 | 즉시 |
| 계절성 이벤트 | 별도 기준 |
| 일상적 변화 | 분기 |

원격 근무 전환처럼 접속 패턴이 통째로 바뀌는 변화가 있으면 기존 기준선은 버린다.`,

'attack-simulation-purple': R`## 어떤 기법을 고르나

전부 시험할 수 없다. 우리 환경에 해당하고 위험이 큰 것부터 한다.

| 기준 | 우선순위 |
| --- | --- |
| 우리 업종에서 관측된 기법 | 1 |
| 최근 침해 사례의 기법 | 2 |
| 탐지 규칙이 있다고 믿는 것 | 3 |
| 자산 가치가 큰 경로 | 4 |
| 규칙이 없는 영역 | 5 |

3번이 의외로 많은 문제를 드러낸다. 있다고 믿었던 탐지가 동작하지 않는 것을 발견하는 것이 이 활동의 핵심 가치다.

## 안전 조건

재현이 실제 사고가 되지 않게 한다.

| 항목 | 준비 |
| --- | --- |
| 승인 | 서면 |
| 범위 | 대상 명시 |
| 파괴적 행위 | 금지 |
| 중단 기준 | 사전 정의 |
| 대응팀 통지 | 목적에 따라 |
| 사후 정리 | 흔적 제거 |

사후 정리를 빠뜨리면 나중에 진짜 침해로 오인된다. 재현으로 남긴 파일과 계정을 목록으로 관리하고 정리한다.`,

'log-integrity-protection': R`## 보호 수준 정하기

전부에 같은 수준을 적용하면 비용이 크다. 조사와 규정 요구를 기준으로 나눈다.

| 로그 | 보호 수준 |
| --- | --- |
| 감사·인증 기록 | 최고 — 잠금·해시 체인 |
| 접근 기록 | 높음 — 별도 계정 보관 |
| 애플리케이션 로그 | 중간 — 원격 전송 |
| 디버그 로그 | 낮음 |

법령이 접속 기록의 위변조 방지를 요구하는 경우가 있다. 해당 항목을 먼저 확인하고 그에 맞춘다.

## 검증 기록

무결성을 주장하려면 검증한 기록이 있어야 한다.

| 항목 | 주기 |
| --- | --- |
| 해시 검증 | 월 |
| 체인 연속성 | 월 |
| 전송 누락 | 매일 |
| 보관 정책 적용 | 분기 |
| 검증 결과 보관 | 로그와 동일 기간 |

검증 자체가 자동화되어야 유지된다. 결과를 남기고 실패 시 알림을 받는다.`,
}
