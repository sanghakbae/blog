const R = String.raw
export default {

'detection-rule-lifecycle': R`## 실제로 이렇게 터진다

로그 형식이 바뀌면서 규칙 열두 개가 아무것도 매칭하지 않게 된 사례가 있다. 오류가 나지 않으니 아무도 몰랐고, 반년 뒤 침해 조사에서 그 영역이 비어 있는 것을 발견했다.

또 하나는 작성자가 퇴사한 뒤 규칙의 의도를 아무도 몰라 손대지 못한 경우다. 오탐이 계속 나왔지만 지울 수도 고칠 수도 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 규칙은 만들면 유지된다 | 조용히 깨진다 |
| 매칭이 없으면 안전하다 | 규칙이 죽었을 수 있다 |
| 오탐은 참으면 된다 | 전체를 무시하게 된다 |
| 규칙 수가 성과다 | 동작하는 수가 성과다 |

## 건강 점검

| 항목 | 주기 |
| --- | --- |
| 무매칭 규칙 | 월 |
| 오탐률 상위 | 월 |
| 대응 절차 유무 | 분기 |
| 작성 의도 기록 | 작성 시 |
| 검토 기한 초과 | 분기 |`,

'alert-triage-workflow': R`## 실제로 이렇게 터진다

경보가 하루 300건 오던 조직에서 담당자가 상위 몇 건만 보고 나머지를 일괄 종료한 사례가 있다. 그 안에 실제 침해 신호가 있었고, 몇 주 뒤 다른 경로로 발견됐다.

또 하나는 자동 보강 없이 운영한 경우다. 경보 하나를 판단하는 데 여러 화면을 열어야 해서 15분씩 걸렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 사람을 늘리면 된다 | 잡음을 줄이는 것이 먼저다 |
| 전부 봐야 안전하다 | 다 못 보면 대충 본다 |
| 자동 종결은 위험하다 | 근거가 있으면 안전하다 |
| 보강은 부가 기능이다 | 처리 시간을 절반으로 줄인다 |

## 처리량 기준

| 지표 | 목표 |
| --- | --- |
| 담당자당 일일 검토 | 20건 이하 |
| 자동 종결 비율 | 늘린다 |
| 미처리 잔여 | 0 |
| 판정까지 시간 | 등급별 |`,

'log-normalization': R`## 실제로 이렇게 터진다

시스템마다 시각 형식이 달라 조사에서 순서를 잘못 읽은 사례가 있다. 한 시스템이 로컬 시각을 쓰고 있었고, 원인과 결과가 뒤바뀐 결론이 나왔다.

또 하나는 정규화 과정에서 원본을 버린 경우다. 나중에 다른 필드가 필요해졌는데 다시 뽑을 수 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 시각은 대충 맞으면 된다 | 순서가 결론을 바꾼다 |
| 정규화하면 원본은 필요 없다 | 손실된 정보가 있다 |
| 파싱은 한 번 만들면 된다 | 형식이 바뀐다 |
| 필드 이름은 자유다 | 교차 조회가 안 된다 |

## 최소 공통 필드

| 필드 | 이유 |
| --- | --- |
| 시각 | UTC, 밀리초 |
| 주체 | 누가 |
| 대상 | 무엇에 |
| 동작 | 무엇을 |
| 결과 | 성공·실패 |
| 출발지 | 어디서 |`,

'security-metrics-that-matter': R`## 실제로 이렇게 터진다

차단 건수를 성과 지표로 삼은 조직에서 규칙을 넓게 걸어 숫자를 올린 사례가 있다. 보고서의 숫자는 올랐고 오탐이 늘어 실제 대응은 나빠졌다.

또 하나는 교육 이수율 100%를 달성했지만 피싱 훈련 신고율이 그대로였던 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 측정하면 개선된다 | 그 숫자만 개선된다 |
| 많이 볼수록 좋다 | 다섯 개를 넘기면 안 본다 |
| 절대값이 중요하다 | 추이가 중요하다 |
| 지표는 평가용이다 | 개선 방향용이다 |

## 지표 점검

| 질문 | 확인 |
| --- | --- |
| 방향이 분명한가 | 늘면 좋은가 나쁜가 |
| 쉽게 올릴 수 있나 | 왜곡 가능성 |
| 위험과 연결되나 | 실질성 |
| 자동 수집되나 | 지속성 |`,

'threat-hunting-hypothesis': R`## 실제로 이렇게 터진다

데이터를 열어 놓고 이상한 것을 찾자는 방식으로 하루를 쓴 사례가 있다. 결과는 없었고 무엇을 확인했는지도 남지 않았다.

또 하나는 가설을 세웠지만 필요한 로그가 없어 확인하지 못한 경우다. 그 사실 자체가 중요한 발견이었는데 기록하지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 데이터를 보면 찾는다 | 가설이 있어야 한다 |
| 결과가 없으면 실패다 | 깨끗함을 확인한 것이다 |
| 도구가 있으면 된다 | 질문이 먼저다 |
| 전문가만 한다 | 환경을 아는 사람이 낫다 |

## 산출물

| 항목 | 활용 |
| --- | --- |
| 가설과 결과 | 다음 회차 |
| 질의 | 재사용 |
| 가시성 공백 | 수집 개선 |
| 새 규칙 | 자동화 |
| 정상 패턴 | 오탐 감소 |`,

'siem-tuning': R`## 실제로 이렇게 터진다

도입 첫 주에 경보가 하루 수천 건 나온 사례가 있다. 조정하지 않고 두 달이 지나자 담당자가 경보 채널을 음소거했고, 그 뒤로는 무엇을 해도 보지 않았다.

또 하나는 제외 조건을 계정 단위로 넣은 경우다. 그 계정이 침해됐을 때 탐지에서 완전히 빠졌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 시간이 지나면 줄어든다 | 조정해야 준다 |
| 제외는 안전하다 | 공백을 만든다 |
| 규칙은 끄면 안 된다 | 안 맞으면 끄는 게 낫다 |
| 처음 며칠은 원래 그렇다 | 그때 조정해야 한다 |

## 조정 기록

| 항목 | 남길 것 |
| --- | --- |
| 무엇을 제외했나 | 조건 |
| 왜 | 근거 |
| 무엇을 포기했나 | 공백 |
| 대체 수단 | 있는가 |
| 재검토 시점 | 언제 |`,

'canary-token-usage': R`## 실제로 이렇게 터진다

설정 파일에 가짜 자격 증명을 심어 둔 조직에서, 침해 발생 20분 만에 경보가 왔다. 공격자가 설정 파일을 뒤지다 그것을 시도했고, 다른 어떤 탐지보다 먼저 울렸다.

반대로 어디에 무엇을 심었는지 기록하지 않아, 나중에 정리하지 못하고 진짜 자격 증명과 헷갈린 경우도 있다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 정교한 도구가 필요하다 | 가짜 키 하나면 된다 |
| 오탐이 걱정된다 | 정상 경로가 없다 |
| 큰 조직만 한다 | 예산이 적을수록 유용하다 |
| 심으면 끝이다 | 목록 관리가 필요하다 |

## 배치 기록

| 항목 | 기록 |
| --- | --- |
| 무엇을 | 토큰 유형 |
| 어디에 | 시스템·경로 |
| 언제 | 심은 날 |
| 담당 | 누가 |
| 경보 대상 | 누구에게 |`,

'baseline-establishment': R`## 실제로 이렇게 터진다

기준선 없이 임계값을 감으로 정한 사례가 있다. 월말마다 정상 배치가 경보를 냈고, 담당자가 그 경보를 무시하는 습관이 생겼다. 실제 이상이 같은 시기에 발생했을 때도 넘겼다.

또 하나는 기준선을 만드는 과정에서 아무도 모르던 정기 외부 전송을 발견한 경우다. 몇 년째 돌던 작업이었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 평균이면 충분하다 | 주기성을 반영해야 한다 |
| 한 번 만들면 된다 | 조직이 바뀌면 낡는다 |
| 기준선은 탐지 준비물이다 | 그 자체로 발견이 있다 |
| 데이터가 많아야 한다 | 4주면 시작할 수 있다 |

## 우선 대상

| 순위 | 대상 | 이유 |
| --- | --- | --- |
| 1 | 인증 | 계정 탈취 |
| 2 | 데이터 조회량 | 유출 |
| 3 | 외부 전송량 | 유출 |
| 4 | 관리 동작 | 권한 오용 |`,

'attack-simulation-purple': R`## 실제로 이렇게 터진다

탐지 규칙이 있다고 믿었던 기법을 재현해 봤더니 아무 경보도 오지 않은 사례가 있다. 로그는 수집되고 있었는데 파싱 규칙이 필드를 잘못 뽑아 규칙이 매칭되지 않았다.

또 하나는 대응팀에 알리지 않고 재현했다가 실제 사고로 오인돼 비상 소집이 걸린 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 규칙이 있으면 탐지된다 | 사슬 어디서든 끊긴다 |
| 도구를 사면 검증된다 | 우리 환경에서 봐야 한다 |
| 대응팀에 알리면 의미 없다 | 목적에 따라 다르다 |
| 한 번 하면 된다 | 변경마다 깨진다 |

## 사슬 점검

| 단계 | 확인 |
| --- | --- |
| 로그 생성 | 이벤트가 남나 |
| 전송 | 중앙에 왔나 |
| 파싱 | 필드가 맞나 |
| 규칙 | 매칭되나 |
| 경보 | 도달했나 |
| 대응 | 조치했나 |`,

'log-integrity-protection': R`## 실제로 이렇게 터진다

침해자가 로컬 로그를 지운 사례가 있다. 중앙 전송이 5분 주기 배치였고, 그 사이 기록이 사라졌다. 초기 침투 시점을 특정하지 못했다.

또 하나는 에이전트가 멈춘 것을 아무도 몰랐던 경우다. 로그가 없는 것이 조용해서인지 수집이 끊겨서인지 구분할 수 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 로그가 있으면 증거다 | 무결성 증명이 필요하다 |
| 주기 전송이면 충분하다 | 그 사이가 비어 있다 |
| 없는 것은 조용한 것이다 | 끊긴 것일 수 있다 |
| 같은 계정에 둬도 된다 | 함께 지워진다 |

## 최소 구성

| 항목 | 설정 |
| --- | --- |
| 전송 | 즉시 |
| 보관 | 별도 계정 |
| 삭제 | 불가 설정 |
| 주기 신호 | 끊김 감지 |
| 검증 | 정기 해시 확인 |`,
}
