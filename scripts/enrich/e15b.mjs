const R = String.raw
export default {

'secret-scanning': R`## 실제로 이렇게 터진다

접근 키를 커밋한 지 4분 만에 사용된 사례가 있다. 공개 저장소를 실시간으로 훑는 도구가 있어서 사람이 알아채기 전에 쓰인다. 커밋을 되돌려도 이력에 남고, 이미 복제된 사본은 회수할 수 없다.

비공개 저장소라 안심한 경우도 있다. 접근 권한자가 200명이었고, 그중 한 명의 계정이 침해됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 커밋을 되돌리면 지워진다 | 이력에 남는다 |
| 비공개라 안전하다 | 권한자 전원이 볼 수 있다 |
| 지우고 나서 회전하면 된다 | 회전이 먼저다 |
| 검사 도구가 다 잡는다 | 형식이 없는 값은 못 잡는다 |
| 이력 정리로 해결된다 | 복제본은 회수 못 한다 |

## 어디에 검사를 두는가

| 시점 | 효과 |
| --- | --- |
| 커밋 전 훅 | 들어가는 것을 막는다 |
| 푸시 전 훅 | 로컬 커밋도 잡는다 |
| 서버 측 검사 | 우회 불가 |
| 저장소 전체 이력 | 이미 있는 것을 찾는다 |
| 공개 감시 | 외부에 노출된 우리 값 |

커밋 전 훅은 우회할 수 있으므로 서버 측 검사가 함께 있어야 한다.

## 발견했을 때의 순서

순서가 중요하다. 지우는 것이 먼저가 아니다.

1. 그 자격 증명을 즉시 무효화한다
2. 사용 기록을 확인한다 — 이미 쓰였는지
3. 새 값을 발급하고 배포한다
4. 이력에서 제거한다 — 필요하면
5. 왜 들어갔는지 확인하고 절차를 고친다

\`\`\`bash
# 저장소 전체 이력에서 찾기
git log -p --all | grep -nE '(AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-|-----BEGIN [A-Z ]*PRIVATE KEY-----)' | head -20

# 커밋 전 훅 — 들어가는 것을 막는다
cat > .git/hooks/pre-commit <<'H'
#!/bin/sh
if git diff --cached | grep -nE '(AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|-----BEGIN [A-Z ]*PRIVATE KEY-----)'; then
  echo '자격 증명으로 보이는 값이 있습니다. 확인 후 커밋하세요.'
  exit 1
fi
H
chmod +x .git/hooks/pre-commit
\`\`\`

훅은 각자 설치해야 하므로 강제 수단이 아니다. 서버 측 검사와 함께 쓴다.`,

'security-regression-test': R`## 실제로 이렇게 터진다

인가 누락을 고쳤는데 세 달 뒤 같은 자리에 다시 생긴 사례가 있다. 리팩터링 과정에서 조건이 빠졌고, 시험이 없어서 아무도 몰랐다. 같은 취약점을 두 번 진단받았다.

시험을 만들었는데 무의미했던 경우도 있다. 정상 요청만 확인하고 비정상 요청이 거부되는지는 보지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 고쳤으니 끝이다 | 되돌아온다 |
| 기능 시험이 있으면 된다 | 거부되어야 할 것을 확인해야 한다 |
| 보안 시험은 어렵다 | 대부분 요청 하나와 기대 코드다 |
| 진단이 잡아 준다 | 주기가 길어 늦게 발견된다 |
| 시험이 느려진다 | 대상만 골라 돌리면 빠르다 |

## 무엇을 시험하는가

| 유형 | 시험 내용 |
| --- | --- |
| 인가 | 남의 자원 접근 시 404·403 |
| 인증 | 토큰 없음·만료 시 401 |
| 입력 | 경계값·형식 위반 거부 |
| 응답 | 민감 필드가 없는지 |
| 상태 전이 | 허용되지 않은 전이 거부 |
| 한도 | 초과 시 429 |

핵심은 "성공하면 안 되는 것이 실패하는지" 를 보는 것이다. 기능 시험은 반대만 본다.

## 진단 결과를 시험으로 바꾼다

발견된 취약점마다 시험을 하나 만들면, 같은 것이 다시 생기지 않는다.

\`\`\`ts
// 진단에서 나온 항목을 그대로 시험으로 옮긴다
describe('인가 회귀', () => {
  it('다른 사용자의 주문은 404 (진단 2026-03 #12)', async () => {
    const res = await request(app)
      .get(`/api/orders/\${orderOfUserB.id}`)
      .set('Authorization', `Bearer \${tokenOfUserA}`)
    expect(res.status).toBe(404)
  })

  it('응답에 내부 필드가 없다 (진단 2026-03 #18)', async () => {
    const res = await request(app).get('/api/me').set('Authorization', `Bearer \${tokenOfUserA}`)
    expect(Object.keys(res.body)).not.toEqual(
      expect.arrayContaining(['passwordHash', 'internalMemo', 'ssn']),
    )
  })
})
\`\`\`

시험 이름에 진단 번호를 적어 두면, 왜 이 시험이 있는지가 남는다.`,

'pentest-scoping': R`## 실제로 이렇게 터진다

범위를 넓게 잡아 며칠 만에 시간이 끝난 사례가 있다. 전 시스템을 대상으로 했지만 실제로는 표면만 훑었고, 결과는 스캐너 출력과 비슷했다.

반대로 범위를 좁혀 정작 위험한 곳이 빠진 경우도 있다. 인증 뒤 영역을 제외하고 공개 페이지만 봤다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 범위가 넓을수록 좋다 | 깊이가 사라진다 |
| 계정 없이 해야 실전이다 | 인증 뒤가 더 위험하다 |
| 취약점 수가 성과다 | 위험한 것 하나가 더 가치 있다 |
| 보고서를 받으면 끝이다 | 조치와 재점검이 남는다 |
| 매년 같은 범위로 한다 | 변경이 큰 곳을 골라야 한다 |

## 무엇을 정하는가

| 항목 | 내용 |
| --- | --- |
| 대상 | 도메인·API·앱, 제외 대상 명시 |
| 계정 | 역할별 2개씩 — 수평 경계 시험용 |
| 깊이 | 표면 점검인지 목표 지향인지 |
| 금지 행위 | 서비스 거부, 실 데이터 변경 |
| 기간·시간대 | 부하 시험 가능 시간 |
| 연락 체계 | 장애 시 즉시 연락 |
| 데이터 취급 | 확보한 자료의 보관·파기 |

역할별 계정 두 개가 특히 중요하다. 하나만 주면 수평 권한 경계를 시험할 수 없다.

## 목표 지향 방식

전 범위를 훑는 대신 목표를 정하는 방식이 결과가 낫다.

| 목표 예시 | 확인하는 것 |
| --- | --- |
| 일반 계정으로 관리자 권한 획득 | 권한 상승 경로 |
| 다른 고객사 데이터 열람 | 테넌트 경계 |
| 결제 금액 조작 | 업무 논리 |
| 내부망 도달 | 외부에서 내부로 |

## 결과를 어떻게 쓰는가

1. 항목마다 담당자와 기한을 정한다
2. 같은 원인의 다른 지점을 함께 찾는다
3. 각 항목을 회귀 시험으로 만든다
4. 조치 후 재점검을 요청한다
5. 반복되는 유형은 공통 계층에서 고친다

보고서를 받고 개별 수정만 하면 다음 진단에서 같은 유형이 다시 나온다.`,

'design-review-gate': R`## 실제로 이렇게 터진다

설계 검토를 필수 관문으로 만든 조직에서 검토 대기가 3주까지 밀린 사례가 있다. 모든 변경이 대상이었고 검토자는 한 명이었다. 팀들은 검토를 피하려고 변경을 잘게 쪼갰다.

반대로 선택 사항으로 두었더니 아무도 요청하지 않은 경우도 있다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 전부 검토해야 한다 | 신뢰 경계가 바뀔 때만 |
| 검토자가 전문가여야 한다 | 질문 목록이 있으면 팀이 한다 |
| 문서가 있어야 검토한다 | 그림 한 장이면 시작할 수 있다 |
| 관문이어야 지켜진다 | 우회 대상이 된다 |
| 오래 걸려야 꼼꼼하다 | 30분에 대부분 나온다 |

## 언제 검토하는가

| 상황 | 검토 |
| --- | --- |
| 새 외부 연동 | 필수 |
| 인증·인가 구조 변경 | 필수 |
| 새 데이터 저장소·새 개인정보 항목 | 필수 |
| 신뢰 경계 이동 | 필수 |
| 기존 패턴 재사용 | 자가 점검표 |
| 화면·문구 변경 | 불필요 |

대부분의 변경은 세 번째 열에 해당한다. 자가 점검표로 처리하면 검토 대기가 사라진다.

## 병목을 없애는 방법

1. 대상을 좁힌다 — 신뢰 경계가 바뀌는 것만
2. 자가 점검표를 만든다 — 대부분은 이것으로 끝
3. 검토자를 늘린다 — 각 팀의 보안 담당
4. 30분 시간 상자를 둔다 — 길어지면 다음으로
5. 결과를 결정 기록으로 남긴다 — 같은 논의를 반복하지 않게
6. 자주 나오는 지적은 기본값으로 만든다

6번이 근본 해법이다. 매번 같은 지적이 나온다면 검토가 아니라 라이브러리가 필요하다는 뜻이다.

## 자가 점검표

| 질문 | 해당하면 |
| --- | --- |
| 새로운 외부 입력을 받는가 | 검토 요청 |
| 인증·인가 판정이 바뀌는가 | 검토 요청 |
| 개인정보 항목이 늘어나는가 | 검토 요청 |
| 새 저장소·외부 서비스를 쓰는가 | 검토 요청 |
| 위 항목이 없는가 | 자가 점검으로 종료 |`,

'vendor-questionnaire': R`## 실제로 이렇게 터진다

100문항 점검표를 보내 받은 답변을 그대로 보관한 사례가 있다. 전부 "예" 였고 아무도 검증하지 않았다. 그 업체가 침해됐을 때 답변과 실제가 크게 달랐다.

반대로 점검표가 너무 길어 업체가 응답을 거부한 경우도 있다. 작은 업체에는 답할 인력이 없다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 문항이 많을수록 꼼꼼하다 | 응답 품질이 떨어진다 |
| 답변을 받으면 관리한 것이다 | 검증 없는 자가 진술이다 |
| 모든 업체에 같은 것을 보낸다 | 위험 등급별로 달라야 한다 |
| 인증서를 받으면 충분하다 | 범위를 확인해야 한다 |
| 한 번 받으면 끝이다 | 관계가 바뀌면 다시 본다 |

## 등급별로 무엇을 묻는가

| 등급 | 방식 | 문항 |
| --- | --- | --- |
| 낮음 | 계약 조항만 | 0 |
| 중간 | 간이 점검표 | 15~20 |
| 높음 | 상세 점검표 + 증적 | 40~60 |
| 매우 높음 | 실사 또는 제3자 보고서 | 현장 확인 |

대부분의 업체는 낮음·중간이다. 상세 점검은 데이터에 직접 접근하는 소수에만 한다.

## 검증 가능한 문항으로 만든다

"예·아니오" 로 끝나는 문항은 검증이 안 된다. 증적을 요구하는 형태로 바꾼다.

| 나쁜 문항 | 나은 문항 |
| --- | --- |
| 접근 통제를 하십니까 | 최근 권한 검토 결과를 제출해 주십시오 |
| 백업을 하십니까 | 최근 복구 시험 일자와 결과는 |
| 로그를 보관합니까 | 보관 기간과 항목을 명시해 주십시오 |
| 사고 대응 절차가 있습니까 | 통지 기한과 연락 경로를 적어 주십시오 |
| 암호화합니까 | 어떤 데이터에 어떤 방식으로 |

## 답변 뒤에 무엇을 하는가

1. 계약 조항과 대조한다 — 답변과 계약이 다르면 확인
2. 위험 항목은 증적을 요구한다
3. 미흡 항목에 개선 기한을 합의한다
4. 등급이 높은 업체는 연 1회 갱신한다
5. 사고나 관계 변화가 있으면 다시 본다

\`\`\`bash
# 계약과 점검 이력을 대조 — 점검이 만료된 업체를 찾는다
python3 - <<'PY'
import csv, datetime
today = datetime.date.today()
for r in csv.DictReader(open('vendors.csv')):
    last = r.get('last_assessed')
    if r['tier'] in ('high', 'critical'):
        if not last:
            print(r['name'], '점검 이력 없음')
        elif (today - datetime.date.fromisoformat(last)).days > 365:
            print(r['name'], '마지막 점검', last)
PY
\`\`\``,
}
