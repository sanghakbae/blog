const R = String.raw
export default {

'response-splitting': R`## 우선순위를 어떻게 정하나

이 취약점은 단독으로 발견되기보다 캐시 계층과 결합했을 때 심각해진다. 같은 결함이라도 앞에 공유 캐시가 있으면 피해가 한 사람에서 전체 사용자로 번진다. 조치 순서를 정할 때 이 조건을 먼저 본다.

| 조건 | 등급 |
| --- | --- |
| 공유 캐시 + 인증 응답 | 심각 |
| 공유 캐시 + 공개 응답 | 높음 |
| 캐시 없음 + 쿠키 설정 가능 | 높음 |
| 캐시 없음 + 이동만 가능 | 중간 |
| 내부 전용 경로 | 낮음 |

같은 코드 결함이라도 배치된 위치에 따라 등급이 두 단계 이상 달라진다. 조치 계획을 세울 때 코드가 아니라 경로 단위로 목록을 만든다.

## 재발을 막는 지점

한 곳을 고쳐도 다음 기능에서 같은 패턴이 다시 들어온다. 헤더를 직접 조합하는 코드를 금지 대상으로 정하고 정적 분석 규칙에 넣는다. 프레임워크가 제공하는 헤더 설정 함수만 쓰게 하면 개발자가 이 문제를 몰라도 안전해진다.

파일 이름처럼 값이 반드시 들어가야 하는 자리는 공통 함수로 만들어 그것만 쓰게 한다. 인코딩 규칙을 각자 구현하면 언젠가 빠뜨린다.`,

'websocket-authorization': R`## 규모에 따라 달라지는 선택

권한 확인을 매 메시지마다 조회할지 캐시할지는 연결 수와 메시지 빈도에 따라 다르다. 작은 서비스에서 복잡한 무효화 구조를 만들면 유지 부담만 커진다.

| 규모 | 권장 방식 |
| --- | --- |
| 연결 수백 | 메시지마다 조회 |
| 연결 수천 | 연결 시 캐시 + 짧은 만료 |
| 연결 수만 | 캐시 + 발행·구독 무효화 |
| 인스턴스 다중 | 중앙 무효화 필수 |

짧은 만료로 시작하는 것이 안전하다. 5분 만료면 권한 회수가 최대 5분 늦게 반영되고, 그 정도면 대부분의 서비스에서 수용 가능하다. 복잡한 무효화 구조는 실제로 지연이 문제가 된 뒤에 만든다.

## 연결 수 자체를 통제한다

한 계정이 수백 개 연결을 열면 자원이 고갈된다. 계정당 연결 수, 연결당 구독 수, 초당 메시지 수에 각각 상한을 둔다. 이 세 가지가 없으면 인증된 사용자 한 명이 서비스를 멈출 수 있다.`,

'cors-preflight-pitfalls': R`## 무엇을 열고 있는지 먼저 센다

설정을 고치기 전에 지금 몇 개의 출처가 허용돼 있는지, 그중 실제로 쓰이는 것이 몇 개인지 확인한다. 대부분의 조직에서 목록의 절반은 이미 쓰지 않는 것이다.

| 확인 | 방법 |
| --- | --- |
| 허용 목록 크기 | 설정 파일 |
| 실제 요청 출처 | 접근 로그의 Origin |
| 차이 | 정리 대상 |

쓰이지 않는 출처를 지우는 것은 위험이 낮으면서 공격면을 직접 줄인다. 목록이 짧아지면 검사 로직도 단순해진다.

## 게이트웨이와 애플리케이션의 분담

두 곳에서 각자 헤더를 붙이면 응답에 중복 헤더가 생기고 브라우저가 거부한다. 어느 계층이 담당할지 정하고 다른 쪽은 손대지 않는다.

| 구성 | 담당 |
| --- | --- |
| 게이트웨이 있음 | 게이트웨이 |
| 게이트웨이 없음 | 애플리케이션 |
| 정적 파일 서버 | 별도 설정 확인 |

정적 파일과 API 가 다른 경로로 서비스되면 설정이 갈라진다. 배포 후 두 경로 모두에서 응답을 확인한다.`,

'client-side-path-traversal': R`## 어떤 코드가 위험한가

같은 패턴이라도 값의 출처에 따라 위험이 다르다. 값이 어디서 오는지 추적하는 것이 판단의 기준이다.

| 값의 출처 | 위험 |
| --- | --- |
| 주소 표시줄 파라미터 | 높음 |
| 사용자 입력 필드 | 높음 |
| 외부 API 응답 | 중간 |
| 우리 서버 응답 | 낮음 — 단, 그 값의 출처를 본다 |
| 코드 내 상수 | 없음 |

우리 서버 응답이라도 그 값이 사용자 입력에서 만들어졌다면 같은 위험이다. 한 단계만 보지 말고 원천까지 따라간다.

## 요청 계층을 하나로 모으는 이득

공통 요청 계층을 만들면 인코딩뿐 아니라 다른 것도 한곳에서 해결된다. 인증 헤더를 붙일 대상을 제한하고, 기본 주소를 고정하고, 응답 오류를 일관되게 처리할 수 있다.

새 화면이 추가돼도 그 계층을 쓰기만 하면 같은 통제가 적용된다. 검토 대상이 화면 수가 아니라 계층 하나로 줄어든다.`,

'json-parser-differences': R`## 어디를 먼저 보나

모든 연동을 한 번에 점검할 수는 없다. 계층 사이에 검증과 처리가 분리된 지점부터 본다.

| 구성 | 우선순위 |
| --- | --- |
| 게이트웨이 검증 + 서비스 처리 | 1 |
| 서명 검증 + 본문 처리 | 1 |
| 언어가 다른 서비스 간 | 2 |
| 같은 언어·같은 라이브러리 | 3 |
| 단일 프로세스 내부 | 4 |

1순위 두 가지가 실제 우회로 이어지는 구성이다. 검증한 값과 사용하는 값이 다를 수 있는 구조인지가 판단 기준이다.

## 규약으로 고정한다

파서를 통일할 수 없다면 데이터 형식 쪽에서 차이를 줄인다. 연동 규약에 다음을 명시하고 스키마 검증으로 강제한다.

| 항목 | 규약 |
| --- | --- |
| 중복 키 | 거부 |
| 큰 정수 | 문자열 |
| 알 수 없는 필드 | 제거 |
| 날짜 | 고정 형식 문자열 |
| null 과 미포함 | 의미 구분 명시 |

마지막 항목이 자주 문제가 된다. 값이 없는 것과 null 인 것을 구현마다 다르게 다뤄 갱신 시 의도치 않은 삭제가 일어난다.`,

'file-upload-content-check': R`## 형식별 처리 방침

모든 파일을 같게 다루면 과하거나 부족하다. 형식마다 위험과 처리 방법이 다르다.

| 형식 | 위험 | 처리 |
| --- | --- | --- |
| 이미지 | 메타데이터·형식 혼동 | 재인코딩 |
| 벡터 이미지 | 스크립트 포함 | 정화 또는 금지 |
| 문서 | 내부 XML·매크로 | 격리 파싱 |
| 압축 | 해제 폭탄·경로 조작 | 크기·경로 제한 |
| 실행 파일 | 직접 실행 | 원칙적 금지 |
| 일반 데이터 | 파싱 취약점 | 형식 검증 |

벡터 이미지가 자주 간과된다. 이미지로 분류되지만 실제로는 스크립트를 담을 수 있는 문서 형식이다.

## 용량과 개수도 통제한다

형식만 보면 자원 고갈을 막지 못한다. 파일 크기, 요청당 개수, 계정당 총량, 시간당 업로드 수에 각각 상한을 둔다.

압축 파일은 해제 후 크기를 별도로 제한한다. 작은 파일이 해제되면서 디스크를 채우는 방식이 오래된 수법이지만 지금도 통한다.`,

'xxe-and-xml-parsers': R`## 처리 위치별 위험도

같은 파서 설정이라도 어디서 무엇을 파싱하느냐에 따라 위험이 다르다.

| 처리 대상 | 위험 |
| --- | --- |
| 인증되지 않은 요청의 XML | 매우 높음 |
| 사용자 업로드 문서 | 높음 |
| 인증 연동 메시지 | 높음 — 검증 전 파싱 |
| 협력사 연동 데이터 | 중간 |
| 내부 설정 파일 | 낮음 |

인증 연동이 특히 위험한 이유는 서명 검증보다 파싱이 먼저 일어나기 때문이다. 인증되지 않은 상태에서 파서에 도달한다.

## 형식을 바꿀 수 있는지 본다

가장 확실한 대응은 XML 을 쓰지 않는 것이다. 새로 만드는 연동이라면 다른 형식을 선택할 수 있다.

| 상황 | 선택 |
| --- | --- |
| 새 내부 연동 | 다른 형식 |
| 기존 연동 | 파서 설정 강화 |
| 표준이 XML 을 요구 | 설정 + 격리 |
| 문서 파일 처리 | 격리 환경 |

기존 연동은 상대가 있어 바꾸기 어렵다. 그런 경우 설정을 강화하고 파싱을 격리 환경에서 수행한다.`,

'open-redirect-chains': R`## 위험도 판단

이동 기능이 있다고 모두 같은 위험은 아니다. 무엇과 결합할 수 있는지가 등급을 정한다.

| 조건 | 등급 |
| --- | --- |
| 인증 흐름의 되돌아올 주소 | 심각 |
| 로그인 후 이동 | 높음 |
| 토큰이 주소에 실림 | 심각 |
| 단순 외부 링크 경유 | 중간 |
| 인증과 무관한 내부 이동 | 낮음 |

주소에 토큰이 실리는 구조라면 이동 기능 하나가 계정 탈취로 이어진다. 그런 구조 자체를 먼저 없애는 것이 순서다.

## 등록 목록 관리

인증 연동의 되돌아올 주소는 시간이 지나며 늘어난다. 개발·시험 환경을 추가하다 와일드카드가 들어가고, 그것이 운영에 남는다.

| 점검 | 주기 |
| --- | --- |
| 등록 목록 전수 확인 | 분기 |
| 와일드카드 사용 여부 | 분기 |
| 쓰지 않는 항목 제거 | 분기 |
| 환경별 분리 여부 | 도입 시 |

목록이 열 개를 넘으면 환경이 섞여 있을 가능성이 높다. 운영 클라이언트와 개발 클라이언트를 나누면 목록이 짧아진다.`,

'web-cache-deception': R`## 어떤 페이지가 대상인가

모든 경로가 같은 위험은 아니다. 인증된 응답에 무엇이 담기는지가 등급을 정한다.

| 페이지 | 노출 시 영향 |
| --- | --- |
| 토큰이 포함된 화면 | 계정 탈취 |
| 개인정보 화면 | 개인정보 유출 |
| 주문·결제 내역 | 거래 정보 |
| 설정 화면 | 계정 정보 |
| 공개 콘텐츠 | 영향 없음 |

첫 줄이 가장 급하다. 화면에 토큰이나 세션 값이 들어가는 구조라면 그것부터 없앤다. 캐시 문제와 무관하게도 위험한 설계다.

## 캐시 규칙을 바꿀 때

규칙 변경이 이 취약점의 조건을 만든다. 인프라와 개발이 서로의 설정을 모르면 조합이 생긴다.

| 변경 | 함께 확인할 것 |
| --- | --- |
| 확장자 기준 규칙 추가 | 인증 경로 영향 |
| 캐시 대상 경로 확대 | 응답 헤더 |
| 캐시 시간 연장 | 개인화 여부 |
| 새 CDN 도입 | 전 경로 재점검 |

변경 요청서에 이 확인 항목을 넣으면 조합이 생기기 전에 걸린다.`,

'client-side-validation-trap': R`## 어떤 값이 위험한가

요청에서 받는 값 중 서버가 다시 정해야 하는 것이 있다. 목록을 만들어 두면 새 기능에서 판단이 빨라진다.

| 값 | 처리 |
| --- | --- |
| 금액·수량·할인율 | 서버가 계산 |
| 등급·권한·역할 | 세션에서 판단 |
| 소유자 식별자 | 세션에서 판단 |
| 상태 값 | 서버가 전이 판단 |
| 시각 | 서버 시각 사용 |
| 표시 설정 | 받아도 무방 |

마지막 줄만 클라이언트가 정해도 되는 값이다. 나머지는 식별자만 받고 서버가 조회하거나 계산한다.

## 세 계층의 규칙을 맞춘다

화면·서버·데이터베이스의 검증 규칙이 어긋나면 화면에서는 되는데 저장이 실패하거나, 반대로 저장은 되는데 화면에서 깨진다.

| 계층 | 역할 |
| --- | --- |
| 화면 | 즉시 안내 |
| 서버 | 실제 통제 |
| 데이터베이스 | 최종 제약 |

규칙을 한곳에서 정의하고 세 계층이 가져다 쓰는 구조가 유지된다. 손으로 세 곳을 맞추는 방식은 시간이 지나며 반드시 어긋난다.`,
}
