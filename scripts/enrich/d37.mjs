const R = String.raw
export default {

'passkey-rollout': R`## 사용자군에 따라 다른 경로

전 사용자에게 같은 방식을 적용하기 어렵다. 기기 환경이 다르기 때문이다.

| 사용자군 | 권장 |
| --- | --- |
| 최신 기기·동기화 사용 | 패스키, 자동 동기화 |
| 회사 지급 기기만 | 패스키 + 하드웨어 키 |
| 기기를 자주 바꿈 | 두 개 이상 등록 유도 |
| 오래된 기기 | 기존 인증 유지 |
| 공용 단말 사용 | 하드웨어 키 |

지원하지 않는 환경을 억지로 전환시키면 문의만 늘어난다. 가능한 사용자부터 옮기고 비율을 올린다.

## 비밀번호를 언제 없애나

등록률이 높아도 비밀번호를 바로 지우면 안 된다. 다음 조건이 갖춰진 사용자부터 개별로 제거를 제안한다.

| 조건 | 확인 |
| --- | --- |
| 패스키 두 개 이상 | 필수 |
| 최근 패스키로 로그인 | 실제 사용 확인 |
| 복구 수단 등록 | 분실 대비 |
| 사용자 동의 | 강제하지 않음 |

전원 일괄 제거는 하지 않는다. 조건을 만족한 사용자에게만 제안하고 거절하면 그대로 둔다.`,

'session-binding': R`## 무엇을 묶을지 정하는 순서

바인딩 요소를 고를 때 안정성과 효과를 함께 본다. 효과만 보고 고르면 정상 사용자가 막힌다.

| 요소 | 안정성 | 효과 | 판단 |
| --- | --- | --- | --- |
| 클라이언트 인증서 | 높음 | 높음 | 배포 가능하면 최선 |
| 기기 등록 식별자 | 중간 | 중간 | 일반적 선택 |
| 브라우저 지문 | 낮음 | 중간 | 보조로만 |
| 접속 주소 | 낮음 | 낮음 | 탐지용 |
| 사용자 에이전트 | 매우 낮음 | 낮음 | 쓰지 않음 |

두 번째 줄이 대부분의 서비스에 맞는 선택이다. 기기를 등록하고 그 식별자를 세션에 묶되, 변화 시 차단이 아니라 재인증을 요구한다.

## 변화를 어떻게 다루나

바인딩 값이 바뀌었을 때의 처리를 미리 정한다. 일률적으로 끊으면 이탈이 생기고, 무시하면 의미가 없다.

| 변화 | 처리 |
| --- | --- |
| 정상 범위 내 | 통과 |
| 기기 변경 | 재인증 |
| 물리적으로 불가능한 이동 | 세션 종료 + 알림 |
| 동시 다중 위치 | 확인 요청 |

세 번째가 가장 명확한 신호다. 판단 기준이 분명해 오탐이 적다.`,

'token-revocation-strategy': R`## 방식 선택 기준

무효화 방식은 서비스 성격에 따라 다르다. 즉시성이 얼마나 필요한지가 기준이다.

| 요구 | 방식 |
| --- | --- |
| 즉시 무효화 필수 | 사용자별 기준 시각 |
| 수 분 지연 허용 | 짧은 수명 |
| 무상태 유지 필수 | 짧은 수명 + 갱신 |
| 규모가 작음 | 세션 저장소 |
| 규모가 큼 | 기준 시각 + 캐시 |

사용자별 기준 시각 방식이 대부분의 경우 균형이 좋다. 조회가 캐시 한 번이고 즉시성도 확보된다.

## 갱신 토큰 관리

접근 토큰을 짧게 하면 갱신 토큰의 중요도가 올라간다. 이쪽이 실제 공격 대상이 된다.

| 항목 | 설정 |
| --- | --- |
| 저장 위치 | HttpOnly 쿠키 |
| 전송 경로 | 갱신 엔드포인트만 |
| 재사용 | 탐지 시 전체 무효화 |
| 회전 | 갱신마다 새로 발급 |
| 수명 | 며칠~수주 |

재사용 탐지가 핵심이다. 같은 갱신 토큰이 두 번 쓰이면 하나는 탈취된 것이므로, 해당 사용자의 모든 세션을 무효화한다.`,

'step-up-authentication': R`## 어떤 수단을 요구하나

작업의 위험도와 사용자가 가진 수단에 따라 다르다. 하나만 강제하면 그 수단이 없는 사용자가 막힌다.

| 작업 | 1순위 | 대체 |
| --- | --- | --- |
| 인증 수단 변경 | 패스키 | 하드웨어 키 |
| 출금·이체 | 패스키 | 인증 앱 |
| 대량 반출 | 인증 앱 | 관리자 승인 |
| 연락처 변경 | 인증 앱 | 문자 |
| 설정 변경 | 비밀번호 | — |

비밀번호 재입력만으로 충분한 작업은 많지 않다. 세션이 탈취된 상황을 가정하면 비밀번호도 함께 유출됐을 수 있다.

## 사용자 경험 설계

재인증을 요구할 때 이유를 한 줄로 보여 준다. 이유를 알면 불편이 덜하고, 예상치 못한 요구가 왔을 때 의심할 수 있게 된다.

| 상황 | 안내 |
| --- | --- |
| 인증 수단 변경 | 계정 보호를 위해 한 번 더 확인합니다 |
| 고액 이체 | 금액이 커서 확인이 필요합니다 |
| 새 기기 | 처음 보는 기기입니다 |

이유 없이 반복해 물으면 사용자가 습관적으로 통과시킨다. 그러면 재인증의 의미가 사라진다.`,

'account-recovery-design': R`## 복구 경로를 설계하는 순서

수단을 늘리는 것이 아니라 강도를 맞추는 것이 목표다. 가장 약한 경로가 계정의 실제 강도이므로, 약한 것을 없애는 쪽이 강한 것을 더하는 것보다 효과가 크다.

| 단계 | 조치 |
| --- | --- |
| 1 | 현재 복구 경로를 전부 나열 |
| 2 | 각 경로의 통과 조건 기록 |
| 3 | 가장 약한 것 식별 |
| 4 | 제거하거나 대기·통지로 보완 |
| 5 | 등록 수단을 늘리도록 유도 |

5번이 근본 대응이다. 등록 수단이 둘 이상이면 복구 절차 자체가 거의 필요 없어진다.

## 상담 경로 통제

사람이 판단하는 경로는 사회공학의 표적이 된다. 확인 항목을 문서로 고정하고 재량을 없앤다.

| 원칙 | 이유 |
| --- | --- |
| 공개 정보로 답할 수 있는 질문 배제 | 유출 정보로 통과 |
| 확인 항목 고정 | 재량이 허점 |
| 긴급 요청도 같은 절차 | 압박이 수법 |
| 처리 후 기존 연락처 통지 | 본인이 알아챈다 |
| 대기 기간 적용 | 취소 창 제공 |

상담원 교육 자료에 실제 공격 대화 사례를 넣으면 인식이 크게 달라진다.`,

'authorization-model-choice': R`## 전환 판단표

모델을 바꾸는 것은 큰 작업이다. 지금 구조로 감당할 수 없다는 근거가 있어야 한다.

| 항목 | 현재 | 한계 신호 |
| --- | --- | --- |
| 역할 수 | | 사용자 수의 10% 초과 |
| 이름의 조건 표현 | | 부서·지역 포함 |
| 1인당 보유 | | 다섯 개 초과 |
| 정책 변경 시 배포 | | 필요함 |
| 인가 코드 위치 | | 여러 곳에 흩어짐 |

두 개 이상 해당하면 전환을 검토한다. 하나만 해당하면 그 항목을 정리하는 것으로 해결되는 경우가 많다.

## 병행 운영이 안전한 이유

기존 모델을 끄고 새 모델을 켜는 방식은 위험하다. 두 모델의 판정을 동시에 수행해 결과를 비교하는 기간을 둔다.

| 단계 | 동작 |
| --- | --- |
| 1 | 새 정책 작성, 판정만 수행 |
| 2 | 기존 판정과 결과 비교·기록 |
| 3 | 차이가 나는 요청 분석 |
| 4 | 정책 수정 후 반복 |
| 5 | 차이가 없어지면 전환 |

3단계에서 대부분의 누락이 드러난다. 실제 트래픽으로 검증하는 것이 시험 데이터보다 정확하다.`,

'service-account-lifecycle': R`## 계정 유형별 관리

서비스 계정이라고 다 같지 않다. 성격에 따라 관리 방식이 다르다.

| 유형 | 특징 | 관리 |
| --- | --- | --- |
| 워크로드 신원 | 자격 증명 없음 | 권한만 관리 |
| 배포 파이프라인 | 넓은 권한 | 조건 제한 |
| 배치 작업 | 주기적 사용 | 만료·소유자 |
| 외부 연동 | 장기 자격 증명 | 교체 주기 |
| 조사·운영 | 사람이 사용 | 임시 부여 |

첫 줄이 목표 상태다. 장기 자격 증명이 없으면 유출될 것도 없다. 나머지 유형을 하나씩 이쪽으로 옮기는 것이 방향이다.

## 목록을 유지하는 방법

한 번 만든 목록은 낡는다. 생성 시점에 기록이 남도록 절차를 바꾸는 것이 유지의 유일한 방법이다.

| 방법 | 효과 |
| --- | --- |
| 생성 시 태그 필수 | 소유자·용도 자동 기록 |
| 태그 없으면 생성 차단 | 누락 방지 |
| 주기적 자동 수집 | 실제와 대조 |
| 만료일 기본값 | 방치 방지 |

태그를 필수로 만드는 것이 가장 효과가 크다. 나중에 채우는 방식은 채워지지 않는다.`,

'impersonation-audit': R`## 권한 설계

대리 접속 권한을 누구에게 줄지가 통제의 시작이다. 지원 업무 전원에게 주면 사실상 통제가 없다.

| 역할 | 범위 |
| --- | --- |
| 1차 상담 | 제한된 화면, 읽기 전용 |
| 2차 지원 | 전체 화면, 읽기 전용 |
| 기술 지원 | 쓰기 포함, 승인 필요 |
| 개발 | 원칙적 불가 |
| 관리 | 전체, 기록 강화 |

읽기 전용을 기본으로 두면 대부분의 지원 업무가 처리된다. 쓰기가 필요한 경우는 드물고, 그때는 별도 승인을 거친다.

## 사용자에게 보여 줄 것

사용자가 자기 계정의 접근 이력을 볼 수 있게 하면 통제 효과가 크다. 부적절한 접근을 사용자가 먼저 발견한다.

| 항목 | 표시 |
| --- | --- |
| 언제 | 시각 |
| 왜 | 문의 번호 |
| 무엇을 | 조회 범위 요약 |
| 누가 | 담당자 식별 정보 |

담당자 실명을 노출할지는 조직 정책에 따른다. 최소한 접근이 있었다는 사실과 사유는 보여 주는 것이 원칙이다.`,

'api-key-vs-token': R`## 연동 형태별 선택

무엇을 쓸지는 연동 상대와 인증 주체가 누구인지로 정해진다.

| 연동 형태 | 인증 주체 | 선택 |
| --- | --- | --- |
| 서버 간 정기 호출 | 시스템 | API 키 또는 워크로드 신원 |
| 사용자 대신 호출 | 사용자 | 위임 토큰 |
| 파트너 시스템 | 조직 | 상호 인증 + 키 |
| 공개 API | 개발자 | 키 + 속도 제한 |
| 내부 서비스 간 | 서비스 | 워크로드 신원 |

내부 서비스 간에는 키를 쓰지 않는 것이 좋다. 실행 환경이 신원을 증명하는 방식이면 관리할 값이 없다.

## 키 수명 주기

발급만 하고 관리하지 않으면 몇 년 된 키가 남는다. 생성 시점에 끝을 정해 둔다.

| 단계 | 정할 것 |
| --- | --- |
| 발급 | 소유자·용도·범위·만료 |
| 운영 | 사용 기록 수집 |
| 교체 | 두 개 병행으로 무중단 |
| 만료 | 자동 비활성화 |
| 폐기 | 사용 없음 확인 후 |

교체 경로가 없으면 교체하지 않게 된다. 두 개를 동시에 유효하게 두는 구조를 처음부터 만든다.`,

'delegated-access-scope': R`## 범위를 나누는 기준

너무 잘게 나누면 사용자가 이해하지 못하고, 너무 크게 묶으면 최소 권한이 되지 않는다.

| 기준 | 적정 |
| --- | --- |
| 사용자가 이해 가능 | 평이한 말로 설명되는 단위 |
| 기능 단위 | 하나의 기능에 하나 |
| 읽기·쓰기 분리 | 항상 |
| 자원 종류별 | 프로필·문서·결제 |
| 개수 | 열 개 이내 |

승인 화면에 열 줄이 넘게 표시되면 사용자가 읽지 않는다. 개수를 줄이고 각 항목을 평이하게 쓰는 편이 실질적인 통제가 된다.

## 철회 경로 제공

연결은 쉽고 끊기는 어렵게 만들면 사용자가 연결 자체를 망설인다. 반대로 끊기 쉬우면 부담 없이 연결한다.

| 항목 | 제공 |
| --- | --- |
| 연동 목록 | 사용자 화면에 |
| 마지막 사용 시각 | 표시 |
| 개별 철회 | 한 번에 |
| 철회 후 통지 | 확인 메일 |
| 미사용 자동 만료 | 일정 기간 후 |

마지막 사용 시각을 보여 주면 사용자가 쓰지 않는 연동을 스스로 정리한다.`,
}
