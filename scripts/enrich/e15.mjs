const R = String.raw
export default {

'bec-response': R`## 실제로 이렇게 터진다

거래처 계좌가 바뀌었다는 메일을 받고 송금한 사례가 반복된다. 메일은 실제 거래 담당자 주소에서 왔고, 이전 대화 내용까지 인용돼 있었다. 그 담당자의 메일 계정이 이미 침해된 상태였다.

임원 사칭도 흔하다. 도메인 한 글자를 바꾼 주소였고, "지금 회의 중이라 통화가 안 된다" 는 문장이 확인 경로를 차단했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 발신 주소를 보면 안다 | 실제 계정이 침해된 경우가 있다 |
| 맞춤법으로 구분된다 | 정교해졌다 |
| 기술로 막을 수 있다 | 절차가 함께 필요하다 |
| 큰 금액만 노린다 | 소액 반복도 있다 |
| 우리는 작아서 표적이 아니다 | 자동화되어 있다 |

## 무엇이 신호인가

| 신호 | 확인 |
| --- | --- |
| 계좌 변경 요청 | 반드시 역연락 |
| 긴급·비밀 강조 | 확인 절차를 막으려는 것 |
| 회신 주소가 다름 | 헤더 확인 |
| 평소와 다른 시간·문체 | 계정 침해 가능성 |
| 첨부 없이 링크만 | 자격 증명 수집 |

## 절차로 막는다

1. 계좌 변경은 등록된 번호로 역연락해 확인한다 — 메일에 적힌 번호가 아니라
2. 일정 금액 이상 송금에 이중 승인을 건다
3. 첫 거래와 계좌 변경에는 대기 시간을 둔다
4. 외부 메일에 표시를 붙인다
5. 사칭 시도를 신고하는 경로를 만들고 훈련한다

## 사고가 났을 때

1. 은행에 즉시 연락해 지급 정지를 요청한다 — 시간이 결정적이다
2. 수사기관에 신고한다
3. 침해된 메일 계정을 찾는다 — 우리 쪽인지 상대 쪽인지
4. 그 계정의 전달 규칙과 위임 설정을 확인한다 — 흔적이 남는다
5. 같은 수법이 다른 거래처에 갔는지 확인한다

\`\`\`bash
# 메일 계정에 몰래 설정된 전달 규칙 — 침해의 흔적이다
gam all users print forwards
gam all users print filters | grep -iE 'forward|delete|archive'
# 사용자가 모르는 전달 규칙이 있으면 계정이 침해된 것이다
\`\`\``,

'breach-notification': R`## 실제로 이렇게 터진다

유출을 인지하고 나서 통지문을 만드는 데 나흘이 걸린 사례가 있다. 법정 기한 안에 신고는 했지만 이용자 통지가 늦었고, 그 사이 언론이 먼저 보도했다.

범위를 잘못 산정한 경우도 있다. 처음에 1만 명이라고 발표했다가 나중에 12만 명으로 정정했고, 그 정정이 더 큰 문제가 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 조사가 끝나야 신고한다 | 기한이 먼저다, 확인된 범위로 신고한다 |
| 범위를 확정해야 통지한다 | 확정 전이라도 알려야 한다 |
| 통지문은 사과문이다 | 행동 안내가 핵심이다 |
| 축소해서 발표하면 낫다 | 정정이 더 큰 손실이다 |
| 신고만 하면 된다 | 이용자 통지가 별도 의무다 |

## 기한과 대상

| 항목 | 기준 |
| --- | --- |
| 이용자 통지 | 인지 후 지체 없이 |
| 기관 신고 | 정해진 기한 내 |
| 신고 대상 규모 | 법령 기준 확인 |
| 통지 방법 | 개별 통지 원칙, 곤란 시 공고 |
| 후속 통지 | 범위가 바뀌면 다시 |

기한은 "조사 완료" 가 아니라 "인지" 부터 센다. 이 점을 놓치면 기한을 넘긴다.

## 통지문에 무엇을 담는가

1. 유출된 항목 — 구체적으로, 항목 단위로
2. 유출 시점과 인지 시점
3. 우리가 취한 조치
4. 이용자가 해야 할 것 — 비밀번호 변경, 결제 내역 확인
5. 문의 창구와 추가 안내 일정

4번이 가장 중요하다. 무엇을 해야 하는지 없는 통지문은 불안만 만든다.

## 미리 준비할 것

| 준비물 | 이유 |
| --- | --- |
| 통지문 초안 3~4종 | 유형별로 빈칸 채우기 |
| 승인 경로 | 누가 최종 결정하는지 |
| 대량 발송 검증 | 수십만 건 발송은 평소와 다르다 |
| 상담 문답 | 창구마다 같은 답이 나가게 |
| 신고 서식 | 필요한 정보 목록 |
| 외부 도움 | 법률·포렌식 연락처 |

\`\`\`sql
-- 영향 범위 산정 — 통지 대상을 정확히 세는 것이 먼저다
SELECT count(DISTINCT user_id) AS affected,
       count(*) FILTER (WHERE email IS NOT NULL) AS notifiable_email,
       count(*) FILTER (WHERE phone IS NOT NULL) AS notifiable_sms
FROM affected_users;
\`\`\``,

'sbom-operations': R`## 실제로 이렇게 터진다

취약점이 공개됐을 때 우리 제품에 그 라이브러리가 들어 있는지 확인하는 데 사흘이 걸린 사례가 있다. 각 팀에 물어보고 빌드를 뒤졌다. 그 사흘 동안 대응이 멈췄다.

목록은 만들었는데 쓰지 않은 경우도 있다. 배포마다 생성했지만 저장만 하고 조회할 방법이 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 목록을 만들면 끝이다 | 조회할 수 있어야 쓸모가 있다 |
| 직접 의존성만 담으면 된다 | 전이 의존성이 대부분이다 |
| 한 번 만들면 유효하다 | 배포마다 달라진다 |
| 형식이 중요하다 | 질의 가능성이 중요하다 |
| 규정 때문에 만든다 | 대응 속도를 위해 만든다 |

## 언제 만드는가

| 시점 | 담기는 것 |
| --- | --- |
| 소스 분석 | 선언된 의존성 |
| 빌드 | 실제로 포함된 것 |
| 컨테이너 이미지 | 시스템 패키지까지 |
| 배포 | 실제 운영 중인 구성 |

빌드 시점에 만드는 것이 정확하다. 선언과 실제가 다른 경우가 있다.

## 어떻게 쓰는가

목록의 가치는 "우리 어디에 이것이 있는가" 에 답하는 것이다.

1. 배포마다 생성해 저장소에 모은다 — 산출물 다이제스트를 키로
2. 검색 가능한 형태로 색인한다 — 패키지 이름과 버전
3. 취약점 공개 시 즉시 질의한다
4. 결과에 배포 환경과 노출 여부를 붙인다
5. 조치 대상 목록을 그대로 티켓으로 만든다

\`\`\`bash
# 빌드 시 생성하고 산출물에 붙인다
syft ghcr.io/example/app@sha256:abc -o cyclonedx-json > sbom.json
cosign attest --predicate sbom.json --type cyclonedx ghcr.io/example/app@sha256:abc

# 특정 패키지가 어느 배포에 있는지 — 이 질의가 목록의 존재 이유다
jq -r --arg n 'log4j-core' \
  '.components[] | select(.name==$n) | "\(.name) \(.version)"' sboms/*.json | sort | uniq -c
\`\`\``,

'dependency-confusion': R`## 실제로 이렇게 터진다

사내 전용 패키지 이름이 공개 저장소에 없다는 점을 이용한 공격이 있다. 공격자가 같은 이름으로 더 높은 버전을 공개 저장소에 올렸고, 빌드 도구가 공개 저장소를 먼저 보거나 높은 버전을 선택하면서 그것을 받았다. 설치 스크립트가 실행되며 빌드 환경이 침해됐다.

사내 저장소를 쓰고 있어도 안전하지 않다. 사내 저장소가 공개 저장소를 대리 조회하는 설정이면 같은 일이 생긴다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 사내 저장소를 쓰니 안전하다 | 대리 조회 설정이 문제다 |
| 이름을 어렵게 지으면 된다 | 저장소 이력과 로그에서 이름이 샌다 |
| 버전을 고정하면 막힌다 | 해석 순서가 먼저 작동한다 |
| 공개에 없는 이름이라 안전하다 | 지금 없을 뿐이다 |
| 개발 환경만 영향받는다 | 빌드 환경이 배포 권한을 쥔다 |

## 어떻게 막는가

| 조치 | 효과 |
| --- | --- |
| 조직 접두사 선점 | 공개 저장소에 이름을 등록 |
| 저장소 범위 지정 | 접두사별로 저장소를 고정 |
| 대리 조회 제한 | 사내 이름은 공개로 넘기지 않음 |
| 잠금 파일 + 무결성 해시 | 다른 것을 받으면 실패 |
| 설치 스크립트 차단 | 실행 자체를 막는다 |

접두사 선점과 범위 지정 둘을 함께 해야 확실하다.

\`\`\`
# npm — 조직 범위를 사내 저장소로 고정한다
@example:registry=https://npm.internal.example.com/
//npm.internal.example.com/:_authToken=\${NPM_TOKEN}

# 설치 스크립트 실행 차단
ignore-scripts=true
\`\`\`

\`\`\`bash
# 사내 패키지 이름이 공개 저장소에 있는지 확인한다 — 없으면 선점한다
jq -r '.dependencies + .devDependencies | keys[]' package.json |
  grep -v '^@example/' | while read -r p; do
    code=$(curl -s -o /dev/null -w '%{http_code}' "https://registry.npmjs.org/$p")
    [ "$code" = "404" ] && echo "공개에 없음(선점 대상): $p"
  done
\`\`\``,

'workflow-permissions': R`## 실제로 이렇게 터진다

외부 기여자가 올린 변경의 워크플로가 저장소 비밀값에 접근한 사례가 있다. 기본 설정이 그것을 허용했고, 그 워크플로는 비밀값을 외부로 보냈다.

토큰 권한이 기본으로 쓰기였던 경우도 있다. 워크플로 하나가 침해되자 저장소 내용을 바꿀 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 기본 설정이 안전하다 | 기본은 대개 관대하다 |
| 외부 기여는 검토를 거친다 | 워크플로는 실행이 먼저다 |
| 비밀값은 마스킹된다 | 우회해 출력할 방법이 있다 |
| 내부 저장소는 무관하다 | 침해된 계정이 내부다 |
| 액션은 신뢰할 수 있다 | 태그는 옮겨 붙일 수 있다 |

## 무엇을 좁히는가

| 항목 | 설정 |
| --- | --- |
| 토큰 권한 | 기본 읽기, 필요한 작업만 쓰기 |
| 외부 기여 실행 | 승인 후 실행 |
| 외부 기여 + 비밀값 | 접근 금지 |
| 액션 고정 | 태그가 아니라 커밋 해시 |
| 배포 환경 | 승인자 지정 |
| 자체 실행기 | 공개 저장소에 쓰지 않는다 |

자체 실행기를 공개 저장소에 붙이면 누구나 우리 인프라에서 코드를 실행할 수 있다.

\`\`\`yaml
# 저장소 기본을 읽기로 두고, 필요한 잡에서만 올린다
permissions:
  contents: read

jobs:
  release:
    permissions:
      contents: write        # 이 잡에서만
      id-token: write        # 신원 연동용
\`\`\`

\`\`\`bash
# 쓰기 권한을 기본으로 갖거나 액션이 태그로 고정된 워크플로
for f in .github/workflows/*.yml; do
  grep -q '^permissions:' "$f" || echo "권한 미지정: $f"
done
grep -rhoE 'uses: [^@]+@[^ ]+' .github/workflows/ |
  grep -vE '@[0-9a-f]{40}' | sort -u
\`\`\``,
}
