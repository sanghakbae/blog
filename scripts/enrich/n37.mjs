const R = String.raw
export default {

'passkey-rollout': R`## 실제로 이렇게 터진다

등록 유도만 하고 복구 경로를 정비하지 않은 사례가 있다. 기기를 바꾼 사용자들이 로그인하지 못해 문의가 몰렸고, 결국 이메일 재설정을 다시 열었다. 피싱 저항이라는 도입 목적이 사라졌다.

또 하나는 패스키 등록 화면을 설정 깊숙이 둔 경우다. 반년 뒤 등록률이 3%였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 비밀번호를 바로 없앨 수 있다 | 병행 기간이 필요하다 |
| 등록하면 다 쓴다 | 로그인 화면에서 제안해야 쓴다 |
| 복구는 나중에 정한다 | 도입의 병목이다 |
| 하나만 등록하면 된다 | 두 개 이상이어야 안전하다 |

## 도입 전에 답할 것

| 질문 | 준비 |
| --- | --- |
| 기기를 잃으면 | 복구 경로 |
| 회사 기기만 쓰는 사용자 | 하드웨어 키 |
| 지원하지 않는 브라우저 | 대체 수단 |
| 등록률이 낮으면 | 제안 시점 조정 |`,

'session-binding': R`## 실제로 이렇게 터진다

세션 쿠키를 접속 주소와 엄격하게 묶은 사례가 있다. 이동 통신 사용자가 몇 분마다 로그아웃됐고, 문의가 늘자 바인딩을 통째로 껐다. 강도를 조절했다면 유지할 수 있었다.

또 하나는 기기 지문만으로 묶은 경우다. 브라우저 갱신 후 전 사용자가 재로그인해야 했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 엄격할수록 안전하다 | 꺼지면 아무 효과도 없다 |
| 주소로 묶으면 충분하다 | 정상 사용자가 막힌다 |
| 기기 지문은 안정적이다 | 갱신마다 바뀐다 |
| 바인딩이 탈취를 막는다 | 어렵게 만들 뿐이다 |

## 강도 조절

| 상황 | 처리 |
| --- | --- |
| 일반 조회 | 변화 허용 |
| 설정 변경 | 재인증 |
| 금전 이동 | 재인증 + 알림 |
| 불가능한 이동 | 세션 종료 |`,

'token-revocation-strategy': R`## 실제로 이렇게 터진다

접근 토큰 수명을 24시간으로 둔 사례가 있다. 퇴사 처리를 했는데도 그 사람이 하루 동안 사내 시스템에 접근할 수 있었다. 계정은 막혔지만 이미 발급된 토큰은 유효했다.

또 하나는 갱신 토큰 재사용을 탐지하지 않은 경우다. 탈취된 토큰과 정상 사용자가 번갈아 갱신하며 몇 주간 공존했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 계정을 막으면 끝이다 | 발급된 토큰은 유효하다 |
| 로그아웃하면 무효다 | 서버가 알지 못한다 |
| 수명이 길어야 편하다 | 노출 창이 길어진다 |
| 폐기 목록은 무겁다 | 사용자별 기준 시각 하나면 된다 |

## 즉시 무효화가 필요한 순간

| 상황 | 조치 |
| --- | --- |
| 퇴사·계정 정지 | 즉시 |
| 비밀번호 변경 | 즉시 |
| 권한 축소 | 즉시 |
| 탈취 의심 | 전체 세션 |
| 정기 | 만료 대기 |`,

'step-up-authentication': R`## 실제로 이렇게 터진다

세션이 탈취된 계정에서 공격자가 인증 수단을 자기 것으로 바꾼 사례가 있다. 그 시점에 재인증을 요구했다면 막혔을 일이다. 계정을 되찾는 데 며칠이 걸렸다.

또 하나는 재인증 상태를 클라이언트가 보관한 경우다. 값을 조작하면 재인증 없이 통과했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 로그인했으면 본인이다 | 세션이 탈취될 수 있다 |
| 비밀번호 재입력이면 충분하다 | 함께 유출됐을 수 있다 |
| 자주 물으면 안전하다 | 습관적으로 넘긴다 |
| 화면에서 막으면 된다 | API 에서 확인해야 한다 |

## 어디에 걸 것인가

| 기준 | 판단 |
| --- | --- |
| 되돌릴 수 없나 | 걸어야 한다 |
| 계정 통제를 바꾸나 | 반드시 |
| 금전이 움직이나 | 반드시 |
| 데이터가 나가나 | 대량이면 |
| 단순 조회 | 불필요 |`,

'account-recovery-design': R`## 실제로 이렇게 터진다

상담 창구에서 생년월일과 연락처 확인만으로 계정을 복구해 준 사례가 있다. 공격자가 유출된 개인정보로 그 확인을 통과했고, 계정을 넘겨받은 뒤 저장된 결제 수단으로 결제했다.

또 하나는 복구 직후 아무 제한이 없던 경우다. 복구 5분 뒤 전액이 빠져나갔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 개인정보 확인이면 본인이다 | 유출된 정보로 통과한다 |
| 수단이 많을수록 친절하다 | 가장 약한 것이 기준이 된다 |
| 복구되면 정상이다 | 직후가 가장 위험하다 |
| 상담원이 판단하면 된다 | 사회공학 표적이 된다 |

## 복구 직후 제한

| 제한 | 기간 |
| --- | --- |
| 출금·결제 | 72시간 |
| 연락처 변경 | 72시간 |
| 대량 반출 | 7일 |
| 신규 기기 등록 | 확인 후 |`,

'authorization-model-choice': R`## 실제로 이렇게 터진다

역할이 60개까지 늘어난 사례가 있다. 이름에 부서와 지역이 들어가 있었고, 새 팀이 생길 때마다 역할이 추가됐다. 어떤 역할이 무엇을 할 수 있는지 아무도 설명하지 못했다.

또 하나는 반대로 역할 세 개만 두고 예외를 코드에 넣은 경우다. 인가 로직이 코드 곳곳에 흩어져 변경할 수 없게 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 역할만으로 다 된다 | 조건이 생기면 폭발한다 |
| 속성 기반이 항상 낫다 | 단순한 조직에는 과하다 |
| 모델을 바꾸면 해결된다 | 설계가 없으면 같은 문제가 온다 |
| 예외는 코드로 처리한다 | 흩어져 관리 불가 |

## 전환 신호

| 신호 | 의미 |
| --- | --- |
| 역할 이름에 조건이 들어감 | 조건을 역할로 표현 중 |
| 한 사람이 다섯 개 이상 | 조합으로 표현 중 |
| 인가 코드가 흩어짐 | 중앙화 필요 |
| 정책 변경에 배포 필요 | 분리 필요 |`,

'service-account-lifecycle': R`## 실제로 이렇게 터진다

용도를 모르는 서비스 계정을 정리하다 배치 작업이 멈춘 사례가 있다. 사용 기록이 없어 미사용으로 판단했는데, 분기 말에만 도는 작업이었다.

또 하나는 반대로 정리를 미루다 몇 년 된 계정이 유출된 경우다. 만들 당시 넓게 준 권한이 그대로 남아 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 90일 미사용이면 안 쓴다 | 분기·연간 작업이 있다 |
| 지우면 바로 안다 | 몇 달 뒤에 안다 |
| 계정은 비용이 없다 | 위험이 비용이다 |
| 소유자는 기억한다 | 담당이 바뀐다 |

## 안전한 정리 순서

| 단계 | 조치 |
| --- | --- |
| 1 | 목록 공지, 소유자 확인 요청 |
| 2 | 응답 없으면 권한만 축소 |
| 3 | 2주 관찰 |
| 4 | 비활성화 |
| 5 | 2주 후 삭제 |`,

'impersonation-audit': R`## 실제로 이렇게 터진다

대리 접속 기능에 사유 입력이 없던 사례가 있다. 직원이 유명인 계정을 호기심으로 열람했고, 나중에 문제가 됐을 때 열람 기록이 없어 범위를 확인할 수 없었다.

또 하나는 대리 상태 표시가 없어 관리자가 자기 계정으로 착각하고 설정을 바꾼 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 지원 기능이라 위험이 낮다 | 모든 계정에 들어갈 수 있다 |
| 작업만 기록하면 된다 | 열람 자체가 처리다 |
| 관리자는 신뢰할 수 있다 | 기록이 억제한다 |
| 사용자에게 알릴 필요 없다 | 알리는 것이 원칙이다 |

## 최소 구성

| 항목 | 필수 |
| --- | --- |
| 사유 입력 | 필수 |
| 시간 제한 | 필수 |
| 읽기 전용 기본 | 필수 |
| 화면 표시 | 필수 |
| 사용자 통지 | 권장 |`,

'api-key-vs-token': R`## 실제로 이렇게 터진다

외부 연동용 키 하나를 여러 파트너가 공유한 사례가 있다. 한 곳에서 유출됐는데 어디서 샜는지 알 수 없었고, 교체하려면 전 파트너가 동시에 바꿔야 했다.

또 하나는 키를 질의 문자열로 받은 경우다. 접근 로그와 참조 헤더에 그대로 남았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 키 하나로 충분하다 | 유출 출처를 알 수 없다 |
| 질의 문자열이 편하다 | 로그에 남는다 |
| 키는 안 바꿔도 된다 | 교체 경로가 없으면 못 바꾼다 |
| 해시 저장은 과하다 | 저장소 유출에 대비한다 |

## 발급 시 정할 것

| 항목 | 내용 |
| --- | --- |
| 소유자 | 연동처 단위 |
| 권한 범위 | 필요한 것만 |
| 출발지 제한 | 가능하면 |
| 만료일 | 기본 1년 |
| 교체 방식 | 두 개 병행 |`,

'delegated-access-scope': R`## 실제로 이렇게 터진다

연동 앱이 전체 접근 권한을 요구했고 사용자가 승인한 사례가 있다. 그 앱이 침해되자 사용자 데이터 전체가 나갔다. 실제로 필요한 것은 프로필 조회뿐이었다.

또 하나는 승인 화면에 기술 용어만 표시된 경우다. 사용자가 무엇을 허용하는지 이해하지 못했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 사용자가 판단한다 | 이해할 수 있어야 판단한다 |
| 범위를 나누면 불편하다 | 점진 요청이 승낙률을 올린다 |
| 승인하면 끝이다 | 철회 경로가 필요하다 |
| 미사용 권한은 무해하다 | 유출 시 그대로 쓰인다 |

## 범위 설계 원칙

| 원칙 | 적용 |
| --- | --- |
| 읽기·쓰기 분리 | 조회만 필요한 연동 |
| 자원별 분리 | 프로필·문서·결제 |
| 기본은 읽기 | 쓰기는 별도 요청 |
| 미사용 만료 | 자동 철회 |`,
}
