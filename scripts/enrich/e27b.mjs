const R = String.raw
export default {

'kernel-hardening-sysctl': R`## 실제로 이렇게 터진다

강화 설정을 실행 중에만 적용하고 파일에 남기지 않은 사례가 있다. 재부팅 후 전부 기본값으로 돌아갔고, 아무도 몰랐다. 점검 보고서는 적용 시점의 것이었다.

또 하나는 네트워크 관련 설정을 잘못 넣어 클러스터 통신이 끊긴 경우다. 전달 기능을 끄면서 컨테이너 네트워크가 죽었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 적용하면 유지된다 | 파일에 써야 재부팅 후에도 남는다 |
| 많이 켤수록 안전하다 | 일부는 기능을 깨뜨린다 |
| 기본값은 안전하지 않다 | 최근 배포판은 상당수 안전하다 |
| 컨테이너 호스트도 같다 | 컨테이너 네트워크와 충돌한다 |
| 설정하면 점검 끝이다 | 현재 값을 확인해야 한다 |

## 효과가 확실한 항목

| 항목 | 효과 |
| --- | --- |
| 주소 위조 패킷 검증 | 중간 |
| 리다이렉트 수신 거부 | 중간 |
| 소스 라우팅 거부 | 중간 |
| 코어 덤프 제한 | 높음 — 메모리 내용 유출 방지 |
| 주소 공간 배치 무작위화 | 높음 |
| 커널 포인터 노출 제한 | 높음 |
| 심볼릭 링크 보호 | 높음 |

주소 공간 무작위화와 심볼릭 링크 보호는 대개 기본으로 켜져 있다. 꺼져 있다면 그것이 이상한 상태다.

## 주의가 필요한 항목

| 항목 | 주의 |
| --- | --- |
| IP 전달 | 컨테이너·라우터에서 필요 |
| 브리지 필터 | 컨테이너 네트워크 정책에 영향 |
| 연결 추적 한계 | 높은 부하에서 조정 필요 |
| 역경로 필터 | 다중 경로 환경에서 문제 |

컨테이너를 돌리는 호스트에서는 이 네 가지를 함부로 바꾸지 않는다. 변경 전에 시험 환경에서 확인한다.

## 적용과 검증

\`\`\`bash
# 파일에 남기고 즉시 적용
cat >/etc/sysctl.d/90-hardening.conf <<'EOF'
kernel.kptr_restrict = 2
kernel.dmesg_restrict = 1
fs.protected_symlinks = 1
fs.protected_hardlinks = 1
fs.suid_dumpable = 0
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
EOF
sysctl --system

# 파일 설정과 현재 값이 일치하는지 — 표류 점검
awk -F= '/^[a-z]/ {gsub(/ /,"",$1); gsub(/ /,"",$2); print $1, $2}' \
  /etc/sysctl.d/90-hardening.conf | while read -r k v; do
    cur=$(sysctl -n "$k" 2>/dev/null)
    [ "$cur" = "$v" ] || echo "불일치 $k: 설정 $v, 현재 $cur"
  done
\`\`\`

마지막 점검을 정기 작업으로 돌리면 표류를 잡을 수 있다. 다른 도구가 값을 바꾸는 경우가 실제로 있다.`,

'auditd-rules': R`## 실제로 이렇게 터진다

권장 규칙 세트를 그대로 넣은 사례가 있다. 로그가 초당 수천 줄씩 쌓였고, 디스크가 찼다. 감사 시스템이 멈추자 서버 전체가 멈추는 설정이었다.

또 하나는 규칙은 많았지만 정작 조사 때 필요한 것이 없던 경우다. 실행된 명령의 인자가 기록되지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 규칙이 많을수록 좋다 | 잡음이 조사를 방해한다 |
| 권장 세트를 쓰면 된다 | 환경에 맞춰야 한다 |
| 디스크가 차도 서비스는 돈다 | 설정에 따라 멈춘다 |
| 성능 영향이 없다 | 규칙 수와 대상에 비례한다 |
| 로컬 보관이면 충분하다 | 침해자가 지운다 |

## 무엇을 기록하나

| 대상 | 이유 |
| --- | --- |
| 인증 관련 파일 변경 | 계정 조작 탐지 |
| 권한 상승 명령 실행 | 상승 시도 추적 |
| 설정 파일 변경 | 지속성 확보 탐지 |
| 예약 작업 변경 | 지속성 확보 탐지 |
| 커널 모듈 적재 | 루트킷 탐지 |
| 감사 설정 변경 | 은폐 시도 탐지 |
| 특정 디렉터리 쓰기 | 웹 셸 탐지 |

마지막 항목이 웹 서버에서 특히 유용하다. 문서 루트에 새 파일이 생기면 대개 문제다.

## 잡음을 줄이는 법

1. 자주 발생하는 정상 동작을 제외 규칙으로 먼저 넣는다
2. 모니터링 에이전트 프로세스를 제외한다
3. 임시 디렉터리 쓰기를 제외한다
4. 규칙별 발생량을 측정하고 상위 항목을 재검토한다

제외 규칙은 순서가 앞에 있어야 적용된다. 뒤에 두면 소용없다.

\`\`\`bash
# 규칙별 발생량 — 상위 항목이 잡음일 가능성이 높다
aureport --key --summary 2>/dev/null | head -20

# 디스크가 찰 때의 동작 — halt 로 두면 서버가 멈춘다
grep -E 'max_log_file_action|space_left_action|disk_full_action' /etc/audit/auditd.conf
\`\`\`

## 디스크 대응 설정

| 항목 | 권장 |
| --- | --- |
| 로그 순환 동작 | rotate |
| 여유 부족 시 | syslog 경고 |
| 디스크 가득 시 | syslog — halt 는 위험 |
| 원격 전송 | 즉시 전송 구성 |

규정상 halt 가 요구되는 환경이 아니라면, 감사 로그 때문에 서비스가 멈추는 설정은 피한다. 대신 원격 전송으로 유실을 막는다.`,

'file-integrity-monitoring': R`## 실제로 이렇게 터진다

무결성 감시를 켜 두었지만 기준값을 침해 이후에 만든 사례가 있다. 변조된 파일이 정상으로 등록됐다.

또 하나는 경보가 하루 수천 건 나온 경우다. 로그 파일과 임시 파일까지 감시 대상이었고, 아무도 보지 않게 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 전부 감시하면 좋다 | 잡음으로 묻힌다 |
| 기준값은 언제 만들어도 된다 | 깨끗한 시점이어야 한다 |
| 변경 탐지가 목적이다 | 예상 밖 변경 탐지가 목적이다 |
| 로컬 기준값이면 된다 | 침해자가 함께 고친다 |
| 실시간이어야 한다 | 주기 점검으로도 충분한 경우가 많다 |

## 무엇을 감시하나

| 대상 | 감시 |
| --- | --- |
| 시스템 실행 파일 | 필수 |
| 설정 파일 | 필수 |
| 예약 작업 | 필수 |
| 서비스 정의 | 필수 |
| 웹 문서 루트 | 필수 (배포 제외) |
| 로그 파일 | 제외 |
| 임시·캐시 | 제외 |
| 데이터 디렉터리 | 제외 |

## 배포와 어떻게 구분하나

정상 배포도 파일을 바꾼다. 배포 시각과 대조하지 않으면 전부 경보가 된다.

| 방법 | 효과 |
| --- | --- |
| 배포 중 감시 일시 중지 | 간단하지만 창이 생긴다 |
| 배포 후 기준값 갱신 | 권장 |
| 배포 아티팩트 해시와 대조 | 가장 정확 |
| 배포 시간대 경보 등급 하향 | 보조 |

배포 파이프라인에서 기준값을 갱신하도록 만드는 것이 실무적이다.

\`\`\`bash
# 기준값 생성 — 깨끗한 시점에, 결과는 원격에 보관
aide --init && cp /var/lib/aide/aide.db.new.gz /secure/remote/aide.db.gz

# 점검
aide --check | grep -E '^(added|removed|changed)' | head -20
\`\`\`

## 기준값 보관 위치

기준값이 같은 서버에 있으면 침해자가 함께 고친다. 읽기 전용 매체나 별도 서버에 둔다. 점검도 원격에서 시작하는 편이 낫다.

| 위치 | 안전성 |
| --- | --- |
| 같은 서버 | 낮음 |
| 별도 서버 | 중간 |
| 읽기 전용 저장소 | 높음 |
| 서명된 기준값 | 높음 |`,

'disk-encryption-boot': R`## 실제로 이렇게 터진다

노트북 전체를 암호화했지만 자동 잠금 해제를 설정한 사례가 있다. 부팅하면 암호 입력 없이 열렸고, 도난 시 암호화가 아무 의미가 없었다.

또 하나는 서버 디스크를 암호화하면서 키를 같은 디스크에 둔 경우다. 디스크를 통째로 가져가면 키도 함께 갔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호화하면 안전하다 | 키 관리가 전부다 |
| 켜져 있는 상태도 보호된다 | 실행 중에는 복호화 상태다 |
| 서버는 물리 접근이 없다 | 폐기·수리 시 나간다 |
| 파일 암호화면 충분하다 | 임시 파일·스왑이 남는다 |
| 성능이 크게 떨어진다 | 하드웨어 가속으로 차이가 작다 |

## 무엇을 막고 못 막나

| 상황 | 보호 |
| --- | --- |
| 전원 꺼진 장비 도난 | 보호됨 |
| 디스크 분리 후 읽기 | 보호됨 |
| 폐기 시 데이터 잔존 | 보호됨 |
| 실행 중 침해 | 보호 안 됨 |
| 화면 잠금 상태 도난 | 부분적 |
| 관리자 권한 공격 | 보호 안 됨 |

전원이 꺼진 상태를 위한 방어다. 그 전제를 이해하지 않으면 다른 통제를 소홀히 하게 된다.

## 키를 어디에 두나

| 방식 | 보안 | 편의 |
| --- | --- | --- |
| 부팅 시 암호 입력 | 높음 | 낮음 — 무인 재부팅 불가 |
| 보안 칩에 봉인 | 높음 | 높음 |
| 네트워크 키 서버 | 중간 | 높음 |
| 디스크 내 파일 | 없음 | 높음 |

서버는 보안 칩 봉인이나 네트워크 키 서버를 쓴다. 무결성 상태가 바뀌면 잠금이 풀리지 않도록 정책을 건다.

## 확인

\`\`\`bash
# 암호화 상태와 키 슬롯
lsblk -o NAME,FSTYPE,MOUNTPOINT | grep -i crypt
cryptsetup luksDump /dev/sda3 | grep -E 'Key Slot|Tokens'

# 스왑도 암호화돼 있는지 — 자주 빠진다
swapon --show
grep swap /etc/crypttab
\`\`\`

## 자주 빠지는 것

| 항목 | 문제 |
| --- | --- |
| 스왑 파티션 | 메모리 내용이 평문으로 남는다 |
| 임시 디렉터리 | 처리 중 데이터 |
| 백업 | 암호화 없이 나간다 |
| 복구 키 보관 | 안전한 곳에 있는가 |

복구 키를 잃으면 데이터도 잃는다. 별도의 안전한 위치에 보관하고, 보관 위치를 문서로 남긴다.`,

'macos-endpoint-baseline': R`## 실제로 이렇게 터진다

개발자 장비를 관리 대상에서 제외한 사례가 있다. 편의를 위해서였는데, 그 장비들에 가장 많은 자격 증명이 있었다. 한 대가 감염되자 여러 시스템의 접근 권한이 함께 나갔다.

또 하나는 관리 도구는 설치했지만 정책을 적용하지 않은 경우다. 등록 대수만 늘었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 맥은 안전하다 | 표적 악성코드가 존재한다 |
| 개발자 장비는 예외로 둔다 | 가장 중요한 장비다 |
| 기본 설정이 안전하다 | 몇 가지는 켜야 한다 |
| 백신이면 충분하다 | 구성 관리가 먼저다 |
| 사용자가 설정한다 | 정책으로 강제해야 유지된다 |

## 최소 기준선

| 항목 | 설정 |
| --- | --- |
| 디스크 암호화 | 켜짐, 복구 키 위탁 |
| 화면 잠금 | 5분, 즉시 암호 요구 |
| 방화벽 | 켜짐, 스텔스 모드 |
| 자동 업데이트 | 켜짐 |
| 게이트키퍼 | 켜짐 |
| 시스템 무결성 보호 | 켜짐 |
| 원격 로그인 | 필요 시에만 |
| 공유 기능 | 전부 꺼짐 |

## 개발자 장비에서 자주 문제가 되는 것

| 항목 | 위험 | 대안 |
| --- | --- | --- |
| 무결성 보호 해제 | 매우 높음 | 대부분 불필요하다 |
| 서명 검증 우회 | 높음 | 개발자 인증서 사용 |
| 임의 스크립트 실행 | 중간 | 출처 확인 습관 |
| 자격 증명 평문 보관 | 높음 | 키체인·관리 도구 |
| 개인 계정 혼용 | 중간 | 프로필 분리 |

무결성 보호를 끈 장비는 관리 대상에서 이탈한 것으로 본다. 왜 꺼야 했는지 확인하고 대안을 찾는다.

## 점검

\`\`\`bash
# 주요 보호 기능 상태
csrutil status
fdesetup status
/usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
spctl --status

# 자동 업데이트
defaults read /Library/Preferences/com.apple.SoftwareUpdate AutomaticCheckEnabled
\`\`\`

## 관리 방식

정책은 관리 도구로 배포하고 사용자가 되돌릴 수 없게 한다. 사용자에게 설정을 맡기면 시간이 지나며 하나씩 꺼진다. 대신 왜 필요한지 설명하고, 업무에 방해가 되는 항목은 함께 조정한다. 설명 없는 강제는 우회를 낳는다.`,
}
