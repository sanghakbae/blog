const R = String.raw
export default {

'cache-authorization': R`## 실제로 이렇게 터진다

응답 캐시 키를 URL 만으로 만든 사례가 있다. 같은 주소라도 사용자에 따라 다른 내용이 나가는 API 였고, 먼저 요청한 사용자의 응답이 다음 사용자에게 갔다.

또 하나는 권한이 바뀐 뒤에도 캐시가 남은 경우다. 퇴사 처리된 계정이 캐시된 응답을 계속 받았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 캐시는 성능 문제다 | 인가 문제이기도 하다 |
| URL 이 같으면 같은 응답이다 | 사용자마다 다르다 |
| 캐시는 짧으니 괜찮다 | 짧아도 유출은 유출이다 |
| 권한 변경이 즉시 반영된다 | 캐시가 남는다 |
| 애플리케이션 캐시만 보면 된다 | 여러 계층에 있다 |

## 캐시 키에 넣을 것

| 요소 | 조건 |
| --- | --- |
| 경로·질의 | 항상 |
| 사용자 또는 역할 | 응답이 달라지면 |
| 테넌트 | 다중 테넌트면 항상 |
| 권한 버전 | 권한 변경 반영용 |
| 언어·지역 | 응답이 달라지면 |

권한 버전을 키에 넣으면 권한 변경 시 자동으로 캐시가 무효화된다. 사용자별 권한 갱신 번호를 하나 두고 키에 붙인다.

## 무엇을 캐시할 수 있나

| 응답 | 캐시 |
| --- | --- |
| 공개 데이터 | 가능 — 공유 캐시 |
| 역할별 공통 데이터 | 가능 — 역할 키 |
| 사용자별 데이터 | 사용자 키 필수 |
| 권한 판정 결과 | 짧게, 무효화 경로 필요 |
| 민감 데이터 | 캐시하지 않음 |

## 권한 판정 캐시

인가 확인을 매번 하면 느리다. 캐시하되 무효화 경로를 만든다.

| 이벤트 | 무효화 |
| --- | --- |
| 역할 변경 | 해당 사용자 |
| 그룹 구성 변경 | 그룹 구성원 전체 |
| 정책 변경 | 전체 |
| 계정 비활성화 | 즉시, 세션 포함 |

계정 비활성화는 즉시 반영돼야 한다. 캐시 만료를 기다리면 퇴사자가 계속 접근한다.

## 점검

\`\`\`bash
# 같은 주소를 두 사용자로 요청해 응답이 섞이는지
a=$(curl -s -H "Authorization: Bearer $TOKEN_A" https://api.example.com/me)
b=$(curl -s -H "Authorization: Bearer $TOKEN_B" https://api.example.com/me)
[ "$a" = "$b" ] && echo "캐시 키에 사용자 정보가 없다"
\`\`\`

## 설계 원칙

개인화된 응답과 공통 응답을 다른 엔드포인트로 나눈다. 공통 부분은 적극적으로 캐시하고 개인 부분은 캐시하지 않으면, 성능과 안전을 함께 얻는다.`,

'search-index-leak': R`## 실제로 이렇게 터진다

전체 문서를 하나의 색인에 넣고 검색 시 필터만 걸었다. 필터를 우회하는 질의가 가능해 다른 부서 문서가 검색됐다.

또 하나는 검색 결과의 미리보기에 본문 일부가 포함된 경우다. 접근 권한이 없는 문서인데도 내용 일부가 노출됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 필터를 걸면 안전하다 | 우회 가능한 경우가 있다 |
| 제목만 보이니 괜찮다 | 제목도 정보다 |
| 미리보기는 짧다 | 반복 질의로 재구성된다 |
| 색인 권한은 원본을 따른다 | 별도로 관리해야 한다 |
| 건수만 나오는 건 안전하다 | 존재 여부가 드러난다 |

## 누출 경로

| 경로 | 내용 |
| --- | --- |
| 검색 결과 목록 | 제목·요약 |
| 미리보기 | 본문 일부 |
| 자동 완성 | 다른 문서의 용어 |
| 결과 건수 | 존재 여부 |
| 집계·패싯 | 분포 정보 |
| 오탈자 교정 | 색인된 단어 |

자동 완성과 오탈자 교정이 자주 간과된다. 색인 전체의 용어를 학습하므로 권한 밖 문서의 고유명사가 제안될 수 있다.

## 안전한 설계

| 방법 | 격리 |
| --- | --- |
| 권한 그룹별 색인 분리 | 강함 |
| 문서에 접근 주체 저장 + 질의 필터 | 중간 |
| 검색 후 권한 재확인 | 보완 |
| 미리보기 별도 권한 확인 | 필수 |

가장 확실한 것은 색인 분리다. 관리 부담이 있지만 우회 위험이 없다.

## 필터 방식을 쓴다면

1. 색인 시 접근 가능 주체를 필드로 저장한다
2. 질의에 사용자 소속을 조건으로 강제 삽입한다
3. 사용자가 조건을 조작할 수 없게 한다
4. 결과 반환 전 문서별 권한을 재확인한다
5. 미리보기는 권한 확인 후 생성한다

3번이 핵심이다. 질의 구성을 클라이언트에 맡기면 조건을 빼거나 바꿀 수 있다. 서버가 조건을 붙인다.

## 점검

\`\`\`bash
# 권한 없는 문서의 내용이 검색으로 드러나는지
curl -s -H "Authorization: Bearer $TOKEN_LOW" \
  "https://api.example.com/search?q=$(printf %s '기밀 프로젝트명' | jq -sRr @uri)" \
  | jq '.hits | length, .[0].highlight'
\`\`\`

건수가 0 이 아니거나 미리보기가 나오면 격리가 되지 않은 것이다.

## 집계 정보

패싯이나 통계에 권한 밖 문서가 포함되면, 개별 문서를 못 봐도 분포를 알 수 있다. 집계에도 같은 필터를 적용한다.`,

'direct-storage-access': R`## 실제로 이렇게 터진다

파일을 저장소에 두고 주소를 직접 노출한 사례가 있다. 주소 규칙이 단순해서 다른 사용자의 파일을 추측할 수 있었다.

또 하나는 접근 통제를 애플리케이션에서만 한 경우다. 저장소 자체는 공개였고, 주소만 알면 앱을 거치지 않고 받을 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 주소가 복잡하면 안전하다 | 유출되면 끝이다 |
| 앱에서 막으면 된다 | 저장소를 직접 부른다 |
| 파일은 민감하지 않다 | 첨부에 개인정보가 있다 |
| 목록이 안 보이면 안전하다 | 추측·유출 경로가 있다 |
| 삭제하면 사라진다 | 캐시·백업에 남는다 |

## 접근 방식

| 방식 | 통제 | 부하 |
| --- | --- | --- |
| 저장소 직접 공개 | 없음 | 최저 |
| 서명 URL | 시간 제한 | 낮음 |
| 애플리케이션 중계 | 요청마다 확인 | 높음 |
| CDN + 서명 쿠키 | 세션 단위 | 낮음 |

민감 파일은 애플리케이션 중계나 짧은 서명 URL 을 쓴다. 저장소를 공개로 두는 것은 진짜 공개 파일에만 해당한다.

## 키 설계

| 방식 | 문제 |
| --- | --- |
| 순차 번호 | 추측 가능 |
| 사용자 식별자 포함 | 열거 가능 |
| 원본 파일명 | 정보 노출 |
| 무작위 식별자 | 권장 |

무작위 식별자를 쓰되, 그것만으로 통제를 대신하지 않는다. 주소가 유출되는 경로는 많다 — 로그, 참조 헤더, 공유, 캡처.

## 저장소 설정 점검

\`\`\`bash
# 공개 접근이 가능한 버킷
aws s3api get-public-access-block --bucket "$B" \
  --query 'PublicAccessBlockConfiguration' 2>/dev/null || echo "차단 설정 없음"

# 버킷 정책에 공개 허용이 있는지
aws s3api get-bucket-policy --bucket "$B" --query Policy --output text 2>/dev/null \
  | grep -o '"Principal":[^,]*' | grep '\*'
\`\`\`

## 업로드 쪽

| 항목 | 통제 |
| --- | --- |
| 파일 형식 | 실제 내용 검사 |
| 크기 | 상한 |
| 실행 권한 | 제거 |
| 저장 경로 | 서버가 결정 |
| 원본 파일명 | 메타데이터로만 보관 |

파일명을 경로에 쓰면 경로 조작이 가능하다. 서버가 무작위 키를 정하고 원본 이름은 별도로 저장한다.

## 정리

쓰지 않는 파일이 계속 쌓인다. 참조되지 않는 객체를 주기적으로 찾아 정리하고, 보존 기간 정책을 건다. 남아 있는 파일은 언젠가 유출될 수 있는 파일이다.`,

'admin-plane-separation': R`## 실제로 이렇게 터진다

관리 기능을 같은 애플리케이션의 경로 하나로 둔 사례가 있다. 일반 사용자와 같은 진입점을 쓰다 보니, 인가 검사가 빠진 엔드포인트 하나가 관리 기능 전체를 노출시켰다.

또 하나는 관리 화면을 인터넷에 열어 둔 경우다. 인증은 있었지만 무차별 시도의 표적이 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 인가만 잘하면 된다 | 실수 하나가 치명적이다 |
| 분리하면 불편하다 | 접속 절차만 다르다 |
| 내부 도구는 위험이 낮다 | 권한이 가장 넓다 |
| 같은 도메인이 편하다 | 쿠키·세션이 섞인다 |
| VPN 이면 충분하다 | 계정 기반 통제도 필요하다 |

## 분리 수준

| 수준 | 방법 |
| --- | --- |
| 경로 분리 | 같은 앱, 다른 경로 — 약함 |
| 도메인 분리 | 쿠키 분리 |
| 애플리케이션 분리 | 코드·배포 분리 |
| 네트워크 분리 | 접근 경로 제한 |
| 인증 분리 | 별도 자격 증명·다중 인증 |

도메인 분리부터가 실질적이다. 쿠키가 섞이지 않으면 사용자 화면의 취약점이 관리 세션으로 이어지지 않는다.

## 관리 평면에 적용할 것

| 항목 | 설정 |
| --- | --- |
| 접근 경로 | 지정 네트워크 또는 인증 프록시 |
| 인증 | 다중 인증 필수 |
| 세션 | 짧은 만료 |
| 감사 | 모든 동작 기록 |
| 위험 동작 | 추가 확인 |
| 대량 작업 | 승인 |

세션 만료가 짧으면 불편하다는 반발이 있다. 실제로는 관리 작업이 짧게 끝나므로 큰 문제가 되지 않는다.

## 점검

\`\`\`bash
# 관리 경로가 외부에서 도달되는지
curl -s -o /dev/null -w '%{http_code}\n' https://example.com/admin
curl -s -o /dev/null -w '%{http_code}\n' https://admin.example.com/

# 사용자 세션 쿠키로 관리 기능이 되는지 — 되면 분리가 안 된 것
curl -s -o /dev/null -w '%{http_code}\n' \
  -H "Cookie: session=$USER_SESSION" https://admin.example.com/api/users
\`\`\`

## 단계적 분리

한 번에 분리하기 어렵다면 순서를 정한다.

1. 관리 경로를 네트워크로 제한한다 — 가장 빠르고 효과가 크다
2. 다중 인증을 요구한다
3. 도메인을 분리한다
4. 애플리케이션을 분리한다

1번만 해도 외부 노출이 사라진다. 나머지는 시간을 두고 진행한다.`,

'capability-reduction': R`## 실제로 이렇게 터진다

기능을 계속 추가하면서 아무것도 없애지 않은 사례가 있다. 몇 년 뒤 아무도 쓰지 않는 기능이 절반이었고, 그중 하나에서 취약점이 나왔다. 유지보수도 되지 않던 코드였다.

또 하나는 관리자 화면에 개발 초기의 디버그 기능이 남아 있던 경우다. 임의 질의 실행이 가능했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 기능은 많을수록 좋다 | 공격면이 늘어난다 |
| 안 쓰면 위험하지 않다 | 유지보수가 안 돼 더 위험하다 |
| 없애면 누가 불편할지 모른다 | 사용량을 측정하면 안다 |
| 제거는 위험하다 | 단계적으로 하면 된다 |
| 개발용 기능은 배포에서 빠진다 | 남는 경우가 많다 |

## 무엇을 줄이나

| 대상 | 확인 |
| --- | --- |
| 사용되지 않는 엔드포인트 | 접근 로그 |
| 옛 버전 API | 호출 통계 |
| 디버그·개발 기능 | 코드 검색 |
| 미사용 의존성 | 빌드 분석 |
| 열린 포트 | 실제 사용 여부 |
| 등록된 통합 | 연동 상태 |
| 권한·역할 | 사용 기록 |

## 측정부터

\`\`\`bash
# 최근 90일간 호출이 없는 엔드포인트 — 제거 후보
awk '{print $7}' /var/log/nginx/access.log \
  | sed 's/?.*//' | sort | uniq -c | sort -n | head -30

# 코드에 있으나 로그에 없는 경로 대조
comm -23 <(grep -rhoE '"/api/[a-z0-9/_-]+"' src/ | tr -d '"' | sort -u) \
         <(awk '{print $7}' access.log | sed 's/?.*//' | sort -u)
\`\`\`

두 목록의 차이가 제거 후보다.

## 제거 절차

1. 사용량을 측정한다
2. 제거 예고를 공지한다
3. 폐기 예정 표시를 응답에 넣는다
4. 일정 기간 뒤 접근을 차단한다 — 코드는 남긴다
5. 문제가 없으면 코드를 제거한다

4번과 5번을 나누는 이유는 되돌리기 위해서다. 차단 상태에서 문제가 발견되면 설정만 바꿔 복구할 수 있다.

## 얻는 것

| 항목 | 효과 |
| --- | --- |
| 공격면 | 줄어든다 |
| 점검 범위 | 줄어든다 |
| 유지보수 부담 | 줄어든다 |
| 빌드·배포 시간 | 짧아진다 |
| 코드 이해 | 쉬워진다 |

새 방어를 추가하는 것보다 쓰지 않는 기능을 없애는 편이 비용 대비 효과가 큰 경우가 많다. 정기적으로 하는 활동으로 만든다.`,
}
