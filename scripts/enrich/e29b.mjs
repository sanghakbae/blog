const R = String.raw
export default {

'third-party-callback': R`## 실제로 이렇게 터진다

결제 연동에서 콜백 서명을 검증하지 않은 사례가 있다. 결제 완료 요청을 직접 만들어 보내자 주문이 완료 처리됐다. 실제 결제는 없었다.

또 하나는 서명은 검증했지만 재사용을 막지 않은 경우다. 같은 콜백을 여러 번 보내 포인트가 중복 적립됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 콜백 주소를 모르면 못 보낸다 | 알아내기 쉽다 |
| 출발지 IP 로 확인하면 된다 | 위조와 변경 위험 |
| 서명 검증이면 충분하다 | 재사용 방지가 필요하다 |
| HTTPS 면 안전하다 | 전송만 보호한다 |
| 콜백 내용을 믿어도 된다 | 원장에서 재확인해야 한다 |

## 검증 항목

| 항목 | 방법 |
| --- | --- |
| 서명 | 공유 비밀 또는 공개키로 검증 |
| 시각 | 허용 범위 내인지 |
| 재사용 | 처리한 식별자 기록 |
| 금액·상태 | 원장 조회로 재확인 |
| 출발지 | 보조 수단으로만 |

가장 확실한 것은 마지막 재확인이다. 콜백은 "확인해 보라" 는 신호로만 쓰고, 실제 상태는 상대 시스템에 직접 물어본다.

## 서명 검증 시 주의

| 실수 | 결과 |
| --- | --- |
| 문자열 비교에 조기 종료 | 타이밍 공격 |
| 원문 재구성 오류 | 검증 우회 |
| 알고리즘을 요청에서 읽음 | 우회 |
| 검증 실패를 로그만 남기고 처리 | 무의미 |

원문 재구성이 자주 문제다. 본문을 파싱한 뒤 다시 직렬화해 서명을 계산하면 원문과 달라진다. 원본 바이트를 그대로 보관해 검증한다.

## 재사용 방지

\`\`\`javascript
// 처리 식별자를 원자적으로 기록 — 이미 있으면 건너뛴다
const first = await redis.set(\`cb:\${provider}:\${eventId}\`, '1', 'NX', 'EX', 86400 * 7)
if (!first) return res.status(200).end()   // 중복, 정상 응답
\`\`\`

성공 응답을 돌려주는 것이 중요하다. 오류를 주면 상대가 계속 재시도한다.

## 응답 규약

| 상황 | 응답 |
| --- | --- |
| 정상 처리 | 200 |
| 중복 수신 | 200 |
| 서명 불일치 | 400, 로그 |
| 일시 장애 | 500 — 재시도 유도 |
| 처리 지연 | 200 후 비동기 처리 |

처리에 시간이 걸리면 먼저 접수 응답을 주고 비동기로 처리한다. 응답이 늦으면 상대가 실패로 보고 재시도한다.`,

'sftp-automation': R`## 실제로 이렇게 터진다

파일 연동 계정에 셸 접근을 함께 준 사례가 있다. 그 계정의 키가 유출되자 파일 전송뿐 아니라 서버 접속까지 됐다.

또 하나는 전송 디렉터리를 격리하지 않은 경우다. 상위 경로 이동으로 다른 고객의 파일에 접근할 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 파일 전송 계정은 위험이 낮다 | 셸 접근이 함께 열리기 쉽다 |
| 디렉터리 지정이면 격리된다 | 상위 이동이 가능하다 |
| 비밀번호 인증이 편하다 | 자동화에는 키가 안전하다 |
| 파일만 주고받는다 | 파일 내용이 민감하다 |
| 전송 후 처리는 별개다 | 파일명 자체가 입력이다 |

## 계정 설정

| 항목 | 설정 |
| --- | --- |
| 셸 | 없음 |
| 강제 명령 | 파일 전송 서브시스템만 |
| 루트 격리 | 전용 디렉터리로 제한 |
| 인증 | 키 기반 |
| 접속 출발지 | 제한 |
| 권한 | 필요한 디렉터리에만 쓰기 |

## 격리 설정에서 자주 하는 실수

격리 디렉터리는 소유자가 관리자여야 하고 다른 사용자에게 쓰기 권한이 없어야 한다. 이 조건을 만족하지 않으면 격리가 적용되지 않고 접속이 실패한다. 실패하면 격리를 포기하는 경우가 많다.

\`\`\`bash
# 격리 디렉터리 조건 확인
stat -c '%U %a %n' /srv/sftp /srv/sftp/partner-a

# 셸 접근이 열린 전송 계정 — 정리 대상
awk -F: '/sftp/ && $7 !~ /(nologin|false)/ {print $1, $7}' /etc/passwd
\`\`\`

## 받은 파일을 다루는 쪽

| 위험 | 대응 |
| --- | --- |
| 파일명에 경로 문자 | 이름 정규화 |
| 파일명에 명령 문자 | 셸을 거치지 않고 처리 |
| 예상 밖 형식 | 형식 검증 |
| 대용량 | 크기 제한 |
| 압축 폭탄 | 해제 크기 제한 |
| 악성코드 | 검사 후 처리 |

파일명을 셸 명령에 넣는 처리가 특히 위험하다. 처리 스크립트에서 파일명을 인용하지 않으면 임의 명령 실행으로 이어진다.

## 운영

| 항목 | 주기 |
| --- | --- |
| 계정 목록 검토 | 반기 |
| 키 교체 | 연 1회 |
| 미사용 계정 정리 | 분기 |
| 전송 로그 확인 | 상시 |
| 오래된 파일 삭제 | 자동 |

전송 디렉터리에 파일이 쌓이면 그 자체가 유출 위험이다. 처리 후 이동하거나 일정 기간 뒤 자동 삭제한다.`,

'bulk-export-risk': R`## 실제로 이렇게 터진다

관리자 화면의 엑셀 내려받기 기능으로 전체 고객 정보가 유출된 사례가 있다. 취약점은 없었다 — 정상 기능이었고, 권한이 있는 계정이 도용됐다.

또 하나는 내려받기 기록을 남기지 않은 경우다. 누가 언제 무엇을 받았는지 알 수 없어 유출 범위를 산정하지 못했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 권한이 있으면 문제없다 | 계정 도용이 흔하다 |
| 정상 기능은 감시 대상이 아니다 | 유출의 주 경로다 |
| 로그는 남는다 | 무엇을 받았는지는 안 남는 경우가 많다 |
| 건수 제한은 불편하다 | 대량 유출을 막는다 |
| 내부 직원은 신뢰한다 | 퇴사 직전 반출이 흔하다 |

## 통제 방법

| 통제 | 효과 |
| --- | --- |
| 건수 상한 | 한 번에 나가는 양 제한 |
| 기간 상한 | 시간당·일별 누적 제한 |
| 사유 입력 | 억제 효과 |
| 승인 절차 | 대량 시 |
| 워터마크 | 유출 시 출처 추적 |
| 상세 기록 | 조건·건수·시각 |
| 알림 | 대량 시 관리자에게 |

건수 상한과 상세 기록이 기본이다. 이 둘만 있어도 대량 유출과 사후 추적이 크게 개선된다.

## 무엇을 기록하나

| 항목 | 이유 |
| --- | --- |
| 누가 | 계정과 실제 사용자 |
| 언제 | 시각 |
| 무슨 조건으로 | 검색 조건 |
| 몇 건 | 반환 행 수 |
| 어떤 필드 | 개인정보 포함 여부 |
| 어디서 | 접속 주소 |

검색 조건과 건수가 핵심이다. 이것이 있어야 나중에 무엇이 나갔는지 재구성할 수 있다.

## 이상 탐지

| 신호 | 판단 |
| --- | --- |
| 평소 대비 건수 급증 | 조사 |
| 조건 없는 전체 조회 | 조사 |
| 업무 시간 외 | 조사 |
| 퇴사 예정자의 반출 | 즉시 조사 |
| 새 계정의 첫 대량 반출 | 조사 |

\`\`\`sql
-- 최근 30일 대량 반출 상위 — 기준선을 벗어난 계정을 찾는다
SELECT actor, date_trunc('day', at) AS d, sum(rows_exported) AS total
FROM export_audit
WHERE at > now() - interval '30 days'
GROUP BY 1, 2 HAVING sum(rows_exported) > 1000
ORDER BY total DESC LIMIT 20;
\`\`\`

## 대안 제공

반출을 막기만 하면 우회 방법을 찾는다. 화면에서 필요한 집계를 볼 수 있게 하거나, 승인된 분석 환경을 제공한다. 반출이 필요한 진짜 이유를 없애는 것이 가장 효과적이다.`,

'notification-abuse': R`## 실제로 이렇게 터진다

인증 문자 발송 기능에 제한이 없던 사례가 있다. 공격자가 같은 번호로 반복 요청해 피해자에게 수백 건의 문자가 갔다. 발송 비용도 함께 나갔다.

또 하나는 알림 내용에 링크를 넣고 그 주소를 요청에서 받은 경우다. 우리 이름으로 피싱 링크가 발송됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 인증 기능이라 제한하면 불편하다 | 제한 없으면 괴롭힘 수단이 된다 |
| 계정당 제한이면 된다 | 대상별 제한도 필요하다 |
| 비용은 크지 않다 | 대량 발송 시 급증한다 |
| 내용은 우리가 정한다 | 요청 값이 들어가면 아니다 |
| 발송 실패는 무시해도 된다 | 공격 신호일 수 있다 |

## 제한 축

| 축 | 기준 |
| --- | --- |
| 요청자 | 계정·IP 당 시간당 |
| 대상 | 수신 번호·주소당 일별 |
| 전역 | 전체 시간당 상한 |
| 비용 | 일 예산 초과 시 중단 |

대상별 제한이 자주 빠진다. 요청자를 바꿔 가며 같은 대상에게 보내는 공격을 막으려면 필요하다.

## 재발송 정책

| 시도 | 대기 |
| --- | --- |
| 1회 | 즉시 |
| 2회 | 60초 |
| 3회 | 180초 |
| 4회 이상 | 차단, 다른 수단 안내 |

점증 대기가 사용자 불편은 적으면서 자동화 공격 비용을 크게 올린다.

## 내용 통제

| 항목 | 원칙 |
| --- | --- |
| 링크 | 서버가 정한 것만 |
| 사용자 입력 | 이름 정도만, 길이 제한 |
| 템플릿 | 고정, 변수만 치환 |
| 발신자 표시 | 고정 |

요청에서 받은 URL 을 그대로 보내는 기능은 만들지 않는다. 필요하면 미리 등록된 목록에서 고르게 한다.

## 비용 감시

\`\`\`bash
# 시간당 발송량 급증 감지 — 비용과 공격을 함께 잡는다
psql -Atc "SELECT date_trunc('hour', at) h, count(*)
           FROM notification_log
           WHERE at > now() - interval '24 hours'
           GROUP BY 1 ORDER BY 1" | awk -F'|' '$2 > 1000 {print "급증:", $0}'
\`\`\`

예산 상한을 걸고 초과 시 자동으로 발송을 멈추는 장치를 둔다. 사람이 알아채기 전에 비용이 커진다.`,

'async-error-handling': R`## 실제로 이렇게 터진다

비동기 처리에서 예외가 발생했지만 아무 데도 기록되지 않은 사례가 있다. 처리가 조용히 실패했고, 데이터가 일부만 반영된 상태로 몇 주가 지났다.

또 하나는 오류 메시지에 처리 중이던 데이터를 그대로 담은 경우다. 로그에 개인정보와 토큰이 남았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 예외는 어디선가 잡힌다 | 비동기에서는 사라진다 |
| 재시도하면 해결된다 | 원인이 남아 있으면 반복된다 |
| 로그에 다 남기면 좋다 | 민감 데이터가 남는다 |
| 실패 큐를 보면 된다 | 보는 사람이 있어야 한다 |
| 부분 실패는 드물다 | 분산 처리에서 흔하다 |

## 반드시 처리할 것

| 항목 | 방법 |
| --- | --- |
| 처리되지 않은 거부 | 전역 핸들러 등록 |
| 실패 큐 | 적재량 감시와 담당자 |
| 재시도 상한 | 무한 재시도 금지 |
| 부분 실패 | 보상 처리 또는 롤백 |
| 타임아웃 | 모든 외부 호출에 |

전역 핸들러가 없으면 비동기 예외가 조용히 사라진다. 언어·런타임마다 등록 방법이 다르므로 확인한다.

## 로그에 무엇을 남기나

| 남긴다 | 남기지 않는다 |
| --- | --- |
| 오류 유형과 위치 | 요청 본문 전체 |
| 처리 식별자 | 개인정보 |
| 재시도 횟수 | 인증 토큰 |
| 소요 시간 | 비밀번호 |
| 대상 식별자 | 결제 정보 |

식별자만 남기고 상세는 필요할 때 조회한다. 그렇게 하면 로그 자체가 유출 대상이 되지 않는다.

## 부분 실패 처리

여러 단계 중 일부만 성공한 상태가 가장 다루기 어렵다.

| 방식 | 적용 |
| --- | --- |
| 보상 트랜잭션 | 각 단계의 되돌리기 정의 |
| 최종 일관성 | 재시도로 수렴 |
| 상태 기계 | 어느 단계까지 갔는지 기록 |

상태를 기록하지 않으면 재시도할 때 어디부터 해야 할지 모른다. 단계마다 상태를 남긴다.

## 감시

\`\`\`bash
# 처리 실패율과 실패 큐 적재 추이 — 조용한 실패를 잡는다
curl -s localhost:9090/metrics | grep -E 'job_failed_total|dlq_depth'
\`\`\`

실패율이 0 인 것이 항상 좋은 신호는 아니다. 처리량도 함께 본다. 처리량이 0 이면 실패율도 0 이다.`,
}
