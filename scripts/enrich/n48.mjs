const R = String.raw
export default {

'prompt-injection-defense': R`## 실제로 이렇게 터진다

문서를 요약하는 기능에서 문서 안에 숨겨진 지시문을 따른 사례가 있다. 흰 글씨로 적힌 문장이 모델에게 다른 행동을 시켰고, 요약 대신 내부 정보를 외부 주소로 보내는 링크를 만들었다.

또 하나는 웹 검색 결과를 읽는 에이전트가 페이지 안의 지시를 실행한 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 프롬프트로 막을 수 있다 | 우회된다 |
| 사용자 입력만 조심하면 된다 | 읽는 문서가 더 위험하다 |
| 필터로 걸러진다 | 표현이 무한하다 |
| 해결된 문제다 | 아직 아니다 |

## 성공해도 피해가 없게

| 조치 | 효과 |
| --- | --- |
| 도구 권한 최소화 | 할 수 있는 것 제한 |
| 읽기·쓰기 분리 | 연쇄 차단 |
| 외부 전송 차단 | 유출 경로 제거 |
| 되돌릴 수 없는 작업 승인 | 최종 방어 |`,

'llm-data-boundary': R`## 실제로 이렇게 터진다

고객 문의 내용을 그대로 외부 모델에 보낸 사례가 있다. 문의에 개인정보가 포함돼 있었고, 처리방침에 위탁 사실이 없었다.

또 하나는 학습 이용 거부 설정을 하지 않은 경우다. 기본값이 학습 허용이었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 기업용은 학습에 안 쓴다 | 계약과 설정을 확인해야 한다 |
| 내부 도구라 괜찮다 | 데이터는 밖으로 나간다 |
| 처리방침은 나중에 고친다 | 위탁 고지가 먼저다 |
| 가공하면 문제없다 | 가공 수준을 정해야 한다 |

## 전송 전 확인

| 질문 | 확인 |
| --- | --- |
| 개인정보가 있나 | 항목 검토 |
| 학습에 쓰이나 | 계약·설정 |
| 어느 국가로 가나 | 이전 고지 |
| 얼마나 보관되나 | 정책 |
| 삭제 요청이 되나 | 절차 |`,

'ai-agent-audit-trail': R`## 실제로 이렇게 터진다

에이전트가 잘못된 데이터를 수정한 뒤 무엇을 했는지 재구성할 수 없던 사례가 있다. 도구 호출 인자를 기록하지 않아 어떤 레코드가 바뀌었는지 몰랐다.

또 하나는 인자를 그대로 기록해 로그에 개인정보가 남은 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 대화만 남기면 된다 | 도구 호출이 실제 행동이다 |
| 인자를 다 남겨야 한다 | 민감 정보가 남는다 |
| 실패만 기록하면 된다 | 성공한 변경이 문제다 |
| 출처는 중요하지 않다 | 주입 조사의 단서다 |

## 기록 수준

| 유형 | 수준 |
| --- | --- |
| 읽기 도구 | 요약 |
| 쓰기 도구 | 상세 |
| 외부 전송 | 상세 |
| 실패 | 상세 |
| 인자 | 해시 또는 요약 |`,

'model-endpoint-security': R`## 실제로 이렇게 터진다

내부용이라 인증을 걸지 않은 추론 엔드포인트가 있었다. 다른 시스템이 침해되자 그 경로로 대량 호출이 들어왔고, 하루 비용이 평소의 40배가 됐다.

또 하나는 요청 수만 제한한 경우다. 긴 입력 하나가 짧은 요청 수백 개보다 비쌌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 내부용이라 인증이 필요 없다 | 침해 후 경로가 된다 |
| 요청 수 제한이면 된다 | 토큰 기준이어야 한다 |
| 비용은 나중에 본다 | 하루 만에 커진다 |
| 출력은 우리 것이다 | 사용자 입력처럼 다룬다 |

## 제한 항목

| 항목 | 기준 |
| --- | --- |
| 토큰 | 계정당 시간·일 |
| 동시 요청 | 계정당 |
| 입력 길이 | 상한 |
| 출력 길이 | 상한 |
| 비용 | 예산 상한 |`,

'training-data-governance': R`## 실제로 이렇게 터진다

고객 데이터를 미세조정에 쓴 사례가 있다. 계약과 처리방침에 그 목적이 없었고, 나중에 문제가 됐을 때 모델을 다시 만들어야 했다.

또 하나는 공개 데이터셋의 라이선스를 확인하지 않은 경우다. 상업적 이용이 금지된 데이터였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 공개 데이터는 자유롭다 | 라이선스가 있다 |
| 우리 고객 데이터는 우리 것이다 | 목적이 정해져 있다 |
| 학습되면 원본과 무관하다 | 재현될 수 있다 |
| 삭제 요청은 원본만 | 모델 대응도 필요하다 |

## 기록할 것

| 항목 | 이유 |
| --- | --- |
| 출처 | 권리 근거 |
| 라이선스 | 사용 조건 |
| 개인정보 처리 | 방법 |
| 사용 모델 | 영향 범위 |
| 삭제 대응 | 방침 |`,

'ai-output-attribution': R`## 실제로 이렇게 터진다

생성된 안내문을 검토 없이 고객에게 보낸 사례가 있다. 존재하지 않는 환불 정책이 적혀 있었고, 고객이 그것을 근거로 요구했다.

또 하나는 자동 응답임을 밝히지 않은 경우다. 사람과 대화한다고 믿은 고객이 불만을 제기했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 대체로 맞으면 쓸 수 있다 | 틀린 하나가 문제다 |
| 도구가 만든 것은 책임이 없다 | 쓴 사람의 책임이다 |
| 표시하면 신뢰가 떨어진다 | 숨기는 것이 더 나쁘다 |
| 모든 결과를 검토한다 | 지속되지 않는다 |

## 용도별 검토

| 용도 | 검토 |
| --- | --- |
| 내부 초안 | 불필요 |
| 고객 안내 | 필수 |
| 법적 문서 | 전문가 |
| 코드 | 리뷰·시험 |
| 수치 | 코드로 계산 |`,

'ai-supply-chain-risk': R`## 실제로 이렇게 터진다

공개 저장소에서 받은 모델을 그대로 로드한 사례가 있다. 직렬화 형식이 코드 실행을 허용했고, 로드하는 순간 스크립트가 돌았다.

또 하나는 확장 도구를 검토 없이 설치한 경우다. 파일 읽기만 필요한 도구가 네트워크 접근 권한을 요구했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 모델은 데이터다 | 형식에 따라 코드가 돈다 |
| 유명 저장소면 안전하다 | 누구나 올린다 |
| 도구는 편의 기능이다 | 권한이 위험이다 |
| 목록은 필요 없다 | 취약점 대응에 필요하다 |

## 도입 검토

| 항목 | 확인 |
| --- | --- |
| 출처 | 공식 계정 |
| 형식 | 안전한 직렬화 |
| 권한 | 기능 대비 과도한가 |
| 유지 상태 | 최근 활동 |
| 내부 저장소 | 미러링 |`,

'ai-red-teaming': R`## 실제로 이렇게 터진다

한 번 시도해 막히는 것을 보고 안전하다고 판단한 사례가 있다. 열 번 반복하자 세 번 통과했다. 모델은 확률적이었다.

또 하나는 단계를 나눈 요청에 대응하지 못한 경우다. 각 단계는 무해했고 결과가 문제였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 한 번 막히면 안전하다 | 확률적이다 |
| 직접 요청만 막으면 된다 | 우회 방식이 많다 |
| 시험은 도입 전에만 | 변경마다 필요하다 |
| 전문가만 할 수 있다 | 반복 시도가 핵심이다 |

## 기록 항목

| 항목 | 이유 |
| --- | --- |
| 시도 입력 | 재현 |
| 성공률 | 실제 위험도 |
| 조건 | 어떤 설정에서 |
| 완화 조치 | 무엇을 바꿨나 |
| 재시험 | 회귀 확인 |`,

'ai-incident-response': R`## 실제로 이렇게 터진다

프롬프트를 콘솔에서 직접 수정하다 문제가 생긴 사례가 있다. 이전 버전이 남아 있지 않아 되돌리지 못했고, 기억에 의존해 다시 작성했다.

또 하나는 잘못된 안내가 나간 것을 알고도 사용자 통지를 하지 않은 경우다. 나중에 더 큰 문제가 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 모델 문제라 어쩔 수 없다 | 설정과 권한 문제인 경우가 많다 |
| 프롬프트는 가볍게 고친다 | 코드처럼 관리한다 |
| 재현 안 되면 해결됐다 | 확률적이다 |
| AI 가 한 일이다 | 책임은 우리에게 있다 |

## 즉시 확인할 것

| 항목 | 확인 |
| --- | --- |
| 최근 변경 | 프롬프트·모델 버전 |
| 입력 문맥 | 무엇이 들어갔나 |
| 도구 호출 | 무엇을 했나 |
| 영향 범위 | 몇 명에게 |
| 되돌림 | 가능한가 |`,

'ai-governance-committee': R`## 실제로 이렇게 터진다

모든 도입 건에 정식 심의를 요구한 사례가 있다. 처리에 한 달이 걸렸고, 팀들이 심의 없이 쓰기 시작했다.

또 하나는 승인 후 조건 이행을 확인하지 않은 경우다. 조건부 승인이 사실상 무조건 승인이었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 심의가 통제다 | 느리면 우회된다 |
| 모든 건을 봐야 한다 | 등급을 나눈다 |
| 승인하면 끝이다 | 조건 이행을 본다 |
| 거부가 안전하다 | 조건부 승인이 낫다 |

## 처리 기한

| 등급 | 기한 |
| --- | --- |
| 낮음 | 3일 |
| 중간 | 1주 |
| 높음 | 2주 |
| 최고 | 4주 |`,
}
