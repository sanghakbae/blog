const R = String.raw
export default {

'security-design-review': R`## 실제로 이렇게 터진다

배포 직전에 검토를 요청받은 사례가 있다. 인가 구조에 문제가 있었지만 이미 데이터 모델이 굳어 고칠 수 없었고, 결국 위험 수용으로 끝났다.

또 하나는 검토에서 구현 방법까지 지시한 경우다. 개발팀이 반발했고 다음부터 요청이 오지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 코드 전에 보면 된다 | 설계 확정 전이어야 한다 |
| 모든 변경을 봐야 한다 | 조건을 정한다 |
| 방법까지 알려줘야 한다 | 무엇이 필요한지만 |
| 회의록이면 된다 | 요구사항으로 등록한다 |

## 검토 대상 조건

| 조건 | 검토 |
| --- | --- |
| 새 외부 연동 | 필수 |
| 인증·인가 변경 | 필수 |
| 개인정보 항목 추가 | 필수 |
| 새 데이터 저장소 | 필수 |
| 화면 수정 | 불필요 |`,

'defense-in-depth-practice': R`## 실제로 이렇게 터진다

웹 방화벽 두 개를 겹쳐 둔 사례가 있다. 같은 우회 기법에 둘 다 통과했고, 계층을 쌓았다는 안심만 남았다.

또 하나는 예방 통제만 쌓은 경우다. 뚫린 뒤에 알아챌 수단이 없어 몇 달간 발견되지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 같은 종류를 겹치면 강해진다 | 함께 뚫린다 |
| 예방이 가장 중요하다 | 탐지와 복구도 필요하다 |
| 많을수록 좋다 | 유지 못 하면 없는 것 |
| 설정하면 동작한다 | 확인해야 안다 |

## 실패 가정 표

| 뚫린 계층 | 다음 계층 |
| --- | --- |
| 웹 방화벽 | 입력 검증 |
| 입력 검증 | 최소 권한 |
| 계정 | 다중 인증 |
| 다중 인증 | 이상 탐지 |
| 서버 | 네트워크 분할 |
| 데이터 접근 | 암호화 |`,

'blast-radius-limitation': R`## 실제로 이렇게 터진다

서버 하나가 침해됐는데 그 서버에 여러 시스템의 자격 증명이 저장돼 있던 사례가 있다. 한 대의 침해가 전체 침해가 됐다.

또 하나는 백업을 같은 계정에 둔 경우다. 침해자가 백업부터 지웠다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 침해를 막으면 된다 | 막지 못할 때를 설계한다 |
| 계정 분리는 번거롭다 | 가장 강한 경계다 |
| 자격 증명 공유가 효율적이다 | 확산 경로가 된다 |
| 백업은 어디 있든 된다 | 함께 지워진다 |

## 우선 분리 대상

| 순위 | 대상 |
| --- | --- |
| 1 | 운영과 비운영 |
| 2 | 백업·로그 계정 |
| 3 | 개인정보 저장소 |
| 4 | 서비스 간 자격 증명 |
| 5 | 네트워크 구간 |`,

'graceful-degradation-security': R`## 실제로 이렇게 터진다

정책 엔진 장애 시 허용으로 동작하도록 만든 사례가 있다. 엔진이 30분 멈춘 동안 모든 요청이 인가 없이 통과했고, 아무도 몰랐다.

또 하나는 반대로 전부 거부로 둔 경우다. 로그 수집 장애 하나로 서비스 전체가 멈췄다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 실패는 드물다 | 반드시 일어난다 |
| 기본값이면 안전하다 | 대개 열리는 쪽이다 |
| 전부 닫는 것이 안전하다 | 가용성 위험이 크다 |
| 시험할 수 없다 | 의도적으로 멈춰 본다 |

## 통제별 실패 동작

| 통제 | 실패 시 |
| --- | --- |
| 인증 | 거부 |
| 인가 | 거부 |
| 암호화 | 거부 |
| 로그 수집 | 허용 + 경보 |
| 이상 탐지 | 허용 + 경보 |`,

'observability-for-security': R`## 실제로 이렇게 터진다

보안 전용 수집 체계를 새로 만든 사례가 있다. 운영팀이 이미 같은 데이터를 수집하고 있었고, 두 시스템의 숫자가 달라 논의가 어긋났다.

또 하나는 추적 데이터에 요청 본문을 통째로 넣은 경우다. 개발자 대부분이 볼 수 있는 시스템에 개인정보가 쌓였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 보안 데이터는 따로 모은다 | 이미 있는 것이 많다 |
| 많이 담을수록 좋다 | 민감 정보가 들어간다 |
| 운영 보존 기간이면 된다 | 조사에는 짧다 |
| 지표는 성능용이다 | 공격 신호가 보인다 |

## 추가하면 좋은 필드

| 필드 | 용도 |
| --- | --- |
| 주체 식별자 | 누구의 요청 |
| 인가 결과 | 허용·거부 |
| 접근 범위 | 무엇을 조회 |
| 출발지 | 어디서 |
| 클라이언트 | 무엇으로 |`,

'legacy-system-security': R`## 실제로 이렇게 터진다

지원이 끝난 시스템을 격리 없이 그대로 둔 사례가 있다. 알려진 취약점으로 침해됐고, 그 시스템이 내부망 전체에 접근할 수 있었다.

또 하나는 보완 통제를 적용했지만 교체 계획이 없던 경우다. 매년 "내년에" 가 반복됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 못 고치면 방법이 없다 | 감쌀 수 있다 |
| 오래됐지만 잘 돌아간다 | 취약점은 쌓인다 |
| 보완하면 해결이다 | 임시 방편이다 |
| 아무도 안 쓴다 | 네트워크에 있다 |

## 보완 통제

| 수단 | 효과 |
| --- | --- |
| 네트워크 격리 | 가장 확실 |
| 앞단 인증 프록시 | 접근 통제 |
| 감시 강화 | 조기 발견 |
| 데이터 최소화 | 피해 축소 |
| 교체 계획 | 근본 해결 |`,

'security-debt-management': R`## 실제로 이렇게 터진다

미조치 항목을 각자 관리한 사례가 있다. 전체가 몇 건인지 아무도 몰랐고, 경영진은 문제가 없다고 생각했다.

또 하나는 만료 없는 예외가 쌓인 경우다. 3년 전 임시 예외가 그대로 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 급한 것부터 하면 된다 | 목록이 없으면 판단 불가 |
| 부채는 자연히 준다 | 늘기만 한다 |
| 목록을 만들면 비난받는다 | 근거가 된다 |
| 예외는 임시다 | 만료가 없으면 영구다 |

## 관리 지표

| 지표 | 의미 |
| --- | --- |
| 총 항목 수 | 규모 |
| 위험 가중 합계 | 실질 위험 |
| 평균 경과 일수 | 방치 정도 |
| 신규 대비 해소 | 증감 |
| 기한 초과 비율 | 관리 상태 |`,

'change-freeze-security': R`## 실제로 이렇게 터진다

연말 동결 기간에 심각 취약점이 공개된 사례가 있다. 긴급 배포 절차가 없어 승인 경로를 찾는 데 이틀이 걸렸고, 그 사이 스캔 트래픽이 들어왔다.

또 하나는 동결 해제 후 밀린 변경을 한꺼번에 배포한 경우다. 장애가 났고 원인을 찾기 어려웠다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 동결하면 안정적이다 | 패치 지연 위험이 생긴다 |
| 보안 패치도 동결 대상이다 | 판단 기준이 필요하다 |
| 해제 후 한 번에 올린다 | 순차 배포가 안전하다 |
| 그때 정하면 된다 | 절차가 미리 있어야 한다 |

## 동결 전 준비

| 항목 | 조치 |
| --- | --- |
| 미조치 취약점 | 앞당겨 처리 |
| 긴급 절차 | 승인자 지정 |
| 대응 인력 | 확보 |
| 해제 후 목록 | 순차 계획 |`,

'security-automation-priorities': R`## 실제로 이렇게 터진다

사고 대응 자동 차단을 먼저 만든 사례가 있다. 오탐 하나가 정상 서비스를 막았고, 자동화 전체가 중단됐다.

또 하나는 자동화 스크립트가 조용히 실패한 경우다. 몇 달간 점검이 돌지 않았는데 아무도 몰랐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 어려운 것부터 자동화한다 | 반복되는 것부터 |
| 자동 조치가 목표다 | 정보 수집이 먼저다 |
| 만들면 유지된다 | 자동화도 감시 대상이다 |
| 실패하면 알 수 있다 | 조용히 멈춘다 |

## 우선순위 판단

| 조건 | 판단 |
| --- | --- |
| 반복 + 판단 불필요 | 최우선 |
| 반복 + 판단 필요 | 정보 수집만 |
| 드묾 + 되돌릴 수 없음 | 자동화 금지 |
| 실수 비용 큼 | 검증 강화 |`,

'security-runbook-writing': R`## 실제로 이렇게 터진다

절차서 첫 세 쪽이 배경 설명이던 사례가 있다. 급한 상황에서 아무도 끝까지 읽지 못했고, 결국 기억에 의존해 대응했다.

또 하나는 사내 위키에만 둔 경우다. 인증 시스템이 침해되자 문서에도 접근할 수 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 자세하면 도움이 된다 | 급할 때 못 읽는다 |
| 배경을 먼저 설명한다 | 행동이 먼저다 |
| 온라인에 두면 된다 | 접근 못 할 수 있다 |
| 한 번 쓰면 된다 | 사용 후 고쳐야 한다 |

## 구조

| 순서 | 내용 |
| --- | --- |
| 1 | 이 문서를 쓸 상황 |
| 2 | 조치 단계·명령 |
| 3 | 판단 지점 |
| 4 | 확인 방법 |
| 5 | 배경 설명 |`,

'security-tool-evaluation': R`## 실제로 이렇게 터진다

시연만 보고 도입한 사례가 있다. 실제 환경에서 오탐이 쏟아졌고, 6개월 뒤 아무도 쓰지 않았다. 계약은 3년이었다.

또 하나는 데이터량 기반 요금제를 쓴 경우다. 로그가 늘면서 비용이 예상의 세 배가 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 시연으로 판단할 수 있다 | 우리 환경에서 봐야 한다 |
| 기능이 많으면 좋다 | 쓰는 것만 의미 있다 |
| 계약하면 쓰게 된다 | 운영 인력이 있어야 쓴다 |
| 종료는 나중에 생각한다 | 이관 조건이 없으면 종속된다 |

## 도입 전 확인

| 항목 | 방법 |
| --- | --- |
| 오탐률 | 2주 이상 시범 |
| 성능 영향 | 부하 시간대 |
| 비용 | 성장 반영 계산 |
| 이관 | 데이터 반출 가능성 |
| 종료 조건 | 사전 합의 |`,
}
