const R = String.raw
export default {

'data-subject-request': R`## 실제로 이렇게 터진다

열람 요구를 받았는데 데이터가 어디에 있는지 몰라 기한을 넘긴 사례가 있다. 운영 데이터베이스 외에 로그, 분석 저장소, 백업, 외부 도구에 흩어져 있었고, 목록이 없었다.

본인 확인이 약해 다른 사람의 데이터를 준 경우도 있다. 이름과 생년월일만으로 확인했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 요구가 오면 그때 찾으면 된다 | 기한 안에 못 찾는다 |
| 운영 데이터베이스만 보면 된다 | 사본이 여러 곳에 있다 |
| 삭제 요구는 전부 삭제다 | 보관 의무가 있는 항목은 예외다 |
| 본인 확인은 형식이다 | 잘못 주면 그 자체가 유출이다 |
| 무료로 무제한 처리한다 | 과도한 반복은 제한할 수 있다 |

## 요구 유형별 처리

| 요구 | 기한 | 주의 |
| --- | --- | --- |
| 열람 | 10일 이내 | 제3자 정보 제외 |
| 정정·삭제 | 10일 이내 | 보관 의무 항목은 사유 통지 |
| 처리 정지 | 10일 이내 | 계약 이행 필요 시 거절 사유 통지 |
| 전송 | 별도 기준 | 형식과 범위 확인 |

거절할 때도 사유를 통지해야 한다. 무응답이 가장 나쁜 처리다.

## 미리 준비할 것

1. 개인정보가 저장된 위치 목록을 만든다 — 운영·로그·분석·백업·외부 도구
2. 식별자로 전 위치를 조회하는 절차를 만든다
3. 본인 확인 기준을 정한다 — 유출 가능한 정보만으로는 부족
4. 삭제 시 보관 의무 항목을 분리하는 방법을 정한다
5. 처리 이력을 남긴다 — 요구·확인·처리·통지

\`\`\`sql
-- 한 사람의 데이터가 어디에 있는지 — 위치 목록이 있어야 만들 수 있다
SELECT 'users' AS src, count(*) FROM users WHERE id = $1
UNION ALL SELECT 'orders', count(*) FROM orders WHERE user_id = $1
UNION ALL SELECT 'comments', count(*) FROM comments WHERE author_id = $1
UNION ALL SELECT 'access_log', count(*) FROM privacy_access_log WHERE subject_id = $1;
\`\`\`

이 질의를 미리 만들어 두면 요구가 왔을 때 몇 분이면 답할 수 있다.`,

'cross-border-transfer': R`## 실제로 이렇게 터진다

클라우드 기본 리전이 해외였던 사례가 있다. 국내 서비스라고 생각했지만 로그 저장소와 오류 추적 도구가 해외에 있었고, 국외 이전 고지가 없었다. 심사에서 지적됐다.

위탁과 이전을 혼동한 경우도 있다. 국내 업체에 위탁했는데 그 업체가 해외 클라우드를 쓰면 결과적으로 국외로 나간다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 국내 업체면 국내 처리다 | 재위탁 경로를 봐야 한다 |
| 서버만 국내면 된다 | 백업·로그·지원 접근도 포함 |
| 동의받으면 다 된다 | 고지 항목이 정해져 있다 |
| 계약서에 있으면 충분하다 | 고지·공개 의무가 별도다 |
| 익명화하면 무관하다 | 가명처리는 여전히 대상이다 |

## 확인할 경로

| 경로 | 흔히 놓치는 것 |
| --- | --- |
| 주 저장소 | 리전 설정 |
| 백업·복제 | 다른 리전으로 복제 |
| 로그·분석 | 외부 서비스가 해외 |
| 오류 추적 | 요청 본문이 함께 전송 |
| 고객 지원 도구 | 상담 이력에 개인정보 |
| 원격 지원 | 해외 인력의 접근 |
| 재위탁 | 수탁자의 하위 업체 |

원격 지원 접근을 자주 놓친다. 데이터가 국내에 있어도 해외에서 열람하면 이전으로 볼 수 있다.

## 점검 절차

1. 개인정보가 흐르는 시스템 목록을 만든다
2. 각각의 저장·백업·로그 위치를 확인한다
3. 외부 서비스는 데이터 처리 위치를 문서로 확인한다
4. 접근하는 인력의 소재도 확인한다
5. 해당하면 고지 항목을 갖추고 처리방침에 공개한다

\`\`\`bash
# 자원이 어느 리전에 있는지 전수 확인
for r in $(aws ec2 describe-regions --query 'Regions[].RegionName' --output text); do
  n=$(aws ec2 describe-instances --region "$r" --query 'length(Reservations[].Instances[])' --output text 2>/dev/null)
  b=$(aws rds describe-db-instances --region "$r" --query 'length(DBInstances)' --output text 2>/dev/null)
  [ "\${n:-0}" != "0" ] || [ "\${b:-0}" != "0" ] && echo "$r 인스턴스 $n 데이터베이스 $b"
done

# 버킷의 리전과 복제 대상
for b in $(aws s3api list-buckets --query 'Buckets[].Name' --output text); do
  loc=$(aws s3api get-bucket-location --bucket "$b" --query LocationConstraint --output text 2>/dev/null)
  printf '%-40s %s\n' "$b" "$loc"
done
\`\`\``,

'consent-management': R`## 실제로 이렇게 터진다

가입 화면에서 전체 동의 하나로 처리한 사례가 있다. 필수와 선택이 묶여 있었고, 선택을 거부하면 가입이 안 됐다. 마케팅 수신 거부를 원한 사용자는 가입할 수 없었다.

철회 경로가 없던 경우도 흔하다. 동의는 한 번에 받았는데 철회하려면 고객센터에 전화해야 했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 전체 동의가 편하다 | 항목별 분리가 원칙이다 |
| 선택을 거부하면 가입을 막아도 된다 | 필수가 아니면 서비스를 제공해야 한다 |
| 한 번 받으면 계속 유효하다 | 목적이 바뀌면 다시 받는다 |
| 철회는 예외 절차다 | 동의만큼 쉬워야 한다 |
| 기록은 남기지 않아도 된다 | 입증 책임이 우리에게 있다 |

## 무엇을 기록하는가

동의를 받았다는 사실만으로는 부족하다. 나중에 입증할 수 있어야 한다.

| 항목 | 이유 |
| --- | --- |
| 동의 항목 | 무엇에 동의했는지 |
| 동의 시각 | 언제 |
| 동의 방식 | 어떤 화면·문구로 |
| 문구 버전 | 그때 무엇을 보여줬는지 |
| 철회 시각 | 언제까지 유효했는지 |

문구 버전이 중요하다. 화면이 바뀌면 그때 사용자가 본 내용을 재현할 수 없다.

## 화면 설계 원칙

1. 필수와 선택을 시각적으로 분리한다
2. 선택 항목은 기본 미선택으로 둔다
3. 전체 동의 버튼을 두더라도 개별 조작이 가능해야 한다
4. 목적별로 나눈다 — 마케팅, 제3자 제공, 국외 이전
5. 철회를 설정 화면에서 바로 할 수 있게 한다
6. 철회 시 그 목적의 처리를 실제로 멈춘다

\`\`\`sql
-- 동의 기록 구조 — 철회까지 남긴다
CREATE TABLE consents (
  id           bigserial PRIMARY KEY,
  user_id      bigint      NOT NULL,
  purpose      text        NOT NULL,        -- marketing, third_party, overseas
  granted      boolean     NOT NULL,
  policy_ver   text        NOT NULL,        -- 그때 보여준 문구 버전
  channel      text        NOT NULL,        -- signup, settings, popup
  at           timestamptz NOT NULL DEFAULT now()
);
-- 현재 상태는 목적별 최신 행으로 판단한다
\`\`\``,

'wifi-segmentation': R`## 실제로 이렇게 터진다

방문객용 무선망이 업무망과 같은 구간이었던 사례가 있다. 비밀번호는 회의실 벽에 붙어 있었고, 그 망에서 내부 파일 서버가 보였다.

업무용 무선망에 공유 비밀번호를 쓴 경우도 흔하다. 퇴사자가 알고 있었고 바꾸지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 비밀번호가 있으면 안전하다 | 공유 비밀번호는 회수가 안 된다 |
| 방문객망은 분리돼 있다 | 이름만 다르고 같은 구간인 경우가 많다 |
| 무선은 유선보다 위험하다 | 구간 설계가 같으면 같다 |
| 숨긴 이름은 안전하다 | 탐지된다 |
| 사무실 안이라 신뢰할 수 있다 | 주차장에서도 잡힌다 |

## 어떻게 나누는가

| 망 | 접근 가능 | 인증 |
| --- | --- | --- |
| 업무 | 업무 시스템 | 기업용 인증, 기기 인증서 |
| 방문객 | 인터넷만 | 임시 코드, 만료 |
| 기기·IoT | 지정된 서버만 | 기기별 자격 증명 |
| 관리 | 관리 대상 장비 | 별도 인증, 유선 우선 |

업무망에 개인 자격 증명 기반 인증을 쓰면 퇴사 시 회수가 자동으로 된다. 공유 비밀번호는 그것이 불가능하다.

## 점검 항목

1. 각 망이 실제로 다른 구간인지 — 이름이 아니라 통신으로 확인
2. 방문객망에서 내부로 가는 경로가 있는지
3. 무선 컨트롤러 관리 화면이 어느 망에서 접근되는지
4. 등록된 기기 목록과 재직자 명부가 맞는지
5. 지원 종료된 접속 장비가 있는지

\`\`\`bash
# 방문객망에서 내부 대역이 보이는지 (방문객망에 연결한 상태에서)
for net in 10.0.0.1 172.16.0.1 192.168.10.1; do
  timeout 2 bash -c "echo > /dev/tcp/$net/445" 2>/dev/null && echo "$net 도달 가능 — 분리 실패"
done
ip route show | head
\`\`\``,

'egress-filtering': R`## 실제로 이렇게 터진다

침해된 서버가 외부로 데이터를 내보낸 사례에서, 인바운드는 촘촘했지만 아웃바운드는 전면 허용이었다. 공격자는 흔한 포트로 나갔고 아무 경보도 없었다.

아웃바운드를 막으려다 서비스가 멈춘 경우도 있다. 패키지 저장소와 시각 동기화, 인증서 검증 경로를 빠뜨렸다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 나가는 것은 위험하지 않다 | 반출과 명령 수신 경로다 |
| 방화벽이 이미 막고 있다 | 대개 아웃바운드는 전면 허용이다 |
| 허용 목록은 관리가 불가능하다 | 서버 역할별로는 짧다 |
| 프록시를 두면 다 보인다 | 우회 경로가 남으면 안 보인다 |
| 클라우드는 자동으로 제한된다 | 기본은 전체 허용이다 |

## 무엇부터 막는가

전면 차단은 어렵다. 위험이 큰 것부터 좁힌다.

| 순서 | 대상 | 이유 |
| --- | --- | --- |
| 1 | 데이터베이스 서버 | 나갈 일이 거의 없다 |
| 2 | 내부 전용 서비스 | 외부 호출이 없다 |
| 3 | 배치·워커 | 대상이 정해져 있다 |
| 4 | 애플리케이션 서버 | 외부 연동 목록화 필요 |
| 5 | 개발자 단말 | 업무 영향이 크다 |

## 빠뜨리기 쉬운 목적지

| 대상 | 용도 |
| --- | --- |
| 패키지 저장소 | 설치·갱신 |
| 시각 서버 | 동기화 — 어긋나면 인증이 깨진다 |
| 인증서 검증 | 폐기 목록 조회 |
| 클라우드 API | 메타데이터·서비스 호출 |
| 로그·모니터링 | 수집기 전송 |
| 컨테이너 레지스트리 | 이미지 내려받기 |

시각 서버를 빠뜨리면 며칠 뒤 인증이 실패하기 시작한다. 원인을 찾기 어려운 유형이다.

\`\`\`bash
# 지금 실제로 어디로 나가는지 — 허용 목록의 근거를 만든다
ss -tanp state established 2>/dev/null |
  awk 'NR>1 {split($5,a,":"); print a[1]}' | sort | uniq -c | sort -rn | head -20

# 흐름 로그로 목적지를 집계한다 (클라우드)
aws logs filter-log-events --log-group-name /vpc/flowlogs \
  --start-time "$(($(date +%s) - 86400))000" --filter-pattern 'ACCEPT' \
  --query 'events[].message' --output text 2>/dev/null |
  awk '{print $5}' | sort | uniq -c | sort -rn | head -20
\`\`\``,
}
