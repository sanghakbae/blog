const R = String.raw
export default {

'pod-security-standards': R`## 예외를 어떻게 격리하나

호스트 접근이 필요한 워크로드가 반드시 있다. 예외를 어디에 두느냐가 관리 가능성을 정한다.

| 방식 | 관리 |
| --- | --- |
| 전용 네임스페이스 | 예외가 한곳에 보인다 |
| 네임스페이스별 수준 하향 | 어디에 예외가 있는지 흐려진다 |
| 파드별 주석 예외 | 추적 불가 |
| 클러스터 분리 | 가장 강함, 비용 큼 |

전용 네임스페이스 방식이 실무적이다. 그 네임스페이스에 무엇이 있는지 목록으로 관리하고, 새 워크로드가 들어올 때 정말 필요한지 확인한다.

## 전환 중 주의

경고 모드에서 강제로 바꿀 때 기존 워크로드의 재시작이 실패할 수 있다.

| 확인 | 시점 |
| --- | --- |
| 현재 실행 중 파드의 위반 여부 | 전환 전 |
| 자동 확장 시 재생성 | 전환 전 |
| 배포 파이프라인 영향 | 전환 전 |
| 롤백 계획 | 준비 |

실행 중인 파드는 정책 변경에 영향받지 않지만, 재시작되는 순간 막힌다. 스케일 아웃이 안 되는 상태가 되므로 전환 전에 확인한다.`,

'k8s-rbac-audit': R`## 권한 설계 기준

역할을 만들 때 범위와 동사를 함께 정한다. 둘 중 하나만 좁히면 부족하다.

| 대상 | 범위 | 동사 |
| --- | --- | --- |
| 개발자 | 자기 네임스페이스 | get·list·create·update |
| 운영자 | 전 네임스페이스 | get·list·update |
| 조회 전용 | 전 네임스페이스 | get·list·watch |
| 배포 파이프라인 | 지정 네임스페이스 | apply 관련만 |
| 관리자 | 전체 | 전체 |

개발자에게 삭제 권한을 주지 않으면 실수로 인한 사고가 크게 준다. 필요하면 그때 임시로 부여한다.

## 서비스 계정 정리

파드마다 서비스 계정이 만들어지지만 대부분 API 접근이 필요 없다.

| 확인 | 조치 |
| --- | --- |
| API 접근이 필요한가 | 아니면 토큰 마운트 끔 |
| 어떤 자원에 접근하나 | 그것만 허용 |
| 기본 계정을 쓰나 | 전용 계정 생성 |
| 클러스터 범위 권한 | 네임스페이스로 축소 |

기본 서비스 계정에 권한을 주지 않는 것이 원칙이다. 그 계정은 모든 파드가 공유한다.`,

'container-image-minimal': R`## 언어별 선택

정적 링크 가능 여부에 따라 도달할 수 있는 수준이 다르다.

| 언어 | 최소 이미지 |
| --- | --- |
| Go·Rust | 빈 이미지 + 정적 바이너리 |
| Java | 배포 전용 + 런타임 |
| Node·Python | 배포 전용 + 런타임 |
| PHP·Ruby | 경량 배포판 |
| C 계열 | 정적 링크 시 빈 이미지 |

인터프리터 언어는 런타임이 필요해 빈 이미지까지 가기 어렵다. 배포 전용 이미지가 현실적인 목표다.

## 전환 후 확인

이미지를 바꾸면 동작이 달라질 수 있다. 배포 전에 확인한다.

| 항목 | 확인 |
| --- | --- |
| 시간대 데이터 | 없으면 시각이 어긋난다 |
| 인증서 저장소 | 없으면 외부 연결 실패 |
| 로케일 | 문자 처리 영향 |
| 사용자·그룹 | 파일 권한 |
| 진입점 | 셸이 없으면 직접 실행 |

시간대 데이터와 인증서가 빠져 문제가 되는 경우가 가장 흔하다. 배포 전용 이미지는 이 둘을 포함하는 변형을 제공한다.`,

'k8s-node-hardening': R`## 노드 그룹 분리 기준

모든 워크로드를 같은 노드에 두면 하나가 뚫렸을 때 전부 영향받는다.

| 기준 | 분리 |
| --- | --- |
| 개인정보 처리 | 별도 그룹 |
| 결제 처리 | 별도 그룹 |
| 인터넷 노출 | 별도 그룹 |
| 배치·분석 | 별도 그룹 |
| 일반 서비스 | 공통 |

분리한 그룹에는 해당 워크로드만 배치되도록 노드 선택 조건과 오염 설정을 함께 건다. 조건만 걸면 다른 파드가 들어올 수 있다.

## 노드 교체 전략

노드를 고치기보다 교체하는 편이 안전하다. 상태를 남기지 않는 구성이면 교체가 쉽다.

| 상황 | 처리 |
| --- | --- |
| 패치 필요 | 새 이미지로 교체 |
| 침해 의심 | 격리 후 교체 |
| 설정 변경 | 새 그룹 생성 후 이전 |
| 문제 조사 | 스케줄 제외 후 보존 |

교체 방식이면 노드에 접속할 이유가 대부분 사라진다. 접속 권한을 회수할 수 있게 된다.`,

'k8s-secret-handling': R`## 외부 관리 서비스 연동 방식

세 가지 방식이 있고 각각 적합한 상황이 다르다.

| 방식 | 애플리케이션 변경 | 동기화 |
| --- | --- | --- |
| 연동 컨트롤러 | 없음 | 주기적 |
| 사이드카 주입 | 없음 | 실시간 |
| 직접 조회 | 필요 | 실시간 |

연동 컨트롤러가 도입이 쉽다. 외부 값을 클러스터 비밀 객체로 동기화하므로 기존 애플리케이션이 그대로 동작한다.

다만 클러스터에 값이 복사되므로 저장소 암호화는 여전히 필요하다.

## 회전 처리

비밀이 바뀌었을 때 애플리케이션이 새 값을 읽어야 한다.

| 방식 | 반영 |
| --- | --- |
| 환경 변수 | 재시작 필요 |
| 파일 마운트 | 자동 갱신, 앱이 다시 읽어야 함 |
| 직접 조회 | 즉시 |

파일 마운트는 내용이 갱신되지만 애플리케이션이 시작할 때 한 번만 읽으면 소용없다. 파일 변경을 감지해 다시 읽는 처리가 필요하다.`,

'admission-control-policy': R`## 정책 배치 순서

여러 정책이 있으면 무엇을 먼저 거는지가 중요하다. 효과가 크고 부작용이 적은 것부터 한다.

| 정책 | 효과 | 부작용 |
| --- | --- | --- |
| 이미지 저장소 제한 | 큼 | 낮음 |
| latest 태그 금지 | 중간 | 낮음 |
| 자원 제한 필수 | 중간 | 중간 |
| 레이블 필수 | 중간 | 낮음 |
| 서명 검증 | 큼 | 높음 |
| 보안 설정 강제 | 큼 | 높음 |

이미지 저장소 제한이 첫 번째다. 외부에서 임의 이미지를 가져오는 경로가 막히고, 기존 워크로드에 미치는 영향이 작다.

## 웹훅 운영

검사 웹훅이 클러스터의 단일 실패 지점이 되지 않게 한다.

| 항목 | 설정 |
| --- | --- |
| 인스턴스 수 | 다중 |
| 배치 | 여러 노드 |
| 시간 제한 | 짧게 |
| 실패 동작 | 정책별 차등 |
| 제외 네임스페이스 | 시스템 |

시스템 네임스페이스를 제외하지 않으면 웹훅 자체가 배포되지 못하는 순환이 생긴다.`,

'container-runtime-detection': R`## 규칙 조정 순서

기본 규칙 세트는 우리 환경에 맞지 않는다. 조정 없이 켜면 경보가 쏟아진다.

| 순서 | 조치 |
| --- | --- |
| 1 | 기록만 하는 모드로 시작 |
| 2 | 일주일 수집 |
| 3 | 발생량 상위 규칙 확인 |
| 4 | 정상 동작 제외 |
| 5 | 등급별 경보 활성화 |

정상 동작 제외 시 무엇을 포기하는지 기록한다. 나중에 그 영역에서 사고가 나면 왜 탐지되지 않았는지 설명할 수 있어야 한다.

## 대응 자동화 범위

탐지 후 무엇까지 자동으로 할지 정한다.

| 조치 | 자동화 |
| --- | --- |
| 기록·알림 | 즉시 |
| 네트워크 격리 | 검증 후 |
| 파드 종료 | 신중히 |
| 노드 격리 | 사람 판단 |

파드 종료를 자동화하면 오탐 하나가 서비스 중단이 된다. 네트워크 격리가 더 안전한 첫 자동 조치다. 증거도 남는다.`,

'image-signing-verification': R`## 외부 이미지 처리

공개 이미지를 그대로 쓰면 검증 조건을 통일할 수 없다. 내부 저장소를 거치게 한다.

| 단계 | 조치 |
| --- | --- |
| 1 | 공개 이미지 검토 |
| 2 | 내부 저장소로 복사 |
| 3 | 우리 서명 부착 |
| 4 | 다이제스트 기록 |
| 5 | 운영은 내부에서만 |

이 구조면 검증 조건이 하나로 유지되고, 외부 저장소 장애나 이미지 삭제에도 영향받지 않는다.

## 서명 키 관리

서명 키가 유출되면 검증 전체가 무의미해진다.

| 방식 | 관리 부담 |
| --- | --- |
| 키 없는 서명 | 낮음 — 신원 기반 |
| 관리 서비스 키 | 중간 |
| 하드웨어 보관 | 높음 |
| 파일 보관 | 위험 |

키 없는 방식이면 관리할 키가 없다. 파이프라인의 신원으로 서명하고 투명성 로그에 기록되므로 사후 검증도 가능하다.`,

'sidecar-trust': R`## 사이드카 도입 판단

편의만 보고 붙이면 신뢰 경계가 흐려진다. 대안과 비교한다.

| 기능 | 사이드카 | 대안 |
| --- | --- | --- |
| 로그 수집 | 파드마다 | 노드 에이전트 |
| 지표 수집 | 파드마다 | 노드 에이전트 |
| 프록시 | 필요 | — |
| 비밀 주입 | 초기화 컨테이너 | 연동 컨트롤러 |
| 인증 처리 | 사이드카 | 게이트웨이 |

로그와 지표는 노드 단위 에이전트가 낫다. 파드마다 붙이면 자원 소모가 크고 신뢰 경계도 늘어난다.

## 필요한 경우의 최소화

사이드카를 써야 한다면 공유를 줄인다.

| 항목 | 설정 |
| --- | --- |
| 볼륨 | 필요한 것만 마운트 |
| 프로세스 이름공간 | 공유 끔 |
| 서비스 계정 토큰 | 필요 없으면 끔 |
| 보안 설정 | 주 컨테이너와 동일 수준 |
| 이미지 | 검토·서명 |

주 컨테이너의 로컬 관리 포트에 인증을 거는 것도 함께 한다. 네트워크는 어차피 공유된다.`,

'k8s-audit-log-use': R`## 정책 설계

수준을 자원별로 나누되 규칙이 너무 많아지지 않게 한다.

| 규칙 순서 | 대상 | 수준 |
| --- | --- | --- |
| 1 | 상태 확인·읽기 다수 | 기록 안 함 |
| 2 | 비밀 | 메타데이터 |
| 3 | 권한 관련 자원 | 요청 본문 |
| 4 | 파드·배포 | 요청 본문 |
| 5 | 나머지 변경 | 메타데이터 |
| 6 | 나머지 조회 | 기록 안 함 |

먼저 매칭된 규칙이 적용되므로 순서가 중요하다. 제외 규칙을 앞에 두면 양이 크게 준다.

## 보관과 조회

로그를 클러스터 밖으로 즉시 내보내고 조회 수단을 준비한다.

| 항목 | 설정 |
| --- | --- |
| 전송 | 즉시, 별도 계정 |
| 보관 | 1년 이상 |
| 삭제 방지 | 객체 잠금 |
| 조회 | 미리 만든 질의 |
| 경보 | 익명 접근·권한 변경 |

조회 질의를 미리 만들어 절차서에 넣는다. 사고 시점에 작성하면 시간이 걸리고 실수도 생긴다.`,
}
