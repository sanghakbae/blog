const R = String.raw
export default {

'envelope-encryption-design': R`## 성능과 비용 계산

구조를 정하기 전에 실제 호출량을 계산한다. 데이터 키 범위가 좁을수록 격리는 좋아지고 호출은 늘어난다.

| 범위 | 일일 키 서비스 호출 | 격리 |
| --- | --- | --- |
| 레코드마다 | 처리 건수만큼 | 최상 |
| 테넌트마다 | 테넌트 수 × 캐시 만료 | 좋음 |
| 테이블마다 | 매우 적음 | 약함 |

테넌트 단위에 데이터 키 캐시를 붙이면 호출이 크게 준다. 캐시 만료를 짧게 두면 키 폐기가 빨리 반영된다.

## 재암호화 계획

마스터 키를 교체하면 감싼 키만 다시 감싸면 되지만, 데이터 키 자체를 바꾸려면 데이터를 다시 암호화해야 한다.

| 교체 대상 | 작업량 |
| --- | --- |
| 마스터 키 | 암호문 키 재감싸기 |
| 데이터 키 | 전체 재암호화 |
| 알고리즘 | 전체 재암호화 |

데이터 키 교체가 필요한 상황을 미리 정하고, 그때 쓸 배치를 준비해 둔다. 유출 사고 중에 만들려고 하면 시간이 걸린다.`,

'crypto-inventory': R`## 목록에서 무엇을 판단하나

수집한 목록으로 답해야 할 질문이 정해져 있다. 그 질문에 답할 수 없으면 항목이 부족한 것이다.

| 질문 | 필요한 항목 |
| --- | --- |
| 이 알고리즘을 어디서 쓰나 | 알고리즘·사용처 |
| 이 키가 유출되면 무엇이 영향받나 | 키·보호 대상 |
| 규정 요구를 만족하나 | 알고리즘·키 길이 |
| 교체하려면 무엇을 고쳐야 하나 | 사용처·의존 관계 |
| 누구에게 물어보나 | 담당 |

네 번째가 실제 작업량을 정한다. 사용처가 한 곳이면 하루 작업이고, 열 곳이면 프로젝트가 된다.

## 우선 교체 대상

전부 한 번에 바꿀 수 없다. 보호 기간과 노출 정도로 순서를 정한다.

| 조건 | 우선순위 |
| --- | --- |
| 약한 알고리즘 + 외부 노출 | 1 |
| 약한 알고리즘 + 내부 | 2 |
| 장기 보호 데이터 | 3 |
| 짧은 수명 데이터 | 4 |

장기 보호 데이터가 세 번째인 이유는 지금 암호화한 것이 나중에 해독될 수 있기 때문이다. 보존 기간이 10년이면 지금 결정이 10년을 간다.`,

'password-storage-migration': R`## 어떤 방식으로 옮기나

현재 방식에 따라 전환 경로가 다르다.

| 현재 | 전환 |
| --- | --- |
| 소금 없는 단순 해시 | 즉시, 이중 해시 병행 검토 |
| 소금 있는 오래된 해시 | 로그인 시점 전환 |
| 최신 방식, 낮은 매개변수 | 로그인 시점 재해시 |
| 최신 방식, 적정 매개변수 | 유지 |

첫 줄이 가장 급하다. 유출 시 대량 복원이 가능하므로, 로그인을 기다리지 말고 기존 해시를 새 방식으로 한 번 더 감싸는 방법을 검토한다.

## 잔여 계정 처리

전환이 90%를 넘으면 남은 것은 휴면 계정이다. 무기한 두지 않고 처리 방침을 정한다.

| 기간 | 처리 |
| --- | --- |
| 6개월 | 로그인 시 자동 전환 |
| 12개월 | 다음 로그인에서 재설정 요구 |
| 24개월 | 휴면 전환 |
| 그 이상 | 파기 검토 |

휴면 계정의 옛 해시가 남아 있으면 유출 시 그것이 약한 고리가 된다. 보유 기간 정책과 함께 정리한다.`,

'searchable-data-protection': R`## 결정 순서

암호화를 먼저 정하지 않는다. 그 컬럼으로 무엇을 하는지 확인하는 것이 먼저다.

| 순서 | 확인 |
| --- | --- |
| 1 | 법령이 암호화를 요구하는 항목인가 |
| 2 | 실제로 어떤 검색에 쓰이나 |
| 3 | 다른 식별자로 대체 가능한가 |
| 4 | 대체 불가면 어떤 방식으로 |
| 5 | 소금 값을 어디에 두나 |

3번에서 해결되는 경우가 의외로 많다. 연락처로 회원을 찾는 기능이 정말 필요한지 확인하면 회원 번호로 대체 가능한 경우가 있다.

## 운영 시 주의

해시 컬럼 방식은 도입 후에도 관리가 필요하다.

| 항목 | 주의 |
| --- | --- |
| 정규화 규칙 | 바꾸면 기존 해시가 무효 |
| 소금 값 교체 | 전체 재계산 필요 |
| 새 데이터 | 같은 규칙 적용 |
| 색인 | 해시 컬럼에 필요 |

정규화 규칙을 바꾸면 기존 해시와 맞지 않는다. 규칙을 코드 한 곳에 두고 변경 시 전체 재계산을 계획한다.`,

'tls-configuration-audit': R`## 점검 대상 목록 만들기

외부 서비스만 보면 절반을 놓친다. 목록을 만들어 두면 자동 점검이 가능해진다.

| 구간 | 대상 |
| --- | --- |
| 인터넷 → 앞단 | 공개 도메인 전체 |
| 앞단 → 서버 | 내부 엔드포인트 |
| 서버 → 데이터베이스 | 연결 문자열 |
| 서비스 간 | 내부 API |
| 관리 접속 | 점프 서버·콘솔 |
| 외부 연동 | 협력사 엔드포인트 |

내부 구간이 오래된 설정으로 남아 있는 경우가 많다. 외부 점검 도구가 보지 않는 영역이기 때문이다.

## 갱신 실패에 대비

자동 갱신은 대부분 잘 동작하지만 실패할 때 조용하다. 두 겹으로 감시한다.

| 감시 | 확인 |
| --- | --- |
| 갱신 작업 결과 | 실패 시 즉시 알림 |
| 외부에서 실제 인증서 | 매일 만료일 확인 |
| 검증 레코드 존재 | 주기 확인 |
| 알림 수신자 | 분기 확인 |

마지막 항목이 자주 문제가 된다. 알림은 갔는데 아무도 보지 않는 주소였던 경우가 흔하다.`,

'random-number-misuse': R`## 길이를 정하는 계산

값의 길이는 추측 시도 횟수와 만료 시간으로 정한다. 감으로 정하지 않는다.

| 용도 | 길이 | 근거 |
| --- | --- | --- |
| 세션 식별자 | 16바이트 이상 | 동시 세션 수 |
| 재설정 토큰 | 32바이트 | 짧은 만료 + 시도 제한 |
| API 키 | 32바이트 | 장기 유효 |
| 초대 코드 | 사용성 고려 | 만료·시도 제한으로 보완 |

사람이 입력해야 하는 코드는 짧을 수밖에 없다. 그 경우 만료를 분 단위로 짧게 하고 시도 횟수를 제한해 보완한다.

## 값 재사용 방지

한 번 쓴 값이 다시 유효하면 길이와 무관하게 위험하다.

| 값 | 재사용 |
| --- | --- |
| 재설정 토큰 | 사용 즉시 폐기 |
| 초대 코드 | 사용 즉시 폐기 |
| 난스 | 절대 재사용 금지 |
| 세션 식별자 | 로그아웃 시 폐기 |

난스 재사용은 일부 암호 모드에서 평문 복원으로 이어진다. 생성 방식이 아니라 관리 방식의 문제이므로 따로 확인한다.`,

'data-retention-automation': R`## 기간을 정하는 근거

기간은 감으로 정하지 않는다. 법정 의무와 실제 필요를 함께 본다.

| 데이터 | 법정 의무 | 실제 필요 | 결정 |
| --- | --- | --- | --- |
| 거래 기록 | 5년 | 정산·분쟁 | 5년 |
| 접속 기록 | 1년 이상 | 조사 | 1년 |
| 마케팅 동의 | 철회 시까지 | 증빙 | 철회 후 일정 기간 |
| 애플리케이션 로그 | 없음 | 장애 분석 | 3개월 |
| 디버그 로그 | 없음 | 개발 | 2주 |

법정 의무가 있는 것과 없는 것을 먼저 나눈다. 의무가 없는 데이터는 짧게 잡을수록 좋다.

## 삭제와 보관 의무의 충돌

파기해야 하는데 보관 의무도 있는 경우가 있다. 이때는 분리 보관한다.

| 상황 | 처리 |
| --- | --- |
| 탈퇴 회원의 거래 기록 | 별도 보관, 접근 제한 |
| 분쟁 중 데이터 | 삭제 보류, 사유 기록 |
| 수사 협조 요청 | 보존 요청 기간 |

보류할 때는 사유와 기한을 기록한다. 기한이 지나면 자동으로 삭제 대상으로 돌아오게 한다.`,

'pseudonymization-practice': R`## 처리 강도 정하기

처리를 강하게 할수록 안전하지만 분석 가치가 준다. 목적에 필요한 만큼만 남긴다.

| 분석 목적 | 필요한 정밀도 | 처리 |
| --- | --- | --- |
| 연령대별 이용 패턴 | 10년 단위 | 출생년도 → 연령대 |
| 지역별 분포 | 시군구 | 주소 → 상위 행정구역 |
| 시간대 분석 | 시 단위 | 시각 → 시간대 |
| 개인 추적 | 불필요 | 식별자 제거 |

목적을 먼저 적고 필요한 정밀도를 정한 뒤 그에 맞춰 처리한다. 반대로 하면 필요 이상으로 남긴다.

## 결합 키 관리

가명처리 데이터를 원본과 다시 연결할 수 있는 키가 있으면 그 키가 통제 대상이다.

| 항목 | 관리 |
| --- | --- |
| 보관 위치 | 처리 데이터와 분리 |
| 접근 권한 | 최소 인원 |
| 결합 절차 | 승인 필요 |
| 보관 기간 | 목적 종료 시 파기 |
| 필요 없으면 | 키 자체를 만들지 않음 |

되돌릴 필요가 없다면 키를 폐기하는 것이 가장 안전하다. 그러면 익명처리에 가까워진다.`,

'secure-deletion': R`## 삭제 수준 정하기

모든 데이터에 같은 수준을 적용할 필요는 없다. 민감도와 매체에 따라 다르다.

| 데이터 | 수준 |
| --- | --- |
| 일반 업무 데이터 | 논리 삭제 |
| 개인정보 | 논리 삭제 + 사본 처리 |
| 고유식별정보 | 암호 삭제 또는 물리 |
| 인증 자격 증명 | 즉시 무효화 + 삭제 |
| 폐기 매체 | 물리적 파괴 |

암호 삭제가 대용량 데이터에 실용적이다. 처음부터 암호화해 두면 키 폐기로 즉시 접근을 차단할 수 있다.

## 삭제 완료의 정의

무엇을 했을 때 삭제로 볼지 미리 정해 둔다. 정의가 없으면 요청 처리 완료를 판단할 수 없다.

| 항목 | 완료 조건 |
| --- | --- |
| 운영 데이터 | 삭제 확인 |
| 복제본 | 전파 확인 |
| 색인 | 검색 불가 확인 |
| 캐시 | 무효화 확인 |
| 백업 | 세대 순환 기한 명시 |
| 위탁처 | 증빙 수령 |

백업은 즉시 지우기 어려우므로 순환 기한을 정책에 명시하고 요청자에게 안내한다.`,

'backup-encryption-keys': R`## 보관 방식 선택

재해 시 접근 가능성과 보안을 함께 만족해야 한다. 둘 중 하나만 보면 실패한다.

| 방식 | 재해 내성 | 보안 | 적합 |
| --- | --- | --- | --- |
| 별도 지역 키 서비스 | 높음 | 높음 | 클라우드 환경 |
| 키 분할 보관 | 높음 | 높음 | 규모가 큰 조직 |
| 하드웨어 토큰 다중 | 중간 | 높음 | 온프레미스 |
| 봉인 문서 금고 | 중간 | 중간 | 소규모 |
| 운영 환경 내 | 낮음 | 높음 | 부적합 |

마지막 줄이 흔한 실수다. 평시에는 편하지만 재해 시 접근할 수 없다.

## 복구 시간 목표와 맞추기

키 접근에 걸리는 시간이 복구 목표를 넘으면 그 방식은 쓸 수 없다.

| 방식 | 키 확보 소요 |
| --- | --- |
| 키 서비스 | 즉시 |
| 하드웨어 토큰 | 보관 장소 이동 시간 |
| 분할 보관 | 인원 소집 시간 |
| 문서 금고 | 개방 절차 시간 |

훈련에서 이 시간을 실측한다. 목표 복구 시간이 4시간인데 키 확보에 6시간이 걸리면 방식을 바꿔야 한다.`,
}
