const R = String.raw
export default {

'landing-zone-design': R`## 계정을 나누는 실제 기준

이론상 여러 기준이 있지만 실무에서는 두 축으로 정리된다. 환경과 서비스다.

| 조합 | 계정 수 | 적합 |
| --- | --- | --- |
| 환경만 | 3~4개 | 서비스가 적을 때 |
| 서비스만 | 서비스 수 | 환경 구분이 약함 |
| 환경 × 서비스 | 곱한 수 | 일반적 선택 |
| 팀 × 환경 | 팀 수 × 3 | 팀 자율성 중시 |

계정 수가 늘어나는 것을 걱정할 필요는 없다. 계정 자체에는 비용이 없고 정책은 상위에서 일괄 적용된다. 오히려 하나의 큰 계정은 권한을 나눌 방법이 없다.

## 이미 늦은 조직의 순서

계정이 이미 많다면 전체 재설계는 현실적이지 않다. 통제부터 적용한다.

| 순서 | 조치 | 난이도 |
| --- | --- | --- |
| 1 | 조직 단위 구성 | 낮음 |
| 2 | 감사 로그 집중 | 낮음 |
| 3 | 리전 제한 정책 | 낮음 |
| 4 | 관리 계정 정리 | 중간 |
| 5 | 신규 계정 표준화 | 중간 |
| 6 | 기존 계정 편입 | 높음 |

1~3번은 며칠이면 되고 효과가 크다. 6번은 몇 년에 걸쳐 진행해도 된다.`,

'cloud-network-segmentation': R`## 어디까지 나눌 것인가

분할이 세밀할수록 안전하지만 관리 부담이 는다. 자산 가치로 기준을 정한다.

| 대상 | 분할 수준 |
| --- | --- |
| 데이터 계층 | 별도 서브넷 + 그룹 참조만 |
| 인터넷 노출 계층 | 별도 서브넷 |
| 내부 애플리케이션 | 그룹 단위 |
| 관리 접속 | 별도 경로 |
| 개발 환경 | 계정 분리 |

데이터 계층 접근을 애플리케이션 그룹으로만 제한하는 것이 가장 효과가 크다. 이것 하나로 유출 경로가 크게 준다.

## 규칙 수를 관리한다

규칙이 수백 개가 되면 무엇이 무엇을 허용하는지 아무도 모른다.

| 방법 | 효과 |
| --- | --- |
| 그룹 참조 사용 | 주소 나열 제거 |
| 용도별 그룹 분리 | 이름으로 파악 |
| 임시 규칙 태그 | 자동 정리 |
| 미사용 규칙 제거 | 흐름 로그 대조 |
| 코드로 관리 | 이력·리뷰 |

주소를 직접 나열하는 방식이 규칙 수를 늘리는 주범이다. 그룹 참조로 바꾸면 인스턴스가 늘어도 규칙은 그대로다.`,

'iam-policy-review': R`## 조건으로 좁히는 방법

동작을 줄이는 것보다 조건을 붙이는 것이 효과가 클 때가 많다. 같은 동작이라도 범위가 크게 좁아진다.

| 조건 | 효과 | 적용 난이도 |
| --- | --- | --- |
| 자원 태그 일치 | 환경 격리 | 낮음 — 태그 필요 |
| 요청 출처 서비스 | 경로 한정 | 낮음 |
| 다중 인증 여부 | 강한 인증 강제 | 낮음 |
| 출발지 주소 | 접근 경로 제한 | 중간 |
| 시각 | 업무 시간 한정 | 중간 |

자원 태그 조건이 가장 실용적이다. 개발 환경 태그가 붙은 자원에만 접근하게 하면 운영을 실수로 건드리는 일이 사라진다.

## 축소 후 관찰

정책을 줄인 뒤 무엇이 막히는지 지켜본다. 조용히 실패하는 작업이 있다.

| 관찰 대상 | 기간 |
| --- | --- |
| 접근 거부 로그 | 2주 |
| 배치 작업 성공률 | 1개월 |
| 분기 작업 | 다음 분기까지 |
| 담당 팀 문의 | 상시 |

분기 작업은 다음 분기가 와야 확인된다. 그때까지 되돌릴 수 있도록 이전 정책을 보관한다.`,

'cloud-storage-public-check': R`## 공개가 필요한 경우의 설계

진짜 공개 파일이 있다면 버킷을 여는 대신 전송 서비스를 앞에 둔다.

| 방식 | 접근 통제 | 기록 | 차단 |
| --- | --- | --- | --- |
| 버킷 직접 공개 | 없음 | 제한적 | 어려움 |
| 전송 서비스 경유 | 가능 | 상세 | 즉시 |
| 서명 주소 | 시간 제한 | 발급 기록 | 만료 |
| 애플리케이션 중계 | 요청마다 | 완전 | 즉시 |

전송 서비스를 쓰면 공개 파일이라도 누가 얼마나 받았는지 알 수 있고, 문제가 생기면 즉시 막을 수 있다.

## 사고 시 확인 순서

공개 노출이 발견되면 순서대로 확인한다.

| 순서 | 확인 |
| --- | --- |
| 1 | 즉시 비공개 전환 |
| 2 | 언제부터 공개였나 |
| 3 | 접근 로그 조회 |
| 4 | 무엇이 들어 있었나 |
| 5 | 검색엔진 색인 여부 |
| 6 | 개인정보 포함 시 신고 판단 |

5번을 빠뜨리면 비공개로 바꿔도 검색 결과에 남는다. 색인 제거 요청을 별도로 해야 한다.`,

'serverless-permission-model': R`## 권한 도출 자동화

함수가 많아지면 손으로 관리할 수 없다. 코드에서 권한을 도출하는 구조로 만든다.

| 방식 | 유지 부담 |
| --- | --- |
| 배포 도구가 자원 참조에서 도출 | 낮음 |
| 함수별 정책 파일 | 중간 |
| 공용 역할 | 낮지만 위험 |
| 콘솔에서 수동 | 높음 |

첫 번째 방식이면 코드에 선언되지 않은 자원에는 권한이 생기지 않는다. 쓰지 않게 된 권한도 코드에서 참조가 사라지면 함께 없어진다.

## 실행 환경의 특성

함수는 실행 환경이 재사용된다. 이 특성이 보안에 영향을 준다.

| 특성 | 영향 |
| --- | --- |
| 환경 재사용 | 이전 호출의 상태가 남는다 |
| 임시 디렉터리 공유 | 파일이 남을 수 있다 |
| 전역 변수 유지 | 캐시에 유용, 오염에 취약 |
| 동시 실행 | 자원 경합 |

임시 디렉터리에 처리 중인 데이터를 남기면 다음 호출에서 다른 사용자의 요청이 그것을 볼 수 있다. 처리 후 정리한다.`,

'multi-account-guardrails': R`## 예방과 탐지를 나누는 기준

전부 예방으로 막으려 하면 정당한 작업까지 막혀 예외 요청이 쏟아진다.

| 항목 | 판단 |
| --- | --- |
| 정당한 사유가 없는 것 | 예방 |
| 대부분 문제지만 예외가 있는 것 | 탐지 |
| 상황에 따라 다른 것 | 탐지 |
| 되돌리기 쉬운 것 | 탐지 |
| 되돌릴 수 없는 것 | 예방 |

리전 제한, 감사 로그 중지 금지, 루트 사용 금지는 정당한 사유가 없으므로 예방으로 건다. 인스턴스 유형이나 태그 규칙은 탐지로 두고 사람이 본다.

## 예외 관리

예외가 쌓이면 가드레일이 무의미해진다. 처음부터 만료와 재검토를 넣는다.

| 항목 | 기본값 |
| --- | --- |
| 만료 | 90일 |
| 승인자 | 위험도별 |
| 사유 기록 | 필수 |
| 적용 범위 | 해당 계정만 |
| 재검토 | 만료 시 |

만료 없는 예외를 허용하지 않는 것만으로 목록이 관리 가능한 크기로 유지된다.`,

'infrastructure-drift': R`## 어긋남의 유형별 처리

모든 차이를 같게 다루면 안 된다. 원인에 따라 처리가 다르다.

| 유형 | 처리 |
| --- | --- |
| 긴급 대응 변경 | 코드에 반영 |
| 실수 | 코드대로 되돌림 |
| 외부 도구가 관리 | 관리 범위에서 제외 |
| 사업자 기본값 변경 | 코드에 명시 |
| 코드 오류 | 코드 수정 |

세 번째가 자주 문제가 된다. 자동 확장이나 관리형 서비스가 자원을 수정하는데 그것을 코드로 관리하려 하면 매번 차이가 난다. 해당 속성을 관리 대상에서 제외한다.

## 상태 파일 보호

인프라 코드의 상태 파일 자체가 민감하다.

| 항목 | 설정 |
| --- | --- |
| 저장 위치 | 원격, 버전 관리 |
| 암호화 | 저장·전송 |
| 접근 권한 | 배포 파이프라인만 |
| 잠금 | 동시 적용 방지 |
| 백업 | 손상 대비 |

상태 파일이 손상되면 기존 자원을 관리할 수 없게 된다. 버전 관리가 되는 저장소를 쓰고 되돌릴 수 있게 한다.`,

'cloud-incident-isolation': R`## 자원 유형별 격리 방법

인스턴스와 컨테이너, 서버리스는 격리 방법이 다르다.

| 유형 | 격리 |
| --- | --- |
| 인스턴스 | 보안 그룹 교체, 확장 그룹 제외 |
| 컨테이너 | 네트워크 정책, 재시작 차단 |
| 서버리스 | 트리거 비활성화, 동시성 0 |
| 관리형 서비스 | 접근 정책 변경 |
| 계정 전체 | 서비스 제어 정책 |

서버리스는 동시 실행 수를 0으로 설정하면 새 호출이 처리되지 않는다. 함수를 지우지 않아 코드와 설정이 증거로 남는다.

## 격리 후 조사 준비

격리했다고 조사가 되는 것은 아니다. 조사자가 접근할 수 있어야 한다.

| 항목 | 준비 |
| --- | --- |
| 조사자 접근 경로 | 격리 그룹에 허용 |
| 필요 권한 | 사전 부여 |
| 도구 | 사전 배포 |
| 저장 공간 | 증거 보관 위치 |
| 작업 기록 | 시각·담당 |

격리 그룹의 인바운드를 완전히 막으면 조사자도 들어갈 수 없다. 조사자 주소는 열어 둔다.`,

'cloud-log-cost-control': R`## 무엇을 줄일 수 있나

로그 유형별로 줄일 수 있는 여지가 다르다. 감사 관련은 손대지 않는다.

| 유형 | 조정 |
| --- | --- |
| 감사 로그 | 줄이지 않음 |
| 인증 기록 | 줄이지 않음 |
| 오류 로그 | 유지 |
| 정상 요청 로그 | 표본화 |
| 상태 확인 | 제외 |
| 디버그 | 운영에서 끔 |
| 흐름 로그 | 필드 선별 |

상태 확인 요청이 전체의 상당 비중을 차지하는 경우가 많다. 제외하는 것만으로 눈에 띄게 준다.

## 질의 비용 관리

저장보다 질의가 비쌀 수 있다. 조사 절차서에 파티션 조건을 포함한 예시를 넣는다.

| 방법 | 효과 |
| --- | --- |
| 파티션 조건 필수 | 스캔량 감소 |
| 기간 먼저 좁히기 | 습관화 |
| 열 지정 | 전체 선택 지양 |
| 결과 캐시 | 반복 질의 |
| 비용 상한 | 사고 방지 |

급할 때 조건 없이 전체를 훑는 질의를 돌리기 쉽다. 예시 질의를 미리 만들어 두면 그런 실수가 준다.`,

'shared-responsibility-gaps': R`## 서비스 도입 시 확인 절차

문서를 읽는 것과 확인하는 것은 다르다. 실제로 해 봐야 아는 항목이 있다.

| 항목 | 확인 방법 |
| --- | --- |
| 백업 | 실제로 복원 |
| 로그 | 실제로 조회 |
| 삭제 | 요청 후 확인 |
| 접근 통제 | 권한 없는 계정으로 시도 |
| 암호화 | 설정 조회 |
| 사고 통지 | 계약 조항 |

첫 두 줄은 반드시 실제로 해 본다. 백업이 있다고 쓰여 있어도 복원해 보기 전에는 모른다.

## 책임 경계 기록

확인한 결과를 문서로 남긴다. 사고 시 판단 근거가 되고, 담당자가 바뀌어도 유지된다.

| 항목 | 기록 |
| --- | --- |
| 서비스명 | |
| 우리 책임 범위 | |
| 사업자 책임 범위 | |
| 확인 방법·일자 | |
| 재확인 주기 | |

재확인 주기가 중요하다. 서비스가 바뀌면 경계도 바뀐다. 연 1회 확인하는 것을 절차에 넣는다.`,
}
