const R = String.raw
export default {

'landing-zone-design': R`## 실제로 이렇게 터진다

계정 하나로 시작해 3년 만에 40개가 된 조직이 있다. 주소 대역이 겹쳐 계정 간 연결이 불가능했고, 재설계에 반년이 걸렸다.

또 하나는 관리 계정에 워크로드를 올린 경우다. 그 워크로드의 취약점 하나가 조직 전체 통제권으로 이어질 수 있는 구조였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 나중에 나누면 된다 | 자원 이동이 어렵다 |
| 계정이 많으면 관리가 어렵다 | 정책은 상위에서 한다 |
| 관리 계정도 그냥 계정이다 | 전체 통제권을 갖는다 |
| 주소 대역은 넉넉하다 | 반드시 부족해진다 |

## 지금이라도 할 것

| 순서 | 조치 |
| --- | --- |
| 1 | 관리 계정에서 워크로드 이전 |
| 2 | 로그를 별도 계정으로 |
| 3 | 리전 제한 정책 |
| 4 | 새 계정에 표준 적용 |
| 5 | 기존 계정 순차 편입 |`,

'cloud-network-segmentation': R`## 실제로 이렇게 터진다

서브넷을 계층별로 나눴지만 보안 그룹은 전부 열어 둔 사례가 있다. 도면상으로는 분리돼 있었고 실제로는 어디서든 데이터베이스에 접근할 수 있었다.

또 하나는 임시로 연 규칙을 닫지 않은 경우다. 장애 대응 중 열었고 6개월 뒤 점검에서 발견됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 서브넷을 나누면 분리된다 | 라우팅은 기본 허용이다 |
| 인바운드만 보면 된다 | 유출은 아웃바운드다 |
| 임시 규칙은 기억한다 | 남는다 |
| 주소로 지정하면 명확하다 | 관리가 무너진다 |

## 정리 절차

| 단계 | 방법 |
| --- | --- |
| 1 | 흐름 로그로 실제 통신 수집 |
| 2 | 쓰이지 않는 규칙 목록화 |
| 3 | 임시 규칙에 만료 태그 |
| 4 | 그룹 참조로 전환 |
| 5 | 아웃바운드 제한 적용 |`,

'iam-policy-review': R`## 실제로 이렇게 터진다

권한을 사용 기록만 보고 줄였다가 분기 배치가 실패한 사례가 있다. 90일 기록에 한 번밖에 없던 동작이 통계에서 묻혔다.

또 하나는 관리자 권한 계정을 정리하지 않아, 그중 하나가 피싱으로 털리며 전 자원이 노출된 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 90일이면 충분하다 | 분기·연간 작업이 빠진다 |
| 안 쓰면 필요 없다 | 드물게 필요할 수 있다 |
| 한 번에 줄이면 된다 | 감사 모드가 먼저다 |
| 동작만 줄이면 된다 | 조건이 더 효과적이다 |

## 조건으로 좁히기

| 조건 | 효과 |
| --- | --- |
| 자원 태그 | 환경 구분 |
| 출발지 주소 | 접근 경로 제한 |
| 다중 인증 여부 | 강한 인증 요구 |
| 시각 | 업무 시간 |
| 요청 출처 서비스 | 경로 한정 |`,

'cloud-storage-public-check': R`## 실제로 이렇게 터진다

임시 공유를 위해 버킷을 공개로 바꾼 뒤 되돌리지 않은 사례가 있다. 며칠 뒤 검색엔진에 색인됐고, 그 안에 고객 명단이 있었다.

또 하나는 버킷은 비공개인데 객체 몇 개만 개별 공개된 경우다. 버킷 단위 점검에서 발견되지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 주소를 모르면 안 열린다 | 스캔 도구가 찾아낸다 |
| 버킷만 보면 된다 | 객체 단위 설정이 있다 |
| 임시로 열고 닫는다 | 닫지 않는다 |
| 공개가 필요하면 열어야 한다 | 전송 서비스를 앞에 둔다 |

## 상시 통제

| 계층 | 수단 |
| --- | --- |
| 조직 | 공개 금지 정책 |
| 계정 | 공개 차단 설정 |
| 배포 | 인프라 코드 검사 |
| 감시 | 설정 변경 경보 |
| 점검 | 주기 스캔 |`,

'serverless-permission-model': R`## 실제로 이렇게 터진다

함수 50개가 공용 역할 하나를 쓰던 사례가 있다. 그중 한 함수의 취약점으로 그 역할의 모든 권한이 노출됐고, 여기에는 운영 데이터베이스 쓰기가 포함돼 있었다.

또 하나는 비밀을 환경 변수에 넣은 경우다. 콘솔 조회 권한만으로 값이 보였다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 함수는 작아서 위험이 적다 | 권한이 곧 위험이다 |
| 공용 역할이 관리가 쉽다 | 권한이 누적된다 |
| 환경 변수는 안전하다 | 설정 조회로 보인다 |
| 트리거는 통제와 무관하다 | 호출 권한도 통제다 |

## 함수마다 확인

| 항목 | 확인 |
| --- | --- |
| 전용 역할인가 | 공용 금지 |
| 접근 자원 | 지정 자원만 |
| 로그 권한 | 자기 그룹만 |
| 비밀 | 실행 시 조회 |
| 호출 주체 | 제한 |`,

'multi-account-guardrails': R`## 실제로 이렇게 터진다

가드레일을 예고 없이 강제로 적용한 사례가 있다. 배포가 막히고 장애가 났으며, 그 뒤로 팀들이 가드레일 자체를 불신하게 됐다.

또 하나는 예외에 만료를 두지 않은 경우다. 2년 전 임시 예외가 그대로 살아 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 강하게 걸수록 안전하다 | 우회가 생긴다 |
| 예외는 최소로 두면 된다 | 만료가 없으면 영구다 |
| 감사 모드는 시간 낭비다 | 장애를 막는다 |
| 개발 환경도 같아야 한다 | 자유도가 필요하다 |

## 적용 순서

| 단계 | 기간 |
| --- | --- |
| 감사 모드 | 2주 |
| 위반 목록 공유 | 1주 |
| 팀 협의 | 2주 |
| 신규 강제 | 1개월 |
| 전체 강제 | 이후 |`,

'infrastructure-drift': R`## 실제로 이렇게 터진다

장애 대응 중 콘솔에서 보안 그룹을 열었고 코드에 반영하지 않은 사례가 있다. 다음 배포에서 그 규칙이 사라져 같은 장애가 재발했다.

또 하나는 반대로 코드가 실제와 달라 배포할 때마다 예상 밖 변경이 일어난 경우다. 아무도 배포를 하지 않게 됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 코드가 곧 현실이다 | 손으로 바꾼 것이 있다 |
| 어긋나도 큰 문제 없다 | 다음 배포에서 되돌아간다 |
| 긴급 변경은 예외다 | 반영 절차가 필요하다 |
| 검사는 배포 때 하면 된다 | 매일 해야 원인을 찾는다 |

## 유지 방법

| 항목 | 조치 |
| --- | --- |
| 콘솔 쓰기 권한 | 평소 회수 |
| 긴급 접근 | 승인 후 임시 부여 |
| 변경 후 | 24시간 내 코드 반영 |
| 정기 검사 | 매일 계획 실행 |
| 상태 파일 | 원격·잠금·암호화 |`,

'cloud-incident-isolation': R`## 실제로 이렇게 터진다

침해 의심 인스턴스를 바로 종료한 사례가 있다. 메모리 증거가 사라졌고, 자동 확장이 새 인스턴스를 띄워 같은 취약점으로 다시 침해됐다.

또 하나는 역할만 떼고 기존 세션을 무효화하지 않은 경우다. 이미 발급된 임시 자격 증명으로 몇 시간 더 활동했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 종료가 가장 빠른 차단이다 | 증거가 사라진다 |
| 역할을 떼면 끝이다 | 발급된 자격 증명이 남는다 |
| 격리 그룹은 그때 만들면 된다 | 시간이 걸린다 |
| 인스턴스만 보면 된다 | 확장 그룹이 새로 띄운다 |

## 미리 준비할 것

| 항목 | 준비 |
| --- | --- |
| 격리용 보안 그룹 | 사전 생성 |
| 세션 무효화 정책 | 문서화 |
| 스냅샷 절차 | 명령 그대로 |
| 확장 그룹 제외 명령 | 절차서에 |
| 조사자 권한 | 사전 부여 |`,

'cloud-log-cost-control': R`## 실제로 이렇게 터진다

로그 비용을 줄이려고 보존 기간을 30일로 낮춘 사례가 있다. 몇 달 뒤 침해가 발견됐을 때 초기 침투 시점의 기록이 없어 경로를 밝히지 못했다.

또 하나는 파티션 조건 없이 질의를 돌려 한 번에 큰 비용이 나온 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 저장이 비싸다 | 수집과 색인이 대부분이다 |
| 짧게 두면 절약된다 | 조사 능력을 잃는다 |
| 질의는 무료다 | 스캔량으로 과금된다 |
| 전부 모아야 한다 | 유형별로 다르다 |

## 계층 배분

| 계층 | 기간 | 대상 |
| --- | --- | --- |
| 색인 | 7~30일 | 최근 조회 |
| 객체 스토리지 | 1년 | 조사용 |
| 저비용 | 그 이상 | 보존 의무 |

감사·인증 로그는 줄이지 않는다. 정상 요청 로그부터 표본화한다.`,

'shared-responsibility-gaps': R`## 실제로 이렇게 터진다

관리형 데이터베이스의 자동 백업을 믿고 별도 백업을 두지 않은 사례가 있다. 실수로 지운 데이터를 복구하려 했을 때 보존 기간이 지나 있었다.

또 하나는 SaaS 도입 후 계정 관리를 사업자 몫으로 생각한 경우다. 퇴사자 계정이 그대로 살아 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 관리형이면 알아서 한다 | 데이터와 접근은 우리 몫 |
| 백업은 포함이다 | 기간과 범위를 확인해야 한다 |
| 인증서가 있으면 안전하다 | 범위를 봐야 한다 |
| 문서에 적혀 있으면 된다 | 시험해 봐야 안다 |

## 도입 시 확인표

| 항목 | 누가 | 확인 방법 |
| --- | --- | --- |
| 백업 | ? | 복원 시험 |
| 패치 | ? | 일정 확인 |
| 로그 | ? | 보존·접근 |
| 암호화 | ? | 설정 확인 |
| 계정 관리 | ? | 회수 절차 |`,
}
