const R = String.raw
export default {

'envelope-encryption-design': R`## 실제로 이렇게 터진다

암호문 키를 데이터와 따로 보관하다 백업 범위에서 빠진 사례가 있다. 복구할 때 데이터는 있는데 키가 없어 아무것도 열지 못했다.

또 하나는 평문 데이터 키를 애플리케이션이 계속 들고 있던 경우다. 메모리 덤프에서 그대로 나왔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 키 서비스만 쓰면 안전하다 | 평문 키 처리 방식이 중요하다 |
| 암호문 키는 안 지켜도 된다 | 잃으면 복구 불가 |
| 데이터 키는 오래 써도 된다 | 범위를 나눌수록 좋다 |
| 백업은 데이터만 | 키도 함께 |

## 설계 시 확인

| 질문 | 답이 있어야 한다 |
| --- | --- |
| 평문 키를 언제 지우나 | 사용 직후 |
| 암호문 키는 어디에 | 데이터와 함께 |
| 키 서비스 장애 시 | 캐시 전략 |
| 데이터 키 범위는 | 테넌트·레코드 |
| 마스터 키 교체는 | 재암호화 없이 |`,

'crypto-inventory': R`## 실제로 이렇게 터진다

알고리즘 취약점이 공개됐을 때 우리가 어디서 그것을 쓰는지 알아내는 데 사흘이 걸린 사례가 있다. 그 사이 대응 여부를 판단하지 못했다.

또 하나는 목록을 만들었지만 갱신하지 않은 경우다. 2년 전 상태였고 새 시스템이 빠져 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 표준 알고리즘만 쓴다 | 확인해 보면 아니다 |
| 라이브러리가 알아서 한다 | 설정으로 정해진다 |
| 한 번 만들면 된다 | 계속 바뀐다 |
| 목록은 감사용이다 | 대응 속도를 정한다 |

## 목록이 답해야 할 질문

| 질문 | 필요한 항목 |
| --- | --- |
| 이 알고리즘을 쓰는가 | 알고리즘·사용처 |
| 이 키가 유출되면 | 키·영향 범위 |
| 언제 교체했나 | 교체 이력 |
| 누구에게 물어보나 | 담당 |`,

'password-storage-migration': R`## 실제로 이렇게 터진다

전 사용자에게 비밀번호 재설정을 요구한 사례가 있다. 문의가 몰렸고 상당수가 이탈했다. 로그인 시점 전환을 썼다면 대부분 조용히 옮길 수 있었다.

또 하나는 반복 횟수를 너무 높게 잡은 경우다. 로그인이 몰리는 시간에 서버가 버티지 못했다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 한 번에 옮겨야 한다 | 점진 전환이 가능하다 |
| 매개변수는 높을수록 좋다 | 서버가 버텨야 한다 |
| 옛 해시는 지우면 된다 | 검증에 필요하다 |
| 전환은 금방 끝난다 | 휴면 계정이 남는다 |

## 매개변수 정하기

| 확인 | 기준 |
| --- | --- |
| 검증 시간 | 0.2~0.5초 |
| 최대 동시 로그인 | 실측 |
| 메모리 사용 | 서버 여유 |
| 부하 시험 | 피크의 2배 |`,

'searchable-data-protection': R`## 실제로 이렇게 터진다

연락처를 암호화한 뒤 관리자 검색 기능이 동작하지 않게 된 사례가 있다. 급하게 복호화 후 전체 조회로 검색하도록 바꿨고, 결과적으로 모든 검색이 전 건을 복호화했다.

또 하나는 해시 컬럼을 만들면서 소금을 쓰지 않은 경우다. 유출 시 사전 공격으로 원본이 복원됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호화하면 검색만 안 된다 | 정렬·범위도 안 된다 |
| 해시면 안전하다 | 소금이 없으면 복원된다 |
| 전체 복호화로 해결한다 | 성능과 노출이 함께 나빠진다 |
| 나중에 바꿀 수 있다 | 이미 암호화된 데이터가 걸림돌 |

## 설계 전 확인

| 질문 | 확인 방법 |
| --- | --- |
| 이 컬럼으로 검색하나 | 질의 로그 |
| 정렬에 쓰나 | 화면 확인 |
| 범위 조건이 있나 | 질의 로그 |
| 다른 식별자로 대체되나 | 기획 협의 |`,

'tls-configuration-audit': R`## 실제로 이렇게 터진다

인증서 자동 갱신을 걸어 두고 안심하다 만료된 사례가 있다. 검증 레코드가 정리 과정에서 지워져 갱신이 실패했고, 실패 알림은 아무도 보지 않는 주소로 갔다.

또 하나는 내부 구간만 옛 설정으로 남은 경우다. 외부 점검은 통과했고 내부는 아무도 보지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 자동 갱신이면 안전하다 | 실패를 감지해야 한다 |
| 외부만 점검하면 된다 | 내부 구간이 더 오래됐다 |
| 한 번 설정하면 유지된다 | 권장 설정이 바뀐다 |
| 로드밸런서 뒤는 상관없다 | 도청 경로다 |

## 감시 항목

| 항목 | 주기 |
| --- | --- |
| 만료 30일 전 | 경고 |
| 갱신 실패 | 즉시 |
| 외부 실제 확인 | 매일 |
| 프로토콜·암호 모음 | 월 |
| 내부 구간 | 분기 |`,

'random-number-misuse': R`## 실제로 이렇게 터진다

비밀번호 재설정 토큰을 시각 기반으로 만든 사례가 있다. 자기 토큰을 하나 받아 값을 관찰하자 다른 사람의 토큰을 좁은 범위에서 추측할 수 있었다.

또 하나는 초대 코드를 짧게 만들고 만료도 두지 않은 경우다. 무차별 시도로 유효한 코드가 발견됐다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 무작위처럼 보이면 된다 | 예측 가능성이 기준이다 |
| 짧아도 충분하다 | 시도 횟수를 계산해야 한다 |
| 한 번 쓰고 버린다 | 만료가 없으면 남는다 |
| 라이브러리가 알아서 한다 | 어떤 함수를 쓰는지에 달렸다 |

## 값 설계

| 항목 | 기준 |
| --- | --- |
| 생성 | 암호학적 난수 |
| 길이 | 32바이트 권장 |
| 만료 | 짧게 |
| 재사용 | 금지 |
| 시도 제한 | 필수 |`,

'data-retention-automation': R`## 실제로 이렇게 터진다

삭제 배치 조건을 잘못 써서 보관해야 할 거래 기록이 지워진 사례가 있다. 되돌릴 수 없었고 법정 보관 의무를 위반한 상태가 됐다.

또 하나는 반대로 정책만 있고 삭제가 한 번도 실행되지 않은 경우다. 감사에서 5년 치 개인정보가 그대로 있는 것이 드러났다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 정책이 있으면 지워진다 | 실행돼야 지워진다 |
| 삭제는 안전한 작업이다 | 되돌릴 수 없다 |
| 운영만 지우면 된다 | 백업·사본이 남는다 |
| 보관은 문제가 없다 | 기간 초과 자체가 위반 |

## 안전장치

| 장치 | 목적 |
| --- | --- |
| 시험 실행 모드 | 대상 확인 |
| 일별 삭제 상한 | 오작동 차단 |
| 실행 기록 | 감사 근거 |
| 법정 보관 예외 | 조건 명시 |
| 알림 | 결과 통지 |`,

'pseudonymization-practice': R`## 실제로 이렇게 터진다

이름과 연락처만 지우고 분석팀에 넘긴 사례가 있다. 생년월일·성별·우편번호 조합으로 상당수가 재식별됐고, 그 사실을 외부 지적으로 알게 됐다.

또 하나는 결합 키를 같은 저장소에 둔 경우다. 접근 권한이 있으면 언제든 원본으로 되돌릴 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 이름을 지우면 익명이다 | 조합으로 특정된다 |
| 가명정보는 개인정보가 아니다 | 여전히 개인정보다 |
| 분석팀은 신뢰할 수 있다 | 통제와 별개다 |
| 한 번 처리하면 끝이다 | 새 데이터에 반복된다 |

## 처리 후 확인

| 확인 | 방법 |
| --- | --- |
| 고유 조합 | 조합별 인원 수 |
| 특이값 | 극단값 존재 |
| 외부 결합 | 공개 데이터 대조 |
| 결합 키 위치 | 분리 여부 |`,

'secure-deletion': R`## 실제로 이렇게 터진다

삭제 요청을 처리했다고 회신한 뒤, 검색 색인에 그대로 남아 있는 것이 발견된 사례가 있다. 운영 데이터베이스만 지웠고 색인은 별도 시스템이었다.

또 하나는 위탁 업체에 삭제 요청을 전달하지 않은 경우다. 계약에 조항이 없어 강제할 수도 없었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 삭제 명령이면 끝이다 | 사본이 남는다 |
| 백업은 예외다 | 정책에 명시해야 한다 |
| 색인은 자동 동기화된다 | 별도 처리가 필요하다 |
| 위탁처는 알아서 지운다 | 요청을 전달해야 한다 |

## 사본 목록

| 위치 | 처리 |
| --- | --- |
| 복제본 | 전파 확인 |
| 검색 색인 | 별도 삭제 |
| 캐시 | 무효화 |
| 로그 | 보존 기간 |
| 분석 시스템 | 요청 전달 |
| 위탁처 | 계약 근거로 요청 |`,

'backup-encryption-keys': R`## 실제로 이렇게 터진다

재해 훈련에서 백업 복호화에 실패한 사례가 있다. 키를 보관하던 담당자가 퇴사했고, 인수인계 목록에 그 항목이 없었다.

또 하나는 키를 백업 서버 자체에 둔 경우다. 그 서버가 함께 침해되자 암호화가 의미를 잃었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 키는 담당자가 안다 | 부재 시 복구 불가 |
| 같은 곳에 두면 편하다 | 함께 유실·침해된다 |
| 문서에 적어 두면 된다 | 그 문서 접근이 문제다 |
| 훈련은 형식이다 | 여기서 문제가 드러난다 |

## 훈련 항목

| 확인 | 통과 조건 |
| --- | --- |
| 키 접근 | 담당자가 실제로 얻는다 |
| 복호화 | 백업이 열린다 |
| 소요 시간 | 목표 이내 |
| 대체 담당 | 주 담당 없이 가능 |
| 절차 문서 | 실제와 일치 |`,
}
