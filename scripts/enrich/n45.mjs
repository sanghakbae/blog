const R = String.raw
export default {

'dns-security-operations': R`## 실제로 이렇게 터진다

서비스를 내리면서 레코드를 지우지 않은 사례가 있다. 가리키던 클라우드 자원이 해제됐고, 다른 사람이 같은 주소를 선점해 우리 하위 도메인에서 피싱 페이지를 띄웠다. 쿠키 범위가 상위 도메인이라 세션까지 위험했다.

또 하나는 등록 기관 계정에 다중 인증이 없던 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 레코드는 남아도 무해하다 | 선점당한다 |
| 도메인은 관리 대상이 아니다 | 최상위 통제점이다 |
| 등록 정보는 공개해도 된다 | 사회공학 재료다 |
| 만료는 알림이 온다 | 그 주소가 유효해야 한다 |

## 상시 점검

| 항목 | 주기 |
| --- | --- |
| 대상 없는 레코드 | 월 |
| 만료 임박 도메인 | 월 |
| 등록 기관 계정 | 분기 |
| 전송 잠금 | 분기 |
| 변경 이력 | 상시 알림 |`,

'bastion-host-operations': R`## 실제로 이렇게 터진다

점프 서버 접속이 느리고 단계가 많아 개발자들이 별도 경로를 만든 사례가 있다. 통제 지점을 하나로 모은다는 목적이 무너졌고, 그 경로는 기록도 남지 않았다.

또 하나는 세션 기록을 점프 서버 자체에 저장한 경우다. 침해 시 함께 지워졌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 만들면 다 그 경로로 온다 | 불편하면 우회한다 |
| 인바운드만 막으면 된다 | 아웃바운드가 침해 후 통로 |
| 기록은 서버에 두면 된다 | 함께 지워진다 |
| 공용 계정이 편하다 | 추적이 안 된다 |

## 우회 점검

| 확인 | 방법 |
| --- | --- |
| 직접 접근 가능한 경로 | 보안 그룹 조사 |
| 다른 접속 도구 | 프로세스·포트 |
| 개인 키 배포 | 서버별 확인 |
| VPN 경유 직접 접속 | 라우팅 확인 |`,

'zero-trust-migration': R`## 실제로 이렇게 터진다

제품 하나를 도입하고 전환이 끝났다고 본 사례가 있다. 신원 체계가 정리되지 않아 여전히 계정이 흩어져 있었고, 단말 상태는 확인하지 않았다.

또 하나는 레거시부터 손댄 경우다. 가장 어려운 것에서 막혀 전체가 멈췄다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 제품을 사면 된다 | 신원·단말·네트워크가 함께 움직인다 |
| VPN 을 없애는 것이 목표다 | 접근 단위를 바꾸는 것이 목표다 |
| 한 번에 전환한다 | 단계가 필요하다 |
| 어려운 것부터 한다 | 새 것부터 한다 |

## 전제 조건

| 항목 | 없으면 |
| --- | --- |
| 통합 인증 | 신원 판단 불가 |
| 단말 관리 | 상태 확인 불가 |
| 애플리케이션 목록 | 범위 불명 |
| 접근 주체 정리 | 정책 작성 불가 |`,

'ddos-response-plan': R`## 실제로 이렇게 터진다

공격이 시작된 뒤 방어 서비스 전환 절차를 찾은 사례가 있다. 담당자가 휴가 중이었고 권한이 없어 두 시간을 잃었다.

또 하나는 무엇을 포기할지 정하지 않은 경우다. 전부 지키려다 전부 느려졌다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 방어 서비스가 알아서 막는다 | 전환과 조정이 필요하다 |
| 대역폭만 문제다 | 애플리케이션 계층이 어렵다 |
| 그때 판단하면 된다 | 판단할 시간이 없다 |
| 전부 지켜야 한다 | 우선순위가 필요하다 |

## 사전 결정 사항

| 항목 | 정할 것 |
| --- | --- |
| 방어 전환 권한 | 누가 |
| 축소 대상 | 무엇을 끄나 |
| 지킬 기능 | 핵심 |
| 지역 차단 | 기준 |
| 공지 문안 | 초안 |`,

'asset-inventory-automation': R`## 실제로 이렇게 터진다

취약점이 공개됐을 때 영향받는 서버를 찾는 데 이틀이 걸린 사례가 있다. 목록이 반년 전 것이었고 신규 서버가 빠져 있었다.

또 하나는 목록에는 있지만 관리 에이전트가 없는 서버가 30%였던 경우다. 패치 준수율은 높게 나왔다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 목록은 있다 | 최신인지가 문제다 |
| 준수율이 높으면 안전하다 | 대상에서 빠진 것을 봐야 한다 |
| 클라우드 API 면 충분하다 | 미관리 장비가 안 보인다 |
| 손으로 관리할 수 있다 | 만든 날부터 낡는다 |

## 사각지대 찾기

| 대조 | 발견 |
| --- | --- |
| 실행 자원 vs 에이전트 | 미관리 서버 |
| 네트워크 스캔 vs 목록 | 미등록 장비 |
| 인증서 기록 vs 목록 | 미등록 도메인 |
| 결제 기록 vs 목록 | 미등록 서비스 |`,

'vpn-alternatives': R`## 실제로 이렇게 터진다

VPN 계정 하나가 피싱으로 털린 사례가 있다. 연결되자 내부망 전체가 열렸고, 공격자는 며칠간 자유롭게 이동했다. 그 계정에 필요했던 것은 사내 위키 접근뿐이었다.

또 하나는 대체 수단을 도입했지만 VPN 을 그대로 둔 경우다. 접근 범위가 줄지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| VPN 은 안전한 통로다 | 연결되면 범위가 넓다 |
| 다중 인증이면 충분하다 | 범위가 그대로다 |
| 새 수단을 더하면 개선된다 | 옛 경로를 줄여야 한다 |
| 전부 대체해야 의미 있다 | 범위 축소만으로도 효과가 크다 |

## 단계별 축소

| 단계 | 조치 |
| --- | --- |
| 1 | 실제 접근 대상 조사 |
| 2 | 웹 대상 인증 프록시로 |
| 3 | 관리 접속 세션 서비스로 |
| 4 | VPN 범위를 잔여 대상으로 |
| 5 | 대상별 네트워크 제한 |`,

'network-monitoring-blindspots': R`## 실제로 이렇게 터진다

같은 서브넷 안에서 일어난 횡적 이동을 전혀 보지 못한 사례가 있다. 게이트웨이를 거치지 않아 흐름 로그에 남지 않았다.

또 하나는 SaaS 접근이 감시 밖이었던 경우다. 대량 다운로드가 우리 네트워크를 지나지 않았다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 흐름 로그면 다 본다 | 서브넷 내부가 안 보인다 |
| 암호화되면 못 본다 | 메타데이터로 안다 |
| 경계만 보면 된다 | 내부 이동이 핵심이다 |
| SaaS 는 사업자 몫이다 | 우리 데이터다 |

## 보완 수단

| 구간 | 수단 |
| --- | --- |
| 서브넷 내부 | 호스트 기반 수집 |
| 컨테이너 간 | 네트워크 정책 로그 |
| SaaS | 제공 로그·프록시 |
| 원격 단말 | 단말 에이전트 |
| 클라우드 API | 감사 로그 |`,

'internal-ca-operations': R`## 실제로 이렇게 터진다

내부 인증서를 손으로 발급하면서 유효 기간을 10년으로 준 사례가 있다. 폐기 체계가 없어 키가 유출돼도 대응할 방법이 없었다.

또 하나는 루트 키를 운영 서버에 둔 경우다. 그 서버가 침해되자 내부 통신 전체를 위조할 수 있었다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 내부용이라 관리가 덜해도 된다 | 전 통신의 신뢰 기반이다 |
| 유효 기간은 길수록 편하다 | 폐기가 어려워진다 |
| 루트로 직접 발급해도 된다 | 노출 기회가 늘어난다 |
| 폐기 목록을 만들면 된다 | 확인하지 않으면 무의미 |

## 운영 기준

| 항목 | 권장 |
| --- | --- |
| 루트 보관 | 오프라인·하드웨어 |
| 중간 인증서 | 발급 담당 |
| 최종 수명 | 짧게, 자동 갱신 |
| 폐기 | 짧은 수명으로 대체 |
| 신뢰 배포 | 관리 도구 |`,

'wireless-network-security': R`## 실제로 이렇게 터진다

사무실 무선 암호를 공유 방식으로 쓴 사례가 있다. 퇴사자와 방문자가 모두 알고 있었고, 몇 년째 바뀌지 않았다. 건물 밖 주차장에서도 접속됐다.

또 하나는 직원이 편의를 위해 개인 공유기를 설치한 경우다. 통제되지 않은 경로가 생겼다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 암호가 있으면 안전하다 | 공유 암호는 퍼진다 |
| 사무실 안에서만 잡힌다 | 밖에서도 잡힌다 |
| 방문자망은 분리돼 있다 | 확인해 봐야 안다 |
| 개인 공유기는 없다 | 스캔해 보면 있다 |

## 점검 항목

| 항목 | 방법 |
| --- | --- |
| 인증 방식 | 개인별인가 |
| 망 분리 | 방문자·업무·기기 |
| 신호 범위 | 건물 밖 확인 |
| 무단 접속점 | 정기 스캔 |
| 접속 기록 | 개인 단위 |`,

'network-change-control': R`## 실제로 이렇게 터진다

장애 대응 중 전체 개방 규칙을 열고 닫지 않은 사례가 있다. 6개월 뒤 외부 점검에서 발견됐고, 그 사이 스캔 트래픽이 계속 들어오고 있었다.

또 하나는 규칙 순서를 바꾸면서 의도와 다른 규칙이 먼저 적용된 경우다.

## 흔한 오해

| 오해 | 실제 |
| --- | --- |
| 임시 규칙은 기억한다 | 남는다 |
| 규칙 추가는 위험이 낮다 | 순서가 바뀐다 |
| 정리는 나중에 한다 | 계속 늘어난다 |
| 콘솔이 빠르다 | 이력이 없다 |

## 임시 규칙 관리

| 항목 | 방법 |
| --- | --- |
| 만료일 | 태그로 기록 |
| 사유 | 태그·티켓 |
| 자동 점검 | 만료 초과 탐지 |
| 알림 | 담당자에게 |
| 정기 정리 | 분기 |`,
}
